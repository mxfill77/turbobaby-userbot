# С 25.09.2026 ЭТОТ ФАЙЛ НИКТО НЕ ЧИТАЕТ. Не удалён намеренно (запрет «ничего не удалять»).
#   • строителей ПК выбирает РЕЕСТР УЧЁТОК вне git — accounts_registry.json (accounts.py); с телефона: «учётки» / «учётка N»;
#   • у RC свой файл вне git — rc_profile_choice.txt (нет файла = ОСНОВНОЙ).
# Причина переезда: этот файл в git, и его правка пачкала дерево — авто-фетч демона вставал. Разбор: docs/artifacts/2026-09-25-UCHETKIODNIMSLOVOM2509.md
# --- прежний текст (до 25.09) ---
# Выбор учётки строителей полосы ПК. Читает profile_choice.py при сборке окружения ребёнка.
# ОТКАТ ОДНОЙ СТРОКОЙ: чтобы вернуть строителей на резервный профиль, замените слово ОСНОВНОЙ ниже на строку D:\claude_profile_2 и сохраните файл — следующий заход подхватит это сам, без перезахода в Windows и без правки реестра.
# Значения: слово ОСНОВНОЙ = ключ CLAUDE_CONFIG_DIR снимается у ребёнка; путь = ставится он;
# пусто/мусор/несуществующий путь = окружение не трогается вовсе и причина уходит в журнал.
# ВНИМАНИЕ (22.09): этим же файлом с 22.09 живёт и RC-канал телефона (rc_supervisor, дети и doctor). Путь D:\claude_profile_2 уведёт устройство с телефона: профиль 2 — другой аккаунт, и `claude rc` там не регистрируется (вывод: висит на вопросе первого запуска). Если файл не разобран, RC всё равно уходит на основной профиль, а демон наследует окружение. RC подхватывает файл при рестарте ветки, а не на лету. Разбор: docs/artifacts/2026-09-22-rc-device-profile2-stall.md
ОСНОВНОЙ
