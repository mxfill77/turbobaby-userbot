# cowork_log — журнал работы Cowork-контура (userbot / клиентский)

Канонический журнал моей (Cowork) работы. Формат: `NOTE` = начал, `DONE` = закончил
(что сделал, commit, что дальше). Время — Пхукет (UTC+7).
Важное дублируется в git-коммиты. Drive `KB_cowork_log` заброшен (нет append → дубли);
штабные `KB_userbot_log*` на Drive — другая роль, их не трогаю.

---

## 2026-06-19

**DONE 18:58 — диагностика состояния userbot/репо (read-only).**
- СОСТОЯНИЕ: pc_agent ЖИВ (PID 2952, старт 19.06 14:35), единственный python-процесс.
  userbot МЁРТВ ~2 дня (PID 35252 в tasklist отсутствует). Репо на main, синхрон с origin.
- КОРНЕВАЯ ПРИЧИНА (2 фактора): (1) userbot = дочерний процесс pc_agent (Popen,
  pc_agent.py:141) — падает агент → падает userbot; перезапущенный агент userbot не
  воскрешал. (2) pc_agent флапает на сетевых (NetworkError: httpx.ReadError),
  add_error_handler отсутствовал → транзиентная ошибка роняла процесс.
- ДЕЙСТВИЯ: снят устаревший userbot.lock (PID 35252, мёртв). Код выгружен штабу. Правок нет.

**DONE 19:10 — закоммичен боевой промпт-парсер (отдельный коммит).**
- collect_booking_prompt.txt: заглушка → боевой промпт (сверен штабом с эталоном
  KB_collect_booking_spec). Добавлен финальный перевод строки (убран «No newline»).
- commit `ace11dc` (main), только этот файл. CRLF-шум (5 файлов) не тронут.
- PUSH выполнен Филиппом с Windows → `ace11dc` на origin.

**NOTE 19:31 — спроектированы 2 фикса pc_agent (отдельный коммит). ЖДУ РЕВЬЮ — не применяю.**
- ФИКС 1: `on_error` + `app.add_error_handler(on_error)` перед run_polling. Ловит
  NetworkError/TimedOut (httpx.ReadError/ReadTimeout PTB заворачивает в NetworkError) →
  WARNING в лог, процесс НЕ падает; PTB сам переподключает long-polling.
- ФИКС 2: авто-реадопшн userbot при старте агента. После захвата лока (acquire_agent_lock
  уже снимает свой устаревший лок) проверяю UB.status() (CIM-поиск userbot_listen.py):
  не жив → UB.start() через VENV_PY (идемпотентно, дубль не создаст); жив → не трогаю.
  Команды стоп/старт из темы 205 не ломаются (проверка разовая, только на старте агента).
  Дефолт: userbot жив всегда.
- Дифф показан Филиппу, синтаксис копии компилируется (py_compile OK). Живой pc_agent.py
  НЕ тронут (агент запущен). СТОП до «да» Филиппа → затем стоп агента по протоколу,
  применение, py_compile, старт через VENV_PY, проверка «ровно 1 процесс», commit.

**DONE 20:1x — фиксы pc_agent применены и ПРОВЕРЕНЫ ВЖИВУЮ. Хрупкость связки вылечена.**
- Применены оба фикса в живой pc_agent.py (стоп агента → правка → старт, по протоколу).
- py_compile через venv-python (Windows): exit=0, файл целый (472 строки), все 3 маркера на месте.
- После старта: ДВА процесса python — pc_agent (PID 19556) + userbot_listen (PID 5308).
- Лог агента подтвердил авто-реадопшн (ФИКС 2): «userbot не запущен при старте — поднимаю
  (авто-реадопшн)» → «userbot запущен агентом PID 5308». Агент САМ поднял userbot.
- on_error зарегистрирован (ФИКС 1): нет «No error handlers are registered».
- commit `6404715` (ace11dc..6404715 на origin), только pc_agent.py (+28), CRLF-мусор не попал.
- Коммит/push делал Филипп с Windows: sandbox-mount показывал оборванный файл (438 строк,
  in-place правка не синхронизировалась) — коммит из песочницы дал бы битьё. Урок на будущее:
  правки существующих файлов через Edit проверять/коммитить с Windows, не из песочницы.
- ОСТАЛОСЬ (отдельным заходом): ФИКС 3 — само-рестарт по команде «обновись». Не начат.

**NOTE 20:4x — ФИКС 3 (само-рестарт «обновись») спроектирован. ЖДУ РЕВЬЮ — не применяю.**
- Новая команда из темы 205 (id 504608015): «обновись»/«перезапустись»/«обнови себя»/restart.
- Поток: ответ «принято» → git pull --ff-only → если ОК: спавн DETACHED-помощника, снять
  лок, os._exit(0); если pull упал: «НЕ перезапускаюсь», остаёмся на текущем коде.
- Анти-«2 агента» / анти-дедлок: помощник (отдельный detached-процесс PowerShell) ЖДЁТ
  смерти нашего PID и ТОЛЬКО ПОСЛЕ этого делает `schtasks /Run /TN pc_agent`. Перекрытия нет
  (новый стартует строго после смерти старого) → нет конфликта getUpdates на токене.
  os._exit гарантирует смерть PID → помощник не повиснет в ожидании.
- singleton-lock НЕ ослаблен; userbot не дублируется (его поднимет авто-реадопшн ФИКС 2).
- Дифф показан Филиппу, py_compile копии OK (545 строк). Живой файл НЕ тронут.
- ОТКРЫТЫЕ РИСКИ для штаба (в ответе расписаны): (1) помощник — единая точка отказа в окне
  «старый умер, новый ещё не стартовал» (ребут/kill помощника → агента поднимет только
  следующий триггер Планировщика); (2) имя задачи TASK_NAME="pc_agent" должно совпадать с
  реальным в Планировщике; (3) git pull --ff-only упрётся, если в дереве есть локальные
  правки тех же файлов (сейчас висит CRLF-шум) — тогда «НЕ перезапускаюсь» (безопасно, но
  обновление не пройдёт; CRLF-шум стоит вычистить отдельно).

**NOTE 21:0x — ФИКС 3 ОДОБРЕН штабом, внесён в живой pc_agent.py (4 якоря). Жду компиляцию Фила.**
- Агент остановлен (PID 19556 завершён, tasklist пуст), правка по протоколу на чистом состоянии.
- Применены 4 правки через Edit. Из песочницы НЕ компилирую (mount-кэш врёт на in-place
  правках) — py_compile делает Фил через venv на Windows.
- Дальше: venv py_compile (exit=0, строк ~540, 4 маркера) → старт → проверка → коммит с Windows.
  TASK_NAME подтвердить = реальному имени задачи в Планировщике перед боевым тестом «обновись».

**DONE 21:xx — ФИКС 3 применён, протестирован вживую и закоммичен.**
- py_compile (venv, Windows) OK, 4 маркера на месте. Агент стартовал, проверка чистая.
- БОЕВОЙ тест команды «обновись» из 205: работает — агент делает git pull, перезапускается
  через помощника/Планировщик, новый экземпляр поднимается, userbot не задвоился.
- ОГОВОРКА (известное поведение): при обрыве сети возможна задержка ~5 мин до полного
  возврата агента в строй. Записано как наблюдение; при необходимости разобрать отдельно.
- commit `1ed503d` (pc_agent.py). Коммит/push — Филипп с Windows.

**РЕШЕНИЕ по журналу (финал):** ведём cowork_log.md ЛОКАЛЬНО + git. Drive KB_cowork_log
заброшен (коннектор умеет только create, без update/delete by id — петля через Drive
невозможна, у штаба тот же коннектор). Петля со штабом — через Филиппа (копирует отчёт/дифф
в чат штаба вручную, как весь день). Автопетля Brain↔штаб — позже через Bridge (П2 в
KB_ROADMAP_MASTER, Claude Code на VPS), отдельным заходом.

**ИТОГ дня:** хрупкость связки pc_agent↔userbot вылечена полностью. ФИКС 1 (error-handler
на сетевые) + ФИКС 2 (авто-реадопшн userbot при старте) = commit 6404715. ФИКС 3 (само-рестарт
«обновись» с телефона) = commit 1ed503d. Промпт-парсер сборщика = commit ace11dc. Все на origin.
Открытые хвосты: CRLF-шум (5 файлов, нормализовать переводы строк); ~5-мин задержка ФИКС 3 при
сетевом обрыве (наблюдение); intake-тест Splinter (исходный п.1 дорожной карты) — не начат.

**DONE <дата> — ЭТАП 2 сборщика: отправка карточки во Входящие.**
- Splinter intake проверен Claude Code — ПРИНИМАЕТ карточку (шлюз открыт).
- collect_booking.py: добавлена отправка карточки в группу Входящие (INBOX_GROUP_ID
  = -1003997419806) как USER через Telethon, ТОЛЬКО по флагу --send. Гарды: адресат строго
  константа группы (не entity клиента, zero-out сохранён), отправка только при старте с
  «🆕 БРОНЬ». add_username → строка «Клиент: <имя> (@username)».
- collect_booking_prompt.txt: карточка выровнена под спеку Splinter (Клиент/Модель/Даты
  ДД.ММ.ГГГГ/Контакт/Доставка/Шлемы/Депозит/Опыт). Логика опыта (ok/review/unknown) и тарифы
  доставки — НЕ тронуты. Добавлено поле deposit.
- Одобрено штабом. commit `f004ce7` (e6c9ef9..f004ce7 на origin), оба файла одним коммитом.
- ДАЛЬШЕ: живой end-to-end тест по команде Фила — стоп userbot (общая сессия) →
  collect_booking "@диалог" --send → карточка во Входящие → Splinter резюме → старт userbot.
- Открытое: passport-photo отдельным сообщением (этап 2b); запуск с телефона через pc_agent
  (вариант B) — позже; CRLF-шум — позже.
