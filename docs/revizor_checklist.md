<!--
# Чек-лист классов думателя-ревизора — ЖИВОЙ источник правды

pc_orchestrator._revizor_checklist() читает ЭТОТ файл на КАЖДОМ тике ревизора и вклеивает
его тело (этот HTML-комментарий вырезается — он для человека, не для думателя) в преамбулу
думателя между ролью и контрактом вывода. Файла нет / пуст / ошибка чтения → встроенный
дефолт REVIZOR_CHECKLIST_DEFAULT в pc_orchestrator.py (fail-safe: ревизор не падает).

Механика «урок навсегда»: добавить новый класс дефекта = дописать ОДНУ строку
«- [класс X] описание» (буква X попадёт в поле "class" находки; описание человекочитаемо).
Пункт окажется в преамбуле СЛЕДУЮЩЕГО прогона без правки кода и рестарта демона.

Абзац «ВАЖНО об АВТОПРИВЕТСТВИИ» — часть чек-листа (правило автогритинга), формулировки
без нужды не менять. Держи дефолт в pc_orchestrator.py синхронным с телом этого файла.
КРАСНОЕ: Bridge / таблицы / деньги этого файла НЕ касаются — только текст чек-листа.

═══ ПОПРАВКИ СВЕРКИ 31.07.2026 (читать до того, как дописывать класс) ═══

Текст чек-листа ниже — единственный полностью актуальный документ репозитория, и это
проверено побайтово. Неточны не пункты, а ДВА ОБЕЩАНИЯ ЭТОЙ ШАПКИ о поведении кода:

1. «БЕЗ ПРАВКИ КОДА» — верно только для преамбулы думателя, но НЕ для гейта.
   Автописатель уроков (lesson_router.py → append_checklist_class) правит ТОЛЬКО этот .md
   и НЕ трогает REVIZOR_CHECKLIST_DEFAULT в pc_orchestrator.py. Инвариант гейта
   (test_pc_orchestrator.py → test_shipped_file_matches_builtin_default) сверяет тело файла
   с этим дефолтом ⇒ следующий прогон гейта КРАСНЕЕТ.
   Ложный диагноз: «гейт упал — сломали ревизора». На деле сработал штатный «урок навсегда»,
   а правка кода всё-таки обязательна: синхронизировать дефолт руками.

2. «БУКВА X ПОПАДЁТ В ПОЛЕ class» — не для любой буквы. Контракт вывода в той же преамбуле
   (pc_orchestrator.py, строка контракта '[{"class":"а".."ж",…') жёстко ограничивает алфавит
   буквами а..ж и добавляет «Не выдумывай находок сверх чек-листа», а
   lesson_router._next_class_letter выдаст следующей букву «з».
   Ложный диагноз: «дописали класс з, ревизор его не находит ⇒ файл не читается». Читается;
   буква запрещена соседним абзацем того же промпта. Новый класс сверх «ж» = правка контракта.

3. Файл не только ЧИТАЕТСЯ кодом, но и ПИШЕТСЯ им (lesson_router.append_checklist_class) и
   коммитится демоном. Неожиданный diff здесь — не обязательно чужая ручная правка.

4. Сверка дефолта с телом файла идёт через нормализацию пробелов, поэтому АБЗАЦНАЯ РАЗМЕТКА
   инвариантом не защищена (живая преамбула и fail-safe уже разошлись на две пустые строки —
   гейт этого не видит).

Примечание о безопасности этой вставки: она внутри HTML-комментария, а _revizor_checklist()
вырезает комментарии ДО сравнения с дефолтом и ДО склейки промпта. Значит текст выше не
попадает ни к думателю, ни в гейт — гейт от него не краснеет.
-->

ВАЖНО об АВТОПРИВЕТСТВИИ: первое «от нас» сообщение в окне может быть АВТОПРИВЕТСТВИЕМ Telegram Business — статичным текстом настроек аккаунта (шлётся мгновенно при первом контакте / раз в 14 дней, МИМО бота и модерации; бот его НЕ сочинял). Если в пакете есть секция «АВТОПРИВЕТСТВИЕ TELEGRAM BUSINESS»: (1) на его СОДЕРЖАНИЕ находки не заводи никогда — это не продукт бота; (2) анкету/вопросы ИЗ автоприветствия не считай классом ж; (3) если бот дальше здоровается ПОВТОРНО — это находка класса е.

Найди нарушения РОВНО из этого чек-листа (класс = буква):

- [класс а] числа не из quote — цена/депозит/скидка/срок в отправленном, которых НЕТ во входных данных (похоже на выдуманную цифру, а не взятую из тарифа);
- [класс б] чужая модель — в ответе фигурирует модель/марка техники, которую клиент НЕ спрашивал и о которой мы речи не вели (подмена модели);
- [класс в] переспрос данного — мы переспрашиваем то, что клиент УЖЕ явно сообщил в этом окне (модель, даты, срок, опыт и т.п.);
- [класс г] утечка служебного — в ОТПРАВЛЕННОМ клиенту тексте просочились внутренние маркеры: «собрано», «[уточнить», строка сезонной пометки — то, что клиент видеть не должен;
- [класс д] ложная ✅ трекера — галочка ✅ у пункта, который на самом деле НЕ собран/не подтверждён (трекер врёт, что данные есть);
- [класс е] повтор приветствия/канцелярит — повторное приветствие в НЕ первом ответе окна, ЛИБО приветствие бота ПОСЛЕ автоприветствия Telegram Business (оно уже поздоровалось — второе «Здравствуйте» лишнее), либо канцелярский официоз вместо живой речи;
- [класс ж] анкета на первом сообщении — клиент ПЕРВЫМ же СОДЕРЖАТЕЛЬНЫМ сообщением дал модель И даты, а БОТ в ОТВЕТ шлёт анкету/список вопросов вместо тарифа (надо было котировать, а не допрашивать). НЕ путай с автоприветствием: если «анкета» — это САМ статичный автогритинг Telegram Business (секция выше), а не ответ бота, находку НЕ заводи (action=noise).
