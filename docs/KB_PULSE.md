# KB_PULSE — пульс деплоев/вех ПК-контура

Файл создан 2026-07-14 (задача «деплой 334»): в репо/ПК файла KB_PULSE не существовало —
заведён здесь как журнал вех. Если канонический KB_PULSE живёт во внешней базе (VPS/manager-bot),
строки ниже готовы для переноса дословно.

> ## ⚠️ Как читать этот файл (сверка 31.07.2026)
>
> **1. Автоматики у ЭТОГО файла нет — он ведётся руками.** Нормативный ПК-код `docs/KB_PULSE.md`
> не читает и не пишет: в корне репо `grep KB_PULSE *.py` → 0. Вывод «пульс не обновлялся ⇒
> сломалась автоматика пульса» **ложен: её здесь никогда не было**.
> Не путать с **Brain-доком** `KB_PULSE` / `pulse` — это другая сущность, у неё писатели есть
> (`tmp/kb_pulse_update.py`, `tmp/write_brain.py` через `brain_writer`; на VPS — `cclog … --pulse`,
> который перезаписывает док целиком). «Пульс обновлён» про док мозга и «файл не менялся» про
> этот `.md` — совместимы и ничего друг о друге не говорят.
>
> **2. Файл заморожен.** Последняя запись — `## 2026-07-24`; после неё в профильный код
> (`pc_orchestrator`+`suggest`+`pretool_guard`) прошло **39 коммитов** (замер 31.07). «Вех не
> было» — их было 39; пробел в файле не равен покою контура.
>
> **3. Числа-якоря протухли, и свежее число тоже протухнет — сверяй КОМАНДОЙ, не числом.**
> «полный гейт 1754 OK» (`## 2026-07-23`) и «531/531» (`## 2026-07-14`) сегодня меньше одного
> файла: `test_pc_orchestrator.py` в одиночку несёт **555**. Всего `def test_` во всех
> `test_*.py` — **2418**, и это уже третий замер за сутки (сверка-1: 2338, сверка-2: 2349,
> починка утром 31.07: 2398). Якорь-число живёт часы, поэтому мерить так:
> ```
> grep -cE "^\s*def test_" test_*.py | awk -F: '{s+=$2} END {print s}'
> ```
> а «гейт зелёный» подтверждать ПРОГОНОМ гейта, а не совпадением числа.
>
> **4. Записи `## 2026-07-24` описывают гард НА VPS**, а не ПК-гард: это разные файлы с разной
> длиной и разным набором фич. Читать их как описание `D:\turbobaby-bot\pretool_guard.py` —
> тот самый класс «чинишь не тот гард».
>
> **5. PID-снимки (1764, 4724) — фото 14.07**, сверять с ними живые процессы бессмысленно.
>
> **6. Среды, которых отсюда не достать:** `/tmp/guard_backup_scanedit_2407`, `stash@{1}`
> (vps-local-24.07) — они на VPS-машине; в ПК-дереве их нет и воспроизвести нечем.

## 2026-07-24
- **Гард manager-bot (VPS) — упрочнение скан-пути, ЖИВЬЁМ в working tree** (НЕ коммит / НЕ пуш /
  НЕ рестарт — хук читается при каждом вызове, правка активна сразу). Канал: SSH ПК→VPS
  (`-i ~/.ssh/turbobaby_vps`, root@5.223.94.179). Две правки, обе взяты из `stash@{1}`
  (vps-local-24.07), НЕ переписаны заново:
  1. **fail-safe стрип в `_scan`** — `_strip_script_cli_args` теперь оборачивает оба возврата на
     пути разбора-сбоя (~строки 713/715), т.е. argv-скрипта = данные даже когда цепочка не
     распарсилась.
  2. **доверие по происхождению в шаге-2 скана тела** — новый `_repo_tracked()` (git
     `ls-files --error-unmatch`): git-отслеживаемые файлы репо телом НЕ сканируются; `_`-префиксные
     черновики, скретчпад и файлы вне репо сканируются как раньше; fail-safe → скан. `+import
     subprocess`. Файл 907→930 строк, компилируется; бэкап `/tmp/guard_backup_scanedit_2407`
     (sha256 6a815f5f).
- Тесты штатным раннером (pytest на VPS нет — скриптами): **guard 7 зелёных / 1 красный**
  (`entity` — недописанная фича [**поправка 31.07:** дописана, но ТОЛЬКО на VPS — `_ENTITY_*`
  живут там, в ПК-гарде их 0. Это не «фича не дописана», а **зеркальная дыра**: класс-фикс
  приземлён на одной полосе, свод правил п. 9], красный БЫЛ до правки), **pretool 3 зелёных / 1 красный**
  (`commit_msg` падает на символе, отсутствующем в origin-линии — красный БЫЛ до правки, доказано
  `grep=0` и в бэкапе). Правка регрессий НЕ внесла: все 24 скан-подтеста commit_msg зелёные.
- Живые проверки гардом-подпроцессом: (а) журнальная строка с красными словами в аргументе →
  **DEFER без карточки**; (б) обращение к файлу секретов → **deny** (жёсткий блок цел);
  (в) вне-репо скрипт с красным телом → всё ещё **ask**. Развилка entity/линий (порт ~25–30 строк
  vs ретайр vs union stash@{1}) — по-прежнему у владельца.

  > **‼️ ПОПРАВКА 31.07 к пункту (б) — на ПК жёсткого блока секретов НЕ существует.**
  > Замер выше сделан на **VPS-гарде**; для ПК он неверен, и это самый дорогой ложный диагноз
  > файла («секреты защищены слоем настроек» → полезут проверять не тот слой):
  > - в `pretool_guard.py` (ПК) слова `deny` **0 вхождений**; `decide()` возвращает только
  >   `defer` / `ask` / `approved` — вердикта «запретить» у него в принципе нет;
  > - секреты стоят в `.claude/settings.json` в списке **`ask`**, не в `deny`; сам список
  >   `deny` секретов не содержит;
  > - сверх того `pretool_guard.py` → `owner_approved_kinds()` **снимает `ask`** по классу,
  >   на который владелец уже сказал «да» в этом прогоне.
  >
  > Два слоя разрешений при этом расходятся на одном и том же файле и одном одобрении:
  > сверка-2 (§1 V14) поймала прогон, где `Read` файла секретов был закрыт слоем настроек,
  > а чтение того же файла через PowerShell прошло. Слой настроек про одобренный класс не знает,
  > гард — знает.
  >
  > Ещё один живой пример расхождения «слово ≠ действие», пойман в этой же сверке 31.07:
  > `grep` по **`.claude/settings.json`** поднял карточку «обращение к секретам» только потому,
  > что подстрока стояла в тексте grep-**паттерна**. Объект чтения был другим файлом.
  > Карточка обязана следовать из ДЕЙСТВИЯ, а не из подстроки (свод правил, п. 5).

## 2026-07-23
- Развилка смоука #274 решена владельцем: **вариант Б** — строку J и строку доставки
  в клиентском черновике собирает КОД из листа (маркерный [QUOTE]-режим, как
  [PRICE_SHEET]); LLM цифр не видит → #92 «дословно» по построению, дедуп #365 снят.
  Смоук `run_chain_smoke.py` PASS 2/2, флип тестов #365; артефакт:
  docs/artifacts/2026-07-23-smoke-verbatim-quote-variant-b.md.
- Тренажёр v2 принят (c28f73d: KB_trainer_log включён, смок пройден); цепь #274 guard-model
  в работе — шаги 1–5/7 в main (голдены окна 7562315636 сквозным прогоном, 970cf6c).
- Цепь #274 доделана Dispatch: шаг 6 — прод на 758f1de (реконсиляция 13:17, оба бота),
  окно закрыто голденами, полный гейт 1754 OK; шаг 7 — ПЕРВЫЙ живой прогон смоука цепей:
  failed (LLM перефразирует строку J/доставки, дедуп #365 маскирует — конфликт с
  инвариантом #92 «дословно», НЕ регрессия #274). Развилка А/Б/В/Г у владельца:
  docs/artifacts/2026-07-23-smoke-verbatim-quote.md.

## 2026-07-14
- **Деплой #334 выполнен** (шаг 6/6, одобрен владельцем): lesson_router b088eb1→0b83582 в
  проде — демон-дирижёр PID 1764, commit a585e33 (веха-эстафета). **LLM-маршрут уроков: ВКЛ**
  (LESSON_LLM_ROUTE=1 в .env постоянно; fail-safe при сбое думателя = keyword-путь; полный
  откат = флаг off + рестарт). Модербот lesson_router не грузит — рестарт не требовался
  (PID 4724 не тронут). Смоук кода: classify_lesson_llm / resume_low_lesson импортируются,
  _lesson_llm_enabled()=True, PEB живого демона несёт LESSON_LLM_ROUTE='1'.
  **Смоук живого цикла: ОЖИДАЕТ реплай владельца** («правка: …» на карточку msg 336
  в «Модерации ответов»; ждём «Понял так… Делаю…»).
- Попутно закрыт блокер-класс: боевой .env-рубильник тёк в гейт-тесты (13 красных, реальный
  думатель из гейта, 350с) — env-гвоздь LESSON_LLM_ROUTE=0 в test_isolation +
  test_pc_orchestrator/test_pc_local_dec; гейт против боевого .env: 531/531 за 3.1с.
