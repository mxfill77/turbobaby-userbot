# Остаток имён мозга: KB_MASTER починен, регресс 22/27 → 26/27

**Дата:** 01.08.2026, репо `D:\turbobaby-bot`, ветка `main`. Основание —
[`2026-08-01-brain-name-translator.md`](2026-08-01-brain-name-translator.md) §3
(там же список из 4 имён с адресами и лечением).

**Режим.** `.env` не открывался; секреты берёт САМ `brain_writer` (класс 328). Drive-коннектор
(красный) не трогался — всё через живой Bridge. Содержимое доков не менялось ни разу: только
реестр (`register_brain_doc`) и чтения. Ключей не снимал, ничего не удалял, архив не двигал.
Временное — в `tmp/names_rest_20260801_166/` (каталог под `.gitignore`).

**Ответ моста фактом не считал** (класс `7a65fa3`): каждая регистрация проверена ОБРАТНЫМ
ЧТЕНИЕМ — `--list-brain` + чтение дока ПО ИМЕНИ, а не по `ok:true` в расписке.

---

## 1. РЕГРЕСС — ЧИСЛОМ

Тот же скрипт замера (`regress_names.py`, скопирован ДОСЛОВНО, списки имён не тронуты), один
прогон, один живой мост, обе полосы:

| полоса | 14 проб имён | 13 указателей §5 | **всего 27 обращений** |
|---|---|---|---|
| **СЫРО** (мост как есть) | 1/14 | 2/13 | **3/27** |
| **ЧЕРЕЗ ПЕРЕВОДЧИК** | 13/14 | 13/13 | **26/27** |

**Было 22 из 27 → стало 26 из 27.** Закрыты ровно 4 обращения: `KB_MASTER`, `KB_trainer_log`
(оба места), `roadmap_master`. Осталось одно — `roadmap_v2` (§4, развилка владельцу).

*Оговорка по сырой полосе:* контрольная тройка регистрозависимости в этом прогоне дала
`infra → HTTP 404` на всех трёх попытках — это флап моста, а не смена поведения: в том же
прогоне `KB_INFRA` через переводчик прочитан (6 702 симв.), а `INFRA`/`Infra` дали честный
`unknown_name`. Поиск ключа на мосту по-прежнему точный и регистрозависимый.

---

## 2. KB_MASTER — ПОЧИНЕН (приоритет задачи)

Имя и ключ разошлись: титул карты Штаба — `KB_MASTER`, ключ у неё `index`. Заведён **ВТОРОЙ
ключ на тот же файл**, а не переезд: `index` цел, ничего не переименовано.

```
brain_writer.py --register master=1-bH3b6c_oamqST551iLxn-voSDragdV0rUZkFwgmAqc
→ {"ok": true, "registered": true, "name": "master", "total": 31}
```

**ПОПРАВКА 22.08.2026: число в команде выше МЁРТВО, команду дословно НЕ повторять.** 22.08 узел
карты мигрирован из Google-Doc в плоский файл, и этот id принадлежит теперь ЗАМОРОЖЕННОЙ КОПИИ
(`KB_MASTER_OLD`, переименованный бэкап, не удалён). Повтор строки вернул бы ключ `master` на
мёрзлое — молча, с зелёным ответом. Живой адрес карты живёт ТОЛЬКО в реестре моста
(`--list-brain`); ключей на неё два: `index` и `master`. Разбор:
[`2026-08-22-map-keys-split.md`](2026-08-22-map-keys-split.md).

**Проверка обратным чтением (расписке не верим):**

| проба | ответ |
|---|---|
| `--list-brain` | `"index"` и `"master"` — ОДИН и тот же id `1-bH3b6c…`; ключей 31 |
| `--resolve KB_MASTER` | `KB_MASTER → name:master [канон master]` (было: «ключа нет, ключей 29») |
| `--probe --name KB_MASTER` | `ok: 92827 символов, строк 973, U+FFFD 0` |
| `--probe --id 1-bH3b6c…` | `ok: 92827 символов, строк 973` — **та же голова, тот же объём** |

Точка входа номер один зовётся по имени и открывает карту. Строки данных в коде не появилось:
имя ловит общее правило канона (`KB_MASTER` → `kb_master` → `master`).

**Два ключа на один файл — штатный вид реестра, а не хак:** `faq` и `turbobaby_faq` держат один
id изначально. Следствие для описей: **считать доки по числу ключей нельзя** — 31 ключ, 29
файлов.

---

## 3. KB_trainer_log — ПОЧИНЕН (оба обращения)

```
brain_writer.py --register trainer_log=13kp-54bzN2Xua8R5VN4EaXR7IElTKg4O
→ {"ok": true, "registered": true, "total": 32}
--probe --name KB_trainer_log → ok: 2190 символов, строк 10, U+FFFD 0
   «TRN LOG v1 | Полный лог тренажёра «Тренеровка» (клиентский контур)…»
```

Id взят из боевого сайдкара `trainer_log_doc.json`; сам сайдкар и `trainer_log.py` не тронуты —
канал тренажёра как ходил по id, так и ходит, ключ добавлен рядом.

---

## 4. roadmap_v2 — ДОКУМЕНТ ЖИВ, но ссылку закрыть НЕЧЕМ БЕЗ РЕШЕНИЯ ВЛАДЕЛЬЦА

**Жив, факт (read-only проба по id, содержимое не менялось):**

```
--probe --id 1ekCOwspbXhJP7F2eVr3tKuwdLJ9ke9IH → ok: 8745 символов, строк 91, U+FFFD 0
   «KB_ROADMAP_v2 — ГРАМОТНЫЙ СВОД ПЛАНА TurboBaby. Срез 26.06.2026 (пересобран штабом
    с Филиппом, заменяет устаревший ROADMAP_MASTER от 19.06)»
```

**Регистрация ОТКАЗАНА мостом — и это факт, а не догадка:**

```
--register roadmap_v2=1ekCOws… → {"ok": false, "error": "not_in_brain", "title": "KB_ROADMAP_v2"}
```

Файл лежит **вне папки Brain**, в архивной подпапке (совпадает с описью 29.07). `register_brain_doc`
ключ такому файлу не даёт. Лечение существует ровно одно — `move_into_brain`, то есть
**ПЕРЕМЕЩЕНИЕ архивного файла**; в этой задаче архивы трогать запрещено, и это в любом случае
решение владельца, а не побочный шаг.

> **РАЗВИЛКА ВЛАДЕЛЬЦУ.** (а) `move_into_brain 1ekCOws…` + `--register roadmap_v2=…` — имя
> заработает, но живой док уедет из архива в корень Brain; (б) оставить как есть — док
> адресуется только по file id, регресс останется 26/27. Побочный факт: в карте (`index`)
> имён `roadmap_v2`/`KB_ROADMAP_v2` **ноль** — на него оттуда не ссылаются вообще.

---

## 5. roadmap_master — МЁРТВ, подтверждено фактом; кто ещё ссылается

**Мёртв по пробе, а не по описи:**

```
--probe --id 1Z70EpgGZmaYMaZ064sXQlCCP8z8sFyZWfzVPvRB4jWE
→ ok:false, error "read_failed": «не найден элемент по указанному коду» (ответ Apps Script)
```

Ничего не удалял и ключей не снимал: ключа `roadmap_master` в реестре нет с 31.07, файла в
Drive нет с 27.06.

**Откуда на него ещё ссылаются** (замер этого захода):

| место | что там | живая ли ссылка |
|---|---|---|
| карта `index`, строки 52, 820, 885 | 3 упоминания, все — **надгробие** («мёртвый roadmap_master снят», «удалён из Drive 27.06, ключ мёртв») | нет, протокол |
| `cowork_log.md`:89 | историческая проза журнала | нет |
| `tmp/brain_*.py` (4 файла) | черновые скрипты прошлых заходов, вне git (`tmp/` в `.gitignore`) | нет |
| VPS `registry_check.py` → `KB_ALIAS` | `roadmap_master`→`roadmap_master`, `north_star`/`roadmap_v2`→`roadmap_master` | **дефект, чужая полоса** — по описи [`2026-08-01-brain-inventory-recount.md`](2026-08-01-brain-inventory-recount.md) §152, **этим заходом НЕ переизмерялся** (на сервер не ходил) |

**СЛЕДСТВИЕ РЕГИСТРАЦИИ `master`, названное вслух.** Мёртвое имя `roadmap_master` теперь
**резолвится** — по границе токена (`roadmap_master`.endswith(`_master`)) оно совпадает с новым
ключом и ведёт в карту, то есть **ровно в своего преемника**: содержимое удалённого файла
перенесено в `KB_MASTER` 27.06. Замер: `roadmap_master → 92 827 симв.` (карта).

Это принято сознательно: избежать было нельзя (канон `KB_MASTER` = `master`, ключ обязан
называться так), а закрыть отдельно — только таблицей мёртвых имён в коде, то есть вторым
реестром, который мандат прошлого захода снял. Следствие закреплено юнитом
`test_dead_name_lands_on_its_successor_and_this_is_deliberate`, чтобы оставаться решением, а не
сюрпризом. Практический риск сегодня нулевой: боевого кода, который пишет по имени
`roadmap_master`, в репо нет (проверено грепом).

---

## 6. ЧТО ЛЕГЛО В КОД

Правило разрешения имён **не менялось ни на строку** — чинилось ДАННЫМИ в реестре, как и
задумано. В коде только правда о новом состоянии:

- `test_brain_writer.py` → `LIVE_KEYS` пересобран с живого `--list-brain` (31 ключ), заведён
  `SAME_FILE` — мок теперь повторяет живой формат «два ключа на один файл» (`master`/`index`,
  `turbobaby_faq`/`faq`). Без этого тест «KB_MASTER открывает ТУ ЖЕ карту» сравнивал бы два
  разных фейковых дока и был бы зелёным при любом исходе;
- голден `test_addresses_that_need_data_refuse_honestly` **сокращён до правды**: держал 4 имени
  в состоянии «честный отказ», осталось одно (`roadmap_v2`) — остальные три закрыты и получили
  свои тесты (`…kb_master_opens_the_same_file_as_index`, `…sidecar_doc_is_caught_by_the_rule…`,
  `…dead_name_lands_on_its_successor…`);
- `test_unresolvable_name_never_reaches_write` перенаведён на имена, которые отказывают
  **сегодня** (три разные причины: нет ключа / голая подстрока / неоднозначность) — иначе он
  проверял бы запись по имени, которое теперь ведёт в карту;
- `brain_writer.py` — комментарий-класс рядом с правилом: чем закрыты титулы, что **регистрация
  не достаёт файл вне папки Brain** (`not_in_brain`, живой замер) и что ключ `master` подбирает
  мёртвое имя.

**Тесты `test_brain_writer`: 58, OK. Полный гейт: `python -m unittest discover -s . -p "test_*.py"`
→ 2 760 тестов, OK (skipped=10), 206.2 с.**

---

## ИТОГ

1. **`KB_MASTER` находится по имени** — второй ключ `master` на файл карты, проверено обратным
   чтением (92 827 симв. по имени = по id). Точка входа номер один открывается.
2. **`KB_trainer_log`** закрыт тем же способом (оба обращения), **регресс 22/27 → 26/27**.
3. **`roadmap_v2` жив** (8 745 симв.), но ключ ему мост не даёт: `not_in_brain` — лежит в архиве.
   Развилка владельцу в §4, сам ничего не двигал.
4. **`roadmap_master` мёртв фактом** (id не читается), ссылки на него — только надгробия;
   побочно он теперь ведёт в карту-преемника, следствие названо и закреплено тестом.
5. Дерево: правки закоммичены, временное — в `tmp/` (вне git).
