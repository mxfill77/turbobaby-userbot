# 10 мест корня А: контракт или совпадение (разметка для решения владельца)

**Дата:** 2026-08-02. **Тип: только чтение.** Код не менялся, коммитов нет, ничего не удалялось,
`.env` не открывался, на сервер не ходили, процессы не трогали, боевых записей нет. Созданы два
объекта: этот артефакт и строка журнала.

**Основание:** [`2026-08-01-root-a-permission-split.md`](2026-08-01-root-a-permission-split.md) §3 —
десять мест группы «разрешения», где поиск по тексту объявлен работающим КАК ЗАДУМАНО и решено их
не трогать. Возражение внешнего оппонента: «работает как задумано» — критерий сегодняшнего
ПОВЕДЕНИЯ, а не устойчивого КОНТРАКТА.

**Критерий разметки (задан владельцем):**
* **контракт** — читается текст, который пишет НАШ СОБСТВЕННЫЙ код в известном формате;
* **совпадение** — читается текст модели, сообщение человека, вывод чужого инструмента, чужой лог:
  сломается при первой смене формулировки.

Два места не легли ни в один полюс честно, поэтому добавлены две промежуточные метки, и они
названы явно, а не спрятаны в «контракт»:
* **полуконтракт** — канонический (структурный) разбор стоит ПЕРВЫМ, поиск по тексту работает
  только как фолбэк на его пустоте;
* **чужой конфиг** — формат не наш, но и не проза: стандартизованный конфиг внешнего инструмента.

**Источники:** живое дерево `D:\turbobaby-bot` (HEAD `d9ff8e9`, гард 3742 строки); клон VPS
`tmp/srv-audit-2` (снимок `main` `7298787` от 01.08 03:26 — непушенные ветки и рабочее дерево
сервера в нём не видны). Мост не опрашивался: всё ниже получено чтением кода.

---

## §1. Таблица: совпадения, решающие про разрешения — ПЕРВЫМИ

Все десять мест взяты из группы «про разрешения» (34 из 78), поэтому вопрос «решает ли место про
разрешения» отвечается для каждого «да», и сортировка идёт по ОПАСНОСТИ = чужеродность текста ×
направление поломки. Направление названо отдельной колонкой: место, ломающееся В СТОРОНУ ВОПРОСА,
дешевле места, ломающегося в сторону пропуска или молчания.

| # | место (файл:строка, символ) | что ищется | чей это текст | вердикт | что при смене формата | направление |
|---|---|---|---|---|---|---|
| **1** | `pretool_guard.py:1965` — карве-аут ветки SQL в `_scan_python` | подстрока `memory.db` где угодно в blob (скан команды + ТЕЛО скрипта) | **модели**: тело неотслеживаемого не-теста `.py`, которое пишет исполнитель | **совпадение** (сам детектор `_RE_SQL_WRITE` — полуконтракт: SQL это формальная грамматика) | слово в комментарии, докстринге или цитате снимает красное со ВСЕХ SQL-записей скрипта разом. **Хуже: на ПК базы `memory.db` НЕТ ВООБЩЕ** — живая локальная БД называется `moderation_ipc.db` (`ls *.db`). Слово принесено доктриной VPS и референта на этой полосе не имеет: законно карве-аут не сработает никогда, незаконно — с одного упоминания | **ОТКРЫВАЕТ** (`ask sqlite` → `defer`, карточки нет) |
| **2** | `orchestrator_daemon.py:1149` `_is_headless_impossible` (вызовы `:3427`, `:1599`) | 15 ключевых слов (`clasp`, `sqlite3`, 8 имён боевых операций, `confirmed=true`, `DOWRITE`, домены Google) | **модели И владельца сразу**: `what` — строка `op=… \| …`, которую печатает исполнитель; `orig` — ТЗ владельца ДОСЛОВНО; в `:1599` — шаги плана, проза планировщика | **совпадение**, самое чистое из десяти | модель перефразирует («деплой Apps Script» вместо `clasp`) → задача уходит в конверт и упирается в то же красное, то есть петля, ради разрыва которой ветка заведена. Обратно: слово в прозе ТЗ владельца («`clasp` не трогай») ПОСЛЕ его «да» закрывает задачу терминальной картой «сделай руками», не попробовав | **СЪЕДАЕТ «ДА»** (решает, что означает нажатая кнопка) |
| **3** | `pc_orchestrator.py:1288` `_owner_reply` → `:1303` `_approved_scope` | восемь имён полей ряда очереди (`owner_reply`, `reply`, `answer`, `approval_text`, `approved_text`, `decision_text`, `comment`, `note`), затем объект внутри свободного ответа | **чужой схемы + человека**: имена полей — схема МОСТА (Apps Script), значение — свободный текст ВЛАДЕЛЬЦА. Карточная половина (`object_from_card` по строке `Объект:`) — наш текст и якорный читатель | **совпадение** по обоим слоям | формат менять нечему: **проверено чтением обеих полос** — ни одно из 8 имён не встречается НИ В ОДНОМ файле обоих репозиториев, а devbot на «да» шлёт `approve_task(qid, "Filipp")` (`devbot.py:384`), то есть названный объект сверяется на VPS и ВЫБРАСЫВАЕТСЯ, в ряд не едет. Значит `PRETOOL_APPROVED_OBJECT` не ставится никогда, и высший вид на ПК не проходит НИКОГДА | **ЗАКРЫТО НАГЛУХО** (fail-closed, но невидимо: выглядит настроенным) |
| **4** | `pretool_guard.py:488` `_detail_parts` (VPS) | кошелёк, сумма, байк, пробег, имя дока — регексами `ключ=значение` по blob | **модели**: аргументов вызова Bridge гард не видит вовсе (они вычисляются в рантайме), читается ИСХОДНЫЙ ТЕКСТ | **совпадение**; канона нет by design | переименован именованный аргумент или значение — переменная (`amount=total`) → объект пуст → `card_gate` False → **карточки владельцу нет, строка уходит в журнал**. Исключение только деньги: `_ALWAYS_CARD` = ровно два (`void_last`, `add_transaction`) — там карточка родится, но с «Объект: —» | **ПРЯЧЕТ** карточку от владельца |
| **5** | `pretool_guard.py:1312` `_analyze`, `if tok in scan` (VPS) | 14 подстрок `RED_TOKENS`: 8 имён боевых операций + `DOWRITE` + **5 ручных написаний одного факта** `confirmed=true` | **модели**: `tool_input.command` из stdin хука (`:1384`) + тела чужих `.py` | **совпадение**; ищутся НАШИ имена в ЧУЖОМ тексте — ломается с двух сторон | (а) переименуем операцию в `bridge_client` — детектор ослепнет МОЛЧА; (б) `confirmed  =  True` в два пробела или `"confirmed" : true` — мимо всех пяти написаний; (в) имя в комментарии — ложное красное И ложный КЛАСС карточки (`RED_TOKEN_HIT` задаёт вид) | двусторонне: слепота ↔ ложное красное |
| **6** | `pretool_guard.py:2919` `_is_mass_delete`, фолбэк | хвост после `rm`/`del`: маска `*`/`?`, флаг рекурсии, счёт токенов-целей | **модели**: строка шелла | **полуконтракт** — структурный `_delete_scan` (shlex + сегменты) стоит ПЕРВЫМ, подстрочный счёт включается только на его пустоте | непонятая форма удаления (`Remove-Item -LiteralPath`, `find … -delete`) → счёт токенов краснит лишнее; ослабить признак может только правка кода, а не смена чужого формата | **СТРОЖЕ** (сознательный fail-safe) |
| **7** | `pretool_guard.py:1073` `_py_write_call` | 13 наших имён (8 Bridge + 5 файловых); подстрока — предфильтр, вердикт — 4 формы вызова | **модели**: тело скрипта | **совпадение** по входу; искомое — наш API + грамматика питона, канона (структурного поля) не существует, нужен AST | алиас импорта (`from bridge import create_booking as cb; cb(...)`) или разрезанный литерал — мимо; переименование нашей операции — слепота. Форма `def create_booking(` намеренно не считается вызовом | **СТРОЖЕ**, но с известной дырой |
| **8** | `pretool_guard.py:579` `_own_ssh_hosts` | строки `Host <алиас>` в `~/.ssh/config` | **владельца через чужой инструмент**: формат OpenSSH, файл ВНЕ репо и ВНЕ гейта | **чужой конфиг** (стабильный, стандартизованный; ни контракт, ни проза) | разбор берёт только `p[0].lower()=="host"` при `len(p)>=2`: `Host=alias`, `Include`, `Match` он не понимает → алиас теряет зелёное, вопрос задаётся лишний раз. Настоящий риск не текстовый: кто может писать в этот файл, тот сам себе выдаёт зелёную зону `network` | теряет зелёное (безопасно) |
| **9** | `devbot.py:442` `_card_objects` | `op=<код>` → наш словарь `_OP_CANON`; литералы в обратных кавычках; явные команды регекспом | **смешанный**: тело карточки `item["result"]` собирает демон по шаблону, но ВНУТРИ стоит проза модели (заявка исполнителя, пункты куратора) | **полуконтракт** — каноническая ветка op-кода первая, кавычки и команды — фолбэк | модель перестанет обрамлять команду обратными кавычками → объектов ноль → «не принято: карточка объект не называет». Владелец теряет форму «да <объект>»; кнопка ✅ и «да N» работают как работали | **СТРОЖЕ** (новых «да» не примет никогда) |
| **10** | `devbot.py:478` `_card_id_from_text` | `задач[аиуе] №? <число>` в тексте сообщения, на которое ответили реплаем | **НАШ**: шапку печатает сам devbot — `⚠️ Задача {qid} [{lane}] требует подтверждения красной зоны:` (`devbot.py:1526`); номер сверяется с живой очередью (`:538`) | **КОНТРАКТ** — единственный полный из десяти | правка шаблона шапки («Карточка №95») молча убьёт привязку по реплаю: писатель `:1526` и читатель `:478` лежат в одном файле, но связи между ними в коде НЕТ — их держит РУЧНАЯ КОПИЯ фразы в фикстуре `tests/test_named_object_approval.py:61`, и после переименования тесты останутся зелёными. Вторая щель: карточка режется на куски по 3500 символов (`_chunks`, `:2177`), шапка есть только в ПЕРВОМ — реплай на второй кусок номера не даст либо возьмёт число из чужой фразы тела | теряет привязку (плюс узкий риск чужого номера) |

---

## §2. Числа

**Разметка десяти:**

| вердикт | сколько | какие |
|---|---|---|
| контракт (наш текст, наш читатель) | **1** | B-7 |
| полуконтракт (канон первым, текст — фолбэк) | **2** | A-11, B-8 |
| чужой конфиг (стабильный формат, вопрос доверия) | **1** | A-10 |
| **совпадение (чужой текст, канона нет)** | **6** | A-2, A-30, A-36, B-1, B-3, B-5 |

**Чьи тексты читаются** (место может считаться дважды — в этом и суть):
текст МОДЕЛИ (команда, тело скрипта, проза заявки, шаги плана) — **7** мест (A-2, A-11, A-30, B-1,
B-3, B-5, B-8); текст ЧЕЛОВЕКА (ТЗ владельца или его ответ) — **2** (A-36, B-5); схема чужого
транспорта (ряд очереди Моста) — **1** (A-36); чужой конфиг (OpenSSH) — **1** (A-10); НАШ
собственный шаблон — **2** полностью или наполовину (B-7; op-ветка B-8) плюс карточная половина A-36.

**Направление поломки:** открывает красное — **1** (A-2); съедает/закрывает «да» молча — **2**
(B-5, A-36); прячет карточку от владельца — **2** (B-3, B-1 в части класса); становится строже
(ложное красное или отказ) — **4** (A-11, A-30, B-1, B-8); теряет зелёное — **1** (A-10); теряет
привязку ответа — **1** (B-7).

**Замеры, на которых стоят выводы:**
* `RED_TOKENS` (VPS) — **14**, из них **5** = ручные написания одного факта `confirmed=true`.
* `_RED_PY_TOKENS` (ПК) — **13** (8 Bridge + 5 файловых).
* `_HEADLESS_IMPOSSIBLE_RE` — **15** альтернатив, IGNORECASE.
* `_REPLY_FIELDS` — **8** имён; найдено в коде обоих репозиториев — **0**.
* `_ALWAYS_CARD` — **2** (`void_last`, `add_transaction`): только деньги переживают пустой объект.
* Баз с именем `memory.db` на ПК — **0**; живая локальная БД — `moderation_ipc.db`.
* Порог нарезки карточки — **3500** символов.
* Плотность текстового разбора: гард ПК **113** вызовов `.search/.match/.findall` на 3742 строки;
  гард VPS — **25**; `orchestrator_daemon.py` — **39**; `pc_orchestrator.py` — **36**; `devbot.py` — **23**.

---

## §3. Итог (5 строк)

1. Из десяти «работает как задумано» **контракт ровно один** (B-7, и тот держится ручной копией
   фразы в фикстуре), полуконтракта два, чужой конфиг один — **шесть чистых совпадений**.
2. Все шесть решают про разрешения, и опасны они по-разному: **A-2 единственное, что ОТКРЫВАЕТ** —
   слово `memory.db` в любом комментарии снимает красное с SQL-записи, притом что базы с таким
   именем на ПК не существует вовсе.
3. **B-5 и A-36 распоряжаются словом «да» владельца** по чужому тексту: первое отменяет уже данное
   согласие по ключевому слову в прозе модели ИЛИ в ТЗ самого владельца, второе — дверь, которая по
   коду не открывается никогда, потому что канала для объекта нет (`approve_task(qid, "Filipp")`).
4. Остальные четыре (B-1, B-3, A-11, A-30) ломаются в сторону вопроса или молчания, а не пропуска;
   их общий корень — **наши собственные имена, разыскиваемые в чужом тексте**: переименование
   боевой операции ослепляет детектор молча, и ни один тест этого не заметит.
5. Ничего не чинилось. Порядок для решения владельца — сверху вниз по §1: A-2 (открывает),
   B-5 и A-36 (распоряжаются «да»), B-3 (прячет карточку), дальше остальное.

## §4. Границы

* Вывод по VPS сделан по клону-снимку от 01.08 03:26; рабочее дерево сервера и непушенные ветки
  в нём не видны — на сервер не ходили сознательно.
* «Мост не отдаёт поля ответа» доказано ОТСУТСТВИЕМ имён в коде обоих репозиториев и константой
  `"Filipp"` в `approve_task`, а не опросом Моста: живой ответ Моста фактом здесь не считается.
* `~/.ssh/config` НЕ открывался (файл вне проекта): суждение о разборе сделано по коду читателя.
* Разметка «контракт/совпадение» — суждение по ИСТОЧНИКУ текста, а не замер логов; живость мест
  здесь не перепроверялась.
