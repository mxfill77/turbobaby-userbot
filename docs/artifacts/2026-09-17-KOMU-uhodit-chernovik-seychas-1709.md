# KOMU uhodit chernovik seychas 1709 — кто сейчас получает черновик и доходит ли до человека пустая цена (задание 62-x)

**ГЛАВНОЕ ОДНОЙ СТРОКОЙ (п.5).** Это **цена риска, а не потеря**. Клиентам не ушло ничего: у обоих
живых процессов при старте в лог записан `TEST_MODE=True`. Живой userbot вообще **не исполняет** путь,
где пустой ответ двери превращается в цену. Он запущен 12.09 в 13:10, до коммита `fb8e8ab` (13.09 00:39),
и ответ двери у него всегда перезаписывается числом из файла. Живая дверь по всему парку дала
**0 пустых ответов из 38**. Риск включится при двух событиях: рестарт userbot на HEAD даст пустоте
дорогу к модератору, а снятие `TEST_MODE` — к клиенту.

Дефект, доказанный заходом #61 (`MOLCHIT…`), — свойство **HEAD**, а не живого процесса. Живьём
модератор видит **другое**: цены в сетке по файлу. Решение владельца от 12.09 («цену называет живая
дверь») в работающем userbot ещё не действует. Код не менялся. Вывод — знание.

| предмет | значение |
|---|---|
| заход | ПК-полоса, задание 62 (ключ `62-x-komu-chernovik.1709`), первое действие 06:13:22 местного (+07) |
| место для временного | `_scratch_pustaya_cena_1709/`: прибор `probe.py`, сырьё `calls.jsonl`, вывод `run.txt`. Ничего не удалялось |
| вызовов двери | объявлено до прогона: 1 `fleet` + по одному `quote_price` на юнит, потолок 60. Сделано **39** (1 + 38), отказов 0, 174.7 с; первая котировка 06:17:05, последняя 06:19:42 (`calls.jsonl`) |
| живое | список процессов (`Win32_Process`, только чтение), `userbot.log`/`userbot.log.1`/`moderation_bot.log` (только числа и штампы), `tmp/expect_pc/state.json` наблюдателя |
| не открывалось | `.env`, `moderation_ipc.db`, лист «Календарь бронирования», брони, чаты Telegram, Планировщик (`schtasks` — красный класс) |
| наружу | ничего: ни клиентам, ни в группы, ни в инбокс, ни в журнал до финальной строки |

---

## 0. Тело задания и признак сделанности

**0.** Тело задания видно от «[от Штаба дата=2026-09-17 ключ=62-x-komu-chernovik.1709]» до строки адреса
результата. Длина **≈4300 символов. Это оценка по абзацам, а не замер**: копию тела на диск не делал.
Хвост цел, адрес виден. Число напечатано первым текстом захода.

**0б.** Файл результата один — этот. Временное место — `_scratch_pustaya_cena_1709/`, слов адреса в имени нет.

**0в.** До захода за 17.09 в `docs/artifacts` лежало 9 файлов: `MOLCHIT`, `SETKA`, `ВОСЕМЬ`,
`zamer-sekund-dveri`, `NEIZVESTNOST`, `OBREZKA`, `SLED`, `STRAZH`, `slovar-chuzhogo-otkaza`.
- **п.1, п.2** не отвечены нигде. `TEST_MODE`/`PRICE_SHEET_RULE_ON` за 10–17.09 встречаются только в
  `MOLCHIT` (12 раз) и в отчёте задачи 61, и там это переменные стенда, а не живые процессы.
- **п.3** отвечен как НЕИЗВЕСТНО (`MOLCHIT…:160-161`: «`.env` не читался»).
- **п.4** отвечен частью. `SETKA` (28 GET, 4 модели) и `ВОСЕМЬ` (8 GET, 1 модель) сняли живые ответы с
  числами, но парк целиком не спрашивали и пустоту не искали. `MOLCHIT…:210-212` прямо говорит, что
  такой ответ живьём не снимался.

Работа начата с п.1.

**0г.** Правок нет: ни клиентского кода, ни ручек, ни прайса. Прибор `probe.py` лежит в месте для
временного и только читает.

## Премисы — да/нет с адресом

| # | исход | адрес |
|---|---|---|
| **П1** | **ДА — для HEAD. Живому процессу НЕ относится** | `docs/artifacts/2026-09-17-MOLCHIT-dver-otvet-klientu-1709.md:17-22` (B1 — 3 формы из 6, сетка — 6 из 6), `:178-179` (строку цены вставляет код при пустой голове), `:185-196`. Живой userbot исполняет код `215e94b` — см. §3 |
| **П2** | **ДА** | `MOLCHIT…:151-154`: 9 видов отказа × 5 веток, число файла не звучит; сетка с `PRICE_SHEET_RULE_ON=1` при отказе — «прайс недоступен» |
| **П3** | **ДА** | `MOLCHIT…:156-161` (ручка отдаёт числа файла; живое значение НЕИЗВЕСТНО), `:164-165` (колонка «Сутки», MT-03 300: −226 ฿/сут) |
| **П4** | **ДА** | `MOLCHIT…:210-212`: «Ошибки ячеек `#N/A`/`#REF!` — … то, что отдаёт лист при сломанной формуле. Живой ответ двери с такой ячейкой … не снимался». Что лист так отдаёт при сломанной формуле — общее знание о таблицах, на этой полосе не замерено |

---

## 1. Кто СЕЙЧАС получает готовый черновик (п.1)

**Живые процессы** (`Win32_Process`, 17.09 06:13:22):

| PID | процесс | создан | касается черновика |
|---|---|---|---|
| **10892** | `userbot_listen.py` | 12.09.2026 13:10:01 | **да**: собирает черновик, кладёт в очередь, ведёт тренажёр |
| **12852** | `moderation_bot.py` | 10.09.2026 02:34:11 | **да**: постит карточку черновика в группу модерации |
| 648 | `pc_orchestrator.py` | 12.09.2026 18:08:16 | нет |
| 13740 | `pc_agent.py` | 12.09.2026 15:28:26 | нет |
| 10604 | `rc_supervisor.py` | 10.09.2026 02:31:32 | нет |

Процесса `wa_bridge.py` нет. Импортёров у него нет ни одного, кроме тестов (`grep "import wa_bridge"` по
корню → 0), лога тоже нет. Разовый запуск WhatsApp-моста по расписанию **НЕИЗВЕСТЕН**: Планировщик
не опрашивался.

**Куда уходит черновик** (штампы старта сверены с временем создания PID):

| адресат | чем доказано | что туда уходит |
|---|---|---|
| **группа модерации `-5031790861`** (люди-модераторы) | `userbot.log:12371` — `2026-09-12T06:10:03Z SUGGEST ВКЛЮЧЁН (TEST_MODE=True, mod_group=-5031790861, bot-режим=да…)`, это старт PID 10892 (13:10:01 +07). `moderation_bot.log:7223` — `2026-09-10 02:34:14 moderation_bot ЗАПУСК (TEST_MODE=True, mod_chat=-5031790861)`, это старт PID 12852 | каждый черновик клиенту: `suggest.on_client_message` → `moderation_ipc.enqueue_draft` (`suggest.py:9590-9595`) → карточка модербота. Резервный reply-режим постит в ту же группу (`suggest.py:9214-9220`) |
| **группа-тренажёр «Тренеровка»** (команда играет клиента) | `userbot_listen.py:294-307`: `_trainer_generate` → `_trainer_send`, **без модерации** | ответ бота на реплику ТЕСТ-клиента, та же сборка цены (`build_pricing_note`) |
| **клиент** | `SUGGEST_TEST_MODE=True` у обоих процессов (строки выше). Замки: `moderation_core._finalize` → `test_held`, а `suggest.send_to_client` отказывает (`suggest.py:9184-9186`). Флаг читается при импорте (`suggest.py:68`), поэтому пока живы эти PID, он не меняется | **ничего** |
| WhatsApp | `suggest.poll_and_send` закрывает WA-строку как `failed` (`suggest.py:9298-9302`), а сам мост не поднят | ничего |
| «Входящие брони» (`INBOX_GROUP_ID`) | `suggest.py:9342` | карточки заявок, а не черновики |

**Кто подписан — НЕИЗВЕСТНО поимённо.** Список одобряющих — `APPROVER_USERNAMES` из окружения
(`suggest.py:95`), `.env` не читался. Состав групп читается только живой сессией Telegram, а это
запрещено. Имена в комментарии кода (`suggest.py:97-98`: «Филипп ×2 аккаунта, Даня, Даша») — проза,
а не живой факт.

## 2. Дошёл ли черновик до живого человека (п.2)

**Срез:** `userbot.log.1` + `userbot.log` = **2026-06-09 15:17 UTC → 2026-09-16 23:13 UTC, ≈99 суток**.
Из них живому процессу PID 10892 принадлежит **12.09 06:10 UTC → сейчас, ≈4.7 суток** (строки лога
12363 и дальше).

| до кого | ответ | число и штамп |
|---|---|---|
| **клиент** | **НЕТ, ни одного** | `TEST_MODE=True` у обоих живых процессов. У наблюдателя `tmp/expect_pc/state.json` (тик 2026-09-16T23:07:24Z) среди открытых эпизодов **нет ключа `o5s|…`**. Этот эпизод «клиенту ушло сообщение» не закрывается никогда (`expectations_pc.py:2026`, `:2468`), значит за время жизни файла состояния он не открывался. Оговорка: ветку О5 можно заглушить порогом из окружения, а окружение не читалось |
| **группа модерации** | **очередь — ДА; сама доставка карточки — вероятна, прямой расписки нет** | черновиков в очередь: **539** за срез (`userbot.log.1` — 372, `userbot.log` — 167), живым процессом — **34** (#678…#711). **Последний — #711, после клиентской реплики 2026-09-16 14:55:50 UTC (21:55 местного).** Модербот пишет в лог только ОТКАЗ постинга (`moderation_bot.py:152`): таких 291, последний **2026-07-05 16:39**, с тех пор 0. Положительная расписка (id сообщения карточки) лежит в `moderation_ipc.db`, базу не открывал. Прочитал ли карточку человек — **НЕИЗВЕСТНО** |
| **тренажёр** | **ДА** | последний ответ бота в тренажёр — `TRN 2026-09-12 18:11 … TEST-11 | bot` (`userbot.log`), живым процессом |
| **была ли в этих черновиках пустая цена** | **по устройству живого кода — нет (§3); в тексте черновиков — НЕИЗВЕСТНО** | текст черновика в лог не пишется. Ответы двери живой процесс в файл не пишет: `quote ok bike=` в обоих логах — **0 строк**, логгер `pricing` в файл не попадает. Чем устанавливается — §6 |

## 3. Ручка сетки в живой среде (п.3)

**Живой userbot ручку `PRICE_SHEET_RULE_ON` не читает вовсе: в его коде её нет.**

1. `door_price.py` родился в коммите `fb8e8ab` **2026-09-13 00:39:04 +0700**
   (`git log --diff-filter=A -- door_price.py`). В том же коммите знак ручки перевёрнут на
   `PRICE_SHEET_RULE_ON`, до этого была `PRICE_SHEET_RULE_OFF`.
2. PID 10892 создан **2026-09-12 13:10:01**. Последний коммит до этой минуты — `215e94b` (13:05:04).
   Модули Python читаются с диска при старте, `reload(` в `userbot_listen.py`/`suggest.py` — 0.
   Наблюдатель это подтверждает: `tmp/expect_pc/state.json` → `code.rows` → userbot «на диске новее»,
   отставание **395 907 с (4.6 сут)**, эпизод `o6c|userbot|1789193401` открыт.
3. В коде `215e94b` сетка считается по файлу, если не задан `PRICE_SHEET_RULE_OFF`
   (`git show 215e94b:suggest.py`, `:3992`, `:4085-4088`). Точечная котировка тоже идёт по файлу, пока
   `PRICE_SOURCE_SEASON` не выключен (`215e94b:price_source.py:60`, `:98-104`, по умолчанию ВКЛ).

**Живое состояние: по файлу считается и сетка, и точечная котировка.** Доказано следом работы живого
процесса, а не конфигом. После старта PID 10892 (строка лога 12363 и дальше):
- **5 сеток** построены: строки 13139, 13251, 13363, 15156, 15427, каждая «111 quote». Последние
  клиентские реплики перед ними: 12.09 16:06 UTC ×3, 14.09 05:19 и 08:21 UTC;
- **519 строк `price_source: … → итого …`** в строках 13028–15426. Это лог `price_source.reprice`
  (`215e94b:price_source.py:553`), тройками 1/7/30 суток с якорями 2026-09-15 (87), 2026-11-01 (108),
  2027-01-09 (324). Тройка сроков — след `_sheet_rule_quotes`, то есть правило в сетке ВКЛЮЧЕНО.

Оговорка авторства: PID в строке лога нет. Владелец файла в реестре логов —
`userbot_listen` (`log_setup.py:75`), обработчик заводит `userbot_listen.py:56-70`; `pc_agent.py` и
наблюдатель имя файла только читают.

Что это значит в терминах HEAD: **живьём стоит то, что HEAD назвал бы «ручка включена»**. Модератор
видит в сетке числа файла, а не двери. Расхождение — по `SETKA` (до −226 ฿/сут на колонке «Сутки»).
Клиенту это не уходит (`TEST_MODE`).

**НЕИЗВЕСТНО — значение `PRICE_SHEET_RULE_ON` для СЛЕДУЮЩЕГО процесса** (HEAD, после рестарта).
Установить без `.env` можно по первой сетке после новой строки `ЗАПУСК` в `userbot.log`: тройки
`price_source:` 1/7/30 есть — ручка включена, нет — выключена.

## 4. Достижима ли пустая цена на живом листе (п.4)

**Объявлено до прогона:** 1 GET `fleet` + по одному GET `quote_price` на каждый юнит парка, потолок
60, окно **18.09.2026 → 25.09.2026 (7 сут)**. Прибор напечатал число до котировок: «ЮНИТОВ В ПАРКЕ: 38;
вызовов quote_price будет: 38; всего с fleet: 39». Клиент моста одолжен у демона, как в
`_scratch_vosem_sutok_1709/door.py`, секреты скрипт не читал.

**Итог: 39 вызовов, отказов 0. Все 38 котировок — `ok:true`, `day_price` и `total` положительные.
Ноль — 0, ошибка ячейки (`#N/A`, `#REF!`, `#VALUE!`, `#ERROR!`, `#DIV/0!`, `#NAME?`, `#NUM!`, `#NULL!`
в цене, итоге или тексте) — 0, ответ без числа — 0, текст без цифры — 0.** 174.7 с.

| модель (юнитов) | day_price / total за 7 сут | депозит |
|---|---|---|
| NMAX 155 (10) | 317 / 2217 | 3000 |
| XMAX 300 нового поколения (8) | 662 / 4632 | 7000 |
| XMAX 300 старого поколения (3) | 557 / 3902 | 7000 |
| ADV 350 (7) | 704 / 4928 | 7000 |
| XSR 155 (2) | 472 / 3303 | — |
| XADV 750 (2) | 1966 / 13759 | — |
| CBR 650R · CB 650R · VULCAN 650S (по 1) | 1360 / 9519 | — |
| NINJA 400 | 896 / 6274 | — |
| MT-03 300 | 825 / 5775 | — |
| CB 300R | 674 / 4716 | — |
| FORZA 300 | 487 / 3408 | — |
| CLICK 125 | 176 / 1230 | — |

Депозиты по всему набору: 3000 ×10, 7000 ×15, 5000 ×4, 20000 ×4, 15000 ×2, 25000 ×2, 2000 ×1.
Свободен — 30, занят — 8. Сырьё — `_scratch_pustaya_cena_1709/calls.jsonl`, построчно.

**Вывод:** на живом листе сегодня пустая цена **не встречается ни у одной модели парка** (одно окно,
7 суток). Вместе с 17.09 — 36 живых ответов `SETKA`+`ВОСЕМЬ` по 4 моделям на разных сроках, отказов 0.
Появится ли она при поломке формулы — **НЕИЗВЕСТНО**: это событие, а не состояние, и пробой его не
вызвать. Сроки 1 и 30 суток (колонки сетки) по всему парку этим заходом не спрашивались.

## 5. Одна строка (п.5)

**Цена риска, не потеря:** до клиента не дошло ни одного черновика (`TEST_MODE=True` у PID 10892 и
12852). Живой userbot исполняет код `215e94b`, где пустой ответ двери перезаписывается числом из
файла, и из 38 живых ответов двери пустых 0. Поэтому дефект #61 сейчас не дошёл ни до модератора, ни
до клиента. Риск взводится **рестартом userbot на HEAD** (дорога к модератору) плюс **снятием
`TEST_MODE`** (дорога к клиенту), и только если дверь в этот момент ответит пустотой.

## 6. Что закрыло бы путь одним местом — НЕ сделано (п.6)

**Место:** `pricing._normalize` (`pricing.py:72-93`). Через него проходит КАЖДЫЙ ответ двери:
точечная котировка, минимальный срок, несколько моделей, процент (`pricing.quote` → `:116`) и сетка
парка (тот же `pricing.quote`). Сейчас там отсекается только `None` (`:91`). Правка: считать
«не котировкой» (`return None`) ответ, у которого ни `day_price`, ни `total` не является
**положительным числом**, а также ответ, где в цене, итоге или `text` стоит форма ошибки ячейки. Тогда
все шесть форм пустоты уходят в уже доказанный третий исход (`door_no_price`,
`door_price.py:260-264` → `suggest.py:3894-3897`), а в сетке модель выпадает как `unchecked`. Второго
места не нужно: `door_price.py:117-119` и `render_price_sheet` получат `None` и до пустоты не дойдут.

**Проверка, которой закрывать:**
1. стенд #61 (`_scratch_otkazy_ceny_1709/stand.py`), шесть форм `hollow_*` × ветки B1–B5 через живой
   `suggest.draft_from_transcript`: пустота как цена — **B1 0 из 6** (было 3), **B4 0 из 6** (было 6),
   в записках B2/B3/B5 — 0. Отдельно с `PRICE_SHEET_RULE_ON=1`: сетка не подставляет пустоте числа
   файла (было 289/642/598 ฿);
2. контроль: `door_ok` (662 ฿) проходит тем же объектом — `test_door_price` (20 тестов,
   `assertIs`) зелёный;
3. двусторонний отрицательный тест в `test_pricing`: `_normalize` на шести формах даёт `None` и
   **красен на HEAD**, а на живой форме ответа (фикстура из `calls.jsonl` этого захода) даёт dict;
4. доставка правки в жизнь: без рестарта userbot правка не действует (§3), и заметка О6 «код на диске
   новее» закрывается только новым PID.

**Чем устанавливается незнание §2** (не делалось, у каждого свой запрет): одним read-only чтением
`moderation_ipc.db` в режиме `mode=ro`, как у `client_silence_pc.py` — статусы и `card_msg_id` черновиков
#678–#711 без текстов, это расписка доставки в группу; поиском форм «0 ฿», `#N/A`, `#REF!` в
`draft` тех же строк — это ответ, была ли пустота в тексте. Прочтение карточки человеком не
устанавливается ничем, кроме самой группы.

## 7. Бюджет

Первое действие 06:13:22. Проба — 06:17:05…06:19:42, артефакт — около 06:25. Потолок 0.7 = 1890 с не
превышен. Код, тесты, фикстуры, ручки и прайс не тронуты. `git status` по `*.py`: изменены только
чужие `pretool_guard.py` и `test_pretool_guard.py`, они были такими до захода.
