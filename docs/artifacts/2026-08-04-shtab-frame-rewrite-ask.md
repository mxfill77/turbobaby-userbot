# Полная замена KB_shtab_frame на версию 04.08.2026: откат снят, ЗАМЕНА НЕ СДЕЛАНА — нет текста

**Дата:** 2026-08-04, задача 260 (headless, полоса ПК).
**Задание:** переписать `KB_shtab_frame` (ключ `shtab_frame`,
id `1DiXr3jxoZYxkWaS7k4X4SR0u2xQKUTCi`) ЦЕЛИКОМ под версию рамки 04.08.2026; перед заменой снять
копию нынешнего содержимого с меткой даты.
**Итог:** сделано всё, кроме самой записи. **Текста новой рамки нет ни в задаче, ни в дереве —
сочинять его нельзя, живой док не тронут.**

## 1. Адрес — сошёлся (живой мост, чтение)

| проба | ответ прода |
|---|---|
| реестр `list_brain` | ключей **33**; `shtab_frame` → `1DiXr3jxoZYxkWaS7k4X4SR0u2xQKUTCi` — **совпадает с id задания** |
| второй ключ на этот файл | **нет**: на id ссылается ровно один ключ (`shtab_frame`) |
| `resolve_name("KB_shtab_frame")` | `name:shtab_frame` (канон `shtab_frame`) |
| чтение по id и по имени | **6780 символов оба раза, посимвольно равны** |

## 2. Живой док ДО замены (замер, не пересказ)

```
6780 символов, 145 строк, U+FFFD 0, «конец рамки» 1 раз
строка 1: КОПИЯ. Источник — инструкции проекта Штаба, версия 01.08.2026.
строка 145: ═══ конец рамки ═══
```

Заодно видно (пригодится при сверке с источником): в нынешней копии **два раздела с номером 11** —
строка 112 «11. ДОРОЖНАЯ КАРТА» и строка 116 «11. ДОРОЖНАЯ КАРТА, продолжение», причём первый
обрывается на полуслове («Этап 3» и конец абзаца). Это след дозаписи хвоста 01.08; новая версия,
судя по заданию, как раз и переписывается целиком.

## 3. Откат снят ДО всего остального — в двух местах

| файл | что это |
|---|---|
| `tmp_shtab_rewrite_20260804_w3/shtab_frame_2026-08-04_before.txt` | копия рабочего каталога задачи (untracked) |
| `docs/artifacts/2026-08-04-shtab-frame-before-rewrite.txt` | **та же копия в git** — чтобы откат пережил уборку временных каталогов |

Обе: **11 817 байт, 6780 символов, sha256 `cade204ef9a158a4…`, посимвольно равны живому доку**
(сверено чтением файла обратно). Откат = записать этот текст обратно тем же каналом.

## 4. Почему замена НЕ сделана

Текста новой рамки нет:

- **в задаче его нет.** Демон отдаёт исполнителю строку очереди целиком, без обрезки
  (`pc_orchestrator.py:1501` — `prompt += text`); значит пришло ровно то, что владелец написал,
  а текста рамки там нет;
- **в дереве его нет.** Просмотрены все файлы репо, изменённые с 03.08 12:00, плюс `tmp/`,
  `artifacts/`, `notes/`, прежние каталоги задач рамки (`tmp_shtab_frame_20260801_k1`,
  `tmp_shtab_tail_20260801_k2`, `tmp_shtab_8v_20260803_m1`) — нового текста нет нигде;
- **«следующего сообщения» в headless не бывает.** Оговорка задания «текст владелец пришлёт
  следующим сообщением» рассчитана на интерактив; в очереди досыл к задаче — это новая задача
  (правило §8 самой рамки).

**Сочинить текст нельзя по существу дока, а не из осторожности.** Этот док — КОПИЯ инструкций
Штаба, и его первая строка велит исполнителям по нему сверяться. Придуманная «версия 04.08.2026»
стала бы для них источником правды, неотличимым от настоящего, — это хуже, чем отсутствие
обновления. Источник живёт в инструкциях проекта у владельца, с ПК он не читается ничем.

## 5. Что готово к запуску (одна команда, когда текст будет)

```
1) положить текст владельца в  tmp_shtab_rewrite_20260804_w3\new_frame.txt   (UTF-8)
2) venv\Scripts\python.exe tmp_shtab_rewrite_20260804_w3\replace_frame.py
3) venv\Scripts\python.exe tmp_shtab_rewrite_20260804_w3\verify.py
```

`replace_frame.py` — полная замена через `brain_writer.apply` (секретов сам не читает, запрет
класса 328):

- головной абзац «КОПИЯ. Источник — инструкции проекта Штаба, версия 04.08.2026 …» ставится
  дословно по заданию, если его нет в файле; если он там уже первым — не дублируется;
- `allow_shrink=True` объявлен **явно**: новая версия может быть короче, и гард усыхания здесь
  снимается сознательно, а не молча;
- `expect` = весь новый текст, `marker` = «конец рамки» (передаётся писателю, только если в новом
  тексте он ровно один раз — иначе число отдаётся в отчёт как есть, без подгонки);
- отказ моста записью не считается: судит обратное чтение; бэкап писателя ложится третьим
  экземпляром отката;
- **без `new_frame.txt` скрипт останавливается и мост не трогает вовсе** — проверено запуском
  сегодня (exit 2, ни одного обращения к доку).

`verify.py` — независимый процесс, читает **ПО ИМЕНИ** через реестр и печатает доказательства
задания: длина до/после, первая строка дословно, текст задания в доке дословно, U+FFFD,
«конец рамки» ровно один раз, реестр (33 ключа, один ключ на файл), наличие копии-отката.

## 6. Развилка владельцу

Пришли текст новой рамки — новой задачей (или файлом `new_frame.txt` в каталоге выше). Тогда
замена и сверка укладываются в один заход.

## 7. Запреты задания — соблюдены

Ничего не удалено (включая старую копию и прежние каталоги задач рамки); `.env` не открывался;
Drive-коннектор не использовался — только живой Bridge; других Brain-доков не касались, второго
документа не создавали; временное — в новом каталоге `tmp_shtab_rewrite_20260804_w3`; процессы
не трогались; клиентский контур не задет.
