# Вариант Г записан правилом, а в код не поставлен: два вопроса решает не исполнитель

pricing разбивка по периодам 07.09

**Дата:** 07.09.2026 13:05 местного (06:05 UTC) · **полоса:** ПК · задание из ящика Штаба,
ключ `01-b-price-split-days.0907` (документ `shtab_task_01-b-price-split-days.0907`,
file id `1cQFogALzG9qvZjQTT5_vOSGfbJuBdV_a`). Временное живёт в `_scratch_price_split_0907/`
и больше нигде. Ничего не удалено, включая уборку за собой. `.env` и конфиги не читались:
`price_source.json` открывал КОД, который его читает (`price_source.load()`), вердикт границы
выносил живой `season_gate.span()`. Процессы не трогались и не перезапускались. Боевых записей
нет: живых чатов, боевой базы и рабочих таблиц не касались, `--write` не звался, **кругов
тренажёра не гонялось ни одного**, корпус не тронут.

**FACT (живое доказательство эффекта):** решение владельца лежит в узле писаных правил
`KB_business_rules` блоком **«РЕШЕНИЕ ВЛАДЕЛЬЦА 07.09.2026 №1 — СРОК ЧЕРЕЗ ГРАНИЦУ ПЕРИОДОВ
СЧИТАЕТСЯ ПО ДНЯМ»**, строка **1389** живого узла. Запись сделана доверенным писателем
`brain_writer.append` (бэкап `tmp/brain_backup_rule_0709_1_20260907-130301.txt`), **обратное
чтение — ОТДЕЛЬНЫМ действием и отдельным процессом**: длина узла **73 662 → 78 939** символов,
sha256 после записи `69c79f18…9dd392f0`. Кода по этому решению не написано **ни одной строки** —
и это исполнение п.2, а не пропуск: см. §3.

---

## Одной страницей

1. **Правило записано, код — нет, и порядок именно такой, как велит задание** («сперва запиши
   это правилом, потом ставь в код»). Правило встало полным блоком решения: номер `07.09.2026-1`,
   автор — владелец, время 07.09.2026, канал — ящик Штаба, дословная цитата «я думаю Г давай
   пробовать», формулировка «срок через границу периодов считается по дням», названный откат.
2. **Код остановлен п.2 задания, и оба вопроса действительно не имеют ответа в писаных правилах.**
   Вопрос 1 (ступень от полной длительности или от куска) — правила **МОЛЧАТ**. Вопрос 2 (чей
   месячный потолок) — правила **СПОРЯТ САМИ С СОБОЙ**: файл цены говорит «кап нельзя применять
   по календарю», а живой код применяет его **ровно по календарю**.
3. **Оба вопроса стоят денег, и это посчитано, а не предположено.** На примере самого задания
   (NMAX 155, 31 сутки с 6 октября 2026): ответ «полной» → **5023 ฿**, ответ «куска» → **8124 ฿**,
   разница **3101 ฿ (61.7 %)** на одной брони. Кепка этой модели — **5000 ฿/мес**, то есть при
   ответе «полной» срок перерастает её на **23 ฿**, и от ответа на вопрос 2 зависит, услышит
   клиент «5023 бата» или «аренда от 5000 ฿/мес».

---

## 0. Признак сделанности: что показало СОДЕРЖИМОЕ

`ls docs/artifacts/ | grep 2026-09-07` до работы дал **шестнадцать** файлов. Адресных слов этого
задания нет ни в одном. Ближайший по теме — `2026-09-07-price_source-граница-периодов-замер.md`
(05:47, коммит `14335db`): он **меряет цену** поведения границы и прямо говорит о себе «**Правок
нет никаких** — ни кода, ни корпуса, ни правил», а решение владельца называет развилкой, которую
не толкует («Правило 19.08 здесь не толкуется и не оспаривается»). Ни решения Г, ни записи
правила, ни ответа на два вопроса п.2 в нём нет.

**Вопросы этого задания не отвечены ни одним артефактом за 07.09. Работа начата с нуля.**

## 1. Премисы Штаба — проверены ПЕРВЫМ действием живыми модулями, все три ПОДТВЕРДИЛИСЬ

| премиса | чем проверено | исход |
|---|---|---|
| пересечение границы гасит сторож, и цена клиенту не называется вовсе | `season_gate.span("2026-10-06","2026-11-06")` → `crosses` («P2 ОКТЯБРЬ → P3 НОЯБРЬ»); `season_gate.note_for(...)` отдал записку, **цифр в ней 0**; в лог ушла карточка «⛔ НЕ СЧИТАЮ» | **ПОДТВЕРДИЛАСЬ** |
| живой пример: NMAX 155 на 31 сутки с 6 октября — 298 × множитель периода × ступень срока = 158 ฿/сут и 4898 ฿ за срок, и сторож этого числа не произносит | `price_source.day_price(doc,"NMAX 155",2026-10-06,31,…)` → **158**; разбор: база 298.0, сезон 1.0 (P2 ОКТЯБРЬ), ступень 0.53 (корзина `30+`), класс «скутер»; 158 × 31 = **4898** | **ПОДТВЕРДИЛАСЬ** |
| периодов девять, они покрывают год целиком, день без периода недостижим | `price_source.load()` → 9 периодов, сумма `days` = **365**; `period_of()` на каждом из 365 дней 2026 — `None` **0 раз** | **ПОДТВЕРДИЛАСЬ** |

Воспроизведение: `venv/Scripts/python.exe _scratch_price_split_0907/premise.py`.

## 2. ПРАВИЛО ЗАПИСАНО ПЕРВЫМ (п.1 задания)

Узел: `KB_business_rules` (ключ реестра `business_rules`), канал записи — **только**
`brain_writer`; секретов скрипт не читает и не видит, в нём живёт лишь текст блока и якорь
вставки. Блок поставлен перед `ЗАПРЕТ БЕЗОПАСНОСТИ` — тем же местом и той же формой, что четыре
решения владельца от 06.09, чтобы узел читался одним рядом.

Что стоит в правиле дословно:

* **выбор Г** — «РЕШЕНИЕ ДОСЛОВНО: «я думаю Г давай пробовать» — выбран ВАРИАНТ Г»;
* **дата** — «АВТОР: владелец. ВРЕМЯ: 07.09.2026. КАНАЛ: чат Штаба, ящик заданий Штаба»;
* **формулировка** — «СРОК ЧЕРЕЗ ГРАНИЦУ ПЕРИОДОВ СЧИТАЕТСЯ ПО ДНЯМ — каждый день идёт по
  множителю СВОЕГО периода, а срок больше не отвергается целиком»;
* **номер** — `07.09.2026-1`;
* **откат** — «отменяется СЛОВОМ ВЛАДЕЛЬЦА — новой записью этого узла, которая называет номер
  07.09.2026-1 явно»; отмена ВОЗВРАЩАЕТ в силу строку 18.08, физически оставшуюся в узле.

**Что правило отменяет — названо прямо, чтобы узел не спорил сам с собой.** Строка раздела
«РЕАЛЬНЫЕ СРОКИ АРЕНДЫ» (18.08.2026) — «ПЕРИОД ЛЁГ НА ОБА СЕЗОНА СРАЗУ — это индивидуальное
предложение: расчёт цену НЕ называет и среднего не выводит» — **больше не действует** для срока
через границу известных периодов. Соседняя строка того же раздела — «ПО СЕЗОНУ ДАТ: длинная
аренда считается по сезону тех дат, на которые попадает» — не отменена, а **исполнена**.

**Расхождение правил с кодом, названное в записи честно.** Строка 18.08 говорит про ДВА сезона
(высокий и низкий), а живой `season_gate` отказывается считать на границе **любых двух из
девяти** периодов — включая `P1 → P2`, где обе стороны низкие и ставка совпадает у всех 14
моделей. Решение `07.09.2026-1` накрывает оба чтения.

**Обратное чтение — отдельным действием** (`read_rules.py`, отдельный процесс, только чтение):
узел вырос 73 662 → 78 939 символов, блок стои́т на строке 1389 и читается целиком.

## 3. ДВА ВОПРОСА, КОТОРЫЕ РЕШАЕТ НЕ ИСПОЛНИТЕЛЬ (п.2 задания) — ОСТАНОВКА

Задание велит: правила отвечают — цитировать и делать; молчат или спорят сами с собой —
остановиться и спросить владельца. **Проверено по живому узлу и по живому файлу цены. Оба
вопроса без ответа, поэтому кода в этом заходе нет.**

### Вопрос 1 — СТУПЕНЬ СРОКА: правила МОЛЧАТ

Что в правилах есть, дословно, и почему ни одна строка не отвечает:

| строка правил | что говорит | почему не ответ |
|---|---|---|
| «ФОРМУЛА: цена суток = БАЗА МОДЕЛИ x СЕЗОН(период) x СТУПЕНЬ СРОКА(корзина)» | называет три сомножителя | не говорит, **от какой длительности** берётся корзина, когда кусков несколько |
| решение `06.09.2026-1`: «ступень срока: граница корзины — 14» | задаёт **границу** корзины | про длительность не говорит ничего |
| «ЧТО ЭТО ЗАКРЫВАЕТ: вопрос о дроблении длинной аренды на несколько ступеней … ступень остаётся одна» | закрывает развилку замера 17.08 | та развилка была про **дробление СЕТКИ корзин** (варианты дословно: «оставить одну ступень; разбить на три» — 30-60, 61-120, дольше 120), а не про то, чем корзина индексируется при разбивке по дням |
| `price_source.json` → `term.origin`, `term.reference_note` | «множители сняты замером, граница опорной корзины — слово владельца» | о разбивке молчит: файла с разбивкой не было никогда |

**Цена ответа посчитана живым файлом** (`_scratch_price_split_0907/illus.py`), пример самого
задания — NMAX 155, 31 сутки с 6 октября 2026, куски `P2 = 26 сут` и `P3 = 5 сут`:

| трактовка | как считает | итого за срок |
|---|---|---|
| как сегодня, по дате начала (сторож снят) | ступень `30+` ×0.53, весь срок по P2 → 158 ฿/сут | **4898 ฿** |
| Г + ступень от **ПОЛНОЙ** длительности | ступень `30+` ×0.53 обоим кускам: 26 × 158 + 5 × 183 | **5023 ฿** |
| Г + ступень от **КУСКА** | 26 сут → корзина `15-29` ×0.82 → 244 ฿/сут; 5 сут → корзина `4-6` ×1.03 → 356 ฿/сут | **8124 ฿** |

**Разница 3101 ฿ — 61.7 % от меньшей суммы, на ОДНОЙ брони.** Это не округление и не мелочь:
ступень `30+` даёт скидку 47 %, а разрезанные куски попадают в корзины БЕЗ скидки и даже с
надбавкой (`4-6` = ×1.03). Ответ «куска» означает, что клиент, снявший байк на месяц через
границу, платит **дороже**, чем снявший его на месяц внутри периода.

> **ВОПРОС ВЛАДЕЛЬЦУ 1. Ступень срока при разбивке берётся от ПОЛНОЙ длительности брони или от
> куска внутри периода? Ответ одним словом: «полной» или «куска».**

### Вопрос 2 — МЕСЯЧНЫЙ ПОТОЛОК: правила СПОРЯТ САМИ С СОБОЙ

| источник | что говорит |
|---|---|
| `price_source.json` → `low_season_caps.for_consumers` | «САМО ПРАВИЛО НЕ ИЗМЕНИЛОСЬ: **КАП НЕЛЬЗЯ ПРИМЕНЯТЬ ПО КАЛЕНДАРЮ**. Он не привязан к периоду и не имеет истории: его положение надо ЧИТАТЬ живым `quote_price` на момент расчёта» |
| `low_season_caps.trigger_rule` | «`cap_active` И **total (сумма за весь период)** > `cap_price`» — сравнение с суммой за ВЕСЬ срок, единица кепки «฿ за месяц (30 суток)» |
| `low_season_caps.switch.tied_to_period` | «**НЕТ, НИ ОДНИМ БИТОМ — ИЗМЕРЕНО**: тот же запрос на датах ПИКА вернул те же `cap_price` и `cap_active`, что запрос низкого сезона, у всех 14 байков» |
| живой код `price_source.reprice` | кепка ставится **только** при `info["low_season"]`, а `low_season` = «множитель периода **ДАТЫ НАЧАЛА** ровно 1.000» — то есть **ровно по календарю** |
| `KB_business_rules` (замер 18.08) | «не прикладывать месячный потолок к сроку короче 30 суток» — про СРОК, про периоды ни слова |

Спор виден без разбивки, а разбивка делает его неразрешимым молча: при счёте по дням часть
суток лежит в низком сезоне, часть нет, и **«низкий сезон» перестаёт быть одним признаком** —
им становится множество суток.

**Вопрос не отвлечённый, и вот число.** Кепка NMAX 155 в файле — **5000 ฿/мес**; срок из примера
при ответе «полной» стоит **5023 ฿**, то есть перерастает кепку на **23 ฿**. Условия
`suggest._cap_applies` (`total > cap_price` и `days ≥ 30`) выполняются оба. От ответа на вопрос 2
зависит буквально, услышит клиент **«5023 ฿»** или **«аренда от 5000 ฿/мес»**.

> **ВОПРОС ВЛАДЕЛЬЦУ 2. Месячный потолок при разбивке — чей? Ответ одним словом: «все» (потолок
> только когда ВСЕ сутки низкого сезона), «любые» (когда хоть одни), «начало» (по периоду даты
> начала, как сегодня) или «никогда» (на разбитом сроке потолок не применяем).**

Оба вопроса записаны **в само правило** (блок `07.09.2026-1`, пункт «ДВА ВОПРОСА ОСТАЛИСЬ
ОТКРЫТЫМИ»), чтобы ответ владельца лёг рядом с решением, а не потерялся в переписке. Своей
трактовки не подставлено ни в коде (кода нет), ни в правиле (вопросы стоят вопросами).

## 4. ТРЕТИЙ ИСХОД НЕ СНЕСЁН (п.3) — и вот чем это доказано

Правки нет, поэтому замок отказа пережил заход по построению. Но «по построению» — это слово, а
задание требует теста, поэтому замки **прогнаны**:

```
venv/Scripts/python.exe -m unittest test_season_gate test_price_source test_noprice_gate test_price_gate
Ran 104 tests in 2.588s — OK
```

Поимённо держат отказ:

| замок | файл | что держит |
|---|---|---|
| `test_unknown_does_not_raise_card` | `test_season_gate.py:235` | таблицы нет → путь ПРЕЖНИЙ, карточки нет |
| `test_date_and_term_gaps_are_not_this_class` | `test_noprice_gate.py:362` | дата вне периодов → код `WHY_DATE_OUT`, цена гасится, и это НЕ класс «модель без цены» |
| `test_missing_file_kills_the_price`, `test_empty_file_…`, `test_broken_json_…`, `test_foreign_schema_…`, `test_empty_models_…` | `test_price_source.py:450–476` | **нечитаемая таблица** → цены нет ни одной дорогой |
| `test_dead_branch_never_returns_the_old_blind_number` | `test_price_source.py:499` | гашение не открывает дорогу старому слепому числу |
| `test_every_day_of_year_belongs_to_exactly_one_period` | `test_price_source.py:145` | сегодня дня без периода нет — вход в отказ по этой причине недостижим живым файлом |

**Находка для следующего захода, и она не косметическая.** Сегодня `season_gate.span()` смотрит
**только два края** срока (`period_of(ds)` и `period_of(de)`) — этого хватало, потому что любое
пересечение края означало отказ. При счёте по дням отказ обязан сработать на дне **в середине**
срока, не попавшем ни в один период, а такой день **ни одним краем не виден**. Значит триггер
третьего исхода меняет форму: «края» → «любые сутки срока». Сегодня это недостижимо живым файлом
(365 из 365 дней покрыты), но замок должен встать вместе с разбивкой, иначе он тихо ослабнет.

## 5. ЧЕГО В ЭТОМ ЗАХОДЕ НЕТ ЧЕСТНО (п.4 и п.5)

* **Контрфакт (п.4) невыполним:** при выключенной разбивке нужно получить ДРУГОЙ ответ, чем при
  включённой, — а включать нечего. Ставить «разбивку» ради контрфакта значило бы подставить свою
  трактовку двух вопросов, что п.2 прямо запрещает. Так и написано: **правка не сделана,
  контрфакт не доказан.**
* **Регресс «внутри периода» (п.5) невыполним по той же причине**, но линия для него снята и
  ждёт следующего захода: `_scratch_price_split_0907/baseline.py` прошёл **365 дат старта × 7
  сроков (1, 3, 7, 14, 30, 31, 60) × 14 моделей с базой**. Окон, целиком лежащих внутри одного
  периода, — **1573** (пересекающих границу — 982); строк цены — **22 022**, цена названа во всех
  22 022. Отпечаток линии: sha256 `86c25fa2214ea4a77080a9fea69d8b309bb9418a796f8bd3e65962f55153afc9`
  (дамп `_scratch_price_split_0907/baseline_inside_period.txt`). Совпадение этого sha256 после
  правки и есть требуемое п.5 «число совпадений» — **22 022 из 22 022** одной строкой.

## 6. АРИФМЕТИКА (п.6)

Кругов тренажёра — **ноль**. Задание разрешало до четырёх (4 × 87 ≈ 348 с, по худшему 716 с при
потолке 2700 с); ни один не понадобился, потому что доказывать было нечего: правка не сделана, а
премисы и обе трактовки считаются живыми модулями за секунды. `--write` не звался, полный набор
не гонялся, корпус тренажёра не тронут ни одним файлом.

## 7. Что этот заход НЕ говорит (границы честности)

* **Не решено, какой ответ на два вопроса верный.** Числа §3 показывают цену выбора, а не выбор.
  Рекомендация здесь сознательно не даётся: п.2 задания требует спросить, а не посоветовать.
* **Правило не проверено деньгами.** Оно записано словом владельца; сходимость варианта Г с
  живой выручкой не мерилась и мериться этим заходом не могла.
* **Продукт не изменился ни одной буквой.** Клиент на границе периодов по-прежнему получает
  третий исход «цену считает человек»; карточка владельцу поднимается как раньше. Правило и код
  сейчас **разошлись сознательно и названо** — правило главнее, и это записано в самом правиле
  строкой «ПОКА НЕ РАБОТА».
* **Год взят 2026 (365 суток).** Границы в файле заданы как «ММ-ДД» и года не знают; на високосном
  числа §5 сдвинутся на единицы.

## 8. Как воспроизвести

Скрипты лежат в `_scratch_price_split_0907/`, запуск из корня репозитория; все, кроме
`write_rule.py`, — только на чтение:

```
venv/Scripts/python.exe _scratch_price_split_0907/read_rules.py KB_business_rules  # узел правил
venv/Scripts/python.exe _scratch_price_split_0907/premise.py                       # премисы Штаба
venv/Scripts/python.exe _scratch_price_split_0907/meta.py                          # мета файла цены
venv/Scripts/python.exe _scratch_price_split_0907/illus.py                          # цена ответа на вопрос 1
venv/Scripts/python.exe _scratch_price_split_0907/baseline.py                       # линия п.5
venv/Scripts/python.exe -m unittest test_season_gate test_price_source test_noprice_gate test_price_gate
```

`write_rule.py` — единственный пишущий, он уже отработал и идемпотентен: повторный запуск увидит
свой текст цельными строками и не сделает ничего.
