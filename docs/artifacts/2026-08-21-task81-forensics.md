# Что сделала задача 81 и куда делся её итог (разведка read-only, 21.08.2026)

**Дерево на входе:** `a8ad93d` (2026-08-21 14:18:02 +07:00 = **07:18:02 UTC**), время разведки
**2026-08-21 07:21:12 UTC**. Кода не правил, тестов и голденов не трогал, источник цены и сторож не
трогал, ботов не перезапускал, в таблицы не писал, `.env` не читал, ничего не удалял, гейт не гонял.

## ВЫВОД (одним из трёх)

**Работа СДЕЛАНА и итог НЕ потерялся — потерялся только ОТЧЁТ задачи.** Заход отработал полностью,
закоммитил три коммита, положил артефакт на диск и **был убит таймаутом прогона через 19.9 с после
последнего коммита**, на выдаче отчёта. Строку в журнал он не успел, но **демон записал исход САМ**,
и эта запись подтверждена реестром обратного чтения.

Уточнение к посылке владельца — она неверна в двух местах, и оба стоили бы ложного диагноза:

1. **«Итога по 81 в журнале нет ни строки»** — есть **две**, и обе легли. Тип у них **`NOTE`**, а не
   `DONE`/`failed`: их пишет ОРКЕСТРАТОР, а не заход. Поиск по `DONE`/`failed` их не находит
   никогда — это тот же класс, что документированный в CLAUDE.md «искать RC-итоги по `^DONE RC`
   бесполезно».
2. **«Строка терялась гонкой перезаписи журнала»** — здесь этого класса **нет**. Спул-файл
   `cowork_log.spool` **отсутствует** (ни одной отложенной строки), а реестр `cowork_log.ledger`
   пополняется ТОЛЬКО после обратного чтения. Причина иная и названа самим демоном: `run_timeout`.

**Докуда дошло по существу задания:** все **16** красных разобраны по корням (это и было заданием),
**6 починено**, **10 оставлено красными осознанно** с названной причиной по каждому. То есть разбор —
полный, починка — частичная и объявленная, а не оборванная.

---

## 1. Коммиты за окно

**За окно, о котором спросил владелец (06:34–06:55 UTC), коммитов НОЛЬ — и это не признак
бездействия.** Первый коммит захода лёг в **06:59:02 UTC**, то есть на 4 минуты позже конца окна.
За реальное окно задачи (**06:33:21 → 07:18:21 UTC**) коммитов **три**:

```
$ git log --since=2026-08-21T06:00:00Z --until=2026-08-21T08:00:00Z \
        --format='%h | author=%ad | commit=%cd | %s' --date=format-local:'%Y-%m-%d %H:%M:%S'
a8ad93d | author=2026-08-21 14:18:02 | commit=2026-08-21 14:18:02 | ARTIFACT шестнадцать красных разобраны по корням 21.08 -> docs/artifacts/2026-08-21-remaining-reds.md
c337973 | author=2026-08-21 14:07:14 | commit=2026-08-21 14:07:14 | голдены цены: транспорт quote-блока и fail-safe черновика переведены на счёт правила
16c347b | author=2026-08-21 13:59:02 | commit=2026-08-21 13:59:02 | голдены цены: корень «канал столбца J упразднён» и корень кепки — три ожидания пересчитаны ручным счётом, кепка переведена с NMAX на NINJA 400
1dfb625 | author=2026-08-21 13:06:32 | commit=2026-08-21 13:06:32 | ARTIFACT граница длинной аренды числом 21.08 -> docs/artifacts/2026-08-21-long-rent-boundary-180.md
```

(`1dfb625` — чужой, это задача 80; заход 81 сам называет его деревом на входе.)

Содержимое трёх коммитов захода — **только тесты и один документ**, прод не тронут:

```
16c347b  test_pricing.py | 67 +++--   1 file changed, 55 insertions(+), 12 deletions(-)
c337973  test_suggest.py | 26 ++--    1 file changed, 22 insertions(+),  4 deletions(-)
a8ad93d  docs/artifacts/2026-08-21-remaining-reds.md | 266 +++  1 file changed, 266 insertions(+)
```

## 2. Незакоммиченное в рабочем дереве

**Отслеживаемых изменений — ноль.** Заход закоммитил всё, что делал:

```
$ git diff --stat
(пусто)
$ git diff --cached --stat
(пусто)
```

Неотслеживаемого — обычный мусор дерева (98 каталогов `_scratch_*`, `tmp_*`, `_tmp_*`, тела
журнальных записей). Свой рабочий каталог заход тоже оставил на диске, вне git, как и обещал в §6
артефакта:

```
$ ls -la _scratch_reds_0821/
-rw-r--r-- 1  2163 2026-08-21 14:03:17 negcheck.py      # отрицательный тест (слом расчёта подменой атрибута)
-rw-r--r-- 1 79346 2026-08-21 13:43:58 run.txt          # прогон ДО  (59+2 при медленном мосте)
-rw-r--r-- 1 12499 2026-08-21 14:15:52 run_after.txt    # прогон ПОСЛЕ (10) — 07:15:52 UTC, за 2.5 мин до убийства
```

## 3. Артефакт задачи на диске

**Есть, целый, дописанный до конца.**

```
$ stat docs/artifacts/2026-08-21-remaining-reds.md
size=20782  mtime=2026-08-21 14:17:57.367116000 +0700   (= 07:17:57 UTC)
```

267 строк, разделы §0–§6 на месте, последний раздел «Как воспроизвести» закрыт. Закоммичен в
`a8ad93d` через 4.6 с после записи на диск.

**Разобраны все пять корней предшественника (`2026-08-21-goldens-deferred.md`) плюс шестой:**

| корень | тестов | исход |
|---|---|---|
| **Г1** · канал «дословный текст столбца J» упразднён (`price_source.py:319` → `out.pop("text")`) | 5 | **ПОЧИНЕНО**, ручной счёт 298/2086 и 307/1535; один голден **перевёрнут** (стережёт, что текст листа НЕ течёт) |
| **Г5** · кепка низкого сезона | 1 | **ПОЧИНЕНО сменой модели, а не числа**: NMAX → NINJA 400, счёт 607 ฿/д · 18210 против кепки 11900 |
| **Г3** · поколения XMAX 300 | 2 | **оставлено красным**: неполно ПРАВИЛО, у раздела `not_included[4]` пустое поле решения владельца. Единственное расхождение за 10 % на живых деньгах (−81 ฿/сут) |
| **Г4-A** · смоук судит свою строку с `avail=None` (`suggest.py:7353`, прошито литералом) | 3 | **оставлено красным**: корень в КОДЕ и в защитном стороже — отдельным заходом |
| **Г4-B** · тест модерации кормит гард КОНВЕРТОМ вместо котировки (`test_moderation.py:322`) | 2 | **оставлено красным**: чинимо, «не успел в бюджет захода» |
| **Г2** · протухшие ручные заглушки смоука (`wait_draft`) | 3 | **оставлено красным**: чинимо и разобрано до готового счёта 307/1535, «не успел в бюджет захода» |
| **§0 (сверх задания)** · набор красных **НЕДЕТЕРМИНИРОВАН** | — | три прогона одной командой: 16 / 62 / 59+2. Причина доказана: часть тестов пути цены ходит в живой Bridge (`suggest.py:1744`, timeout=30) и краснеет по сети. **61 красный = 16 устаревших + 45 плавающих** |

Итог самого артефакта: **было 16 → стало 10**, отрицательный тест 6 из 6 краснеет, прод не тронут,
число тестов как было 858.

**Оба «не успел в бюджет захода» теперь читаются иначе:** бюджета не хватило не по нерасторопности —
заход упёрся в стену таймаута и это знал.

## 4. Что говорит очередь про задачу 81

**Состояние — `failed`, результат записан, расписка чистая.** Дословно из `pc_orchestrator.log`
(время лога локальное, +07:00):

```
8193:2026-08-21 13:33:21,362 INFO CLAIM id=81 in_progress
8194:2026-08-21 13:33:21,780 INFO RUN id=81 (timeout=2700s, попытка 1/2)
8204:2026-08-21 14:18:21,942 WARNING id=81 ТАЙМАУТ 2700s → failed
     2026-08-21 14:18:21,990 WARNING FAIL причина=run_timeout окно=21.08 06:33–07:18 UTC следы: коммитов=3, записей журнала=0
     2026-08-21 14:18:21,991 INFO METRICS task=81 lane=pc type=read mode=prod src=pc_orchestrator.py model=claude-opus-5 effort=xhigh start=2026-08-21T06:33:21+00:00 end=2026-08-21T07:18:21+00:00 dur_s=2700.61 outcome=failed attempts=1 selfheals=0 tokens_in=na tokens_out=na
8207:2026-08-21 14:19:20,250 INFO COMPLETE id=81 status=failed
8208:2026-08-21 14:19:20,255 INFO личка не дублируется (failed id=81): карточка уже в личке (гард) и в теме 829 с кнопками | ⚠️ Оркестратор: задача #81 — в окне есть работа, закрытие не состоялось — ⏱ НЕ ЗАКРЫТА, но В ОКНЕ ЗАДАЧИ ЕСТЬ РАБОТА [причина=run_timeout · таймаут прогона]: та…
```

Штамп начала — из `pc_orchestrator.task_started.json`, дословно:

```
"81": {"at": "2026-08-21T06:33:21.381086+00:00", "pid": 18872, "proc": "18872-1787239635", "child": 7724}
```

**Расписка `complete_task` пришла чистой:** строка `COMPLETE id=81 status=failed` без сопровождающего
`COMPLETE-VERIFY` (таким предупреждением демон помечает потерянную расписку — см. живой пример
`id=78` в 12:05:48 того же лога). Значит запись в очередь долетела с первого раза.

**Живого чтения очереди через мост я не делал сознательно** — задача read-only и запрещает читать
`.env`/конфиги, а санкционированного модуля-читателя очереди (аналога `brain_writer` для записи) в
репозитории нет. Всё выше — собственные расписки демона, а не пересказ.

**Хронометраж последних 90 секунд задачи 81 (в UTC):**

| время | событие | до убийства |
|---|---|---|
| 06:33:21.4 | CLAIM, RUN (timeout=2700s) | — |
| 07:15:52 | дописан `run_after.txt` — контрольный полный прогон, 10 красных | 2 мин 30 с |
| 07:17:57.4 | артефакт записан на диск, 20782 симв. | **24.5 с** |
| 07:18:02 | коммит `a8ad93d` — артефакт в git | **19.9 с** |
| 07:18:21.9 | **ТАЙМАУТ 2700s → failed**, headless прерван | 0 |
| 07:19:20.3 | COMPLETE id=81 status=failed | +59 с |
| 07:19:31.3 | исход лёг в мозг (NOTE), подтверждён обратным чтением | +70 с |

Заход не завис и не сорвался — он **успел всё и не успел рассказать**: между последним коммитом и
ножом 19.9 секунды.

## 5. Отчёт задачи в каталоге отчётов

**Файла `…-task81.md` в `docs/artifacts/reports/` нет.** Последний файл за сегодня — задачи 80:

```
$ ls docs/artifacts/reports/ | grep 81
(пусто)
$ ls -la docs/artifacts/reports/ | tail -3
 8784 2026-08-21 12:05:10 2026-08-21-050510-task78.md
14239 2026-08-21 13:08:45 2026-08-21-060845-task80.md
```

**МИНА, из-за которой отсюда легко сделать ложный вывод.** Каталог отчётов — **не архив всех
отчётов, а спил переростков**: `result_spill.cap_result` (`result_spill.py:204`) при `n <= cap`
возвращает текст **байт-в-байт и файла не создаёт вовсе**. Файл появляется ТОЛЬКО когда отчёт
перерос потолок очереди 4500 символов — и тогда рядом в логе стоит `ОТЧЁТ ОБРЕЗАН`. Отчёты задач 77
и 79 сегодня тоже отсутствуют в каталоге, а обе закрыты `done`. **Поэтому пустота по 81 сама по себе
ничего не доказывает** — доказывает `run_timeout`: отчёта не было вообще, не только длинного.

Последние строки последнего живого отчёта (задача 80) — дословно, для образца формы, которой у 81
нет:

```
5. **Кода бота не тронуто ни строки**, бот не запускался, клиентам ничего не отправлялось, других
   узлов мозга не касались; красных операций не потребовалось. Не сделано (за следующей задачей):
   сверка, что в коде стои́т именно 180, а не «полгода» в ином виде.

RESULT: в узел мозга business_rules записан раздел «ГРАНИЦА ДЛИННОЙ АРЕНДЫ ЧИСЛОМ (решение
владельца 21.08.2026)» с дословной фразой «граница длинной аренды 180 суток» (1 вхождение); живое
чтение свежим процессом 60966 → 61978, прирост 1012 == длине вставки, обратимость и неизменность
через 62 с подтверждены; кода не тронуто; артефакт
docs/artifacts/2026-08-21-long-rent-boundary-180.md, коммит 1dfb625; журнал ARTIFACT+DONE записан.
```

## 6. Куда на самом деле делся итог: он в журнале, типом `NOTE`

Реестр `cowork_log.ledger` пополняется **только после обратного чтения** дока
(`cowork_log_append.py`: `ledger_add` стои́т ПОСЛЕ `readback_landed` и `spool_clear`). Обе строки про
81 в нём есть — значит обе физически лежат в мозге:

```
{"ts": "2026-08-21T06:33:32.630746+00:00", "line": "NOTE 2026-08-21 06:33 UTC: Orchestrator: взял задачу #81 (in_progress)"}
{"ts": "2026-08-21T07:19:31.289579+00:00", "line": "NOTE 2026-08-21 07:19 UTC: Orchestrator: задача #81 → failed · ⏱ НЕ ЗАКРЫТА, но В ОКНЕ ЗАДАЧИ ЕСТЬ РАБОТА [причина=run_timeout · таймаут прогона]: таймаут 2700s — headless прерван, задача не завершилась. СЛЕДЫ в окне 21.08 06:33–07:18 UTC: коммитов 3 (16c347b «голдены цены: корень «канал столбца J у…"}
```

Тело второй строки выехало файлом (638 символов при пороге 600) —
`docs/artifacts/journal/2026-08-21-071920-note.md`, дословно:

```
NOTE Orchestrator: задача #81 → failed · ⏱ НЕ ЗАКРЫТА, но В ОКНЕ ЗАДАЧИ ЕСТЬ РАБОТА
[причина=run_timeout · таймаут прогона]: таймаут 2700s — headless прерван, задача не завершилась.
СЛЕДЫ в окне 21.08 06:33–07:18 UTC: коммитов 3 (16c347b «голдены цены: корень «канал столбца J
упразднён» и корень ке»; c337973 «голдены цены: транспорт quote-блока и fail-safe черновика пе»;
a8ad93d «ARTIFACT шестнадцать красных разобраны по корням 21.08 -> do»). Формальное закрытие не
состоялось — НЕ переделывай вслепую: сверь эти следы с заданием и закрой руками. (Окно, а не
авторство: в него попадают и параллельные сессии ПК.)
```

**Демон уже сказал ровно то, что владелец пришёл выяснять — и заранее предупредил «не переделывай
вслепую».** Строка не потерялась; её не нашли, потому что искали по типу `DONE`/`failed`, а
оркестратор пишет свои исходы типом `NOTE`.

**Ноль отложенных записей:** файла `cowork_log.spool` на диске нет вовсе — ни одна журнальная запись
за сутки не осталась недосланной. Класса «потеря строки гонкой перезаписи» здесь не было.

## 7. Почему экран показал очередь пустой в 06:51 UTC

В 06:51 UTC задача 81 была **`in_progress`** (взята 06:33:21, убита 07:18:21) — то есть экран
показал пустоту при живой незакрытой задаче. Это отдельный, уже разобранный класс:
[`docs/artifacts/2026-08-19-queue-empty-lies.md`](2026-08-19-queue-empty-lies.md) — строка экрана
живёт в коде VPS (`devbot.py`), а не здесь. Настоящим свидетелем состояния очереди тот экран быть не
может; свидетель — расписки демона из §4.

## 8. Что ЧИНИТЬ НЕ НАДО и что остаётся

Разведка ничего не чинила и не доделывала — по заданию. Что вытекает фактами:

* **Переотправлять задачу 81 целиком НЕЛЬЗЯ**: её работа в дереве, коммиты в `main`. Слепая
  переотправка заставит следующий заход разбирать уже разобранное и, хуже, править уже правленые
  голдены вторым слоем. Ровно от этого предупреждает сама запись демона.
* **Закрыть 81 руками** как выполненную по существу (разбор 16/16, починка 6/16) — решение владельца,
  здесь не принималось.
* **Остаток работы объявлен самим заходом**, а не потерян: Г4-B (2 теста, корень в тесте) и Г2
  (3 теста, счёт готов: 307 ฿/день · 1535 ฿) — механические, первые кандидаты на следующий заход;
  Г4-A (3 теста) — корень в коде смоука (`suggest.py:7353`, `avail=None` прошито литералом), нужен
  отдельный заход; Г3 (2 теста) — **ждёт решения владельца**: заводить ли ось поколений XMAX 300 в
  правиле (сегодня одно имя покрывает две строки прайса с разрывом 18.7 %).
* **Открытый класс, который дороже самой задачи 81** (найден заходом в §0, не чинен): часть тестов
  пути цены ходит в живой Bridge, поэтому счёт красных достоверен **только при отвечающем мосте**,
  а «стало лучше/хуже» по одному прогону судить нельзя. 45 красных из 61 в том прогоне были сетевым
  шумом.
* **Родовой класс задачи 81 — бюджет прогона**: `TASK_TIMEOUT=2700s`, и заход упёрся в него на
  выдаче отчёта, потеряв ровно отчёт. Сегодняшние соседи укладывались (77 — 2191 с, 78 — 2039 с,
  79 — 1770 с, 80 — 585 с), 81 — 2700.61 с, то есть первый упор за сутки. Правка порога — не
  разведка, здесь не делалась.
