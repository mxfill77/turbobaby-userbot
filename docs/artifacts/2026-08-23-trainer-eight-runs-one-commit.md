# Восемь прогонов тренажёра на ОДНОМ коммите `1ded105` — 23.08.2026, вечер

**Главный ответ: чистых «12 из 12» — 7 из 8. Серия НЕ оборвана (вершина не двигалась ни разу),
но и восьми чистых НЕТ.** Красное вышло ровно одно — прогон 28, **кейс 5 «новичок + мощный байк
(A.3)», чек `обязательное упоминание`**. Кейс 6 «опыт не упомянут (A.2)» зелен **8 из 8**.

**Порог, названный ЗАРАНЕЕ и не пересматриваемый: «хоть один красный — дефект „опыт“ жив и ключа
нет». Красный есть. Значит по названному порогу — дефект жив, ключа нет.** Ветка «восемь чистых →
вероятность совпадения 23 %» не наступила и не применяется.

---

## 0. Премисы задания — проверены первым действием, все три ВЕРНЫ

| премиса | чем проверена | вердикт |
|---|---|---|
| четыре прогона сняты, все «12 из 12», все на `1ded105` | пересчёт по файлам `_scratch_livenum4_0823/live_run2{1..4}.json`: 12/12 ×4, коммит `1ded105` ×4 | **верна** |
| вершина не двигалась, дерево чистое, промахи границ ноль | `head_moved` пуст 4/4, `tree_dirty` false 4/4, сумма промахов 0 | **верна** |
| дефект «опыт» ранее давал красное примерно раз из шести | прежний корпус `_scratch_livenum_0823/live_run1{1..6}.json`: кейс 6 красен ровно в прогоне 11 → **1 из 6 (16.7 %)** | **верна** |

Состояние на входе добора: `HEAD` = `1ded105`, `git status --porcelain -uno` пуст.

---

## 1–2. Прогоны 25–28 (добор), по одному, синхронно, улика на диск сразу

| прогон | на диск | число | красные поимённо | неизв. | вершина сдвинулась | дерево грязным | промахи границ (мост/док/голова/дверь6) | коммит | сек |
|---|---|---|---|---|---|---|---|---|---|
| 25 | 20:29:16 | **12 из 12** | нет | нет | **НЕТ** | нет | 0 / 0 / 0 / 0 | `1ded105` | 245.2 |
| 26 | 20:33:57 | **12 из 12** | нет | нет | **НЕТ** | нет | 0 / 0 / 0 / 0 | `1ded105` | 274.3 |
| 27 | 20:38:41 | **12 из 12** | нет | нет | **НЕТ** | нет | 0 / 0 / 0 / 0 | `1ded105` | 274.8 |
| 28 | 20:43:07 | **11 из 12** | **кейс 5 «новичок + мощный байк (A.3)»: `обязательное упоминание`** | нет | **НЕТ** | нет | 0 / 0 / 0 / 0 | `1ded105` | 253.8 |

Останавливаться не пришлось: `verify_head` дал пустой `head_moved` во всех четырёх, серия цела.

---

## 3. Свод всех восьми — ГЛАВНЫЙ ОТВЕТ

**Чистых «12 из 12» — 7 из 8.** Красное одно, в прогоне 28, у кейса 5.

| прогон | 21 | 22 | 23 | 24 | 25 | 26 | 27 | 28 |
|---|---|---|---|---|---|---|---|---|
| число | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | 12/12 | **11/12** |
| красные | — | — | — | — | — | — | — | **кейс 5: `обязательное упоминание`** |

Покейсно за восемь прогонов:

| # | кейс | зелёных из 8 | исходы |
|---|---|---|---|
| 1 | первый контакт, одна модель | 8/8 | ZZZZZZZZ |
| 2 | прайс-интент «все модели и цены» | 8/8 | ZZZZZZZZ |
| 3 | модель + даты + гео-пин Раваи | 8/8 | ZZZZZZZZ |
| 4 | только про депозит — контрпример прайса | 8/8 | ZZZZZZZZ |
| 5 | **новичок + мощный байк (A.3)** | **7/8** | ZZZZZZZ**R** |
| 6 | **опыт не упомянут (A.2)** | **8/8** | ZZZZZZZZ |
| 7 | месяц+ — кап месячного тарифа | 8/8 | ZZZZZZZZ |
| 8 | EN-клиент — язык ответа | 8/8 | ZZZZZZZZ |
| 9 | фото паспорта маркером — засчитано трекером | 8/8 | ZZZZZZZZ |
| 10 | один день — назван минимальный срок | 8/8 | ZZZZZZZZ |
| 11 | «есть ли в наличии?» без данных Bridge | 8/8 | ZZZZZZZZ |
| 12 | тайский в реплике — strip_thai, ฿ сохранён | 8/8 | ZZZZZZZZ |

Флап за восемь прогонов ровно у одного кейса — **5**. Число живых чеков у каждого кейса одинаково
во всех восьми прогонах (у кейса 5 — 15), то есть ни один чек не потерялся и не был снят;
исходов «неизвестно» — ноль во всех восьми.

### Красное дословно — ОЖИДАНИЕ / ФАКТ (прогон 28, кейс 5)

* **чек:** `обязательное упоминание`
* **ожидание:** в ответе есть одно из: `менеджер, коллег, опыт, ездил, водил`
* **факт:** нет ни одного
* **черновик (160 симв.):** «Здравствуйте! Уточню детали у команды и подберём вариант вместе, а по
  датам подскажите — с какого числа планируете и на сколько дней? [собрано: модель ✅ срок ✅]»

Голова **эскалацию сделала**, но словом «команда», которого в списке ожидания нет, и про опыт не
спросила вовсе. Судить сам чек мне запрещено заданием (чеки не трогать, вердиктов не выписывать) —
факт назван как есть.

---

## 4. Кейс с вопросом про опыт — прямо

**Кейс 6 «опыт не упомянут (A.2)» зелен во ВСЕХ ВОСЬМИ прогонах.**

И тем не менее **дефект жив, и это законный и полезный исход** — по трём фактам, а не по
ощущению:

1. **Порог был назван заранее и не пересматривается:** «хоть один красный — дефект „опыт“ жив и
   ключа нет». Красный есть.
2. **Упал ЧЕК С ТЕМ ЖЕ ИМЕНЕМ и из того же правила-семейства.** `обязательное упоминание` — это
   тот самый чек, которым кейс 6 краснел до починок (прогон 11, коммит `59edfdb`). Кейсы 5 и 6 —
   это A.3 и A.2, две половины одного правила об опыте; списки ожидания у них пересекаются по
   словам `опыт, ездил, водил`. Разброс головы на обязательном упоминании никуда не делся — он
   просто выпал на другой половине правила.
3. **Наблюдённое РОВНО совпадает с ожидаемым при НЕИЗМЕННОМ темпе дефекта.** При прежнем темпе
   1 из 6 (16.7 %) за восемь прогонов ожидалось **1.33 красных** — вышло **1**. Данные не дают ни
   малейшего основания говорить, что темп упал.

---

## 5. Один ли коммит и чистое ли дерево

**Да и да: все восемь прогонов на `1ded105`**, `head_moved` пуст 8 раз из 8, `head_after` =
`1ded105` 8 раз из 8, `tree_dirty` = false 8 раз из 8. Коммит фиксировался `bind_head` на входе
КАЖДОГО прогона и сверялся `verify_head` на выходе КАЖДОГО. Промахи границ — **ноль** во всех
восьми (мост 129 вызовов/прогон, доки 12, шестая дверь 9 вызовов и 17 спросов сторожа;
`playbook_sha` `fe37713e14cd367b` одинаков во всех восьми).

---

## 6. Ветка «восемь чистых» — НЕ наступила

Восьми чистых нет (их семь), поэтому названную заранее вероятность совпадения 23 % применять не к
чему. Для полноты: (5/6)⁸ ≈ **23.3 %** — это была бы вероятность увидеть восемь чистых подряд при
неизменном темпе дефекта. Ключом ни семь, ни восемь чистых не являются и здесь не называются;
решение о ключе принимает владелец.

---

## 7. Одной строкой

**Дефект «опыт» ЖИВ: за восемь прогонов на одном коммите он выпал один раз — не на кейсе 6
(тот зелен 8/8), а на его половине A.3, кейсе 5, и ровно тем же чеком `обязательное упоминание`.**

---

## 8. Побочное наблюдение (не дефект): пояс наличия отработал на живом черновике

В прогоне 27 при генерации кейса 5 в поток вышла строка
`guard_availability VIOLATION [initial] … признаки=[{'kind': 'avail_neg', 'raw': <7 символов>}]` —
свежепоставленный пояс наличия (`166a81a`) поймал утверждение о наличии в ПЕРВИЧНОМ черновике и
продукт себя переписал: кейс остался **зелёным**, время генерации выросло до 42.7 с против обычных
~21 с. Это единственный такой случай за восемь прогонов, и он показывает пояс в работе, а не поломку.

Оговорка о точности: сам пойманный фрагмент (`raw`) вышел в консоль мохибейком (поток консоли
cp1251), поэтому дословно он здесь НЕ цитируется — читаемы только имя признака `avail_neg` и
маркер `[initial]`. Первичный черновик нигде не сохраняется, восстановить его текст постфактум
нечем; в улике `live_run27.json` лежит уже переписанный, зелёный черновик.

---

## 9. Границы честности

* Число снято при **живой голове** и **замороженной сети** (границы 1–5 и шестая дверь из снимков
  `_scratch_gate6_0823/pin_snapshot_0823.json` и `gate_snapshot.json`, сняты 23.08 в 01:35/01:44,
  открывались ТОЛЬКО НА ЧТЕНИЕ). Это та же конфигурация, что у прогонов 21–24 и у прежнего захода
  «3 из 6», — ряды сравнимы; живым прогоном продукта целиком это не является.
* Порог не пересматривался и под результат не подгонялся: он записан выше дословно и применён как
  есть, хотя красное выпало не на том кейсе, который ожидался.
* Правок не делалось никаких: ни бота, ни чеков, ни кейсов, ни ожиданий, ни порогов, ни пинов, ни
  ворот. Снимки не переснимались (`record`-режима в раннере нет, `save()` не зовётся), вердикт не
  писался (`pc_orchestrator.client_trainer_green.json` не тронут — mtime 31.07 19:40), ворота не
  открывались, детям и клиентам ничего не отправлялось, ничего не удалено, коммитов не делалось.
* Дефект «опыт» не трогался — только измерялся.
* В фоне не запускалось ничего: четыре прогона выполнены синхронно, по одному, каждый результат
  лёг на диск ДО начала следующего (20:29:16, 20:33:57, 20:38:41, 20:43:07).
* Временное место названо явно: **`_scratch_livenum4_0823/`** — раннер `probe_livenum4.py`, свод
  `svod4.py`, разбор красного `red_detail.py`, улики `live_run21..28.json`; каталог untracked.
* Первая половина серии разобрана в
  [`docs/artifacts/2026-08-23-trainer-live-number-after-instrument-fixes.md`](2026-08-23-trainer-live-number-after-instrument-fixes.md).
