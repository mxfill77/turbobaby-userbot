# Эталон менеджера рядом с ответом бота: один живой кейс из обезличенной базы собран, цена единицы замерена

ETALON menedzhera kejs 1709

**Дата:** 17.09.2026, 19:20–19:40 по Пхукету · **Полоса:** ПК · **Задание:** ящик Штаба, ключ
`63-n-etalon-menedzhera-kejs.1709` (док `shtab_task_63-n-etalon-menedzhera-kejs.1709`, file id `1NYIThgOfLNWkRIgMX-1tURUCLLqSqXue`)
**Коммит кода:** **`881f421`** (`exam_show.py` +98/−3, `test_exam_show.py` +89, `.gitignore` +4).
**Временное место, названо явно:** `_scratch_zhivoy_pervyy_0917/` (вне git). Черновик доклада лежит в `tmp/pc_report/`.
Ничего не удалялось.

**Переписки в этом файле нет ни строкой.** Здесь только имена полей, длины, метки обезличенной базы
(`дN·рM` — номер диалога и реплики) и числа. Сам кейс с текстом лежит на диске, в `exam_live/`. Каталог
под `.gitignore`: база переписки в git не идёт, и снимок с её текстом тоже не идёт.

---

## FACT — живые числа

| что доказано | число / факт |
|---|---|
| коммит кода | `881f421` в `git log` |
| сцепка «первое сообщение → ответ менеджера» | строгая **12 из 710**; с пропуском шаблонов-автоответов **131 из 710**, не сцепилось **579** |
| выбранный кейс | вопрос **д206·р0**, эталон **д206·р1**: вопрос **48** знаков, эталон **29**, ответ человека через **146 с** |
| снимок | `exam_live/shots/case-1@881f421.json`, 6236 байт, `built_at` 2026-09-17T12:35:29Z |
| ответ бота / подсказки критика | **713** знаков / **3** подсказки, исход `hints`, критик ждал 93.6 с при пределе 257 |
| карточка | кнопок **6**, раскладка `[1,3,2]` · длина **2896 знаков / 2903 ед. UTF-16** (запас до 4096: 1193) · версия правил **строкой** `"7d19b1db352e8f76"` |
| эталон в карточке | отдельный блок «👤 ЭТАЛОН — ОТВЕТ ЧЕЛОВЕКА», стоит **после** блока бота; блок бота равен `draft` побайтно; эталон в карточке встречается **1** раз и внутри блока бота его нет |
| ловушка | попыток отправки **0**, попыток сети **0** при сборке карточки; карточек владельцу на круге заморозки **0** |
| цена единицы | отбор **0.58 с** + заморозка **400.3 с** + сборка карточки **0.18 с** ≈ **401 с** |
| прежние кейсы | sha256 20 снимков и `trainer_cases.json` до и после — **совпали все 21**; карточки 20 снимков прежним кодом (`41f88c0`) и новым — **совпали 20 из 20**, кнопки тоже 20 из 20 |
| набор до → после | `test_exam_show`: **134 OK → 140 OK** |
| боевые стол / уроки / журнал | `exam_session.json` 133 байт, mtime `1789211402` · `lesson_store.tsv` 7223 байт, mtime `1789127894` — те же, что до захода · `exam_verdicts.tsv` не существует |

---

## 0. Признак сделанности

Первым действием поискал артефакт по адресу: `docs/artifacts/*ETALON*`. Файла нет, работа начата с нуля.

## Премисы — проверены первым действием

| премиса | исход |
|---|---|
| **П1** 17 кейсов собраны из тестов тренажёра, а не из живых вопросов клиентов | **ВЕРНА ПО СУТИ, с уточнением.** Поле `source` у 17 кейсов: живой тест тренажёра (1, 10), фраза инцидента #51 и её части (2, 4), проба runLiveSmoke (3), голдены (5, 6), «формат живой заявки» без адреса (7, 8, 9, 11, 12), EN-зеркала (13–16). Дословная реплика живого клиента с адресом одна: кейс 17, взят из лога userbot, даты в нём заменены. **Из обезличенной базы — 0 из 17. С ответом менеджера рядом — 0 из 17.** Слово владельца 12.09 об источнике на диске есть только цитатой в коде (`style_examples.py`, шапка; `suggest.py:580`: учиться надо по скачанной переписке с клиентами). Отдельного документа поправки в `docs/` нет, поиск дал 0. Мозг не читал. |
| **П2** на диске есть обезличенная база со стабильными метками, и она годится для сцепки | **ВЕРНА.** `client_chats.anonstable.jsonl`: 710 диалогов, 31 245 реплик, из них компании 15 845 и клиента 15 400. Поля записи: `peer_name, peer_username, peer_phone, peer_id, messages`. Поля реплики: `date, who, text`. Отпечаток `36d68c71d6019fe2` тот же, что у замера 12.09, значит база с тех пор не менялась. Соседний `client_chats.anon.jsonl` с позиционными метками не открывался. **Для сцепки годится, но надёжно сцепляется только часть: см. §5.** |
| **П3** карточка показывает вопрос, ответ, причину и подсказки, а места под эталон нет | **ВЕРНА.** `card_text` на `41f88c0` выводит шапку, КЛИЕНТ, БОТ, ПОЧЕМУ, ОБРАЗЦЫ, подсказки и легенду кнопок. Слов «эталон» и «reference» в `exam_show.py@41f88c0` — **0**, в `trainer_cases.json` — **0**. |

---

## 1. Отбор

**Метка:** вопрос **д206·р0**, эталон **д206·р1**. Вопрос **48** знаков, эталон **29** знаков, ответ одной репликой.
Ответ человека пришёл через **146 с**. Диалог февраля 2026 года, в нём 7 реплик.

**Правила сцепки** (скрипты `_scratch_zhivoy_pervyy_0917/otbor.py`, `sceplenie.py`, `vybor.py`). Скрипты печатают
только числа и метки.
1. Диалог открывает клиент. До его первого сообщения у компании могут стоять только шаблоны.
2. Первое обращение — одно сообщение, а не серия.
3. В вопросе 8–400 знаков, нет переноса строки, нет метки обезличивания и нет ссылки.
4. Эталон — первая **не шаблонная** реплика компании и все следующие подряд реплики компании. Она пришла
   раньше второго сообщения клиента и не позже чем через 24 ч.
5. В эталоне нет метки обезличивания и нет ссылки.

Шаблоном считается реплика компании, которая дословно повторяется в 10 и более диалогах; таких 49.

**Чем кейс типичен:**
- **Форма.** Из 131 сцепившегося первого сообщения форма «аренда, без модели, дат, цены и срока, по-русски»
  встречается у **20 (15.3 %)**. Это самая частая форма с содержанием. Чаще неё только форма «без тем» (26,
  19.8 %): по длинам (медиана 19 знаков) это приветствие без вопроса, судить там нечего.
- **Длины.** Медианы формы: вопрос 51, ответ 49. У кейса 48 и 29. По всем 131: вопрос 52, ответ 48.
- **Чистота сцепки.** Кейс проходит и строгую сцепку (12 диалогов), и ослабленную (131). Шаблона между
  вопросом и ответом человека нет.
- **Образцы.** Замок «свой диалог в образцы не идёт» (`style_examples`, замок 2) сработал: вопрос лежит в
  индексе дословно. В снимке 5 образцов: д373·р4, д88·р10, д208·р1, д213·р27, д401·р9. **Д206 среди них нет**,
  то есть свой эталон голова не видела и через образцы.

Первым проходом (строгим) выбирался д57. Он сцеплен и во втором проходе, и форма у него та же, но вопрос
длиннее (95). Файл первого прохода сохранён копией: `_scratch_zhivoy_pervyy_0917/cases.prohod1-d57.json`.

## 2. Эталон в снимке и в карточке — код `881f421`

- **Снимок.** Поле `reference` лежит рядом с `draft`, а не внутри него. Поля эталона: `who, text, mark, parts,
  gap_sec, base`. Эталон приходит из корпуса кейса (`exam_live/cases.json`, поле `reference`). Кейс без поля
  ключа не получает: у основного набора состав полей снимка прежний.
- **Три замка от смешения.**
  1. `freeze` отдаёт голове кейс **без** поля `reference` (`head_case`). Транскрипт строится из того же кейса.
  2. Критику эталон не передаётся.
  3. В карточке эталон идёт своим блоком `👤 ЭТАЛОН — ОТВЕТ ЧЕЛОВЕКА (менеджер, тогда, д206·р1, база …). Это
     НЕ ответ бота, бот его не видел:` после блока `🤖 БОТ` и до `📌 ПОЧЕМУ`. Если поле есть, а текста нет,
     карточка говорит «ответ менеджера … в базе НЕ сцеплен — сравнивать не с чем».
- **Старые снимки.** Когда поля нет, блока нет вовсе, и карточка совпадает байт в байт. Это проверено на всех
  20 снимках: прежний код против нового.
- **Живой набор — `exam_live/`** (`cases.json`, `shots/`), под `.gitignore`. В CLI добавлен ключ `--live`,
  который разрешает только `--freeze`, `--card` и `--slots`.
- **`--live --show` и `--live --tap` отказывают словами (rc 2).** Колбэк `exam:<слово>:N` агент несёт в дверь
  **основного** набора, а журнал вердиктов и стол у наборов общие. Тап по живому кейсу 1 лёг бы вердиктом кейсу 1
  тренажёра.
- **Почему не дописан в `trainer_cases.json`.** Отпечаток корпуса держит все 20 снимков основного набора. Кейс,
  дописанный туда, отказал бы показу каждого из них («черновик собран на корпусе …, а на диске сейчас …»). Кроме
  того, в git попал бы текст переписки.

**Тесты:** новый класс `TestTheReferenceStandsApart`, 6 проверок.
- Снимок без поля: блока нет, карточка та же.
- Блок эталона стоит отдельно, после бота, с пометкой «человек»; блок бота равен `draft`.
- Пустой эталон называется «не сцеплен».
- `freeze` кладёт эталон в снимок, а голова и критик его не получают.
- Кейс без поля ключа не получает.
- `--live` с показом, тапом, журналом или без действия отказывает и ничего не переключает.

## 3. Кейс собран целиком

`venv/Scripts/python.exe exam_show.py --live --freeze --case 1`: rc 0. Лог из 3 строк лежит в
`_scratch_zhivoy_pervyy_0917/freeze-live-1.log`.
- `✅ черновик кейса 1 собран и сохранён: exam_live\shots\case-1@881f421.json`
- `🔎 критик: подсказок 3, ждали 93.6 с (предел 257).`
- `📮 карточек владельцу на круге сборки: 0 — ловушка стояла, ни одна ветка карточку не подняла`

Traceback, WARNING, таймаутов, `season_gate` и `noprice` в логе — 0.

**Поля снимка:** `built_at, case, commit, corpus, critic, draft, examples, hints, lang, name, note, owner_cards,
question, question_raw, reference, rules, total`. Длины: вопрос 48, ответ бота 713, эталон 29, записка 1279.
Сырой ответ критика — 1134 знака, `tmp/exam_critic/case-1-20260917T123529Z.txt`.
Вопрос снимка совпал с вопросом базы, эталон снимка — с эталоном корпуса. Отпечаток корпуса живого набора
`e68a554d1c38e960` совпадает у снимка и на диске.

**Карточка собирается** (`_scratch_zhivoy_pervyy_0917/karta.py`: те же `card_text`/`markup`/`card_callbacks`,
которые зовёт показ). Ловушка сети и отправителя стояла до первого импорта `exam_show`.

| мерка | значение |
|---|---|
| кнопок | **6**, `[1,3,2]`: `exam:ok:1`, `exam:h1:1`, `exam:h2:1`, `exam:h3:1`, `exam:own:1`, `exam:go:1` |
| длина | **2896 знаков / 2903 ед. UTF-16**, запас до 4096 — 1193 |
| версия правил | тип `str`, **`"7d19b1db352e8f76"`** |
| отпечаток текста карточки | sha256[:16] `81607ece7f213372` |
| порядок блоков | БОТ < ЭТАЛОН < ПОЧЕМУ — ДА; метка «ОТВЕТ ЧЕЛОВЕКА» — ДА; «НЕ ответ бота» — ДА |
| замок агента (только чтение лока и исходника) | ДА. Живой показ этим всё равно не открыт: см. §2, отказ `--live --show` |
| ловушка | отправок **0**, сети **0** |

**Форма ответа бота против эталона — только числами:**
- вопросительных знаков: бот 1, эталон 0;
- цена с валютой: у бота есть, у эталона нет;
- бот длиннее эталона в **24.6 раза** (713 против 29).

Кто прав, заход не судил: это решение владельца, и эталон теперь стоит у него перед глазами. Посмотреть
карточку на ПК: `venv/Scripts/python.exe exam_show.py --live --card --case 1`. Ничего не отправляется.

**Одна оговорка про первый прогон проверки.** Сначала он дал «блок бота = draft: False». Причина в проверке,
а не в карточке: ответ бота сам содержит пустые строки, а скрипт резал блок по первой из них. После правки
нарезки (отрезок от «🤖 БОТ» до «👤 ЭТАЛОН») — True. Вывод первого прогона сохранён:
`_scratch_zhivoy_pervyy_0917/karta-prohod1.txt`.

## 4. Цена единицы и арифметика пересборки

**Единица «отбор → готовый снимок»:**
- отбор 0.58 с (проход по всей базе, один раз на набор);
- заморозка **400.3 с** стенных (`1789648130.13 → 1789648530.43`);
- из неё критик 93.6 с; голова вместе с импортами и подбором образцов — разность, **≈ 306.7 с**;
- проверка карточки 0.18 с.

**Итого ≈ 401 с.**

Эта единица дороже всех прежних замеров полосы: 68 · 91 · 206 · 216 (17.09) и 303 (12.09). Дороже и оценки
«худшая по построению ≈ 357 с» из 63-g. **Почему голова заняла ≈ 307 с — НЕИЗВЕСТНО.** Лог заморозки из 3 строк
разбивки по вызовам не даёт, и отдельно я её не мерил.

**Пересборка всего набора** (потолок захода 2700 с, доля 0.7 — 1890 с; единицы идут последовательно):

| объём | по этой единице (401 с) | по медиане шести замеров (≈ 211 с) | по лучшей (68 с) |
|---|---|---|---|
| **17 кейсов** (размер экзамена) | 6817 с — **НЕ влезает**: 4 единицы на заход (1604 с), **5 заходов** | 3587 с — не влезает: 8 на заход, **3 захода** | 1156 с — влезает |
| **131 кейс** (все сцепившиеся) | 52 531 с ≈ 14.6 ч — **33 захода** | 27 641 с — 17 заходов | 8908 с — 5 заходов |

Все 17 влезли бы в один заход только при единице ≤ 111 с (1890 / 17). Из шести замеров так было дважды.
**Ответ: пересборка набора из 17 в один заход не влезает, нужно 3–5 заходов.**

## 5. Честная ветка — сколько сцепилось

Эталон не выдуман: у выбранного кейса сцепка проходит оба прохода. По всей базе:

**Строгий проход** (первый ответ компании не шаблон, компания до клиента не писала): **сцепилось 12, не сцепилось 698.**

| почему не сцепилось (строго) | диалогов |
|---|---|
| первой пишет компания | 285 |
| первый ответ компании — шаблон (дословно в 10+ диалогах) | 252 |
| в вопросе перенос строки | 100 |
| первое обращение — серия из 2+ сообщений | 31 |
| вопрос короче 8 или длиннее 400 знаков | 20 |
| в вопросе метка обезличивания или ссылка | 5 |
| клиенту не ответили | 4 |
| в ответе метка обезличивания или ссылка | 1 |

**Шаблон — это часто автоответ, а не человек.** Задержка шаблонной реплики сразу за клиентом: медиана 15 с,
быстрее 15 с — 900 из 1793. Для сравнения, не шаблонная реплика: медиана 62 с, быстрее 15 с — 1187 из 7014.
Поэтому второй проход пропускает шаблоны и ищет ответ человека за ними.

**Второй проход: сцепилось 131, не сцепилось 579.** Из сцепившихся шаблон стоял между вопросом и человеком у 84,
не стоял у 47.

| почему не сцепилось (второй проход) | диалогов |
|---|---|
| клиент написал снова раньше, чем ответил человек | 234 |
| до первого клиента компания писала сама, не шаблоном | 185 |
| в вопросе перенос строки (в транскрипте станет ⏎, и замок своего диалога не сработает) | 67 |
| первое обращение — серия из 2+ сообщений | 59 |
| после первого обращения нет ответа человека | 17 |
| в ответе метка обезличивания или ссылка | 11 |
| вопрос короче 8 или длиннее 400 знаков | 3 |
| в вопросе метка обезличивания или ссылка | 3 |

**Граница, названная вслух.** «Шаблон» здесь определён частотой текста, а не признаком отправителя: у базы нет
поля «кто из компании писал». Короткий частый ответ живого менеджера тоже попадает в шаблоны. Отсюда 131 — это
счёт по правилу, а не доказанное число ответов людей. Строгие 12 от этой границы не зависят.

## 6. Наборы до и после

| набор | до (на `41f88c0`) | после (`881f421`) |
|---|---|---|
| `test_exam_show` (в нём и `exam_reshow`) | **134 OK**, 2.8 с | **140 OK**, 2.6 с |

Других импортёров `exam_show` в git нет (`git grep`: только `exam_reshow.py` и `test_exam_show.py`). Новых
импортов правка не завела, поэтому замыкание клиентского контура не менялось, и `test_client_contour` не гонялся.
Полный гейт не гонялся, как велено. Выводы наборов: `_scratch_zhivoy_pervyy_0917/tests-do-exam_show.txt`,
`tests-posle-exam_show.txt`.

## 7. Бюджет

Задача создана в **19:20:17** (`1789647617`). Этапы:
- код закоммичен в 19:28:44 (**~507 с**);
- заморозка шла 19:28:50 → 19:35:30;
- проверки закончились в 19:36:03;
- запись артефакта начата в **19:36:52** (`1789648612`) — **995 с = 0.37 потолка**, ниже доли 0.7 (1890 с).

Коммит артефакта и журнал идут следом. Их секунды — в отчёте захода.

## 8. Гард — что было

Один отказ: «высшая цена, а ОБЪЕКТ НЕ НАЗВАН … код из stdin». Команда, которая его вызвала, запускала скрипт
проверки и считала секунды через `echo … | bc`, и гард принял трубу за код в stdin. Обхода не искал: та же
проверка без `bc` прошла. Файлы гарда не открывались и не правились.

## 9. Чего заход НЕ сделал

- **Живого показа владельцу нет.** Живой набор к агенту не подключён (отказ `--live --show`). Для показа нужны
  три вещи: свой префикс или смещение номера кейса в колбэках, правка разбора `pc_agent` и «обновись» агенту,
  свои журнал и стол для живого набора. Оценка — 1 заход, не делалось.
- Наружу не ушло ничего. Процессы, агента и демона не трогал. Клиентского бота не правил, детей не поднимал.
- Прежние 17 кейсов и 20 снимков не тронуты (sha256). `trainer_cases.json` не менялся.
- `.env` и конфиги не читал, секретов не печатал. Цен, прайса и депозитов не менял, в таблицы не писал. Лист
  «Календарь бронирования» и брони своей командой не открывал. **Оговорка:** путь головы при заморозке — тот
  же, что у всех прежних снимков (`trainer_run.run_case`: голова, книга правил, чтение прайса мостом там, где
  его зовёт пайплайн). Звал ли он мост на этом вопросе без модели и дат, лог не показывает.
- Разбивку ≈ 307 с головы не мерил (§4).

## 10. Временное место `_scratch_zhivoy_pervyy_0917/` (ничего не удалялось)

| файл | что |
|---|---|
| `otbor.py`, `otbor.txt` | строгий проход (12/710) и первая запись кейса |
| `sceplenie.py`, `sceplenie.txt` | задержки шаблонов и второй проход (131/710) |
| `vybor.py`, `vybor.txt`, `cases.prohod1-d57.json` | выбор д206 и запись `exam_live/cases.json`, копия первого прохода |
| `freeze-live-1.log`, `freeze-time.txt` | вывод и стенное время заморозки |
| `karta.py`, `karta.txt`, `karta-prohod1.txt`, `exam_show_41f88c0.py` | проверка карточек (старые прежним кодом против нового, живая с ловушкой) |
| `tests-do-exam_show.txt`, `tests-posle-exam_show.txt`, `sha-do.txt`, `sha-posle.txt` | наборы до и после, хеши старых снимков и корпуса |

Кейс на диске: `exam_live/cases.json` и `exam_live/shots/case-1@881f421.json`, оба под `.gitignore`.

ETALON menedzhera kejs 1709
