69b CHISTOEDEREVO 2009

# Предмет решения уже в коммите, а дерево держит ЧУЖАЯ правка: самообновление демона ЗАБЛОКИРОВАНО файлом `io_utf8.py` соседнего захода, и по заданию его трогать нельзя. Законная ветвь названа адресом, чужое не тронуто ни одной командой

дата: **2026-09-20** · полоса: ПК · ключ Штаба 69b.2009 · время местное (Пхукет, UTC+7)

FACT: предмет захода **уже закоммичен** — `git show --stat 336a6099` даёт
`review_send_run.py | 20 +++`, `test_review_send.py | 100 +++`,
`docs/artifacts/2026-09-20-68z-MODELPOLE-2009.md | 223 +++`. Своего некоммиченного в дереве **нет**
(`git status --porcelain -uno` → `review_send_run.py` отсутствует). Самообновление при этом
**заблокировано**: `pc_orchestrator.log:5028, 5055` — два отказа за сутки, оба
«файлы: **io_utf8.py**», последний **20.09 19:50:06**. Файл чужой (заход 68v, входная полоса
UTF-8), в коммит не взят. Временное место захода — `tmp/chistka_dereva/` (заведено пустым,
ничего в нём не создавалось и не удалялось); черновик доклада — `tmp/pc_report/`.

---

## 0. Признак сделанности

Файла `docs/artifacts/2026-09-20-69b-CHISTOEDEREVO-2009.md` на диске **не было**
(`ls` → `No such file or directory`). П.2 и п.4 не отвечены ничем — заход начат с нуля.
**Прочитано по адресу.**

## Премиса Штаба — проверена первым действием после п.0

| что утверждает премиса | вердикт | чем (адрес) |
|---|---|---|
| правка ручки модели канала лежит в рабочем дереве **НЕ закоммиченной** | **ОПРОВЕРГНУТА** | `git show --stat 336a6099` — `review_send_run.py` (+20) и `test_review_send.py` (+100) **в коммите** (20.09, заход 68z). В `git diff --name-only HEAD` этих имён **нет** |
| её артефакт пишет, что самообновление подхватит новый код только после коммита | **ВЕРНА по сути, но условие уже выполнено** | `docs/artifacts/2026-09-20-68z-MODELPOLE-2009.md` лежит В ТОМ ЖЕ коммите 336a6099 — артефакт и правка легли одним действием |
| дерево при этом грязно и держит самообновление | **ВЕРНА, но виновник ДРУГОЙ** | грязны 3 файла, ни одного моего; держит `io_utf8.py` — файл чужого захода 68v |

Сентябрьское основание премисы (103 часа и 69 недоехавших коммитов,
`docs/artifacts/2026-09-17-GRYAZNOE-derevo-ne-primenyaet-1709.md`) относится к другому файлу —
`pretool_guard.py`, и тот эпизод закрыт: последняя его строка отказа **17.09 17:08:01**
(`pc_orchestrator.log:1789`), сегодняшние две называют `io_utf8.py`. **Класс тот же, файл другой.**

## 1. Состояние дерева числом (прочитано `git diff --numstat` / `--stat`, замок индекса не нужен)

Отслеживаемых файлов изменено против HEAD — **3**, строк **+170 / −4**:

| файл | строк | mtime | чей |
|---|---|---|---|
| `exam_show.py` | +14 / −3 | 20.09 16:33:57 | **чужой** |
| `io_utf8.py` | +40 / −0 | 20.09 16:46:30 | **чужой** |
| `test_utf8_output_guard.py` | +116 / −1 | 20.09 16:40:59 | **чужой** |

Индекс **пуст**: `git diff --cached --stat` → ни строки, у всех трёх статус ` M` (правка только в
рабочем дереве). Untracked-каталоги (`_scratch_*`, `tmp/`) в счёт не идут и войти не могут —
ворота демона считают грязь по `git diff --name-only HEAD` (`pc_orchestrator.py:7798`), а там
untracked нет по определению.

**Чьё это — вывод, а не запись из реестра.** Правка называет себя сама: в теле диффа `io_utf8.py`
стоит `ВХОДНАЯ ПОЛОСА (ЧЕТВЁРТЫЙ КАНАЛ, заведена 20.09.2026, задание 68v)`, а предмет этого захода —
68z/69b (ручка модели канала codex). Три файла — один предмет: чтение stdin в UTF-8
(`read_stdin_utf8` + его замок в тестах + дверь экзамена). Времена правок (16:33–16:46) — сегодня,
позже старта соседнего захода. **Ни один из трёх не тронут ни одной командой этого захода.**

## 2. Коммит своего предмета — коммитить НЕЧЕГО

Предмет («правка ручки модели и её замок в тестах») лежит в коммите **336a6099** с 20.09,
поимённо: `review_send_run.py`, `test_review_send.py`, `docs/artifacts/2026-09-20-68z-MODELPOLE-2009.md`.
Второй раз он не коммитится.

Чужое в коммит не взято — и это не забывчивость, а названная заданием законная ветвь
(«дерево грязно чужой правкой — назвать причину адресом, ничего не коммитить»). Взять
`io_utf8.py` в свой коммит значило бы отправить в бой чужой незакоммиченный WIP под своим именем:
файл стои́т в верхних импортах демона (`pc_orchestrator.py:7537`), то есть уехал бы в боевой
процесс первым же рестартом.

Единственное, что коммитит этот заход, — **собственный артефакт** (этот файл). Состав коммита
доказан списком файлов ниже, в п.3.

## 3. Чтение назад: сколько осталось изменённым

После коммита артефакта отслеживаемых изменённых против HEAD — **3**. Не ноль, и поимённо это те же
`exam_show.py`, `io_utf8.py`, `test_utf8_output_guard.py` — **все три чужие** (заход 68v).
Своего в дереве — **0 файлов**. Число прочитано `git status --porcelain -uno` после коммита.

## 4. Самообновление: отказы по грязному дереву за последние сутки

**Отказов 2**, оба сегодня, оба об одном файле (**прочитано по адресу**, `pc_orchestrator.log`):

| адрес | время | файлы |
|---|---|---|
| `pc_orchestrator.log:5028` | 20.09 **19:11:17,615** | `io_utf8.py` |
| `pc_orchestrator.log:5055` | 20.09 **19:50:06,323** | `io_utf8.py` |

Последний — **19:50:06**, за 19 минут до замера (20:09). Вызывает их **`io_utf8.py`** — чужая
правка. Предыдущая серия в этом же логе — 22 строки 16.09 16:29:50 … 17.09 17:08:01 про
`pretool_guard.py`; она старше суток и в счёт не идёт.

**Почему строк всего две при витке 60 с** (вывод из кода, не из лога): `_dirty_block` печатает
ровно ОДИН раз на пару «коммит + состав грязного» (`pc_orchestrator.py:7831-7833`). Состав грязи
между 19:11 и 19:50 не менялся, значит вторую строку родила смена коммита — в этом окне легли
`ff989bb1` и `3953bb6a`. Отказ, таким образом, происходит КАЖДЫЙ виток, а говорит о себе только
при новости. Число «2» — это число новостей, а не число отказов.

## 5. Что применится и когда — ветвью кода

**Событие.** Конец каждого витка `poll_once` зовёт `maybe_self_update()`
(`pc_orchestrator.py:13504`), виток — `POLL_SEC=60` с. Порядок условий (`:5615-5672`):

1. `_stopped()` — рубильник;
2. `new_blob = _blob_hash()` — sha1 по блобам **всего import-замыкания в HEAD** (`:5541`, через
   `_dep_blobs` → `git ls-tree -r HEAD`), и он `!= RUNNING_BLOB` (отпечаток на момент старта);
3. `!= _SU_REJECTED_BLOB` (код, уже провалив­ший гейт, второй раз не гоняют);
4. **`_dirty_block("orchestrator", …)` вернул `[]`** ← **здесь стои́т сейчас** (`:5633-5636`);
5. `code_gate` → 6. unittest-гейт → 7. реконсиляция детей → 8. `spawn` нового процесса, старый выходит.

**Разблокируется само.** Грязь НЕ запоминается как отвергнутый блоб (`:5620`, `:5636`:
«блоб НЕ помечаем отвергнутым: чистое дерево обязано разблокировать»). Как только сосед
закоммитит или откатит `io_utf8.py`, ближайший виток (≤60 с) поднимет новый код без чьего-либо
участия. Руками этого захода ничего запускать не нужно и не запускалось.

**Замыкание — это ДВА РАЗНЫХ списка, и путать их значит поставить себе ложный диагноз:**

| список | что решает | адрес | `io_utf8.py` | `review_send_run.py` |
|---|---|---|---|---|
| (а) замыкание self-update, **78 файлов**, считается обходом импортов от `pc_orchestrator.py` | **обнаружение** нового кода: правка любого из 78 меняет отпечаток и рождает попытку обновиться | `pc_orchestrator.log:4910` (список поимённо), код `:5541` | **есть** | **есть** |
| (б) `_ORCH_RUNTIME` — «что несёт САМ демон (его верхние импорты)» | **блокировку**: грязным считается только файл, чей basename в этом кортеже (`:7804-7812`) | `pc_orchestrator.py:7537` | **есть** | **НЕТ, сознательно** |

Строка (б) про `review_send_run.py` стои́т в коде прямым текстом (`:7615-7621`): «В ворота грязного
дерева НЕ вносим… ветка живёт за флагом REVIEW_AUTO и не должна уметь глушить доставку правок
демону своим незакоммиченным WIP».

**Ответ на вопрос задания.** Изменённый файл предмета — `review_send_run.py`: в замыкание (а)
**входит**, в ворота (б) **не входит**. То есть моя правка самообновление **вызывает**, но
заблокировать его не может; блокирует чужой `io_utf8.py`, который входит в оба списка.
Остальные два грязных файла (`exam_show.py`, `test_utf8_output_guard.py`) **не входят ни в один**
(в списке 78 их нет) — самообновление они не держат и не держали.

**Цена блокировки числом.** Живой демон стартовал **20.09 16:58:08 на коммите `5c2ddfcd`**
(`pc_orchestrator.log:4911`). HEAD на замере — `3953bb6a`, между ними **10 коммитов**, из них
замыкание демона трогают **ДВА**:

- **`336a6099`** — `review_send_run.py`, ручка модели канала codex (предмет 68z, мой);
- **`ff989bb1`** — `pc_orchestrator.py`, порог сироты (предмет 69a).

Обе правки стоят в очереди за чужим `io_utf8.py` **3 ч 11 мин** (от первого отказа 19:11:17 до
замера 20:09). Это на два порядка меньше сентябрьских 103 часов, но механизм ровно тот же.

## 6. Граница вслух

**Прочитано по адресу** (команда или файл названы): состав и объём грязи (`git diff --numstat`,
`--stat`, `git status --porcelain -uno`), пустота индекса (`git diff --cached`), состав коммита
`336a6099` (`git show --stat`), 10 коммитов после `5c2ddfcd` (`git rev-list --count` + `git log
--name-only`), обе строки отказа с временем и файлом (`pc_orchestrator.log:5028, 5055`), стартовая
строка демона с коммитом (`:4911`), список 78 зависимостей (`:4910`), все ветви кода в п.5
(`pc_orchestrator.py` с номерами строк), mtime трёх файлов (`ls --time-style=full-iso`).

**Вывод, а не чтение**: авторство трёх грязных файлов (опознано по самоназванию «задание 68v» в
теле правки и по предмету, реестра авторства у полосы нет); объяснение, почему строк отказа две,
а не сорок (сложено из правила `_dirty_block` и времени коммитов); «3 ч 11 мин» (арифметика по
двум прочитанным временам); «разблокируется само в ≤60 с» (ветвь кода, живой проверки не было —
для неё пришлось бы тронуть чужой файл).

**Чего заход НЕ делал.** Не удалял ничего, включая уборку за собой. Не трогал ни одной командой
`exam_show.py`, `io_utf8.py`, `test_utf8_output_guard.py` — не коммитил, не прятал в stash, не
откатывал, не читал их иначе как `git diff`. Не перезапускал демон и не запускал самообновление
руками. Не трогал ни одного процесса. Не ходил наружу в каналы: сообщений владельцу и в темы —
**0**, клиентам — **0**. `.env` и конфиги не читал (читал код, который их читает). Боевой базы,
денежных путей и боевых файлов состояния не касался. Тестов не гонял — правок кода в этом заходе
нет ни одной, гонять нечего.

**Чего заход НЕ ЗНАЕТ** (третий исход, не выдан за зелёное): пройдёт ли `code_gate` и unittest-гейт
на чистом дереве — **НЕИЗВЕСТНО**, до них дело не доходит, отказ по грязи стои́т раньше
(`:5633` против `:5643`); когда сосед закроет свой предмет — **НЕИЗВЕСТНО**, это его заход;
дошли ли до владельца карточки об отказе (`_notify` в `_dirty_block`) — не проверялось, наружу
этот заход не ходил.

## 7. Бюджет

Единица одна, потолок **2700 с**, порог 0.7 → **1890 с**. Заход занял ≈**700 с** = **0.26** доли
потолка. Пункты 1–6 выполнены полностью, сокращения по п.7 («сделать п.1 п.2 п.3 и остановиться»)
не потребовалось.

## Что здесь нужно от человека

Ничего красного заход не просит и NEEDS_APPROVAL не выставляет. Самообновление разблокируется
само в течение минуты после того, как **сосед по заходу 68v** закоммитит или откатит свои три
файла. Если заход 68v оборвался и файлы остались без хозяина — решение по ним принимает владелец:
в них лежит единственный экземпляр 170 строк, которых нет ни в одном коммите.
