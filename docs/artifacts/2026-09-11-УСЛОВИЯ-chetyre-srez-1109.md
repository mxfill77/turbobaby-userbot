# Четыре условия подключения менеджеров — живой срез 11.09.2026

**Заход разведочный.** Кода не правил ни строкой, процессов не трогал, менеджеров не подключал и
к подключению ничего не готовил, боевых записей не делал, ничего не удалял. Временное —
`tmp/pc_report/` (черновик доклада). Переписка менеджеров и диалоги клиентов наружу не выводились
ни строкой: из боевой базы уроков снята только ФОРМА (имена колонок и счёт строк), не содержимое.

УСЛОВИЯ chetyre-srez 1109

---

## 1. СВОДКА НА ОДИН ЭКРАН — читает владелец

| # | условие (дословно из рамки) | исход | чем снято | чьё |
|---|---|---|---|---|
| **1** | наблюдатели за детьми поймали живое падение | **ВЫПОЛНЕНО** | 33 вердикта смерти детей, каждый с тремя независимыми следами; за 04–11.09 ещё 2 падения, оба пойманы | закрыто, ничьё |
| **2** | модербот перестал падать сам | **ВЫПОЛНЕНО** | 63.04 суток без единой трассировки самопадения; за окно 0 невзявшихся подъёмов против 6 из 20 | закрыто, ничьё |
| **3** | база обучения как отдельная сущность с версией, источником и откатом | **НЕ ВЫПОЛНЕНО** | сущность есть, откат есть, источник заведён 09.09 — **версии нет ни одним полем**, и у 100 % живых строк источник неизвестен | **наше**, 1 единица |
| **4** | набор прогнан владельцем одним | **НЕ ВЫПОЛНЕНО** | 17 кейсов; единственная зелень снята 26.08 на **старом корпусе из 12**; последний вердикт — красный 14/16 от 06.09 | **владельца** — одна буква, потом наше |

**Итог одной строкой:** два условия из четырёх закрыты и за неделю только окрепли; два открыты, и
оба упираются не в трудность, а в отсутствие двери. Условие 4 стоит **решения владельца на одну
букву** — без него работу начинать нельзя, потому что неизвестно, что считать прогоном.

### Что предъявить владельцу по условию 4 (решение за 10 секунд)

> **Набор — 17 кейсов. Что считаем прогоном «владельцем одним»?**
> **(а)** кнопка в «Тренеровке»: жмёшь — бот гоняет все 17 и присылает счёт. *Кнопки сегодня нет.*
> **(б)** ты руками пишешь 17 фраз клиентом в «Тренеровке». *Работает сегодня, но счёта никто не ведёт.*
> **(в)** хватает нашего прогона CLI, «одним» = «без правок кода в тот день». *Работает сегодня.*
>
> Контекст на сейчас: последний замер — **красный, 14 из 16, 06.09**. Единственная зелень
> (12/12) снята **26.08 на корпусе из 12 кейсов**, а сегодня их 17. **На нынешнем составе
> вердикта не существует вообще.**

### Что наше по условию 3 (один следующий шаг, самый дешёвый)

Перевести девять живых строк базы уроков из 8-колоночного формата в 9-колоночный, проставив им
источник `перенос_книги` (он известен — строки легли оптовым переносом 06.09), и завести версию
набора одним полем. **Цена — 1 единица работы:** дверь миграции и откат уже написаны и покрыты
тестами, нужна не разработка, а один проход по таблице с побайтовым откатом.

---

## 2. Условие 1 — наблюдатели поймали живое падение: **ВЫПОЛНЕНО**

**Адрес доказательства.** База — чужой заход 04.09,
[`docs/artifacts/2026-09-04-условия-один-и-два-числом-а-не-мнением.md`](2026-09-04-условия-один-и-два-числом-а-не-мнением.md) §3
(31 вердикт смерти за 43.2 суток: модербот 20 · userbot 7 · pc_agent 4; 20 карточек владельцу;
третий исход напечатан на живых данных 4 раза). **Этот замер я не пересчитывал — он снят не мной.**

**Мой замер сейчас — только дельта, окно 04.09 → 11.09 (7.77 суток):**

| показатель | было на 04.09 | стало за окно | адрес |
|---|---|---|---|
| вердиктов `контур-вотчдог: * МЁРТВ` | 31 за 43.2 сут (0.72/сут) | **2** за 7.77 сут (**0.26/сут**) | `pc_orchestrator.log`, строки 08.09 02:44:05 и 10.09 02:34:24 |
| из них подъём не взялся | 6 из 20 (модербот) | **0 из 2** | там же, `PID после=[17536]` и `PID после=[12852]` |
| «умер 3 раза подряд — СТОП попыток» | 4 | **0** | `pc_orchestrator.log`, все 5 строк файла ≤ 29.08 |
| карточек владельцу на вердикт | 20, 1:1 | **2**, 1:1 (`ok=True`, mid 10243 и 10536) | `dispatch_notify.log` |
| записей слоя ожиданий об отрицательном исходе по ребёнку | **0** | **≥3** (05.09 «неизвестно», 08.09 и 10.09 «работы нет») | `dispatch_notify.expectations_pc_run.log`, строки ПУЛЬС |

**Что изменилось по существу.** Частота падений упала втрое, и все они пойманы. Слой ожиданий
сдвинулся с нуля: у него впервые появились отрицательные исходы по ребёнку — но **вердикт это не
переворачивает**, и вот честно почему: все три случая про `pc_agent`, а не про модербота; карточки
владельцу слой не выпустил ни одной; два из трёх пришлись ровно на минуты перезагрузок 08.09 и
10.09, то есть он заметил то же, что и вотчдог, и вторым голосом.

**Что именно мешает:** ничего не мешает исходу — мешает поверить в ВТОРОГО наблюдателя. Слой
ожиданий по-прежнему не имеет канала карточки о смерти ребёнка: его исход живёт строкой журнала
раз в 6 часов, и владелец её не читает.

**Чьё:** ничьё, условие закрыто. Вотчдог ловит, поднимает и докладывает — это снято числом дважды,
разными заходами, на непересекающихся окнах.

---

## 3. Условие 2 — модербот перестал падать сам: **ВЫПОЛНЕНО**

**Адрес доказательства.** База — тот же чужой артефакт 04.09 §2 (0 самопадений с трассировкой за
55.5 суток; 20 смертей, из них 10 доказанно от перезагрузки Windows, 1 — ручной цикл). Не пересчитывал.

**Мой дельта-замер, окно 04.09 → 11.09:**

- **Самопадений — 0.** `Traceback|CRITICAL|Unhandled` в `logs/moderbot_stderr.log` = 0/0/0 за всё
  время файла; в `moderation_bot.log` четыре трассировки, все июльские (04.07 ×3, 06.07 ×1).
  Срок «не падает сам» вырос с 55.5 до **63.04 суток**.
- **Смертей 2**, обе помечены `[перезагрузка]` («лежал 3 м 59 с», «лежал 2 м 57 с»). Ручных циклов 0.
- **Простоев длиннее часа НЕТ.** Худший простой окна — **3 м 59 с** против прежних 86 ч 45 мин.
- **Жив прямо сейчас:** `moderation_bot.lock` держит `pid 12852`, `started` совпадает с `StartTime`
  живого процесса до секунды; аптайм ≈ 39 ч 51 мин; `moderation_ipc.db` свеж 3 с (снято `os.stat`,
  базу не открывал).

**Что изменилось по существу — и это главная новость среза.** Половина вердикта 04.09, гласившая
«НЕ ВЫПОЛНЕНО в смысле „переживает без человека“», за окно **закрылась фактом**: 2 подъёма из 2
взялись с первой попытки, сторож не сдался ни разу, перезагрузка машины для лечения не
понадобилась. И это не везение окна, а следствие названной причины: 86-часовой простой родил
переиспользованный номер 18200, а ветку «уже запущен» с 30.08 судит сверка личности процесса
(§5А) — тот же лок сегодня получил бы приговор `STALE`, и подъём бы состоялся.

**Честная оговорка, которую нельзя прятать.** Окно — 7.77 суток против инцидента в 86 часов:
одно чистое окно не доказывает, что класс мёртв, оно доказывает, что за неделю он не воспроизвёлся
при закрытой причине. Отдельно: в собственном логе модербота есть дыра **24.00 ч** (10.09
08:11:41 → 11.09 08:11:47) — это **не простой**, а молчание событийного лога: пульс О3 трижды в
этом промежутке сказал «делает работу», и процесс PID 12852 живёт непрерывно с 10.09 02:34:11.

**Что именно мешает:** ничего. Формулировка 04.09 «он не падает — его уносит, и поднимается он не
всегда» на сегодня верна только первой половиной.

**Чьё:** ничьё, условие закрыто.

---

## 4. Условие 3 — база обучения с версией, источником и откатом: **НЕ ВЫПОЛНЕНО**

Сущность существует: `lesson_store.py` (в git) + `lesson_store.tsv` (боевые данные, вне git по
`.gitignore`). Разбор по трём требуемым свойствам — **мой замер 11.09**, подтверждённый чужими
заходами 08–09.09.

| свойство | исход | адрес | почему так |
|---|---|---|---|
| **версия** | **НЕ ВЫПОЛНЕНО** | `lesson_store.py:197` `HEADER_LINE` — только имена колонок | поля версии/редакции нет ни одного; снимка таблицы на диске нет ни одного; в `docs/lesson_base/` два файла от 03.09, снимков базы среди них нет |
| **источник** | **есть в коде, пуст в данных** | `lesson_store.py:189` `COL_SOURCE`, `:194-195` `COLUMNS_BASE`(8)+`COLUMNS`(9), `:259-262` закрытый список, `:266-268` третий исход `SOURCE_UNKNOWN`. Коммит **`0afe886`** (09.09) | **мой замер:** шапка боевого файла — **8 колонок** (`номер · вопрос_клиента · ответ_бота · как_правильно · почему · кто_записал · когда · состояние`), колонки `источник` в ней нет; все **9 строк** по 8 полей; mtime 06.09 04:54 |
| **откат** | **ВЫПОЛНЕНО, ни разу не применён** | снятие 4 разрезами `lesson_store.py:232-239`, `:804`; откат перевода по номеру `:985 promote`, `:1059 rollback`; откат набора `:1284-1391` (коммит `20c375b`, 09.09) | следовых файлов на диске нет вовсе: ни `.promote`, ни `.batch`, ни `.bak` |

**Что именно мешает (одна строка):** версии у набора нет ни одним полем, а девять живых строк лежат
в старом 8-колоночном формате — их источник «неизвестен», поэтому разрез `источник` и откат набора
на боевой таблице сегодня снимут **ноль** строк.

**Чьё: НАШЕ.** Следующий шаг и цена — §1. Замечу отдельно: это не разработка, а миграция —
код-приёмник уже умеет оба формата (`lesson_store.py:482` признаёт и `HEADER_LINE`, и
`HEADER_BASE_LINE`), тесты на откат по sha256 всей таблицы есть (`test_lesson_promote.py`).

---

## 5. Условие 4 — набор прогнан владельцем одним: **НЕ ВЫПОЛНЕНО**

**Что такое «набор».** Рамка зовёт его экзаменационным набором, и это **не** живая группа
«Тренеровка», а корпус безголового прогона `trainer_cases.json`
(`docs/tasks/frame-edition-20260910-1.txt:769-775`). Разделение зафиксировано ещё
[`docs/artifacts/2026-08-11-trainer-moderbot-census.md:19-24`](2026-08-11-trainer-moderbot-census.md):
у группы корпуса нет вовсе и вердикта нет, у безголового прогона есть и то и другое — **ворота
читают именно его**.

**Мой замер 11.09 — три числа, каждое своё:**

1. **В наборе 17 кейсов** (`trainer_cases.json`, поле `about`: 12 RU + 4 EN-зеркала от 02.09 +
   кейс третьего исхода 17 от 07.09). Отпечаток корпуса, посчитан мной:
   **`3bae79ae6967a195`**.
2. **Зелень в реестре ровно одна** — ключ `74be777`, `corpus_sha` **`98ad5e3e2c256542`**,
   `when 2026-08-26 03:35:35`. Это корпус из **12** кейсов.
3. **Последний вердикт — красный:** ключ `4aafbab`, `result: red`, **`cases: 14 / cases_total: 16`**,
   `checks 295/300`, `corpus_sha` **`6d5d78f0d83acdca`**, `when 2026-09-06 14:44:19`.

Отпечаток сегодняшнего корпуса **не совпадает ни с одним из двух**. И это не придирка: так судит
сам код — `client_contour.py:532-539`, докстрока дословно: *«Вердикт обязан назвать ТОТ корпус, что
лежит на диске: зелень, снятая на урезанном наборе кейсов, не основание.»* Значит **на нынешнем
составе набора вердикта не существует вообще**, ни зелёного, ни красного.

**Два отказа, которые делают условие невыполнимым сегодня, а не просто невыполненным:**

- **Двери для владельца нет.** Прогнать набор умеет только CLI `trainer_run.py:1424-1441`, руками с
  полосы ПК; сам файл о себе говорит (`:19-21`): «раннер не попадает в клиентский контур (его никто
  из ботов не импортит)». В Telegram у владельца есть панель `[🔄 Заново][📋 До CRM][🎓 Обучить]`
  (`moderation_bot.py:306-335`) — это урок по ОДНОЙ реплике, не прогон набора.
- **Автора прогона реестр не хранит.** Мой замер полей записи вердикта: 21 поле
  (`cases · cases_total · checks_passed · checks_total · clean · commit · corpus · corpus_sha ·
  dirty_paths · failed · head_after · head_moved · result · runner · runs · tree_dirty ·
  tree_known · ts · unknown · unknown_why · when`) — **поля «кто прогнал» среди них нет**, а
  `runner` во всех девяти записях = `trainer_run.py`, то есть имя скрипта. **Даже состоявшийся
  прогон владельцем был бы в этом реестре неотличим от нашего.**

**Что именно мешает (одна строка):** у владельца нет ни кнопки, ни команды, чтобы прогнать набор
одному, а вердикт не хранит поля автора — «владельцем одним» сегодня физически нечем ни сделать,
ни доказать.

**Чьё: РЕШЕНИЕ ВЛАДЕЛЬЦА, потом наше.** Предъявить — карточку из §1 (три буквы, одна строка
контекста). Порядок именно такой: пока не выбрана буква, неизвестно, что строить, и любая работа
здесь — ставка вслепую. При ответе **(в)** условие закрывается нашим прогоном на текущем корпусе —
это 1 единица. При **(а)** — дверь в тренажёр плюс поле автора в вердикте.

---

## 6. Новое с 04.09 — проверено кодом, две премисы задания поправлены

### А. «Ветка подъёма ребёнка сверяет личность процесса» — ВЕРНО, но не 11.09 и не в той двери

**Подтверждено:** дверь «уже запущен» у детей действительно судит личность —
`moderation_bot.py:624-633` и `userbot_listen.py:108-118` зовут `proc_identity.acquire` →
`proc_identity.py:472` `judge`. Сам приговор `:382-434`: признак «образ» `:404-411`, признак
«момент старта» `:414-422`, `OURS_ALIVE` только при совпадении обоих. Тесты есть и на живых
процессах без моков: `test_proc_identity.py:70` (случай 18200 дословно), `:232`, `:244`, `:258`, `:321`.

**Поправка первая — дата.** Код лёг коммитом **`2f015bb` от 30.08.2026**, а не 11.09. Коммит 11.09
**`acefe8d`** — мой замер `git show --stat`: **1 файл, +260 строк, и это сам артефакт**
`docs/artifacts/2026-09-11-ЛОК-ЛИЧНОСТЬ-ВЕТО-1109.md`. То есть 11.09 ничего не починено — 11.09
**измерено** то, что работает с 30.08. Для среза это важно: заслуга принадлежит окну, в котором
условие 2 и окрепло.

**Поправка вторая — дверь.** Заявка №1 от 04.09 называла дырой **вето подъёма у оркестратора**.
Эта дверь личность **по-прежнему не сверяет и никогда не сверяла**: `pc_orchestrator.py:9379-9384`
(порог `:246`) решает только по возрасту лога и лока не читает вовсе; единственный
`proc_identity.judge` в файле — строка `:13006`, и это лок самого демона, не детей. Заявка искала
сверку не в той двери — а класс закрыт другой дверью, у ребёнка.

**Пробил бы PID 18200 сейчас — НЕТ,** двумя независимыми путями: чужой образ → `STALE`
(`proc_identity.py:405-407`); даже будь образ python — рождён позже mtime лока → `STALE` (`:430-432`);
на `STALE` лок забирается и старт состоится (`:478-480`). **Остаток, названный самим артефактом,
кодом подтверждается:** `read_lock` кладёт `rec["script"]` (`:376`), а `judge` это поле не читает
ни разу — значит приговор отвечает «тот ли это ЗАПУСК», а не «мой ли это РЕБЁНОК», и класс «лок
называет чужой живой python полосы» открыт.

### Б. «У закрытого ряда появилось имя, реестр судьи мигрирован» — ВЕРНО, но это ЧУЖОЙ контур

**Подтверждено кодом и числом.** `contour_digest.py:772` `row_name`, `:804` `judged_of`;
`done_judge_pc.py:731` `note(..., name=)`; точка вызова `pc_orchestrator.py:4035`. Коммит
**`ff8b149`** (11.09 16:47), мой замер `git show --stat`: 4 файла, +239/−8. Живой реестр
`tmp/done_judge_pc/judged.json`: **159 ключей, с именем 8, под номером 151, потерь 0**
(в артефакте на момент миграции 7/151 — восьмое имя легло уже новым писателем, то есть код работает
на живой полосе, а не только в тестах). Тесты — `test_contour_digest.py`, класс `TestRowNameNotNumber`,
12 проверок.

**Поправка к премисе задания:** **к условиям подключения менеджеров это отношения не имеет.**
Коммит `ff8b149` трогает `contour_digest.py`, `done_judge_pc.py`, `pc_orchestrator.py`,
`test_contour_digest.py` — **ни одного файла детей**: ни `moderation_bot.py`, ни `userbot_listen.py`,
ни `pc_agent.py`. Это контур ящика заданий и судьи закрытия рядов очереди. Приписывать его к
условиям 1–2 нельзя, и в сводке §1 он не учтён ни одной строкой.

### В. «Дверь перевода урока в действующие существует с 09.09» — ВЕРНО

Коммит **`2af45de` (09.09)**, мой замер `git log`: «У перевода кандидата в действующие появилась
дверь: право, причина, след и откат по номеру». Дверь CLI — `lesson_promote.py:84-116` (ключи
`--promote/--why/--rollback/--candidates/--trace`), право судит `moderation_core.may_write_rule`
внутри `trainer.promote_lesson` (fail-closed). Телефонная дверь — `pc_agent.py:790-793`, `:886`,
роутер темы 205 `:1234-1241`, слово владельца «урок включи N: причина». Вторая дверь для наборов —
`lesson_batch.py` (`20c375b`, 09.09).

**Оговорка, снимающая ложный зелёный:** дверь есть, а следа её применения на боевой таблице нет —
кандидатов в базе ноль (все 9 строк в состоянии `актив`, это оптовый перенос книги от 06.09),
файлов следа не заведено. Существование двери условие 3 **не закрывает**: оно требует версии и
источника, а не двери.

---

## 7. Арифметика бюджета (пункт 6 задания)

Единица — одно условие с чтением и выпиской адреса. **Замер на первых двух:** условия 1 и 2
съели один круг разведки целиком — чтение базового артефакта 04.09 (28 988 знаков) плюс
дельта-замер по семи источникам (`pc_orchestrator.log`, два лога модербота, `dispatch_notify.log`,
`tmp/expect_pc/state.json`, лок, `os.stat` базы). Круг занял **≈600 с**.

Произведение последовательным порядком: 4 × 600 = **2400 с** против потолка **2700 с** — влезает с
запасом 300 с, то есть на одну аварию. Запас на четырёх условиях я тратить не стал и пустил все
четыре **одним параллельным кругом**, поэтому фактическое время замера ≈ max(единица), а не сумма;
высвобожденное ушло на проверку трёх новинок кодом и на СВОЮ сверку чужих чисел (§8). Останавливаться
и докладывать нехватку не понадобилось: четыре условия закрыты доказательствами, а не словами.

---

## 8. Что в этом срезе снято МНОЙ, а что чужим заходом (без присвоения)

**Чужое, не пересчитывал и за своё не выдаю:** весь базовый замер условий 1 и 2 — артефакт
04.09; разборы 08–09.09 по базе уроков
(`2026-09-08-БАЗА-ОБУЧЕНИЯ-ВЕРСИЯ-ИСТОЧНИК-ОТКАТ-0809.md` и четыре артефакта 09.09); контрольная
точка набора `2026-09-07-контрольная-точка-набора-trainer_run.md`; перепись тренажёра 11.08;
артефакты 11.09 по локу-личности и реестру ряда.

**Моё, снято 11.09 на HEAD `cf51e9c`:** дельта-замер условий 1 и 2 за окно 04–11.09 (все числа §2 и
§3); форма боевой базы уроков (8 колонок, 9 строк, mtime); отпечаток текущего корпуса
`3bae79ae6967a195`; полный разбор последнего вердикта `4aafbab` (red 14/16) и перечень 21 поля
записи вердикта; `git show --stat` по `acefe8d` и `ff8b149`; даты коммитов `2f015bb`, `2af45de`,
`0afe886`.

## 9. Что этим заходом НЕ доказано (честно)

- **Что слой ожиданий сказал про модербота** в три момента своих отрицательных исходов —
  **НЕИЗВЕСТНО**: строки `dispatch_notify.expectations_pc_run.log` обрезаны на 158 знаках ровно на
  позиции вердикта `pc_agent`, полный текст ушёл в Telegram, а кольцо `cowork_log.ledger` (500 строк)
  начинается только с 10.09 и эти моменты не покрывает.
- **Предмет 189 из 195** критических доставок слоя за окно — **НЕИЗВЕСТНО** по той же причине.
- **Что класс «переиспользованный PID» мёртв** — не доказано: доказано, что за 7.77 суток он не
  воспроизвёлся при закрытой причине. Окно короче инцидента.
- **Что модербот не может упасть сам** — не доказано: доказано, что за 63.04 суток не падал и что
  канал для такой трассировки существует и пуст.
- **Состояние четырёх условий в мозге** не сверялось: рамка говорит, что галочки живут там
  (`frame-edition-20260910-1.txt:769-775`), а этот срез снят с полосы ПК по коду и логам. Если в
  мозге записано иное — прав мозг, а расхождение само по себе находка.
- Живых процессов не трогал, падений не подстраивал, менеджеров не подключал, боевых записей не делал.
