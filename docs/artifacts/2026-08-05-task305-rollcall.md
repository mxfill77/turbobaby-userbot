# Перекличка ПК после падения задачи 305 (read-only, 05.08.2026 13:50–14:00 +07)

Повод: 305 (личка pc_agent) висела 62 минуты без heartbeat. Гипотеза владельца — известный
класс «обрыв внешней сети ПК роняет задачу, а система объявляет её зависшей».

**Гипотеза не подтвердилась. Обрыва сети в окне 305 не было. Причина другая: потеря расписки
моста на claim (класс «задача 193», 02.08).**

---

## 1. Обрыва сети в окне 305 НЕ БЫЛО

Окно 305: claim 12:44:01 → сейчас. Четыре независимых замера, все отрицательные.

| канал | замер | вердикт |
|---|---|---|
| `Microsoft-Windows-WLAN-AutoConfig/Operational` | последнее событие **09:49:15**, после него за 4 часа **0 событий** | разрыва не было |
| драйвер Wi-Fi `Netwtw08` (Intel Wireless-AC 9462) | последнее событие **09:51:29** | разрыва не было |
| **живой сокет userbot** | `userbot_listen` PID 17504 держит ОДНО TCP-соединение `91.108.56.105:443` (Telegram DC) **непрерывно 242 мин**, с 09:51:35 | физическое доказательство: обрыв внешней сети порвал бы этот сокет |
| System-лог, сетевые провайдеры | в окне ровно одно событие: `Tcpip 4266` **12:21:02** — «A request to allocate an ephemeral port number from the global UDP port space has failed due to all such ports being in use» (единичное, за 23 мин ДО claim; парное `Tcpip 4231` по TCP — 01:21:01) | к 305 не приложимо |

Сам дирижёр на КАЖДОЙ строке отказа пишет `связь есть`. Это не догадка: по коду
`bridge_http.py:275-277` ответ класса `KIND_BRIDGE` (включая сам `BridgeReceiptLost`)
засчитывается как ДОКАЗАТЕЛЬСТВО связи, против связи играет только `KIND_NETWORK`.

**Обрыв сети сегодня был — но утром, 09:22:04–09:49:15**, за три часа до 305:
пять переассоциаций WLAN, четыре ошибки `8002 WLAN AutoConfig service failed to connect`,
и в `moderation_bot.log` дословно:

```
2026-08-05 09:49:06,418 | сетевая ошибка (не падаю, PTB переподключится): NetworkError: httpx.ConnectError: [Errno 11001] getaddrinfo failed
2026-08-05 09:51:29,728 | сетевая ошибка (не падаю, PTB переподключится): NetworkError: httpx.ConnectError: [SSL: CERTIFICATE_VERIFY_FAILED] certificate verify failed: unable to get local issuer certificate (_ssl.c:1000)
```

Он стоил двух смертей userbot (09:30, 09:51) и провала доставки карточки в личку
(`09:30:43 | фолбэк личка не прошёл (code=None URLError)`). После 09:51:35 — тишина, ни одной
сетевой ошибки. **К 305 это отношения не имеет.**

## 2. Задача 305: на ПК не запускалась НИКОГДА

```
2026-08-05 12:39:55,376 WARNING get_pending(in_progress) ошибка: BridgeTransportError — … второе плечо моста (echo) бросает обратно на наш же /exec — ключ расписки мёртв.
2026-08-05 12:44:01,807 INFO claim id=305 не удался (BridgeReceiptLost) — пропуск
2026-08-05 12:45:04,719 INFO виток растянулся на 321с: залипание вызова моста (сна 0с, связь есть) — событием не считаем
```

- Дочернего исполнителя **нет и не было**: в `pc_orchestrator.task_started.json` (локальная
  отметка старта, 105 записей) есть `315` и `317`, а ключа **`305` нет вовсе**.
- Успела сделать **ничего**: ни коммита, ни строки журнала, ни правки файла.
- Незакоммиченные изменения в дереве **к 305 не относятся** (см. §3).

**Почему висит.** У дирижёра есть страховка «судить по ФАКТУ, а не по расписке»
(`pc_orchestrator.py:582-590`, заведена после задачи 193 от 02.08): при отказе расписки он
ПЕРЕЧИТЫВАЕТ статус задачи, и если та `in_progress` — claim долетел, работаем.
Здесь страховка не сработала: чтение полосы упало за 4 минуты до этого (12:39:55), `_in_status`
вернул `None` = «не знаю», и цикл честно пропущен (fail-safe). А сам claim, судя по всему,
**долетел**: 305 исчез из выдачи `new` — следующим тактом дирижёр взял не его, а 315.

Итого 305 — сирота `in_progress` на мосту без исполнителя. Ровно поэтому она «без heartbeat».

**Реапер её сейчас не снимет.** Порог `PC_SINGLE_STALE = 5400` (90 мин, `pc_orchestrator.py:116`)
истечёт ~**14:14:01**, но реапер отказывается судить, пока не прочитал полосу, а чтение падает
подряд: 13:34:51, 13:39:58, 13:42:50, 13:45:26, 13:48:22 — все с
`BridgeTransportError … ключ расписки мёртв` и припиской
`реапер молчит: не прочитал полосу — не сужу задачи`.

## 3. Задача 315 — дубль 305: отработала 45 минут и была убита за 10 секунд до финиша

```
2026-08-05 12:46:04,856 INFO CLAIM id=315 in_progress
2026-08-05 12:46:05,365 INFO RUN id=315 (timeout=2700s, попытка 1/2)
2026-08-05 13:31:05,535 WARNING id=315 ТАЙМАУТ 2700s → failed
2026-08-05 13:31:05,624 WARNING FAIL причина=run_timeout окно=05.08 05:46–06:31 UTC следы: коммитов=0, записей журнала=0
2026-08-05 13:31:10,857 INFO COMPLETE id=315 status=failed
```

Жива — **ни одна**: 315 закрыта `failed`, 305 висит сиротой. Дочерний процесс 315 убит,
в списке процессов его нет.

**Но «следы: коммитов=0, записей журнала=0» — неправда по факту.** 315 сделала реальную работу,
она лежит в дереве несохранённой:

```
 dispatch_notify.py      |  14 ++++
 pc_orchestrator.py      |  35 ++++++--
 pretool_guard.py        | 209 +++++++++++++++++++++++++++++++++++++++++--
 test_dispatch_notify.py |  53 ++++++++++++
 test_pc_orchestrator.py |  56 ++++++++++++-
 test_pretool_guard.py   | 190 +++++++++++++++++++++++++++++++++++++++
 6 files changed, 531 insertions(+), 26 deletions(-)
```

Атрибуция — не по времени, а по журналу гарда `pretool_guard.log`: непрерывная headless-полоса
**12:46:12 → 13:30:55**, дальше провал до 13:50:07 (это уже задача 317). Штампы `Edit`/`Write`
совпадают с mtime файлов посекундно (`12:59:57` → `dispatch_notify.py`, `13:00:53` →
`pc_orchestrator.py`). Последние шаги 315:

```
2026-08-05 13:24:45 | headless | Bash  | defer | - | TURBOBABY_TEST_LOGS=1 venv/Scripts/python.exe -m unittest discover -s . -p "test_*.py" 2>&1 | tail -8
2026-08-05 13:30:47 | headless | Write | defer | - | D:\turbobaby-bot\tmp\dmcards-20260805-a1\measure_final.py
2026-08-05 13:30:55 | headless | Bash  | defer | - | venv/Scripts/python.exe tmp/dmcards-20260805-a1/measure_final.py 2>&1
```

Прогнала полный тест-набор, писала финальный замер — и убита в 13:31:05, **через 10 секунд**
после последнего шага, не успев закоммитить.

Ещё 315 подняла одну карточку гарда, доставленную в личку и **не отвеченную**:

```
2026-08-05 12:47:46,781 | DM ok → 504608015
2026-08-05 12:47:46,781 | итог: channel=DM ok=True | 🔴 Хочу обратиться к .env / секретам — разрешить?
Объект: .env
Число: —
Откат: .env вне git
```

Дирижёр её не увидел: `NEEDS_APPROVAL` он опознаёт по маркеру при ВЫХОДЕ ребёнка (так закрылась
302 в 12:23:49), а ребёнка 315 убил таймаут — выхода не было.

**Два дефекта, оба видны числом:**
1. счётчик «следов» в `FAIL`-вердикте считает коммиты и строки журнала, но **не считает правки
   файлов** — задача с 531 строкой работы записана как бесследная;
2. задача, поднявшая карточку одобрения в 12:47:46, продолжила жечь бюджет ещё 43 минуты и
   умерла по `run_timeout`, а не по `approval_ttl=1800s`.

## 4. Перекличка процессов (снято 13:50–13:57)

| роль | PID | старт | аптайм | экз. |
|---|---|---|---|---|
| `pc_orchestrator.py` | **23788** | 05.08 11:54:02 | 2,0 ч | **1** |
| `pc_agent.py` | **12124** | 31.07 12:58:42 | 120,9 ч (5,04 сут) | **1** |
| `moderation_bot.py` (moderbot) | **10080** | 31.07 12:58:50 | 120,9 ч | **1** |
| `userbot_listen.py` | **17504** | 05.08 09:51:35 | 4,0 ч | **1** |
| `rc_supervisor.py` (+2 claude RC) | 12444 | 31.07 00:26:07 | 133,5 ч | 1 |
| вотчдог демона | — резидента нет | тик schtasks раз в 5 мин | — | — |

Дублей нет ни у одного. Заметки:

- вотчдог — не процесс, а `--watchdog`-запуск по расписанию; **живой факт вместо `schtasks`
  (красный класс, не трогал):** `pc_orchestrator.watchdog_state.json` перезаписан в **13:53:02**,
  содержимое `{"state": "alive", "last_alert": 0.0, "raises": []}`;
- `pc_orchestrator` 23788 и `userbot_listen` 17504 оба имеют PPID **6376** — это прежний демон,
  вышедший по self-update в 11:54:02. userbot сейчас сирота по родителю (работе не мешает);
- реконсиляция детей на новый коммит **сознательно остановлена** — заморозка клиентского
  контура держится: `ворота контура … применение 8b28626 к «userbot» ОСТАНОВЛЕНО — клиентские
  файлы: userbot_listen.py`.

### Смерти userbot и глушение — числом

Сейчас (`pc_orchestrator.client_watch.json`, ts 13:43:31):
**`deaths = 0`, `halted = false`**, порог глушения `max_deaths = 3`, `cooldown = 900`с.

**Ноль здесь означает «счётчик обнулён», а не «день был чистый»:** состояние живёт в RAM демона,
а демон перезапустился по self-update в 11:54:03 и стёр его. За сутки по логу:

```
01:19:00 WARNING контур-вотчдог: userbot МЁРТВ (лежал 331с, смерть 1/3) → подъём ok=True, PID после=[16412]
01:38:58 WARNING контур-вотчдог: userbot МЁРТВ (лежал 1129с, смерть 2/3) → подъём ok=True, PID после=[21600]
01:58:44 ERROR   контур-вотчдог: userbot умер 3 раз подряд — СТОП попыток, нужен разбор
09:30:18 WARNING контур-вотчдог: userbot МЁРТВ (лежал 174с, смерть 1/3) → подъём ok=True, PID после=[20324]
09:51:46 WARNING контур-вотчдог: userbot МЁРТВ (лежал 1220с, смерть 2/3) → подъём ok=True, PID после=[17504]
```

- сегодня: **4 подъёма**, 4 строки смертей, **1 глушение** (01:58:44, порог 3/3);
- за 7 суток: **8** строк смертей/глушения, **9** подъёмов (31.07 — 3 смерти и глушение в 15:52,
  01–04.08 — ноль);
- у состояния `halted` веток в тике нет: после глушения — ни карточки, ни строки лога, ни
  попытки подъёма, до первого «жив». Сколько длилось молчание после 01:58:44, не записано нигде.

## 5. Клиентский бот доступен — живой факт

- **moderbot** (PID 10080): установленное TLS-соединение с Telegram `2001:67c:4e8:f004::9:443`.
  Два замера подряд: 13:56:35 возраст 241с → 13:57:00 возраст 266с — тот же сокет, длинный
  polling живой. Ошибок в `moderation_bot.log` нет с 09:51:35.
- **userbot** (PID 17504): MTProto к `91.108.56.105:443`, непрерывно 242 мин; в
  `logs/userbot_stderr.log` свежая активность (`Got difference for channel … updates`, 13:46).
- **pc_agent** (PID 12124): соединение с Telegram живо, возраст 48,8 мин.

Клиентский контур **доступен**. Ни один процесс не тронут, заморозка соблюдена.

---

## Что осталось владельцу

1. **305 — сирота на мосту.** Реапер снимет её не раньше 14:14:01 и только если прочитает полосу;
   сейчас каждое чтение падает. Первично чинится **мост** (мёртвый ключ расписки на плече echo),
   а не ПК.
2. **Работа 315 лежит в дереве несохранённой** (531 строка, 6 файлов, тесты прогнаны в 13:24:45).
   Решение владельца: коммитить как есть, доделывать или откатывать. Сейчас — не тронуто.
3. Карточка `.env` от 12:47:46 висит без ответа; задача, её поднявшая, уже мертва.
4. Два дефекта из §3 (счёт следов без правок файлов; `run_timeout` вместо `approval_ttl` на
   задаче с поднятой карточкой) — кандидаты в правку, здесь не делались.
