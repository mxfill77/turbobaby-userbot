# Копия рамки в мозге: рамка поднята до 01.09.2026 #1 (KB_shtab_frame, 23967 → 25094 симв.)

**Дата:** 2026-09-01, headless-заход полосы ПК. **Ось задания:** обслуживание нас самих.
**Задание:** поднять копию рамки в мозге до редакции **01.09.2026 #1** — записать
`docs/tasks/frame-edition-20260901.txt` в узел `KB_shtab_frame` ЦЕЛИКОМ через доверенного
писателя, затем прочитать назад отдельным вызовом и сверить посимвольно.

**Итог одной строкой: рамка поднята до 01.09.2026 #1; живой док посимвольно равен файлу-источнику
(sha256 совпал), расхождений 0 из 25094 символов.** Прошлая редакция (22.08.2026 #2) в доке не
осталась ни одним вхождением. Других узлов мозга заход не касался, кода не менял.

## 1. Премиса задания проверена ДО любой записи

```
sha256 docs/tasks/frame-edition-20260901.txt
  = d4315466194f9f6a72380a805cca8c521fd66b65815576c144cc5323ad6b1bab
ожидался префикс d4315466194f9f6a → СОШЁЛСЯ
```

Премиса сверена **дважды и разными руками**: сначала вручную (`sha256sum`, до чтения файла),
потом самим скриптом записи как стоп-условие — при несовпадении он выходит кодом 2 и не зовёт
`brain_writer` ни одной строкой. Байты и символы дают один и тот же хеш: в файле **CR = 0** и
BOM нет, поэтому `sha256(байты файла) == sha256(текст.encode("utf-8"))` — это и позволяет
сравнивать хеш файла с хешем текста, вычитанного из мозга.

Источник: **25094 символа, 478 строк, CR 0, U+FFFD 0.**

## 2. Адрес — по ИМЕНИ, не по file id

Оба скрипта захода зовут док `name="KB_shtab_frame"`; числовой id в коде захода не появляется
нигде. Разрешение делает сам писатель (`имя KB_shtab_frame → ключ реестра shtab_frame`), и
запись идёт ровно по тому адресу, по которому состоялось чтение — `apply` фиксирует адрес
после `_read` и повторно его не разрешает.

## 3. Числа ДО и ПОСЛЕ

| замер | ДО (22.08.2026 #2) | ПОСЛЕ (01.09.2026 #1) |
|---|---|---|
| символов | **23967** | **25094** (Δ **+1127**) |
| строк | 459 | 478 |
| U+FFFD | 0 | 0 |
| CR | 0 | 0 |
| sha256 (utf-8) | — (снят бэкапом) | `d4315466194f9f6a…ad6b1bab` |
| шапка | `версия 22.08.2026 #2` | `версия 01.09.2026 #1` |

Замер «ДО» — живая проба `brain_writer.py --probe --name KB_shtab_frame` перед заходом.
Диффстат старой редакции против новой (по бэкапу): **33 ханка, +151 строка, −133 строки** —
это редакция целиком, а не досыл блока.

**Гард усыхания не задет и не отключался:** новый текст длиннее старого (25094 > 23967), поэтому
`allow_shrink` в заходе не объявлялся вовсе — штатный замок писателя оставался взведён.

## 4. Запись — ЦЕЛИКОМ, через доверенного писателя

Канал: `brain_writer.apply(mutate, name=…)` — движок, объявленный в CLAUDE.md для одноразовых
скриптов. `append` здесь не годится по существу задания: он ДОПИСЫВАЕТ, а нужна замена всего
тела, поэтому мутация — `lambda old: new` (старый текст в новый не входит ни куском).

Скрипт захода (`_scratch_frame0901_0901/write_frame.py`) держит только строковую логику: чтение
файла, сверка хеша, мутация, печать чисел. **Секретов он не читает** — ни `load_env`, ни
`BRIDGE_*` в нём нет; конфиг моста берёт сам процесс писателя. Единственная правка поведения —
`brain_writer.HTTP_TIMEOUT = 240` на время захода: штатных 30 с на плечо для узла этого размера
мало с запасом (известный класс — таймаут писателя на крупном узле).

Встроенные замки писателя, сработавшие штатно:

```
СТАТУС: ok; док name:shtab_frame; 23967 → 25094 символов
БЭКАП СТАРОГО ТЕКСТА: tmp/brain_backup_shtab_frame_0901_20260901-015402.txt
РАСПИСКА МОСТА: ok
ОБРАТНОЕ ЧТЕНИЕ (внутри apply): 25094 символов, sha256 = d4315466194f9f6a…ad6b1bab
```

`marker="версия 01.09.2026 #1"` требовал **ровно одно** вхождение — это ловушка на гибрид
редакций (известный вид отказа этого дока: шапка новая, тело склеенное). Маркер прошёл.
Расписка моста пришла `ok`, но судит здесь не она, а чтение (класс «расписка ≠ факт записи»).

**Откат** — запись файлом бэкапа `tmp/brain_backup_shtab_frame_0901_20260901-015402.txt` тем же
`apply`; бэкап снят писателем ДО записи, автоматически, и в git не попадает.

## 5. Обратное чтение — НЕЗАВИСИМЫМ процессом, сверка посимвольная

Чтение внутри `apply` доказательством по заданию не считается: это тот же процесс и та же
сессия. Поэтому второй скрипт (`_scratch_frame0901_0901/witness.py`) запущен **отдельным
вызовом**, читает док заново по имени и ничего не пишет:

```
ЖИВОЙ ДОК ПО ИМЕНИ KB_shtab_frame: 25094 символов, строк 478, U+FFFD 0, CR 0
ИСТОЧНИК: 25094 символов, строк 478
sha256 живого:    d4315466194f9f6a72380a805cca8c521fd66b65815576c144cc5323ad6b1bab
sha256 источника: d4315466194f9f6a72380a805cca8c521fd66b65815576c144cc5323ad6b1bab
ПОСИМВОЛЬНАЯ СВЕРКА: РАВНО, расхождений 0 (сверено 25094 символов)
ШАПКА ЖИВОГО: ═══ РАМКА ПРОЕКТА TurboBaby — ИНСТРУКЦИИ ШТАБА (версия 01.09.2026 #1) ═══
«версия 01.09.2026 #1» в живом доке: 1 раз
«22.08.2026» (прошлая редакция) в живом доке: 0 раз
```

Сверка именно **посимвольная**, а не «текст найден в доке»: свидетель ищет позицию первого
расхождения по всей длине и печатает окрестность, если она есть. Проверка через `in` или через
одну длину не отличила бы усечённый док от целого — ровно та развилка, ради которой в `apply`
живёт сверка длины отправленного. Здесь сошлись все три независимых признака: длина, sha256 и
позиционное равенство.

Строка «`22.08.2026` → 0 раз» отвечает на отдельный вопрос: старая редакция не осталась хвостом
ниже новой. Замена, а не наслоение.

## 6. Границы захода (что НЕ делалось)

- Других узлов мозга не касались: единственный вызов записи — `write_doc` по `shtab_frame`;
  `KB_MASTER`, `KB_PULSE`, `cowork_log` только не трогались (журнальная строка — штатным
  `cowork_log_append.py`, это отдельный канал ритуала).
- Кода репозитория не менялось: заход не редактировал ни одного трекуемого `.py`, оба скрипта —
  одноразовые, в `_scratch_frame0901_0901/` (untracked). Оговорка, чтобы не выдать чужое за
  своё: `git status` показывает `M CLAUDE.md` и `M todo.md` — они были изменены **до** этого
  захода и лежали так уже в стартовом снимке; заход их не касался.
- Ничего не удалялось, процессы не трогались, клиентский контур не трогался.
- Содержание новой редакции заход **не сочинял и не редактировал**: что было в файле — то и
  легло, побайтово. Смысловую правоту текста этот артефакт не удостоверяет, только доставку.

## 7. Файлы захода

| файл | что |
|---|---|
| `docs/tasks/frame-edition-20260901.txt` | источник, 25094 симв., sha `d4315466…` |
| `_scratch_frame0901_0901/write_frame.py` | запись целиком через `brain_writer.apply` |
| `_scratch_frame0901_0901/witness.py` | независимое чтение + посимвольная сверка |
| `tmp/brain_backup_shtab_frame_0901_20260901-015402.txt` | старая редакция 22.08.2026 #2 (откат) |
