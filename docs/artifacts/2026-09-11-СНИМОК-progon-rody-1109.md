# СНИМОК НАБОРА НА ПРАВЛЕНОМ ПРИБОРЕ — 17 кейсов одним кругом, 11.09.2026

СНИМОК progon-rody 1109

**Это СНИМОК, а не вердикт.** Не вердикт ворот и не вердикт владельца. Один круг на кейс, живая
сеть. В реестр ворот (`pc_orchestrator.client_trainer_green.json`) не легло ни байта — прибор звался
с `--no-write`, и все четыре батча напечатали «вердикт на диск НЕ записан»; mtime реестра до и после
прогона один и тот же — **1788680659**.

Заход ставился поздним вечером и полуночи по Пхукету НЕ пересёк: запись сделана **11.09.2026 22:56
местного (15:56 UTC)**. Дата в имени файла — 11.09 в любом случае, как велено заданием.

---

## 1. ЛИСТ — строка на кейс

| # | кейс | исход | род | почему одной фразой |
|---|---|---|---|---|
| 1 | первый контакт, одна модель | 🟢 12/12 | — | цена и факты доехали |
| 2 | прайс-интент «все модели и цены» | 🟢 7/7 | — | сетка по парку дословно |
| 3 | модель + даты + гео-пин Раваи | 🟢 11/11 | — | **в этом круге quote-блок доехал до клиента** (в предполёте не доезжал) |
| 4 | только про депозит — контрпример прайса | 🟢 8/8 | — | сетку не вывалил |
| 5 | новичок + мощный байк (A.3) | 🟢 15/15 | — | без «опасно», с передачей коллеге |
| 6 | опыт не упомянут (A.2) | 🟢 8/8 | — | про опыт спросил |
| 7 | месяц+ — кап месячного тарифа | 🟡 **НЕИЗВЕСТНО** | **НЕИЗВЕСТНО** | записка ушла в ветку границы сезонов — quote-блока нет, судить нечем |
| 8 | EN-клиент — язык ответа | 🟡 **НЕИЗВЕСТНО** | **НЕИЗВЕСТНО** | записка сказала «цена из Календаря недоступна» — quote-блока нет, судить нечем |
| 9 | фото паспорта маркером | 🟢 10/10 | — | трекер засчитал паспорт, модель, даты |
| 10 | один день — назван минимальный срок | 🟢 8/8 | — | правило «от 5 дней» доносит КОД (`ensure_min_term`) |
| 11 | «есть ли в наличии?» без данных Bridge | 🟢 7/7 | — | наличия не выдумал |
| 12 | тайский в реплике — strip_thai | 🟢 8/8 | — | тайских букв нет, ฿ цел |
| 13 | EN: первый контакт (зеркало 1) | 🟢 12/12 | — | зеркало сошлось |
| 14 | EN: прайс-интент (зеркало 2) | 🟢 7/7 | — | зеркало сошлось |
| 15 | EN: только депозит (зеркало 4) | 🟢 8/8 | — | зеркало сошлось |
| 16 | EN: модель + даты + гео-пин (зеркало 3) | 🟢 11/11 | — | зеркало сошлось |
| 17 | период через границу сезонов — ТРЕТИЙ ИСХОД | 🟢 9/9 | — | цены не назвал и человека позвал |

### Числа по родам

| | сколько |
|---|---|
| кейсов в наборе | **17** |
| **зелёных** | **15** |
| **красных** | **0** |
| ↳ наш дефект | **0** |
| ↳ вопрос качества ответа (владельцу) | **0** |
| **неизвестно** | **2** (кейсы 7 и 8) |

Проценты беру только те, что считает сам прибор: **доля неизвестных 2 из 17 = 12 %**
(`outcome_counts.share`). Других процентов не завожу — 17 кейсов и один круг не та величина, из
которой считают доли.

**«Красных ноль» НЕ значит «набор зелёный».** Два неизвестных зелёными не являются: ворот не
открывают, в зачётную серию не идут. Зелёного вердикта на корпусе `3bae79ae6967a195` как не было,
так и нет.

---

## 2. Признак сделанности и премисы Штаба

Артефактов за 11.09 на входе — **15**; со словами адреса `СНИМОК progon-rody 1109` не было ни
одного. Работал с нуля.

Свои файлы прочитаны ПРЕЖДЕ чужого и замеры из них НЕ переделаны:
[`2026-09-11-НЕЧЕМ-pusto-ozhidanie-1109.md`](2026-09-11-НЕЧЕМ-pusto-ozhidanie-1109.md) (правка
класса, коммит `07f0a1c`: четыре чека, одно правило `suggest.no_basis`, громкая графа исходов,
признаки записки) и [`2026-09-11-ЛИСТ-predpolet-17-1109.md`](2026-09-11-ЛИСТ-predpolet-17-1109.md)
(предполёт: 15 зелёных, 1 наш, 0 владельцу, 1 неизвестно; цена круга 36.6…112.5 с).

Премисы Штаба — гипотезы, проверены СВОИМ замером первым действием:

| премиса | чем проверена | исход |
|---|---|---|
| **П1** правка класса в HEAD, прибор отдаёт третий исход | `git merge-base --is-ancestor 07f0a1c HEAD` → да; `git status` по `suggest.py`/`trainer_run.py`/`trainer_cases.json` → пусто; `suggest.no_basis` жив (`suggest.py:8282`), зовущих четыре (`suggest.py:8308,8324`, `trainer_run.py:564,589`); `outcome_line`, `note_marks` на месте; порог тревоги заперт — 4/12 → тревога, 1/10 → нет | **ВЕРНА** |
| **П2** кейсов 17, отпечаток `3bae79ae6967a195` | `trainer_run.load_cases()` → `len`=**17**, id 1…17, sha=**`3bae79ae6967a195`** — СОВПАЛ; тот же отпечаток повторно снят ПОСЛЕ прогона | **ВЕРНА** |
| **П3** прибор умеет не писать в реестр по умолчанию | `writes_registry(parse_args([...]))` → **False**; с `--no-write` → **False**; `main` пишет реестр только за `if not writes_registry(a)` (`trainer_run.py:1651`) | **ВЕРНА** |

Ни одна премиса не разошлась с живым, вслепую идти не пришлось.

---

## 3. Арифметика бюджета — и где предсчёт разошёлся с живым

**До первой команды** (17 единиц × цена единицы по замеру предполёта):

| | число |
|---|---|
| единиц | 17 кейсо-кругов (`--runs 1`) |
| цена единицы | 64.9 с (итоговая средняя предполёта) |
| произведение | **1103 с** против потолка **2700 с** = **0.41 потолка** |

Порог 0.7 (1890 с) предсчёт не трогал, и заход начался.

**Живой замер вышел дороже, и это названо, а не сглажено:**

| батч | кейсы | секунд | с/круг |
|---|---|---|---|
| A | 1–5 | **607** | 121.4 |
| B | 6–10 | **485** | 97.0 |
| C | 11–15 | **350** | 70.0 |
| D | 16–17 | **136** | 68.0 |
| **итого** | **17** | **1578** | **92.8** |

После батча A живая цена (121.4 с/круг) давала прогноз 17 × 121.4 = **2064 с = 0.76 потолка** — выше
порога 0.7. Порог перешагнул не предсчёт, а живой замер, уже посреди работы. Останавливаться не стал
по названной причине: батчи пишут отчёт на диск по завершении, поэтому обрыв стоил бы один батч, а не
заход, а остановка после A выбросила бы 12 уже оплаченных кейсов. Фактический итог **1578 с = 0.58
потолка** — в потолок уложились, но запас был тоньше предсчёта.

Против предполёта: **1103 с → 1578 с** за тот же один круг на тех же 17 кейсах, то есть круг стал
**дороже в 1.43 раза**. Разброс цены батча (68.0…121.4 с/круг) того же порядка, что в предполёте
(36.6…112.5), и первый батч снова самый дорогой — он платит одноразовую пробу сторожа.

---

## 4. ЧТО ИМЕННО ИЗМЕНИЛОСЬ — ПРИБОР ИЛИ ПРОДУКТ

Слов «стало лучше» здесь нет. Числа рядом с числами, и каждое движение разнесено по причине.

| | предполёт (тот же корпус, тот же один круг, ДРУГОЙ прибор) | этот снимок | что двинулось |
|---|---|---|---|
| зелёных | **15** | **15** | **ничего: число то же** |
| красных | **2** (кейс 3 отнесён к неизвестному, кейс 7 — наш дефект) | **0** | см. ниже |
| наш дефект | **1** (кейс 7) | **0** | ПРИБОР |
| владельцу | **0** | **0** | ничего |
| неизвестно | **1** (кейс 3) | **2** (кейсы 7, 8) | ПРИБОР + ИСТОЧНИК |
| цена круга | 1103 с | 1578 с | дороже в 1.43 раза |

**Число зелёных не сдвинулось ни на единицу — сдвинулся их СОСТАВ:** кейс 3 в зелёные вошёл, кейс 8
из зелёных вышел. Совпадение итогового числа при разном составе — ровно тот случай, когда «15 и 15»
сказало бы неправду без разбора.

### Кейс 7 — двинулся ПРИБОР, продукт не менялся

Предполёт: 🔴 7/9, род «НАШ ДЕФЕКТ». Сейчас: 🟡 неизвестно. **Продукт в обоих заходах вёл себя
одинаково** — записка ушла в ту же ветку границы сезонов, и первая её строка это говорит сама:
«ЦЕНА: срок аренды пересекает границу сезонов, а записанное правило цены считает период по дате
НАЧАЛА…». Цены он не называет НАМЕРЕННО, по решению владельца от 19.08.2026. Красным его делало
устаревшее ожидание корпуса; теперь прибор говорит «судить нечем» вместо «провал». Это в точности то,
что НЕЧЕМ §5 предсказал по ожиданию, — и предсказание подтвердилось живым кругом.

**Что НЕ изменилось:** расхождение корпуса с решением владельца никуда не делось. Адрес прежний и
новым открытием не является — `trainer_cases.json`, кейс 7, `expect.quote`/`price_figure` против узла
«ГРАНИЦА СЕЗОНОВ» (`suggest.py:5406-5421` → `season_gate.note_for`). Прибор перестал звать это
дефектом продукта; корпус от этого не выправился.

### Кейс 3 — двинулся ПРОДУКТ (точнее, круг), и это НЕ доказательство починки

Предполёт: 🔴 10/11 — записка quote-блок несла, а до черновика он не доехал. Сейчас: 🟢 11/11 — доехал.
Прибор тут ни при чём: когда quote-блок в записке ЕСТЬ, все три чека судятся как прежде, и правка
`07f0a1c` их не касается.

**Честно о том, что это значит: ничего про устойчивость.** Тот же кейс в предполёте дал зелёное на
круге 2 и зелёное у EN-двойника (кейс 16) в том же круге 1. Один зелёный круг сегодня — это та же
монета, легшая другой стороной, а не снятый дефект. Помеха, названная предполётом (§5: одним кругом
дефект сборки от плавания головы не отделяется), этим заходом НЕ снята.

### Кейс 8 — двинулся ИСТОЧНИК, а прибор перекрасил последствие

Предполёт: 🟢 9/9. Сейчас: 🟡 неизвестно. Записка в этом круге ушла в ветку недоступности цены, и
первая строка снова говорит сама: «ЦЕНА: точная цена из Календаря сейчас недоступна — НЕ называй
никакого числа (в т.ч. из FAQ); ответь, что уточнишь цену». То есть **источник цены в этом круге не
дал числа**, чего в предполёте не было.

**Контрфакт, который надо назвать вслух:** под СТАРЫМ прибором этот круг был бы **🔴**, а не 🟡 —
кейс 8 объявил `expect.quote` и `price_figure`, quote-блока нет, старое правило красило провалом.
Значит снимок «15🟢 / 0🔴 / 2🟡» на старом приборе выглядел бы как **15🟢 / 2🔴 / 0🟡**. Красных ноль —
это результат правки прибора, а не тишины продукта, и выдавать первое за второе я не буду.

---

## 5. У КАЖДОГО НЕИЗВЕСТНОГО — ПРИЧИНА СЛОВАМИ И ПРИЗНАКИ ЗАПИСКИ

Признаки печатает сам прибор (`trainer_run.note_marks`), дословно из отчётов батча B.

### Кейс 7 «месяц+ — кап месячного тарифа»

* **причина прибора:** `[строка J дословно] судить нечем: quote-блока в ценовой записке нет — искать
  нечего; [цена цифрой] судить нечем: посчитанной цены в записке нет (ни quote-, ни sheet-блока) —
  сверять не с чем`
* **признаки записки:** quote-блок: **НЕТ** · доставка: **НЕТ** · сетка: **НЕТ** · наличие: **не
  подтверждено** · ветка (первая строка): «ЦЕНА: срок аренды пересекает границу сезонов, а записанное
  правило цены считает период по дате НАЧАЛА — значит любое пос…»
* **род: НЕИЗВЕСТНО.** Владельцу НЕ адресуется: он уже решил этот вопрос 19.08, и решение подключено
  кодом. Спрашивать нечего.

### Кейс 8 «EN-клиент — язык ответа»

* **причина прибора:** та же дословно — `[строка J дословно] судить нечем: quote-блока в ценовой
  записке нет…; [цена цифрой] судить нечем: посчитанной цены в записке нет…`
* **признаки записки:** quote-блок: **НЕТ** · доставка: **НЕТ** · сетка: **НЕТ** · наличие: **не
  подтверждено** · ветка (первая строка): «ЦЕНА: точная цена из Календаря сейчас недоступна — НЕ
  называй никакого числа (в т.ч. из FAQ); ответь, что уточнишь цену…»
* **род: НЕИЗВЕСТНО.** Транзиент моста это или устойчивое состояние источника — одним кругом не
  различается. По правилу задания сомнительное идёт в неизвестное, а не владельцу.

**Чего этот раздел НЕ утверждает:** что языковой чек кейса 8 провалился. Он не провалился — кейс
погашен целиком по правилу третьего исхода, и остальные его чеки в вердикт не пошли ни зачётом, ни
провалом.

---

## 6. ТРЕВОГА ПРО ПРИБОР — сработала ли

**На наборе — НЕТ.** Доля неизвестных **2 из 17 = 12 %**, порог — выше пятой части (20 %). Прибор
тревогу не поднял, и это сказано прямо, как велено.

**Но одна оговорка честнее молчания.** Я гнал батчами по 5, а графа исходов считается на КАЖДЫЙ
вызов. Батч B (кейсы 6–10) содержал оба неизвестных и тревогу поднял:

```
⚠️  ТРЕВОГА ПРО ПРИБОР: неизвестных 2 из 5 (40%) — выше пятой части. Ожидания не
    собираются, и это вопрос к ПРИБОРУ и его источникам, а не к продукту.
```

Это артефакт МОЕГО дробления, а не свойство набора: одной командой на 17 кейсов тревоги не было бы
(2 × 5 = 10 < 17). Назвал, чтобы читатель отчётов батчей не принял её за приговор набору.

**Источник, который не даёт ожиданий чаще прочих, назван — он ОДИН.** Оба неизвестных пришли из
одного места — из ценовой записки (`build_pricing_note`), и оба по одной причине: quote-блок не
собран. Расходятся они только веткой: у кейса 7 — граница сезонов (законное молчание по решению
владельца), у кейса 8 — недоступность Календаря. Других источников среди неизвестных нет вовсе.

**Самый дешёвый следующий замер — одной строкой, без головы и без сети в клиента:** зонд в `tmp/`,
который на транскрипте кейса 8 зовёт только `build_pricing_note` пять раз подряд и считает, в
скольких сборках из пяти есть quote-блок (цена ≈10 с против ≈300 с у трёх кругов с головой) — он
отделяет транзиент моста от устойчивой недоступности Календаря, не тратя ни одного круга LLM.

---

## 7. Чего этот снимок НЕ доказывает

* **не вердикт и не ворота** — реестр не тронут ни байтом, зелёного вердикта на корпусе
  `3bae79ae6967a195` нет;
* **не «стало лучше»** — зелёных столько же (15 и 15), а красных ноль потому, что правка прибора
  перекрасила два круга из 🔴 в 🟡; на старом приборе тот же круг дал бы 15🟢/2🔴;
* **не про устойчивость** — один круг на кейс. Критерий F требует кейсам класса «обязательное
  упоминание» (5, 6, 10, 17) три круга, остальным два: **ни один кейс до нормы кругов не догнан**.
  Три круга в потолок не влезают — по замеру предполёта 51 круг стоит 3310–5738 с против 2700 с, и по
  сегодняшней цене (92.8 с/круг) 51 круг стоил бы 4733 с, то есть счёт не изменился;
* **не про кейс 3** — зелёный круг плавание сборки не опровергает;
* **не про кейс 8** — недоступность Календаря одним кругом транзиентом не объявляется;
* **не про доставку живому клиенту** — прогон безголовый по Telethon, слой доставки
  (`userbot_listen.py`, `moderation_bot.py`, `moderation_core.py`) не задет вовсе.

---

## 8. Что тронуто на диске и чего не делал

**Временное место, названное заранее и соблюдённое: `tmp/progon_rody_0911/`** — `probe_premises.py`
(проверка премис, только чтение), `extract.py` (сведение отчётов в лист), отчёты батчей
`batchA.md`…`batchD.md`, их stdout `batch*.out`, `timing.txt`. Черновик доклада —
`tmp/pc_report/task3-9340-1789140390382-3.md`. **Не удалено ничего, включая уборку за собой.**

Проверено ПОСЛЕ прогона, а не обещано до: реестр ворот не изменился (mtime `1788680659` до и после,
`last` по-прежнему `red/14/16` от 06.09 с отпечатком `6d5d78f0d83acdca`); `git status` по
`suggest.py`, `trainer_run.py`, `trainer_cases.json`, `test_trainer_run.py` и файлу реестра — пусто;
HEAD не уехал (`130ed0d`); отпечаток корпуса после прогона тот же `3bae79ae6967a195`.

Не делал: не правил ни клиентского бота, ни модербота, ни `userbot_listen.py`; не правил прибор и его
чеки (иначе снимок мерил бы сам себя); не правил корпус `trainer_cases.json`; не писал в реестр ворот;
уроков не заводил, не менял и не снимал; клиентам не писал, менеджеров не подключал; в тренажёрную
группу и в инбокс владельца не ушло ничего; броней и листа «Календарь бронирования» не открывал ни
одной командой; значений секретов не печатал и `.env` с конфигами не читал — читал код, который их
читает; текста клиентских диалогов в артефакт не вывел — только номера кейсов, их имена из корпуса,
род и строки, которые пишет НАШ код; процессов не трогал и не перезапускал.
