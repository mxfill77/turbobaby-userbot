# Перепись классов карточек, доходящих до владельца — КАРТОЧКИ-КЛАССЫ-ПЕРЕПИСЬ-ОТКАТ-1009

**Дата:** 10.09.2026 (заход 11.09 ночью, ключ Штаба `20-a-kartochki-perepis.1009`).
**Полоса:** ПК. **Заход read-only:** ни одной ветки гарда, ни одного порога, ни одного класса не
тронуто; наружу владельцу из этой проверки не ушло ничего.
**Временное место захода:** `tmp/pc_report/` (черновик доклада + `census_0910.py`, читатель логов).

**Окно чисел:** `04.09.2026 01:00 — 11.09.2026 01:00` местного (UTC+7), ровно 7×24 ч.
Конец окна — момент переписи, поэтому последнее зафиксированное нажатие владельца
(11.09 00:37 местного) в окно попадает.

**Чем измерено** (всё — файлы следа, живой ряд моста НЕ опрашивался):
`pretool_guard.log` + `.1` + `.2` (22 818 строк, окно покрыто целиком) · `pc_orchestrator.log` ·
`dispatch_notify.log` · `pc_agent.log` + `.1` + `.2` · `pc_orchestrator.card_watch.json` ·
`pc_orchestrator.client_release.json` · `pc_orchestrator.shtab_box_hold.jsonl` ·
`pc_orchestrator.revizor_verdicts.json` · `zayavki_pc_state.json` · `review_intake_state.json` ·
`zayavki_route_pc_state.json`.

---

## 0. Ответ в одну строку

За семеро суток владельцу ушло **не менее 190 обращений**, из них решения ждали **≈155**.
Ответ владельца получили **12** (9 «да» по заявкам, 1 «нет» по ревизору, 2 нажатия по ящику).
**Настоящий «нет» за окно — ОДИН** (owner-карточка ревизора #198). Ещё **один «нет» система
потеряла** (ворота, 05.09 10:29 — `ok=False`, записи об отказе нет вовсе). Все **три** карточки
гарда, дошедшие рядом очереди, закрылись **таймаутом**: ответов — ноль.

---

## 1. Перепись классов (имя из кода, файл и строка рождения)

Отдельная колонка **«высший класс»** — решение Штаба, заходом не обсуждается: деньги, живые
таблицы, клиентский контур, выкатка прода, удаление вне временного места, снятие живых процессов.

### 1.1 Классы РЕШЕНИЯ — владелец должен ответить

| № | имя класса в коде | рождается (файл:строка) | канал к владельцу | кнопки | высший? |
|---|---|---|---|---|---|
| **К1** | решение `"ask"`, вид `kind` ∈ `_KIND_VOCAB` (19 имён, `pretool_guard.py:3950`) | `pretool_guard.py:5196` `_card()` · решение `:5394` `card_decision()` · выход `:5802` `_emit_ask()` | ДВА: (1) прямой пуш `_push` `:5649` → `dispatch_notify` → инбокс 1160; (2) файл-маркер `_write_marker` `:5748` → демон `_detect_needs_approval` `pc_orchestrator.py:1985` → ряд `needs_approval`, тема 829 | канал 1 — нет; канал 2 — ✅/❌ рисует devbot VPS (в этом репо кода кнопок НЕТ) | 6 из 19 — `_TOP_TIER` `pretool_guard.py:4801` |
| **К2** | `ROUTE_CARD = "card"` (`client_contour.py:817`), `callback_data` `gate:yes|no:<commit>` | текст `client_contour.py:1024` `card_text()` · отправка `pc_orchestrator.py:7769` `_client_block()` · дверь `:1311` `_notify_gate_card` | `dispatch_notify.py:950-961` → инбокс 1160 | ✅ `_gate_markup` `dispatch_notify.py:282` | **ДА** — клиентский контур |
| **К3** | заявка внешнего канала ступени G, `callback_data` `zayavka:yes|no:<tid>` | текст `zayavki_pc.py:777` `message()` · кнопки `:831` `buttons()` · отправка `zayavki_pc_run.py:508` | `dispatch_notify.deliver` → инбокс 1160 | ✅ | нет |
| **К4** | `RELEASE_CB = "box:free:%s"` (`shtab_box_signals.py:1429`), схема `HOLD_SCHEMA` | `shtab_box_signals.py:1497` `hold_notice()` · отправка `shtab_box_run.py:1203` | `send_critical` → инбокс 1160 | ✅ ОДНА («🔓 Снять остановку») | нет |
| **К5** | `REVIZOR_OWNER_MARK = "[ревизор-находки]"`, `from = "Filipp-revizor"` (`pc_orchestrator.py:12139-12140`) | текст `:12418` `_revizor_owner_card_text` · постановка `:12472` `_revizor_post_owner_card` | `set_needs_approval(topic=829)` | ✅ рисует devbot VPS | **ДА** — находки правят `suggest.py` (клиентский контур) |
| **К6** | заявка ступени B, `REVIEW_CLAIM_MARK = "[заявка-ревью"` (`pc_orchestrator.py:11213`), `from = "Filipp-review-claim"` (`review_intake_run.py:68`) | `review_intake_run.py:405` `place()` · текст `review_intake.py:660` | ряд `needs_approval` → devbot | ✅ devbot | нет |
| **К7** | заявка ступени E, `RECON_ASK_MARK = "[разведка-заявка"` (`pc_orchestrator.py:11450`), `from = "Filipp-recon-ask"` | `recon_auto_run.py:354` `place_ask()` · текст `recon_auto.py:565` | ряд `needs_approval` → devbot | ✅ devbot | нет |
| **К8** | вид `"notification"` (хук Claude Code) | `dispatch_notify.py:892` | `deliver` → инбокс 1160 (признак `_RE_ASKS` `:502`) | нет — владелец отвечает в самой сессии | нет |
| **К9** | карточка черновика клиенту: `ACT_SEND/ACT_EDIT/ACT_REJECT` (`moderation_card.py:53-55`), статусы `DECISION_STATUSES` (`moderation_ipc.py:245`) | `moderation_bot.py:123` `job_poll_new`, отправка `:148` | модер-группа | ✅ `_kb_initial` `moderation_bot.py:84` | **ДА** — клиентский контур |
| **К10** | заявка «В CRM», `callback_data` `crm:{intake_id}` | `moderation_bot.py:220` `_post_booking_card`, отправка `:256` | модер-группа реплаем | ✅ `_kb_intake` `:214` | **ДА** — деньги/бронь |
| **К11** | панель тренажёра: `tr:reset` · `tr:crm` · `tr:teach` · `tr:hyp:*` | `moderation_bot.py:306`/`:315`, отправка `:427`, `:544` | группа «Тренеровка» | ✅ | нет |
| **К12** | owner-карточка неясного урока, `build_owner_clarification_card` | `lesson_router.py:545`; поводов 4 (`:769`, `:846`, `:929`, `:954`) | `pc_orchestrator.py:3446` `_deliver_owner_card` → `send_critical` → 1160 | **НЕТ кнопок** | нет |
| **К13** | заявка тренажёра «нужен код-фикс», `from = "Filipp-trainer"` (`trainer.py:1072`) | `trainer.py:1285` `code_fix_claim()` | ряд `needs_approval` → devbot | ✅ devbot | нет |

### 1.2 Классы ИЗВЕЩЕНИЯ — занимают инбокс, но решения не просят

Считаются отдельно, потому что надзора не покупают вовсе: ответить на них нечем.

| имя | рождается | в окне |
|---|---|---|
| О2-тревога «⛔ ДЕМОН ЖИВ ПРОЦЕССОМ, НО НЕ РАБОТАЕТ» | `expectations_pc_run.py` → `deliver` | **21** |
| «⚠️ self-update … провалил unittest-гейт» | `pc_orchestrator` self-update | **8** |
| «ℹ️ pc_agent изменён — ЖДЁТ РУЧНОГО рестарта» | там же | **8** |
| «🚨 ВЫКАТКА КЛИЕНТСКОГО КОДА БЕЗ ВОРОТ» | `deploy_voice` | **5** |
| «🔇 … ПОДНЯТ В БЕЗОПАСНЫЙ РЕЖИМ» | `deploy_voice.announce_safe_mode` | **5** |
| «⛔ авто-выкатка … ОСТАНОВЛЕНА» (текстом, без кнопок) | `client_contour` под заморозкой | **5** |
| «⛔ авто-рестарт … ОТМЕНЁН» | `pc_orchestrator` | **1** |
| «📣 отказ гарда · ПК · задача N · команда ОТКЛОНЕНА» (тема 829) | `mark_vs_address_pc.py:150` `notice()` | **3** |
| **итого извещений** | | **56** |

### 1.3 Что карточкой НЕ является (важные «нет»)

- `deny` (`pretool_guard.py:5256` `_deny_text`, выход `:5829` `_emit_deny`) — **до владельца не
  доходит вовсе**, ни маркером, ни пушем. 40 отказов за окно видела только модель.
- `journal` (`card_decision` `:5461`) — **ветка недостижима**: `card_gate()` возвращает `True`
  безусловно (`:5391`).
- `card_or_journal` (`:5468`) — помощник ТЕСТОВ, в проде 0 вызовов.
- `card_duty.py`, `mark_vs_address_pc.py`, `card_load.py`, `card_terminal_log.py` — карточек не
  рождают, только СНИМАЮТ или пишут след.
- `decision_waits.digest_text` (`:303`) и `answer_class` (`:340`) — код сводки открытых решений
  есть, **отправителя и обработчика ответа нет**: в проде живут только `park` и `stale_explain`.

---

## 2. Числа за окно 04.09 01:00 — 11.09 01:00

| класс | выписано | дошло до владельца | ответ получил | ждёт до сих пор | ждало >1 суток |
|---|---|---|---|---|---|
| **К1** карточка гарда | 14 вердиктов `ask` | 7 пушем в инбокс · 3 рядом с кнопками | **0** | **1** (#91, `env`, открыта 01.09 14:20 UTC — **220 ч**) | **1** |
| **К2** ворота контура | 17 с кнопками + 5 текстом | 22 | **1 нажатие, ПОТЕРЯНО** | **1** (`asked` = `4528917`, 26.9 ч) | **1** |
| **К3** заявка ступени G | 3 карточки + 1 напоминание | 4 | **9 нажатий, все «да»** | по реестру судить нельзя (см. §2.3) | — |
| **К4** ящик Штаба | 2 остановки | 2 | 2 нажатия (1 сорвалось, 1 сработало) | 0 | 0 (max 23.5 ч) |
| **К5** owner-карточка ревизора | 1 (`tid=198`, 04.09 16:30) | 1 | **1 «НЕТ»** (05.09 04:15 UTC, ждала 18.7 ч) | 0 | 0 |
| **К6** заявка ступени B | **130 рядов** | все, чей `from` девбот знает (§2.4) | **0** | ≥52 (130 минус 78 снятых отбором) | почти все |
| **К7** заявка ступени E | 1 (#232, 09.09) | 1 (тема снята — карточка всё равно пришла) | 0 | 1 | 1 |
| **К8** хук `notification` | 4 | 4 | след наружу не остаётся | — | — |
| **К9–К11** модер-контур | **НЕ ИЗМЕРЕНО** | | | | |
| **К12** карточка урока | 0 в окне | — | — | — | — |
| **К13** заявка тренажёра | 0 в окне | — | — | — | — |
| **извещения (1.2)** | 56 | 56 | ответить нечем | — | — |

**Итого владельцу за 7 суток: ≥190 обращений; решения ждали ≈155; ответ получили 12.**

### 2.1 Карточка гарда — разбор 14 вердиктов

По видам: `env` 11 · `sqlite` 2 · `kill` 1. По роли: **headless 10, interactive 4.**

- **Все 4 interactive** дошли до владельца ровно как 4 доставки хука `notification`
  (07.09 22:31 / 22:33 / 23:19→23:20 / 23:35→23:36 — совпадение поминутное). Это отдельный
  канал: рядом очереди они не становятся и ответа в файлах не оставляют.
- **7 из 14** ушли прямым пушем в инбокс (`итог: channel=inbox ok=True`): 05.09 09:38 `env`,
  06.09 04:54 `sqlite`, **07.09 00:23 `kill` — «⛔ ВЫСШАЯ ЦЕНА · НЕОБРАТИМО»**, 07.09 23:19 `env`,
  08.09 10:04 `env`, 09.09 15:22 `env`, 10.09 00:02 `env`.
- **3 из 14** стали рядом `needs_approval` с кнопками — и все три умерли таймаутом:

| ряд | вид | объект | открыта → закрыта | исход |
|---|---|---|---|---|
| #239 | `env` | скрыт | 05.09 02:56 → 03:45 UTC (49 мин) | **истекло** |
| #72 | `sqlite` | таблица `collections` | 05.09 21:59 → 22:50 UTC (51 мин) | **истекло** |
| #210 | `env` | скрыт | 08.09 03:06 → 03:55 UTC (49 мин) | **истекло** |

`FAIL причина=approval_timeout` — `pc_orchestrator.py:4634` `process_approval_timeouts`,
срок `APPROVAL_TTL` = 46 мин (`:185`).

### 2.2 Реестр ответов `pc_orchestrator.approvals.jsonl` мёртв

Файл не менялся с **31.07.2026**, в нём 5 записей, и **все пять — `origin=agent`**
(`by=Filipp` там же означает агента, а не человека; две — `smoke-check-85`/`smoke-85-rerun`,
одна — `task=999999`). Живые ответы владельца идут через devbot VPS и в этот реестр не попадают
вовсе. **Судить по нему о надзоре нельзя** — он покажет ноль при любом числе живых решений.

### 2.3 `zayavki_pc_state.json` не годится для счёта ожидания

`sent` (27 ключей) — это реестр **дедупа и напоминаний**, он не чистится никогда:
`answer()` (`zayavki_pc_run.py:562`) зовёт `q.accept/q.reject` **на ряде моста**, а в
`state["closed"]` пишет только отбор (`SIFT_OUTCOME = "снято отбором"`, все 200 записей).
Поэтому «27 выписано, 0 закрыто» читается как «27 ждут», а на деле часть из них отвечена
(нажатия по qid 156, 79, 32, 26, 27, 28, 18, 214 в логе агента есть). **Ждёт до сих пор по
этому классу — НЕИЗВЕСТНО** из локального следа.

### 2.4 130 рядов ступени B — самый крупный класс, и он невидим в логе демона

`review_intake_state.json` → `claims` c `placed_at` в окне: **130**. Из них **78** закрыл наш же
отбор («снято отбором»), то есть решение по ним принимал НЕ владелец. Замок маршрута
`_route_needs_approval` (`pc_orchestrator.py:894`) снял тему у **одного** ряда за всё время
(`zayavki_route_pc_state.json` → `routed` = 1 запись, #232), и **даже он карточку не погасил**:
комментарий-замер в `pc_orchestrator.py:1084-1091` говорит прямо — *«Карточку рождает НЕ ТЕМА,
а САМ РЯД: её несёт devbot VPS, выбирая ряды по метке `from`… 09.09 в 17:25:13 ряд #232 встал
`needs_approval`, в 17:25:16 тема была снята — и карточка владельцу всё равно пришла.»*

Отсюда честная оговорка: **сколько из 130 физически стало карточкой в Telegram, с полосы ПК не
видно** — доставку выполняет чужая полоса. Нижняя граница — все, чей `from` девбот знает.

---

## 3. ГЛАВНЫЙ ВОПРОС: был ли хоть один ответ «нет»

| класс | «нет» в окне | «нет» за всё время класса |
|---|---|---|
| **К5** owner-карточка ревизора | **ДА — 1** | **ДА — 3** (#27, #42, #198) |
| **К1** карточка гарда | **НЕТ** | ДА — 9 «отказано» из 20 терминалов; последний **03.09 16:42**, то есть до окна |
| **К2** ворота контура | **попытка была, записи нет** | **НИ ОДНОГО** — ключа `denied` в `pc_orchestrator.client_release.json` нет вовсе |
| **К3** заявка ступени G | **НЕТ** (9 нажатий, все «да») | ДА — 11 «нет», **все 03.09**, в день заведения класса |
| **К4** ящик Штаба | **невозможно** | **невозможно** — кнопка ОДНА, ответа «нет» у класса не существует |
| **К6/К7** заявки B/E | **НЕТ** | нет следа ни одного |
| **К8** `notification` | **НЕИЗВЕСТНО** | следа наружу не остаётся вовсе |
| **К9–К11** модер | **НЕ ИЗМЕРЕНО** | |
| **К12/К13** | 0 выписок в окне | |

**Единственный настоящий «нет» окна** — `pc_orchestrator.revizor_verdicts.json`:

```
"#92|1791810538|депозит без противоречий":
    verdict = "ложная"
    at      = 2026-09-05T04:15:34.795452+00:00
    why     = "отказ владельца по карточке #198: (кнопка)"
```

Карточка #198 создана 04.09 16:30 местного (`ревизор: owner-карточка создана (tid=198, инбокс 829)`),
отказ пришёл через **18.7 ч**.

### 3.1 Второй «нет» система ПОТЕРЯЛА, и это замерено

`pc_agent.log.1`:

```
2026-09-05 10:29:12,728 | INFO | chain-callback: data='gate:no:4528917' uid=504608015 → action=None ok=False
2026-09-06 23:55:18,055 | INFO | chain-callback: data='box:free:ddac4cc3b98a' uid=504608015 → action=None ok=False
2026-09-11 00:37:46,337 | INFO | chain-callback: data='box:free:db2bbe4aa216' uid=504608015 → action=free ok=True
```

Владелец нажал «⛔ Не выкатывай» — и получил «карточка устарела». Причина названа в самом коде
(`pc_agent.py:1031` `_chain_cb_route`, комментарий у ветки `parsed is None`): обработчик кнопки
ворот внесён коммитом `4bbe548` в **05.09 05:20**, живой процесс агента стартовал 03.09 и
перезапустился только в 12:06 — **кнопка была новее процесса, который её ловил**. Тот же класс
повторился 06.09 с ящиком: `2cb08e0` лёг в 21:34, нажатие в 23:55 сорвалось.

**Цена класса — ровно то, ради чего заведена перепись:** за 7 суток владелец сказал «нет»
дважды, а система записала один раз. Из двух «нет» одно — по **высшему классу** (выкатка на
клиентский контур).

### 3.2 Вывод по п.3

Надзора НЕ покупают (за окно ни одного «нет»): **К1** (гард), **К2** (ворота — за всё время
ни одного), **К3** (заявки G), **К6**, **К7**, **К8**. Класс **К4** не может купить надзор
конструктивно: у него нет кнопки «нет». Единственный класс, где надзор в окне сработал, — **К5**.

---

## 4. Откат по каждому классу: чем отменяется разрешённое и проверялся ли

**Общий факт, который надо назвать первым: автоматического отката исполненной операции нет
НИ У ОДНОГО класса полосы ПК.** Везде либо текстовая строка-инструкция человеку, либо «отменять
нечего, потому что ничего не применялось».

| класс | откат | где живёт | проверялся? | кандидат на приём без человека? |
|---|---|---|---|---|
| **К1** гард | ТЕКСТ в карточке: словарь `_ROLLBACK` (`pretool_guard.py:4628-4654`), `_ROLLBACK_PY` (`:4660`), `_env_rollback` (`:4689`). Само «да» гаснет само: живёт в env одного дочернего запуска (`PRETOOL_APPROVED_KINDS` `:3870`), процесс кончился — разрешения нет; команды «отозвать» НЕТ | текст | **НЕТ следа ни одной проверки.** Есть замок от вранья: `_rollback_conflicts` (`:4760`) снимает строку, называющую ЧУЖОЙ объект | **НЕТ** — откат не автоматизирован и не проверен |
| **К2** ворота | `git revert --no-edit <commit>` (`client_contour.py:1072`). «Да» само по себе ничего не выкатывает — только пишет основание (`pc_orchestrator.py:3143`); «нет» не откатывает ничего и глушит форму на `DENY_MUTE_SEC = 24 ч` | текст | **НЕТ** | **НЕТ** — высший класс (клиентский контур), решением Штаба остаётся владельцу целиком |
| **К3** заявка G | отката НЕ ТРЕБУЕТСЯ по построению: **оба** ответа задач не рождают (`pc_agent.py:684-686`, слова ответа `zayavki_pc_run.py:573-578`) | — | конструкция | **ДА** — единственный класс, где «да» ничего не исполняет |
| **К4** ящик | сама кнопка и есть снятие; «не снимать» = бездействие. Исполненного нет — остановка только запрещает БРАТЬ задания | код | **ДА, 10.09 сработало** (`by="Telegram"`, `released:true`) | **ДА** — но смысла мало: у класса нет «нет», надзора он и так не покупает |
| **К5** ревизор | вердикт «ложная» в `pc_orchestrator.revizor_verdicts.json` — сам себе откат: находка перестаёт выписываться | код | **ДА, и с числом**: по ключу #27 — **18 глушений**, по #42 — **9**, по #198 — **2** | **НЕТ** — высший класс: находки правят `suggest.py`, клиентский контур |
| **К6/К7** заявки B/E | нечего откатывать: «да» задачи НЕ рождает (`pc_orchestrator.py:4517-4536`) | конструкция | конструкция | **ДА** |
| **К8** `notification` | нет | — | — | **НЕТ** — предмет решения неизвестен заранее |
| **К9** модер-черновик | `supersede_open` (`moderation_ipc.py:339`) гасит предложение, если клиент написал снова. **Отправленное клиенту не отзывается ничем** — это прямо сказано в `expectations_pc.py:2094` | частично код | — | **НЕТ** — высший класс |
| **К10** «В CRM» | отмены нет; повторный тап отбивается предусловием `status='draft'` | — | — | **НЕТ** — высший класс (деньги/бронь) |
| **К11** панель тренажёра | `tr:hyp:cancel` — мультивыбор не станет правилом до `✔ Применить` | код | — | **ДА** |
| **К12** карточка урока | ничего не применялось | — | — | **ДА** |
| **К13** заявка тренажёра | своего отката нет; общий `APPROVAL_TTL` | — | — | **НЕТ** — откат не назван |

**Прямым текстом, как просил п.4:** отката нет или он не проверен у **К1, К2, К8, К9, К10, К13**
— эти классы кандидатами на приём без человека **не становятся**. Проверен откат ровно у двух:
**К4** (живое снятие 10.09) и **К5** (18 + 9 + 2 глушения по вердикту).

---

## 5. Рождено настоящей операцией против рождённого нашим дефектом

| рождено НАСТОЯЩЕЙ операцией | число в окне |
|---|---|
| К1 `kill` — `taskkill /PID 25688 /F` (высшая цена, необратимо) | 1 |
| К1 `sqlite` — запись в таблицу `collections` | 1 |
| К1 `sqlite` — `nonparse_scan.py` | 1 |
| К2 ворота на РЕАЛЬНЫХ коммитах (`4528917`, `ece01cc6d`, `78cde76`) | 3 формы |
| К5 owner-карточка ревизора #198 | 1 |
| **итого надзора по существу** | **≈7** |

| рождено НАШИМ ДЕФЕКТОМ (ремонт руками, не надзор) | число в окне |
|---|---|
| **К2: залп 15 карточек ворот за 20 секунд** 05.09 05:14:37–05:14:56, из них **8 несут НЕСУЩЕСТВУЮЩИЕ коммиты** — `new777` ×4 и `452891745` ×4 (`git cat-file -t new777` → `fatal: Not a valid object name`). Это проба, утёкшая владельцу в боевой инбокс | **8** |
| **К1 на ДАННЫХ, а не на операции**: `printf '%s' 'DONE Dispatch …'` (08.09 10:04 и 10.09 00:02) — красное слово стои́т в ТЕКСТЕ журнальной записи; `grep -rn "SUGGEST_TEST_MODE" .env.example` (09.09 15:22) — файл-пример, не секрет; `cat > tmp/key_trace_0910/hitfiles.txt` (10.09 00:48) | **4 из 14** |
| извещения §1.2 целиком — про наши поломки (демон не даёт оборота, гейт красный, агент не перезапущен, выкатка мимо ворот) | **56** |
| два потерянных нажатия владельца (кнопка новее процесса) | **2** |
| **итого ремонта руками** | **≈70** |

**Соотношение: на каждое обращение по существу приходится десять про наши поломки.**
Отдельно стои́т отметить нарушение того самого правила, которое стои́т в задании этого захода
(«из проверки владельцу не уходит ничего»): 05.09 оно было нарушено чужим заходом на 15 карточек
разом. Глушение пробы у гарда есть и работает (`_push` → `isolated()`, `pretool_guard.py:5654`),
а у карточки ворот такого замка **нет**.

---

## 6. Где ФИЗИЧЕСКИ живёт решение о классе

**Не в одной функции. В семи местах, и четыре пары из них умеют разойтись молча.**

| # | что решает | где живёт |
|---|---|---|
| 1 | **какой класс у операции** | `pretool_guard.decide()` `:3826` → `decide_for_role()` `:4554` → `card_decision()` `:5394` → `_card()` `:5196`. Внутри `decide` класс собирают ЧЕТЫРЕ разных разборщика: `_RED_CMD` (текст команды), `_decide_read`/`_decide_write` (путь цели), `_scan_python`/`_py_write_call` (тело скрипта), `_sqlite_decide` (оператор запроса) |
| 2 | **какие имена вообще существуют** | `pretool_guard._KIND_VOCAB` `:3950` (19) и `pc_orchestrator.PREAMBLE` `:435` (12) |
| 3 | **высший ли класс** | `pretool_guard._TOP_TIER` `:4801` + `is_top_tier()` `:4805` (6 имён) |
| 4 | **чем кончилась карточка** | `card_terminal_log.classify()` `:136` |
| 5 | **какого рода заявка** | `zayavki_pc.split_awaiting()` — по МАРКЕРУ В ПРОЗЕ тела ряда |
| 6 | **быть ли карточке вообще** | `_route_needs_approval` `pc_orchestrator.py:894` + `zayavki_route_pc.card_topic` (тема) · `_maybe_mark_vs_address` `:3950` (`MARK_VS_ADDR`, дефолт **ВКЛ**) · `_maybe_card_duty` `:3855` (`CARD_DUTY`, дефолт **ВЫКЛ**) · `zayavki_pc.route` (`card` / `card_over` / `digest`) |
| 7 | **кто рисует кнопки** | ПК: `dispatch_notify._chain_markup` `:273` / `_gate_markup` `:282`, `zayavki_pc.buttons` `:831`, `shtab_box_signals.release_buttons` `:1483`. VPS: devbot — для ВСЕХ рядов `needs_approval`, выбирая их по метке `from` |

### Где они РАСХОДЯТСЯ (каждое — замер, а не опасение)

1. **`_KIND_VOCAB` (19) ↔ `PREAMBLE` (12).** Семь классов гард ставит, а исполнитель назвать не
   может: `clasp_deploy`, `clasp_push`, `clasp_run`, `edit_secret`, `live_sheet`, `read_secret`,
   `write_outside`. Зеркально: `other` есть у исполнителя и нет у гарда. Следствие живое: чтобы
   владелец открыл `Read` файла секретов, в карточке нужен класс `read_secret`, а выставить его
   штатным путём нечем — «да» на `env` вернётся на `env`.
2. **Исход читается ПО СЛОВАМ, а не по полю.** `card_terminal_log.classify` ищет
   `REJECT_MARK = "отклонено Филиппом"` `:74` и `TIMEOUT_MARK = "причина=approval_timeout"` `:75`
   внутри текста `result`. Литерал продублирован в `pc_orchestrator._REJECT_PREFIX` `:5859`.
   Правка формулировки в одном месте молча превращает все отказы в «закрыто».
3. **Род заявки — маркер в прозе.** `split_awaiting` делит ряды по подстрокам `[заявка-ревью`,
   `[разведка-заявка`. Класс уже срабатывал: до 03.09 заявки ступени E падали в кучку `cards` и
   звались «карточками гарда» — четыре ряда из десяти в замере того дня.
4. **Тема ≠ доставка.** Снятие темы карточку не гасит: девбот выбирает ряды по `from`. Замер
   09.09 (#232) — в комментарии `pc_orchestrator.py:1084-1091`. Значит «решение о карточке» на
   полосе ПК ЧАСТИЧНО не принадлежит полосе ПК вовсе.
5. **Кнопка и её обработчик живут в разных телах.** Кнопку рисует код карточки, ловит —
   долгоживущий процесс `pc_agent`. Между коммитом и рестартом агента кнопка существует, а
   обработчика нет: два потерянных нажатия за окно (§3.1).

---

## 7. Что заход НЕ измерил (честно)

- **Модер-контур (К9, К10, К11)** — числа живут в боевой `moderation_ipc.db`; запрет захода
  «боевой базы не касаться» соблюдён, база не открывалась. Класс назван, число — **НЕИЗВЕСТНО**.
- **Живой ряд моста** не опрашивался (запрет на конфиги с секретами): все числа сняты со следа
  на диске. Поэтому «ждёт до сих пор» по классам К3/К6/К7 — нижняя граница, а не точное число.
- **Сколько из 130 рядов ступени B физически стало карточкой в Telegram** — доставку выполняет
  devbot на VPS, с полосы ПК это не видно.
- **К8 (`notification`)** ответа наружу не оставляет: был ли «нет», отсюда не узнать никогда.

---

## 8. Итог для решения владельца

1. Надзор действительно стал пересылкой у **шести классов из тринадцати**: за семеро суток по
   ним не случилось ни одного «нет», а по **К1** три карточки подряд просто истекли молча.
2. Возвращать надзор по-настоящему дешевле всего у **К2**: там «нет» физически терялся, и это
   починка одного класса (обработчик кнопки против живого процесса), а не пересмотр политики.
3. Снимать надзор без риска можно только там, где **«да» ничего не исполняет** — это **К3, К6,
   К7, К11, К12**. У них откат не нужен по построению, а не «не проверен».
4. Классы **К2, К5, К9, К10** остаются владельцу целиком: высший класс, решение Штаба.
5. Классы **К1, К8, К13** кандидатами не становятся по другой причине: **у них не проверен ни
   один откат**. Прежде чем говорить о приёме без человека, откат надо один раз выполнить и
   записать число.

---

<sub>Перепись 10–11.09.2026, полоса ПК, read-only. Временное место — `tmp/pc_report/`.
Числа сняты `tmp/pc_report/census_0910.py` по файлам следа, перечисленным в шапке.
Ни одна ветка гарда, порог или класс заходом не менялись; владельцу из проверки не ушло ничего.</sub>
