# Живая проба второго мнения после правки 7515d6c: все части в одной задаче, критик промолчал

PROBA manus zhivaya 1709

дата: **2026-09-17** · полоса: ПК · задача очереди 74 (CLAIM 16:36:46 местного)
код не менялся: проба шла на `74cf433` (правка отправщика — `7515d6c`)
FACT: живая задача канала `CJbg7YrHmiQXyi6EyPtJyH` — 10 частей отправлено, 10 из 10 в теле ОДНОЙ задачи, сообщений ассистента 0, `credit_usage` 1, вердикт `unknown / channel_idle` на части 10/10

Тела пакета и переписки здесь нет: только числа, длины, имена полей. Наружу ушла ровно одна отправка
(10 сообщений одной задачи) в один канал — manus. Codex, клиенты, группы и инбокс владельца не задеты.

---

## 0. Коротко

1. **Правка работает вживую.** До неё до критика доезжала 1 часть из 10, теперь **10 из 10** в одной
   задаче. Разница **+9 частей**.
2. **Критик промолчал.** Ассистент не написал ни одного сообщения: ни «ПРИНЯТО» на промежуточные
   части, ни разбора на последнюю. Ответа по трём вопросам про упрощение **нет**.
3. **Исход честный:** «части доехали, критик промолчал». Это не обрезок и не обрыв: `split_error`
   пуст, `part_error` пуст, код каждого ответа канала — 200.
4. **Прежняя догадка отпала.** Разбор 63-i допускал, что критик молчит из-за просьбы «ничего не
   разбирай» в рамке части 1. Теперь до него доехала последняя часть с просьбой ответить и с тремя
   вопросами. Он промолчал и на неё, значит эта причина молчание не объясняет.

## Премисы

| # | премиса | вердикт | чем |
|---|---|---|---|
| П1 | правка в дереве коммитом `7515d6c`, её проверял артефакт PAKET-chasti-kritik-1709 | **ВЕРНА** | `7515d6c` в `main` (16:05:57), артефакт `2026-09-17-PAKET-chasti-kritik-1709.md` проверял её на стенде с ловушкой сети, **без** живой отправки |
| П2 | демон на старом коде, самообновление держит грязное дерево | **ВЕРНА** | 1) заход демона 16:14:11–16:15:02, уже после `7515d6c`: протокол `tmp/review_auto/manus_protocol.json` — `parts` 10 / `parts_sent` 1, полей `parts_seen`, `silent_turns`, `split_error` нет, а новый код заводит их всегда; 2) `pc_orchestrator.log` 16:18:13 — `ERROR self-update демона: дерево ГРЯЗНОЕ … файлы: pretool_guard.py` (так же 13:34, 14:25, 15:08). Поэтому проба шла прямым запуском отправщика свежим процессом |
| П3 | канал с 04.09 закрывает задачу молча за секунды, квитанция 0, ассистент 0 | **ВЕРНА частью** | ассистента 0 — **верно**. Квитанция на этой пробе **1**, а не 0. Сколько секунд канал держал каждый ход, не записано. Видно одно: 22 опроса на 10 частей при подтверждении 2 опросами, то есть почти каждый ход закрывался до ближайшего опроса (шаг 10 с). Задача прожила 231 с только потому, что мы 10 раз в неё досылали |

---

## 1. Пакет

Самый свежий в лотке исходящих — `docs/review_outbox/2026-09-17-chain-pc-2026-09-16-56.md`
(собран демоном 16:30:59, ни одним каналом не отправлялся: оба лежат, ближайшая проба manus демоном
не раньше 15:13 UTC). Нового пакета не собирал.

| что | число |
|---|---|
| пакет | **14 545** знаков (22 652 байта, CR 0), sha256 `f40f27fa1347bfd1…` |
| исходящее (пакет плюс конверт) | **15 280** знаков, sha256 `89fcd8031863…`, стража исходящего: чисто |
| частей | **10** |
| длины сообщений (кусок плюс рамка) | 2317 · 290 · 2053 · 2035 · 1021 · 2053 · 2010 · 1998 · 2021 · 552 = 16 350 |
| где вопросы | заголовок `## ВОПРОСЫ РЕВЬЮЕРУ` в части 9, конец пакета и хвост конверта «по пунктам 1, 2, 3» в части 10 |

## 2. Живая отправка — протокол

Место временного: `tmp/t74_odna_otpravka/` (`run.txt`, `late_read.txt`, `work/manus_protocol.json`,
`inbox/…-manus.md`). Лоток `docs/review_inbox/` и рабочие каталоги демона не тронуты: ответ и
протокол уведены туда ключами `--inbox` и `--workdir`. Журнала отправщик не писал: ключа `--journal` не было.

Запуск: `review_send_run.py --pack <пакет> --channel manus --inbox … --workdir … --manus-wait 600 --timeout 120`.
Ключ канала взят из окружения процесса по имени. `.env` не читался.

| поле | значение |
|---|---|
| старт → конец процесса | 16:39:08 → 16:43:21 = **253 с** (`waited_sec` 252) |
| частей отправлено (`parts_sent`) | **10** |
| частей в ТЕЛЕ задачи канала (`parts_seen`) | **10: [1…10]**. Признак — сообщение `user` этой задачи, которое начинается рамкой части. Отдельно в теле найдено по одной метке `[ЧАСТЬ k/10]` на каждое k |
| задач | **одна**: `CJbg7YrHmiQXyi6EyPtJyH`. `split_error` пуст, чужого `task_id` в ответах на досылку нет |
| обращений | POST **10** (создание плюс 9 досылок, все без `part_error`, то есть код < 400) · GET **22** опроса · итого **32**; плюс 1 поздний GET (п. 3) |
| промежуточных ходов, закрытых молча (`silent_turns`) | **9** из 9 |
| код создания / последнего опроса | 200 / 200 |
| вердикт | **НЕИЗВЕСТНО / `channel_idle`**: «закончена состоянием completed и не сказала ни слова на часть 10/10, подтверждено 2 опросами, квитанция 1». Код возврата отправщика 4 |
| цена по вердикту | 1 request |

## 3. Ответ канала

| что | значение |
|---|---|
| сообщений в `output` | **10**, все с ролью `user` (наши части), у каждого `status: completed` |
| сообщений ассистента | **0** |
| `credit_usage` | **1** |
| текст ответа | **нет**: текстовых кусков после метки последней части 0, ответ 0 знаков |
| статус задачи | `completed`, модель канала `manus-1.6-adaptive` |
| жизнь задачи (`updated_at − created_at`) | 1789637951 → 1789638182 = **231 с** (16:39:11 → 16:43:02) |
| поздний забор, +585 с от старта (16:48:53, только чтение, `--manus-task`) | код 200, `completed`, сообщений 10, все `user`, `updated_at` тот же 1789638182, `credit_usage` 1. Вывод `accepted_no_answer`. За **351 с** после последнего изменения задачи не появилось ничего |

**Ответ по трём вопросам про упрощение — НЕТ.** Пересказывать нечего.

## 4. Почему нет ответа — что видно со стороны канала

Факты:

- Приём исправен: все 10 POST приняты, каждое сообщение легло в одну задачу, в теле не видно
  вложений и строки о слишком длинном сообщении (совпадений `attachment` и `message too long` — 0).
- **Заголовок задачи канал переписал.** В ответе на создание стоял один заголовок, а в
  конечном теле другой, со словом «Упрощение». Корень «упрощ» есть уже в части 1, поэтому читал ли
  канал поздние части, по заголовку не сказать. Видно одно: после создания на той стороне что-то
  обработало задачу ещё раз.
- Ассистент при этом не написал ни одного сообщения. Квитанция 1 за 10 сообщений. До правки была
  0 за одно сообщение (протоколы 16:14 и 10:46 сегодня).
- Время жизни задачи не истекало. Задача терминальна (`completed`) к ближайшему опросу после почти
  каждой части и за 351 с после последнего изменения не менялась. Кодов ошибок нет, `failed` нет.

Догадка (именно догадка, из API этого не видно): сообщение доходит до канала, лёгкая модель на его
стороне пишет заголовок, а агентный заход, который пишет ответ, не запускается. Вероятнее всего,
дело в настройках или остатке кредитов кабинета Manus либо в режиме задачи, который канал подставляет
сам (поле `agentProfile` мы не шлём). Проверить это можно только в кабинете Manus, это не наша полоса.

## 5. Сравнение с прошлым

| заход | пакет | частей | доехало в задачу | ассистент | квитанция | вердикт |
|---|---|---|---|---|---|---|
| прямой CLI 17.09 10:46 (старый код) | `-55`, 15 239 | 10 | **1** | 0 | 0 | channel_idle на 1/10 |
| демон 17.09 16:14 (старый код в памяти) | `-55`, 15 239 | 10 | **1** | 0 | 0 | channel_idle на 1/10 |
| **эта проба, 16:39 (`7515d6c`)** | `-56`, 15 280 | 10 | **10** | 0 | 1 | channel_idle на **10/10** |

Доехало частей: **1 → 10, разница +9** (доля текста у критика 14% → 100%). Ответов критика: 0 → 0.

## 6–7. Потолок и бюджет

- Потолок ожидания ответа 600 с соблюдён: ожидание 252 с, поздний забор на 585 с, дальше не ждал.
- Бюджет захода: CLAIM 16:36:46, к записи артефакта (16:50) прошло около **800 с** из доли 1890 с и
  потолка 2700 с. Точные секунды с коммитом и журналом назовёт итоговый отчёт.

## Что осталось

- **Почему manus молчит.** С нашей стороны это больше не объяснить: весь пакет доехал в одну задачу,
  и она терминальна. Нужен кабинет Manus: кредиты, агентный режим, история задачи
  `CJbg7YrHmiQXyi6EyPtJyH`. Это решение владельца.
- **Демон всё ещё шлёт старым кодом.** Его заход даёт 1/10, пока self-update заперт грязным
  `pretool_guard.py`. Файлы гарда не трогал: по ним идёт отдельное решение владельца.
- Повторов не было. Повтор пакета `-56` в manus не имеет смысла, пока канал нем: он дал бы тот же исход.
