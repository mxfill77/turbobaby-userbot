# ZAMYKANIE rebra poimenno 1709: разбор красного `test_vyshel_rovno_odin_i_eto_raner` по файлам (задание 62-y, 17.09.2026)

**FACT:** разбор только читал. Код, тесты, числа в тесте и связи не менялись (запрет 0г). Живые
замеры: 3 прогона теста, весь файл теста, два независимых прохода замыкания на трёх снимках
дерева и живом дереве, прогон гейта самообновления `test_pc_orchestrator test_pc_local_dec`:
**1161 OK, exit=0**. Временное место: `_scratch_contour63_0917/` (скрипты `measure.py`, `lists.py`,
выводы `out_run1.txt`, `out_lists*.txt`, `gate_run.txt`, снимки `.py` трёх коммитов). Ничего не удалено.

## Итог в пяти строках

1. Красный честный и стабильный: **(88, 87) ≠ (82, 81)** и **(50, 49) ≠ (42, 41)** на трёх прогонах
   подряд. Числа зашиты 95c1a5b (12.09.2026 13:52), и на дереве 95c1a5b они верны до единицы.
2. В замыкание вошло **10 разных файлов**: 6 при `cut=()` и 8 при `cut=умолчание`. Не вышел
   ни один. Сверено независимым проходом по байткоду, множества совпали на всех 12 замерах.
3. **Предмет теста на HEAD верен:** граница снимает ровно `trainer_run.py`, данные не уводит.
   Краснеют только размеры. Пока они красные, проверки предмета **не исполняются**:
   `assertEqual` размеров стоит в subTest первым и обрывает его.
4. **Ожидаемых 6, побочных 4.** Все четыре побочных приехали **одним ребром**:
   `queue_snapshot_pc.py:570` → `shtab_box_signals` (6598de9, 15.09.2026 15:29).
5. **В гейт самообновления тест не входит** (литерал `pc_orchestrator.py:5431`), гейт зелёный.
   Тест подтягивают гейты детей (после задачи и на реконсиляции), но только когда в диапазоне
   есть `client_contour.py`.

## 0. Тело задания и признак сделанности

- **п.0:** тело задания — около **3900 символов**. Это оценка на глаз, а не замер. Первым действием
  число не напечатано: сначала заведён черновик и найден тест. Нарушение порядка, называю его.
- **п.0б:** результат — ровно один файл, этот. Временное место `_scratch_contour63_0917/`, слов адреса
  в его имени нет.
- **п.0в:** за 17.09 в `docs/artifacts` лежат **11** файлов. Замыкание упоминает только
  `2026-09-17-OBREZKA-ne-sudit-yakor-1709.md` (строки 117 и 119): там числа 88/87 и 50/49 и довод
  «правка не двигала». Имён, рёбер и классификации там нет. **Вопросы п.2–п.4 не отвечены ни в одном**,
  поэтому работа начата с п.2.

## Премисы

| # | вердикт | адрес |
|---|---|---|
| П1 | **ДА.** «88 против 82, 50 против 42» — размеры без границы (`base`), с границей 87/81 и 49/41 | `OBREZKA-ne-sudit-yakor-1709.md:117`; черновик захода `tmp/pc_report/task57-648-1789586396089-57.md:12`; коммит захода 44f9761 |
| П2 | **ДА, и подтверждено сильнее, чем чтением.** Строк `import`/`from` в `git show 44f9761 -U0 -- done_judge_pc.py queue_snapshot_pc.py` нет. Главное — `git blame` всех 12 строк-рёбер, втянувших 10 файлов (п.3), даёт коммиты e99451e, 0cc1c4d, fb8e8ab, 72b518d, 6598de9, cfca880, 096e84b, 8238d91. **44f9761 среди них нет** | `OBREZKA…:119`; п.3 ниже |
| П3 | **ДА, но с поправкой.** Числа названы «зеркалом дерева», сверка стоит 12.09. Однако имена в том же докстринге **неверны**: «78 → 81 — `exam_show.py`, `wa_bridge.py`, `zayavki_lotok_pc.py` и соседи». Замер cb42441 (07.09) → 95c1a5b при `cut=()` дал рост на `style_examples.py`, `zayavki_lotok_pc.py`, `zayavki_lotok_run.py`, `zayavki_route_pc.py`. `exam_show.py` и `wa_bridge.py` не входят ни в одно замыкание 95c1a5b: `pc_agent.py` называет `exam_show.py` только в комментарии и командной строке ребёнка, `wa_bridge.py` импортирует только его тест | `test_client_contour.py:270–278`; `_scratch_contour63_0917/out_lists_cb42441.txt` против `out_lists.txt` |
| П4 | **ДА**: «НЕИЗВЕСТНО, в гейте ли он». Закрыто п.5: **не в гейте самообновления** | `OBREZKA…:119`; черновик #57 строка 12 |

## п.1. Три прогона подряд одной командой

```
for i in 1 2 3; do venv/Scripts/python.exe -m unittest test_client_contour.TestGranicaProcessa.test_vyshel_rovno_odin_i_eto_raner; done
```

| прогон | `cut=()` | `cut=умолчание` | время |
|---|---|---|---|
| 1 | (88, 87) ≠ (82, 81) | (50, 49) ≠ (42, 41) | 4.97 с |
| 2 | (88, 87) ≠ (82, 81) | (50, 49) ≠ (42, 41) | 5.04 с |
| 3 | (88, 87) ≠ (82, 81) | (50, 49) ≠ (42, 41) | 4.95 с |

Числа не пляшут. Весь `test_client_contour`: **111 тестов, FAIL=2**, и оба — subTest этого теста.
«Два красных теста» из задания на деле одна функция с двумя subTest.

## п.2. Кто вошёл сверх зашитого, обе границы

Замер `_scratch_contour63_0917/measure.py` сделан двумя проходами, у которых нет общей строки кода:
- **признак**: `client_contour.closure` ровно как в тесте, с границей и без неё;
- **независимый проход**: собственный обход по **байткоду** (`compile` + `dis`, инструкции
  `IMPORT_NAME`/`IMPORT_FROM`, рекурсия по вложенным code-объектам). Срез и границу он реализует сам.

| дерево | `cut=()` без границы / с границей | `cut=умолчание` без / с | совпадение множеств с байткодом |
|---|---|---|---|
| cb42441 (07.09, прежняя сверка) | 78 (литерал теста, не мерил) / **77** | 41 (литерал, не мерил) / **40** | — (только признак, только с границей) |
| **95c1a5b (12.09, зашито)** | **82 / 81** | **42 / 41** | да, да |
| 0478ca5 (HEAD) | 88 / 87 | 50 / 49 | да, да |
| живое дерево 17.09 ~07:00 | 88 / 87 | 50 / 49 | да, да |

Живое дерево совпало с HEAD поимённо: неотслеженные и изменённые файлы (`pretool_guard.py`) состава
не меняют. Во всех шести парах «без границы − с границей» = `{trainer_run.py}`, а по данным
(на живом дереве) — пусто.

**Вошли 95c1a5b → HEAD** (с границей; без границы то же, `trainer_run.py` в обоих):
- `cut=()`, **+6**: `door_price.py`, `photo_inbox.py`, `photo_inbox_fetch.py`, `polzunki_pc.py`,
  `polzunki_pc_run.py`, `trainer_photo.py`;
- `cut=умолчание`, **+8**: `contour_digest.py`, `door_price.py`, `photo_inbox.py`,
  `photo_inbox_fetch.py`, `series_pc.py`, `shtab_box_signals.py`, `trainer_photo.py`, `zayavki_pc.py`;
- **вышли: 0 и 0.** Объединение — **10 файлов**, поэтому остатка сверх десяти по п.8 нет.

Почему в умолчании плюс 8, а в `cut=()` только плюс 6. Четыре файла (`contour_digest`, `series_pc`,
`shtab_box_signals`, `zayavki_pc`) лежали в `cut=()` уже 12.09: туда их вело ребро через демона.
В умолчание они перешли. А `polzunki_*` живут только под демоном, поэтому в умолчании их нет.

## п.3 и п.4. Чем втянут каждый файл и чей это рост

Строка-ребро снята AST-обходом снимка 0478ca5, коммит — `git blame -L` на 0478ca5. «Ленивый» значит,
что импорт стоит внутри функции (функция названа).

| # | файл | граница | чем втянут (модуль:строка, где) | ребро: коммит, дата | файл заведён | вердикт |
|---|---|---|---|---|---|---|
| 1 | `door_price.py` | обе | `suggest.py:40` `import door_price`, верхний уровень | fb8e8ab 13.09 00:39 | тот же | **ожидаемый** |
| 2 | `trainer_photo.py` | обе | `userbot_listen.py:45`, верхний уровень (обратное ребро `photo_inbox.py:229` в `sniff_kind`, ленивое, 0cc1c4d) | e99451e 12.09 20:20 | 4808436 12.09 19:26 | **ожидаемый** |
| 3 | `photo_inbox.py` | обе | `trainer_photo.py:368` в `intake`, ленивый; ещё `photo_inbox_fetch.py:45`, верхний (0cc1c4d 12.09 19:48) | e99451e 12.09 20:20 | 0cc1c4d 12.09 19:48 | **ожидаемый** |
| 4 | `photo_inbox_fetch.py` | обе | `trainer_photo.py:369` в `intake`, ленивый | e99451e 12.09 20:20 | 0cc1c4d 12.09 19:48 | **ожидаемый, с оговоркой** |
| 5 | `polzunki_pc_run.py` | только `cut=()` | `pc_orchestrator.py:12238` в `maybe_polzunki`, ленивый | 72b518d 15.09 14:22 | тот же | **ожидаемый по устройству ворот, не клиентский** |
| 6 | `polzunki_pc.py` | только `cut=()` | `polzunki_pc_run.py:42`, верхний уровень | 72b518d 15.09 14:22 | тот же | **как №5** |
| 7 | `shtab_box_signals.py` | умолчание (в `cut=()` был) | `queue_snapshot_pc.py:570` в `external_refusal_words`, ленивый | **6598de9 15.09 15:29** (62-h) | cfca880 02.09 20:32 | **ПОБОЧНАЯ СВЯЗЬ** |
| 8 | `contour_digest.py` | умолчание (в `cut=()` был) | `shtab_box_signals.py:200`, верхний уровень | ребро cfca880 02.09 20:32, в умолчание вошёл с 6598de9 | 8238d91 02.09 02:44 | **побочная, через №7** |
| 9 | `zayavki_pc.py` | умолчание (в `cut=()` был) | `shtab_box_signals.py:203`, верхний уровень | ребро 096e84b 03.09 19:12, в умолчание вошёл с 6598de9 | a209241 02.09 22:25 | **побочная, через №7** |
| 10 | `series_pc.py` | умолчание (в `cut=()` был) | `contour_digest.py:126`, верхний уровень | ребро 8238d91 02.09 02:44, в умолчание вошёл с 6598de9 | 099ae5a 17.08 16:25 | **побочная, через №7 → №8** |

Порядок разбора — по цене ошибки: сначала то, что меняет ответ клиенту.

**№1 `door_price.py` — ожидаемый.** `suggest.py:3695` `door_price.client_quote(...)` и `:3897`
`door_price.kind_of(res)` стоят на пути ответа, и это **число цены клиенту**. Код исполняется в процессе
userbot. Ворота обязаны держать именно такой файл: дефект #61 из 62-x живёт здесь.

**№2 `trainer_photo.py` — ожидаемый.** `userbot_listen.py:441` `await trainer_photo.intake(...)`,
а также `:449` и `:454`. Импорт верхнего уровня: ошибка импорта роняет весь слушатель, включая
клиентскую личку. Изоляция тренажёра по `chat_id` — свойство поведения, а не процесса.

**№3 `photo_inbox.py` — ожидаемый.** Его зовёт `trainer_photo.intake` в процессе userbot:
`photo_inbox.plan_save` (`trainer_photo.py:390`), `commit_save` (`:409`), `INBOX_DIR` (`:378`).

**№4 `photo_inbox_fetch.py` — ожидаемый, с оговоркой.** Ребро настоящее: `intake` исполняет
`pif.is_image_message` (`trainer_photo.py:390`), `pif._fix_extension` (`:408`) и `pif.sender_label`
(`:411`) в процессе userbot, поэтому убирать файл из охраны нельзя. Оговорка: по своей шапке файл —
«РУКИ… здесь, и только здесь, живёт Telegram», то есть отдельный процесс с копией сессии. В память
бота он приезжает целиком ради трёх помощников. Правку его сессионной части ворота будут держать,
хотя клиента она не меняет. Это цена размещения помощников, а не побочная связь признака.

**№5–6 `polzunki_pc_run.py`, `polzunki_pc.py` — ожидаемый рост ворот `cut=()`, но не клиентская полоса.**
В живой бот не грузятся: `maybe_polzunki` зовётся только из витка демона (`pc_orchestrator.py:13299`).
В замыкание ворот их ведёт известное ребро `moderation_core.py:385` / `lesson_router.py:425`
`import pc_orchestrator` (ленивое), а срез для ворот выкатки снят сознательно 07.09 (58c827a,
`_CLIENT_CUT = ()` в `pc_orchestrator.py:7743`, цена там же названа: «останавливалось 14 → 47»).
Число `cut=()` будет двигать **каждый** новый модуль, который демон импортирует. Это объявленная
цена решения 07.09, а не находка.

**№7 `shtab_box_signals.py` — ПОБОЧНАЯ СВЯЗЬ.** Обоснование по звеньям:
- ребро ленивое, внутри `queue_snapshot_pc.external_refusal_words` (строки 555–577);
- эту функцию зовут только `outcome_of`, `unknown_by`, `unknown_words_of` (`queue_snapshot_pc.py:610,
  627, 633`), а их — `apply_failed` (`:658`, вызов `:682`). Это сборка слепка очереди, и демон
  поднимает её **отдельным процессом** `queue_snapshot_pc.py`;
- импортёров `queue_snapshot_pc` в корне шесть: `price_gate`, `price_freshness_run`,
  `price_snapshot_collect`, `review_intake_run`, `review_auto_run`, `zayavki_pc_run` (grep строк
  импорта). В умолчание HEAD из них входят **только первые два**;
- клиентская сторона берёт из `queue_snapshot_pc` **ровно два имени**, `_guard_test_logs` и `_daemon`:
  `price_gate.py:181` (`_bridge_caller`) и `price_freshness_run.py:76`. Это одолженный клиент моста,
  и ни одного вызова разбора исходов там нет (grep `qs\.` по обоим файлам);
- значит, сигнальный слой ящика Штаба в процесс бота не грузится ни одной найденной дорогой, и его
  правка ответ клиенту не меняет. Файл стал «клиентским» только потому, что в модуль, который бот
  одалживает ради моста, дописали ленивый импорт.

**№8–10 `contour_digest.py`, `zayavki_pc.py`, `series_pc.py` — побочные, транзитивно через №7.**
Их собственные рёбра (`shtab_box_signals.py:200/203`, `contour_digest.py:126`) законны и старше
12.09. В умолчание они вошли в тот момент, когда вошёл №7. Это сводка контура владельцу
(8238d91 «демон сам говорит, что с полосой СЕЙЧАС»), заявки полосы и серии — продукты демона
и Штаба, а не ответа клиенту.

**Что стоит побочная связь сейчас.** Ворота выкатки (`cut=()`) она не меняет: все четыре файла они
держали и 12.09 через демона. Меняется **признак по умолчанию**, а его читают четыре места:
- `pc_orchestrator._revizor_finding_touches_client` (`pc_orchestrator.py:12345`, ворота ВХОДА:
  находка ревизора, где названо одно из четырёх имён, становится owner-карточкой вместо задачи
  дирижёру);
- `recon_auto_run.client_lens` (`:254`, `:289`);
- `zayavki_pc_run` (`:284`, `:293`);
- `deploy_voice.client_of` (`:295`, замыкание `:299`).

Направление ошибки безопасное (лишнее держим), цена — шум карточек владельцу. Сколько карточек это
уже дало, не мерилось.

## п.5. Гейт самообновления и что с ним, пока тест красный

- **Гейт самообновления демона = две ступени, и тест ни в одну не входит.**
  - `self_update_ok` (`pc_orchestrator.py:13881`) → `selfupdate_gate.code_gate`: `py_compile`
    по `_dep_files()` плюс import-smoke `pc_orchestrator`, без unittest;
  - `_gate_unittests` (`pc_orchestrator.py:5421–5436`): литерал
    `[VENV_PY, "-m", "unittest", "test_pc_orchestrator", "test_pc_local_dec"]`.
  - `git grep test_client_contour 0478ca5` вне `test_*`: только два комментария в `pc_orchestrator.py`
    (строки 1197 и 7927). В хуках ПК (`core.hooksPath=deploy/hooks`) лежит один `pre-commit` — сторож
    реестра зависимостей, тестов он не гоняет.
- **Живой факт:** прогон `venv/Scripts/python.exe -m unittest test_pc_orchestrator test_pc_local_dec`
  на живом дереве 17.09 ~07:05 — **Ran 1161 tests in 131.224s, OK, exit=0**
  (`_scratch_contour63_0917/gate_run.txt`). **Красный тест гейт самообновления не держит.**
- Сейчас гейт самообновления не достигается вовсе, и по другой причине. `pc_orchestrator.log`
  17.09 в 04:12:49, 04:45:20, 05:49:58 и 06:32:31: «self-update демона: дерево ГРЯЗНОЕ — авто-рестарт
  ЗАПРЕЩЁН; файлы: pretool_guard.py».
- **Где тест всё-таки кусается — гейты ДЕТЕЙ.**
  - `maybe_update_bots` (`pc_orchestrator.py:8114–8124`) и `reconcile_children_tick` (`:8654–8660`)
    гоняют `_affected_test_modules(files)`, то есть `test_<stem>` изменённых файлов бота.
  - `test_client_contour` попадает туда, когда в диапазоне есть `client_contour.py`. Это клиентский
    файл, и `_client_block` держит его раньше гейта (`:8087`, `:8632`), так что до гейта дело
    доходит только после «да» владельца или зелёного тренажёра.
  - Дальше гейт красный, рестарт отложен и уходит
    `_notify("⚠️ Оркестратор: userbot НЕ перезапущен — тесты красные …")`, то есть ложный повод.
  - На `maybe_update_bots` при включённых флагах селективного гейта финальный шаг цепи и сбой
    селектора гонят **все** тесты (`gate_selective.decide`), и тогда красный тест держит любой шаг.
    Значения флагов лежат в `.env`, который не читался по запрету, поэтому живая ветка **НЕИЗВЕСТНА**.
    В `pc_orchestrator.log.1` строки `гейт mode=selective` есть 28.07 и 30.07, позже нет.
- **Сколько это уже стоило по логам:** «гейт КРАСНЫЙ» в `pc_orchestrator.log` (с 15.09 20:56) — 0.
  В `pc_orchestrator.log.1` (22.07–15.09) последний такой отказ 26.08, строк с `test_client_contour`
  в обоих — 0. Красное лежит **молча** с 12.09 20:20: по blame первое ребро роста — e99451e, отдельно
  не мерилось.

## п.6. Чем закрывается побочная связь (не делалось: клиентский контур)

Корень один — ребро `queue_snapshot_pc.py:570` → `shtab_box_signals`. Варианты от узкого к широкому:

- **В. Вынести различитель `external_refusal` в модуль-лист**, который импортируют и
  `shtab_box_signals`, и `queue_snapshot_pc`. Правило остаётся в одном экземпляре (довод докстринга
  «РАЗЛИЧИТЕЛЬ НЕ СВОЙ, А ЕДИНСТВЕННЫЙ» соблюдён).
  - Эффект по признаку: умолчание 49 → 46, уходят 4 файла, лист входит один. Это расчёт, не замер.
  - Не проверено: зависит ли `external_refusal` от `shtab_box`/`contour_digest` — если зависит,
    лист потянет их за собой.
- **Б. Объявить границей процесса пару `("queue_snapshot_pc.py", "shtab_box_signals")`.** Право —
  ленивость импорта плюс отсутствие вызовов разбора исходов в клиентском процессе.
  - Это **сужение охраны** (опасное направление). Оно меняет два замка — `test_kortezh_granicy_nazvan_poimenno`
    (кортеж ровно из одного ребра) и предмет «вышел ровно один», — поэтому решает владелец.
  - Эффект: умолчание 49 → 45, `cut=()` 87 без изменений.
- **А. Увести одолженный клиент моста** (`_guard_test_logs`/`_daemon`) из `queue_snapshot_pc` в
  отдельный модуль. Шире всего: рвёт названную голденом цепь `suggest → price_gate →
  queue_snapshot_pc → brain_writer` (`test_client_contour.py:182–187`), и размер эффекта не мерился.
  Не рекомендую без отдельного замера.

**Какой проверкой доказывать (для любого варианта):**
1. `measure.py` на дереве с правкой: оба прохода (признак и байткод) совпали; из умолчания ушли
   **ровно** `contour_digest.py`, `series_pc.py`, `shtab_box_signals.py`, `zayavki_pc.py`
   (для В ещё вошёл лист); `cut=()` — 87 без изменений; остальные 45 имён на месте.
2. Отрицательный на синтетическом дереве по образцу `test_granica_derzhit_rovno_svoyo_rebro`: тот же
   `shtab_box_signals`, импортированный **другим** клиентским модулем, въезжает, как раньше.
3. Замок вызовов: AST-скан модулей умолчания — ни один, кроме самого `queue_snapshot_pc`, не зовёт
   `outcome_of`/`unknown_by`/`unknown_words_of`/`apply_failed`/`external_refusal_words`. Появится
   такой вызов — право границы пропадает, и замок краснеет.

Отдельно о самом красном тесте (тоже не делалось, запрет 0г). Честное закрытие — «ЗАПИСЬ СМЕНЫ»
с 10 именами и вердиктами отсюда, а не новые числа. Второй остаток устройства теста: проверки
предмета (`base − cl == {trainer_run.py}`, данные) стоят в subTest после размеров и при красных
размерах не исполняются. Предмет слепнет ровно тогда, когда тест красный.

## п.7. Чего разбор не видит

- **Признак, а не рантайм.** Ребро — это AST-импорт и basename. Не видны: импорт по строке
  (`importlib` от переменной), вызовы через `getattr`/строки, дети-процессы (`queue_snapshot_pc.py`,
  `lesson_regress.py --after-lesson`, `exam_show.py` из `pc_agent`). Вывод «№7 в бот не грузится»
  стоит на чтении вызовов в двух названных файлах (`price_gate.py`, `price_freshness_run.py`) и на
  grep вызывающих в `queue_snapshot_pc.py`. Трассировки живого процесса не было, процессы не
  трогались.
- **Срез.** `.py` отслеженных деревьев cb42441, 95c1a5b и 0478ca5 плюс живое дерево 17.09 около
  07:00. Снимки коммитов вынуты без не-`.py` файлов, поэтому **данные** (не-`.py` по литералам) между
  коммитами **не сравнивались**. Проверено только «граница не уводит данные» на живом дереве (пусто).
- **Только прирост после 12.09.** 41 имя умолчания 12.09 не переклассифицировалось. Среди них
  `shtab_box.py`, `recon_auto.py`, `review_intake.py`, `review_pack.py`, `review_send.py`,
  `done_judge_pc.py`, `expectations_pc.py`, `price_freshness_run.py`, `hq_context_pack.py` — по имени
  это продукты демона и Штаба, возможно более старые побочные связи. **НЕИЗВЕСТНО.**
- Флаги селективного гейта в `.env` не читались. Сколько owner-карточек дала побочная связь на
  воротах входа, не мерилось. Полоса VPS и каталог `manager-bot/` вне разбора.
- Бюджет: единица одна, файлов ровно 10, разобраны все, остатка нет.
