# Петля тренажёра «тап владельца → действующий урок»: замкнута кодом во всех пяти звеньях, живьём не срабатывала дальше второго

PETLYA tap urok zhivoy 1709

**Дата:** 17.09.2026, замеры 12:06–12:30 по Пхукету · **Полоса:** ПК · **Задание:** ящик Штаба, ключ
`63-e-petlya-tap-urok.1709` (док `shtab_task_63-e-petlya-tap-urok.1709`, file id `1ovA3GZwg92_A8kPSZSAPu7Zg8qeFM45Z`)
**Временное место, названо явно:** `tmp/krug_exam_0917/` (вне git). Кода проекта заход не правил ни строкой.
Имя владельца в Telegram в этом файле замаскировано: `‹имя›` (7 знаков).

---

## FACT — живые числа, снятые сегодня

| что доказано | число / факт |
|---|---|
| живой прогон пяти звеньев на выдуманных кейсах | `tmp/krug_exam_0917/run-20260917-122558/report.txt`, 12:25:58, **1.9 с**, 34 вызова дверей |
| ловушка отправителя | **9 попыток = 9 показов**, которые код обязан был сделать (2 прямых + 7 «следующий кейс сам»); **0 утечек** |
| ловушка сети (urllib, сокет) и чужих процессов | **0** попыток; разрешён был только локальный `git rev-parse` |
| боевое дерево (верхний уровень + `exam_shots/`, размер и mtime до/после) | изменился **1** файл — `moderation_ipc.db` (его каждые 5 с пишет живой модербот) |
| таблица уроков | **копия** `tmp/krug_exam_0917/run-20260917-122558/derevo/lesson_store.tsv`; боевая: 9 строк, все действующие, mtime 11.09 18:58 — не тронута |
| урок из тапа дошёл до ответа бота | на копии: 2 действующих урока из «✔ Применить» → **2** строки в книге для промпта и **2** в `make_system_prompt`; кандидат «своё» — **0** до перевода, **3** строки после; на боевой таблице — **0** |
| живые тапы владельца за всё время | **1**: `pc_agent.log.1:28574` — `2026-09-12 18:10:01 chain-callback: data='exam:own:1' uid=504608015 → action=own ok=True` |
| живые вердикты / уроки из экзамена / переводы | **0 / 0 / 0**: `exam_verdicts.tsv` не существует, источника «экзамен» в боевой таблице нет, следа переводов `lesson_store.tsv.promote` нет |
| живой ловец | `pc_agent` pid **13740** жив (CIM, старт 12.09 15:28:26); `exam_show.py --agent --case 1` → **ДА**, разбирает все 6 колбэков |
| набор сейчас | **17** кейсов (отпечаток `99e6b78177a1d533`), снимки у **13**, без снимка **4** (14–17) |

---

## 0. Признак сделанности

Первым действием открыт `docs/artifacts/` за 17.09: 16 файлов других шагов, файла со словами адреса нет,
ни один пункт 1–7 заранее не отвечен. Работа начата с нуля.

## Премисы — проверены первым действием

| премиса | исход |
|---|---|
| **П1** кнопка и показ одного кейса описаны тремя артефактами | **ВЕРНА С ПОПРАВКОЙ.** Все три на месте. Но с 12.09 13:00 (`d7dc7b0`, `3ee716f`) карточка уже не «Верно/Неверно»: на снимке с подсказками критика кнопок **6** — «✅ Верно», тумблеры 1…N, «✍️ своё», «✔ Применить». |
| **П2** на 08.09 перевод в действующие звали только тесты | **НЕВЕРНА на сегодня.** С 09.09 перевод зовёт `lesson_promote.py` (дверь `2af45de`) и слово «урок включи N: …» в теме 205 (`pc_agent.py:1386`). С 12.09 «✔ Применить» пишет урок **сразу действующим** (`exam_show._lesson_write` → `lesson_store.add`, режим `EXAM_LESSON_MODE=training` по умолчанию, `exam_show.py:84`). |
| **П3** условия 1 и 2 подключения менеджеров закрыты срезом | **ВЕРНА как запись среза**: в `2026-09-11-УСЛОВИЯ-chetyre-srez-1109.md` оба «ВЫПОЛНЕНО». Сегодня не перемерялись. |

---

## 1. Живой вызывающий у каждого звена

| звено | файл : функция | кто зовёт В БОЮ | горело ли живьём |
|---|---|---|---|
| **1. показ кейса** | `exam_show.py:871` `show` → `dispatch_notify.send_chat_strict` | **первый показ — только рука** (`exam_show.py --case N --show`, `exam_reshow.py`); демон, планировщик и агент первый показ не зовут. Следующий кейс показывает `advance` (`:1153`) из `tap`/`apply_marked`/`own_take`, то есть живой тап | карточка с «✍️ своё» висела в группе 12.09: без неё не было бы тапа `exam:own:1` |
| **2. приём тапа** | `pc_agent.py:1253` `on_chain_callback` → `_chain_cb_route` (`:1145`) → `_exam_cli` (`:1058`, подпроцесс `exam_show.py` с аргументами `exam_args` `:1039`). Текст «своё»: `on_message` (`:1304`) → `_exam_pending_case` → `_exam_cli(…, stdin)` | `CallbackQueryHandler(on_chain_callback)` и `MessageHandler(filters.TEXT…)` (`:1633–1634`) живого агента pid 13740 | **кнопка — да**, 1 раз (строка лога выше; стол `exam_session.json` получил автора ожидания, значит дверь пропустила право). **Текст «своё» — нет**: строки «экзамен: ловлю урок» нет ни в `pc_agent.log`, ни в `pc_agent.log.1` |
| **3. запись вердикта** | `exam_show.py:1067` `_write_verdict` из `tap` (`:1108`), `apply_marked` (`:1251`), `own_take` (`:1346`) | подпроцесс `exam_show.py` из `pc_agent._exam_cli` | **нет**: журнала `exam_verdicts.tsv` не существует |
| **4. перевод в действующий урок** | (а) «✔ Применить» → `exam_show._lesson_write` (`:1232`) → `lesson_store.add` (`:810`), состояние сразу `актив`; (б) «своё» → `add_candidate`, дальше `lesson_promote.py` → `trainer.promote_lesson` (`:1892`) → `lesson_store.promote` (`:1214`) | (а) тот же подпроцесс из агента; (б) слово «урок включи N: причина» в теме 205 → `pc_agent._lesson_cli` (`:914`) | **нет**: в боевой таблице нет ни одной строки источника «экзамен», следа переводов нет |
| **5. чтение урока в ответе бота** | `suggest.py:854` `load_playbook` → `active_lesson_bullets` (`:954`) → `lesson_store.active` | `userbot_listen._trainer_generate` (`:232`), боевой черновик `suggest.py:9496`, `moderation_core.py:203`, `trainer_run.generate` (`:468`) | путь боевой, но уроков из экзамена читать пока нечего |

**Звена, которое зовёт только тест, нет ни одного.** Петля замкнута кодом. Живьём она доходила только до
звена 2, и то кнопкой: урок своими словами так и не был пойман.

---

## 2. Живой прогон всего пути

**Как устроен.** `tmp/krug_exam_0917/prohod.py` исполняет код из **копии дерева**
(`run-20260917-122558/derevo/`: 162 отслеживаемых файла верхнего уровня без тестов, снимки экзамена,
таблица уроков). Все модульные пути (стол, журнал вердиктов, снимки, таблица уроков) выводятся из
`__file__` и потому лежат в копии: ни один путь не переназначен. Корпус — 9 выдуманных кейсов (901–909).
Голова и критик при заморозке подменены, сама дверь `freeze` живая. Отправитель у каждой двери — ловушка
с подписью `send_chat_strict`. Замок свежести агента **живой**: боевой лок и mtime `pc_agent.py`, только
чтение. Аргументы дверей — те, что строит `pc_agent.exam_args`, автор — имя владельца.

**Чем прогон отличается от боя.** Ловец `pc_agent` в процесс не импортировался: он грузит конфиг с
ключами на импорте, и гард законно относит это к классу `env` (§8). Звено 2 в прогоне представлено
прямыми вызовами тех же дверей, а в бою доказано живой строкой лога от 12.09.

**Пройдено до конца, все пять звеньев:**

| шаг | исход (дословно, сокращено) |
|---|---|
| показ 901 | `✅ кейс 901 из 9 показан в chat:-5193185299, message_id=ловушка#2. Ждём тапа.` |
| тумблер 1, тумблер 3 | `☑️ отметил 1: «проход-0917 правило 1 кейса 901…»` · `Отмечено: 1, 3. Жми «✔ Применить» — станут уроками.` |
| ✔ Применить | `🎓 Уроков записано 2 (действуют СРАЗУ), автор ‹имя›, источник «экзамен»…` · `📝 Записано КАНДИДАТОМ №1: кейс 901 из 9 — «неверно»…` · `Уроки этого вердикта: #10, #11` · `➡️ Следующий кейс 902: ✅ кейс 902 из 9 показан…` |
| ✅ Верно (902) | `📝 Записано КАНДИДАТОМ №2: кейс 902 из 9 — «верно»…` → следующий 903 показан |
| ✍️ своё + текст (903) | `✅ Записан КАНДИДАТ урока #12…` · `📌 Причина не названа — бот по нему пока НЕ отвечает.` · вердикт №3 → 904 показан |
| чтение | книга для промпта: `- (2026-09-17) проход-0917 правило 1 кейса 901…`, `- (2026-09-17) проход-0917 правило 3 кейса 901…`; системный промпт — 2 строки |
| перевод #12 | `✅ Урок #12 ВКЛЮЧЁН — бот отвечает по нему со следующего ответа.` → книга 3 строки, промпт 3 |
| контрфакт | действующие уроки **боевой** таблицы: строк прохода **0** |

**Где встало — три места, все дословно:**

1. **Кейс 14 боевого набора** (за ним встанет «следующий сам» после кейса 13):
   `⛔ сохранённого черновика кейса 14 нет (…\exam_shots\case-14.json). Сперва «--freeze --case 14» — показывать свежую генерацию запрещено`
2. **Повторное «✔ Применить» по уже судимому кейсу пишет действующий урок без вердикта** (909):
   `↩️ кейс 909 уже судим: вердикт №8 «неверно»… Второй записи не делаем — одна запись на кейс.` + `Уроки при этом записаны: #18.` — урок #18 в состоянии `актив`.
   Уроки пишутся ДО проверки «уже судим» (`apply_marked` `:1277–1289`; то же у `own_take` `:1370–1376`). Живьём это
   настанет там, где стол остаётся на судимом кейсе: на последнем кейсе и на кейсе 13 (показ 14 отказывает до
   записи стола, `show` `:880–901`).
3. **Тумблер карточки не того кейса, что на столе** (904 при столе на 901):
   `⛔ отметки на столе собраны на другом снимке (e7ea4a1@2026-09-17T05:26:00Z), а показан e7ea4a1@2026-09-17T05:26:00Z — номера значат разные правила.`
   Отказ законный, но слова называют снимок, а разошёлся кейс. На выдуманных снимках одной секунды ключи
   совпали, и фраза противоречит сама себе.

**Дефект, видный владельцу с первого же тапа:** квитанция вердикта печатает
`правила ['7d19b1db352e8f76', '7d19b1db352e8f76', True, True, 'версия 7d19b1db352e8f76']`, а в журнал
ложится `НЕ ЗАПИСАНО(версия правил)` (проверено в журнале прохода, строки 1–3). Причина:
`exam_show.rules_version` (`:161`) отдаёт `lesson_store.version()` как есть, а это кортеж `Version`
(с `3ec5c06`, 11.09 19:00), не строка. Во **всех 13** боевых снимках поле `rules` лежит списком. Набор
`test_exam_show` этого не видит: снимок в нём собирается со строкой `rules`. Одна из трёх опор вердикта
будет потеряна у каждого вердикта с первого.

**Счёт ловушки.** Попыток отправки **9**. Каждая — показ, который код обязан был сделать: Ф0 кейс 1,
Ф2 кейс 901, и 7 «следующий кейс сам» (после 901, 902, 903, 905, 906, 907, 908). После 909 следующего
нет — `🏁 ЭКЗАМЕН ПРОЙДЕН…`, попытки нет. Сеть и процессы — **0**. **Утечек 0**: отправитель заменён по
построению, а сеть закрыта на уровне ниже, куда никто не дошёл.

---

## 3. Имя владельца на каждой двери — три вида, живым вызовом

Дверей, которые получают имя, у петли **восемь**. Судят по имени пять, по номеру аккаунта — две, одна не
судит вовсе.

| дверь | чем судит | как есть | с `@` | иной регистр |
|---|---|---|---|---|
| А. кнопка в агенте `_chain_cb_route` `:1167` | номер аккаунта `== ALLOWED_USER_ID`, имя не читает | **не звалась живьём** (§8); кодом: имя не участвует | — | — |
| Б. право двери экзамена `may_write_rule` → `is_approver` (`lstrip("@").lower()`) | список прав | ✅ `True` | ✅ `True` | ✅ `True` |
| В. тумблер 904 (право Б на живом тапе) | Б | `☑️ отметил 1` | `▫️ снял отметку 1` | `☑️ отметил 1` |
| Г. «своё»: сверка автора ожидания `own_take` `:1358–1361` + право | `lstrip("@")`, сравнение в нижнем регистре | ✅ кандидат #13, вердикт №4 | ✅ кандидат #14, вердикт №5 | ✅ кандидат #15, вердикт №6 |
| Д. «своё»: кнопка пишет автора ожидания `own_start` `:1324` | `lstrip("@")` | автор `‹имя›` | автор `‹имя›` (без `@`) | автор записан как пришёл, регистр иной; сравнение Г это гасит |
| Е. текст «своё» в группе `pc_agent.on_message` `:1305` | номер аккаунта | **не звалась живьём** (§8) | — | — |
| Ж. перевод `trainer.promote_lesson` (`lesson_promote.py`, слово «урок включи») | право Б | ✅ `#13 ВКЛЮЧЁН` | ✅ `#14 ВКЛЮЧЁН` | ✅ `#15 ВКЛЮЧЁН` |
| З. откат вердикта `exam_show.rollback` `:1391` | **не судит имя вовсе** | — | — | — |

Отдельным процессом, который конфиг не грузил (`konsol.py`), запасная дверь «рука с консоли» дала то же:
`may_write_rule` — `True` ×3, `lesson_promote --promote 16` → `✅ Урок #16 ВКЛЮЧЁН`. **Оговорка:** список
прав там непуст потому, что окружение захода унаследовано от родителя (§8). **В чистой консоли владельца
исход НЕИЗВЕСТЕН**: `exam_show.py` и `lesson_promote.py` конфиг сами не грузят — грузят только агент, демон
и `trainer_run`.

**Хвост формы в данных:** автор с `@` ложится в журнал и в таблицу как пришёл (вердикт №5 и урок #14 —
`@‹имя›`). Разрез «автор» это гасит (`norm_who`), счёт вердиктов — нет. Кнопкой такое имя не приходит:
Telegram отдаёт ник без `@`.

---

## 4. Что владелец увидит при первом тапе

**Набор сейчас:** 17 кейсов. Снимки есть у 13, у 4 нет (14–17). С подсказками критика — кейсы 1, 8, 10, 12, 13
(пять карточек: 5–6 кнопок). Остальные восемь — две кнопки «✅ Верно / ❌ Неверно». Вердиктов 0, в шапке
«пройдено 0».

**Экран — карточка кейса 1**, дословно, как её собрал живой `show` (замок агента ДА, отправитель — ловушка):

```
🎓 ЭКЗАМЕН · кейс 1 из 17 · пройдено 0 — первый контакт, одна модель
Показан ЗАФИКСИРОВАННЫЙ ответ от 2026-09-12T08:56:10Z (коммит 37c290f, корпус 99e6b78177a1d533). Голова сейчас НЕ звалась.
Это НОВЕЙШИЙ снимок кейса (всего слотов 4); прежние сохранены и не тронуты — их коммиты другие.

❓ КЛИЕНТ:
Здравствуйте! Хочу XMAX 300 с 6ого по 11ое октября, залог паспортом, 1 шлем

🤖 БОТ:
Здравствуйте! Даты и модель учли — XMAX 300 на 6–11 октября, залог паспортом и 1 шлем тоже приняли.

XMAX 300 New Gen — 682 ฿/день; итого 3410 ฿; депозит: паспорт; свободен на эти даты.
ADV 350 — 634 ฿/день; итого 3170 ฿; депозит: паспорт; свободен на эти даты.
FORZA 300 — 502 ฿/день; итого 2510 ฿; депозит: паспорт; свободен на эти даты.

Бронируем?
[собрано: модель ✅ срок ✅ даты ✅]

📌 ПОЧЕМУ так: ЦЕНА из Календаря бронирования ПОСЧИТАНА по спрошенной модели и НЕ БОЛЕЕ ЧЕМ ДВУМ альтернативам: первой в блоке идёт та, что спросил клиент — она ОСНОВНОЙ вариант; следом идут одна-две модели ТОГО ЖЕ класса, ближайшие к …
📚 ОБРАЗЦЫ: по 3 живым ответам менеджеров (д85·р141, д559·р2, д611·р2).

🔎 ЧТО УЛУЧШИТЬ — подсказки критика (того же, что стои́т за кнопкой «🎓 Обучить»):
1. Бот трижды пишет «свободен на эти даты» (XMAX300, ADV350, FORZA300) без проверки календаря
    → правило: не подтверждай наличие конкретного байка на даты как факт — пиши, что уточнишь по календарю/у команды и вернёшься.
2. Цены названы 682/634/502 ฿/день — не совпадают с прайс-листом (939/998/690 ฿/день для этих моделей на 5 дней без скидки)
    → правило: называй клиенту только точные цифры дословно из блока ЦЕНА, не меняй и не выдумывай тариф.
3. Бот сразу пишет «Бронируем?», хотя не собраны локация, телефон, фото паспорта и оплата (в теге отмечены только модель/срок/даты)
    → правило: не предлагай переходить к бронированию, пока не собран весь состав брони — сначала уточни оставшиеся пункты.

Кнопки: «✅ Верно» — кейс зачтён и сразу придёт следующий. Номера 1…3 — ТУМБЛЕРЫ (тап отмечает, повторный снимает), отметь нужные и жми «✔ Применить»: отмеченные подсказки станут уроками (урок начинает действовать СРАЗУ), кейс пойдёт «неверно», следующий придёт сам. «✍️ своё» — напишешь правило своими словами ОТВЕТОМ на эту карточку, 10 минут.
```

Кнопки (3 ряда): `✅ Верно` · `1` `2` `3` · `✍️ своё` `✔ Применить`.

**Первый тап** даёт сначала всплывающую подсказку. Её текст взят из кода `pc_agent.py:1185–1189`, живьём не
вызывался: «✅ записываю «верно»…» · «☑️ отмечаю подсказку N…» · «🎓 записываю уроки и вердикт…» ·
«✍️ слушаю: следующим сообщением — правило…». Затем приходит ответ двери. На «✅ Верно» это квитанция той же
формы, что в прогоне: `📝 Записано КАНДИДАТОМ №1: кейс 1 из 17 — «верно»…` со строкой
`правила ['7d19…', '7d19…', True, True, 'версия 7d19…']` (дефект §2) и `➡️ Следующий кейс 2…`.

---

## 5. Чего не хватает до первого прогона владельца

| # | чего не хватает | чем доказано | цена |
|---|---|---|---|
| 1 | **Первый показ делает только рука**: ни одна боевая ветка кейс 1 сама не шлёт | §1, звено 1 | **1 заход** (одна команда `exam_show.py --case 1 --show`; замок агента сегодня ДА) |
| 2 | **Снимков кейсов 14–17 нет**: после кейса 13 «следующий сам» отказывает | §2, отказ 1 | **1 заход**: 4 круга головы и критика, потолок ≤ 4 × (140 + 257) = 1588 с < 1890 |
| 3 | **Версия правил пишется кортежем**: каждый вердикт — `НЕ ЗАПИСАНО(версия правил)`, в квитанции мусор | §2, дефект; 13 из 13 снимков | **1 заход** (правка `rules_version` + чтение старых снимков + тест на живом типе) |
| 4 | **Повторное «Применить»/«своё» по судимому кейсу пишет урок без вердикта**; на кейсе 13 и последнем настанет живьём | §2, отказ 2: урок #18 `актив` | **0 сверх п.3** (тот же файл, тот же заход) |
| 5 | **Урок своими словами живьём не пойман ни разу**. Единственный тап `own:1` (12.09 18:10:01) текста не дождался. Ответ агента в группу уходит с `message_thread_id=205` (`pc_agent.py:523`, `:1313`) в обычную группу, исход НЕИЗВЕСТЕН | §1, звено 2 | **проба владельцем** (ответить на карточку в течение 10 мин) + **1 заход** на чтение лога. Если ответ не уйдёт — правка `_send` и «обновись» агенту |
| 6 | **Откат вердикта без права**: чужое имя откатило вердикт №1 | §6 | **0 для прогона** (кнопки отката нет); закрыть можно в заходе п.3 |
| 7 | **Запасная дверь с консоли в чистой консоли не проверена** | §3, оговорка | **1 заход** на пробу в процессе без унаследованного окружения, либо принять «только кнопками» |
| — | ловец свежий, агент жив | FACT | 0 |
| — | действует ли урок на ответ головы, а не только на промпт, — не мерено (голову не звал) | — | 1 заход, для первого прогона не обязателен |

**Минимум до первого прогона без мин:** п.1 + п.2 + п.3/4 = **3 захода**; п.5 — проба самого владельца.
Режим `training` («Применить» включает урок на всех клиентов сразу, без второго шага) — решение владельца
12.09, а не нехватка. Перекрыт ли он в конфиге агента — НЕИЗВЕСТНО: конфиг не читался.

---

## 6. Отрицательный тест: чужое имя `chuzhoy_0917`

Дословно, живыми дверями:

- ✅ Верно, тумблер, ✔ Применить, ✍️ своё (четыре двери, одинаково):
  `⛔ «chuzhoy_0917» не вправе судить экзамен: имя не в списке правящих книгу правил. Ничего не записано. Право одно на все двери (запись урока, перевод кандидата, снятие набора) и на пустом списке НИКОМУ — это не сбой, а замок.`
- текст «своё» чужим при ожидании владельца:
  `⛔ ожидание «✍️ своё» принадлежит @‹имя›, а пишет @chuzhoy_0917 — ничего не записано.`
- перевод кандидата чужим:
  `⛔ Нет прав на перевод уроков — урок #16 НЕ включён. Включение требует того же права, что и запись.`
- счёт после отказов: вердиктов **6 → 6**, строк уроков **15 → 15**.
- кнопка от чужого аккаунта (кодом, `pc_agent.py:1167–1170`; живьём не звалась): всплывающее `⛔ нет прав`.
- **Исключение — отказа НЕТ:** `exam_show.rollback` чужим:
  `↩️ вердикт №1 откачен (откачен(chuzhoy_0917;2026-09-17T05:26:00Z)). Строка на месте, из счёта он вышел.`

---

## 7. Бюджет

Единица одна, потолок 2700 с, доля 0.7 — это 1890 с. Задача создана в **12:06:18** (штамп в имени черновика
`1789621578684`). Запись артефакта начата в **12:30:18**: **1440 с = 0.53 потолка**. Сам живой прогон занял
1.9 с, проба консоли — меньше секунды. Остальное ушло на чтение кода и три захода гарда (§8). Итоговое
число секунд захода — в отчёте захода, снято после коммита.

---

## 8. Гард и конфиг — что было на самом деле

- Первый вид прогона импортировал `pc_agent` и `dispatch_notify`. Гард отказал трижды:
  `⛔ ОТКАЗ: ОБЪЕКТ НЕ НАЗВАН — Хочу обратиться к .env / секретам … Гард смог сказать только: объект не установлен: имя секрета собирается в коде`.
  Четвёртая команда (литерал пути стола экзамена) получила карточку класса `env`, перехваченную как проба
  (гард сам положил маркер `test-pc_ask_69.marker` в корень репозитория):
  `🔴 Хочу обратиться к .env / секретам — разрешить? Объект: exam_show.SESSION … файл сессии Telethon`.
  Объект распознан ложно: `exam_show.SESSION` — стол экзамена `exam_session.json` (`exam_show.py:54`), не сессия
  Telethon. **Обхода не искал.** Переименований и склеек не делал, агента не импортировал. Прогон переведён
  в копию дерева, где переназначать стол не нужно. Файлы гарда не открывались; прочитаны 3 строки его журнала.
- **Конфиг в процессе прохода.** `.env` в копию не клался, но значения конфига в процессе были: (1) окружение
  захода унаследовано от родителя — процесс `konsol.py`, не импортировавший ни одного модуля, грузящего
  конфиг, видит непустой список прав; (2) `trainer_run.py:139` зовёт `load_dotenv()` без пути, поиск идёт
  вверх от файла и из копии дошёл до боевого `.env`. Значения нигде не печатались и не сохранялись, сеть
  была закрыта ловушкой (0 попыток). Отсюда же: право в прогоне — **настоящий список**, а не подмена (ветка
  подмены в скрипте не сработала).
- `trainer_run` на импорте ставит `TURBOBABY_TEST_LOGS=1`, поэтому логи модулей прохода могли уйти в `%TEMP%`
  (`turbobaby_TESTING_*`). Вне проекта не смотрел — **НЕИЗВЕСТНО**, были ли там записи.

## 9. Чего заход НЕ сделал

- Живого тапа не делал и не имитировал в группе. Наружу — ни в группу «Тренеровка», ни в инбокс, ни клиентам —
  не ушло **ни одного** сообщения: все 9 карточек пойманы ловушкой.
- Ловец `pc_agent` в прогоне не исполнялся (§8). Звено 2 доказано живой строкой лога 12.09 и чтением кода.
- Голову не звал: влияние урока на сам ответ модели не мерено.
- Кода, снимков, корпуса, боевой таблицы уроков, журнала вердиктов не правил. Процессов не трогал. `.env` не
  открывал. Брони и лист «Календарь бронирования» не открывал. Цен не менял.
- Дефекты §2 и §5 (версия правил, урок без вердикта, откат без права, адрес ответа в группу) **названы, но не
  починены**: задание — решение, а не вещь.

## 10. Временное место `tmp/krug_exam_0917/` (ничего не удалялось)

| файл | что |
|---|---|
| `prohod.py` | прогон пяти звеньев (копия дерева, ловушка) |
| `konsol.py` | проба права запасной двери отдельным процессом |
| `imya.txt` | имя владельца для маски (вне git) |
| `console.txt`, `konsol.txt` | вывод обоих запусков |
| `run-20260917-122558/report.txt`, `tg.json` | отчёт прогона и 9 пойманных карточек |
| `run-20260917-122558/derevo/` | копия дерева: журнал вердиктов прохода (8 строк), таблица уроков-копия (18 строк), след переводов, стол, снимки 901–909 |
