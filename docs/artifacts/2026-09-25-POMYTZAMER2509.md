# POMYTZAMER2509 — совет C «помыть, воск, чехол» по положению байка: замер без кода (задание Штаба 0036-74e.2509, #76)

**Сводка.** Срез — 14 суток (11.09 14:40 → 25.09 14:40 UTC), советов C в нём **11** (7 отправлено + 4 подавлено
замком повторов). Положение на момент совета: **ВОЗВРАТ 0 · В АРЕНДЕ 0 · РЕМОНТ 4 · ВЫДАЧА 0 · ПОМЫТ 0 ·
НЕИЗВЕСТНО 7**. Правило «совет только при ВОЗВРАТ и не ПОМЫТ, один раз за цикл возврата» **оставит 0, погасит 11**
(из них реально отправленных 7). **НЕИЗВЕСТНО по CRM — 11 из 11**: своих записей о положении по этим байкам у
Splinter ближе 70 суток до совета нет ни одной. **ВОЗВРАТ одними словами — 0.** **N = 10 ч** (замер #75, пустой
промежуток 4.70–15.23 ч). Цена единицы — 1.04 с и 1.23 с машины на совет; срез 14 суток занял 12.6 с против
потолка 0.7 × 2700 = 1890 с. Вне среза за всё время журнала советов C 18, и ровно у одного положение по своим
записям Splinter — ВОЗВРАТ (байк 1813, 29.08): правило не пусто по построению, оно оставило бы этот один.

```
FACT: read-only. Живой Splinter 48ee30a6c3780ab665787e61f80dcf8b79284852 (rev-parse 25.09 14:40:44 UTC сервера),
      splinter.py живой = git 48ee30a по sha256 (ed1f98d9b75f9296…), служба active PID 2408174 NRestarts 0 — не тронута.
FACT: #75 (0033-74b) взят 13:22:31 UTC, ТАЙМАУТ 2700 с в 14:07:32 UTC, «следы: коммитов=0, записей журнала=0»
      (pc_orchestrator.log 21:07:32 местного); артефакта нет, строки DONE в журнале нет.
FACT: ветка #75 splinter-pomyt-sost-2509 = 69ea8143 (родитель 48ee30a) в живом дереве и на origin — НЕТ
      (rev-parse --verify на refs/heads, refs/remotes/origin и на сам хеш — пусто).
FACT: числа этого захода — tmp/pomyt_zamer_2509/{unit2b,slice14,alltime,s1_recon,s2_own,s3_1813}.out (ПК),
      /tmp/pomytzamer2509/units2.jsonl (сервер, строка на совет сразу после совета).
```

## 1. Что оставил #75 (П0) — взято, не пересчитано

| что | где | состояние |
|---|---|---|
| временное ПК | `tmp/pomyt_sost_2509/` | скрипты разведки r0–r6 и их выводы, `zamer.py` + `zamer.out` + `zamer_itog.md` (замер до кода), клон `srv/`, `pomyt.bundle`, выводы сверки байтов `bc_*` |
| временное сервера | `/tmp/pomytsost2509/` | `zamer.py`, `zamer_facts.json`, два клона из пакета: `before` = 48ee30a, `after` = 69ea814; выводы наборов `out_before` (786 файлов), `out_after` (789) |
| ветка | `splinter-pomyt-sost-2509`, коммит `69ea814` «ПОЛОЖЕНИЕ БАЙКА (0033-74b): совет «помыть» только после возврата, фраза о депозите в J только на возврате», 25.09 21:03 местного | живёт ТОЛЬКО в клоне ПК `tmp/pomyt_sost_2509/srv`, в пакете и в `/tmp/pomytsost2509/after`. 7 файлов, +1386/−21: новый `bike_position.py` (422 строки), `splinter.py` +264, `tests/test_bike_position.py` (634 строки, по черновику #75 32/32 на ПК), правки трёх соседних тестов, раздел CLAUDE.md. Полный набор сервера: до 262 всего / 25 красных, после 263 / 25 — все 25 имён совпадают (сверено `diff` по `suite_before.out` / `suite_after.out`), то есть ветка красных не добавила (их причина — среда клона, у #75 разобрана в `r6_reds.out`). На GitHub не ушла, в живое дерево не попала. **Не продолжалась — запрет задания.** |
| журнал | мозг | записи о #75 нет (демон: «записей журнала=0»); только карточка «❌ задача #75 провалена — ⏱ таймаут» и сигнальная остановка ящика 21:11 местного («две подряд недоказанные: #74, #75») |
| готовые числа | `zamer_itog.md` | C 11 (7 + 4), J 3, РЕМОНТ 4, НЕИЗВЕСТНО 7, ВОЗВРАТ 0, N = 10 ч — **взяты** как нога сервиса и контекста сообщения; этот заход добавил ногу собственных записей Splinter (CRM) и цену единицы |

Черновик #75 (`tmp/pc_report/task75-…-75.md`) называет премисы верными; здесь они перепроверены сами (п. 2).

## 2. Премисы Штаба — проверены первым действием

| | вердикт | чем |
|---|---|---|
| **П1** совет C по картинке; «фикс C» гасит по сервисным словам, с 48ee30a — и по сервисной подписи | **ВЕРНА с уточнением** | `splinter.py` при 48ee30a, строка 9570: `vis.dirt and not _service_ctx and not _ho_ctx and not _cap_service`. «Сервисные слова» в коде = масло-контекст / разбор `repair` / снимок чека / пробег на снимке / открытый вопрос сервиса (9560–9563) — не словарь, а признаки. Ветка подписи пришла коммитом d8c4c69 (24.09), на живой — вместе с 48ee30a (рестарт 25.09 11:14:38 UTC). Выдача (`_ho_ctx`) гасит C тоже. Возврат (`_ret_ctx`) ветку C **не трогает вовсе** |
| **П2** положение видно из событий Splinter в Bot Data и из контекста сообщения | **ВЕРНА ЧАСТЬЮ** | контекст сообщения — да: `_is_return_context` (1405) и `_is_handover_context` (1396), оба пишут строку журнала. События Bot Data пишет `bridge.add_event`, но **таблицы по заданию не читались**; собственный журнал Splinter несёт положение редко: за всё время `RETURN-контекст` 13, `closing авто` 13, `HANDOVER-контекст` 3, `state выдача` 3, `event_type=return` 6, `event_type=handover` 10, `contract_km: бронь` 3, строк CRM-проверки возврата 0 (`s2_own.out`). В срезе `add_event` лёг видами: photo 43, repair 10, handover 1, other 1, return 0 |
| **П3** записи «помыт» нет нигде | **ВЕРНА** | в коде 48ee30a вида события «мойка» нет (только текст совета `msg_dirty_care`, 1858); строк бота со словами мойки за всё время журнала — 0; слов мойки во входящих среза — 0 (#75) |
| **П4** на живом 48ee30a | **ВЕРНА** | rev-parse HEAD = 48ee30a6…; `splinter.py` и `caption_intent.py` живые = `git cat-file 48ee30a:…` по sha256 |

## 3. Срез и почему

**14 суток: 11.09 14:40 → 25.09 14:40 UTC** (от минуты разведки сервера). Бюджет пропускает его с запасом в
сотню раз (п. 4), брать 7 суток незачем. Новых советов C/J после конца окна #75 (25.09 13:25 UTC) нет — срез
содержит те же 11 советов, что и у #75 (первый 12.09, окно #75 начиналось 11.09 13:25).

## 4. Цена единицы и бюджет

Единица = один полный проход `splinter.log` от начала до минуты совета с разбором собственных строк Splinter по
номеру байка и признаков последнего входящего (скрипт `tmp/pomyt_zamer_2509/zamer_c.py`, подан трубой, без
общего кэша между советами — честная цена).

| | число |
|---|---|
| совет 1 (4685, 12.09 — ранний) | **1.04 с** машины, 374 828 строк |
| совет 2 (5065, 25.09 — поздний, почти весь журнал) | **1.23 с**, 436 519 строк |
| перечень советов среза | 0.12 с |
| прогноз: 11 × 1.23 + 0.12 | **13.6 с** |
| потолок: 0.7 × 2700 | **1890 с** → прогноз = 0.7 % потолка, 14 суток влезают |
| факт среза | 12.6 с машины (1.03…1.22 на совет), 14 с по стене с ssh |
| факт дополнения «всё время» (18 советов) | 18.35 с машины, 19 с по стене |

Честная оговорка: цена машины — не цена захода. #75 сгорел не на замере, а на коде, тестах и наборах сервера;
этот заход от старта (14:37:43 UTC) до конца замера занял около 8 минут по часам ПК.

## 5. Числа по классам (срез 14 суток)

Классы — порядком силы из слова владельца: мойка/возврат в сообщении → выдача → сервис (сообщение, тема за N ч,
подпись-указание) → CRM по собственным записям Splinter → НЕИЗВЕСТНО. Нога сервиса и контекста — из замера #75
(привязка сообщений к теме), нога собственных записей — этот заход (`slice14.out`).

| # | совет, UTC | байк | C | положение | чем решено |
|---|---|---|---|---|---|
| 1 | 12.09 06:08 | 4685 | отпр | НЕИЗВЕСТНО | сервиса в теме нет; собственных записей Splinter о байке **0 за всё время** |
| 2 | 12.09 07:57 | 4957 | отпр | РЕМОНТ | сервис в теме 1.43 ч до (разбор `repair`, тревога J 06:30) |
| 3 | 15.09 09:16 | 7530 | отпр | НЕИЗВЕСТНО | последний возврат своими записями 1781 ч (74 сут) назад — не этот цикл |
| 4 | 21.09 06:16 | 5065 | отпр | РЕМОНТ | сервис в теме 1.365 ч до |
| 5 | 21.09 06:17 | 5065 | подав | РЕМОНТ | 1.384 ч |
| 6 | 23.09 07:25 | 4255 | отпр | НЕИЗВЕСТНО | сервис темы 2052 ч; возврат своими записями 2184 ч |
| 7–9 | 23.09 07:28 / 07:39 / 08:46 | 4255 | подав ×3 | НЕИЗВЕСТНО | то же |
| 10 | 24.09 07:09 | 2478 | отпр | РЕМОНТ | подпись-указание в сообщении совета (случай, ради которого сделана ветка d8c4c69) |
| 11 | 25.09 06:44 | 5065 | отпр | НЕИЗВЕСТНО | сервис 96.4 ч назад — дальше N |

| класс | всего | отпр | подав | правило |
|---|---|---|---|---|
| ВОЗВРАТ | **0** | 0 | 0 | — |
| В АРЕНДЕ | **0** | 0 | 0 | — |
| РЕМОНТ | **4** | 3 | 1 | гасит 4 |
| ВЫДАЧА | **0** | 0 | 0 | — |
| ПОМЫТ | **0** | 0 | 0 | — |
| НЕИЗВЕСТНО | **7** | 4 | 3 | гасит 7 |
| **итого** | **11** | **7** | **4** | **оставит 0, погасит 11** |

- **ВОЗВРАТ, поднятый одними словами: 0.** Ни в одном из 11 сообщений совета нет слов возврата, строки
  `RETURN-контекст` и разбора `return`; во всём срезе сигналов возврата 0 из 64 входящих обслуживания (#75).
- **НЕИЗВЕСТНО по CRM: 11 из 11.** Собственные записи положения по этим байкам (возврат/выдача/бронь/аренда) —
  не ближе 1699 ч (70 сут) до совета: 7530 — 1781 ч, 4957 — 1818 ч, 2478 — 1699 ч, 4255 — 2075 ч; у 4685 и 5065
  их нет вообще. Для четырёх РЕМОНТ это не меняет класса (сервис сильнее CRM); семь НЕИЗВЕСТНО остаются
  неизвестными. Верхняя граница «оставит», если бы CRM в тот день показала возврат: **4** (4685, 7530, 4255 один
  раз за цикл из четырёх, 5065 25.09) — только при закрытой накануне/в тот день аренде, проверить нечем.
- **Против живого 48ee30a (по признакам, не прогоном кода):** из 7 отправленных живой код уже гасит 1 (#10,
  сервисная подпись); остальные 6 признаков сервиса, выдачи и подписи не несут и ветку C проходят. Чистый вклад
  правила за 14 суток — **6 советов**, которые живой код всё ещё отправил бы.

**Дополнение вне среза — всё время журнала (советов C 18, первый 26.08; `alltime.out`).** Ногами собственных
записей Splinter (без привязки сообщений к теме — сервис у 7 старых советов не досчитан): **ВОЗВРАТ ровно 1** —
байк 1813, 29.08: `RETURN-контекст` 06:00:15 и `closing авто` 06:00:32, строку в CRM приёмка не нашла, совет C в
09:21:44 (через 3.35 ч), слов мойки нет → правило **оставило бы** этот совет (первый в цикле). РЕМОНТ хотя бы 1
(6908, 02.09, сервисная запись за 36 с до совета). Остальные 5 старых — без записи положения ближе 767 ч.

## 6. N для РЕМОНТ — из данных

Из замера #75 по всему журналу: 66 промежутков «сервисный сигнал → следующее сообщение той же темы» (привязано
181 из 993 входящих обслуживания, сервисных 79). Распределение двугорбое: **сессия 0.001…4.697 ч (39 шт), дальше
пусто до 15.23 ч**, потом 27.3 · 28.1 · 32.0 · 49.2 · 73.7 · 96.4 … (27 шт). **N = 10 ч** — середина пустого
промежутка (4.70; 15.23); любое N из этого интервала делит корпус одинаково. На срезе запас ещё шире: возрасты
сервиса 1.365 · 1.384 · 1.43 | 96.4 · 2052 ч — любое N в (1.43; 96.4) даёт те же классы.

## 7. Чего не сделано

- **Нога CRM на момент совета не восстановлена:** живые таблицы запрещены заданием, Bot Data не читалась;
  собственных записей Splinter нет (п. 5) — отсюда НЕИЗВЕСТНО 7 и граница «оставит ≤ 4».
- Правило **не прогонялось живым кодом**: ветка #75 по запрету не продолжалась; классы — по признакам журнала.
- Для совета #10 признаки сообщения взяты у #75: мой проход берёт последнее входящее перед строкой совета, а у
  альбома это соседний снимок (возраст 0 с) — подпись там не видна.
- Семь советов вне среза классифицированы только ногой собственных записей (сервис темы не досчитан).
- Файл `/tmp/pomytzamer2509/units.jsonl` на сервере — первый пуск с ошибкой останова (читал 58 строк, строка без
  штампа в начале журнала), в счёт не идёт; числа — `units2.jsonl`. Временное не удалялось.

POMYTZAMER2509
