# Опись мозга (Brain) — по дампу offline-сессии

**Дата:** 30.07.2026. Репо `D:\turbobaby-bot`, ветка `main`, HEAD `4528917`.
**Режим:** строго ТОЛЬКО ЧТЕНИЕ. Drive/Bridge не дёргался ни разу — вся фактура взята из дампа,
снятого предыдущей RC-сессией. Ни правок, ни коммитов, ни записи в Brain, ни касания реестра.
`.env`/VPS/секреты не открывались и не печатались. Правки не предлагаются — только факты.

---

## 0. ДАМП: найден, цел

| факт | значение |
|---|---|
| путь | `C:\Users\mxfill1\AppData\Local\Temp\claude\D--turbobaby-bot\0b8f25ba-eee3-4021-ab67-db93ca3e5b47\scratchpad\brain\` |
| размер | **3 589 543 байта** (3,42 МБ), из них `docs/` — 3 544 851 байт |
| тел документов | **26 файлов** в `docs/` (`*.txt`), суммарно **2 350 199 символов**; без дубля `faq`/`turbobaby_faq` — 2 342 534 |
| время снятия | 29.07.2026 19:07–19:11 (mtime файлов) |
| сопутствующее | `manifest.json` (27 записей `list_brain`), `summary.json` (27 проб чтения с sha256/chars/сек), `analysis.txt` (профиль доков + 209 дословных совпадений строк), `pointer_probes.txt` (5 живых проб id вне манифеста) |

Распарсено по факту: `manifest.json` → 27 записей (26 ключей-документов + `folder_id`, это папка);
`summary.json` → 27 проб, из них **26 `ok`** и **1 `FAIL`** (`roadmap_master`). Файлов тел — ровно 26,
сходится с числом успешных проб. Дамп **ЦЕЛ**.

Второй, более ранний дамп той же работы найден в
`…\7e3ca6b7-f199-4590-a2b6-201567871bb4\scratchpad\bodies\` (25 тел, снят 29.07 17:56–17:58,
плюс `registry_rows.json` с пробами живости). Использован как перекрёстная сверка: расхождение
только в `cowork_log` (46 811 симв. в 17:5x против 50 127 в 19:08) — журнал рос между снимками.

**Дату последней правки канал не отдаёт.** Ответ `read_doc` содержит ровно `ok`/`id`/`name`/`text`
(зафиксировано в `summary.json` и в описи 29.07). Во всех строках таблицы — «неизвестно».

---

## 1. ТАБЛИЦА ПО КАЖДОМУ ДОКУ

Роль ставится по содержимому, основание — дословная цитата или структурный факт.
«Пишет»/«читает» — по грепу кода репо `D:\turbobaby-bot` (`read_doc`/`write_doc`/`brain_writer`/
имя дока как адрес). **Оговорка:** штаб (Claude-на-сайте) читает Brain через Drive-коннектор,
мимо кода — такое чтение грепом не видно и отмечено отдельно.

### 1.1 Ключи манифеста (26)

| # | ключ | id | РОЛЬ | основание роли | ПИШЕТ | ЧИТАЕТ | симв. | дата правки |
|---|---|---|---|---|---|---|---|---|
| 1 | `index` (KB_MASTER) | `1-bH3b6c_oamqST551iLxn-voSDragdV0rUZkFwgmAqc` | **СМЕШАННАЯ**: справочник + состояние + журнал + инструкция | стр. 60 «KB_MASTER — ЕДИНАЯ КАРТА ПРОЕКТА TurboBaby. ТОЧКА ВХОДА №1», при этом §3 «ЧТО ПОСТРОЕНО» — 450 датированных строк с хешами | `kb_master_append_2307.py:30,90` (разовый, `brain_writer.apply(name="index")`), `tmp/write_brain.py:51`; штатно — Claude Code вручную | `tmp/kb_master_fetch.py:9`, `tmp/read_brain.py:16`, `tmp/read_brain2.py:9` (разовые). Боевого читателя в коде нет; штаб читает вручную (§0 и ревизия §6) | 86 535 | неизвестно |
| 2 | `knowledge_base` | `1Mv_zi1P33jNM0MFUBX_UEf9CnzOvCFeZ` | **СМЕШАННАЯ**: справочник + реестр id | стр. 1 «# 🧠 TurboBaby Knowledge Base», стр. 542 «## 📑 ДОКУМЕНТЫ И ID» — 15 Drive-id внутри бизнес-справочника | никто в коде репо (синк из `docs/*.md` делает VPS Claude Code — `project_state`:413–417) | **НИКТО** | 56 521 | неизвестно |
| 3 | `project_state` | `1fuptOFp2bqZva7eJRlEanaCO6eRkAO20` | **СМЕШАННАЯ**: архив + состояние + справочник | стр. 1 «# TurboBaby — состояние проекта (АРХИВ-СНИМОК)» и стр. 8 «**🎯 ПРИОРИТЕТ №1 (поправка владельца 03.07.2026)**» | никто в коде | **НИКТО** | 37 994 | неизвестно |
| 4 | `faq` | `1tv8Y-K3gLyT9Y0mXyYXZLf2c2rEzPMLHPzvg4lGqs98` | справочник | стр. 1 «# TurboBaby — FAQ и шаблоны ответов (v1)» | никто в коде | **`suggest.py:255,1735` `load_faq()` — единственный БОЕВОЙ читатель Brain на ПК** | 7 665 | неизвестно |
| 5 | `turbobaby_faq` | `1tv8Y-…Gqs98` (**ТОТ ЖЕ ФАЙЛ**) | справочник, дубль-ключ | тела совпали побайтно, sha256 `bf787dbc32f46195` у обоих | никто | `suggest.py` — второй в порядке `FAQ_DOCS="faq,turbobaby_faq"` | 7 665 | неизвестно |
| 6 | `park_list` | `1jD3VJGeoET8Yf5ma6iZPmwwmuw_yIYyP2A4N5RAEZho` | справочник | стр. 1 «# 🏍️ Парк TurboBaby — актуальный список (источник: Байки.xlsx, Лист1)» | никто в коде | **НИКТО** | 2 642 | неизвестно |
| 7 | `rules` | `1AnBAniKQtrpJQevVWlzxVrdb1n51aj1G` | **СМЕШАННАЯ**: инструкция + предметный справочник | стр. 1 «KB_RULES — СВОД ПРАВИЛ РАБОТЫ», при этом стр. 106 — карта колонок таблицы «Байки» | никто в коде | **НИКТО в коде**; штаб на полной ревизии, пункт [5] | 16 736 | неизвестно |
| 8 | `infra` | `1z2wS0I0nm-dJGqF3RRpOXlEkTKwLOsuF` | справочник | стр. 1 «KB_INFRA — СВОД КАРТЫ ИНФРАСТРУКТУРЫ ТАБЛИЦ TurboBaby» | никто | **НИКТО в коде**; штаб, ревизия [5] | 6 702 | неизвестно |
| 9 | `state_model` | `1OGUzeb60UbzAaCOsLNR_aBKFy-blfdn-` | справочник | стр. 1 «KB_STATE_MODEL — СВОД МОДЕЛИ СОСТОЯНИЙ БАЙКА И ЦИКЛА АРЕНДЫ» | никто | **НИКТО в коде**; штаб, ревизия [5] | 9 753 | неизвестно |
| 10 | `executors_map` | `1NyfcErxNt09CH8JB-V0in9e-UQB-K4_uJrwZ-FG3Za8` | справочник | стр. 1 «KB_executors_map — КТО ЧТО МОЖЕТ (разделение труда исполнителей)» | никто | **НИКТО** | 5 588 | неизвестно |
| 11 | `orchestrator_plan` | `1_ogUGFim24Ifw60mSsfcONFc8hXMPzw539oXTbhGggo` | инструкция (план) | стр. 1 «KB_ORCHESTRATOR_PLAN — ПЛАН АВТОНОМНОГО СТРОИТЕЛЯ» | никто | **НИКТО** | 6 119 | неизвестно |
| 12 | `orchestrator_safety` | `1UB1MWs8ZQWDkwHYBqNgK7UyVD4dkPKYdEVYo3IM2-Zs` | инструкция | стр. 1 «KB_ORCHESTRATOR_SAFETY — ДОКТРИНА БЕЗОПАСНОСТИ» | никто | **НИКТО** | 5 107 | неизвестно |
| 13 | `payments_plan` | `13WtxQaDLdixR4EsjUFimtNhESn9nFDtk` | инструкция (план) | стр. 3 «СТАТУС: ОТЛОЖЕНО. Отдельный план. Делать ПОСЛЕ Ф1» | никто | **НИКТО** | 3 441 | неизвестно |
| 14 | `pulse` | `1v3ezYbEeI1kGNQFI6xi8mDhnM9uE3YSE` | **состояние** | одна строка: «2026-07-29 10:32 \| 🟢 \| cb4e3e2 выкачен на VPS…» | `tmp/pulse_write.py:18`, `tmp/write_brain.py:60` (разовые); боевой писатель — VPS, той же операцией, что `cc_log` (`rules`:115) | `tmp/pulse_read.py:12`, `tmp/read_brain.py:16` (разовые); **штаб на каждое «проверь»** | 270 | неизвестно |
| 15 | `cc_log` | `1464zaINaLnOwXMsHNaEyy-4FpuQCVTYF` | журнал | стр. 1 «DONE 2026-07-29 10:32 UTC (local): выкатка cb4e3e2 на VPS…»; 59 DONE / 31 PLAN / 3 NOTE | VPS-`cclog` (вне этого репо) | `build_dec_port_spec.py:11,66` (разовый), `tmp/probe_bridge.py:44`, `tmp/verify_pc_write.py:33` | 49 955 | неизвестно |
| 16 | `cc_log_archive` | `1haE-9OFgs1-ZeH-fV1g1mIxxM3L_BLYq` | архив | 5 539 строк, даты 2025-05-30…2026-08-03, 453 DONE / 138 NOTE / 50 PLAN | VPS-ротация | **НИКТО** | 988 521 | неизвестно |
| 17 | `cowork_log` | `1s9Fy3xm4FB99ah9xovLjYIMOjnDKz3Jc` | журнал | стр. 1 «DONE 2026-07-29 12:00 UTC: Dispatch 19:03: детектор ТИХОЙ СМЕРТИ сессий — коммит aed3e4e» | `cowork_log_append.py:18,215`; `pc_agent.py:854,890`; `pc_orchestrator.py:280` (спавн `cowork_log_append`); `dispatch_notify.py:229` (хук SessionEnd) | `cowork_log_append.py:195` и `pc_agent.py:877` — чтение РАДИ склейки (read-modify-write), не потребление; **штаб на «проверь Dispatch»** (`rules`:56) | 50 127 | неизвестно |
| 18 | `cowork_log_archive` | `1UC-fKIpjb2zwzrvCsbJjBxmgglmSlj46` | архив | 2 097 строк, 1265 NOTE / 449 DONE / 62 ASK | серверная ротация (`cowork_log_append.py`:124 — «серверная ротация делит журнал по ЗАГОЛОВКАМ записей») | **НИКТО** | 763 217 | неизвестно |
| 19 | `cowork_log_test` | `1zGPT5DqNdUT8FOrOxsVI8-mI7sZjz3kU` | журнал (тестовый) | всё тело: «DONE 2026-07-29 00:00 UTC: проба замка» | разовые пробы замка/ротации | **НИКТО** | 39 | неизвестно |
| 20 | `cowork_log_test_archive` | `1HvhvlxKFPiHUYlMp41yQL0-50q4Dv_zq` | **СМЕШАННАЯ**: заявлен тестовым архивом, хранит боевые записи | стр. 1 «📦 KB_cowork_log_archive — АРХИВ журнала Dispatch/Cowork», а 2 099 из 2 372 симв. — дословно боевые строки из живого `cowork_log` | разовые пробы ротации | **НИКТО** | 2 372 | неизвестно |
| 21 | `sessions_log` | `1gNFRnHv09SKeagkGA3moG0VNlzffECeh` | журнал | стр. 1 «📒 KB_sessions_log — ХРОНИКА сессий Claude Code (DONE/диагностики/коммиты). Самочистится» | никто в коде репо | **НИКТО** | 7 665 | неизвестно |
| 22 | `sessions_log_archive` | `1D-iq-Rp1g_RC9uYzqsjPJtmw6TY0l6cV` | архив | стр. 1 «📦 KB_sessions_log_archive — архив хроники (старше ~7 дней)» | никто в коде | **НИКТО** | 44 141 | неизвестно |
| 23 | `cc_userbot_log` | `1yqzVrSFRN-1Zr3y6kprn71c-T-1xTl1qfO1BzA-Rihg` | **СМЕШАННАЯ**: журнал + свод | стр. 1 «KB_userbot_log — журнал userbot-ветки», стр. 2 «(Свод от 23.06.2026: единый журнал из бывш. v2 + 5 update-дублей, без потерь.)» | вручную (Claude-на-сайте/Cowork), кода нет | **НИКТО** | 5 394 | неизвестно |
| 24 | `review` | `1vDfD_n8i-8cSvJmqqvvEaLZ1YC639-in` | **СМЕШАННАЯ**: навигация + журнал + состояние канала, всё в 7 строках | стр. 1 — указатель на архив, стр. 5–6 — журнальные PLAN, стр. 7 — «(review_archive: запись >183К — ограничение payload…)» | никто в коде | **НИКТО** | 511 | неизвестно |
| 25 | `review_archive` | `1K0gPMOyM-ER7nweda8-f9MK3edbepYCniZpQy0HkpAA` | архив | 1 783 строки, 52 PLAN / 13 DONE, даты 26.05–20.06.2026 | никто в коде | **НИКТО** | 183 329 | неизвестно |
| 26 | `roadmap_master` | `1Z70EpgGZmaYMaZ064sXQlCCP8z8sFyZWfzVPvRB4jWE` | **МЁРТВ — файла нет** | `summary.json`: `FAIL` и по `name`, и по `id`, одна и та же ошибка Drive «объект по указанному id не найден» | — | — | — | — |

`folder_id` `1uWqHsxk7aEWoSNqaUBMmqkYOh2UKYLkY` — папка Brain, не документ.

### 1.2 Живые Brain-доки ВНЕ манифеста (4)

| # | имя | id | РОЛЬ | основание | ПИШЕТ | ЧИТАЕТ | симв. | дата |
|---|---|---|---|---|---|---|---|---|
| 27 | `KB_trainer_log` | `13kp-54bzN2Xua8R5VN4EaXR7IElTKg4O` | журнал | «TRN LOG v1 \| Полный лог тренажёра «Тренеровка» (клиентский контур)…»; 10 строк `TRN` | `trainer_log.py:220,438` — userbot + moderbot по `id` | `trainer_log.py:203,414` (read-modify-write) | 2 190 | неизвестно |
| 28 | `KB_trainer_log_archive` | `1lOco1SI58UNT0a4-TBuU3yYeZzbPkWUP` | архив | `index`:541 «архив-док ротации заведён — file id 1lOco1SI…»; `trainer_log_doc.json` → `"archive_id"` | `trainer_log.py:424–429` при перерастании порога | `trainer_log.py:426` | **тела в дампе нет** | неизвестно |
| 29 | `KB_booking_flow` | `11HvMKGZRdSoRjojvAcmpN6_KJzkc1eRlIIx12JW6DQU` | справочник (спека) | проба `pointer_probes.txt`: ЖИВ, «# TurboBaby — Анализ диалогов: флоу брони и логика для userbot-сборщика / Источник: 710 реальных диалогов…» | Штаб вручную | обращений в коде нет | 6 501 | неизвестно |
| 30 | `KB_collect_booking_spec` | `1PZ7TeEQJLtzrw66Ll3PZBXIw-K70dnkh-3ClxDEMW48` | инструкция (спека) | проба `pointer_probes.txt`: ЖИВ, «collect_booking.py — СПЕЦИФИКАЦИЯ + готовый промпт-парсер (для Claude Code)» | Штаб вручную | обращений в коде нет; текст скопирован в `collect_booking_prompt.txt` вручную | 4 553 | неизвестно |

**Итого Brain-доков: 28** (24 живых файла под 26 ключами манифеста + 4 вне манифеста), плюс
1 мёртвый ключ и 1 папка.

---

## 2. ВЫВОД 1 — ДУБЛИ (дословно)

**(а) `faq` ≡ `turbobaby_faq` — один и тот же файл под двумя ключами.**
Оба ключа манифеста указывают на `1tv8Y-K3gLyT9Y0mXyYXZLf2c2rEzPMLHPzvg4lGqs98`; тела совпали
побайтно (sha256 `bf787dbc32f46195`, 7 665 симв. у обоих). Первая строка обоих:

> `# TurboBaby — FAQ и шаблоны ответов (v1)`

Последняя строка обоих:

> `4. На базе этого FAQ — клиент-бот: сначала черновики ответов менеджеру, потом узкие авто-сценарии. Языки бота: русский (основной) + английский. Без тайского.`

**(б) `index` ↔ `state_model` — 11 дословно совпадающих блоков, 2 662 символа.** Самый крупный,
430 символов, стоит в обоих доках:

> `ПОТОК: таец отфоткал байк в обслуживании (проверка байка — отдельная ветка обслуживания) → когда байк ОК, таец САМ заходит в группу обслуживания и жмёт кнопку «уведомить клиента о выезде» → выбирает байк (если НЕ подменён — плановый; если подменён — подтверждает заменённый байк ещё раз) → бот-менеджер пишет КЛИЕНТУ ('выезжаем, ожидаете?') → клиент отвечает → бот пишет тайцу 'клиент ожидает' → таец`

и следом ещё 296 символов, тоже в обоих:

> `— КАРТОЧКА ВЫДАЧИ: плановая, ОДНА бронь = ОДНА карточка = ОДИН (плановый) байк. Триггер — бронь дня из CRM (дата начала известна). Бот высвечивает УТРОМ карточки выдач на сегодня. Кнопки: ✅ выдан (этот байк) / 🔁 другой (подмена этого байка). state → 'в аренде'. Внутри учётного контура. = СЛОЙ 1.`

**(в) `cc_log_archive` ↔ `review_archive` — 186 совпадающих строк, 20 495 символов.** Пример,
203 символа в обоих:

> `«🐀 Splinter · 📌 <байк>\n🇹🇭 ✅ รับเลขไมล์ <km> กม. แล้ว ТО ปกติ ครбกำหนดถัดไปที่ <next> กม. (เหลือ ~<left> กм.)\n      🇷🇺 ✅ Пробег <km> км принят. ТО в норме, следующее на <next> км (осталось ~<left> км)».`

и 164 символа в обоих:

> `- _handle_servicing ТО-блок: УБРАТЬ взвод _OIL_MARKER и A2-симметрию; _ask_mileage_confirm без oil_done; oil_hint=_is_oil_done_marker(text,vis) прокинуть в pending.`

**(г) `cowork_log` ↔ `cowork_log_archive` — 7 строк, 2 531 символ.** Запись лежит И в живом
журнале, И в архиве одновременно. Самая длинная, 1 382 символа, начинается так:

> `DONE Dispatch 28.07 21:15: закрыта мина VPS-гарда — прогон гейта из headless-задачи выписывал БОЕВУЮ красную карточку самой этой задаче и убивал её. Корень воспроизведён прогоном: гейт отдаёт тестам копию окружения (gate.py:86), наследуется CC_TASK_ID…`

**(д) `cowork_log` ↔ `cowork_log_test_archive` — 3 строки, 2 099 символов**, то есть 89 % тела
«тестового» архива — это боевые записи. Например, 1 170 символов:

> `DONE 2026-07-28 18:15 UTC: Dispatch 29.07 разведка передачи клиентов партнёрам (только чтение, ничего не правил, никому не писал): переписки ЕСТЬ обе, в client_chats.jsonl — @phuket_travel_admin (peer_id 45349667) 692 сообщения…`

**(е) `knowledge_base` ↔ `faq` ↔ `park_list` — прайс и парк описаны трижды.** Не дословно, но
одними числами, и с расхождением. `knowledge_base`:95:

> `| HONDA PCX 150 ⚠️ нет в парке | 349 ฿/день | 3,000 |`

`faq`:138 те же цифры, но без пометки об отсутствии:

> `- HONDA PCX 150 — 349 ฿/день, депозит 3 000`

Тот же разрыв на PCX 160 / ADV 150 / ADV 160 / XSR 900 / R7 / REBEL 300: `knowledge_base` метит их
«⚠️ нет в парке», `faq` подаёт как действующий прайс. Третий источник, `park_list`, даёт 38
поимённых байков с номерами — и `knowledge_base`:25 повторяет итог словами: `**Размер парка:** 38
байков актуально`.

**(ж) `project_state` дублирует сам себя.** Блок «ID базы знаний (Brain)» стоит в доке ДВАЖДЫ —
строки 401–408 и 426–432, с явной пометкой на втором: `### ID базы знаний (Brain) — дубль для
быстрого доступа`. Оба списка содержат одни и те же **мёртвые** id (см. вывод 5).

**Групп дублей: 7.**

---

## 3. ВЫВОД 2 — НИКЕМ НЕ ЧИТАЕМОЕ

Греп кода репо (`read_doc` / `brain_writer.read_text` / имя дока как адрес канала, `*.py`,
без `venv`/`.git`). **20 документов не открывает ни один процесс и ни один скрипт:**

`knowledge_base`, `project_state`, `park_list`, `rules`, `infra`, `state_model`, `executors_map`,
`orchestrator_plan`, `orchestrator_safety`, `payments_plan`, `review`, `review_archive`,
`sessions_log`, `sessions_log_archive`, `cc_userbot_log`, `cc_log_archive`, `cowork_log_archive`,
`cowork_log_test`, `cowork_log_test_archive`, `roadmap_master` (нечего открывать).

Суммарно — **2 143 585 символов**, то есть **91 % всего объёма мозга** не читает никто из кода.

Читатели, которые есть:

| док | читатель | характер |
|---|---|---|
| `faq` / `turbobaby_faq` | `suggest.py:1735 load_faq()` | **боевой**, на каждом черновике клиенту |
| `KB_trainer_log` (+архив) | `trainer_log.py:203,414,426` | **боевой** (userbot+moderbot), по `id` |
| `cowork_log` | `cowork_log_append.py:195`, `pc_agent.py:877` | служебное чтение ради склейки перед записью — не потребление |
| `index` | `tmp/kb_master_fetch.py`, `tmp/read_brain.py`, `tmp/read_brain2.py` | разовые/диагностические |
| `cc_log` | `build_dec_port_spec.py`, `tmp/probe_bridge.py`, `tmp/verify_pc_write.py` | разовые |
| `pulse` | `tmp/pulse_read.py`, `tmp/read_brain.py` | разовые |

Ложные срабатывания наивного грепа, проверены и отброшены: `manager-bot/bot.py:218,586,819`
(`ctx["rules"]`, команда `/rules`), `manager-bot/bot.py:816` (`/pulse`), `lesson_router.py:96`
(строка `"faq"` в списке типов урока) — к Brain-докам отношения не имеют.

**Оговорка, без которой вывод читается неверно:** штаб (Claude-на-сайте) читает мозг Drive-коннектором,
мимо кода. `index`, `rules`, `infra`, `state_model` и `pulse` он читает — но по ритуалу, а не программно;
греп такое чтение не видит и видеть не может. «Никем не читаемое» здесь означает: **ни один процесс,
ни один скрипт репозитория к этому доку не обращается**.

---

## 4. ВЫВОД 3 — СМЕШАННЫЕ РОЛИ (главное)

Семь документов держат в одном теле разные роли. Ниже по каждому — цитаты кусков РАЗНЫХ ролей.

### 4.1 `index` (KB_MASTER), 86 535 симв. — четыре роли в одном файле

**СОСТОЯНИЕ (строки 1–58, стоит ВЫШЕ собственной шапки дока):**

> ```
>   2| === КАРТА МИГРАЦИИ, зафиксировано 29.07.2026 ===
>  15| ЭТАП 0 — ФУНДАМЕНТ. СДЕЛАНО 28–29.07.
>  21| ЭТАП 1 — РАЗДЕЛИТЬ РОЛИ МОЗГА. ИДЁТ.
>  49| ОТКРЫТО НА 29.07:
>  50| - Опись мозга — идёт.
>  51| - Post-run уборка маркеров гарда (маркер остаётся навсегда).
>  53| - Триггер ротации журнала — выключен, ждёт решения.
> ```

Это оперативное состояние на конкретную дату — по сути строка журнала, развёрнутая в блок.

**СПРАВОЧНИК (строка 60, настоящая шапка):**

> `KB_MASTER — ЕДИНАЯ КАРТА ПРОЕКТА TurboBaby. ТОЧКА ВХОДА №1.`
> `ЭТОТ ДОКУМЕНТ ЧИТАЕТСЯ ПЕРВЫМ. Он даёт всю структуру сжато. За деталями — ссылки в Разделе 5.`

**ИНСТРУКЦИЯ (§0, строки 65–77):**

> `РАЗДЕЛ 0 — ПРАВИЛО СВЕРКИ (ПЕРВИЧНОЕ, ОБЯЗАТЕЛЬНОЕ)`
> `ПЕРЕД тем как предлагать направление, разворачивать новую стройку, или говорить «давай решим, строить ли X» — ШТАБ ОБЯЗАН сверить с этой картой…`

**ЖУРНАЛ (§3, строки 128–577 — 450 строк, 53 % дока):**

> `- Касса A/B/C — фикс в проде (commit 13ee3b1, подтверждено сверкой кода 26.06). KB_CASH_CLOSED.`
> `- ⭐ 23.07 — ШТОРМ ГАРДА И ИТОГИ ДНЯ:`
> `  • ТРЕНАЖЁР v2 ПРИНЯТ (fa3746a, ПК-репо): корень провала был не в воронке, а в живой форме числа в датах клиента («25ого»/«30ое»).`
> `  • KB_trainer_log ПОЛНЫЙ (758f1de, ПК-репо): ретрай+спул TRN-строк… архив-док ротации заведён — file id 1lOco1SI58UNT0a4-TBuU3yYeZzbPkWUP.`

Это записи с датой, звёздочкой, хешем коммита — формат журнала, а не карты.

**Итог по `index`:** документ, объявленный «единой картой» и «точкой входа №1», больше чем
наполовину состоит из журнала, а сверху к нему приклеен блок текущего состояния. Читателю,
исполняющему §0 («прочитать KB_MASTER»), выдаётся 86 535 символов, из которых карта — меньше половины.

### 4.2 `project_state`, 37 994 симв. — архив + состояние + справочник

**АРХИВ (объявлено в шапке):**

> `# TurboBaby — состояние проекта (АРХИВ-СНИМОК)`
> `> **⚠️ ЭТОТ ДОКУМЕНТ = ИСТОРИЧЕСКИЙ СНИМОК (11 июня 2026). Единая карта проекта = KB_MASTER (Brain `name=index`). Читать KB_MASTER первым.**`

**СОСТОЯНИЕ (строки 6–8 — новее заявленной даты снимка на три недели):**

> `## 🚀 СТАРТ НОВОГО ЧАТА (читать первым, 11 июня)`
> `**🎯 ПРИОРИТЕТ №1 (поправка владельца 03.07.2026):** ОРКЕСТРАТОР СТУПЕНЬ 2 (O4) — допилить дев-бота/оркестратора…`

Док зовёт себя снимком 11 июня и в той же секции просит читать себя первым, а внутри несёт
поправку от 3 июля.

**СПРАВОЧНИК (строки 401–408 и, повторно, 426–432):**

> ```
> 401| ID базы знаний (Brain):
> 402| - Папка Brain : `1uWqHsxk7aEWoSNqaUBMmqkYOh2UKYLkY`
> 403| - knowledge_base (KB_knowledge_base): `1TwNzB_bGw1aHD1ThEuT9V6EgG7p70Tud6WeB0sKDt1Y`
> 407| - index         (KB_index)         : `1klCUC5domgR6t-3hu-Yc7UC75hgf-y6-rCILFnN7JJs`
> ```

**ИНСТРУКЦИЯ (строки 409–424) — в том же доке:**

> `### 🔁 ПРАВИЛО СИНКА (источник правды = git, Drive = генерируемое зеркало)`
> `- ОБЫЧНЫЙ СИНК после правки .md = одна команда Claude Code «синкни мозг»…`

### 4.3 `rules`, 16 736 симв. — инструкция + предметный справочник

**ИНСТРУКЦИЯ:**

> `6. ПРОВЕРЬ → ПУЛЬС ПЕРВЫМ, ЛОГ ТОЛЬКО ДЛЯ ИСТОРИИ (заведено 29.06, единая точка свода штаба)`
> `На любое «проверь/проверяй» штаб ПЕРВЫМ читает KB_PULSE (ключ `pulse`) — ОДНУ свежую строку статуса…`

**СПРАВОЧНИК (строка 106) — карта колонок таблицы, место которой в `infra`:**

> `ПРЕДМЕТНАЯ МОДЕЛЬ ТО — ЧТО КУДА ПИШЕТСЯ (Филипп, 28.06): (1) ОСНОВНЫЕ КОЛОНКИ (РЕГЛАМЕНТ, Лист1 Байки): масло (кол I «ТО Oil»), ВОЗДУШНЫЙ ФИЛЬТР (кол L), gear oil, ABS — каждое в СВОЮ колонку со СВОИМ…`

### 4.4 `review`, 511 симв. — три роли в семи строках

**НАВИГАЦИЯ (стр. 1):**

> `📦 KB_review — КОМПАКТНЫЙ канал ревью. Старое → KB_claude_review_archive (id 1K0gPMOyM-ER7nweda8-f9MK3edbepYCniZpQy0HkpAA).`

**ЖУРНАЛ (стр. 5–6):**

> `— PLAN 2026-07-02 21:40 UTC: ОРКЕСТРАТОР СТУПЕНЬ 2 (O4) — реализован 03.07.2026 → KB_MASTER §3`

**СОСТОЯНИЕ КАНАЛА (стр. 7):**

> `(review_archive: запись >183К — ограничение payload; история в cc_log + KB_MASTER §3)`

### 4.5 `cc_userbot_log`, 5 394 симв. — журнал + свод

**ЖУРНАЛ:**

> `KB_userbot_log — журнал userbot-ветки (КЛИЕНТСКИЙ контур). Ведёт Claude-на-сайте/Cowork (ПК-инстанс).`

**СВОД (стр. 2) — то есть тело уже не хроника, а слитые пять доков:**

> `(Свод от 23.06.2026: единый журнал из бывш. v2 + 5 update-дублей, без потерь.)`

### 4.6 `knowledge_base`, 56 521 симв. — бизнес-справочник + реестр инфраструктуры

**СПРАВОЧНИК БИЗНЕСА:**

> `### Шаг 3: Депозит — два варианта на выбор`
> `- **Байки с депозитом от 15,000 ฿** (CB 300R, MT-03, NINJA 400, CBR 650R, CB 650R, VULCAN, XSR 900, R7, XADV 750) → **АКТИВНО ПРЕДЛАГАТЬ паспорт**…`

**РЕЕСТР ID (строки 542–559) — техническая карта Drive внутри бизнес-документа:**

> ```
> 542| ## 📑 ДОКУМЕНТЫ И ID
> 545| - **Главная папка:** `19UHmWP1dsb1ADk8nwpZThOP0pCu_uhea` ← "база всей компании"
> 547| - **"менеджеру Байки" (CRM):** `1sL-rw0klRcJKtWKpgIzLVge6U1GswtBUacJ_jo0JgL0`
> 559| - **Главный проект:** `17OTrEgB0Z8uPYNI6vuF3KzY8VRqyovQG80Igis2WOzeAAPy1aqccJ2yq`
> ```

### 4.7 `cowork_log_test_archive`, 2 372 симв. — имя тестовое, содержимое боевое

**ЗАЯВЛЕННАЯ РОЛЬ (стр. 1) — заголовок БОЕВОГО архива, а ключ в манифесте тестовый:**

> `📦 KB_cowork_log_archive — АРХИВ журнала Dispatch/Cowork (старые записи cowork_log, newest-first).`
> `Ротация по размеру: записи сверх порога COWORK_MAX_BYTES переносятся сюда. Ничего не удаляется.`

**ФАКТИЧЕСКОЕ СОДЕРЖИМОЕ — боевые DONE, дословно совпадающие с живым `cowork_log`:**

> `DONE 2026-07-28 17:58 UTC: Dispatch 29.07 кодировка журнала: обратное чтение показало, что пять моих строк легли мохибейком («С‚РёРї РўРЎ» вместо «тип ТС») — sys.stdin.read() на Windows декодировал пайп кодировкой консоли…`

2 099 из 2 372 символов (89 %) — боевые записи в доке с ключом `cowork_log_test_archive`.

**Смешанных ролей: 7.**

---

## 5. ВЫВОД 4 — ЧТО ЧИТАЕТ ШТАБ НА КАЖДОЕ «ПРОВЕРЬ»

Правило зафиксировано в двух местах, дословно.

`rules`:109–113 (правило 6):

> `6. ПРОВЕРЬ → ПУЛЬС ПЕРВЫМ, ЛОГ ТОЛЬКО ДЛЯ ИСТОРИИ (заведено 29.06, единая точка свода штаба)`
> `На любое «проверь/проверяй» штаб ПЕРВЫМ читает KB_PULSE (ключ `pulse`) — ОДНУ свежую строку статуса «где я сейчас», мгновенно. Полный cc_log (`name=cc_log`) читать ТОЛЬКО когда нужна ИСТОРИЯ: разбор причины, аудит, сверка цепочки коммитов. НЕ на каждое «проверь».`

`rules`:56 (правило 4, отдельная разновидность команды):

> `ИТОГ: «проверь Dispatch» = читать cowork_log (id 1s9Fy3xm4FB99ah9xovLjYIMOjnDKz3Jc) — там И DONE, И ASK.`

`index`:698 повторяет то же самое в навигации:

> `- ⚡ ПРОВЕРЬ → KB_PULSE ПЕРВЫМ (однострочный статус «где я сейчас», мгновенно); полный cc_log читать ТОЛЬКО для ИСТОРИИ (разбор причины/аудит/сверка цепочки коммитов) — НЕ на каждое «проверь».`

### Сколько это символов

| сценарий | что открывается | символов |
|---|---|---|
| «проверь» по правилу 6 | `pulse` | **270** |
| «проверь Dispatch» (`rules`:56) | `pulse` + `cowork_log` | **50 397** |
| «проверь» + сверка по §0 (новая тема/разворот: «сначала прочитать KB_MASTER») | `pulse` + `cowork_log` + `index` | **136 932** |
| «проверь» с уходом в историю (`rules`:112) | + `cc_log` | **186 887** |
| если история уходит в архив | + `cc_log_archive` (988 521) | **1 175 408** |

### Полная ревизия (`index` §6, чек-лист) — отдельный ритуал, не «проверь»

Пункты чек-листа, которые суть Brain-доки:

> `[3] KB_MASTER — отражает реальность: что в проде / что строится / что тупиково / куда дальше? (ведёт Claude Code).`
> `[4] KB_ROADMAP_MASTER (ключ roadmap_master) — статусы вех верны (✅/в работе/отложено)?`
> `[5] KB_RULES / KB_INFRA / KB_STATE_MODEL — своды актуальны, без дублей?`
> `[6] ВЕСЬ DRIVE-МОЗГ — нет нового нароста дублей/эфемерного? счёт файлов в норме (~21 активных)?`

| пункт | доки | символов |
|---|---|---|
| [3] | `index` | 86 535 |
| [4] | `roadmap_master` | **0 — файла нет** |
| [5] | `rules` + `infra` + `state_model` | 33 191 |
| [3]+[4]+[5] | | **119 726** |
| + отметка ревизии (`index`:770 «краткая отметка в cc_log») | `cc_log` | **169 681** |
| [6] «ВЕСЬ DRIVE-МОЗГ», если читать телами | все 26 тел | **2 350 199** (2 342 534 без дубля `faq`) |

Два факта по §6, зафиксированы как есть:
- пункт [4] отправляет читателя в файл, которого нет с 27.06 (см. вывод 5);
- пункт [6] задаёт норму «~21 активных» файлов, а по факту в манифесте 26 ключей на 25 уникальных
  id, плюс минимум 4 живых дока вне манифеста.

---

## 6. ВЫВОД 5 — МЁРТВЫЕ УКАЗАТЕЛИ

Известные три (заданы в постановке): `roadmap_master` мёртв, `KB_NORTH_STAR` не открывается,
`KB_trainer_log` жив только по id. Ниже — весь список, с пометкой, был ли указатель назван
описью 29.07 или найден сейчас.

| # | указатель | где стоит | состояние | новый? |
|---|---|---|---|---|
| 1 | ключ `roadmap_master` → `1Z70Epg…4jWE` | `index`:700, `index`:765 (чек-лист [4]), `rules`:61, `orchestrator_plan`:2, `project_state`:393 | **МЁРТВ.** `read_doc` и по имени, и по id: `read_failed`, Drive «объект по указанному id не найден». Файл в корзину 27.06 (`cc_log_archive`:1986), корзина обратима ~30 дн — срок вышел | известен |
| 2 | `KB_NORTH_STAR` | `index`:699 (§5, навигация), `index`:442 | **НЕ ОТКРЫВАЕТСЯ.** Ни ключа, ни id. §5 подаёт как живой ориентир: «— философия главной оси (рамка над всем)» | известен |
| 3 | `KB_trainer_log` | `index`:713, `index`:540 | **ЖИВ ТОЛЬКО ПО ID** `13kp-54bz…`. По имени — `unknown_name`. Id в тексте дан, читатель дойдёт | известен |
| 4 | `KB_REVISION_RULE` | `rules`:64 — «оболочка — в архив (см. KB_REVISION_RULE / РАЗДЕЛ 6 KB_MASTER)» | **В ПУСТОТУ.** Ни ключа в манифесте, ни id в тексте. Стоит внутри ДЕЙСТВУЮЩЕГО правила 5, и в перечне «слитых в свод» (`index`:717 — только `KB_RULE_*`/`KB_DISPATCH_ASK_RULE`) его нет. Спасает только вторая половина ссылки — «РАЗДЕЛ 6 KB_MASTER» | **НОВЫЙ** |
| 5 | id `1TwNzB_bGw1aHD1ThEuT9V6EgG7p70Tud6WeB0sKDt1Y` (старый `knowledge_base`) | `project_state`:403 и :428 (дважды) | **МЁРТВ по живой пробе** (`pointer_probes.txt`: та же ошибка Drive, что у `roadmap_master`) | **НОВЫЙ** (опись 29.07 назвала мёртвым только `1klCUC…`) |
| 6 | id `1cRedmoltyntz5oNqjB3S3MloLftefEN9HlY7QPOHMQ8` (старый `project_state`) | `project_state`:404 и :429 (дважды) | **МЁРТВ по живой пробе** (там же) | **НОВЫЙ** |
| 7 | id `1klCUC5domgR6t-3hu-Yc7UC75hgf-y6-rCILFnN7JJs` (`KB_index`) | `project_state`:407 и :432 (дважды) | **МЁРТВ.** Отправлен в корзину 27.06 — `cc_log_archive`:2008: «- KB_index id 1klCUC5domgR6t-3hu-Yc7UC75hgf-y6-rCILFnN7JJs (пустой 1024б) → в корзину (KB_MASTER его заменил).» | известен |
| 8 | id `1lOco1SI58UNT0a4-TBuU3yYeZzbPkWUP` (архив тренажёра) | `index`:541, `trainer_log_doc.json` → `archive_id` | **ВНЕ МАНИФЕСТА, ЖИВОСТЬ НЕ ПРОБОВАНА.** В дампе тела нет, Drive не дёргал. При этом это боевой адрес: `trainer_log.py:424–429` пишет туда срез при перерастании порога | **НОВЫЙ** |
| 9 | имя `KB_MASTER` | 18 упоминаний, из них `index`:60,75,719, `rules`:61,64, `orchestrator_plan`:2, `project_state`:3 | **ИМЕНЕМ НЕ ОТКРЫВАЕТСЯ.** Манифест знает только ключ `index`; `read_doc name=KB_MASTER` → `unknown_name`. Док жив, но имя, которым его зовут ВЕЗДЕ, каналом не резолвится. Механизм («манифест знает только строчные ключи») опись 29.07 назвала, сам указатель в список не выписала | **НОВЫЙ как указатель** |
| 10 | `KB_userbot_log` | `project_state`:38 — «Детали — в KB_userbot_log» | По имени — `unknown_name`, id не дан. Док жив под ключом `cc_userbot_log` (его же первая строка начинается словами `KB_userbot_log — журнал userbot-ветки…`) | известен |
| 11 | `KB_INFRA_FORMS`, `KB_INFRA_UNIFY_FUTURE` | `infra`:53, `infra`:75 | Ключей нет, id нет. Мягкие: `index`:705 объявляет `KB_INFRA_*` слитыми в свод, а `infra`:53 говорит в прошедшем времени («полная карта — **была** в KB_INFRA_FORMS»). `infra`:75 при этом ссылается на него как на будущую работу | известен |
| 12 | `KB_claude_review_archive` id `1K0gPMOy…HkpAA` | `review`:1 | **ЖИВ, но опасен таймаутом:** чтение 69,2 с при живом теле 183 329 симв.; проба с таймаутом 45 с не уложилась (`TimeoutError` в `summary.json`). Имя устарело, нынешний ключ — `review_archive` | известен |

**Мёртвых/неоткрываемых указателей: 12, из них новых сверх известных трёх — 5**
(`KB_REVISION_RULE`, два мёртвых id в `project_state`, id архива тренажёра, имя `KB_MASTER`).

Отдельно, зафиксировано как факт без правки: `index`:700 утверждает «KB_ROADMAP_MASTER — В АРХИВЕ
(_archive; перемещён ревизией 16.07)», тогда как `cc_log_archive`:1986 фиксирует отправку этого файла
в корзину **27.06** — на три недели раньше. Один из двух источников описывает не то, что было.

---

## 7. ПЕРЕДЕЛАННАЯ ПРОВЕРКА: ID ВНЕ МАНИФЕСТА (файл без ключа / ключ без файла)

### Что не доработала прошлая опись

`docs/artifacts/2026-07-29-brain-registry-liveness.md`, §3.2(б), строка про `KB_booking_flow` и
`KB_collect_booking_spec`:

> `в манифесте нет; текст говорит «СПЕКИ (Brain, читаются по id)», но id в строке не приведён` →
> `**Не проверяемо изнутри канала** — ни ключа, ни id`

Вывод был неверен по причине, которую видно по дампу: опись смотрела `project_state`:50–51, где id
действительно нет, и не дошла до строк 106–107 того же дока, где оба id выписаны прямо:

> ```
> 106| `KB_booking_flow` (id `11HvMKGZRdSoRjojvAcmpN6_KJzkc1eRlIIx12JW6DQU`) и `KB_collect_booking_spec`
> 107| (id `1PZ7TeEQJLtzrw66Ll3PZBXIw-K70dnkh-3ClxDEMW48`) пока читаются ТОЛЬКО по id (в BRAIN_MANIFEST НЕ
> ```

Второе, независимое подтверждение тех же id — `review_archive`:54:

> `ЧАСТЬ 2 (🔴 — нужен clasp redeploy, ОСТАНОВ по твоему указанию): регистрация booking_flow→11HvMKGZRdSoRjojvAcmpN6_KJzkc1eRlIIx12JW6DQU и collect_booking_spec→1PZ7TeEQJLtzr…`

**Живость обоих проверена** — не мной и не сейчас, а той же offline-сессией, которая сняла дамп
(`pointer_probes.txt`, живые пробы по id):

> ```
> [ЖИВ]  KB_booking_flow           11HvMKGZRdSoRjojvAcmpN6_KJzkc1eRlIIx12JW6DQU  6501 симв. | голова: # TurboBaby — Анализ диалогов: флоу брони и логика для userbot-сборщика
> [ЖИВ]  KB_collect_booking_spec   1PZ7TeEQJLtzrw66Ll3PZBXIw-K70dnkh-3ClxDEMW48  4553 симв. | голова: collect_booking.py — СПЕЦИФИКАЦИЯ + готовый промпт-парсер (для Claude Code)
> ```

**Это и есть искомые ДВА id вне манифеста.** Оба живы, оба используются как спеки, ни один не
достижим через `list_brain`.

### Полный свод проверки по дампу

Из тел 26 доков извлечено **68 разных Drive-подобных id**, из них **49 отсутствуют в манифесте**.
Разобраны все 49; Brain-доками среди них оказались четыре.

**ФАЙЛЫ БЕЗ КЛЮЧА (живой Brain-док, в манифесте нет) — 4:**

| id | имя | статус | где назван |
|---|---|---|---|
| `11HvMKGZRdSoRjojvAcmpN6_KJzkc1eRlIIx12JW6DQU` | `KB_booking_flow` | **ЖИВ**, 6 501 симв. | `project_state`:106, `review_archive`:54 |
| `1PZ7TeEQJLtzrw66Ll3PZBXIw-K70dnkh-3ClxDEMW48` | `KB_collect_booking_spec` | **ЖИВ**, 4 553 симв. | `project_state`:107, `review_archive`:54 |
| `13kp-54bzN2Xua8R5VN4EaXR7IElTKg4O` | `KB_trainer_log` | **ЖИВ**, 2 190 симв. (тело в дампе) | `index`:713, `cowork_log_archive`:691, `trainer_log_doc.json` |
| `1lOco1SI58UNT0a4-TBuU3yYeZzbPkWUP` | `KB_trainer_log_archive` | живость **НЕ ПРОБОВАНА** (Drive не дёргал) | `index`:541, `trainer_log_doc.json` → `archive_id`; пишет `trainer_log.py:424–429` |

**КЛЮЧИ БЕЗ ФАЙЛА (ключ манифеста, файла нет) — 1:** `roadmap_master` → `1Z70Epg…4jWE`.

**ДВА КЛЮЧА НА ОДИН ФАЙЛ — 1:** `faq` + `turbobaby_faq` → `1tv8Y-…Gqs98`.

**Сходимость реестра:**

```
записей в list_brain            27
  − folder_id (папка)            1
= ключей-документов             26
  − дубль-ключ turbobaby_faq     1
= уникальных id                 25
  − мёртвый roadmap_master       1
= живых файлов в манифесте      24
  + живых доков вне манифеста     4   (2 спеки + trainer_log + его архив)
= ВСЕГО BRAIN-ДОКОВ             28
```

Число 28 сходится с оценкой владельца «~28 доков».

**Остальные 45 id вне манифеста — не Brain-доки**, разобраны и отброшены по группам:
рабочие таблицы и папки компании (CRM «менеджеру Байки», «Байки», «Цены», «Зарплаты», шаблон
договора, папки «Паспорта» и готовых договоров, форма Rental Agreement — все из `knowledge_base`:545–556
и `infra`:12,32); удалённые OLD-копии и orphan-файлы из записей чистки 27.06 (`cc_log_archive`:503,
2005–2008, 2821–2841); одноразовые тест-доки обкатки (`cc_log_archive`:3099, 3142, 3173).
Отдельно: `1c0c913UFlSd84-W3vaqqGJEZ5fBpDjHsapO3Un1ysi8` («Правила», Google Doc договора) —
**ЖИВ** по пробе `pointer_probes.txt`, 4 820 симв., но это бизнес-документ вне папки Brain,
а не элемент мозга.

---

## Сводка

| вывод | итог |
|---|---|
| Дамп | ЦЕЛ. 26 тел, 2 350 199 симв., 3 589 543 байта; манифест 27 записей, проб 27 (26 ok / 1 FAIL) |
| Доков | **28** Brain-доков: 24 живых под 26 ключами + 4 вне манифеста; 1 мёртвый ключ |
| 1. Дубли | **7 групп**: `faq`≡`turbobaby_faq` побайтно; `index`↔`state_model` 2 662 симв.; `cc_log_archive`↔`review_archive` 20 495 симв.; `cowork_log`↔`cowork_log_archive` 2 531; `cowork_log`↔`cowork_log_test_archive` 2 099; прайс/парк втроём (`knowledge_base`/`faq`/`park_list`, с расхождением «нет в парке»); `project_state` дублирует блок id внутри себя |
| 2. Никем не читаемое | **20 доков**, 2 143 585 симв. = **91 % объёма мозга** не открывает ни один процесс репо. Боевых читателей всего два: `suggest.py`→`faq`, `trainer_log.py`→`KB_trainer_log` |
| 3. Смешанные роли | **7 доков.** Главный — `index`: «единая карта, точка входа №1», у которой §3 = 450 датированных журнальных строк (53 % тела), а сверху приклеен блок состояния на 29.07. Далее `project_state` (архив+состояние+справочник+инструкция), `rules`, `review`, `cc_userbot_log`, `knowledge_base`, `cowork_log_test_archive` (тестовый ключ, боевое содержимое на 89 %) |
| 4. Штаб на «проверь» | По `rules`:111 — `pulse`, **270 симв.** «Проверь Dispatch» (`rules`:56) — плюс `cowork_log`, **50 397**. Со сверкой по §0 — **136 932**. Полная ревизия §6 [3][4][5] — **119 726**, из них пункт [4] ведёт в мёртвый файл |
| 5. Мёртвые указатели | **12**, новых сверх известных трёх — **5**: `KB_REVISION_RULE` (`rules`:64), мёртвые id `1TwNzB…` и `1cRedmo…` (`project_state`, каждый дважды), id архива тренажёра `1lOco1SI…` (вне манифеста, боевой писатель есть), имя `KB_MASTER` (18 упоминаний, каналом не резолвится) |
| Проверка id вне манифеста | **ДОВЕДЕНА.** Два id — `KB_booking_flow` `11HvMKG…` и `KB_collect_booking_spec` `1PZ7Te…` — есть в телах (`project_state`:106–107, `review_archive`:54) и **ЖИВЫ** (6 501 и 4 553 симв.), но в манифесте отсутствуют. Прошлая опись объявила их «не проверяемо» — вывод снят. Обратная сторона: ключ `roadmap_master` без файла. Плюс ещё два файла без ключа: `KB_trainer_log` и его архив |
