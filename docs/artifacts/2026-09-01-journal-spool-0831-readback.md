# Спул журнала за 31.08: сведён и прочитан назад (01.09.2026)

Read-only разбор. Ни одной записи в мозг этим разбором не сделано, ничего не удалено,
`.env` не читан — конфиг моста брал САМ `brain_writer` (запрет класса 328 соблюдён).

Инструмент замера: `tmp/journal_spool_0901/readback.py` (импортирует `brain_writer` и
`cowork_log_append`, свою логику держит строковой).

## 1. Премиса проверена и ОПРОВЕРГНУТА: спул пуст

| что | значение |
|---|---|
| путь спула | `D:\turbobaby-bot\cowork_log.spool` — взят ИЗ КОДА (`cowork_log_append.SPOOL_PATH`), не угадан |
| состояние | **файла НЕТ** → отложенных строк **0** |
| других спулов журнала на диске | нет: `*.spool` по дереву (кроме `manager-bot/`) → 0 файлов; `pc_orchestrator.revizor_spool.json` — спул НАХОДОК ревизора, не журнала, и он пуст (2 байта) |
| реестр успешных записей | `D:\turbobaby-bot\cowork_log.ledger`, 500 строк (кольцо на 500), mtime 01.09 00:57 |

Премиса была ВЕРНА в момент постановки и протухла до исполнения: 21 запись действительно
копилась в спуле с 16:02 до 17:22 UTC 31.08 — и была вытолкнута **до** старта этой задачи.

## 2. Кто и когда свёл спул — механика, а не догадка

Писатель досылает отложенное на ПЕРВОЙ ЖЕ успешной записи: `main()` → `compose(new_line,
pending, old)` → `spool_clear()` → цикл `ledger_add` по `[new_line] + pending`. Поэтому в
реестре пачка досылки опознаётся по ОДНОМУ штампу `ts` с точностью до секунды.

| пачка (UTC) | строк | состав |
|---|---|---|
| 2026-08-31 16:01:41 | 6 | 1 новая (`NOTE 16:01 авто-фетч`) + **5 отложенных** (15:23–15:53) |
| 2026-08-31 17:57:38 | 22 | 1 новая (`NOTE 17:57 Orchestrator: взял задачу #56`) + **21 отложенная** (16:02–17:22) |

**Спул свела сама постановка этой задачи.** Строка «взял задачу #56 (in_progress)» — это и
есть та первая успешная запись после паузы моста; отложенное уехало вместе с ней в 17:57:38 UTC.
Причина паузы названа в самих записях: `BLOCKED 16:41 … Bridge socket permission (WinError 10013)`.

## 3. Досылка по принадлежности полосы

Слать нечего: спул пуст. Разделять по полосам тоже нечего — **все 28 строк обеих пачек
принадлежат полосе ПК** и лежат в её доке `cowork_log`. Локального спула чужой (VPS) полосы
на диске нет вовсе, `cclog` отсюда не зовётся.

## 4. Чтение назад: 28 из 28 — НАЙДЕНО

Снимки живого мозга через `brain_writer.read_text` (сохранены в
`tmp/journal_spool_0901/*.snapshot.txt`):

* `cowork_log` — **658 462 символа, 1936 строк**;
* `cowork_log_archive` — 763 217 символов, 2097 строк.

Сверка префиксная, а не дословная: реестр режет строку до `LEDGER_LINE_MAX = 300`, поэтому
длинная запись сверяется по НАЧАЛУ строки дока, короткая — целиком (класс «подстрока внутри
чужой строки вхождением не считается» соблюдён — сравнение идёт по границам строк).

| исход | сколько |
|---|---|
| **НАЙДЕНО** | **28** (все — в живом `cowork_log`, до архива дело не дошло) |
| НЕ НАЙДЕНО | 0 |
| НЕИЗВЕСТНО | 0 |

Поимённо (штампы UTC 31.08):

пачка 16:01:41 — `NOTE 16:01` авто-фетч · `DONE 15:23` ARTIFACT H1→V0 owner gate ·
`DONE 15:43` ARTIFACT H1→V0 shadow result · `DONE 15:43` RC shadow run ·
`DONE 15:50` RC copy-only mirror · `DONE 15:53` ARTIFACT C1 Claude shadow STOP.

пачка 17:57:38 — `NOTE 17:57` взял задачу #56 · `DONE 16:02` ARTIFACT operating frame ·
`ASK 16:02` RC frame documented · `DONE 16:12` ARTIFACT Claude architecture review ·
`WAITING 16:12` RC request sent · `DONE 16:14` RC review completed · `DONE 16:18` ARTIFACT
G1 spec · `ASK 16:18` RC G1-spec развилка · `WAITING 16:29` RC OPERATING_SNAPSHOT ·
`DONE 16:33` ARTIFACT G1 preflight STOP · `DONE 16:35` ARTIFACT Claude operating snapshot ·
`BLOCKED 16:35` RC G1 preflight · `DONE 16:41` ARTIFACT Frame synchronization v1 ·
`BLOCKED 16:41` RC WinError 10013 · `NOTE 16:42` Frame sync preflight · `DONE 16:43`
ARTIFACT Frame change packet · `DONE 16:50` ARTIFACT frame-sync-v1 · `DONE 16:51` Codex
FRAME_CHANGE_PACKET applied · `PLAN 17:02` Codex S1 packet · `PLAN 17:13` Codex R1 packet ·
`DONE 17:15` ARTIFACT R1 registry source names · `DONE 17:22` ARTIFACT S1 fresh queues.

## 5. Четыре именованных факта

| факт | вердикт | чем предъявлен |
|---|---|---|
| **S1: зашитый ключ убран, коммит `768729b`** | **НЕ НАЙДЕНО** | В живом доке и архиве **0** строк со словами `containment` / `s1-suggest` / `secret-containment`. Хеш `768729b` встречается ровно 2 раза и оба — про другое: `DONE 14:06` (база изолированного worktree) и `NOTE 13:14` (ворота контура остановили применение `768729bf0`). На диске факт есть — `docs/artifacts/2026-08-31-s1-suggest-secret-containment-result.json` и коммит `768729b` «Contain suggest credential source locally» (`suggest.py` +5, `test_suggest.py` +17). **В мозг он не уехал ни строкой** |
| **ветка впереди `origin/main` на 45 коммитов** | **НАЙДЕНО** | `DONE 2026-08-31 14:06 UTC: RC … local branch is 45 commits ahead`. Число ПРОТУХЛО: на 01.09 ветка впереди на **47** (легли `93b0ed2` и `acba7e5`) |
| **рамка-синк 16:45** | **НАЙДЕНО ПО ФАКТУ, НЕ ПО ШТАМПУ** | Строки со штампом `16:45` в журнале нет ни одной (единственное «16:45» в доке — из записи 22.08). Событие действительно произошло в 16:45: бэкап писателя `tmp/brain_backup_frame_sync_v1_20260831-234545.txt` = 23:45:45 +07 = **16:45:45 UTC**. В журнал оно легло двумя строками ПОЗЖЕ — `DONE 16:50` (ARTIFACT frame-sync-v1) и `DONE 16:51` (Codex: FRAME_CHANGE_PACKET applied). Штамп в журнале — время ПИСАТЕЛЯ, а не время события; искать заход по времени события бесполезно |
| **вечерние заходы G1 и R1** | **НАЙДЕНО оба** | G1 — **7 строк** 31.08 (16:12 → 16:35: spec, ASK-развилка транспорта, preflight, STOP, BLOCKED). R1 — **2 строки** (`PLAN 17:13` пакет подготовлен, `DONE 17:15` ARTIFACT `docs/artifacts/2026-09-01-r1-state-registry-source-names.md`) |

## 6. Что осталось НЕИЗВЕСТНЫМ (честно)

Запись, которая не легла НИ в мозг, НИ в спул, НИ в реестр, этим разбором не ловится: спул —
единственный местный отстойник, и он пуст, а реестр — кольцо на 500 строк. Утверждать
«за вечер 31.08 не потеряно ничего» права нет; предъявлено ровно то, что **всё, что дошло до
спула или реестра, в мозге есть**.

## 7. Мина, которую стоит держать в голове

`cowork_log.spool` живёт по прямому пути `HERE/cowork_log.spool` и, в отличие от реестра, **не
проходит через `log_setup.state_path`**. Значит `spool_clear()` под тест-прогоном сносит БОЕВОЙ
спул — тот самый класс «гейт съел спул» (05.08), закрытый для реестра и для спула ревизора, но
не для спула журнала. Здесь это ни на что не повлияло (спул свела штатная досылка, а не тест),
но как класс — открыт.
