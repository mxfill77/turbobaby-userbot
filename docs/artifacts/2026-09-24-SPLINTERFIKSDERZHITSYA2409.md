# SPLINTERFIKSDERZHITSYA2409 — фикс спама Splinter (0027-72f) на живом сервере держится; самообновления, которое сбросило бы дерево, нет

Задание Штаба `0028-73a.2409` (ящик, полоса ПК), только чтение. Замер 24.09.2026 02:49–02:55 UTC
(09:49–09:55 по Пхукету).

**Итог (п.5): ФИКС ДЕРЖИТСЯ** — на диске, в живом процессе и в GitHub. Риск П4 кодом **не подтверждён**:
самообновления у серверного дерева нет, а доставка в GitHub **уже прошла** (ПК, 23.09 22:09:18 UTC),
поднимать её не надо. **Живьём на сообщении форума фикс ещё НЕ проверен:** после рестарта форум не
прислал ни одного сообщения.

## FACT

- `systemctl show splinter` (02:49:53 UTC): `active/running`, `MainPID=2370103`,
  `ActiveEnterTimestamp = ExecMainStartTimestamp = 23.09.2026 22:05:22 UTC`, `NRestarts=0`. В журнале
  юнита с 22:00 одна пара `Stopping` 22:05:18 → `Started` 22:05:22, дальше рестартов нет.
- `git rev-parse` на сервере: `HEAD` = `main` = **`941af26`**; локальный ref `origin/main` = `2e98f5c`.
- `git ls-remote` на GitHub с ПК (02:50:05 UTC): `refs/heads/main` = **`941af26`**, ветка
  `splinter-floor-quiet-2409` = `941af26`.
- sha256 на диске сервера = `git cat-file 941af26:<файл>` для `reply_floor.py` (`78433008…`),
  `splinter.py` (`84d3e25e…`), `tests/test_floor_quiet.py`, `tests/test_album_one_pass.py`. Для `2e98f5c`
  два модуля дают другие хеши, а обоих тестов там нет.
- `splinter.log` после 22:05:22 UTC: строк `Splinter [servicing]` **0**, строк `ПОЛ ОТВЕТА` **0**.
  Три последних ответа пола в теме `75` (= NMAX 155 BLACK GOLD 4255) — 23.09 **07:28:26 · 07:39:29 ·
  08:46:52 UTC**.
- На сервере ничего не менялось: git только `rev-parse`/`log`/`cat-file`, чтение файлов, `systemctl show`,
  `journalctl`. Временных мест не создано ни на ПК (`tmp/splinter_check_2409/` не понадобился), ни на
  сервере (`/tmp/sc2409/` не создавался).

SPLINTERFIKSDERZHITSYA2409

## 1. П1 — версия и процесс

| что | премиса Штаба | замер |
|---|---|---|
| splinter | active, PID 2370103, старт 22:05:22 UTC | **верно**: `active/running`, PID 2370103, 22:05:22 UTC, `NRestarts=0`. `ps`: `bot.py` из venv, прожил 17070 с |
| HEAD сервера | 941af26 | **верно**. Reflog `main`: 21:13:38 fast-forward на `7789243` и в ту же секунду `reset` назад на `2e98f5c` (откат 0026-72e) → **22:05:14 fast-forward на `941af26`**. После этого reflog пуст |
| origin/main | 2e98f5c | **верно только для ЛОКАЛЬНОГО ref сервера**, и он устарел. Последний раз двигался 22.09 09:09:22 UTC (`update by push`): сервер сам не делает fetch. **В GitHub `main` = `941af26`** |
| процесс держит фикс | — | файлы с фиксом записаны в 22:05:14 UTC, процесс стартовал в 22:05:22, значит модули с фиксом он прочитал уже новыми. Файлы на диске байт-в-байт совпадают с `941af26` |

**Главная находка п.1 — отклонений нет:** HEAD = `941af26`, после 22:05:22 процесс не перезапускался.
Новое по сравнению с премисой: GitHub `main` уже стоит на фиксе. Его туда довёз ПК:
`tmp/srv_delivery/state.json` → `last_deliver = [e28d409, 941af26]`,
`last_deliver_at = 1790201358` = **23.09.2026 22:09:18 UTC**; в голом зеркале `hub/main = srv/main = 941af26`.
Это штатный оборот `srv_delivery.py`: через 4 минуты после fast-forward на сервере.

## 2. П2 — время ответов пола в теме 4255

Тема `75` — это `NMAX 155 BLACK GOLD 4255` (`topics_map.json:31` на сервере). Строки журнала вида
`ПОЛ ОТВЕТА: заход промолчал (исход …) — отвечаю сам, тема=75`:

| UTC | Пхукет | исход | вид снимка |
|---|---|---|---|
| 23.09 07:28:26 | 14:28 | unclear | bike |
| 23.09 07:39:29 | 14:39 | unclear | bike |
| 23.09 08:46:52 | 15:46 | unclear | wheel |

Все три стоят **за 13 ч 18 мин – 14 ч 37 мин ДО** рестарта в 22:05:22. **Премиса П2 верна**, скриншоты
сделаны с 23.09, до фикса. Второй, независимый свидетель — архив форума на ПК
(`chatlog/servicing/2026/09/2026-09-23.jsonl`, снят userbot'ом 23.09 около 14:46 UTC): в теме 75
сообщения бота стоят в 07:28:27, 07:39:30 и 08:46:53 UTC, через секунду после строк журнала. Там же
последнее сообщение форума за 23.09 — **08:46:53 UTC**.

## 3. П3 — окно после рестарта (23.09 22:05:22 → 24.09 02:50:46 UTC, 4 ч 45 мин)

| что | число | как названо в журнале |
|---|---|---|
| обновлений форума `-1002751134848`, дошедших до Splinter | **0** | вход журналирует `splinter.py:10182` (`Splinter [servicing] <chat> …`) на каждое сообщение и на каждый альбом. После рестарта id форума встречается в журнале один раз, в баннере старта |
| решений пола «молчу» | **0** | `ПОЛ ОТВЕТА: молчу по делу (<почему>) — тема=N` (`splinter.py:10146`). За всё время журнала — 0 |
| решений пола «отвечаю сам» | **0** | `ПОЛ ОТВЕТА: заход промолчал (исход …, упал=…, вид снимка=…) — отвечаю сам, тема=N` (`splinter.py:10154`). За всё время — 53, последнее 23.09 08:46:52 |
| сообщений Splinter в форум | **0** по следам журнала: пол 0, подсказок 0 (ни `✅ подсказка … отправлена`, ни `🔁 … ПОДАВЛЕНА`), записей работ 0 (нет ни одного входящего) | запись работ 0 · совет 0 · пол 0 |

**Ноль обновлений — живой проверки фикса ещё не было.** Последнее сообщение форума, дошедшее до
Splinter, — 23.09 08:46:20 UTC. С тех пор форум молчит 18 ч, и «утро механиков» к 09:50 по Пхукету
ещё не началось. То, что Splinter жив и принимает сообщения, видно по другим группам: `[delivery]` в
23:03, 23:05 и 23:07 UTC и `[attendance]` в 02:39:56 UTC. Молчание форума — не смерть опроса.

Счёт исходящих ограничен. Единственная дверь к Telegram, `_send` (`splinter.py:142`), сама
каждое сообщение не журналирует. Поэтому «0» здесь — это сумма журнальных следов всех дорог в
форум, а не показание счётчика отправок. `service_pending_reminder` запускался 5 раз, но его
отправка в тему идёт через `_hint_send`, а тот журналирует оба исхода (`splinter.py:835`, `:843`).
Таких строк после рестарта 0.

## 4. П4 — что делает с деревом `/root/turbobaby-manager-bot` самообновление или доставка

**Ответ: ни то ни другое — «иное». Ни один механизм это дерево не пишет. Сброса к `origin/main` в коде
нет, fast-forward на сервере тоже нет, а доставка идёт в обратную сторону: с сервера в GitHub, и только
fast-forward.**

| где | файл:строка | что делает |
|---|---|---|
| расписание сервера | `crontab -l` → `no crontab for root`; таймеры `expectations`, `invariants`, `splinter-health` + системные | деревом не распоряжается никто по часам |
| демоны сервера | `orchestrator_daemon.py:3476`, `expectations_run.py:355`, `prod_drift.py:294`, `health.py`, `status_truth.py`, `invariants_check.py` | git только читает: `diff --name-only origin/main`, `log origin/main`, `rev-parse`, `ls-files`. В 21 названном файле сервера (демоны, таймеры, гейт, pre-push хук, `bot.py`, `splinter.py`, откат, доставка моста) нет ни `reset`, ни `pull`, ни `fetch`, ни `checkout`, ни `merge`, ни `stash` |
| единственная пишущая git-команда сервера | `orchestrator_daemon.py:1847` (`_exec_git_push`) | `git push` по операции `git_push` (подтверждение владельца), через pre-push гейт. Дерево не трогает |
| доставка (ПК) | `srv_delivery.py:421`, `:426` · `:445–446` · `:483` | `fetch` с сервера и из GitHub — только в голое зеркало под `tmp/` на ПК. Проверка родства `merge-base --is-ancestor`. **Не форсный** `push hub <sha>:refs/heads/<ветка>`. На сервер не пишет ничего (шапка `:23–25`) |
| самообновление (ПК) | `pc_orchestrator.py:8870–8893` (`git_ff_pull_tick`) | только `pull --ff-only`. Если локальная ветка впереди origin — «пропуск», merge и rebase не делает никогда. Работает с **деревом ПК**, не с сервером |

**Остаток риска — только ручной, не автоматический.** Локальный ref `origin/main` сервера устарел
(`2e98f5c`). `git reset --hard origin/main` без `fetch` снял бы фикс с диска, а при следующем рестарте
и из процесса. После `git fetch` та же команда встала бы на `941af26` и ничего бы не сняла. Устаревший
ref задевает и наблюдателей: они сравнивают диск с `origin/main` (`expectations_run.py:355`,
`orchestrator_daemon.py:3476`), поэтому `reply_floor.py`, `splinter.py` и оба теста для них
«расходятся с origin/main». Это шум сверки, а не угроза фиксу. Лечится одним `git fetch` на
сервере; в этом заходе он **не делался**, потому что задание его запрещает.

## 5. Итог одной строкой

**Фикс держится:** диск = `941af26`, процесс поднят после правки и больше не перезапускался, GitHub
`main` = `941af26`, самообновления серверного дерева нет. **Риск П4 не подтверждён.** Доставку в
GitHub поднимать не нужно, она уже прошла 23.09 22:09:18 UTC. **Функционально — НЕИЗВЕСТНО до первого
сообщения форума.** Признак проверки — строка `ПОЛ ОТВЕТА: молчу по делу (…) — тема=N` на снимке без
приборки или на болтовне.

## 6. Чего заход не знает и не делал (честно)

* **Значение ручки `FLOOR_QUIET` в живом процессе не прочитано.** Файл настроек и окружение процесса
  не читались: это запрет. Дефолт кода — «вкл» (`splinter.py` → `_floor_quiet_on`: `FLOOR_QUIET` по
  умолчанию `"1"`). 0027-72f файл настроек не правил. Если строку `FLOOR_QUIET=0` туда внёс кто-то
  другой, фикс выключен, и узнать это можно только по первой строке пола в журнале.
* Кто именно запушил `main` в GitHub, заход установил по машинной записи (`state.json` доставки ПК), а не
  по журналу мозга: строку `srv_delivery` в `cowork_log` заход не читал.
* Попутно, вне предмета: после рестарта в `splinter.log` 21 `[ERROR] bridge_client` (HTTP 302 на
  `get_pending` и `read_doc`) и 3 `telegram.error.NetworkError: Bad Gateway` в 01:11 UTC (опрос
  `get_updates`, дальше опрос жив). Фраза 0027-72f «ошибок 0» относилась к первым 45 строкам и
  сегодня неверна. К фиксу пола эти ошибки отношения не имеют и здесь не разбирались.
* На сервере не выполнялись `git status`, `fetch`, `pull`, `push`, `reset`, `merge`, `checkout`, рестарт и
  правка файлов. Наружу (форум, группы, владелец, клиенты) не ушло ничего. Текст переписки механиков в
  этот файл не выведен.
