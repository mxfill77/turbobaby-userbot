# Сверка ДОКУМЕНТЫ ↔ КОД, проход 2: проверка проверки, 2026-07-30 (вечер)

Read-only. Правок не делал и не предлагаю — по заданию.

Задание пришло второй раз. Первый проход уже лежит артефактом:
[`2026-07-30-docs-vs-code-audit.md`](2026-07-30-docs-vs-code-audit.md) (коммит `d14d450`,
22:44) — 66 расхождений, 375 строк. Повторять его смысла нет; ценность второго прогона —
в двух вещах, которых первый сделать не мог:

1. **Проверить сам аудит.** Он — такой же документ о поведении кода, как `dec_port_spec.md`.
   Если он врёт, на нём построят ровно тот же ложный диагноз, ради которого затевалась
   сверка. Пять независимых проверяющих получили задание **опровергать** его утверждения.
2. **Закрыть `.env`.** Первый проход девять раз написал «истина зависит от `.env`, который в
   этом прогоне не читался». В этом прогоне владелец одобрил класс `env` — открытые вопросы
   закрыты живым конфигом.

---

## §0. Главная находка прогона: аудит №1 искал не там

`D:\foreign\turbobaby-manager-bot` — **живой локальный клон VPS-репозитория на этой же
машине**. Свой git (HEAD `cb4e3e2`), 25 python-модулей, свой `docs/`.
Путь закреплён в `.claude/settings.json:26-29` (Edit/Write на `pretool_guard.py` и
`tests/test_guard_card_min.py`) — то есть о нём знает конфигурация репозитория.

Аудит №1 доказывал отсутствие через `git ls-files` и `grep *.py` **по ПК-репозиторию** —
и объявил 14 сущностей несуществующими. Все 14 найдены и **совпали по значению** с тем, что
пишет `dec_port_spec.md`:

| «grep → 0» в §4.1 аудита №1 | живой факт |
|---|---|
| `ORPHAN_TTL` = 600 | `foreign/orchestrator_daemon.py:71 ORPHAN_TTL = _env_int("ORPHAN_TTL", 600)` |
| `TASK_TIMEOUT`=600 / `TASK_TIMEOUT_DEV`=2700 | `:65`, `:66` — дословно как в доке |
| `PC_STEP_TIMEOUT` = 3600 | `:110` |
| `APPROVED_TTL` = 1800 | `:115` |
| `HEARTBEAT_SEC` = 45, «фон-поток бьёт updated» | `:60` — комментарий дословно из доки |
| `ORCH_MODEL` дефолт «fable» / `ORCH_MODEL_FALLBACK` = `claude-fable-5` | `:173`, `:174` |
| `EXECUTOR_MODEL`, `EXECUTOR_EFFORT` | `:205`, `:211` |
| `process_orphans`, `_task_timeout`, `_DEC_WAIT_STATUSES`, `_earlier_new_sibling`, `PC_SILENT_MARK` | `orchestrator_daemon.py` + `tests/test_orphan_timeout.py`, `tests/test_pc_dec.py` |
| `get_pending_multi`, `report_results` | `bridge_client.py`, `devbot.py`, `orchestrator_daemon.py` |
| `cclog`, `cclog … --pulse` | `foreign/cclog.py:69`, `:181`, `:210 bridge.write_doc(text=pulse_str, name="pulse")` |
| `_repo_tracked()` («сущность, которой нет») | `foreign/pretool_guard.py:795 def _repo_tracked` — жива |

**Следствие для диагноза.** `dec_port_spec.md` не «неверен» — он **не помечен полосой**.
Его числа точны для VPS и проверяемы одной командой, не выходя с этой машины. Вердикты
аудита «НЕТ КОДА» надо читать как «нет в ПК-репо», а не «нет нигде». Ложный диагноз, который
на этом строится: «дока описывает несуществующий механизм ⇒ выбросить» — выбросят живую
спеку соседней полосы.

**И тут же — расхождение, которого не видел никто.** Док лежит в ОБОИХ репозиториях, и копии
**разошлись**:

| | наш `docs/dec_port_spec.md` | `foreign/docs/dec_port_spec.md` |
|---|---|---|
| строк | 99 | 93 |
| mtime | 2026-07-12 22:37 | 2026-07-23 16:59 |
| sha256 | `F258A5F2…58A0DD` | `F523AADC…5C84E2` |
| «Полоса pc реапером НЕ трогается» | строка **89** | строка **83** |

Затравочная фраза жива в обеих копиях. При этом `ENV_PLAYBOOK.md` в двух репозиториях
**байт-в-байт одинаков** (`F1EC34E1…F6A103` = `F1EC34E1…F6A103`) — то есть правило
«файл одинаков в обоих репозиториях» (`ENV_PLAYBOOK.md:4`) выполняется машинно-проверяемо
ровно для одного файла, а соседний док разъехался на 11 дней, и этого никто не заметил.

---

## §1. Что изменил живой `.env` (закрытие открытых вопросов прохода 1)

Секретные значения не печатал и не цитирую — только ключи конфигурации.
14 проверок: **7 совпадений, 6 расхождений, 1 наблюдение прогона.**

| # | док:строка | что написано | что в живом окружении | вердикт |
|---|---|---|---|---|
| V1 | `CLAUDE.md:16` | основная голова ПК — `claude-opus-5` | `.env`: `THINKER_MODEL = claude-opus-5` | СОВПАДАЕТ |
| V2 | `CLAUDE.md:16` | запасная — `claude-opus-4-8` | `.env`: `THINKER_FALLBACK = claude-opus-4-8` | СОВПАДАЕТ (только у думателя; у headless-исполнителя ветки фолбэка нет — B2 прохода 1 подтверждён) |
| V3 | `CLAUDE.md:17-18` | «Так на ПК (`THINKER_MODEL`, **`EXECUTOR_MODEL`**)» — как env-ручки | **ключа `EXECUTOR_MODEL` в `.env` НЕТ**; нет и в User/Machine-переменных; код его не читает (`pc_orchestrator.py:860` — константа) | **РАСХОЖДЕНИЕ.** B1 прохода 1 подтверждён живым конфигом: ручки не существует ни в коде, ни в `.env` |
| V4 | `CLAUDE.md:21` | «`suggest` живёт на своей паре (`SUGGEST_MODEL=sonnet`)» | `.env`: `SUGGEST_MODEL = sonnet`, `SUGGEST_MODEL_FALLBACK = sonnet` | **СОВПАДАЕТ.** Вердикт B3 прохода 1 («РАСХОДИТСЯ») **снимаю**: он держался на гипотезе пустого `.env`. Риск остаётся теоретическим: дефолт кода — `fable` (`suggest.py:242`), короткий алиас → HTTP 404 |
| V5 | `dec_port_spec.md:71` | думатель: `--model ORCH_MODEL (.env, дефолт «fable»)` | в ПК-`.env` ключа нет и ПК-код его не читает; `foreign/orchestrator_daemon.py:173` — дефолт «fable» | док **верен для VPS**, к ПК не относится (см. §0) |
| V6 | `revizor_recon.md:214-215` | «локальный дефолт 829, переопределяется `PC_NA_TOPIC` в .env (**в проде = 1160**)» | **ключа `PC_NA_TOPIC` в `.env` НЕТ** ⇒ живая тема красных карточек = `829` (`pc_orchestrator.py:117`) | **РАСХОЖДЕНИЕ, доказано конфигом.** «В проде 1160» неверно для этого ПК. Ложный диагноз: «красное с ПК не пришло в 1160 ⇒ карточка потерялась» — она в 829 |
| V7 | `revizor_recon.md:151` | «Все задачи **жёстко** в `LANE = "pc"`» | `PC_LANE` в `.env` не задан ⇒ фактически «pc»; но `pc_orchestrator.py:98 LANE = os.getenv("PC_LANE","pc")` | док прав по факту, неверен по механике (G11) |
| V8 | `dec_port_spec.md:69` | `STEP_SELFHEAL=1` — самопочинка шага включена | **ключа нет** в `.env`, нет в User/Machine-переменных, нет ни в одном лаунчере (`*.vbs/*.xml/*.bat/*.cmd/*.ps1`). Читатель один: `pc_orchestrator.py:2289 return (os.environ.get("STEP_SELFHEAL") or "").strip() == "1"` ⇒ **ВЫКЛЮЧЕНО**. Живой лог: 55 строк со словом `selfheal` — **все до одной `selfheals=0`** внутри METRICS | **РАСХОЖДЕНИЕ, ранг A** |
| V9 | `dec_port_spec.md:79` | `PLAN_ADAPT=1` — адаптация плана включена | то же: ключа нет нигде; `pc_orchestrator.py:2565` ⇒ **ВЫКЛЮЧЕНО**. Живой лог `pc_orchestrator.log` (1986 строк): `коррекц` — 0, `adapt` — 0, `♻️` — 0, `🩹` — 0 | **РАСХОЖДЕНИЕ, ранг A.** Заодно **снимает вердикт G4 прохода 1**: аудит писал «думатель адаптации зовётся» — он не зовётся вовсе |
| V10 | артефакт `…rc-token-staleness-prevention` | детектор авторизации RC | `RC_AUTH_WATCH` не задан ⇒ дефолт `"1"` ⇒ `rc_auth_detect.ENABLED = True` | СОВПАДАЕТ (F2 подтверждён окружением) |
| V11 | `KB_PULSE.md:25-26` | «обращение к файлу секретов → **deny** (жёсткий блок цел)» | `.claude/settings.json:49-54` — `.env` в **`ask`**; список `deny` (`:100+`) секретов не содержит вовсе; сверх того `pretool_guard.py:1743` снимает `ask` по одобренному классу | **РАСХОЖДЕНИЕ (полосы).** Жёсткого блока секретов на ПК не существует |
| V12 | `ENV_PLAYBOOK.md:4` | «файл живёт в ОБОИХ репозиториях **одинаковым**» | sha256 совпал точно | СОВПАДАЕТ — **но** `dec_port_spec.md` в тех же двух репозиториях разошёлся (§0) |
| V13 | память/класс «CLAUDE_BIN стухшая версия» | пин `.env` на версионный каталог ⇒ «claude CLI не найден» | пин задан на версию **2.1.197**, путь **мёртв** (`Test-Path` = False). Но `_resolve_claude_once` (`pc_orchestrator.py:808`) идёт **PATH-first** (`shutil.which`), а версионный пин уступает более новой версии (`:815`) | **класс ЗАКРЫТ кодом**: живой `claude` берётся из `C:\Users\mxfill1\.local\bin`. Задачи 66/70/71 сегодня отработали на `claude-opus-5` |
| V14 | — | — | **наблюдение прогона**: `Read(D:\turbobaby-bot\.env)` заблокирован слоем `permissions.ask`; чтение того же файла через PowerShell **прошло** — гард знает про одобренный владельцем класс (`pretool_guard.py:1743`), а `settings.json` про него не знает | два слоя разрешений разошлись на одном классе и одном одобрении |

**V8/V9 — самое дорогое в этом прогоне.** Два механизма, которым посвящены сотни строк кода
(`_maybe_selfheal`, `_loc_after_done`, `ADAPT_PREAMBLE`, `PLAN_ADAPT_MAX`, 🩹/♻️/🧭-карты) и
два абзаца спеки, **на этой машине не включены ни разу**. Проверено тремя независимыми
способами: ключа нет в `.env`, ключа нет в переменных среды пользователя и машины, в живом
логе нет ни одного следа срабатывания. Это ровно сегодняшний класс, только наоборот: в
случае с реапером доке верили про «не трогает», здесь верят коду про «чинит».

---

## §2. Ошибки самого аудита №1, ранжированные по вреду

46 его утверждений (A1–A8, C1–C4, D1–D6, E1–E12, F1–F5, G1–G11) перепроверены адверсариально.
Итог: **34 ПОДТВЕРЖДЕНО, 12 ЧАСТИЧНО/ОПРОВЕРГНУТО.** Ниже — 19 найденных неточностей;
**12 из них меняют вердикт или вывод**, 7 — гниль собственных адресов.

### Ранг 1 — на этом строится неверный диагноз

| # | где в аудите | что он утверждает | что на самом деле | цена |
|---|---|---|---|---|
| X1 | §4.1 целиком | 14 сущностей «не существуют» (grep → 0) | существуют в `D:\foreign\turbobaby-manager-bot`, значения совпадают с докой (§0) | выбросят живую спеку соседней полосы как «описание несуществующего» |
| X2 | A5 | «`process_approval_timeouts` … исключение ровно одно» | **пропущен второй авто-килл по тому же порогу**: `process_approved` (`pc_orchestrator.py:1932-1938`) рубит `approved`-задачи старше `APPROVAL_TTL` → `failed` | «задачу одобрили, значит она защищена» — нет: одобренная тоже умирает через 30 минут |
| X3 | G6 | `dec_port_spec.md:95` «единый инбокс 1160 для ОБЕИХ полос» → РАСХОЖДЕНИЕ | док содержит **условие**: «задан `INBOX_TOPIC_ID` → единый инбокс; **не задан/0 → vps→328, pc→829**». Дефолт 829 — вторая ветка условия, а не расхождение. Аудит процитировал условие как абсолют | обвинили верный док; настоящее расхождение — в `revizor_recon.md:214-215` («в проде 1160»), и оно доказано только сейчас (V6) |
| X4 | G5 | «ищут защиту, которой нет» (док учит про `_DEC_WAIT_STATUSES`) | `dec_port_spec.md:48` прямым текстом: «у `pc_orchestrator` НЕТ guard'а последовательности». Строки 45/96 описывают `process_new` **VPS-демона** | аудит применил к себе класс 2 из собственного §6 — прочитал VPS-абзац как ПК-абзац |
| X5 | A7 | «ждут авто-halt цепи от реапера — его нет» | halt **есть**, его делает `process_local_chains()` строкой ниже в том же `poll_once` (`:2063` → `_loc_chain_tick:3404` → `_loc_after_fail:3202` → `_loc_post_summary`). Отсутствует только каскад «⏭ пропущен» | «цепь после реапа не остановится» — остановится |
| X6 | A8 | «верхняя граница живого прогона = **ровно** 5400 = `PC_SINGLE_STALE`, запас нулевой» | граница **строго больше** 5400: между `claim_task` и `complete_task` кроме двух попыток по 2700 идут `git rev-parse`, `_maybe_selfheal` (ещё один `claude -p`), `maybe_update_bots`. Плюс порог нестрогий: `:2040 if age <= PC_SINGLE_STALE: continue` | аудит **преуменьшил**: комментарий кода `:104-107` «живой прогон под нож не попадёт» ложен по построению, а не «шире факта» |
| X7 | A1 | реапер «закрывает застрявшие **одиночки**» | ни `process_stuck_singles`, ни `_lane_ok` не отличают одиночку от шага цепи: фильтра по `from` нет. Аудит повторил ошибку докстринга `:2020` | «шаг цепи реапер не тронет» — тронет |
| X8 | C4 | «спул наполняется **только** в `except` (`:266`)» | второй `spool_add` — на `:226` (ветка «нет `BRIDGE_URL`/`BRIDGE_TOKEN`», срабатывает ДО любого HTTP). И «~122с» — это **пол**, а не потолок: `urlopen(timeout=)` — таймаут сокет-операции, а `RETRY_TRIES`/`RETRY_PAUSE_SEC` переопределяемы из env | вывод «внешний timeout 30 теряет строку» верен, но только для **ручного** вызова: `dispatch_notify._cowork` (`:220-232`) запускает запись DETACHED, внешний таймаут к ней неприменим |
| X9 | D6 | «имя модуля не спасает от веток `.env`→ask и `edit_claude`, стоящих раньше» | живой прогон `decide()`: `python cowork_log_append.py "DONE RC: pravil .env…"` → `('defer','','')`; `"…popravil .claude/settings.json"` → `('defer','','')`. Причина — `_scan_text` (`:1115`) вырезает позиционные аргументы `.py`-скриптов как ДАННЫЕ. Сработала только ветка `outside` (она намеренно смотрит сырую `cmd`). Пропущен цикл `_RED_CMD` (`:1429-1458`), стоящий на 47 строк раньше | утверждение `CLAUDE.md:96-97` «содержимое не сканируется» верно **сильнее**, чем аудит допускает |
| X10 | F4 | «`PT10M` в трекаемом XML нет; страховки нет» | `PT10M` **есть** — `pc_remote_control.task.xml:48` (`IdleSettings/Duration`); греп-ловушка. Отличий hardened.xml названо 3 из 7. Главное: трекаемый XML — **установочный шаблон** с URI `\pc_remote_control`, а живая задача установлена как `\TurboBabyRC` ⇒ «в репо не применено» вообще ничего не говорит о живой задаче. Рядом лежал `2026-07-24-TurboBabyRC.before.xml` — дамп живой задачи, аудит его не использовал | вывод про Планировщик построен не на том файле |
| X11 | E5 | «на этой ветке LLM цифр не видит» → РАСХОЖДЕНИЕ | вырезание `<<<QUOTE>>>` работает только в marker-режиме. Числа в промпте остаются на трёх путях: percent-вопрос (`suggest.py:4344`), `kind != "ok"`, **мульти-модель** (`:4373`, `:4381`). Плюс `CRITICAL_FACTS:280` («NMAX155 449/3000») уходит в **каждый** промпт безусловно | док `notes/guard-quote-price.md:48-49` прав чаще, чем сказано; правку понесут не туда |
| X12 | E7 | «шаг „Бронируем?“ невидим по доке» | опровергается дословно: `sales-method-2026-07-22.md:41` «5. **Закрытие**: „Бронируем ?“…», `:74` «код дописывает ровно один вопрос». Неполна одна сводная строка `:59`, а не документ. И стрелки `dates → dates_fix → model` подменяют **ветвление** последовательностью | обвинили верный док |

### Ранг 2 — вывод неточен, диагноз не разрушает

| # | где | поправка |
|---|---|---|
| X13 | E9 | «детерминированной страховки нет» опровергается `drop_answered_questions` (`suggest.py:5752`, вызовы `:6218`, `:6274`) — он режет вопросы детерминированно и стоит строго перед `ensure_closing_question` |
| X14 | E10 | «ложный диагноз» не воспроизводится: в таблице `intake` (`moderation_ipc.py:117-120`) нет ни транскрипта, ни черновика — искать там окно бессмысленно; `revizor_recon.md:14` как «источник правды по окнам» по-прежнему прав |
| X15 | §4.2 | `guard_quote_price`/`_PC_PERIOD`/`quote_price_numbers` поданы как «сущности, которых нет», и это вменено доку — а `notes/guard-quote-price.md:116-118` **сам сообщает**, что они удалены. Обвинение доку в том, что он документирует |
| X16 | D2 | правило приписано `card_or_journal()`, живёт в `card_gate()` (`pretool_guard.py:1902`); `card_or_journal` — обёртка с fail-safe |
| X17 | F1 | атрибуция фикса: `2e0c214` (24.07) дал только серверный порог и страйки; `NAMED_LIVENESS_MAX_AGE=43200` пришёл `a773df7` **30.07 20:53** — за 1 ч 51 мин до самого аудита |
| X18 | F3 | «взят вариант 3» неточно: артефакт предлагал «до суток» (86400), в коде 43200 (12 ч), обоснованные заново |

### Ранг 3 — аудит о гнили адресов сгнил адресами

| # | заявлено | сегодня |
|---|---|---|
| X19 | `heartbeat_timeout` на `:511` | `:510` (`:511` — `FAIL_RUN_TIMEOUT`) |
| | `cowork_log_append.py:65` | `:64` (`:65` — `return json.loads(...)`) |
| | `pretool_guard.py:1082-1085` (APPROVED_KINDS_ENV) | присваивание на `:1083` |
| | `suggest.py:2790` как живой литерал | это **комментарий**; живых литерала два — `:2795`, `:2978` |
| | `suggest.py:1112` (`who`) | `:1114` |
| | `suggest.py:6286` как определение `strip_greeting_for_window` | это **вызов**; определение `:1262` |
| | `pc_orchestrator.py:5285`/`:5086` (докстринги «инбокс 1160») | `:5290`/`:5091` — аудит мерил **HEAD**, а рабочее дерево уже несло незакоммиченный дифф (+5 строк). Он объявил опорой «`pc_orchestrator.py` (5958)» = HEAD, при живых 5963, и посвятил §3 гнили адресов |

---

## §3. Новые расхождения: документы, которых проход 1 коснулся бегло

Отдельный проход по недоохваченным докам. **96 утверждений: 34 совпадают, 61 расхождение,
1 непроверяемо с ПК.** Ни одно не пересекается со строками аудита №1.

| документ | утверждений | совпало | расхождений |
|---|---|---|---|
| `docs/KB_PULSE.md` | 22 | 12 | 9 (+1 непроверяемо) |
| `docs/revizor_checklist.md` | 10 | 6 | 4 |
| `docs/ENV_PLAYBOOK.md` | 13 | 7 | 6 |
| `README.md` | 4 | 0 | 4 |
| `docs/notes/greet-guard.md` | 13 | 2 | 11 |
| `docs/notes/guard-quote-price.md` | 14 | 2 | 12 |
| `docs/task_notes/revizor-2026-07-13-zh.md` | 20 | 5 | 15 |
| **ИТОГО** | **96** | **34** | **61** |

Из 61 расхождения **≈31 — чистая гниль адресов** (содержание верно, `file:line` мимо) и
**30 — по существу**.

### 3.1. `docs/KB_PULSE.md` — самое дорогое

| # | док:строка | что написано | что в коде | вердикт | ложный диагноз |
|---|---|---|---|---|---|
| K2 | весь файл | подан как «журнал вех ПК-контура» | `grep KB_PULSE *.py` по ПК → **0**. Писателя нет: файл ведётся руками | НЕТ КОДА | «пульс не обновлялся ⇒ автоматика пульса сломалась». Её на ПК никогда не было |
| K7 | `:18` | «Файл 907→930 строк» | `foreign/pretool_guard.py` = **1201** | УСТАРЕЛО | «размер сходится ⇒ смотрю описанную версию» |
| K9 | `:20-23` | «`entity` — недописанная фича» | VPS: 13 попаданий `_ENTITY_*`; ПК: **0** | УСТАРЕЛО + зеркальная дыра | «фича не дописана» — дописана, но на одной полосе (свод п.9) |
| **K10** | `:25-26` | «обращение к файлу секретов → **deny** (жёсткий блок цел)» | ПК: слова `deny` в `pretool_guard.py` — **0**; `decide()` возвращает только `defer`/`ask`; `.env` в `ask` (`settings.json:50-54`); `:1743` снимает `ask` по одобренному классу | **РАСХОЖДЕНИЕ (полосы)** | «секреты на ПК жёстко заблокированы» — жёсткого блока не существует вовсе |
| K14 | `:38` | «полный гейт **1754 OK**» | `def test_` в `test_*.py` = **2349** | УСТАРЕЛО | второй якорь зелени после `guard-model.md:81` (там 1754) |
| K19 | `:45,47` | демон PID 1764, модербот PID 4724 | снимки 14.07 | УСТАРЕЛО | сверяют живой PID со снимком двухнедельной давности |
| K21 | `:54` | «гейт против боевого .env: 531/531» | один `test_pc_orchestrator.py` несёт сегодня **549** тестов | УСТАРЕЛО | «531 прошло ⇒ гейт полный» — сегодня это меньше одного файла |
| K22 | весь файл | «пульс» | последняя запись `## 2026-07-24`; после неё в `pc_orchestrator+suggest+pretool_guard` — **36** коммитов | ПРОБЕЛ | «вех не было» — их было 36 |
| K5 | `:12-14` | `_strip_script_cli_args` ~строки 713/715 | `foreign/pretool_guard.py:723`, возвраты `:964`, `:966` | АДРЕС (+250) | — |
| K1,K3,K4,K6,K11–K13,K15–K18,K20 | — | канал SSH, хук на каждый вызов, маркерный `[QUOTE]`-режим, дедуп #365 снят, LLM-маршрут уроков, env-гвозди в тестах | — | СОВПАДАЕТ (12) | — |

### 3.2. `docs/revizor_checklist.md` — «единственный актуальный» проверен адверсариально

Побайтовая сверка тела файла с `REVIZOR_CHECKLIST_DEFAULT` (без импорта модуля,
`ast.literal_eval` по исходнику): `EQUAL_EXACT=False`, `EQUAL_WS_NORM=True`, разница —
**ровно две пустые строки**. Аудит №1 здесь прав. Но выводы другие:

| # | док:строка | что написано | что в коде | вердикт |
|---|---|---|---|---|
| R5 | `:14` | «Держи дефолт синхронным с телом файла» | инвариант гейта (`test_pc_orchestrator.py:4504-4507`) сравнивает **через `_norm`** (`re.sub(r"\s+"," ",s)`) ⇒ абзацная разметка не защищена: живая преамбула 11 строк, fail-safe 9 | РАСХОЖДЕНИЕ (мягкое) |
| **R6** | `:9-11` | новый класс = дописать ОДНУ строку, он попадёт в преамбулу **без правки кода** | автописатель `lesson_router.py:476 append_checklist_class` пишет **только .md** (`:506`), `REVIZOR_CHECKLIST_DEFAULT` не трогает ⇒ гейт-тест `test_shipped_file_matches_builtin_default` **краснеет** на следующем прогоне | **РАСХОЖДЕНИЕ.** «Гейт упал — сломали ревизора»; на деле сработал штатный «урок навсегда», и правка кода всё-таки обязательна |
| **R7** | `:10` | «буква X попадёт в поле `"class"` находки» | контракт вывода в том же промпте диктует `pc_orchestrator.py:4768 '"class":"а".."ж"'` + `:4770` «Не выдумывай находок сверх чек-листа»; `lesson_router.py:454 _next_class_letter` выдаст «з» | **РАСХОЖДЕНИЕ (промпт против промпта).** «Дописали класс з, ревизор его не находит ⇒ файл не читается» — читается, буква запрещена соседним абзацем |
| R9 | шапка | файл описан только как ЧИТАЕМЫЙ кодом | он ещё и **пишется** кодом (`lesson_router.py:506`) и коммитится (`pc_orchestrator.py:1411`) | ПРОБЕЛ: неожиданный diff читают как чужую ручную правку |
| R1–R4, R8, R10 | — | чтение на каждом тике, срез HTML-комментариев, вклейка, fail-safe, «КРАСНОЕ не касается», адреса `:18`/`:27` | — | СОВПАДАЕТ (6) |

**Вывод:** «полностью актуален» верно про **текст** и неверно про **утверждения о поведении
кода** — центральное обещание («класс без правки кода») роняет гейт и конфликтует с
контрактом вывода.

### 3.3. `docs/ENV_PLAYBOOK.md` — 13 утверждений (7 совпало / 6 расхождений)

| # | док:строка | что написано | что в коде | вердикт |
|---|---|---|---|---|
| **P2** | `:16`, `:112` | тип записи `ASK` (`ASK RC <дата> …`) | ПК: `cowork_log_append.py:162` — `ASK` есть. **VPS: `foreign/cclog.py:80 TYPES = ("DONE","PLAN","NOTE","BLOCKED","WAITING","SKIPPED")` — `ASK` НЕТ**; `:100` не опознаёт строку как ретрансляцию | **РАСХОЖДЕНИЕ (полосы).** «ASK лёг в общую память обеих полос» — на серверной ляжет мимо типа; поиск развилок по `ASK` даст ноль |
| **P5** | `:92` | слой 2 сужен до имён операций: `add_transaction`, `delete_event`, `create_booking`, `gspread` | ПК-слой 2 (`pc_orchestrator.py:2593-2595 _HEADLESS_IMPOSSIBLE_RE`) из четырёх содержит **только `delete_event`**; остальные три живут в ГАРДЕ (`pretool_guard.py:728-735`); рядом на `:2595` по-прежнему ТЕМЫ `деньг\|касс\|байки\|календар\|crm` | **РАСХОЖДЕНИЕ.** Поимённое уточнение D7 прохода 1: грепают `add_transaction` в оркестраторе, не находят, решают «фикс приземлён» — он в другом модуле |
| **P9** | `:106` | «явный запрет владельца («read-only», «не коммитить») **бьёт всё остальное**» | в `pretool_guard.py` такого детекта нет (`grep` → 0); вердикт зависит только от `_stays_red` | **НЕТ КОДА.** «Написал в ТЗ read-only ⇒ гард не даст записать» — слово в тексте задания на вердикт гарда не влияет ВООБЩЕ. Тот же класс, что «`ultrathink` меняет усилие» из этого же свода (`:35-38`) |
| P11 | `:149-152` | «страховка: мок фолбэка не должен содержать основную модель» | в ПК-тестах **0**; `modelUsage` в ПК-коде 0 | НЕТ КОДА (полоса VPS): у ПК-исполнителя ветки фолбэка нет вовсе |
| P12 | `:167-169` | таблица: ПК проверял окружение и способ запуска, **не имя входного файла** | `log_setup.py:73 if argv0.startswith("test_"): return True` — имя проверяется | УСТАРЕЛО (фикс на месте, таблица в настоящем времени) |
| P13 | `:21-22` | финальный `DONE` **обязан** ссылаться на артефакт путём | в `cowork_log_append.py` валидации нет | НЕТ КОДА: правило есть, проверяющего нет, нарушение молчаливо |
| P1,P3,P4,P6,P7,P8,P10 | — | одинаковость файла в двух репо (sha сверен), `cclog --pulse`, `EFFORT_LEVELS`, отсутствие слов «гейт/тесты/коммит» в признаках, вырезание данных без подстановок, кавычки в сегментах, `fixtures/*.live.json` | — | СОВПАДАЕТ (7) |

### 3.4. `README.md` — 4 утверждения, 0 совпадений

| # | что | факт | вердикт |
|---|---|---|---|
| M1 | пять HTML-комментариев «smoke pc-полоса 2026-07-04/05» | живой смоук — `pc_orchestrator.py:2713 _exec_smoke_step` + `run_chain_smoke.py`; строки README к нему отношения не имеют | УСТАРЕЛО |
| M2 | «Проект userbot/оркестратора turbobaby (PC-сторона)» | не названы: `suggest.py` (532 КБ), `moderation_bot.py`, `rc_supervisor.py`, `pretool_guard.py`, `trainer_run.py`, `lesson_router.py` | НЕПОЛНО: новый агент лечит клиентский баг как «баг оркестратора» |
| M3 | — | после последней правки (05.07) — **165** коммитов в профильный код | ПРОБЕЛ |
| M4 | — | не назван способ запуска (`pc_remote_control.task.xml`, `rc_remote_control.vbs`, `venv/Scripts/python.exe`), не назван гейт | ПРОБЕЛ: стартуют не тем интерпретатором |

### 3.5. `docs/notes/greet-guard.md` (13: 2 совпало / 11 расхождений)

| # | док:строка | что написано | что в коде | вердикт |
|---|---|---|---|---|
| **N2** | `:16` | `first = first_contact_from(...) and not greeting_already_sent(...)` на `:3124` | `suggest.py:6931 is_first_bot_reply = …` — **переменная переименована** | РАСХОЖДЕНИЕ + адрес: правку «`first`» кладут мимо, грепу не за что зацепиться |
| **N12** | `:74-81` | основная точка вставки — расширить `greeting_already_sent` | взята **альтернатива**: `autogreeting_already_sent` (`:1234`, вызов `:6932`); `greeting_already_sent` (`:1152`) по-прежнему матчит только `_GREETING_RE` | РАСХОЖДЕНИЕ: «расширяли `greeting_already_sent` ⇒ все её вызовы защищены» — защищён только путь, где рядом стоит `autogreeting_already_sent` |
| N11 | `:59-60` | «дубль дефолта в `pc_orchestrator.py:2807+`» | дефолт — `:4714`; на `:2807` код декомпозера | адрес в осмысленный чужой код |
| N1,N3,N5–N9,N13 | — | `on_client_message` 3113→**6899**, `first_contact_from` 675→**1131**, `greeting_already_sent` 690→**1152**, `make_system_prompt` 2147→**4426**, `transcript_from` 651→**1107**, `_REVIZOR_AUTOGREETING_RES` 3003→**4915** | содержание верно | СОВПАДАЕТ / АДРЕС СГНИЛ (+1900…+3800) |
| N4, N10 | — | `FIRST_CONTACT_HOURS` (=18, env), ссылки на `revizor_checklist.md:18,27` | — | СОВПАДАЕТ (2) |

### 3.6. `docs/notes/guard-quote-price.md` (14: 2 совпало / 12 расхождений, 10 — адреса)

| # | док:строка | что | факт | вердикт |
|---|---|---|---|---|
| C12 | `:71-78` | `_PC_PRICE_TOTAL_RE`, `_PC_PERIOD`, `_PC_DEPOSIT_RE` как ориентиры | удалены; замена — `extract_money_figures` (`suggest.py:5203`), `_MF_PERIOD` (`:5190`) | УСТАРЕЛО (док сам фиксирует это на `:114`) |
| C14 | `:121-125` | хвостовой раздел «Файлы» перечисляет `_PC_PRICE_TOTAL_RE` как живой | удалён | РАСХОЖДЕНИЕ дока **с самим собой** через 7 строк |
| C11 | `:62-65` | `CRITICAL_FACTS:142`, «NMAX155 449/3000», в промпт ВСЕГДА | `suggest.py:275`, `:280` | СОВПАДАЕТ / адрес (+133) — это и есть остаточный источник 449 (X11) |
| C13 | `:104-119` | РЕШЕНО: `extract_money_figures` — одна точка правды, `has_cur`, репро в `TestDraftPostcheck` | `:5203`, `:5219`, `:5312`, `test_suggest.py:2772` | **СОВПАДАЕТ полностью** |
| C1–C10 | — | `quote`/`_normalize` (9 полей), `quote_for_model` (4 статуса), `sanity_days_ok`, `build_pricing_note`, `_resolve_model_price`, `_client_price`, `_wrap_single`, `make_system_prompt`, `generate_draft` | содержание верно | СОВПАДАЕТ / АДРЕС СГНИЛ (+8…+3400) |

### 3.7. `docs/task_notes/revizor-2026-07-13-zh.md` (20: 5 совпало / 15 расхождений, 11 — адреса)

| # | док:строка | что | факт | вердикт |
|---|---|---|---|---|
| **Z8** | `:36-38` | класс «ж» на `pc_orchestrator.py:2583`, «клиент дал модель И даты, а **мы** шлём анкету» | живая формулировка `:4735-4738`: «…ПЕРВЫМ **СОДЕРЖАТЕЛЬНЫМ** сообщением…, **БОТ** в ОТВЕТ…, **НЕ путай с автоприветствием — находку НЕ заводи (action=noise)**». На `:2583` — парсер вердикта плана | РАСХОЖДЕНИЕ: разбирают «почему ревизор не завёл класс ж» по снятой формулировке |
| **Z19** | `:105-106` | `_detect_model` = `suggest.py:935` → сегодня `:2104` | на `:29` **того же документа** `:2104` отдан тексту приветствия | один номер строки в одном доке указывает на два разных объекта |
| Z6 | `:29` | приветствие — `suggest.py:2104` | там `def _detect_model`; живой текст — `:4433` | адрес в осмысленный чужой код |
| Z2 | `:14` | каталог для клиента — `suggest.py:224` | живой текст `:436`; на `:224` — конец опознания байка, `:228 MODERBOT_TOKEN` | адрес ведёт в блок токена |
| Z5 | `:26-27` | `first = …` на `:3073` | `:6931`, имя другое | тот же дефект, что N2, но с **другим** адресом: два дока шлют в две разные несуществующие точки |
| Z1, Z12, Z15, Z20 + 11 адресных | — | «строки анкеты в коде нет» (устояло), `_candidates`, требования `PRICING_ACTION`/`BRIDGE_*`, `topics_map.json:14`, `KNOWN_MODELS` (20 пар, состав и порядок), `_bike_key`, `fleet`/`FLEET_TTL=300` | содержание верно | СОВПАДАЕТ / АДРЕС |

### 3.8. §5 аудита №1 перепроверен грепом

`rc_supervisor|session_watch|selfupdate_gate|gate_selective|client_contour|diag_status_truth|reviewer|scout`
по `docs/*.md` + `docs/notes/*.md` + `docs/task_notes/*.md` + `CLAUDE.md` + `README.md` →
**0 попаданий, ни одного ложного срабатывания.** Утверждение «8 живых модулей не упомянуты ни
в одном нормативном доке» **подтверждено**. Добавка: `docs/task_notes/` в грепе аудита не был
назван — там тоже 0.

---

## §4. Устаревшие упоминания сред, которых у нас нет — поправка к §4 аудита №1

Главное — в §0: 14 «несуществующих» сущностей существуют в `D:\foreign\turbobaby-manager-bot`.
Ниже — то, чего нет **нигде**, и что аудит №1 не назвал.

| сущность | где упомянута | факт |
|---|---|---|
| **дефолтная голова `fable` / `claude-fable-5`** | `foreign/orchestrator_daemon.py:173-174`, док `dec_port_spec.md:71` | **снятая 30.07 голова жива как дефолт в коде соседней полосы, на этой же машине.** Третья мина того же класса — аудит назвал только `.claude/settings.json.new` и `.bak-2026-07-23` |
| `ASK` как тип журнальной записи на серверной полосе | `ENV_PLAYBOOK.md:16`, `:112` | `foreign/cclog.py:80 TYPES` его не содержит: половина ритуала свода на VPS не опознаётся |
| `/tmp/guard_backup_scanedit_2407` (sha256 `6a815f5f`) | `KB_PULSE.md:19` | путь на VPS-машине; ни в ПК-дереве, ни в `foreign/` не существует |
| `root@5.223.94.179` + `~/.ssh/turbobaby_vps` | `KB_PULSE.md:9`, закреплено в `.claude/settings.json:13-14` | среда прибита к allow-листу, грепу по коду не видна |
| `stash@{1}` (vps-local-24.07) — источник обеих правок гарда | `KB_PULSE.md:11` | stash чужой машины; воспроизвести нечем |
| `modelUsage`-моки, страховка «мок фолбэка не содержит основную модель» | `ENV_PLAYBOOK.md:149-152` | 0 в ПК-тестах |
| PID 1764 (демон), PID 4724 (модербот) | `KB_PULSE.md:45,47` | снимки 14.07 |
| «полный гейт 1754 OK» как якорь зелени | `KB_PULSE.md:38` | второй экземпляр после `guard-model.md:81`; сегодня `def test_` = 2349 |
| `orchestrator_daemon.log` | `ENV_PLAYBOOK.md:190` | нет **и** в `foreign/` (`ls foreign/*.log` → пусто): журнал живёт только на сервере |
| `_PC_PRICE_TOTAL_RE` в разделе «Файлы» | `notes/guard-quote-price.md:125` | удалён — но это не «док документирует удалённое», а **несогласованный хвост** того же дока |

---

## §5. Ранжирование по вреду: на чём легче всего построить неверный диагноз

Ровно как сегодня с реапером — то есть где документ звучит как абсолют, читается на этой
машине и опровергается живым фактом.

1. **V8/V9 — `STEP_SELFHEAL` и `PLAN_ADAPT` выключены.** Спека обещает `=1`, код полон
   механики, живой лог показывает ноль срабатываний за всю историю. Вопрос «почему думатель
   не перезапустил упавший шаг / не скорректировал план» уведёт в код; причина — отсутствующий
   ключ. Зеркало сегодняшнего случая: там верили доке про «не трогает», здесь верят коду
   про «чинит».
2. **§0 — `dec_port_spec.md` не ложен, а не помечен полосой.** Соседний репозиторий лежит на
   этом же диске, его числа сходятся с докой до единицы. Вывод «дока описывает
   несуществующее» стоит живой спеки. Плюс две копии дока разошлись на 11 дней, и ни одна
   не помечена как копия.
3. **X2 — второй авто-килл по `APPROVAL_TTL`.** «Одобрено ⇒ защищено» неверно:
   `process_approved` рубит одобренную задачу через 30 минут. Аудит №1 нашёл только один из
   двух путей, `dec_port_spec.md:90` — ни одного.
4. **V6 — красные карточки уходят в 829, а не в 1160.** Доказано отсутствием `PC_NA_TOPIC`
   в `.env`. Докстринги кода (`:5091`, `:5290`) и `revizor_recon.md:214-215` обещают 1160.
   «Карточка не пришла» будут искать не в той теме.
5. **K10 + V11/V14 — «жёсткого блока секретов» на ПК нет.** Он `ask`, снимается одобренным
   классом, и в этом самом прогоне один и тот же файл при одном и том же одобрении оказался
   закрыт для `Read` и открыт для PowerShell. Диагноз «секреты защищены слоем настроек»
   неверен: слои разошлись.
6. **R6/R7 — «урок навсегда без правки кода».** Обещание чек-листа роняет гейт
   (`test_shipped_file_matches_builtin_default`) и конфликтует с зашитым контрактом
   `"class":"а".."ж"`. Красный гейт прочитают как «сломали ревизора».
7. **P2 — `ASK` не существует на серверной полосе.** Поиск развилок по типу даст ноль на
   половине памяти.
8. **X7 — реапер не отличает одиночку от шага цепи.** Докстринг обещает «ТОЛЬКО одиночки»;
   фильтра по `from` нет. Ошибку повторили и код, и аудит.

---

## §6. Счёт

Числа посчитаны по строкам таблиц ЭТОГО файла.

| | |
|---|---|
| утверждений аудита №1 перепроверено адверсариально | **46** (A1–A8, C1–C4, D1–D6, E1–E12, F1–F5, G1–G11) |
| из них подтверждено дословно | **34** |
| из них ЧАСТИЧНО / ОПРОВЕРГНУТО | **12** |
| найдено неточностей в самом аудите №1 | **19** (§2): 12 меняют вердикт или вывод, 7 — гниль собственных адресов |
| новых утверждений сверено (доки, пройденные бегло) | **96** (§3) |
| из них расхождений | **61** (30 по существу + 31 гниль адресов) |
| собственных проверок живого окружения (`.env`, переменные среды, логи, два репозитория) | **14** (§1): 7 совпадений, 6 расхождений, 1 наблюдение |
| **ВСЕГО проверяемых утверждений за прогон** | **156** |
| **ВСЕГО расхождений** | **86** = 19 (ошибки аудита) + 61 (новые) + 6 (по `.env`) |
| механизмов, документированных как включённые, но выключенных в живом конфиге | **2** — `STEP_SELFHEAL`, `PLAN_ADAPT` |
| копий одного дока, молча разошедшихся между репозиториями | **1** — `dec_port_spec.md` (99 строк 12.07 против 93 строк 23.07) |
| утверждений аудита №1, которые обвиняли верный документ | **3** — G6, E7, §4.2 (`guard-quote-price`) |

**Метод-урок прогона.** Аудит №1 ошибся тремя способами, и все три — его же классы из §6,
применённые к себе: прочитал VPS-абзац как ПК-абзац (X4), процитировал условие как абсолют
(X3), сгнил собственными адресами, потому что мерил HEAD при живом незакоммиченном диффе
(X19). Документ о том, что документы врут, соврал ровно теми же способами. Отсюда единственный
надёжный приём, подтверждённый дважды за два прогона: **вердикт даёт живое действие**
(запуск `decide()` на точной строке, `Test-Path` на пине, `selfheals=0` в логе), а не чтение
кода рядом с чтением доки.

Правок по заданию не предлагаю.
