68h SRVART 2009

# Два серверных артефакта 20.09 прочитаны с ПК: 68b принят, 68c НЕ принят

**Полоса:** ПК · **Заход:** 68h из ящика заданий Штаба · **Дата:** 20.09.2026
**Тело задания:** 3563 символа без хвостового перевода строки (3564 с ним), 31 строка (п.0).
**Временное место ПК:** `tmp/vps_read_sept/` (тело задания для замера) и `tmp/pc_report/`
(черновик доклада, задан обёрткой) — ни в одном имени нет слов адреса `68h`, `SRVART`, `2009`
(п.0в). **Временное место на сервере: НЕ СОЗДАВАЛОСЬ ВОВСЕ** — ни одного файла, ни одного
каталога; всё чтение ушло в stdout ssh. **Не удалено НИЧЕГО.**

## FACT

* **FACT: read-only на сервере.** Ни одной пишущей команды: только `ls`, `cat`, `grep`, `sed`,
  `awk`, `wc`, `git log`/`rev-list`, `systemctl show`. Тестов не запускал, читателя парка не
  запускал, процессов не трогал.
* **FACT:** живой демон сервера `MainPID=2160442`, `ExecMainStartTimestamp=Sat 2026-09-19
  16:46:52 UTC`, `ActiveState=active` — этим заходом не тронут.
* **FACT:** оба артефакта прочитаны дословно: 21919 и 25642 байта.
* **FACT:** пишущая операция захода ровно одна и она на ПК — этот файл.

---

## 1. Премисы — все три ДА, с адресами

**П1. ДА.** Каталог `/root/turbobaby-manager-bot/docs/artifacts/`, оба файла за 20.09 на месте:

| файл | размер | mtime (UTC) |
|---|---|---|
| `2026-09-20-68b-FLEETREADER-2009.md` | **21 919 байт** | 2026-09-19 20:10 |
| `2026-09-20-68c-SIGNALA-TRI-2009.md` | **25 642 байта** | 2026-09-19 20:24 |

Первая строка каждого — ровно слова адреса (`68b FLEETREADER 2009`, `68c SIGNALA TRI 2009`).
Рядом лежит третий файл ночи — `2026-09-20-67x-TOKENSLOT-AB-2009.md`, 20 092 байта (ряд #114,
предметом 68h не является).

**П2. ДА — оба закрыты `done`, упавшим ни один.** Журнал
`/root/turbobaby-manager-bot/orchestrator_daemon.log` (3 461 114 байт), дословно:

```
20:13:48 COMPLETE id=115 status=done bridge_ok=True
20:28:40 COMPLETE id=116 status=done bridge_ok=True
```

| ряд | ключ | `claude -p` | `dur_s` | tokens_in / out | attempts / selfheals | outcome |
|---|---|---|---|---|---|---|
| **#115** | `68b-fleetreader.2009` | `exit=0` (вывод 2513 симв) | **1548.82** | 27 545 312 / 90 415 | 1 / 0 | `done` |
| **#116** | `68c-signala-tri.2009` | `exit=0` (вывод 1922 симв) | **799.74** | 10 630 670 / 46 415 | 1 / 0 | `done` |

**Оговорка, без которой П2 читается неверно: «закрыт не как упавший» ≠ «признан сделанным».**
У ОБОИХ теневой судья снял зелёное:
`ТЕНЬ id=115 настоящий=done теневой=неизвестно (разошлось, строже) — зелёное снято: адрес не
назван либо источник не прочитан [адрес: не назван]` — и та же строка по id=116. То есть
очередь считает их закрытыми, а судья — непрочитанными. Это ровно тот сорт (2), который
описывает сам 68c, и он касается его собственного ряда.

**П3. ДА — требовались обоим, и это проверено по телам заданий, а не по пересказу.** Тела
лежат на сервере в разведочных каталогах заходов (`_recon_parkread_sep/task_body.txt` — 3922
символа/30 строк; `_recon_vecher_ostanovka/body.txt` — 3725/30, оба совпали с числами, которые
заходы назвали у себя в шапке):

* 68b, п.4: «**ОТРИЦАТЕЛЬНЫЙ ТЕСТ ПЕРВЫЙ, про секрет**…»; п.5: «**ОТРИЦАТЕЛЬНЫЙ ТЕСТ ВТОРОЙ,
  про исход**… Показать ЧИСЛОМ». В артефакте есть оба, объявлены пройденными.
* 68c, п.4: «**ОТРИЦАТЕЛЬНЫЙ ТЕСТ, без него замка нет**: собрать пару “внешний отказ плюс
  внешний отказ” и показать ЧИСЛОМ, что сигнал А НЕ встаёт». В артефакте его **НЕТ** — §5
  озаглавлен «Пункты 4 и 5 — отрицательный тест и контрфакт НЕ собраны».

---

## 2. п.1 — 68b: парк числом и отрицательный тест про секрет

**Читатель видит 38 байков; исход НЕИЗВЕСТНО у 17 из них** (снимок `2026-09-19T20:04:04Z`).
Разбивка по байкам: просрочено **19**, НЕИЗВЕСТНО **17**, в норме **2**. По регистрам НЕИЗВЕСТНЫ
**55 клеток из 152**: пусто 46, не число 6, ноль 1, отрицательное число 1, интервал не задан 1.
Заход сверил порядок с переписью 09.08 (56 непрочитанных нулей из 152).

**Отрицательный тест про секрет — ПРОЙДЕН, и он поймал настоящую утечку, а не подтвердил
ожидание.** Наружу идёт только признак; значения я не печатаю и не читал. Голденов по этой
ветке пять, все физически есть на диске (`tests/test_park_read.py`, 32 251 байт, **41**
`def test_`): `test_no_secret_in_output_of_broken_run_subprocess` (стр. 280),
`test_scrub_hides_specific_value` (253), `test_scrub_hides_url_carried_by_transport_error`
(263), `test_url_path_segment_is_masked_too` (542), `test_secret_absent_from_healthy_output`
(310). Заведомо неверные креды, соединение на `127.0.0.1:9` — наружу из проверки ничего не
уходит.

Что тест поймал (цитата, одна строка): «Голден … показал утечку **токена целиком**. Источник не
мой: токен едет **GET-параметром** (`bridge_client.py:661`) … клиент печатает его САМ —
`bridge_client.py:531`». Вторая половина — после фильтра утекал уже **сегмент пути URL**,
отсюда `_variants()` (значение, процентная форма, путь и каждый сегмент ≥ 8 символов).
Заслонов стало три: `_emit` (stdout), `_ScrubFilter` на корневом обработчике (журнал),
`sys.excepthook` (трассировка).

**Класс шире читателя, и это число:** в боевом `splinter.log` строк с `token=` — **29**, из них
строк вида `Bridge request error … token=` — **10**. Не чинилось намеренно (общий транспорт
`splinter` и демона) → остаток владельцу, адреса названы.

---

## 3. п.2 — 68b: второй отрицательный тест (непрочитанный источник)

**ПРОЙДЕН, показан числом.** Живой парк, спрошенный БЕЗ разметки клеток (`--no-cells`):

| | с разметкой | без разметки |
|---|---|---|
| байков видно | 38 | 38 |
| просрочено | 19 | **0** |
| в норме | 2 | **0** |
| НЕИЗВЕСТНО | 17 | **38** |
| регистров «не прочитано» | 0 | **152** |
| регистров «пусто» | 46 | **0** |

Ни один байк не остался «в норме»: непрочитанный источник нормой не притворяется. Два разных
НЕИЗВЕСТНО разведены и не смешиваются — «пусто» 46 против «не прочитано» 152, разные корзины
`unknown_registers_by_why`. Отдельный голден на мёртвый мост —
`test_dead_bridge_is_unread_not_empty_park` (стр. 508), исход `Unread`, код возврата 4; цитата
причины: «Первая редакция читала ответ как `.get("bikes") or []` и отвечала `ok: true` с нулём
байков». Забор действий закрыт на обеих дверях после живого промаха — замок
`test_gate_closes_the_POST_door_too` (стр. 216). Регистрация потребителя разметки проверена
мной на диске: `FLEET_CELLS_CONSUMERS = frozenset({"splinter.py", "park_read.py"})`
(`tests/test_fleet_cell.py:297`), инвариант `PARK_VERDICT_PURE` зарегистрирован
(`invariants_check.py:1022`).

---

## 4. п.3 — 68c: три исхода и отрицательный тест в обе стороны

**Развёл ли три исхода: ОПИСАНИЕМ — да, КОДОМ — нет.** Сорта названы и у каждого назван ряд за
19.09: (1) «работа не сделана» — **за 19.09 ни одного**; (2) «сделана, судья не прочитал» —
**#114 и #115**; (3) «заход не начинался вовсе» — **#113**. Сорт (3) доказан числами, которые я
сверил с журналом сам: `dur_s=19.80`, `tokens_in=0 tokens_out=0`, `modelUsage пуст`,
`WARNING FAIL причина=exec_error id=113`, `COMPLETE id=113 status=failed`. Развилка названа
адресом: `status_truth.py:95` (совпадение английского текста в склейке stdout+stderr), причём
«код выхода в решении НЕ участвует».

**Кода заход не изменил ни строкой** — цитата из его §4: «код не изменён НИ СТРОКОЙ (единственные
новые файлы — этот артефакт и `_recon_vecher_ostanovka/body.txt`)». Заход объявил законную ветвь
цели: развести нельзя ОДНОЙ правкой, правок нужно **минимум три**, каждая на своей стороне —
(а) условие сигнала на полосе ПК, (б) доставка `5e78c36` перезапуском демона, (в) расширение
словаря терминалов моста (`completeTask_` принимает только `done|failed`, красная зона).

**Отрицательный тест в обе стороны: НЕ ПОКАЗАН НИ ОДНИМ ЧИСЛОМ.** Стендов 0, прогонов 0, пар
«внешний+внешний» 0, пар «провал+провал» 0. Цитата причины дословно: «Числа даёт условие;
условия здесь нет. Любой “стенд”, который я бы собрал, был бы стендом МОЕЙ реконструкции
условия». Причина названа честно и адресом, но пункт от этого не выполнен.

**Что в 68c проверено мной независимо и сошлось** (важно: диагноз захода держится, даже когда
его пункт не выполнен):

| утверждение 68c | моя проверка 20.09 | сошлось |
|---|---|---|
| рядов ящика за всю жизнь журнала 9, за 19.09 — 4 | `grep -c from=Filipp-shtab` = **9**; за 19.09 ровно **4** (#113–#116) | да |
| `grep -c "🚦"` = 0 (ветка «чужая сторона» в проде не срабатывала) | **0** | да |
| `git rev-list --count origin/main..HEAD` = 0 | **0** (HEAD теперь `6de90de`) | да |
| демон `PID=2160442`, старт 19.09 16:46:52 UTC | то же, `ActiveState=active` | да |
| фикс `limit_slot.py` записан ПОЗЖЕ старта демона | mtime **17:36:34** против старта **16:46:52** → разрыв **49 мин 42 с** | да |

---

## 5. п.4 — 68c: сколько подъёмов за семь суток были бы ложными

**3 из 3 = 100 %**, при **0 из 3** закрытых рядов, кормящих сигнал. Ряды окна 13.09–20.09 — **4**
(#113, #114, #115, #116), закрытых на момент письма **3**:

| ряд | исход в очереди | сорт по новому условию | кормит сигнал А? |
|---|---|---|---|
| #113 | `failed` (`exec_error`) | (3) заход не начинался — чужая сторона | нет |
| #114 | `done`, вердикта «сделано» нет | (2) судья не прочитал | нет |
| #115 | `done`, вердикта «сделано» нет | (2) судья не прочитал | нет |
| #116 | на момент письма шёл (закрылся `done` в 20:28:40) | (2) — тот же теневой «неизвестно» | нет |

**Слабое место этой доли названо самим заходом, и его надо передать Штабу вместе с числом:**
знаменатель 3 взят ИЗ ТЕКСТА ЗАДАНИЯ («встала ТРИ раза за вечер»), а не измерен — «отсюда
подъёмы не наблюдаются, наблюдаются только ряды, которые их кормили». То есть 100 % — это
«ни один ряд полосы VPS за окно не был сортом (1)», а не пересчёт самих подъёмов по леджеру
ящика. Леджер живёт на полосе ПК; 68h его проверки не просило, и я его не открывал.

Шире окна: за всю жизнь журнала ряды ящика падали **4 раза** — #145 (03.09 `exec_error`),
#194 (04.09 `run_timeout`), #172 (07.09 `run_timeout`), #113 (19.09 `exec_error`); сорт (1) у
трёх, сорт (3) у одного. Дальше семи суток пересчёт не распространён сознательно: «по коду
причины сорт (2) не отличить от сорта (1)».

---

## 6. п.5 — границы, которые заходы поставили сами, и остатки

**68b — граница (его §4 п.7 и §5):** не чинит общую утечку токена в журнал (решение владельца,
адреса `bridge_client.py:661` + `:531`); не пишет ничего и не умеет; брони и «Календарь
бронирования» недоступны по устройству забора (`ALLOWED_ACTIONS = {fleet, service_list,
read_doc}`); возраст регистра неизвестен по существу — у клеток Лист1 штампа времени нет вовсе;
«интервал не задан» свален в НЕИЗВЕСТНО (1 случай на парк, `park_verdict.WHY_NO_INTERVAL`);
полоса ПК не зеркалена; ветка `Unread` живым мёртвым мостом не проверена. Срез: HEAD `5e78c36`,
мост @84, живой `splinter.log`.
**Остаток назван:** утечка токена в транспорте НЕ закрыта (29 строк `token=`, из них 10 —
транспортные отказы); `CLAUDE.md` и база знаний не правились.
**Бюджет 68b:** единица одна, потолок 2700 с; заход назвал «около 35 минут». **Журнал говорит
`dur_s=1548.82` = 25.8 мин** — заход завысил свой расход на ~9 мин, то есть в безопасную
сторону; оценка сделана на глаз, а не по `METRICS`.

**68c — граница (его §7):** условие сигнала А, витрина и вычёркивание сигнала Д — полоса ПК,
отсюда непроверяемы иначе как отрицательно; ряд #92 — ряд леджера ящика, на VPS его нет; сами
подъёмы отсюда не наблюдаются; полоса ПК целиком не видна; доставка исправленного корня
запрещена ЗАПРЕТАМИ задания; корень сорта (2) — адрес результата приезжает прозой, а
`result_ref.parse` (`result_ref.py:139`) понимает только канон `[result_ref: <вид> <указатель>]`.
**Остаток назван вслух:** не сделаны п.4 (отрицательный тест), п.5 (контрфакт) и сам развод
(п.2); работа, остающаяся полосе VPS, ровно одна — доставить `5e78c36` перезапуском
`orchestrator-daemon`.
**Бюджет 68c:** единиц две, потолок 2700 с, порог 1890 с; заход назвал «~25 минут (≈1500 с)».
**Журнал говорит `dur_s=799.74` = 13.3 мин** — завышение на ~11.7 мин, тоже в безопасную
сторону. Вторая единица не расходовалась.

---

## 7. п.6 — принят или НЕ принят, без смягчения

### 68b FLEETREADER — **ПРИНЯТ**

Оба требовавшихся отрицательных теста есть, и второй показан ЧИСЛОМ (таблица §3: 17 → 38
НЕИЗВЕСТНО, 2 → 0 «в норме», 0 → 152 «не прочитано»). Первый пойман не рассуждением, а
прогоном: он **покраснел дважды** и оба раза на настоящей утечке (токен целиком, затем сегмент
пути URL) — это и есть доказательство, что тест работает, а не украшает. Продукт лежит в
`origin/main` (`3b8f4d8`), файлы на диске проверены поштучно, потребитель разметки
зарегистрирован, инвариант в гейте. Главное число задания дано: **38 байков, 17 НЕИЗВЕСТНО**.

Что Штаб обязан забрать вместе с приёмкой: **утечка токена в журнал через общий транспорт
открыта** (29/10 строк) — это решение владельца, а не остаток исполнителя.

Чего я НЕ перепроверял и что поэтому идёт со слов артефакта: «41 проверка, все зелёные» —
я проверил существование файла, число `def test_` (41) и семь имён голденов построчно, но
**тестов не запускал** (запрет на пробные скрипты); числа парка 38/19/17/2 — тоже со слов, так
как запуск читателя был бы прогоном.

### 68c SIGNALA TRI — **НЕ ПРИНЯТ**

Отрицательного теста числом нет: **0 стендов, 0 прогонов, 0 пар в обе стороны** — при том что
его собственное задание называет этот пункт «без него замка нет». По правилу 68h п.6 этого
достаточно, и смягчать нечего. Сверх того не выполнен п.2 (сам развод — кода не изменено ни
строкой) и нет отдельной строки теста из п.3 («Отдельной строки теста здесь нет намеренно»).

Отдельно и честно: заход выбрал ветвь, которую его ЦЕЛЬ прямо объявила законной («развести
нельзя одной правкой — назвать причину адресом и остановиться»), причины назвал тремя адресами
на трёх сторонах, и его диагноз я перепроверил по пяти фактам — все пять сошлись (§4). Это
делает его **годной разведкой**, но приёмкой решения не делает: предмет задания — развод и
замок, а их нет.

**Что делать Штабу с 68c, если он хочет закрытия:** развод упирается в предмет, которого на
VPS не существует. Условие сигнала А, его счётчики и вычёркивание живут на полосе ПК — значит
и задание на развод, и его отрицательный тест ставятся НА ПОЛОСУ ПК; VPS-части там ровно одна
и она красная — перезапуск `orchestrator-daemon` ради доставки `5e78c36`.

---

## 8. п.7, п.8 — что сюда не переписано, и бюджет

Тексты артефактов целиком не переписаны: взяты числа, имена, пути и по одной-две строки цитаты
на вывод. Полные тексты остаются на сервере по путям из §1 (21 919 и 25 642 байта).

**Где искал:** `/root/turbobaby-manager-bot/docs/artifacts/`, `orchestrator_daemon.log` (вся
глубина), `tests/test_park_read.py`, `tests/test_fleet_cell.py`, `invariants_check.py`, тела
заданий в `_recon_parkread_sep/` и `_recon_vecher_ostanovka/`, `systemctl show`, `git log`.
**Чем искал:** только чтение — `ls`, `cat`, `grep`, `sed`, `awk`, `wc`, `git`, `systemctl show`.
`.env`, секреты и конфиги не читались, значений не печаталось; наружу не ушло ничего; брони и
«Календарь бронирования» не открывались; цены не трогались; сигнальная остановка и её метка не
тронуты ни одной командой; процессы не трогались.
**Срез:** сервер на 20.09 (HEAD `6de90de`, демон `PID=2160442`); ПК — рабочее дерево 20.09.

**Бюджет:** единица одна, потолок 2700 с. Заход уложился в одну единицу; замер по часам сессии,
не по `METRICS` (своей строки METRICS у полосы ПК в этом месте нет). Остатка, кроме названного
в §7, нет.
