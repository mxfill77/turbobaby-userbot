# Claude operating snapshot — read-only result

**Status:** `FACTS_WITH_FRESHNESS_LIMITS`.  This is a redacted read-only
handoff from the existing Claude project, collected 2026-08-31.  No files,
services, Brain records, Drive records, runtime settings or external messages
were changed by the request.

## Direction

The north star is an owner-operated premium motorcycle-rental business whose
eventual client workflow can run without the PC.  The current work is not a
client release: it is evidence and failure-contract work for the autonomous
builder's stage 3.

The client userbot remains frozen.  Do not touch live tables, money, bookings,
client text, secrets/configs, production records, process restarts or external
assignments.  A fixture-only training environment is explicitly separate from
the client contour.

## Confirmed active work

- **H1 → V0:** local V0 result is `PROVEN_LOCAL`; independent acceptance is
  `UNKNOWN` because the reviewer cannot read the exact case.  H1/V0 are not
  released or integrated.
- **C0 price advisor:** fixture-only shadow work; application to client-facing
  descendants was stopped by gates.
- **C1:** explicitly `STOP`, not merely waiting.  Its prior executor lacked the
  required architectural basis and correctly did not invent a packet.
- **Expectations layer:** active; coverage work for PC, Bridge and moderation
  remains parallel, non-unfreezing work.

## Current authoritative order

1. Restore trust in stale queue snapshots before using them to infer readiness.
2. Make the V0 case addressable and readable back by an independent reviewer.
3. Demonstrate the V0 negative case: a declared but empty address is non-green.
4. Receive the owner decision on the price-base discrepancy.
5. Only then return to C1 as a spec, a separately approved fixture-only run,
   and independent review.

## G1 reconciliation

The owner-approved review-only preflight was completed without a commit.  It
found no credential pattern but did find an absolute local path in one exact
V0 evidence artifact.  Replacing it makes a derivative case; committing it
unchanged transmits the local path.  Therefore G1 is a real `STOP`, not a
missing implementation step.  See
`docs/artifacts/2026-08-31-g1-v0-review-preflight-result.md`.

## Freshness and unknowns

- PC and VPS queue snapshots were already more than nine hours old at the
  snapshot time; lane state is therefore `UNKNOWN`, not idle/healthy.
- The pulse was about a day old, and some canonical nodes were only partially
  read.  Do not infer that a rule or fact is absent from those sources.
- Independent V0 acceptance, the price-base decision, client-contour gate
  opening, and the final C1 evidence basis are all unresolved.

## Capacity and channel

- Architecture, negative contracts, reviews and Brain reading: Claude Opus 5
  xhigh in the existing Claude project.
- Simple bounded checks: Codex low/medium and Manus Lite.
- Medium bounded packets: Codex medium/high and Manus 1.6.
- Complex cross-system or safety work: Codex high/xhigh and Manus 1.6 Max.

Before any Manus message, the selected Manus level must be set explicitly.
Who builds does not independently accept their own case.
