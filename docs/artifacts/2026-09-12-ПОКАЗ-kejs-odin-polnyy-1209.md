# ⛔ ПОКАЗ КЕЙСА 1 НЕ СДЕЛАН — замок свежести ловца ОТКАЗАЛ (законно), наружу не ушло НИ ОДНОГО сообщения

**Дата замера: 12.09.2026, 16:24–16:31 по Пхукету (UTC+7).** Задание Штаба
`shtab_task_52-c-pokaz-kejs-odin.1209`, полоса ПК.

ПОКАЗ kejs-odin-polnyy 1209

Исход одним словом: **ОТКАЗ ЗАМКА**. Не сбой, не нехватка данных, не запрет гарда — штатная
fail-closed ветка, сработавшая ровно на то, для чего заведена. Счётчик отправок за заход —
**0**: ни в группу -5193185299, ни в инбокс 1160, ни в тему 328, ни клиентам, ни менеджерам.
Кнопок никто не нажимал и не имитировал. Кода заход не тронул ни строкой, снимков не собирал,
голову не звал.

---

## 1. Замок спрошен САМ, а не пересказан

Спрошен штатной ручкой продукта — `exam_show.py --agent` («несёт ли живой pc_agent код кнопки»),
пробного скрипта не заводилось. Ответ дословно, как он вышел в консоль:

> НЕТ — живой агент (pid 13740) СТАРШЕ кода кнопки на 1575 с: файл «exam_show.py» правлен после
> его старта. Тап по такой кнопке вернёт «карточка устарела» и пропадёт — так было 05.09 в 10:29.
> Цена снятия: владелец шлёт агенту в тему 205 одно слово «обновись» (он сам делает git pull и
> перезапускает себя), после чего показ пойдёт

Код ответа 0 — то есть это **вердикт замка**, а не падение вызова. Сам замок —
`exam_show.agent_carries_button` (:694–729), предмет — момент рождения процесса против `max(mtime)`
по `BUTTON_CLOSURE = ("pc_agent.py", "exam_show.py")` (:691).

## 2. Четыре числа, которые требует пункт 2 задания

| что | значение | откуда |
|---|---|---|
| номер живого процесса | **pid 13740** | `pc_agent.lock` И независимо CIM-проба `Win32_Process` — два источника сошлись |
| момент его старта | **12.09.2026 15:28:26.324 +07** = **08:28:26.324 UTC** (`started` 1789201706.3242168) | `pc_agent.lock`; CIM `CreationDate` = 12.09.2026 15:28:26 — совпало посекундно |
| самый свежий файл замыкания | **`exam_show.py`**, mtime **12.09.2026 15:54:41.438 +07** = **08:54:41.438 UTC** (1789203281.4384) | `stat` |
| разница | **1575.114 с** (замок печатает целым: **1575 с**) = **26 мин 15 с** | арифметика двух строк выше |

Второй файл замыкания виновным **не является и назван, чтобы его не искали заново**:
`pc_agent.py` mtime 12.09.2026 **04:36:01** (1789162561) — на 10 ч 52 мин СТАРШЕ старта процесса,
то есть свой собственный код кнопки живой агент несёт. Блокирует ровно один файл — `exam_show.py`.

Живость процесса проверена read-only: CIM `Win32_Process` даёт
`"D:\turbobaby-bot\venv\Scripts\python.exe" pc_agent.py`, pid 13740. Ни одного удара по процессу
не нанесено (`taskkill`/`schtasks` — красное, к ним не подходили).

## 3. Премиса Штаба подтверждена, и у неё есть поправка, которую надо назвать вслух

Штаб предположил: «агент поднят владельцем 12.09 в 08:28:26Z (pid 13740), а `exam_show.py` правлен
заходом 52-b позже — значит замок, скорее всего, снова отказывает». **Подтверждено полностью**,
включая минуту и номер процесса.

Поправка по сути, а не по букве: **владелец перезапуск СДЕЛАЛ, и он сработал.** Прошлый отказ
(артефакт `2026-09-12-АГЕНТ-perezapusk-svezhest-1209.md`) держался на pid 22484, рождённом
13:09:59, и отставании 1097 с. Сегодняшний отказ — **другой, второй по счёту**: новый процесс
13740 родился 15:28:26 и был свежее ВСЕГО кода на тот момент, а потом заход 52-b переписал
`exam_show.py` в 15:54:41 (коммит `3cd17b5`, 15:55:55). То есть замок ловит не застарелый долг,
а **правку, легшую через 26 минут после подъёма**.

Отсюда вывод, важный для планирования, а не для оправдания: **перезапуск и правка кода кнопки,
идущие в одни сутки, гонятся друг за другом.** Порядок обязан быть «сначала весь код кнопки, потом
подъём, потом показ», иначе каждый следующий заход-правщик обнуляет чужой перезапуск. Сегодня это
случилось дважды подряд.

## 4. Слот к показу ГОТОВ — блокирует только замок

Проверено чтением `exam_shots/case-1@37c290f.json` (только чтение, карточка наружу не собиралась):

| признак задания | требовалось | в слоте | сходится |
|---|---|---|---|
| подсказок критика | 3 | **3** | да |
| живых примеров менеджеров | 3 | **3** (`д85·р141`, `д559·р2`, `д611·р2`) | да |
| фигурных скобок в вопросе | 0 | **0** (`question` — «Здравствуйте! Хочу XMAX 300 с 6ого по 11ое октября, залог паспортом, 1 шлем»; скобки `{when_sloppy}` остались только в `question_raw`, в карточку он не идёт) | да |
| подбор по новому правилу | — | `note` несёт правило «основная модель + не более двух альтернатив ТОГО ЖЕ класса» | да |

Круг критика в слоте: `outcome` = `hints`, `parsed` = **3**, ждали **71.3 с** при пределе **257 с**,
`raw_len` 684. Снимок: `commit` **37c290f**, `corpus` **99e6b78177a1d533**, `total` 17,
`built_at` **2026-09-12T08:56:10Z**.

**Число, которое снимает вопрос «а хватит ли одного перезапуска».** `built_at` снимка (08:56:10Z)
ПОЗЖЕ mtime `exam_show.py` (08:54:41Z) на 89 с. Значит любой подъём агента, начиная с этой минуты,
делает процесс свежее ОБОИХ файлов замыкания — второй правки ждать не надо, ловить нечего.

## 5. Кнопок будет ШЕСТЬ, а не пять — точное число и подписи

Задание назвало «кнопок пять» и тут же перечислило шесть подписей. Считает их
`exam_show.markup` (:647–670) по числу подсказок; при 3 подсказках выходит **6 кнопок в 3 рядах**:

| ряд | подписи | токен колбэка |
|---|---|---|
| 1 | ✅ Верно | `exam:ok:1` |
| 2 | 1 · 2 · 3 | `exam:h1:1` · `exam:h2:1` · `exam:h3:1` |
| 3 | ✍️ своё · ✔ Применить | `exam:own:1` · `exam:go:1` |

Кнопка «❌ Неверно» в этом наборе **отсутствует намеренно**: она живёт во второй ветке `markup`,
для снимков БЕЗ подсказок. Расхождение с текстом задания — арифметическое (перечень верный,
число в прозе на единицу меньше), поведения кода оно не меняет.

## 6. Живость ловца — проверена ЧТЕНИЕМ регулярки, тапов не было (пункт 5)

`pc_agent.py:727`: `EXAM_CB_RE = re.compile(r"^exam:(ok|no|own|go|h[1-4]):([0-9]{1,3})$")`.
Все шесть токенов будущей карточки (`ok`, `h1`, `h2`, `h3`, `own`, `go`) ловец понимает; молчащих
кнопок в наборе нет. Ни одна кнопка не нажата и не сымитирована — тап остаётся делом владельца.

## 7. Почему отказ не обойдён и почему это правильно

Обхода не искали — он запрещён заданием, и он здесь вреден. Известно (замер 51-c), что
`pc_agent` **не импортирует `exam_show`**: тап спавнит отдельный процесс, читающий файл с диска
заново, — то есть сегодняшний отказ консервативен. Но замок fail-closed, и ошибается он в
безопасную сторону: цена его отказа — один перезапуск, цена ложного «несёт» — сожжённое первое
впечатление владельца от первой полной карточки, а оно тратится один раз. Права править замок у
этого захода нет (кода не трогаем), и вопрос «должен ли `exam_show.py` стоять в `BUTTON_CLOSURE`»
остаётся открытым для Штаба вторые сутки.

## 8. Арифметика бюджета (пункт 6)

Считалась до первого действия. Единиц по заданию — **три**: проба замка · печать карточки ·
одна отправка. Кругов головы — **0** (снимок готов, критик отработал ещё в 52-b). Оценка каждой
единицы ≤ 20 с ⇒ произведение **3 × 20 = 60 с против потолка 2700 с** — 2.2 %, запас 45×.
**Фактически потрачена одна единица** (проба замка), остальные две не наступили: пункт 2 велит
остановиться на отказе. Разведка чтением (снимок, `markup`, регулярка ловца) уложилась в тот же
порядок секунд.

## 9. Что владелец делает дальше — двумя строками (пункт 7)

**Что пришло.** Карточка кейса 1 НЕ показана: живой агент (pid 13740, поднят 15:28:26) на 26 минут
старше файла `exam_show.py`, правленного в 15:54:41, — тап по такой кнопке вернул бы «карточка
устарела» и пропал, поэтому показ остановлен ДО отправки; сам слот `case-1@37c290f.json` готов
полностью — подсказок 3, примеров 3, скобок 0.

**Что делать.** Послать агенту в тему 205 одно слово **«обновись»** (он сам сделает `git pull` и
перезапустится, красных операций не нужно) — и поставить задание на показ заново; после подъёма
замок пропустит, потому что снимок собран в 08:56:10Z, позже обоих файлов кнопки, и второй правки
ждать не требуется.

---

### Чистота дерева и границы захода

`git status --porcelain exam_show.py pc_agent.py` → **пусто**; HEAD на момент замера `49aba2e`.
Наружу — 0 сообщений. Процессов не трогали. Старых карточек в группе не касались. В базу уроков
и журнал вердиктов боевых строк не писали. Броней и листа «Календарь бронирования» не открывали.
Значений токенов не печатали. Текста клиентских диалогов здесь нет — имена примеров даны
короткими метками (`д85·р141`), как их хранит снимок.
