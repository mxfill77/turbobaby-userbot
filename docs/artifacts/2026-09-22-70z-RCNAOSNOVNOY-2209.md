# 70z RCNAOSNOVNOY 2209 — файл выбора учётки ДОСТАЁТ до полосы RC: покрыта с 17:17, живая с 17:18

Задание Штаба `0015f-70z.2209` (док `shtab_task_0015f-70z.2209`, file id
`17xHXFHg3tQkQznhrCnbwkOIwcjtSLPm2s9oOMw2vF9g`). Полоса ПК. Замер **22.09.2026, 18:05–18:30
местного (11:05–11:30 UTC)**. Владелец задачу не пересылал.

RCNAOSNOVNOY

Временное место одно, названное заданием: **`tmp/rc_osnovnoy/`**. Ничего не удалялось, включая
уборку за собой. Реестр Windows не трогали ни на запись, ни на чтение. Процессы не трогали: ни
супервизор, ни его детей, ни зомби. Было ровно одно обращение к процессам, только чтение:
`tasklist` по двум PID. Файлы учётных данных и `.claude.json` не открывались. Клиентский контур не
трогали. Наружу к человеку ничего не ушло.

---

## ОТВЕТ ОДНОЙ СТРОКОЙ

**Премиса Штаба на момент захода устарела. Файл выбора покрывает RC с коммита `4264ca90` (17:17:34),
а в живом RC это действует с рестарта `TurboBabyRC` в 17:18:18 (pid 19924). Повторно — с рестарта
в 18:25:52 (pid 25020, соседний заход); сейчас живой супервизор — 25020. Обе ветки и doctor
получают окружение через `child_env` → `profile_choice.apply_to`. В живом логе обе ветки пишут
«слово ОСНОВНОЙ — ключ CLAUDE_CONFIG_DIR снят», и rc-server зарегистрирован в организации
телефона `30c614de`. Гипотеза «профиль RC задаёт переменная из реестра» верна для окна
18.09 21:39 → 22.09 17:18 и для самого процесса супервизора, но не для его детей сейчас. Дописывать
чтение было некуда. Вместо этого закрыта дыра замка: провод «спавн/doctor → файл» не держал ни
один тест.**

---

## п.0. ПРИЗНАК СДЕЛАННОСТИ

Греп `RCNAOSNOVNOY` по `docs/artifacts/` на входе: **0 совпадений**. За 22.09 лежали 70f–70y и
`2026-09-22-rc-device-profile2-stall.md`. Последний отвечает ЧАСТЬ вопросов: там сделана починка
`4264ca90` и рестарт с «да» владельца. Переписи всех веток спавна, доказательства сборкой и мутанта
там нет. Работа продолжена с этих пунктов.

---

## п.1. ВСЕ ВЕТКИ ПОДЪЁМА CLI В ДЕРЕВЕ — И ЧИТАЮТ ЛИ ОНИ ФАЙЛ

Метод, дерево и дата: `git grep` по **отслеживаемым** `*.py` без `test_*`, HEAD 22.09 18:1x. Мины
из CLAUDE.md (`manager-bot/`, архивные копии) так не попадают. Порядок такой:

1. файлы с вызовом `subprocess.Popen/run/call/check_*`, `os.system/popen/spawn/exec`,
   `create_subprocess_*` — **36**;
2. из них подняли бинарь claude — **5** файлов, **7** мест спавна. Отбор по вызову резолвера бинаря
   (`resolve_claude()` / `suggest._resolve_claude()`) и чтению мест вызова. Остальные 31 зовут
   git, powershell, tasklist, netstat, python-писателей и node;
3. не-питон-лаунчеры (`*.vbs/*.ps1/*.cmd/*.bat`, отслеживаемых 4): claude не зовёт ни один.
   `rc_remote_control.vbs` поднимает python-супервизор, а не CLI;
4. вызывающие проверены: у `run_claude` ровно 1 вызов (`_run_task_impl:2990`), у `_thinker_exec`
   7 вызовов (`pc_orchestrator.py`, `lesson_router.py`), и все они проходят через одно
   `apply_to` внутри.

| # | ветка | место спавна (HEAD) | чем собрано окружение | читает файл |
|---|---|---|---|---|
| 1 | исполнитель демона `claude -p` | `pc_orchestrator.run_claude:2522` ← `_run_task_impl:2990` | `profile_choice.apply_to` `:2951` | **да, 1 вызов** |
| 2 | думатель демона `claude -p --max-turns 1` | `pc_orchestrator._thinker_exec:6017` | `profile_choice.apply_to` `:6001` | **да, 1 вызов** |
| 3 | **RC: rc-server** `claude rc` | `rc_supervisor.default_spawn:509` | `child_env()` `:504` → `apply_to` | **да** |
| 4 | **RC: named-channel** `claude --remote-control turbobaby-pc` | тот же `default_spawn:509` | тот же `child_env()` | **да** |
| 5 | **RC: doctor пре-флайта** `claude doctor` | `rc_supervisor.rc_ready:384` | `child_env()` `:381` | **да** |
| 6 | клиент: черновик ответа | `suggest._cli_llm:6538` | `dict(os.environ)` минус API-ключи | **нет, 0** |
| 7 | клиент: экстракция брони | `booking_draft._extract_llm:85` | то же | **нет, 0** |
| 8 | тренажёр: чтение снимка | `trainer_photo._cli_vision:221` | то же | **нет, 0** |

**Итого: 8 веток на 7 местах спавна. Файл читают 5 из 8: весь демон и весь RC. Не читают 3, и все
они клиентского контура**, который заморожен и не правился. Профиль этих трёх — окружение
процесса-хозяина (userbot / модербот / тренажёр). Какое оно сейчас, в этом заходе не мерялось:
**НЕИЗВЕСТНО**.

Вне дерева: интерактивные окна и Desktop берут профиль из переменной пользователя (замер 70u).
Сессии, которые rc-server сам поднимает для телефона (`spawnMode=same-dir`), — его дети. Отсюда
вывод, не прямой замер: они наследуют его окружение без ключа.

---

## п.2. ОТКУДА RC БЕРЁТ ПРОФИЛЬ В ЖИВОМ ПОДЪЁМЕ

**Цепочка подъёма, по файлам дерева:** задача Планировщика `TurboBabyRC` (шаблон
`pc_remote_control.task.xml`: `LogonTrigger`, `LogonType=InteractiveToken`, пользователь SID
`…-1002`) → `wscript.exe //B //Nologo rc_remote_control.vbs` → `WshShell.Run` python
`rc_supervisor.py`. Скрипт `vbs` задаёт только `CurrentDirectory`, окружение не трогает.

**Сам супервизор получает профиль НАСЛЕДОВАНИЕМ блока окружения от родителя.** Планировщик
собирает блок из переменных пользователя, куда входит `CLAUDE_CONFIG_DIR=D:\claude_profile_2`
(значение из замера 70u / RC-USTROYSTVO; здесь реестр не читали). Сам `rc_supervisor` ключ не
ставит ни одной строкой. До 17:18 дети рождались без `env` в `Popen`, то есть с тем же
унаследованным блоком — отсюда 91 старт, 88 ЗОМБИ и 0 регистраций в разборе stall.

**Дети с 17:18:18 получают профиль ИЗ ФАЙЛА.** `child_env` копирует окружение супервизора и
применяет к копии `profile_choice.apply_to`. Слово ОСНОВНОЙ снимает ключ, и CLI уходит в профиль
по умолчанию, то есть на основной аккаунт. Чем показано (живое, только чтение):

| что | факт |
|---|---|
| первая жизнь на файле | `rc_remote_control.log` 17:18:18,974 — `супервизор стартовал (pid=19924…)`; 17:18:20,690 `[named-channel]` и 17:18:20,692 `[rc-server]`: `выбор учётки строителей: слово ОСНОВНОЙ — ключ CLAUDE_CONFIG_DIR снят из окружения ребёнка (основной профиль)`; мост: 10:18:21Z `Registered … env_01QFWiKT…`, `organization_uuid` = **`30c614de…`** (организация телефона); опрос жил до 11:23:52Z; `tasklist` 18:25:24: `19924` и `24504` есть |
| **вторая жизнь (сейчас)** | 18:25:52,571 `супервизор стартовал (pid=25020…, … RC_UNREG=1 …)` — перезапуск сделал соседний заход (его коммит `4b168ea7`), не этот; 18:25:55,567 `[rc-server]` и 18:25:55,570 `[named-channel]`: та же строка «слово ОСНОВНОЙ — ключ … снят»; rc-server pid 2084, канал pid 26460 с `--no-chrome` |
| регистрация второй жизни | `rc_server_debug.log` 11:25:57Z `Registered, server environmentId=env_01QFWiKT…` (тот же environment), `organization_uuid` = **`30c614de…`** → 11:25:58Z `Starting poll loop` |
| жив сейчас | `tasklist` 18:28:34: `python.exe 25020` и `claude.exe 2084` есть, `19924` нет |

---

## п.3. ДОПИСЫВАТЬ НЕЧЕГО — ЗАТО ЗАКРЫТА ДЫРА ЗАМКА

Условие пункта («если файл НЕ покрывает») ложно, поэтому строк чтения в `rc_supervisor.py` этот
заход не добавил: **0**. Семантика в коде, который исполняет живой супервизор (`4264ca90`),
совпадает с требованием задания: ОСНОВНОЙ удаляет ключ, путь ставит путь, мусор не меняет ничего,
пустую переменную не ставит.

Дыру нашёл мутант (п.5). Спавн-тесты `test_rc_supervisor` подставляют `envf` руками, и провод по
умолчанию не держал никто. Новый файл **`test_rc_choice_wiring.py`** (5 тестов) зовёт боевые
`default_spawn`, `rc_ready` и цикл `supervise_branch` БЕЗ `envf`. Файл выбора кладётся во
временный REPO, `subprocess` подменён только внутри модуля супервизора, процессов ноль. При отказе
тест называет только ИМЕНА ключей. Политику третьего исхода он не судит: её держит
`test_rc_supervisor`, а спор о ней — ниже в разделе «Развилка».

---

## п.4. СБОРКА ОКРУЖЕНИЯ ТЕМ ЖЕ КОДОМ, БЕЗ ПОДЪЁМА ЖИВОГО ПРОЦЕССА

Метод: `tmp/rc_osnovnoy/measure_rc.py`. Код берётся `git show HEAD:` в песочницу
`tmp/rc_osnovnoy/head/`, логгер уводится в temp. Окружение родителя — копия окружения процесса
замера плюс `CLAUDE_CONFIG_DIR=D:\claude_profile_2`. Дети — боевые `default_spawn` (обе ветки) и
`rc_ready` без `envf`. Вместо claude с теми же `env`/`cwd` поднимается
`tmp/rc_osnovnoy/child_report.py`; он печатает только свой взгляд на ключ.

**Код первой жизни на файле, 17:18–18:25** (HEAD `3e00a315`; ветка файла там та же, что в
`4264ca90`). Это ровно формулировка задания. Код нынешней жизни — в разделе «Развилка».

| случай | решение | ключ у каждого из 3 детей | ребёнок докладывает | ключей разошлось с родителем |
|---|---|---|---|---|
| живой файл (HEAD) | `drop` | НЕТ КЛЮЧА | НЕТ КЛЮЧА | 1 |
| живой файл (дерево в 18:2x) | `drop` | НЕТ КЛЮЧА | НЕТ КЛЮЧА | 1 |
| **ОСНОВНОЙ** | `drop` | **НЕТ КЛЮЧА** | НЕТ КЛЮЧА | 1 (`CLAUDE_CONFIG_DIR`) |
| **путь** существующего каталога | `set` | **= путь** (`…\cases_head\fake_profile`) | тот же путь | 1 |
| **мусор** ×7: две значимые строки · управляющий символ · пусто · пустые кавычки · одни комментарии · путь не существует · файла нет | `keep` | **= `D:\claude_profile_2`** (как у родителя) | то же | **0** |

Числом по 33 окружениям (11 случаев × 3 ребёнка): **9** без ключа, **3** с путём, **21** равны
родителю, **0** пустых; доклад ребёнка совпал с собранным окружением **33 из 33**. Вывод:
`tmp/rc_osnovnoy/measure_head.out.json`.

---

## п.5. МУТАНТ: ЗАМОК ДОКАЗАН ЧИСЛОМ

Метод: `tmp/rc_osnovnoy/mutant_rc.py`. Каждый вариант — песочница `tmp/rc_osnovnoy/mutant/<имя>/` с
HEAD-версиями `rc_supervisor.py`, `profile_choice.py`, `test_rc_supervisor.py`, файла выбора,
лаунчеров и фикстур. `IMPORTED_RC` в выводе показывает, что тест подхватил именно копию. Боевой
файл не правился: sha256 рабочего `rc_supervisor.py` одинаков до и после каждого прогона.

**Мутант задания — A:** ветка чтения файла в RC выключена целиком (`child_env` сразу отдаёт
«наследовать»). Дополнительно B — спавн мимо `child_env` — и C — doctor мимо `child_env`.

| вариант | HEAD `3e00a315`, 112 тестов | HEAD `9395af27`, 118 тестов |
|---|---|---|
| контроль (копия без мутации) | **0** красных | **0** |
| **A: чтение файла в RC выключено** | **8 методов** (5 в `test_rc_supervisor` + 3 в новом), отказов 14 + ошибок 1 | **11 методов** (8 + 3), 14 + 4 |
| B: спавн мимо файла | **0 в `test_rc_supervisor`** → 3 в новом | **0 в `test_rc_supervisor`** → 4 в новом |
| C: doctor мимо файла | **0 в `test_rc_supervisor`** → 3 в новом | **0 в `test_rc_supervisor`** → 4 в новом |

Главное число — ноль в строках B и C. **До этого захода оборванный провод к файлу проходил ВСЕ
тесты RC** (84 из 84 на версии до слияния RC-UNREG, 107 из 107 на `3e00a315`, 113 из 113 на
`9395af27`). Это и есть дефект 18–22.09 в обличье «файл вроде бы решает». Теперь его держат 3–4
метода `test_rc_choice_wiring`. Выводы: `tmp/rc_osnovnoy/mutant_head.out.txt`,
`mutant_head9395.out.txt`, `mutant_before.out.txt`.

---

## п.6. ГОТОВНОСТЬ ПО КОММИТУ И ПРИМЕНЕНИЕ — ОТДЕЛЬНО

**Готовность по коммиту — НАСТУПИЛА.** Коммит **`9395af27`**, 1 файл (`test_rc_choice_wiring.py`,
+206), пуш `16a7a3ad..9395af27 main -> main`. Шло под замком полосы (`git_serial_pc.py -- git add`
/ `-- git commit --only` / `-- git push`), потому что в дереве второй заход. После коммита:
`test_rc_choice_wiring` + `test_rc_supervisor` + `test_profile_choice` + `test_rc_auth_detect` —
**184 OK**; `test_utf8_output_guard` + `test_no_test_cards` + `test_log_setup` — **66 OK**.

**Применение по строке самообновления — В ЭТОМ КОММИТЕ ПРИМЕНЯТЬ НЕЧЕГО**, и вот почему это факт,
а не отговорка. Коммит несёт только тест: ни один файл замыкания демона и ни одна строка
`rc_supervisor.py` не тронуты. Самообновление демона (последняя эстафета —
`pc_orchestrator.log` 17:14:26 `self-update: 2297a7e7→b70d2965`) от него не сработает. Супервизор
RC сам не обновляется вовсе: он применяет код только перезапуском `TurboBabyRC`. Действующая
починка RC применена раньше этого захода: строка `супервизор стартовал (pid=19924…)` в
17:18:18 после коммита `4264ca90` в 17:17:34.

**Чужое, но рядом и важно:** коммит соседа `16a7a3ad` («RC-страховка», `--no-chrome` у канала, BOM
в файле рычага) в живом RC **ПРИМЕНЁН в 18:25:52** перезапуском, который сделал соседний заход
(строка `супервизор стартовал (pid=25020…)`). Этот заход процессов не трогал. В 18:25:24 ещё жил
19924; первая редакция этого артефакта писала «не применён», и через 28 секунд это протухло.
`profile_choice.py` соседа входит в замыкание демона, поэтому эстафету демона после этого захода
вызовет он, а не `9395af27`.

**Владельцу руками — одно движение:** перезапускать ничего не нужно — открой на телефоне список
устройств Claude Code и проверь, что там есть mxfillpc.

---

## РАЗВИЛКА (решение владельца или Штаба, здесь НЕ решалась)

Задание требует для RC «пусто или мусор не меняет ничего», то есть так же, как у демона. Соседний
заход в 18:18–18:2x сознательно закоммитил обратное: `16a7a3ad`, `rc_supervisor._rc_fallback`. На
третьем исходе RC **снимает** унаследованный ключ и уходит на основной профиль. Довод соседа:
наследовать у RC сегодня значит получить профиль 2, то есть второй аккаунт и 0 регистраций. Замер
тем же `measure_rc.py` на HEAD `9395af27`: из 33 окружений **30 без ключа, 3 с путём, 0 равны
родителю**. Все 21 мусорных окружения расходятся с формулировкой задания. У демона третий исход
по-прежнему наследует окружение.

Обе позиции держат инвариант «пустую переменную не ставить»: 0 пустых в обоих замерах. Мой тест
провода от выбора не зависит — он зелёный на обоих кодах. Выбрать нужно одно:
**(а)** RC как демон: мусор → наследовать, по заданию 70z; **(б)** RC-страховка: мусор → основной
профиль, как в `16a7a3ad`. **С 18:25:52 в живом RC действует (б)** (супервизор 25020). При
нынешнем файле (ОСНОВНОЙ) обе дают одно и то же — ключа нет. Расходятся они только при
испорченном файле.

---

## ЧЕСТНЫЕ ОСТАТКИ

1. **Видимость mxfillpc в Devices подтверждает только владелец глазами.** С ПК доказана регистрация
   в организации телефона и живой опрос моста, а не отрисовка списка.
2. **Значение переменной в блоке супервизора здесь не перемерялось**: реестр и окружение чужого
   процесса не читали. Наследование показано устройством цепочки (шаблон задачи, `vbs`, код
   супервизора) и поведением до 17:18, а значение взято из замеров 70u и RC-USTROYSTVO.
3. **Три клиентские ветки файла не читают** (п.1, строки 6–8). Контур заморожен; какой профиль у
   их хозяев сейчас — НЕИЗВЕСТНО.
4. **Инцидент захода, названный прямо.** Первый черновик нового теста при падении вывел в консоль
   этой сессии окружение процесса целиком, а в нём значения секретов демона. В файлы, коммит,
   журнал и наружу это не попало. До коммита тест переписан: при отказе он называет только имена
   ключей. В выводах мутантов ноль вхождений имён секретов (проверено счётчиком).
5. Первый прогон мутантов шёл по рабочему файлу, пока сосед его менял. Его числа (A: 5, B: 0, C: 0
   из 84) сохранены в `mutant_before.out.txt`, но как доказательство взяты прогоны по HEAD-снимкам.

---

## ФАЙЛЫ ЗАМЕРА (`tmp/rc_osnovnoy/`, ничего не удалено)

| файл | что |
|---|---|
| `measure_rc.py` · `child_report.py` | п.4: сборка окружения боевым кодом, доклад ребёнка |
| `measure_head.out.json` · `measure_head_9395af27.out.json` · `measure_wt.out.json` | выводы п.4 и развилки |
| `mutant_rc.py` · `mutant_head.out.txt` · `mutant_head9395.out.txt` · `mutant_before.out.txt` | п.5 |
| `head/` · `mutant/<вариант>/` · `cases_head/` · `cases_wt/` · `choice_head.bin` · `commit_msg.txt` | песочницы и входы |
