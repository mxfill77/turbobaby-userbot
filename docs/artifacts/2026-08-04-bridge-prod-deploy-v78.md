# Выкладка моста в прод: @76 → @78 (04.08.2026)

Деплой сделан **с ПК по ssh** — headless-контур на сервере этого сделать не может.
Разрешение владельца дано в сессии. Живая папка `/root/turbobaby-bridge-gs` НЕ трогалась,
`clasp pull` НЕ выполнялся ни разу.

## Что выложено

Папка слияния `/root/turbobaby-bridge-merge-20260804` (17 файлов, собрана заранее задачей 257,
база слияния = HEAD проекта, а не прод-версия @76).

| файл | что несёт |
|---|---|
| `BotData.js` | наша работа: правка строки события на месте (`edit_event`) |
| `ReadFleet.js` | наша работа: ветка исправления пробега |
| `Bridge.js` | маршрут `edit_event` + замок красной зоны + имя в списке действий |
| `BrainTrash.js` | **чужая** работа реестра мозга (30.07, лежала в HEAD как @77 «временный») — уехала вместе с нашей, потому что `redeploy` публикует снимок HEAD целиком |

Остальные 13 файлов побайтно равны проду @76.

## Ход (по шагам, с фактами)

| шаг | факт |
|---|---|
| папка слияния | 18 видимых записей, 16 `.js` + `appsscript.json` + README; `node --check` **16/0** |
| снимок отката | `/root/_bridge_prod_snapshot_20260804` — `prod_v76/` 17 файлов, `head/` 17 файлов, `meta.json` |
| авторизация clasp | `info@turbophuket.com`, clasp 3.3.0, node v22.22.2 |
| `clasp status` | tracked ровно 17; `README_ДЕПЛОЙ.txt` и `.clasp.json` — untracked, не уедут |
| деплои ДО | `…k9nI9m @HEAD` и `…hXNOqw @76` ← прод |
| `clasp push -f` | `Pushed 17 files at 9:55:19 AM.`, exit 0 |
| `clasp redeploy …hXNOqw` | `Redeployed …hXNOqw @78`, exit 0 — номерная версия, **не** `@HEAD` |
| деплои ПОСЛЕ | `…k9nI9m @HEAD` и `…hXNOqw @78` — второй деплой не тронут |

`clasp deploy` СОЗНАТЕЛЬНО не использовался: он создал бы новый деплой и сменил URL — Splinter
отвалился бы.

## Проверка живым фактом

`deploy/bridge_after_deploy_check.py` (read-only, в листы не пишет), прогон ДО и ПОСЛЕ:

```
ДО:     3) вызов маршрута (origin=agent, без билета): error='unknown_action'
           ВЕРДИКТ: маршрута в проде НЕТ — деплой не доехал.
        2) действий в проде: 62 ; маршрут 'edit_event' перечислен: False

ПОСЛЕ:  3) вызов маршрута (origin=agent, без билета): error='no_ticket'
           ВЕРДИКТ: маршрут в проде ЕСТЬ, запись отбита замком 4.2 (в лист не писали).
```

Прогонов ПОСЛЕ было **два**, оба дали `no_ticket` — факт устойчив.

**Пункт 2 после деплоя вернул 0** («ответ потерян echo-слоем»): probe-POST теперь отбивается
плечом расписки (`unauthorized — Invalid or missing token`) ДО ветки `unknown_action`. Это не
регресс деплоя, а известное поведение echo-слоя, ради которого решающим и назначен пункт 3 —
скрипт помечает это сам. Число 66 подтверждено иначе: см. ниже.

## Отпечаток задеплоенного кода (перечитан у Apps Script API, не у clasp)

`deploy/bridge_prod_recon.py` в **отдельный** каталог `/root/_bridge_prod_snapshot_20260804_after`
(снимок отката не тронут):

- прод обслуживает версию **78**, создана `2026-08-04T09:56:02.266Z`;
- `HEAD vs @78 расходятся файлы: —` (HEAD == прод, чужого пуша между push и redeploy не было);
- `diff -rq @78 ↔ папка слияния` — **пусто**: задеплоено байт-в-байт то, что готовили;
- действий в `Bridge.js` — **66** (было 62: +`edit_event` и +3 реестра мозга).

## Ротация журнала 29.07 — ЦЕЛА

Судим по коду В ПРОДЕ, а не по локальной папке:

- вхождений `cowork` в задеплоенном @78 — **52**, ровно как в @76 (`Bridge.js` 7 + `Archive.js` 39);
- `Archive.js` в @78 md5-идентичен `Archive.js` из @76 (`684e9f1765b82d1b40ca57c8fffd9bc6`);
- все три маршрута ротации в задеплоенном `Bridge.js`: `prune_cowork_log`,
  `setup_cowork_prune_trigger`, `prune_cowork_selftest`;
- `@78 vs @76` расходятся ровно 4 файла — те самые, что планировались.

Именно ради этого не пушили из живой папки: в ней `cowork` — **0** вхождений, push из неё стёр бы
работу 29.07 молча.

## Откат (не понадобился)

`/root/_bridge_prod_snapshot_20260804/prod_v76` — 17 файлов, точная копия прежнего прода.
Быстрый путь: `clasp redeploy …hXNOqw -V 76`. Снимки НЕ удалялись, оба на месте.

## Остатки (не наше решение, владельцу)

1. Чужая работа реестра мозга (@77 «временный») стала боевой — снять её отдельно = стереть чужой
   код из HEAD, в версии @77 он сохранён навсегда.
2. Живая папка `/root/turbobaby-bridge-gs` теперь ПОЗАДИ прода на всю работу 29.07 + 30.07 + нашу.
   Следующий push из неё снова затрёт прод. Работать дальше — из папки слияния либо перенести её
   содержимое в живую.
