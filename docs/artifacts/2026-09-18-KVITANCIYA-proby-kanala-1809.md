# Квитанция пробы внешнего критика после починки доставки: путь цел, их сторона молчит

KVITANCIYA proby kanala 1809

дата: **2026-09-18** · полоса: ПК · задача очереди 86 · ключ Штаба 63-v
FACT: commit **8acfde0** в git log (push `730e591..8acfde0`): протокол manus теперь хранит код каждой части и ответ на досылку. Выводы стоят на двух живых протоколах самого демона, записанных после починки: `tmp/review_auto/manus_protocol.json` (17.09 22:22:11) и `tmp/review_send/manus_protocol.json` (17.09 22:44:38). Во внешний канал в этом заходе не ушло **ничего**, проб **0**

Ниже только коды, длины, числа, имена полей и служебные строки. Тела пакета и переписки здесь нет.
Временные файлы лежат в `tmp/t86_zamer_svyazi/`: выводы тестов, контрфакт на старом коде, замер храповика, текст коммита, вывод push. Ничего не удалялось. Процессы, `.env`, конфиги, база, таблицы, лист «Календарь бронирования» и сервер не тронуты.

---

## 0. Коротко

1. **Путь доставки цел до последнего шага.** В обоих заходах демона 17.09 из 10 частей 10 ушли, и все 10 лежат в теле **одной** задачи их стороны. Создание ответило кодом 200, досылки — кодом ниже 400, опросы — кодом 200. `split_error` и `part_error` пусты.
2. **Обрывается путь на их стороне, на шаге «ответить».** Задача переходит в `completed`, `credit_usage` равен 1, а сообщений ассистента **0**. Молчит только их сторона.
3. **Новое, до сих пор не замеченное.** 01.09, когда manus отвечал, задачи вела модель `manus-1.6-agent` (2 тела из 2). С 05.09 во всех записанных телах стоит `manus-1.6-adaptive` (4 из 4), и ни одного ответа нет. Наш запрос за это время не менялся: после `6cdc00e` (01.09 21:13) форма `{"prompt"[, "taskId"]}` не правилась ни одним коммитом.
4. **Второй канал, codex, лежит по другой причине.** Их сторона отвечает кодом **400** `invalid_request_error`: модель `gpt-6-astra` требует более новой версии Codex, а на ПК стоит CLI **v0.151.0**. Такой ответ лежит в 21 файле лотка, первый — от 12.09.
5. **Проба не делалась.** Протоколов хватило. К тому же код её сейчас не разрешает: ближайшая проба ступени A не раньше `2026-09-17T21:17:15Z`, то есть 18.09 04:17 местного.

## Премисы

| # | премиса | вердикт | чем |
|---|---|---|---|
| П1 | витрина за 17.09: заходов 4, ответов 0, ответило 0 из 2 каналов, находок 0, оба канала лежат | **ВЕРНА как показ; число заходов занижено устройством счёта** | Резервная копия узла витрины `tmp/brain_backup_vitrina_pc_20260918-004207.txt`: «внешний контур за 2026-09-17: заходов 4 · ответов 0 · каналов ответило 0 из 2 · находок 0 · КАНАЛ ЛЕЖАЛ: codex, manus». Витрина считает **файлы** лотка с датой отправки 17.09 (`contour_digest`, `attempts = len(rows)`). Повтор ступени F перезаписывает файл того же пакета и канала, поэтому живых обращений 17.09 было **13**: 4 пробы ступени A × 2 канала, 4 повтора F (10:35 codex, 10:46 manus, 22:28 codex, 22:39 manus) и ручная проба задачи 74 в 16:39 (manus) |
| П2 | резка починена, все части одного пакета уходят в одну задачу их стороны | **ВЕРНА, подтверждено живьём у демона** | Демон стартовал 22:15:01 на `eaa22b2`, коммит содержит `7515d6c` и `d6ca969`. В обоих его заходах `parts` 10, `parts_sent` 10, `parts_seen` [1…10], одна задача, `split_error` пуст (п.1) |
| П3 | помощник не запускается: кредит 1, сообщений 0 | **ВЕРНА числами, причина уточнена** | Кредит 1 и ассистент 0 — в 3 из 3 заходов после починки. Слова «не запускается» API не показывает. Видно другое: заголовок задачи их сторона переписывает, значит какая-то модель текст читала. И модель задачи сменилась с `manus-1.6-agent` (когда ответы были) на `manus-1.6-adaptive` (когда их нет), п.3 |

---

## п.1. Путь пакета по шагам

Пакет `docs/review_outbox/2026-09-17-chain-pc-2026-09-16-56.md`. По строке демона 22:22:44 в нём 14 382 знака (статус ok).

| шаг | ступень A, проба лежачего (17.09 22:17) | ступень F, повтор (17.09 22:39) |
|---|---|---|
| задача их стороны | `PN58J4p4UjDZj5VuB4dj9d` | `DiKrt9UFTM8AoR2wqJznWo` |
| **частей собрано** (`parts`) | **10**, длины 2293 · 151 · 2053 · 2035 · 1021 · 2053 · 2010 · 1998 · 2021 · 552 = 16 187 | **10**, те же длины |
| **частей ушло** (`parts_sent`) | **10** | **10** |
| код на создание (часть 1) | **200** (`create_status`) | **200** |
| код на досылки 2…10 | **< 400 у всех 9** (`part_error` пуст, транспортных ошибок нет). Точного кода протокол до `8acfde0` не хранил — **НЕИЗВЕСТНО** | то же |
| **частей принято их стороной** (`parts_seen`: сообщение `user` в теле ЭТОЙ задачи начинается рамкой части) | **10 из 10** | **10 из 10** |
| задач | 1 (`split_error` пуст) | 1 |
| промежуточные ходы закрыты молча (`silent_turns`) | 9 из 9 | 9 из 9 |
| опросов GET / код последнего | 24 / **200** | 27 / **200** |
| состояние задачи | `completed` | `completed` |
| жизнь задачи (`updated_at − created_at`) | 1789658513 − 1789658259 = **254 с** (22:17:39 → 22:21:53) | 1789659859 − 1789659574 = **285 с** (22:39:34 → 22:44:19) |
| сообщений в `output`: user / assistant | **10 / 0** | **10 / 0** |
| квитанция `credit_usage` | **1** | **1** |
| ожидание `waited_sec` | 274 | 306 |
| вердикт | `unknown / channel_idle` на части 10/10 | то же |

**Где обрывается.** Сборка, резка, отправка, приём и укладка в одну задачу **целы**. Путь рвётся на последнем шаге — у их стороны, на ответе ассистента: задача закончена, а ответа нет. Наш читатель ничего не пропустил: в `output` 10 элементов, все с ролью `user`, чужих ролей 0 («чужих ролей пропущено 0» в `poll_note`).

Ручная проба задачи 74 в 16:39 (`CJbg7YrHmiQXyi6EyPtJyH`, артефакт PROBA-manus-zhivaya-1709) дала то же самое: 10/10, ассистент 0, кредит 1, жизнь 231 с. **Живых заходов нового кода 3 из 3 с одним исходом.**

## п.2. Дословно их ответ (служебная часть)

**manus, создание задачи** (`POST /v1/tasks`) — код **200**. Тело состоит ровно из трёх полей: `task_id`, `task_title`, `task_url`. Полей ошибки, статуса и модели в нём нет.

**manus, опрос результата** (`GET /v1/tasks/<id>`) — код **200**. Служебные поля тела:

```
"object":"task", "status":"completed", "model":"manus-1.6-adaptive",
"metadata":{"task_title":…, "task_url":…},
"output":[ 10 × {"status":"completed", "role":"user", "type":"message", …} ],
"credit_usage":1
```

Полей `error`, `failed`, `stopped` и сообщений ассистента нет. **Заголовок задачи их сторона переписывает** между созданием и концом: в заходе A 33 → 39 знаков, в заходе F 41 → 34 знака.

**Наша служебная строка на этот ответ** (`idle_error`, заход A, дословно):
`channel_idle: задача PN58J4p4UjDZj5VuB4dj9d закончена состоянием completed и не сказала ни слова на часть 10/10 (подтверждено 2 опросами подряд, ждали 274 с, квитанция канала 1; состояние completed, сообщений 0 (до метки отброшено 10, чужих ролей пропущено 0), текстовых кусков 0)`

**manus, ответ на досылку** — до `8acfde0` не записывался (п.3, дефект Н1).

**codex** (`codex exec`, CLI на ПК). Шапка запуска: `OpenAI Codex v0.151.0`, `model: gpt-6-astra`, `provider: openai`. Ответ, дословно:

```
warning: Model metadata for `gpt-6-astra` not found. Defaulting to fallback metadata; this can degrade performance and cause issues.
ERROR: {"type":"error","status":400,"error":{"type":"invalid_request_error","message":"The 'gpt-6-astra' model requires a newer version of Codex. Please upgrade to the latest app or CLI and try again."}}
```

Эта строка есть в **21** из 162 файлов codex в лотке. Первый — `2026-09-12-…digest-2026-09-12-codex.md` (12.09 08:16), канал лежит с `2026-09-12T07:22:53Z`.

## п.3. Чья половина

**Наше:**

| # | дефект | что с ним |
|---|---|---|
| Н1 | протокол manus не хранил код ответа на каждую досылку и само тело ответа на досылку, поэтому п.1 не мог назвать код части k/N | **ПОЧИНЕНО `8acfde0`**: поля `part_status` (код каждого POST, часть 1 — создание) и `part_replies` (тело ответа на досылку, до 400 знаков, `None` при обрыве). Тесты `test_review_send` + `test_review_outbox_queue` + `test_review_auto`: **258 OK**, новый тест `test_refused_follow_up_keeps_its_own_code_in_the_protocol`. Контрфакт: на копии старого кода оба затронутых теста падают `KeyError: 'part_status'`. Храповик неразбора по `review_send_run.py`: 5 точек до и после, вклад 0. Точные коды досылок появятся с первого захода демона на этом коммите |
| Н2 | codex на ПК устарел для модели, которую выбирает сам CLI: код полосы модель не задаёт (`--codex-model` пуст, демон его не передаёт), `gpt-6-astra` берётся из собственной настройки CLI (её не читал) | не чинится в коде: нужно либо обновить CLI (установка ПО и сеть — красная операция), либо закрепить модель, которую понимает v0.151.0, а какую — **НЕИЗВЕСТНО** без пробы. См. п.4 |
| Н3 | manus: мы не шлём ни профиль, ни режим, и модель задачи выбирает их сторона. Их выбор сменился с `agent` на `adaptive` | **риск, а не доказанный дефект**. Запрос не менялся (п.0.3). Есть ли в API поле, которое вернёт `manus-1.6-agent`, — **НЕИЗВЕСТНО**: комментарий кода говорит, что поля `mode` нет ни в v1, ни в v2, а `agentProfile` живьём не проверен. Литерал вслепую не шлю (так же решено в коде 01.09) |
| — | счёт «заходов» витрины: файл на пакет и канал перезаписывается повтором, 13 обращений показаны как 4 | называю, не правлю (не предмет задания) |

**Их (называю, не переформулирую):**

- **manus**: принимает все 10 частей кодом 200, складывает их в одну задачу, закрывает её `completed` с `credit_usage` 1 и **не пишет ни одного сообщения**. Модель задачи `manus-1.6-adaptive`. Данные по модели: 01.09 `manus-1.6-agent` в 2 телах из 2, ответы 2 и 11. С 05.09 `manus-1.6-adaptive` в 4 телах из 4 (05.09, 17.09 16:39, 22:17, 22:39), ответов 0. Точный день смены между 01.09 22:02 и 05.09 не записан: тел за 02–04.09 на диске нет.
- **codex**: код 400, `invalid_request_error`, «The 'gpt-6-astra' model requires a newer version of Codex. Please upgrade to the latest app or CLI and try again.»

## п.4. Что нужно от владельца

- **manus:** открыть в кабинете Manus задачу `PN58J4p4UjDZj5VuB4dj9d` (17.09 22:17) и посмотреть: есть ли там ответ ассистента или сообщение об остановке, кредитах или режиме, и в каком режиме она шла (Agent или Adaptive). Если не в Agent — поставить режим, в котором задачи 01.09 шли моделью `manus-1.6-agent`. **Как узнаем:** в протоколе следующей пробы демона `tmp/review_auto/manus_protocol.json` (не раньше 18.09 04:17 местного) поле `model` станет `manus-1.6-agent`, в `output` появится роль `assistant`, а вердикт перестанет быть `channel_idle`. Если же ответ виден в приложении, но API его не отдал, дело в чтении, и это возвращается к нам.
- **codex:** обновить Codex CLI на ПК (сейчас v0.151.0) тем же способом, каким он ставился: в PATH лежат `codex`, `codex.cmd`, `codex.ps1`. Это установка ПО с выходом в сеть, красная операция, её делает владелец. **Как узнаем:** в `tmp/review_auto/codex_stderr.txt` следующей пробы в шапке будет версия новее 0.151.0 и не будет строки «requires a newer version of Codex».

## п.5. Цена ожидания

Замер по журналу демона и mtime протоколов 17.09. Начало пробы ступени A — штамп `last_probe_at` (22:17:15.245 = следующая проба минус 6 ч); начало повтора F — строка «ящик Штаба» перед ним.

| что | секунд |
|---|---|
| проба ступени A целиком (codex + manus + лоток + журнал), 22:17:15.245 → 22:22:44.936 | **329.7** |
| из неё: уступка и сборка / codex / **manus** / лоток и журнал | 11.1 / 10.3 / **275.0** / 33.3 |
| повтор F в manus, 22:39:30.675 → протокол 22:44:38.645 → строка F 22:46:16.449 | канал **308.0**, всего ≤ **405.8** |
| повтор F в codex, 22:27:58.290 → 22:28:07.310 → 22:28:24.431 | канал 9.0, всего ≤ 26.1 |

**Сколько законно в сутки.** Ступень A пробует лежачий канал не чаще раза в 6 ч (`review_auto.CHANNEL_PROBE_AFTER_SEC = 6 * 3600`), обе пробы идут одним оборотом: **≤ 4 в сутки**, ≈ 4 × 330 = **1 319 с**. Ступень F повторяет сверх расписания проб, **≤ 6 в сутки на всю полосу** (`review_outbox_queue_run.RETRY_BUDGET = 6`). Если все 6 уйдут в manus, это ≈ 6 × 406 = 2 435 с. Законный потолок лежачего контура ≈ **3 754 с в сутки (4.3%)**, и каждое обращение к manus стоит квитанцию 1. На новом коде 17.09 (22:17 и 22:39) ушло ≈ 736 с.

## п.6. Бюджет

Потолок 2 700 с, доля 0.7 = 1 890 с. Старт захода по часам процесса 00:45:14 (epoch 1789667114), запись артефакта ≈ 00:57 — **≈ 700 с**, в пределах доли. На пробу не ушло ни секунды: пробы не было. Точные секунды с коммитом артефакта и журналом назовёт итоговый отчёт.

## НЕИЗВЕСТНО (честно)

- Точные коды ответов на досылки 2…10 в прошедших заходах: они не записывались. Известно только «ниже 400».
- Что видно в приложении Manus по задачам 17.09: API ответа не отдаёт, кабинет не наша полоса.
- День, когда модель задач сменилась с agent на adaptive: между 01.09 22:02 и 05.09, тел за 02–04.09 нет.
- Какая модель codex работает с CLI v0.151.0, и откуда CLI берёт `gpt-6-astra`: его настройка не читалась.
