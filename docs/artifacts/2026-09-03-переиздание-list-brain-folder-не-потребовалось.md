# Переиздание `list_brain_folder` НЕ ПОТРЕБОВАЛОСЬ: @84 жив, проба Штаба видна

**Дата:** 03.09.2026 · полоса Dispatch · заход **без единой записи в прод**
**Исход:** выкладка 02.09 на месте, откатов не было. Экшен опрошен живьём и **видит документ,
которого реестр не видит** — `shtab_box_probe`, положенный Штабом 03.09 напрямую в папку.
Основание задания: [`2026-09-03-виден-ли-реестру-документ-созданный-напрямую.md`](2026-09-03-виден-ли-реестру-документ-созданный-напрямую.md)
(исход НЕТ, 14 документов без ключа) и [`2026-09-02-ящик-берёт-задания-из-папки-по-префиксу.md`](2026-09-02-ящик-берёт-задания-из-папки-по-префиксу.md) (вариант А).
Предыдущий заход: [`2026-09-02-мост-перечисляет-папку-мозга.md`](2026-09-02-мост-перечисляет-папку-мозга.md).

---

## 1. Премиса первым действием: свежий `clasp pull` — расхождение НОЛЬ

Свежий pull в **новый** каталог `/root/bridge-folderlist-20260903` (с диска ничего не бралось).

| дерево | файлов | функций | объявлений | маршрутов (уник.) | всего `case` |
|---|---|---|---|---|---|
| **свежий `clasp pull` 03.09** | **18** | **214** | 215 | **95** | 96 |
| зеркало `bridge_prod/` (локальная копия под git) | 18 | 214 | 215 | 95 | 96 |
| каталог сборки 02.09 (что заливали) | 18 | 214 | 215 | 95 | 96 |

**Расхождение с обеими локальными копиями: 0 файлов, 0 функций, 0 маршрутов** — 18 из 18
совпали **побайтно** (sha256), «только в pull» и «только там» пусто в обе стороны.

В свежем pull **есть и функция `handleListBrainFolder_`, и маршрут `list_brain_folder`.**
`deploy/bridge_prod_diff.py` → **код 0**: прод обслуживает **@84**, HEAD == прод == зеркало,
описание версии на месте («v84 02.09.2026: list_brain_folder …»). Хвост версий: @83 23.08 →
**@84 02.09 15:10 UTC**, ничего после.

**Значит переклеивать нечего:** `clasp push`, `version` и `redeploy` в этом заходе **не звались
ни разу**, прод не двигался, откат не понадобился.

## 2. Живая проверка чтением: список приехал, все три документа на месте

Прибор — `brain_writer._get` (секреты берёт сам доверенный писатель). Первый вызов уткнулся в
`TimeoutError` на штатных 30 с — перечисление папки дороже чтения одного узла; подняли таймаут
до 240 с **в своём процессе** и добавили два повтора на транспорт. Дальше — с первой попытки.

```
list_brain_folder → ok=True
детей-файлов 49 · подпапок 1 (_archive) · ВСЕГО ДЕТЕЙ 50
count=49 · folders_count=1 · scanned=49 · truncated=False · folder_id непустой
```

| искомое | в списке | ключ в манифесте |
|---|---|---|
| `shtab_box` (узел ящика) | **ДА** | есть |
| `KB_shtab_frame` (рамка) | **ДА** | есть |
| **`shtab_box_probe`** (положен Штабом 03.09) | **ДА** | **НЕТ** |

**Третья строка — и есть ответ задания.** Проба найдена **по имени в папке**, а не через реестр:
ключа у неё нет, то есть манифестной дороги к ней не существует вовсе, и до 02.09 её id взять
было неоткуда. Живой отбор по префиксу подтверждает это одним вызовом:

```
list_brain_folder&prefix=shtab_box → count=2
  · shtab_box        id=1I4A2Oda…7Zvs
  · shtab_box_probe  id=1K9I2hUJ…rq-8
```

**Сверка с артефактом 03.09.** Детей без ключа сейчас **15**; артефакт того же дня насчитал **14**
(замер 06:41 UTC, до появления пробы). 14 + проба = 15 — сходится. Три файла, которых не было в
замере 02.09 (было 46 детей-файлов): `cowork_esign_2026-09-02_install`,
`cowork_esign_2026-09-03_closed`, `shtab_box_probe`; 46 + 3 = 49 — тоже сходится.

## 3. Прежние экшены живы: число до и после

**Выкладки не было, поэтому «до» и «после» этого захода равны по построению.** Числа названы и
сверены с предыдущей версией, чтобы было видно, что за сутки прод не двигали:

| ось | @83 (до выкладки 02.09) | **@84 (и до, и после этого захода)** |
|---|---|---|
| case-маршрутов (уник.) | 94 | **95** |
| функций | 213 | **214** |
| файлов | 18 | 18 |
| POST-отпечаток живого прода | 67 | **67** |
| имён в легаси-`help` | 9 | **9** |

Пять дверей вызваны живьём, форма ответа прежняя:

| дверь | ответ |
|---|---|
| `ping` | `ok=True`, version `1.0.0`, ключи `_status, message, ok, time, version` |
| `list_brain` | `ok=True`, **37** ключей-доков, ключи ответа `manifest, ok` |
| `read_doc name=shtab_box` | `ok=True`, **39** символов, ключи `id, name, ok, text` |
| `help` | `ok=True`, **9** имён |
| `delivery_zones_get` | `ok=True`, ключи `_status, action, config, ok, zones` |

## 4. Строка отката (не применялась)

```
ssh -i ~/.ssh/turbobaby_vps root@5.223.94.179 'cd /root/bridge-folderlist-20260902 && clasp redeploy AKfycbxNC9gCM7-a635gDMk_jtPKsBNeCcBA23uuyrWXcMWHNREANzFSnpE1kXISAYZ_hXNOqw -V 83'
```

Снимок прода до выкладки цел: `/root/_bridge_prod_snapshot_20260902_before_listfolder`.
После отката зеркало пришлось бы свести обратно
(`/root/bridge-folderlist-20260902/sync_mirror_after_manual_deploy.py`).

## 5. Хвост от 02.09 — ОТКРЫТ, повторяю числом

`tests/test_bridge_prod_mirror.py` — **14/14 зелёный**. `tests/test_undo_door.py` — **красный**,
дословно: «сборка объявлена выложенной как @83, а зеркало описывает @84 — сведи зеркало к проду».
Гейт VPS берёт тесты глобом, значит предпушевой гейт `turbobaby-manager-bot` по-прежнему не
пройдёт. Страж сверяет паспорт **чужой** сборки `bridge_build/BUILD.json` (заход двери отмены
23.08) с версией зеркала и не моделирует уход прода дальше другим заходом; правкой числа не
лечится — `build_sha256` той сборки расходится с зеркалом по `Bridge.js`. Развилка не полосы:
снять `delivered_as_version` у сборки 23.08 либо расширить модель стража.

## 6. Что НЕ тронуто

Прод не двигался (ни `push`, ни `version`, ни `redeploy`); функции и маршруты не менялись и не
переименовывались; зеркало не трогалось; таблицы учёта не открывались; процессы не
перезапускались; удалено — ничего; в git не коммитилось ничего; ключи и содержимое конфигов не
печатались.

**Временное (не удалено):** VPS — `/root/bridge-folderlist-20260903/` (свежий pull + сверка);
ПК — `_scratch_boxfolder_action_0903/` (`cmp_pull_0903.py`, `probe_live_0903.py`).
