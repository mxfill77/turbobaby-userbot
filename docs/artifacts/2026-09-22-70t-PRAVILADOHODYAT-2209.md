# 70t PRAVILADOHODYAT 2209 — откуда клиентский бот берёт правила В МОМЕНТ ответа, и сколько уроков владельца туда доходит

PRAVILADOHODYAT

- **Задание:** Штаб, ящик заданий, ключ `0017-70t.2209`, дата 22.09.2026, полоса ПК.
- **Срез замера:** рабочее дерево `D:\turbobaby-bot`, HEAD `3b8c075a` (2026-09-22 15:52:21 +0700), время замера 22.09.2026 днём.
- **Характер захода:** чтение + тест на КОПИЯХ. Боевых файлов не изменено ни байтом (сверка sha256 до/после — ниже, 4 из 4 не изменены). Наружу к человеку не ушло ничего.
- **Живого текста клиента и менеджера в артефакте нет ни строки** — только числа и признаки.

---

## РЕШЕНИЕ ОДНОЙ СТРОКОЙ

Правила в момент ответа клиенту бот берёт из **ОДНОГО файла-источника выученных правил —
`D:\turbobaby-bot\lesson_store.tsv`** (9 строк, все `актив`) плюс неучебные секции книги
`manager-bot/docs/playbook.md`. Уроков владельца, поставленных в тренажёре/экзамене, туда доходит
**НОЛЬ**: из **11 вердиктов владельца от 18.09 — 0**, из **15 строк живого набора
`exam_live/lessons.tsv` — 0** (в том числе 0 из 12 действующих). Девять доезжающих строк — не
уроки владельца, а **перенос книги от 03.09** (`источник=перенос_книги`).

Ноль здесь доказан так же строго, как доезжающие девять: положительный контроль на копии зажёгся,
отрицательный — нет, и контрфакт показал, что уроки живого набора удерживает **адрес, а не формат**.

---

## 1. ЖИВОЙ ЧИТАТЕЛЬ ПРАВИЛ — ПУТЬ ДО РЕШЕНИЯ, А НЕ ДО ИМПОРТА

Цепочка от файла до текста, который увидит клиент (все ссылки — на живой код, не на архивные копии):

| # | место | что делает |
|---|---|---|
| 1 | `suggest.py:9496` `pb = load_playbook()` | внутри `draft_from_transcript` (`suggest.py:9438`) — ОБЩЕЕ тело клиентского пути Telegram + WhatsApp |
| 2 | `suggest.py:854` `load_playbook()` | книга В ТОМ ВИДЕ, В КАКОМ ОНА ИДЁТ В ПРОМПТ |
| 3 | `suggest.py:954` `active_lesson_bullets()` | → `lesson_store.load(LESSON_BASE_PATH if path is None else path)` (`suggest.py:970`) |
| 4 | `lesson_store.py:172` `STORE_PATH` | `D:\turbobaby-bot\lesson_store.tsv` — АДРЕС читателя (живое значение `suggest.LESSON_BASE_PATH = None`) |
| 5 | `lesson_store.py:885` `active()` | отбор строго по `состояние == "актив"`; кандидаты и снятые не проходят |
| 6 | `suggest.py:983` `_put_learned_section()` | секция «## Выученные правила» книги **ЗАМЕЩАЕТСЯ** буллетами базы целиком |
| 7 | `suggest.py:9500` `generate_draft(..., playbook=pb)` | → `suggest.py:8425` `make_system_prompt(..., playbook=playbook, ...)` |
| 8 | `suggest.py:6216` `playbook_block` | блок «КНИГА ПРАВИЛ» вшивается в системный промпт (`suggest.py:6293`) |
| 9 | `suggest.py:8429` `call_llm(system, transcript)` | системный промпт уходит в голову → текст черновика |
| 10 | `suggest.py:9529` `return {…}` | запись в `moderation_ipc` → карточка модератора → одобрение → отправка клиенту |

**Чем доказано, что это решение, а не импорт.** Пункт 5 (тест на копиях) наводит читателя на копию
источника с заметной строкой и меряет СОБРАННЫЙ системный промпт: маркер в нём **= 1**. То есть
байты файла превращаются в текст промпта, который и есть вход головы. Импорт такого не даёт.

**Точек вызова `load_playbook()` в живом дереве — 4, источник у всех ОДИН:**

| вызов | путь | клиентский ответ? |
|---|---|---|
| `suggest.py:9496` | `draft_from_transcript` — TG + WA | **да** (первичный черновик) |
| `moderation_core.py:203` | `_default_regen` → `regenerate_draft` | **да** (перегенерация по директиве модератора) |
| `userbot_listen.py:232` | `_trainer_generate` | нет — тренажёр |
| `trainer_run.py:468` | прогон тренажёра | нет — тренажёр |

---

## 2. ВСЕ МЕСТА-КАНДИДАТЫ (не первое найденное)

**Где искал:** `suggest.py`; корневые `*.py` дерева ПК; поимённо — `moderation_core.py`,
`moderation_bot.py`, `userbot_listen.py`, `trainer.py`, `trainer_run.py`, `wa_bridge.py`,
`client_contour.py`, `exam_show.py`, `exam_set.py`, `lesson_store.py`, `lesson_promote.py`,
`lesson_migrate_book.py`, `lesson_regress.py`, `lesson_router.py`, `moderation_card.py`,
`style_examples.py`, `rules_decided.py`, `pc_agent.py`; папки `exam_live/`, `docs/lesson_base/`,
`manager-bot/docs/`.
**Чем искал:** поиск по содержимому (ripgrep) по образцам `lesson_store|lessons\.tsv|exam_live|
verdicts\.tsv|playbook|LESSON_BASE_PATH|load_playbook|generate_draft|make_system_prompt|
rules_decided`; инвентаризация файлов и `git ls-files`; чтение самого кода.
**На каком срезе:** рабочее дерево на 22.09.2026, HEAD `3b8c075a`.
**Рекурсивного поиска по всему дереву не запускал** — задание его запрещает; отсюда честная граница
утверждения в конце пункта.

| # | кандидат | читается в момент ответа? | чем это показано |
|---|---|---|---|
| 1 | `lesson_store.tsv` (корень) | **ДА** — секция «Выученные правила» | цепочка п.1 + маркер на копии = 1 |
| 2 | `manager-bot/docs/playbook.md` | **ДА**, но только НЕучебные секции («Стиль общения», «Факты», «Так НЕ говорим») | `load_playbook_file()` → `_put_learned_section` замещает учебную секцию базой |
| 3 | `exam_live/lessons.tsv` | **НЕТ** | в `suggest.py` слов `exam_live` / `lessons.tsv` / `verdicts.tsv` — **0 совпадений**; тест п.5 даёт 0 из 15 |
| 4 | `exam_live/verdicts.tsv` | **НЕТ** — журнал тапов | там же, 0 совпадений; журнал читают только двери тренажёра |
| 5 | `exam_live/cases.json`, `shots/`, `desk.json`, `ledger.tsv`, `rolled_back.jsonl` | **НЕТ** | принадлежат набору обучения (`exam_set.py:58`), не пути ответа |
| 6 | `rules_decided.RULES` (4 записи) | **НЕТ** | `import rules_decided` встречается ровно в одном файле — `test_rules_decided.py:23`; боевой код его не импортирует |
| 7 | 8 блоков правил КОНСТАНТАМИ КОДА `suggest.py`: `CRITICAL_FACTS`(:321), `EXPERIENCE_SAFETY_RULE`(:345), `APPROVAL_WHITELIST_RULE`(:363), `AVAILABILITY_INVARIANT_RULE`(:384), `GENERATION_DEFAULT_RULE`(:403), `PICKUP_RULE`(:422), `RECEIPT_LEXICON_RULE`(:440), `ANTI_LOOP_NOTE`(:5809) | **ДА** | склейка промпта `suggest.py:6290-6293`. **Уроком владельца не правятся ни одной веткой** — только правкой кода и коммитом |
| 8 | FAQ: Bridge (`FAQ_DOCS`) → фолбэк `manager-bot/docs/turbobaby_faq_v1.md` (`suggest.py:267`) | **ДА** | `suggest.py:6294` — блок «FAQ и эталонные формулировки» |
| 9 | Парк: Bridge → фолбэк `manager-bot/docs/park_list.md` (`suggest.py:648`) | **ДА** | `park_block`; парк не получен → черновика нет вовсе (`suggest.py:9491`) |
| 10 | Живые примеры `style_examples` по последней реплике клиента | **ДА** | `suggest.py:8428` `client_question=_last_client_line(...)` → `:6295` |
| 11 | `docs/lesson_base/playbook-2026-09-03.md` (5045 байт) + `README.md` | **НЕТ** | снимок для восстановления переноса; в пути ответа не открывается |

**Об ЕДИНСТВЕННОСТИ источника ВЫУЧЕННЫХ ПРАВИЛ — пакет, а не слово.** Адрес читателя один
(`lesson_store.STORE_PATH`); переопределяет его только поле `suggest.LESSON_BASE_PATH`, и все
совпадения этого имени в дереве, кроме объявления и самого чтения, — **тесты** (`test_lesson_read`,
`test_lesson_cancel`, `test_isolation`, `test_trainer`). Второго парсера таблицы в `suggest.py` нет
(`active_lesson_bullets` сам это объявляет и делегирует `lesson_store`). Откат чтения
`LESSON_BASE_READ_OFF` на замере **False**.
**Граница утверждения, названная честно:** «других мест нет» сказано **для среза, перечисленного
выше**, а не для всего диска — рекурсивный обход деревом запрещён заданием. За пределами этого
среза исход — **НЕИЗВЕСТНО**, а не «отсутствует».

---

## 3. ЧИСЛА ПО КАЖДОМУ ИСТОЧНИКУ

### 3.1 `D:\turbobaby-bot\lesson_store.tsv` — ЧИТАЕМЫЙ источник

| признак | значение |
|---|---|
| строк данных | **9** (+1 строка шапки, 9 колонок) |
| состояние | `актив` — **9**; кандидатов 0; снятых 0 |
| происхождение (графа «источник») | `перенос_книги` — **9**; `экзамен` — **0**; `тренажёр` — **0**; `экспорт_переписки` — **0** |
| графа «кто_записал» | `владелец` — 9 (это литерал `lesson_migrate_book.MIGRATION_WHO`, служебная метка переноса, а не след урока владельца) |
| свежайшая дата записи (графа «когда») | **2026-09-03T00:00:00Z** у всех 9 (штамп снимка книги, `lesson_migrate_book.py:262` `when=snap.stamp`) |
| mtime файла | **11.09.2026 18:58:14** (та же минута у `.bak` и `.version`) |
| sha256 | `7d19b1db…7465e6eb`, 7223 байта; `lesson_store.tsv.version` = `7d19b1db352e8f76` |
| в git | **нет** (`git ls-files lesson_store.tsv` → пусто) |
| кто в него пишет | `lesson_store.add` / `add_candidate` / `promote` / `withdraw`, а зовут их: `lesson_migrate_book.py:259`, `exam_show.py:1845-1846` (`_lesson_write`, при `LESSON_PATH=None`), `trainer.py:1489` и `:1673`, дверь `lesson_promote.py`, партия `lesson_batch.py` |
| когда писал последний раз | **11.09.2026 18:58:14**; за 11 суток после этого — ни одной записи |

### 3.2 `manager-bot/docs/playbook.md` — книга, ЧИТАЕМАЯ частично

| признак | значение |
|---|---|
| строк | **27**; секций — **4**: «Стиль общения», «Факты (кошельки/контакты)», «Так НЕ говорим (запреты)», «Выученные правила» |
| буллетов всего | **15** |
| свежайшая запись | mtime **23.07.2026 01:26:26** — два месяца без изменений |
| sha256 | `88e73178…c8ffc6dd`, 5045 байт (побайтово равен снимку `docs/lesson_base/playbook-2026-09-03.md`) |
| в git | **нет** (каталог `manager-bot/` под `.gitignore`) |
| кто в него пишет | `suggest.append_playbook_rule` / `replace_playbook_rule` / `remove_playbook_rule`; `lesson_router._style_sink`; `moderation_core` |
| что из него ДОХОДИТ | только НЕучебные секции: секция «Выученные правила» **замещается целиком** базой (`suggest.py:983`). Буллет, дописанный в книгу руками, на ответ с 06.09.2026 не влияет |

### 3.3 `exam_live/lessons.tsv` — живой набор, НЕ читаемый

| признак | значение |
|---|---|
| строк данных | **15** (9 колонок, тот же формат) |
| состояние | `актив` — **12**; `кандидат` — **3** |
| происхождение | `экзамен` — **15 из 15** |
| свежайшая дата записи | **2026-09-18T20:27:55Z** (все 15 — 18.09, девятью штампами) |
| mtime / sha256 | **19.09.2026 03:27:55**; `9b147b6c…e3155000`, 20982 байта; `lessons.tsv.version` = `9b147b6ce06e35b9` |
| в git | **нет** (весь `exam_live/` под `.gitignore`) |
| кто в него пишет | `exam_show._lesson_write` (`exam_show.py:1830`) при `LESSON_PATH = LIVE_LESSONS`, что ставит `use_live_set()` (`exam_show.py:2397`) — то есть кнопки живого набора |
| когда писал последний раз | **19.09.2026 03:27:55** |

### 3.4 `exam_live/verdicts.tsv` — журнал вердиктов, НЕ читаемый

| признак | значение |
|---|---|
| вердиктов | **11** (13 колонок) |
| состояние | `кандидат` — **11 из 11** |
| автор | `SamHold` — 11 из 11 |
| исход | `неверно` — **9**; `верно` — **2** |
| набор | `живой` — 11 из 11 |
| свежайшая запись | **2026-09-18T20:27:55Z** (11 штампов 18.09, от 16:13:10 до 20:27:55) |
| ссылки на уроки (графа «уроки») | 9 вердиктов назвали уроки 1…15; **2 вердикта без уроков** |
| mtime / sha256 | **19.09.2026 03:27:55**; `68540e73…936eaa11`, 1967 байт |
| кто в него пишет | двери вердиктов `exam_show.py` при `VERDICTS = LIVE_VERDICTS` |

---

## 4. СВОД ЧИСЛОМ: СКОЛЬКО ДОХОДИТ ДО ТЕКСТА ОТВЕТА

Замер живым читателем на живом адресе (`suggest.LESSON_BASE_PATH = None`), сравнение по тексту
графы «как_правильно» внутри собранного системного промпта (18860 символов):

| вопрос задания | ответ |
|---|---|
| из **11 вердиктов владельца 18.09** доходит до текста ответа | **0** |
| из **15 строк живого набора** доходит | **0** (из 12 действующих — **0**, из 3 кандидатов — **0**) |
| маркер-строка, вписанная в копию живого набора, в промпте | **0** |
| из **9 строк боевой базы** доходит | **9 из 9** (буллетов в секции «Выученные правила» — 9) |
| из этих 9 — уроков, поставленных владельцем в тренажёре/экзамене | **0** (все 9 — `перенос_книги`, 03.09) |
| длина книги, ушедшей в промпт | 2882 символа |

**Ноль назван прямо: до текста ответа клиенту не доходит НИ ОДИН из 11 вердиктов и НИ ОДНА из 15
строк живого набора.**

Причина — не поломка, а **объявленный развод хранилищ** (18.09.2026, задание 63-t), записанный в
самом коде: `exam_show.py:72-74` — «УРОКИ ЖИВОГО НАБОРА НА ОТВЕТЫ БОТА НЕ ДЕЙСТВУЮТ… голова читает
базу тренажёра, а не эту. Перенос урока отсюда в боевую базу — отдельное движение владельца, а не
побочный эффект тапа». Это движение с 18.09 не совершалось ни разу: боевая база не менялась с
11.09, и строк с `источник=экзамен` в ней **0**.

Второе следствие, которое стоит назвать: даже попади эти строки по адресу, **3 из 15 —
кандидаты**, а кандидатов `lesson_store.active` не пропускает ни одной веткой. Потолок этого
набора на сегодня — **12**, а не 15.

---

## 5. ОТРИЦАТЕЛЬНЫЙ ТЕСТ НА КОПИЯХ (боевое — только чтение)

**Место для временного названо явно:** `tmp/pravila_probe/`. Прибор — `tmp/pravila_probe/probe_pravila.py`,
копии — `copy_lesson_store.tsv`, `copy_exam_lessons.tsv`. Ничего не удалялось, уборки за собой нет.

Устройство: в КОПИЮ каждого источника дописана одна заметная строка (`состояние=актив`), затем
живой читатель `load_playbook()` наводится на копию, и результат прогоняется через
`make_system_prompt` — меряется СОБРАННЫЙ системный промпт, а не промежуточная строка.

| опыт | что подставлено читателю | маркер базы | маркер экзамена | вывод |
|---|---|---|---|---|
| **А** | ничего (живой адрес) | **0** | **0** | ни одна из проб в живой промпт не попадает |
| **Б** | копия боевой базы (9 + маркер = 10 действующих) | **1** | — | **источник `lesson_store.tsv` читателем ЧИТАЕТСЯ** |
| **В** | копия живого набора (12 + маркер = 13 действующих) | — | **1** | контрфакт: читай он этот файл — дошли бы **12 из 12** действующих |

Опыт **В** — самая ценная строка таблицы: он отделяет две разные причины нуля. Формат живого
набора читателем разбирается без единой жалобы (`база прочитана = True`, 13 буллетов), значит
уроки владельца держит снаружи **АДРЕС, а не брак файла**. Вывод «источник читателем не читается»
здесь — доказательство, а не догадка.

**Сверка отпечатка боевых файлов ДО и ПОСЛЕ прогона — 4 из 4 не изменены:**

| файл | sha256 | байт | исход |
|---|---|---|---|
| `lesson_store.tsv` | `7d19b1db…7465e6eb` | 7223 | НЕ ИЗМЕНЁН |
| `exam_live/lessons.tsv` | `9b147b6c…e3155000` | 20982 | НЕ ИЗМЕНЁН |
| `exam_live/verdicts.tsv` | `68540e73…936eaa11` | 1967 | НЕ ИЗМЕНЁН |
| `manager-bot/docs/playbook.md` | `88e73178…c8ffc6dd` | 5045 | НЕ ИЗМЕНЁН |

---

## ПРЕМИСА ШТАБА — ПРОВЕРЕНА ПЕРВЫМ ДЕЙСТВИЕМ

| утверждение премисы | замер 22.09 | исход |
|---|---|---|
| `lesson_store.tsv` — 9 строк | 9 | **верно** |
| у всех источник `перенос_книги` и время 2026-09-03 | 9 из 9 / 9 из 9 | **верно** |
| уроков владельца нет ни одного | строк с `источник=экзамен`/`тренажёр` — 0 | **верно по сути**; уточнение: графа «кто_записал» у всех девяти несёт литерал `владелец` — это служебная метка переноса (`MIGRATION_WHO`), и по ней легко ошибиться в обратную сторону |
| `exam_live/lessons.tsv` — 15 строк, 12 актив, 3 кандидат | 15 / 12 / 3 | **верно** |
| `exam_live/verdicts.tsv` — 11 вердиктов от 18.09, все кандидат | 11 / 18.09 / 11 из 11 | **верно** |
| `manager-bot/` — июльский снимок, в git не числится, основанием не является | `git ls-files` → пусто; mtime книги 23.07.2026 | **верно**, но с поправкой, которую нельзя пропустить: файл `manager-bot/docs/playbook.md` **боевым путём ЧИТАЕТСЯ** (`suggest.PLAYBOOK_FILE`) — три его НЕучебные секции идут в промпт. Основанием для суждения об уроках он не является, но «не влияет на ответ» про него сказать нельзя |

---

## ЧЕГО ЭТОТ ЗАХОД НЕ ДЕЛАЛ И НЕ УТВЕРЖДАЕТ

- **Ничего не чинил** — задание этого и не просило. Ни один боевой файл, ни одна строка бота не
  правились; клиентский контур остался замороженным.
- **Не судил, ПРАВИЛЬНО ли устроен развод хранилищ.** Факт установлен, оценка — не предмет захода.
- **Не мерил ОТВЕТ головы** — мерился системный промпт (вход головы). Утверждение «урок изменил
  текст, который прочёл клиент» требует живого прогона головы и здесь не делается.
- **Не обходил дерево рекурсивно** (запрещено заданием): полнота перечня кандидатов заявлена для
  названного среза, за его пределами — НЕИЗВЕСТНО.
- Про `docs/PROJECT_OPERATING_FRAME.md` и прочие хранимые проекции здесь не судится ничего.

## АРИФМЕТИКА БЮДЖЕТА (как просило задание)

Единиц работы — 6. Замер на первых двух: пп.1–2 обошлись примерно в 0.06 M токенов контекста;
средняя цена единицы по факту всех шести ≈ 0.03 M. Шесть единиц ≈ 0.18 M против потолка 15 M —
это **≈1.2 % потолка**, то есть 0.7 потолка не выбрано и близко. Остановка по арифметике не
потребовалась, третий пункт начат законно.

## СЛЕД НА ДИСКЕ

- прибор и копии: `tmp/pravila_probe/probe_pravila.py`, `tmp/pravila_probe/copy_lesson_store.tsv`,
  `tmp/pravila_probe/copy_exam_lessons.tsv` (ничего не удалено);
- черновик доклада: `tmp/pc_report/task40-18772-1790068227434-40.md`;
- этот артефакт: `docs/artifacts/2026-09-22-70t-PRAVILADOHODYAT-2209.md`.
