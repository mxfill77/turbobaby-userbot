# Чтение назад: доехал ли О8 (`e5f581a`) в демон — 19.08.2026

Заход **read-only**. Кода не правил, гейт не гонял, процессы не трогал, ничего не удалял,
`.env` и конфиги не читал, файла счёта серии и состояния ожиданий не касался, клиентского
контура не касался. Единственная запись — строка `DONE` в `cc_log` и пульс (разрешены заданием).

Адрес результата: `[result_ref: file docs/artifacts/2026-08-19-deliver-o8-readback.md]` — этот файл.

**ВЕРДИКТ: ДОСТАВЛЕНО.** Признаки 1 и 2, названные заходом доставки (§7 артефакта
`2026-08-19-deliver-o8.md`), сошлись оба; пульс переведён в зелёный.

---

## 0. Признак сделанности

```
$ ls -la /root/turbobaby-manager-bot/docs/artifacts/2026-08-19-deliver-o8-readback.md
ls: cannot access '...2026-08-19-deliver-o8-readback.md': No such file or directory
```

Артефакта не было — заход не повтор. Рядом лежит `2026-08-19-deliver-o8.md` (сам заход доставки),
он и прочитан первым как источник двух признаков.

## 1. Главный номер процесса и время старта — ДОСТАВЛЕНО

```
$ systemctl show orchestrator-daemon -p MainPID -p ExecMainStartTimestamp -p ExecMainStartTimestampMonotonic -p ActiveState -p SubState -p NRestarts -p Result
ActiveState=active
SubState=running
MainPID=1358703
Result=success
NRestarts=0
ExecMainStartTimestamp=Wed 2026-08-19 05:40:59 UTC
ExecMainStartTimestampMonotonic=2223489441398
```

```
$ git log -1 --format=%H%n%cI%n%ci%n%s e5f581a
e5f581adc71526018fea153299055fcb61d687cf
2026-08-18T18:45:20Z
2026-08-18 18:45:20 +0000
О8 «полоса не может выполнить ни одного захода» поднято на VPS: судится ПОДРЯД идущая череда внешних отказов, порог 2 взят замером (при 3 не ловится сам повод), в теле тревоги — ДОСЛОВНЫЙ ответ внешней системы
```

| факт | значение |
|---|---|
| главный номер процесса ДО доставки (назван §7) | **1328816** |
| главный номер процесса СЕЙЧАС | **1358703** — сменился |
| старт юнита | **2026-08-19 05:40:59 UTC** |
| коммит `e5f581a` | 2026-08-18 18:45:20 UTC |
| **старт минус коммит** | **+10 ч 55 мин 39 с** — строго позже |

Оба признака §7 выполнены: старт **моложе 19.08 05:40 UTC**, главный номер **не 1328816**.
Отложенная единица отработала.

**Проверено шире, чем просил вопрос:** перезапуск поднимает в память ВЕСЬ HEAD, поэтому старт
сверен со всеми коммитами вершины, а не только с судимым.

```
$ git log --since="2026-08-18 12:00" --format="%h %cI %s"
323a8ef 2026-08-19T05:02:06Z артефакт изоляции метки доставки: дописан замок полного гейта 223/223…
1bc5f78 2026-08-19T04:44:54Z изоляция метки доставки в двух тестах гейта: конверт доставки больше не красит собственный гейт
2dfd4fd 2026-08-18T19:23:01Z разведка внешних отказов…
e25310b 2026-08-18T18:48:02Z догон сьюта О6: читателей журнала полосы стало ЧЕТВЕРО…
e5f581a 2026-08-18T18:45:20Z О8 «полоса не может выполнить ни одного захода» поднято на VPS…
```

Старт 05:40:59 позже вершины `323a8ef` (05:02:06) на 38 мин 53 с и позже `1bc5f78` (04:44:54)
на 56 мин 5 с. Ни одного коммита между стартом и «сейчас» нет — недоставке взяться неоткуда.

## 2. Чистый ли старт: обрыва нет, оборот идёт

**Юнит:**

```
$ journalctl -u orchestrator-daemon --since "2026-08-19 05:40:00" --no-pager -n 60
Aug 19 05:40:56 splinter systemd[1]: Stopping orchestrator-daemon.service - TurboBaby Orchestrator Daemon (ступень1 заход2а — исполнитель очереди задач)...
Aug 19 05:40:59 splinter systemd[1]: orchestrator-daemon.service: Deactivated successfully.
Aug 19 05:40:59 splinter systemd[1]: Stopped orchestrator-daemon.service - TurboBaby Orchestrator Daemon (ступень1 заход2а — исполнитель очереди задач).
Aug 19 05:40:59 splinter systemd[1]: orchestrator-daemon.service: Consumed 25min 38.611s CPU time over 14h 43min 38.546s wall clock time, 638.5M memory peak.
Aug 19 05:40:59 splinter systemd[1]: Started orchestrator-daemon.service - TurboBaby Orchestrator Daemon (ступень1 заход2а — исполнитель очереди задач).
```

`Deactivated successfully` (не `Failed`), `Result=success`, `NRestarts=0` — остановка штатная,
подъём один, петли рестарта нет.

**Порядок самомодификации соблюдён и это видно строкой:** предыдущий заход (задача 31) успел
отчитаться ДО того, как его погасил его же рестарт —

```
2026-08-19 05:40:56,263 INFO получен сигнал 15 — останавливаюсь после текущей итерации
2026-08-19 05:40:56,665 INFO id=31 claude -p погашен ПЛАНОВЫМ рестартом демона (exit=143) → done с пометкой
2026-08-19 05:40:59,477 INFO COMPLETE id=31 status=done bridge_ok=True
2026-08-19 05:40:59,490 INFO === ДЕМОН ОСТАНОВЛЕН (рубильник/сигнал) ===
2026-08-19 05:40:59,722 INFO === ДЕМОН СТАРТ (poll=60s, task_timeout=600s/dev=2700s, approved_ttl=1800s, na_lifetime=86400s, auto_ops=git_push,restart_splinter, claude=/usr/bin/claude, selfheal=1, plan_adapt=1, curator=1, curator_scope=1, gate_single_sel=1, drift=4.0ч/900с, fact_ttl=21600s, model=claude-opus-5, fallback=claude-opus-4-8, executor_model=claude-opus-5, effort=xhigh, mem_gate=on(min=2560MB avail=6451MB), rss_gate=on(max=4096MB cur=0MB), proc_gate=on(max=4 cur=0), chain_cache=200) ===
```

Баннер **полный** — все ручки прочитаны, ни одна ветка не отвалилась на старте.

**Обрыва в трассировке нет ни одного:**

```
$ grep -c "Traceback" /root/turbobaby-manager-bot/orchestrator_daemon.log
0
```

Ноль за **весь файл** (2.4 МБ, вся история), не только за эту жизнь. Разбор строк с последнего
старта:

```
LINES_SINCE_START 16
LEVELS {'INFO': 3, 'WARNING': 9, 'ERROR': 2}
HAS Traceback 0
HAS Exception 0
HAS CRITICAL 0
HAS ОСТАНОВЛЕН 0
--- ERROR/WARNING kinds ---
7 | Bridge: echo-слой сбоит (HTTP 404 на redirect-echo) — ретрай
1 | Bridge timeout (>90s) for action=get_pending
1 | Bridge get_pending: timeout — backoff-ретрай 2/3
1 | Bridge request error (get_pending): HTTP 302
1 | Bridge get_pending: request_failed — backoff-ретрай 2/3
--- INFO кроме METRICS ---
2026-08-19 05:40:59,722 INFO === ДЕМОН СТАРТ (…)
2026-08-19 07:31:26,760 INFO NEW id=33 from=Filipp-328-dev text=ultrathink ЧТЕНИЕ НАЗАД: доехал ли О8 в демон. Только чтение.
2026-08-19 07:31:28,974 INFO ИСПОЛНЕНИЕ id=33 через claude -p (timeout=2700s)
```

**Все 11 некрасивых строк — внешние сбои моста**, и каждая несёт ретрай: ни одного отказа
контура, ни одной своей ошибки. Это ровно та погода, что стоит у моста третьи сутки, а не
следствие доставки.

**Оборот идёт — прямым прибором, а не по косвенным признакам.** Пульс оборота
(`_expect_pulse`, единственная новая запись всего слоя ожиданий):

```
$ cat /tmp/cc_expect_pulse/pulse.json
{"ts": 1787124589.6692326, "n": 77, "pid": 1358703, "started": 1787118059.7195454,
 "busy": {"since": 1787124688.9748375, "limit": 2700.0, "task": 33, "pid": 1358703}}
```

| поле | человеческим языком |
|---|---|
| `started` | 2026-08-19 **05:40:59 UTC** — сходится с `ExecMainStartTimestamp` до секунды |
| `pid` | **1358703** — сходится с `MainPID` |
| `n` | **77 состоявшихся оборотов** с подъёма |
| `ts` | последний оборот **07:29:49 UTC** |
| `busy` | штамп «занят задачей **33** с 07:31:28, обещал уложиться в 2700 с» — это ЭТОТ заход |

Штамп `busy` — та самая машинерия `7bcddba`: пока идёт длинный `claude -p`, О2 не читает
молчание пульса как остановку. Он стоит на моей же задаче, то есть работает.

## 3. Эпизод недоставки `e5f581a` — ПОГАШЕН

**Погас первым же прогоном яруса 2 после подъёма:**

```
$ journalctl -u expectations.service --since "2026-08-19 05:41:00" --no-pager -g verdicts
Aug 19 05:48:42 splinter python3[1358727]: {"verdicts": 0, "notes": [], "tasks": [], "closed": [], "held": [], "quiet": ["o3|e5f581a"], "journal": ["o3|e5f581a"], "dry": false, "qstate": {"write": true, "why": "состояние сменилось", "wrote": true}}
Aug 19 05:56:44 splinter python3[1358774]: {"verdicts": 0, …, "held": [], "quiet": [], "journal": [], …}
Aug 19 06:07:01 splinter python3[1358829]: {"verdicts": 0, …, "held": [], "quiet": [], "journal": [], …}
Aug 19 06:17:41 splinter python3[1358896]: {"verdicts": 0, …, "held": [], "quiet": [], "journal": [], …}
Aug 19 06:27:59 splinter python3[1358959]: {"verdicts": 0, …}
Aug 19 06:38:25 splinter python3[1359134]: {"verdicts": 0, …}
Aug 19 06:49:04 splinter python3[1359212]: {"verdicts": 0, …}
Aug 19 06:59:00 splinter python3[1359289]: {"verdicts": 0, …}
Aug 19 07:08:59 splinter python3[1359365]: {"verdicts": 0, …}
Aug 19 07:20:03 splinter python3[1359440]: {"verdicts": 0, …}
Aug 19 07:30:20 splinter python3[1359511]: {"verdicts": 0, …}
```

Форма закрытия — **дословно та**, которую заход доставки предсказал в §8, вплоть до пустого
`closed`: `verdicts` упал **с 1 до 0**, ключ ушёл из `held`, появился **один раз** в `quiet` и в
`journal`, и **НЕ** в `closed` — потому что о начале эпизода владельцу не объявляли (`noted: 0`),
и закрытие пошло веткой «в мозг — всегда, владельцу — только если объявляли начало».

**Состояние наблюдателя подтверждает независимо от журнала:**

```
$ venv/bin/python3 -c "import json; d=json.load(open('/tmp/cc_expect_seen/state.json')); …"
OPEN_N 1
 OPEN o3|7e348d4 o3_unknown first 2026-08-10 13:47:32 UTC
QUIET_N 55 | last8 ['o3|e5f581a', 'o5s|1787067606', 'o3|4e6e0ce', 'o3|3d63d42', 'o3|25ade7b', 'o3|55d2f1d', 'o3|7f147a0', 'o3|107b7ce']
```

`o3|e5f581a` в `open` **отсутствует** и стоит **первым** (то есть самым свежим) в списке тихо
погашенных. Отдельная проверка вида вердиктов в файле состояния: `o3_undelivered` не встречается
**ни разу**, `o3_unknown` — один раз.

**Хронология эпизода целиком:** открыт 18.08 22:50:06 → 39 прогонов держали вердикт → закрыт
19.08 **05:48:42**, то есть через **7 мин 43 с** после подъёма демона (таймер рестартом не будят,
ближайший тик и был потолком ожидания — §8 обещал «до 10 минут»). Прожил эпизод **6 ч 58 мин 36 с**.

## 4. Сколько вердиктов вынес О8 с подъёма — **НОЛЬ**

Прямой ответ: **0**. Одиннадцать прогонов яруса 2 после подъёма — `verdicts: 0` во всех,
`held: []`, `notes: []`, `tasks: []`; эпизода с ключом `o8|…` в состоянии наблюдателя нет; в
`reports/2026-08-19/` нет ни одного файла `expect-o8-*` (и вообще ни одного `expect-*`).

**Ноль — это «нарушения нет», а не «прибор молчит», и это проверено живым опросом.** О8 —
чистая функция; спросить её текущее состояние можно, ничего не записав. Спрошено на срезе журнала
полосы ПК (момент последнего чтения, 06:48:47 UTC), с конфигом по умолчанию:

```
$ venv/bin/python3 -c "… expectations.lane_runs_state({'now':now,'pc':pc,'bridge':…}, expectations.config({}), now) …"
O8_AT_FETCH идут | {"run": 0, "limit": 2.0, "total": 12, "err": "", "err_ts": null,
                    "first": null, "last": 1787120220.0, "nums": [], "runs": [ … ]}
CFG_lane_run 2.0
```

Читается так: состояние полосы — **«идут»**, длина ПОДРЯД идущей череды внешних отказов **0** при
пороге **2**, заходов в окне **12**, последний заход 19.08 **06:17 UTC**. Порог 2 — тот самый,
взятый замером в `e5f581a`.

**Факты О8 в проде действительно собираются** (а не ветка выключена): в состоянии наблюдателя
лежит разобранный ряд заходов полосы, включая дословный ответ внешней системы у пары, ради
которой О8 и заведён —

```
"runs": [ {"num": 7,  "word": "needs_approval", "ext": false, …},
          {"num": 8,  "word": "done",   "ext": false, …},
          {"num": 9,  "word": "failed", "ext": true,
           "err": "529 Overloaded. This is a server-side issue, usually temporary — try again in a moment. If it persists, check https://status.claude.com.", …},
          {"num": 10, "word": "failed", "ext": true, "err": "529 Overloaded. …", …},
          …, {"num": 23, "word": "failed", "ext": false},
          {"num": 29, "word": "done"}, {"num": 30, "word": "done"} ]
```

Пара 9/10 (18.08 17:18 и 17:25) — та самая череда длины 2, поднявшая порог. Сегодня она вердикта
не даёт по двум независимым причинам, и обе видны в данных: череду **оборвал успех** (заходы 29 и
30 закрылись `done` уже 19.08), и сама она **ушла из окна свежести** — от 17:25 18.08 до сейчас
14 ч при окне 6 ч. Прибор молчит по факту, а не по слепоте.

**Отдельная поправка к формулировке вопроса, иначе отчёт соврал бы в удобную сторону.**
«О8 в демоне» доставлено как **согласованность памяти**, а не как новый источник вердиктов:
вердиктов О8 демон не выносит **по устройству**. Единственный зов вердиктов ожиданий во всём
демоне —

```
$ grep -n "expectations\." /root/turbobaby-manager-bot/orchestrator_daemon.py
3405:    for unit, entry in expectations.TICK_UNITS:
3445:    win = expectations.limit_env(expectations.DELIVER_WINDOW_ENV, …)
3467:            rel = expectations.norm_path(rel)
3505:        st = expectations.delivery_state(c, facts)
3506:        ok = {expectations.DELIVERED: True, expectations.UNDELIVERED: False}.get(st["state"])
6246:        notes = expectations.verdict(facts, expectations.config(os.environ))
```

Строка 6246 живёт в `_maybe_deliver_ask` и подаёт `facts = {"now": now, "delivery": facts}` —
фактов полосы ПК там нет вовсе, значит `_o8` на этом пути вернёт пустоту при любом состоянии
мира. Вердикты О8 выносит **ярус 2** (`expectations.timer`), и он был на новом коде **с первого
же тика после коммита** — что заход доставки и назвал прямо (§4). Доставкой в демон закрыт
разрыв «диск ≠ память», а не работоспособность детектора.

## 5. Число открытых требований доставки — **0**

Открытых эпизодов О3 всего **один**, и он не требование доставки:

| ключ | вид | открыт | это требование доставки? |
|---|---|---|---|
| `o3|7e348d4` | `o3_unknown` | 2026-08-10 13:47:32 UTC | **нет** — «неизвестно», третий исход |

Вердиктов `o3_undelivered` в состоянии **ноль** (проверено и разбором JSON, и прямым `grep` по
файлу состояния). `o3|7e348d4` — известный легаси-остаток, названный в CLAUDE.md и в §10 артефакта
доставки; этим заходом не трогался.

Со стороны второго прибора то же: последняя строка «доставка:» в журнале демона —

```
$ grep -n "доставка:" /root/turbobaby-manager-bot/orchestrator_daemon.log   (хвост)
21189:2026-08-19 04:06:40,803 INFO доставка: карточка 21 одобрена (✅) → задача 22 (коммит e5f581a, юниты orchestrator-daemon)
```

После неё ни одной — новых вопросов владельцу о доставке путь C не задавал.

## 6. Побочная находка: почему доставка потребовала двух заходов

Вопрос не задавался, но он объясняет жёлтый пульс и стоит одной строки. Карточка доставки
`e5f581a` была одобрена владельцем **04:06:40**, и конверт (задача 22) честно отказался:

```
$ head -n 20 reports/2026-08-19/task-22.md
**НЕ ДОСТАВЛЕНО: гейт красный (2/223) — перезапуска не делал.** Красное вызвано САМИМ фактом
доставки: метка `CC_PROD_DELIVERY` наследуется подпроцессами гейта, `test_gate_selective` и
`test_gate_single_selective` её не изолируют. Следствие: гейт будет красным у ЛЮБОГО конверта
доставки, пока два теста не изолируют метку.
```

Это чинили `1bc5f78` (04:44:54) и `323a8ef` (05:02:06), после чего заход 05:40 прогнал полный
гейт **223/223** и поставил отложенный рестарт — тот, чей исход и читался сейчас. Цепочка
сомкнулась: отказ был честным, диагноз верным, лекарство подействовало.

## 7. Что сделано с пульсом

Признаки 1 и 2 сошлись, поэтому пульс переведён в зелёный **одной строкой** (той же операцией,
что и запись `DONE` в `cc_log` — правило «пульс не расходится с журналом»):

```
$ venv/bin/python3 -c "… BridgeClient()._call('read_doc', name='pulse') …"
ok= True
2026-08-19 07:36 UTC | 🟢 | О8 ДОСТАВЛЕН в демон: PID 1358703, старт 05:40:59 UTC позже коммита
e5f581a; старт чистый, оборот идёт (77); эпизод o3-e5f581a погашен в 05:48:42; вердиктов О8 с
подъёма 0 (череда 0 при пороге 2); открытых требований доставки 0 | ничего не жду, доставка
закрыта | детали→do…
```

Запись в `cc_log`: `old=509741 → new=511083`.

## 8. Границы захода

`pretool_guard` не тронут ни строкой. Кода не правил вовсе, коммитов и пушей нет. Гейт не гонял
(прямой запрет; вердикт гейта здесь ничего не решает — дерево не менялось). Процессы не трогал:
ни рестарта, ни остановки, ни сигнала. Ничего не удалял. `.env` и конфиги юнитов не читал —
конфигурация О8 спрошена **дефолтами** (`expectations.config({})`), и это названо прямо, потому
что живое значение ручки отсюда не наблюдается. Состояние ожиданий и файл счёта серии только
читал; единственные записи за заход — строка `cc_log` и пульс, оба разрешены заданием. Клиентского
контура не касался. Скрипты в корень репозитория не клал (R17): разведка велась прямыми командами
и инлайн-разбором.

## 9. Остатки (на 19.08.2026, `docs/artifacts/2026-08-19-deliver-o8-readback.md`)

* **Живое значение ручки `EXPECT_LANE_RUN` отсюда не проверено** — читать конфиги задание
  запрещает, поэтому О8 опрошен дефолтом (порог 2). Косвенное свидетельство, что ветка не
  выключена, есть и оно сильное: журнал полосы ПК читается и факты заходов (`pc.runs`) в
  состоянии лежат разобранными, а чтение гаснет только при общем молчании всех четырёх читателей
  (О4, О6, О7, О8). Прямого доказательства это не заменяет.
* **Вердикт О8 «идут» снят на срезе 06:48:47, а не «сейчас»** — журнал полосы читается раз в час,
  и на текущую минуту прибор честно отвечает «проверить не удалось: срез журнала ПК устарел
  (46 мин назад) — полоса могла ответить после». Это штатное состояние между чтениями, а не
  дефект; гранулярность О8 — час, и она названа его же остатками.
* **Настоящей череды длины ≥2 в живом окне за наблюдение не случилось ни разу** — О8 в проде
  проверен на молчании, а не на срабатывании. Первое живое срабатывание ещё впереди.
* **Легаси-эпизод `o3|7e348d4`** (открыт 10.08, вид «неизвестно») по-прежнему висит в состоянии —
  известный остаток, этим заходом не трогался.
* **Артефакт не закоммичен** — коммиты запрещены заданием; файл лежит untracked, на сверку дерева
  с `origin/main` это не влияет (untracked в `git diff` не входит), но адрес результата живёт вне
  версий до отдельного захода. Тот же остаток был назван и заходом доставки.
* Зеркала ПК-полосы заход не касался вовсе.
