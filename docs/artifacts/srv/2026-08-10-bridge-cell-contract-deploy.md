# ВЫКЛАДКА МОСТА: контракт клетки ТО, собранный НА БАЗЕ ПРОДА (@78 → @79)

**Дата:** 10.08.2026 · **Полоса:** VPS · **Разрешение владельца:** дано (Filipp) ·
**Продолжение** остановленного захода 09.08 (`docs/artifacts/2026-08-10-bridge-gs-prod-diverged.md`).

Заход 09.08 остановился на шаге 2: контракт был написан в рабочую папку
`/root/turbobaby-bridge-gs`, а она отстала от прода — `push` из неё стёр бы мёрж 04.08
(ротация `cowork_log`, реестр мозга). Здесь контракт собран заново **на базе прода**.

## 1. База: чем доказано, что она равна проду

| что | факт |
|---|---|
| `/root/bridge-gs-prodpull-20260809-194334Z` (свежий `clasp pull` прода) | `md5sum` 17 `.js` + `appsscript.json` |
| `/root/turbobaby-bridge-merge-20260804` (мёрж 04.08) | те же 18 хешей |
| сверка | `diff` листингов хешей → **пусто**, `IDENTICAL_JS_SET` |

База взята из pull'а прода. Рабочая копия — **новый** каталог
`/root/bridge-cellcontract-20260809-q7v4` (`base/` — прод нетронутым, `build/` — прод+контракт,
`prodpull_prepush/`, `prodpull_prepush2/`, `headpull_afterpush/`, `probe/`, `tools/`).
Исходники (`bridge-gs`, `merge-20260804`, снимки) не правились ни разу.

## 2. Дельта: РОВНО {`cellState_`, опциональный `params.cells`}

**`ReadFleet.js` — перенос файла целиком ЗАКОНЕН, и это доказано, а не предположено.**
Дозаходный снимок `/root/turbobaby-bridge-gs/ReadFleet.js.bak-cellstate-20260809` имеет md5
`ae9200b181526337504173e6467ae710` — **тот же, что у прод-версии**. Значит контракт писался
поверх ровно прод-файла, и рабочая версия = прод + дельта. `cmp` молчит.

**`Bridge.js` — прод ШИРЕ отставшей папки, целиком тянуть НЕЛЬЗЯ.** Перенесён скриптом
`tools/patch_bridge_cells.py` **только блок маршрута `fleet`** (блок ищется по уникальности,
замена падает, если он не единственный): +2 строки комментария и
`getFleetStatus()` → `getFleetStatus(params.cells)`. Длина файла 21443 → 21638 символов.

Полный `diff base → build`: **два файла, 72 строки** (64 «+», 4 «−»), и ни одной вне контракта:

```
Bridge.js    +2 комментария, data: getFleetStatus() -> getFleetStatus(params.cells)
ReadFleet.js шапка getFleetStatus + аргумент withCells/wantCells
             bikes.push({...}) -> const bike = {...}; if (wantCells) bike.cells = {...}; bikes.push(bike)
             новая функция cellState_ (с докстрингом) перед formatDate
```

## 3. Инвентарь: ничего из прода не потеряно

`tools/inventory.py` разбирает **все** `.js` каталога и считает `function` и `case`:

| каталог | функций | маршрутов |
|---|---|---|
| прод-база | 200 | 93 |
| собранная база | **201** | **93** |
| отставшая рабочая папка | 183 | 88 |

- потеряно при сборке: **функций 0, маршрутов 0**;
- добавлено: **ровно одна функция `cellState_`**, маршрутов 0;
- «есть у прода, нет в отставшей папке» (то, что стёр бы слепой push) — **18 функций**
  (`coworkRotate_`, `pruneCoworkLog_`, `pruneCoworkLogUnlocked_`, `pruneCoworkLogTrigger`,
  `setupCoworkPruneTrigger`, `coworkPruneSelfTest_`, `coworkPruneSelfTestUnlocked_`,
  `coworkArchiveId_`, `coworkMaxBytes_`, `brainPlainId_`, `pruneCcLogUnlocked_`,
  `pruneReviewUnlocked_`, `pruneReviewBySizeUnlocked_`, `pruneSessionsLogUnlocked_`,
  `moveIntoBrain_`, `unregisterBrainDoc_`, `withBrainLock_`, `writeDocUnlocked_`)
  **и 5 маршрутов** (`move_into_brain`, `unregister_brain_doc`, `prune_cowork_log`,
  `setup_cowork_prune_trigger`, `prune_cowork_selftest`) — **потеряно из них 0**.

## 4. Харнесс на ТОМ ЖЕ файле, что уехал

`tests/fleet_cells_harness.js` исполняет `ReadFleet.js` в `node` с мок-листом.
`cmp build/ReadFleet.js /root/turbobaby-bridge-gs/ReadFleet.js` → байт-в-байт, значит гонялся
именно выкладываемый файл. **32 кейса, красных 0**, в том числе:
`plain.identical-to-marked-minus-cells` (разметка добавляет и ничего не заменяет),
`mirror.cellstate-vs-parsenumber` (23 значения, расхождений 0),
`regress.parsenumber-unchanged` (11 форм, расхождений 0),
`impossible.true-zero` (настоящий 0 → `value`, не `empty`).

## 5. Сверка с продом ПЕРЕД push — дважды, дрейфа нет

`clasp pull` во **временные** копии (`prodpull_prepush` в 20:35Z, `prodpull_prepush2` в 00:18Z,
между ними 3ч42м):

- свежий прод **vs база до контракта** → `diff -rq` **пусто** оба раза (`PROD==BASE_NO_DRIFT`);
- свежий прод **vs собранная база** → ровно `Bridge.js` и `ReadFleet.js`, 72 строки дельты;
- хеши свежего пулла == хешам вчерашнего (`PROD_UNCHANGED_SINCE_19:43Z`).

Сверх дельты контракта прод не разошёлся ни на строку — условие «расходится → СТОП» не наступило.

## 6. Копия-защита

**`/root/_bridge_prod_snapshot_20260809_q7v4_prepush`** (вне рабочей копии):

```
prod_before/            17 файлов ТЕКУЩЕГО прода + .clasp.json   ← материал отката
build_pushed/           то, что уехало
prod_before.md5, build_pushed.md5
prod_version_before.txt = 78
prod_deployment_id.txt  (chmod 600, в отчёт не печатается)
```

Проверено: `diff -rq prod_before  свежий_pull` → пусто (`SAFETY_EQUALS_LIVE_PROD`).

**Откат (если понадобится):** положить `prod_before/*.js` + `appsscript.json` в каталог
с `.clasp.json` → `clasp push -f` → `clasp redeploy <прод-деплой> -V 78`. Версия 78 неизменна
и хранит прежний код целиком.

## 7. Выкладка

```
clasp push -f          Pushed 17 files at 12:20:15 AM (00:20:15Z 10.08)
обратный clasp pull    HEAD_EQUALS_BUILD  (HEAD после push == собранная база)
clasp create-version   Created version 79
clasp redeploy -V 79   Redeployed <прод-деплой> @79
clasp list-deployments @HEAD и @79 «v79 10.08.2026: контракт клетки ТО …»
```

**Какая версия отвечает после выката: 79.** Прод-деплой (тот же id, что обслуживает мост, —
`clasp deploy` НЕ звался, URL не менялся) переведён с @78 на @79. Живой поведенческий признак,
а не расписка: `fleet(cells=1)` отдаёт разметку у **38 байков из 38**, до выката — у 0 из 38.
`ping.version` = `1.0.0` — это константа `CONFIG.VERSION` в коде, версией деплоя она не является
и не менялась.

## 8. Три исхода на живом Лист1 — поимённо

| исход | байк | клетка | сырое | старый ответ моста |
|---|---|---|---|---|
| `ok` (значение) | ADV 350CC BLACK PHUKET 5849 | кол.I `oil_last_km` | `'18302'` | 18302 |
| `empty` (пусто) | ADV 350CC BLACK PHUKET 5849 | кол.K `abs_last_km` | `''` | **0** |
| `mismatch` (прочерк) | CBR 650R PHUKET 4505 | кол.J `gear_last_km` | `'-'` | **0** |

Два нижних до выката были **одним и тем же нулём**.

## 9. Распределение 152 клеток ТО: ДО и ПОСЛЕ

| | ДО (09.08 и перед выкладкой) | ПОСЛЕ |
|---|---|---|
| ЗНАЧЕНИЕ | 96 | **97** |
| неразличимый НОЛЬ | 56 | — |
| ПУСТО | — | **49** |
| НЕ-ЧИСЛО | — | **6** |
| источник не прочитан | 152 | **0** |

**56 неразличимых нулей разложились: 49 пустых + 6 прочерков + 1 настоящий ноль** (49+6+1 = 56).

- 6 прочерков (`'-'`): CBR 650R PHUKET 4505 (J), CLICK 125CC PHUKET 5580 (K),
  XSR 155СС BLACK PHUKET 8949 (J, K), XSR 155СС GREEN (J, K);
- 1 настоящий ноль: XMAX 300CC NEW BLUE PHUKET 1813, кол.K — замена **была**, на нулевом пробеге;
- 49 пустых — замену не делали никогда, мерить было нечего.

Отдельно: `abs_last_km = −5000` у XMAX 300CC NEW BLACK PHUKET 8969 доехал как `value` −5000 —
транспорт не чинит и не судит, суждение выносит тот, кто считает просрочки.

## 10. Потребители: как до выката

Сняты 11 живых вызовов ДО (00:16Z) и ПОСЛЕ (06:41Z), сверка путями (`tools/compare_probe.py`):

- **`fleet` без `cells` — СОВПАЛ путь-в-путь** (38 байков, 0 с разметкой до и после).
  Это и есть главный замок: сегодняшние потребители не получили ни одного лишнего байта;
- **СОВПАЛИ**: `ping`, `clients(active)`, `delivery_zones_get`, `quote_price`,
  список действий (**66 имён до и после** — контракт маршрутов не заводит);
- **`fleet(cells=1)`: разошлось ровно 38 путей — все добавление ключа `cells`**, ни одного
  изменения существующего поля. «Добавляет, ничего не заменяет» — на живых данных;
- разошлись только **часозависимые** значения, и каждое объяснено числом:
  `hours_left` 9→2 у аренды с концом 16:00 местного (пробы в 07:16 и 13:41 местного);
  окно `finance` `from`/`to` сдвинулось ровно на прошедшее время; `daily_pulse.time`;
  `returns_soon(3)` 1→2 — в трёхдневное окно вошла аренда MT-03 300СС BLUE PHUKET 5068
  с концом **13.08 13:00** (в 07:16 окно кончалось 13.08 07:16, в 13:41 — 13.08 13:41).

**Ни одного расхождения, объяснимого выкладкой. Откат не потребовался.**

## 11. Попутно и границы захода

- снесён `/root/]x27` — перед сносом проверено `stat`: **0 байт, regular empty file**;
  единственное удаление захода;
- живые таблицы **только на чтение**: все вызовы читающие, единственный POST — заведомо
  несуществующее действие (ветка `unknown_action`, в чёрный ящик клиента не попадает);
- `.env` не открывался (секреты берут сами `bridge_client`/`dotenv` внутри процесса);
- `clasp pull` — только во временные копии; splinter не перезапускался; клиентский контур
  не трогался; полный адрес `/exec` и id деплоя в отчёт не печатались.

## 12. Что осталось

- рабочая папка `/root/turbobaby-bridge-gs` **по-прежнему отстала от прода** (теперь на мёрж
  04.08 + этот контракт). Следующий `push` из неё снова стёр бы прод. Развилка владельцу:
  перенести содержимое собранной базы в рабочую папку либо работать дальше из
  `/root/bridge-cellcontract-20260809-q7v4/build`;
- `docs/artifacts/`-файлы этого и прошлого захода лежат **вне git** (untracked), как и раньше.
