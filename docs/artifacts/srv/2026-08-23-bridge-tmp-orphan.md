# Осиротел ли чужой временный код в каталоге моста — проверка живыми фактами (23.08.2026)

**Заход read-only.** Ни одной записи: `clasp` не звался, версии не создавались, деплои не
трогались, файлы не удалялись и не двигались, код не правился, коммитов нет.

**Итог одной строкой:** прод обслуживает **@79**, всего версий **82** (`@1…@82`, пропусков нет) —
и по НОМЕРУ премиса верна; но операционная её часть **устарела на два часа**: временного кода в
каталоге моста БОЛЬШЕ НЕТ — он снят из HEAD 22.08 в **21:05:22 UTC** заходом коммита `e8830bd`, и
живой судья `deploy/bridge_prod_diff.py` отвечает кодом **0**: `зеркало == прод == HEAD`.
Снимать нечего: **0 файлов, 0 строк, 0 коммитов.**

---

## 1. ВЕРСИИ И ДЕПЛОИ — ЖИВОЙ ОТПЕЧАТОК (22.08.2026 23:13–23:26 UTC)

Снято read-only через Apps Script API теми же учётками, что у `clasp`
(`deploy/bridge_prod_recon.py` + собственная пагинация, снимки в `_scratch_bridge_orphan/`).

| факт | число |
|---|---|
| версия, которую обслуживает `BRIDGE_URL` | **@79** |
| всего версий у проекта | **82** (`@1…@82`, **пропущенных номеров 0**) |
| деплоев | **3** |

**ПОПРАВКА К ПРИБОРУ, которую нашёл этот заход:** штатный `bridge_prod_recon.py` печатает
«последние 6 из **50**» — это НЕ общее число версий, а размер ОДНОЙ страницы
(`versions.list?pageSize=50`, `nextPageToken` он не читает). Настоящее число — **82**, получено
пагинацией. Прежние отчёты, цитировавшие «50», цитировали страницу.

**Деплои дословно (когда их трогали в последний раз):**

| deploymentId | версия | обновлён | описание |
|---|---|---|---|
| `AKfycbxNC9gCM7-a…` | **@79** | 2026-08-10T06:30:47Z | `v79 10.08.2026: контракт клетки ТО…` **← ПРОД** |
| `AKfycbynbblRMLIA…` | **@82** | 2026-08-20T19:36:56Z | `TEMP 21.08 page margins task` |
| `AKfycbzaELdFO11V…` | HEAD | никогда (`1970-01-01`) | — |

**Живая версия старше 80 → эта часть премисы ВЕРНА, и сказано прямо.** Но вывод, который премиса
из неё делает («значит выкладку откатили»), фактам не соответствует — §6.

**Хвост версий и все временные за всю историю:**

```
@79  2026-08-10T06:30:20Z  v79 10.08.2026: контракт клетки ТО — cellState_ + fleet cells=1
@80  2026-08-20T18:57:30Z  TEMP 21.08 contract header task (снять сразу)
@81  2026-08-20T19:07:34Z  TEMP 21.08 contract header — apply-ready (снять сразу)
@82  2026-08-20T19:36:53Z  TEMP 21.08 page margins task
```

Временные версии — не новость этой недели, их за историю восемь: `@67`, `@70`, `@71`, `@73`
(08.07, разбор инцидента row705), `@77` (30.07, реестр мозга), `@80`, `@81`, `@82`.

**О ДАТАХ ЧЕСТНО: описания говорят «21.08», а метки UTC — 20.08 вечер.** Автор именует по
Бангкоку (UTC+7): `20.08 18:57 UTC` = `21.08 01:57` местного. Ниже всё время — UTC.

## 2. ЧТО В КАТАЛОГЕ МОСТА СЕЙЧАС

`projects/…/content` без номера версии — то самое состояние, которое перезаписал бы `clasp push`
и которое `redeploy` опубликовал бы ЦЕЛИКОМ:

* файлов **17**, временных среди них **0**;
* сканом по всем 17 исходникам подстроки `Tmp`, `tmp_`, `tmpMg`, `tmpCh`, `TMPMG` — **ни одного
  вхождения**;
* `HEAD vs @79 расходятся файлы: — (HEAD == прод)`;
* судья репозитория `deploy/bridge_prod_diff.py` — **код 0**, `зеркало == прод == HEAD.
  Выкладка ничего чужого не сотрёт.` (17/17 совпало с зеркалом и по проду, и по HEAD);
* замок зеркала `tests/test_bridge_prod_mirror.py` — **14/14 зелёных** (в т.ч.
  `test_mirror_files_match_passport`, `test_bridge_gs_is_disarmed`).

**Отрицательный тест по живому проду:** POST с несуществующим действием отдаёт список дверей
задеплоенного кода — **66 действий, временных маршрутов среди них 0**; `ping` → `ok=True,
version=1.0.0`. То есть в проде временных дверей нет и не было.

**Прочие каталоги на этой машине — тоже чисто:**

| каталог | что там | временное |
|---|---|---|
| `bridge_prod/` (зеркало @79 под паспортом) | 17 файлов + `MIRROR.json`, тронуты 10.08 | **0** |
| `bridge_build/` (каталог сборки двери отмены ТО) | `Bridge.js`, `ReadFleet.js`, `ServiceUndo.js`, `BUILD.json`, 22.08 19:09 | **0** |
| `/root/turbobaby-bridge-gs/` (обезврежена 10.08) | последняя правка 10.08, `.clasp.json.DISARMED-…` на месте | **0** |

## 3. ЧТО ЛЕЖАЛО В @80/@81/@82 — И ЧЕМ ПРЕМИСА ОШИБАЕТСЯ В НОМЕРАХ

Содержимое версий прочитано напрямую (`content?versionNumber=N`, версии Apps Script неизменяемы):

| версия | файлов | временный файл | знаков | добавка в `Bridge.js` |
|---|---|---|---|---|
| @79 (прод) | 17 | — | — | — |
| @80 | 18 | `TmpContractHeader` | 7969 | **+7 строк**: `tmp_hdr_read`, `tmp_hdr_apply` |
| @81 | 18 | `TmpContractHeader` | 9391 | **+7 строк**: те же два маршрута |
| @82 | 18 | `TmpPageMargins` | 5698 | **+7 строк**: `tmp_mg_read`, `tmp_mg_set_right` |
| HEAD (сейчас) | 17 | — | — | — |

**Это ДВЕ РАЗНЫЕ одноразовые задачи, а не одна.** Премиса говорит «временный код задачи полей
договора деплоили версиями 80-82» — по фактам **поля страницы жили только в @82**; `@80`/`@81`
несли задачу ШАПКИ договора (`TmpContractHeader`, маршруты `tmp_hdr_*`), и её модуль автор убрал
из HEAD ещё до нарезки `@82` — в самой `@82` его уже нет.

Пометка «снять сразу» стоит в описаниях **@80 и @81**; у **@82** описание без неё —
`TEMP 21.08 page margins task`. Дословная добавка @82 в диспетчер:

```js
      // === Одноразовые маршруты задачи «поля страницы шаблона договора» (21.08.2026) ===
      case 'tmp_mg_read':
        return jsonResponse(Object.assign({ action }, tmpMgRead_(body)));

      case 'tmp_mg_set_right':
        return jsonResponse(Object.assign({ action }, tmpMgSetRight_(body)));
```

## 4. ССЫЛКИ НА ВРЕМЕННЫЙ МОДУЛЬ И ЕГО МАРШРУТЫ — ЧТО ИСКАЛ И ЧТО НАШЁЛ

Искомое: `TmpPageMargins`, `tmpMg`, `TMPMG`, `tmp_mg_read`, `tmp_mg_set_right`,
`TmpContractHeader`, `tmp_hdr_read`, `tmp_hdr_apply`, а также id временного деплоя
`AKfycbynbblRMLIA…`.

| где смотрел | срез | находки |
|---|---|---|
| живой HEAD моста, все 17 файлов | 22.08 23:13 UTC | **0** |
| зеркало `bridge_prod/`, сборка `bridge_build/`, папка `turbobaby-bridge-gs/` | 22.08 | **0** |
| `bridge_client.py`, `splinter.py`, `orchestrator_daemon.py`, `tests/`, `deploy/` | 22.08 | **0** |
| живая ОЧЕРЕДЬ задач, обе полосы, все 7 статусов (79 строк: 1 открытая + 78 закрытых) | 22.08 23:22 UTC | **1** — задача **71** (`failed`, vps): временное имя стоит в ОТЧЁТЕ о заходе выкладки, не в вызове |
| мозг: реестр `list_brain` | 22.08 23:26 UTC | 36 ключей, узлов с временным именем **0** |
| журналы мозга `cowork_log` (577 260 зн.), `cc_log` (548 521 зн.), `pulse`, `queue_state` | 22.08 23:26 UTC | по одной ЖУРНАЛЬНОЙ строке в `cowork_log` (22.08 21:17 UTC — запись О НАШЕМ снятии) и в `cc_log` (22.08 19:43 UTC) |
| история git VPS, все ветки, `log -S` по содержимому | вся | **2 коммита**, оба — только документы: `5bd87a4`, `e8830bd` |
| рабочее дерево репо VPS целиком | 22.08 | 4 адреса, ВСЕ наблюдатели: 2 артефакта, снимок `_recon_undodeploy_0822/snap/head/`, отчёт задачи 71 |

**ССЫЛОК-ВЫЗОВОВ НОЛЬ** во всём, что достижимо с этой машины. Ни код моста, ни клиент, ни задача,
ни тест их не зовут. Найденное — записи О работе, а не потребители.

**НО ОДНО ЖИВОЕ УКАЗАНИЕ ЕСТЬ, И ОНО НАЗВАНО ПОИМЁННО:** деплой **`AKfycbynbblRMLIA…`**, закреплённый
на **@82**, ЖИВ и до сих пор отдаёт оба временных маршрута, включая ПИШУЩИЙ `tmp_mg_set_right`
(версии неизменяемы — снятие кода из HEAD его не касается никак). По свидетельству артефакта
`2026-08-23-bridge-head-tmp-removal.md` §4 у него есть потребитель — авторский откат правки полей
`_scratch_pagemargins_0821/set_right.py` на ПК, который разрешает адрес по МЕТКЕ описания.
**Отсюда это не проверяется** (ПК-репо на другой машине) — см. §8. Трогать его нельзя и не нужно:
`clasp undeploy` — удаление, красная зона владельца, и в это задание оно не входит.

## 5. МЕНЯЛ ЛИ КТО-НИБУДЬ ЭТИ ФАЙЛЫ ПОСЛЕ 21.08

| событие | метка UTC | кто |
|---|---|---|
| последняя ЧУЖАЯ запись в HEAD (все 18 файлов снимка «до» несут ровно её) | **2026-08-20T19:36:37Z** | автор, момент нарезки `@82` |
| создание последней версии `@82` | 2026-08-20T19:36:53Z | автор |
| обновление временного деплоя `@82` | 2026-08-20T19:36:56Z | автор |
| снимок HEAD нашим заходом (модуль ещё на месте) | 2026-08-22T19:30:48Z | мы |
| **снятие временного кода из HEAD** | **2026-08-22T21:05:22Z** | мы (`e8830bd`) |
| `updateTime` проекта сейчас | 2026-08-22T21:05:23Z | мы |

**Свежей чужой правки НЕТ: между 20.08 19:36 UTC и нашим снятием 22.08 21:05 UTC — 49 ч 29 мин, и
за это окно чужая рука файлов не касалась ни разу.** Версий после `@82` не создавалось,
деплоев после 20.08 19:36:56 никто не трогал. Признака «работа продолжается» нет ни одного.

Честная оговорка о приборе: метка записи в Apps Script — **проектная, а не пофайловая** (все файлы
одного `updateContent` несут одну и ту же), и `lastModifyUser` у всех записей одна учётка
`info@turbophuket.com` — её делят обе машины и `clasp`. Поэтому «кто именно правил ИМЕННО этот
файл» API не отвечает в принципе; различаются МОМЕНТЫ записи, и их хватает.

## 6. ЧТО РОВНО НУЖНО, ЧТОБЫ НАША ВЫКЛАДКА СТАЛА ОБЫЧНОЙ — ОДНОЙ СТРОКОЙ

**Снимать: 0 файлов, 0 строк, 0 коммитов — снятие уже сделано** (22.08 21:05:22 UTC, ОДНА операция
`projects.updateContent` над HEAD, зафиксирована коммитом `e8830bd`); чтобы выкладка стала обычной,
осталось не снятие, а штатный порядок: «да» владельца на `clasp push` + `clasp redeploy` (красная
зона), каталог сборки ВНЕ репо из **3 файлов** (`Bridge.js`, `ReadFleet.js`, `ServiceUndo.js` —
`bridge_build/BUILD.json`, база `@79`, `base_sha256` с зеркалом сходится), один полный гейт и
обновление зеркала `bridge_prod/` + `MIRROR.json` после выкладки.

## 7. ВЕРДИКТ ПО ПРЕМИСЕ — ПО ЧАСТЯМ, БЕЗ ВЕЖЛИВОСТИ

| часть премисы | вердикт | чем доказано |
|---|---|---|
| «прод сидит на 79» | **ВЕРНО** | живой отпечаток: прод-деплой `AKfycbxNC9gCM7-a…` = `@79` |
| «живая версия старше 80» | **ВЕРНО** | @79 < @80 |
| «временный код задачи ПОЛЕЙ договора деплоили версиями 80-82» | **НЕВЕРНО в номерах** | поля — только `@82`; `@80`/`@81` — другая задача, ШАПКА договора (`TmpContractHeader`, `tmp_hdr_*`) |
| «с пометкой „снять сразу“» | **ВЕРНО НАПОЛОВИНУ** | пометка в описаниях `@80`/`@81`; у `@82` её нет |
| «его выкладку уже откатили» | **НЕВЕРНО** | отката не было ни одного: прод НИКОГДА не съезжал с `@79` (деплой обновлён 10.08 и с тех пор не тронут), временный код жил на ОТДЕЛЬНОМ деплое `@82`, который жив и сейчас |
| «в каталоге осталась только неубранная копия исходника» | **УСТАРЕЛО** | было верно до 22.08 21:05:22 UTC; сейчас в HEAD 17 файлов и ноль временного |
| «…и она блокирует нашу выкладку» | **НЕВЕРНО СЕЙЧАС** | судья `bridge_prod_diff.py` — код 0, `зеркало == прод == HEAD` |

**Сводно: догадка Штаба угадала СОСТОЯНИЕ прода и промахнулась по МЕХАНИЗМУ и по СРОКУ.** Прод
старше 80 не потому, что откатили выкладку, а потому, что временный код туда и не выкладывали —
для него подняли отдельный версионный деплой; а «неубранная копия» перестала существовать за
два часа до постановки задачи. Это тот же класс, что записан в `CLAUDE.md` («утверждение о
состоянии пережило свою правду») и что вскрылся вчера у задачи 72 — премиса пришла на уже
сделанную работу.

## 8. ЧТО ОСТАЛОСЬ НЕПРОВЕРЕННЫМ (честное НЕИЗВЕСТНО)

1. **ПК-репо `D:\turbobaby-bot` с этой машины недоступно.** Живой потребитель временного деплоя
   (`_scratch_pagemargins_0821/set_right.py`) и `clasp_prod_pins.json` — по свидетельству
   артефакта 23.08, своими глазами не видел. Поэтому утверждение «на @82 держится авторский
   откат» здесь ПЕРЕСКАЗАНО, а не проверено.
2. **Авторство записей API не различает** (одна учётка на обе машины) — «чужая рука» в §5
   выведена из МОМЕНТА записи и из того, что наш собственный заход в это окно не писал.
3. **Что автор делает прямо сейчас**, видно только через журнал `cowork_log`; после 20.08 про эти
   файлы там одна запись — наша, о снятии. Молчание журнала не есть доказательство бездействия.
4. **Судьба деплоя `@82`** не решалась и не предлагалась: снятие его — `clasp undeploy`, то есть
   удаление и красная зона владельца.
5. **Содержимое версий @1…@78** на временный код не сканировалось — вопрос был про сегодняшний
   каталог, а не про историю.

## 9. ГРАНИЦЫ ЗАХОДА

Записей — ноль: `clasp` не звался ни разу, `projects.updateContent` не вызывался, версии не
создавались, деплои не трогались, чужие файлы не удалялись и не двигались, код не правился,
коммитов нет, в рабочие таблицы и зеркало обслуживания не писали, данные 5960 не трогали, сервисы
не перезапускались. Все обращения к Google — только `GET` (`projects`, `projects/content`,
`projects/versions`, `projects/deployments`) плюс один read-only POST-зонд списка действий и
чтения мозга/очереди. Временное — в `_scratch_bridge_orphan/` (снимки HEAD и версий, сырые ответы
API, скрипты чтения), ничего не удалено.
