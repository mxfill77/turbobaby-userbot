# Позеленела ли цепочка с адресом вида `brain` — факт из живого файла счёта (18.08.2026)

**Заход read-only.** Ничего не правилось, не удалялось, не деплоилось; гейт не гонялся; в узлы мозга
не писалось; `.env` и конфиги не читались; процессы не трогались.

**ИСТОЧНИК — ОДИН И ЖИВОЙ:** `/root/turbobaby-manager-bot/chain_series.json` (40 166 байт, mtime
`Aug 18 04:31`, `derived.updated = 2026-08-18T04:31:16Z`). Числа ниже взяты ИЗ НЕГО, а не из журнала
демона и не из `reports/*/shadow-*.jsonl`.

**ЗАМОК НЕПРИКОСНОВЕННОСТИ (файл не изменён ни на байт):**

```
$ sha256sum chain_series.json            # ДО чтения
7f4565e1940a1740eb8bc87a3be457e0f0978822097202e0ead2136cb5636aff
$ sha256sum chain_series.json            # ПОСЛЕ всех чтений
7f4565e1940a1740eb8bc87a3be457e0f0978822097202e0ead2136cb5636aff
```

**ПРИЗНАК СДЕЛАННОСТИ:** этого артефакта на диске не было —
`ls: cannot access 'docs/artifacts/2026-08-18-series-after-brainfix.md': No such file or directory`.

---

## 0. Опорные метки, которые файл даёт сам

| что | значение (из живого файла) |
|---|---|
| момент включения правила зелёного | `rule.since = 2026-08-16T18:54:56Z` |
| подъём демона с починенным судьёй | `units["orchestrator-daemon"] = 2026-08-17T16:30:34` |
| цепочек в файле / в счёте | 71 / 7 |
| прежних цепочек (старше включения правила) | 64 |
| служебных корней | 0 |
| потолок памяти файла | `SERIES_KEEP = 400` — **ничего не урезано**, перепись `brain`-адресов полна |

Пересчёт живым `chain_series` по живому файлу **совпал с `derived`, записанным демоном**, ключ в ключ:

```
=== СВЕРКА С derived (демон, updated 2026-08-18T04:31:16Z) ===
  расхождений: НЕТ — пересчёт совпал с записанным демоном
```

---

## 1. Вердикт именно этой цепочки: **ДОКАЗАН** — и это её первый `brain`-зелёный

**Цепочка 42**, полоса `vps`, создана `2026-08-15T17:55:10`, закрыта `2026-08-17T21:04:22`
(записи: корень `42` = `done`, шаг `45` = `envelope`/`done`).

**Адрес дословно, как он лежит в файле:**

```json
"refs": {
  "42": {
    "named": true,
    "state": "доказан",
    "kind": "brain",
    "pointer": "business_rules полгода не сдаём",
    "why": "узел «business_rules» содержит названное"
  },
  "45": { "named": false, "state": "неизвестно", "kind": "", "pointer": "", "why": "адрес не назван" }
},
"rule": {
  "green": true,
  "judge": "доказан",
  "why": "адрес цепочки назван и продукт по нему прочитан — зелёное устояло",
  "judge_why": "узел «business_rules» содержит названное",
  "root_named": true, "steps_named": 0,
  "at": "2026-08-17T21:04:24"
}
```

То есть в форме канона адрес звучит `[result_ref: brain business_rules полгода не сдаём]`
(живой разбор: `result_ref.parse` → `{'kind': 'brain', 'pointer': 'business_rules полгода не сдаём'}`).

### НО ЗЕЛЁНОЕ СУДЬИ И ЗЕЛЁНОЕ СЧЁТА — РАЗНЫЕ ВЕЩИ, И ЗДЕСЬ ОНИ РАЗОШЛИСЬ

Живой `chain_verdict` по этой же цепочке:

```json
{ "root": 42, "break": false, "cause": null, "weight": true, "closed": true,
  "unresolved": false, "counts": {"шум": 0, "воля": 1, "ремонт": 0, "неизвестно": 0},
  "counted": false, "service": "",
  "skip_why": "цепочка старше включения правила (создана 2026-08-15T17:55:10, правило с 2026-08-16T18:54:56) — прошлое не пересчитываем",
  "rule_state": {"judged": true, "green": true, "judge": "доказан"} }
```

**`counted = false`.** Цепочку породили 15.08, а правило зелёного включено 16.08 в 18:54:56 —
по решению владельца прошлое НЕ пересчитывается, и такие цепочки в счёт не входят ВООБЩЕ: ни +1,
ни обрыва. Она одна из тех самых **64 «прежних цепочек»**. Поэтому идущая серия как была
`[33, 34, 35, 37, 41]`, так и осталась: **судья позеленел, а метрика этого не увидела.**

Единственное вмешательство внутри цепочки — карточка 43 (`ops: ["service:orchestrator-daemon"]`),
сорт **`воля`** («названа операция операционного класса, к моменту ответа не выполненная»), а воля
по конструкции обрыва не даёт. Вес у цепочки есть: коммиты `42ef84a`, `3d63d42` и 1 перезапуск.

**КОНТРФАКТ (модель, НЕ факт — назван честно):** если бы цепочка 42 не была старше включения
правила, серия читалась бы так — идёт **6** (`[33, 34, 35, 37, 41, 42]`), вес **2 из 6 = 33.3 %`,
зачётная по-прежнему «нет» («длины нет: 6 из 30 цепочек»). Это моделирование одного поля `counted`
на живых вердиктах, а не наблюдение.

---

## 2. Доказательство: что прочитано, по какому ключу, какая подстрока, сколько заняло

* **Что прочитано:** узел мозга по ключу **`business_rules`**. Указатель адреса судья делит как
  `«<ключ> <ожидаемая подстрока>»` (`result_judge`, стр. 167): ключ `business_rules`, ожидаемая
  подстрока **`полгода не сдаём`**.
* **Что найдено:** подстрока `полгода не сдаём` НАЙДЕНА в теле узла. Слова судьи дословно —
  **«узел «business_rules» содержит названное»**.
* **Прироста длины в справке нет** — и это ожидаемо: `_len_note` дописывает «вырос N → M» только
  когда известна прежняя длина, а её (по разбору 17.08) не пишет никто. С 17.08 длина вердикта
  и не решает: доказательство = найденная подстрока.
* **Сколько заняло чтение — В ФАЙЛЕ СЧЁТА НЕ ЗАПИСАНО НИ ОДНИМ ПОЛЕМ.** Ни `refs`, ни `rule`
  длительности не несут; `shadow-*.jsonl` её тоже не пишет (в `shadow_rule.py` нет ни одного поля
  времени чтения). Единственная временная граница, которую живой файл даёт САМ: `closed_at =
  21:04:22` (метка берётся первым действием терминала) → `rule.at = 21:04:24` (метка ставится ПОСЛЕ
  того, как посчитан вердикт адреса). Значит **весь вердикт вместе с единственным чтением узла
  через мост уложился в ≤ 2 с настенного времени** — это ВЕРХНЯЯ ОЦЕНКА, а не замер чтения, и
  называть её «чтение заняло 2 с» было бы враньём того же рода, что «нуль по неразбору».
  Для сравнения: бюджет одного чтения узла `BRAIN_REF_BUDGET_SEC` по умолчанию 120 с.

---

## 3. Причина, если бы вердикт был не тот

Не применимо: вердикт **доказан**. Но контраст с прежней причиной виден в том же файле и он полный —
все ПЯТЬ прежних `brain`-вердиктов несут дословно `«узел «X» не прочитан (к мосту не ходили)»`,
а в новом этой фразы **нет вовсе**: узел прочитан.

---

## 4. Состояние серии сейчас (строка `render` из живого файла)

```
серия цепочек: идёт 5 (вес 1 из 5 наблюдаемых, перешагнула неразобранных 0), лучшая 5,
всего цепочек 7, не разобрано 0, обрывов 2 [шум 0 · ремонт 0 · отказ 0],
вес по наблюдаемым 14.3%, зачётная: нет | правило зелёного с 2026-08-16T18:54:56:
обрывов по правилу 2, служебных корней 0, прежних цепочек 64 (в счёт не входят)
```

| показатель | число |
|---|---|
| идёт | **5** (`[33, 34, 35, 37, 41]`) |
| рекорд (`best` / `best_ever`) | **5** |
| цепочек в счёте | **7** |
| обрывов | **2** |
| не разобрано / открытых | 0 / 0 |
| зачётная | нет — «длины нет: 5 из 30 цепочек» |

**Разбивка обрывов по трём названным сортам: шум 0 · воля по конструкции 0 · ремонт руками 0.**
Оба обрыва — **четвёртого сорта, `правило`** (правило зелёного), и оба несут ПРЕЖНЮЮ причину:

```
root=10 cause=правило at=2026-08-17T10:31:25 lane=vps broke_len=0
  why: зелёное снято с ЦЕПОЧКИ: корневая запись адреса не назвала либо источник не прочитан
       [вердикт адреса: неизвестно, узел «business_rules» не прочитан (к мосту не ходили)]
root=15 cause=правило at=2026-08-17T12:06:45 lane=vps broke_len=0
  why: зелёное снято с ЦЕПОЧКИ: корневая запись адреса не назвала либо источник не прочитан
       [вердикт адреса: неизвестно, узел «business_rules» не прочитан (к мосту не ходили)]
```

Оговорка честная: «воля» обрыва не даёт ПО ПОСТРОЕНИЮ (`chain_verdict`: обрыв дают шум, ремонт и
необъяснённый отказ), поэтому нуль в её колонке — свойство правила, а не результат недели.

---

## 5. Вес серии

| знаменатель | цепочек с весом | доля |
|---|---|---|
| идущая серия, наблюдаемых **5** | **1** | **20.0 %** |
| все цепочки счёта, наблюдаемых **7** | **1** | **14.29 %** |

Единственная весомая — цепочка **33** (перезапуск 1, коммитов 0). Остальные четыре чистых (34, 35,
37, 41) операционного состояния не меняли: коммитов нет, перезапусков нет.

```
цепочка 10  исход=обрыв/правило вес=False коммиты=[] рестартов=0
цепочка 15  исход=обрыв/правило вес=False коммиты=[] рестартов=0
цепочка 33  исход=чистая        вес=True  коммиты=[] рестартов=1
цепочка 34  исход=чистая        вес=False коммиты=[] рестартов=0
цепочка 35  исход=чистая        вес=False коммиты=[] рестартов=0
цепочка 37  исход=чистая        вес=False коммиты=[] рестартов=0
цепочка 41  исход=чистая        вес=False коммиты=[] рестартов=0
```

Порог веса (`WEIGHT_MIN_SHARE = 0.30` при знаменателе `WEIGHT_MIN_KNOWN = 10`) сейчас не применяется
вовсе: и длина (5 < 30), и знаменатель (5 < 10) до него не дотягивают — зачётность решается длиной.

---

## 6. Вердикты вида `brain` после 17.08 16:30:34 UTC — **ОДИН, и он «доказан»**

Полная перепись `brain`-адресов живого файла (по времени вердикта `rule.at`):

```
rule.at=2026-08-17T07:01:09  цепочка 1  (запись 1)   вердикт=неизвестно
    адрес: [result_ref: brain trainer_log РАЗМОРОЗКА 17.08: ДОКОРМ И ЦЕНА]
    слова судьи: узел «trainer_log» не прочитан (к мосту не ходили)          ПОСЛЕ рубежа? нет
rule.at=2026-08-17T10:31:25  цепочка 10 (запись 10)  вердикт=неизвестно
    адрес: [result_ref: brain business_rules ИСТОЧНИК ЦЕНЫ: ЦЕЛЕВАЯ КОНСТРУКЦИЯ]
    слова судьи: узел «business_rules» не прочитан (к мосту не ходили)       ПОСЛЕ рубежа? нет
rule.at=2026-08-17T12:06:45  цепочка 15 (запись 15)  вердикт=неизвестно
    адрес: [result_ref: brain business_rules ГИБКОСТЬ ЦЕНЫ: КОГДА УСТУПАЕМ]
    слова судьи: узел «business_rules» не прочитан (к мосту не ходили)       ПОСЛЕ рубежа? нет
rule.at=2026-08-17T14:20:54  цепочка 24 (запись 24)  вердикт=неизвестно
    адрес: [result_ref: brain shtab_frame АРИФМЕТИКА БЮДЖЕТА ДО ОТПРАВКИ]
    слова судьи: узел «shtab_frame» не прочитан (к мосту не ходили)          ПОСЛЕ рубежа? нет
rule.at=2026-08-17T14:43:51  цепочка 26 (запись 26)  вердикт=неизвестно
    адрес: [result_ref: brain handover ОЧЕРЕДЬ СЛЕДУЮЩИХ ШАГОВ 17.08]
    слова судьи: узел «handover» не прочитан (к мосту не ходили)             ПОСЛЕ рубежа? нет
rule.at=2026-08-17T21:04:24  цепочка 42 (запись 42)  вердикт=доказан
    адрес: [result_ref: brain business_rules полгода не сдаём]
    слова судьи: узел «business_rules» содержит названное                    ПОСЛЕ рубежа? ДА

brain-адресов всего: 6 ; вердиктов ПОСЛЕ 2026-08-17T16:30:34: 1
  из них доказан: 1, не доказан: 0, неизвестно: 0
```

Рубеж `2026-08-17T16:30:34` — это `units["orchestrator-daemon"]` из того же файла, то есть подъём
демона с починенным судьёй. **До рубежа — 5 вердиктов, все «неизвестно / к мосту не ходили».
После рубежа — 1 вердикт, и он «доказан».** Ни одного `brain`-вердикта между 16:30:34 и 21:04:24
файл не содержит, и урезкой это не объясняется: `SERIES_KEEP = 400` при 71 цепочке в файле.

---

## 7. Что из этого следует (и чего НЕ следует)

1. **Класс закрыт по существу:** судья впервые прочитал узел мозга и выдал `доказан` — прежняя
   причина «к мосту не ходили» исчезла из вердиктов после доставки `8fc4ef8`. Это факт, а не ожидание.
2. **Но серия этого не увидела**, и не по ошибке: цепочка 42 родилась 15.08, до включения правила
   зелёного (16.08 18:54:56), а прошлое не пересчитывается. **Первый `brain`-зелёный, который
   реально удлинит серию, ещё не случился** — его даст цепочка, РОЖДЁННАЯ после 16.08 18:54:56.
3. **Выборка одна.** Один доказанный `brain` — это один случай, а не статистика: сказать «вид
   `brain` работает» на нём нельзя, можно сказать «вид `brain` дал ДОКАЗАН впервые и без ошибок».
4. Оба живых обрыва серии (10 и 15) — цепочки, оборванные ИМЕННО прежней слепотой судьи. Задним
   числом они не пересчитываются: правило судит в момент терминала.

## ОСТАТКИ (на 18.08.2026, `chain_series.json`, sha256 `7f4565e1940a1740…`)

* **Длительность чтения узла не записывает никто** — ни `refs`/`rule` в файле счёта, ни
  `shadow-*.jsonl`. Верхняя оценка ≤ 2 с выведена из пары меток `closed_at`/`rule.at` и замером
  чтения НЕ является. Точки касания, если понадобится: `result_judge_facts.brain_fact` (там же
  живёт бюджет `BRAIN_REF_BUDGET_SEC`) и `orchestrator_daemon._series_rule_note`.
* **Прежняя длина узла (`len_before`) по-прежнему не пишется ни одним производителем**, поэтому
  справка о приросте в вердикте пуста, а забор Честертона «подстрока могла лежать в узле и ДО шага»
  остаётся снятым: его заменяет дисциплина адреса (подстрока обязана быть тем, чего до шага быть не
  могло), и это ответственность пишущего адрес, а не машины.
* **Цепочка 42 в счёт не войдёт никогда** — вносить её задним числом нельзя (метрику правит только
  живой демон, рубеж `_IS_DAEMON`), и этот артефакт остаётся единственным её следом в метрике.
* Зеркала ПК-полосы у этого замера нет: файл счёта живёт только на VPS.
* Заход read-only: артефакт на диск положен, в git НЕ коммитился и не пушился — гейт этим заданием
  запрещён, а `git push` неминуемо позвал бы его хуком `pre-push`.
