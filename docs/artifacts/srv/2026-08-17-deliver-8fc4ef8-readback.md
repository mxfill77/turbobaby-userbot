# ЧТЕНИЕ НАЗАД по доставке коммита `8fc4ef8` — 17.08.2026

Заход **только читающий**: кода не правил, ничего не удалял, живых процессов не трогал, гейт не
гонял, файл счёта серии не изменил ни на байт (sha256 до и после совпал). Ниже — дословный вывод
каждой команды и вердикт по каждому из четырёх пунктов.

**Коммит-предмет:** `8fc4ef84e30caa156c532d393300bf7fc5b14e24`, время коммита
**2026-08-17T15:59:10Z**, тема — «вид brain умеет давать ДОКАЗАН».

---

## ИТОГ ОДНОЙ СТРОКОЙ

| # | что проверялось | исход |
|---|---|---|
| 1 | новый PID демона, старт юнита моложе 15:59:10 UTC | **СОШЁЛСЯ** |
| 2 | живой прибор О3 по коммиту `8fc4ef8` | **СОШЁЛСЯ** — `доставлен` |
| — | **вывод по доставке** | **ДОСТАВЛЕН** (сошлись оба) |
| 3 | вид `brain` даёт ДОКАЗАН живьём | **ДА** — разные исходы на одном узле; **но демон им ещё не пользовался** (см. §3б) |
| 4 | счёт серии числами | идёт **1**, рекорд **2**, цепочек **6**, обрывов **3** |

---

## 1. ПРИЗНАК ПЕРВЫЙ — НОВЫЙ PID И СТАРТ МОЛОЖЕ КОММИТА

```
$ systemctl show orchestrator-daemon -p MainPID -p ActiveState -p SubState \
    -p ExecMainStartTimestamp -p ExecMainStartTimestampMonotonic -p ActiveEnterTimestamp -p NRestarts
ActiveState=active
SubState=running
ActiveEnterTimestamp=Mon 2026-08-17 16:30:34 UTC
MainPID=1285762
NRestarts=0
ExecMainStartTimestamp=Mon 2026-08-17 16:30:34 UTC
ExecMainStartTimestampMonotonic=2089664612178
```

```
$ journalctl -u orchestrator-daemon --since "2026-08-17 15:00" -n 40 --no-pager -o short-iso
2026-08-17T16:30:29+00:00 splinter systemd[1]: Stopping orchestrator-daemon.service - TurboBaby Orchestrator Daemon (ступень1 заход2а — исполнитель очереди задач)...
2026-08-17T16:30:34+00:00 splinter systemd[1]: orchestrator-daemon.service: Deactivated successfully.
2026-08-17T16:30:34+00:00 splinter systemd[1]: Stopped orchestrator-daemon.service - TurboBaby Orchestrator Daemon (ступень1 заход2а — исполнитель очереди задач).
2026-08-17T16:30:34+00:00 splinter systemd[1]: orchestrator-daemon.service: Consumed 8min 18.122s CPU time over 2h 1min 2.315s wall clock time, 577.8M memory peak.
2026-08-17T16:30:34+00:00 splinter systemd[1]: Started orchestrator-daemon.service - TurboBaby Orchestrator Daemon (ступень1 заход2а — исполнитель очереди задач).
```

**Разбор.** Старт **16:30:34 UTC** моложе коммита **15:59:10 UTC** на **31 мин 24 с** — первая
половина признака сошлась дословно.

Вторая половина («новый PID») доказана не только текущим числом `MainPID=1285762`, но и **сроком
жизни прежнего экземпляра**: строка `Consumed … over 2h 1min 2.315s wall clock time` относится к
остановленному процессу, то есть прежний экземпляр стартовал ≈ **14:29:32 UTC** — ЗАДОЛГО ДО
коммита. Процесс заменён, а не пережил правку.

Старт чистый, баннер полный (хвост боевого журнала демона):

```
2026-08-17 16:30:34,681 INFO === ДЕМОН ОСТАНОВЛЕН (рубильник/сигнал) ===
2026-08-17 16:30:34,902 INFO === ДЕМОН СТАРТ (poll=60s, task_timeout=600s/dev=2700s, approved_ttl=1800s,
  na_lifetime=86400s, auto_ops=git_push,restart_splinter, claude=/usr/bin/claude, selfheal=1,
  plan_adapt=1, curator=1, curator_scope=1, gate_single_sel=1, drift=4.0ч/900с, fact_ttl=21600s,
  model=claude-opus-5, fallback=claude-opus-4-8, executor_model=claude-opus-5, effort=xhigh,
  mem_gate=on(min=2560MB avail=6525MB), rss_gate=on(max=4096MB cur=0MB), proc_gate=on(max=4 cur=0),
  chain_cache=200) ===
2026-08-17 16:30:56,093 INFO серия: pc-карточка id=4 увидена живой, тело записано (операции: нет)
2026-08-17 16:38:44,810 INFO NEW id=35 from=Filipp-328-dev text=ultrathink ЧТЕНИЕ НАЗАД по доставке 8fc4ef8…
```

Попутно виден **штатный ход правила самомодификации**: сама задача доставки (`id=33`) была погашена
своим же отложенным рестартом и закрыта правильно, а не как падение —

```
2026-08-17 16:30:29,790 INFO id=33 claude -p погашен ПЛАНОВЫМ рестартом демона (exit=143) → done с пометкой
2026-08-17 16:30:33,943 INFO COMPLETE id=33 status=done bridge_ok=True
```

---

## 2. ПРИЗНАК ВТОРОЙ — ЖИВОЙ ПРИБОР О3

Спрошен не пересказ, а сам прибор: руки `expectations_run.delivery_facts` и решение
`expectations.delivery_state` — те же, которыми О3 живёт на таймере.

```
$ venv/bin/python3 _scratch_readback_0817/readback_o3.py
сейчас (UTC): 2026-08-17T16:42:27Z
вокабуляр прибора: DELIVERED='доставлен'  UNDELIVERED='не доставлен'  UNKNOWN='неизвестно'

== ФАКТЫ О3 ==
ok=True   коммитов в окне=30   dirty_ok=True   разошлось с origin/main файлов=0
наблюдаемые процессы:
   orchestrator-daemon    alive=True  pid=1285762  старт=2026-08-17T16:30:34Z
   splinter               alive=True  pid=1239284  старт=2026-08-16T14:30:42Z

форма записи коммита: ['ct', 'files', 'sha', 'subject']

== КОММИТ 8fc4ef8 В ОКНЕ О3 ==
  "sha": "8fc4ef8",
  "ct": 1786982350,
  "files": [
    "orchestrator_daemon.py",
    "result_judge.py",
    "result_judge_facts.py",
    "tests/test_brain_ref_provable.py",
    "tests/test_result_judge.py",
    "tests/test_rule_on_count.py",
    "tests/test_shadow_chain.py",
    "tests/test_shadow_rule.py"
  ]
время коммита (UTC): 2026-08-17T15:59:10Z

== ВЕРДИКТ ПРИБОРА О3 ==
state = 'доставлен'
done (3):
   ('orchestrator_daemon.py', 'orchestrator-daemon')
   ('result_judge.py', 'orchestrator-daemon')
   ('result_judge_facts.py', 'orchestrator-daemon')
missing (0):
unknown (0):
```

**Разбор.** `state='доставлен'`, `missing` и `unknown` ПУСТЫ — то есть замок против ложного
зелёного не сработал ни разу: дерево с `origin/main` сверено (`dirty_ok=True`, расхождений **0**),
процесс-потребитель наблюдается, старт моложе коммита.

Судимых файлов **3 из 8**: пять `tests/*.py` — «вне доставки» (`KIND_NONE`), они в память процесса
не попадают по построению. Это не умолчание прибора, а его ярлык.

### ВЫВОД ПО ДОСТАВКЕ

**Оба признака сошлись → `8fc4ef8` ДОСТАВЛЕН.** Ни «неизвестно», ни «не доставлен» здесь не
уместны: каждый признак закрыт живым фактом, а не рассуждением.

---

## 3. ТРЕТЬЕ — ДАЁТ ЛИ ВИД `brain` ДОКАЗАН В ПРОДЕ

### 3а. Живая проба: один узел, две подстроки, разные исходы

Сперва узел прочитан живьём, чтобы подстроку «заведомо есть» назвать по факту, а не по памяти:

```
$ venv/bin/python3 _scratch_readback_0817/readback_brain_look.py business_rules
узел: business_rules
read=True  broken=False  знаков=14588  чтение=2.35 с
-- первые 300 знаков --
=== БИЗНЕС-ПРАВИЛА TurboBaby (перенесено из рамки Штаба 01.08.2026) ===
Меняет владелец или менеджеры. Штаб эти правила не выдумывает —
читает отсюда. Клиентский и внутренний контуры обращаются сюда же.

ПАРК И АРЕНДА
- HONDA CLICK 125 в аренду не сдаётся никогда.
- Клиентам называть только модели р
-- последние 150 знаков --
ЗАПРЕТ БЕЗОПАСНОСТИ (не бизнес-правило, снимает только владелец)
- Никаких массовых рассылок: защищает номер userbot от бана.
=== конец документа ===
```

Затем — сам прибор, боевые `result_judge_facts.gather` + `result_judge.verdict`:

```
$ venv/bin/python3 _scratch_readback_0817/readback_brain_judge.py
вокабуляр судьи: PROVEN='доказан'  UNPROVEN='не доказан'  UNKNOWN='неизвестно'
узел: business_rules
подстрока ЗАВЕДОМО ЕСТЬ : HONDA CLICK 125 в аренду не сдаётся никогда.
подстрока ЗАВЕДОМО НЕТ  : ЧТЕНИЕ-НАЗАД-8fc4ef8-подстроки-этой-в-узле-нет-17.08.2026

plan 'business_rules HONDA CLICK 125 в аренду не сдаётся никогда.…' -> ok=True parts={'key': 'business_rules', 'expect': 'HONDA CLICK 125 в аренду не сдаётся никогда.'}
plan 'business_rules ЧТЕНИЕ-НАЗАД-8fc4ef8-подстроки-этой-в-узле-не…' -> ok=True parts={'key': 'business_rules', 'expect': 'ЧТЕНИЕ-НАЗАД-8fc4ef8-подстроки-этой-в-узле-нет-17.08.2026'}

gather: чтений узла=1  за 2.25 с   read=True  знаков=14588

== ВЕРДИКТЫ ПРИБОРА ==
подстрока: HONDA CLICK 125 в аренду не сдаётся никогда.
   state = 'доказан'
   why   = узел «business_rules» содержит названное
подстрока: ЧТЕНИЕ-НАЗАД-8fc4ef8-подстроки-этой-в-узле-нет-17.08.2026
   state = 'не доказан'
   why   = узел «business_rules» названного не содержит (прочитано 14588 знаков)

исходы РАЗНЫЕ: True
положительный == ДОКАЗАН: True
отрицательный == НЕ ДОКАЗАН: True
```

**Разбор.**

* Узел **прочитан живьём** — `read=True`, 14588 знаков, **2.25 с**. Число сошлось с замером самого
  коммита («business_rules 2.25 с при 14588»), то есть цена узла подтвердилась независимо.
* **Исходы РАЗНЫЕ и оба правильные**: названное содержится → `доказан`; названного нет → `не доказан`
  (а НЕ «неизвестно» — прибор различает «не прочитал» и «прочитал, не нашёл»).
* **Отрицательный тест честный**: подстрока-маркер собрана из хеша этого коммита и сегодняшней даты,
  до сегодня её не существовало нигде.
* **Цена названа числом**: `чтений узла=1` на ДВА адреса — `gather` спрашивает узел один раз.
* В узлы не писалось ничего: зовётся только `read_doc`.

Дверь в проде та же — в демоне прежний литерал снят:

```
$ grep -n "brain=\|BRAIN_REF_BUDGET" orchestrator_daemon.py
5652:# здесь стоял литерал `brain=False` с доводом «чтение узла доходило до 543 с (замер О5)», и
5659:# 120 с (ручка BRAIN_REF_BUDGET_SEC, «0» — как было). Адрес не назвал узла → к мосту не идём
5701:    return result_judge.verdict(ref, result_judge_facts.gather([ref], brain=True))
```

### 3б. ЧЕСТНАЯ ГРАНИЦА — САМ ДЕМОН ЭТИМ ЕЩЁ НЕ ПОЛЬЗОВАЛСЯ

Это главное, что нельзя сгладить. Проверен теневой журнал цепочек за сегодня:

```
$ venv/bin/python3 _scratch_readback_0817/readback_shadow.py
записей в журнале: 12
перезапуск демона: 2026-08-17T16:30:34 UTC

время                корень вид адреса     вердикт      после рестарта
2026-08-17T03:46:21  43     —              неизвестно   нет
2026-08-17T07:01:09  1      brain          неизвестно   нет
2026-08-17T10:31:25  10     brain          неизвестно   нет
2026-08-17T11:16:48  12     file           доказан      нет
2026-08-17T12:06:45  15     brain          неизвестно   нет
2026-08-17T13:35:16  18     —              неизвестно   нет
2026-08-17T13:42:20  18     —              неизвестно   нет
2026-08-17T14:20:54  24     brain          неизвестно   нет
2026-08-17T14:43:51  26     brain          неизвестно   нет
2026-08-17T15:14:48  28     file           доказан      нет
2026-08-17T16:07:03  31     file           доказан      нет
2026-08-17T16:30:33  33     file           доказан      нет

== ВИД brain ==
корень 1  2026-08-17T07:01:09  state='неизвестно'
   указатель: trainer_log РАЗМОРОЗКА 17.08: ДОКОРМ И ЦЕНА
   причина  : узел «trainer_log» не прочитан (к мосту не ходили)
корень 10  2026-08-17T10:31:25  state='неизвестно'
   указатель: business_rules ИСТОЧНИК ЦЕНЫ: ЦЕЛЕВАЯ КОНСТРУКЦИЯ
   причина  : узел «business_rules» не прочитан (к мосту не ходили)
корень 15  2026-08-17T12:06:45  state='неизвестно'
   указатель: business_rules ГИБКОСТЬ ЦЕНЫ: КОГДА УСТУПАЕМ
   причина  : узел «business_rules» не прочитан (к мосту не ходили)
корень 24  2026-08-17T14:20:54  state='неизвестно'
   указатель: shtab_frame АРИФМЕТИКА БЮДЖЕТА ДО ОТПРАВКИ
   причина  : узел «shtab_frame» не прочитан (к мосту не ходили)
корень 26  2026-08-17T14:43:51  state='неизвестно'
   указатель: handover ОЧЕРЕДЬ СЛЕДУЮЩИХ ШАГОВ 17.08
   причина  : узел «handover» не прочитан (к мосту не ходили)

вердиктов ПОСЛЕ перезапуска: 0
из них по виду brain: 0
```

**Что это значит дословно.** За сегодня демон вынес **5 вердиктов по виду `brain`, и ВСЕ пять — до
рестарта, все «неизвестно (к мосту не ходили)»**: это работа старого литерала `brain=False`, ровно
того дефекта, который снял `8fc4ef8`. **После рестарта вердиктов НЕТ ВОВСЕ — ни одного, ни по
какому виду**: с 16:30:34 ни одна цепочка ещё не закрылась (первая задача нового экземпляра — этот
самый заход).

Поэтому формулировка держится строго:

* **вид `brain` даёт ДОКАЗАН живьём — ДА**, это показано боевым кодом на боевом мосте, а не мокой;
* **код, который это делает, лежит в памяти демона — ДА**, это и есть вердикт О3 «доставлен» (§2);
* **но сам демон ещё ни разу этим не воспользовался** — первое живое применение впереди, на первой
  же цепочке с адресом вида `brain`, закрывшейся после 16:30:34.

Говорить «доказано в проде демоном» сегодня нельзя — это было бы то самое «неизвестно», выданное за
зелёное, против чего и построен весь прибор.

---

## 4. ЧЕТВЁРТОЕ — СЧЁТ СЕРИИ ЧИСЛАМИ

Файл открыт **на чтение**; пересчёта не делалось намеренно (пересчёт позвал бы судью, то есть мост,
и подменил бы «что в файле» на «что я насчитал сейчас»). Печатаются числа, положенные САМИМ
демоном.

```
$ sha256sum chain_series.json        # ДО чтения
45dd7c490cd21c5085020318cb0a3d52a8d6142593dd09da4329664e65d5dda0
$ sha256sum chain_series.json        # ПОСЛЕ чтения
45dd7c490cd21c5085020318cb0a3d52a8d6142593dd09da4329664e65d5dda0
```

```
$ venv/bin/python3 _scratch_readback_0817/readback_series.py
верхние ключи файла: ['chains', 'changes', 'derived', 'rule', 'started_counting', 'units', 'windows']
счёт начат: 2026-08-10T19:30:55Z

== ПРАВИЛО ЗЕЛЁНОГО ==
  "since": "2026-08-16T18:54:56Z"

== СЫРЬЁ ==
цепочек в файле: 70
из них с закрытием: 70

== ЧИСЛА, ПОЛОЖЕННЫЕ ДЕМОНОМ (derived) ==
  "chains": 6,          "current": 1,      "current_span": [41],
  "best": 2,            "best_span": [33, 34],
  "clean": 3,           "unresolved": 0,
  "current_weight": 0,  "current_known": 1,
  "weight_share": 0.1667,  "weight_chains": 1,  "weight_known": 6,
  "open": 0,
  "qualified": "нет",   "qualified_why": "длины нет: 1 из 30 цепочек",
  "rule_since": "2026-08-16T18:54:56",  "rule_breaks": 3,
  "service": 0,         "before_rule": 64,
  "updated": "2026-08-17T16:38:34Z"

строка счёта словами демона:
   серия цепочек: идёт 1 (вес 0 из 1 наблюдаемых, перешагнула неразобранных 0), лучшая 2,
   всего цепочек 6, не разобрано 0, обрывов 3 [шум 0 · ремонт 0 · отказ 0], вес по наблюдаемым 16.7%,
   зачётная: нет | правило зелёного с 2026-08-16T18:54:56: обрывов по правилу 3,
   служебных корней 0, прежних цепочек 64 (в счёт не входят)
```

### Числа, как их просили

| величина | число |
|---|---|
| **идёт** | **1** (цепочка 41) |
| **рекорд** | **2** (цепочки 33, 34) |
| **цепочек** | **6** в счёте (в файле 70: 64 прежних в счёт не входят) |
| **обрывов** | **3** — все с причиной «правило» (шум 0 · ремонт 0 · отказ 0) |
| чистых | 3 |
| не разобрано | 0 |
| вес по наблюдаемым | 16.7 % (1 из 6) |
| зачётная | **нет** — «длины нет: 1 из 30 цепочек» |
| счёт начат | 2026-08-10T19:30:55Z; правило зелёного — с 2026-08-16T18:54:56Z |

**Живой факт рядом:** `derived.updated = 2026-08-17T16:38:34Z` — то есть счёт обновлён **уже новым
экземпляром демона** (старт 16:30:34), а не остался от прежнего.

**Наблюдение, прямо связанное с §3.** Все три обрыва — по правилу зелёного, и **два из трёх**
(корни 10 и 15) оборваны дословно так:

```
"зелёное снято с ЦЕПОЧКИ: корневая запись адреса не назвала либо источник не прочитан
 [вердикт адреса: неизвестно, узел «business_rules» не прочитан (к мосту не ходили)]"
```

Это и есть цена дефекта, снятого `8fc4ef8`: серия рвалась не потому, что работа не сделана, а
потому что прибор не ходил к мосту. Третий обрыв (корень 37) — другой природы: адрес был назван
криво (`«KB_trainer_log»` без подстроки), и это правилом не лечится, это дисциплина адреса.

---

## ЧТО ОСТАЛОСЬ / ЧЕГО ЭТОТ ЗАХОД НЕ ДЕЛАЛ

* **Первого живого brain-вердикта от самого демона ещё нет** (0 вердиктов после рестарта) — он
  появится на первой закрывшейся цепочке с адресом вида `brain`. До тех пор §3 читать вместе с §3б.
* **Артефакт НЕ закоммичен и НЕ запушен**: `git push` поднял бы pre-push гейт, а задание гейт гонять
  запретило. Файл лежит на диске — этого довольно для адреса вида `file`; коммит — отдельным ходом.
* **Ничего не удалялось**, в том числе временные каталоги прошлых заходов. Читающие скрипты этого
  захода лежат в `_scratch_readback_0817/` (`readback_o3.py`, `readback_brain_look.py`,
  `readback_brain_judge.py`, `readback_series.py`, `readback_shadow.py`) — по той же конвенции, что
  у соседних `_scratch_*`/`_recon_*` каталогов.
* `.env` и конфиги не читались; боевых записей нет; живые процессы не трогались; `chain_series.json`
  не изменён (sha256 сверен до и после).
* Проба вида `brain` шла под задокументированным `BRIDGE_ALLOW_NETWORK=1`, выставленным **внутри
  скрипта**: env-префикс в оболочке сбивает allow-матч по голове команды. Сетевой запрет рубежа 3
  взводится только под `ORCH_TEST_MODE=1`, здесь это не тест-прогон, а живое read-only чтение.
