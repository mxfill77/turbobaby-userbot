# Обо что упёрлась задача 50 и что от неё осталось (22.08.2026, read-only разбор)

Заход-разборщик — задача 53 (`Filipp-328-dev`, vps, старт 2026-08-22 13:15:11 UTC). Ничего
не чинилось, не коммитилось, не удалялось; рабочее дерево не трогалось. Единственное, что этот
заход положил на диск, — ЭТОТ файл (адрес результата задания) и одна строка в `cc_log`.

---

## ДОСЛОВНАЯ ПРИЧИНА

Поле `result` строки 50 живой очереди, **889 знаков, приведено целиком, без единого сокращения**:

```
задача упала → думатель: halt, причина: Задача упёрлась в жёсткий блок гарда: исполнитель вызвал set_fleet_oil по ЖИВОЙ сущности (NMAX 155 GRE, пробег 15600) — красная зона, кнопки «да» нет по доктрине, а ТЗ прямо запрещало писать в рабочие таблицы; переформулировкой это не чинится, нужен владелец.
Перерождение не поможет (диагноз думателя выше), нужен человек.
✋ ЖЁСТКИЙ БЛОК: запись в живые таблицы по ЖИВОЙ сущности. Кнопки «да» здесь нет намеренно — по доктрине такое одобряется только для ТЕСТ-сущностей write-смока, а живую правит владелец сам.
op=other | [set_fleet_oil] 🔴 КРАСНОЕ
Что: запись «ТО масло» в Лист1 Байки (колонка I)
Объект: байк NMAX 155 GRE
Число: пробег 15600
Откат: вписать прежний пробег в колонку I руками
Проверь: тот ли байк и пробег — жду твоё «да».
[guard-block задача 50]
🔒 источник карточки: ГАРД (маркер сверен) — перехвачена реальная команда этой задачи
```

Рядом — строка журнала самого гарда (`/tmp/cc_pretool_guard.log`, последняя запись файла),
дословно:

```
{"ts": "2026-08-22 12:52:37", "event": "set_fleet_oil", "cmd": "venv/bin/python3 _scratch_works_buf/measure.py 2>&1 | tail -45", "why": "Сущность «не опознана» без пометки ТЕСТ: в живые таблицы не пишем ВОВСЕ — решение владельца вне агента"}
```

и строка демона (`orchestrator_daemon.log`), дословно:

```
2026-08-22 12:52:39,887 WARNING id=50 guard-block ЖЁСТКИЙ (живая сущность) → failed без approve: op=other | [set_fleet_oil] 🔴 КРАСНОЕ
```

---

## 1. Строка очереди целиком. Чья это задача

Снята живым `get_pending(..., lane="all")` 22.08 около 13:20 UTC (только чтение).

| поле | значение |
|---|---|
| `id` | 50 |
| `status` | **failed** |
| `lane` | vps |
| `from` | Filipp-328-dev |
| `created` | 2026-08-22T12:29:10.659Z |
| `updated` | 2026-08-22T12:53:15.359Z |
| `approved_by` | «» (пусто) |
| `task_text` | 2955 знаков |
| `result` | 889 знаков (приведён выше целиком) |

**Цель первой строкой ТЗ:**

> ЦЕЛЬ: закрыть КЛАСС молчаливой потери работ во внутреннем обслуживании одной правкой корня
> плюс сторож. Корень назван разбором 22.08: буфер отложенных инфо-работ ЗАМЕЩАЕТСЯ присваиванием
> и выгружается ТОЛЬКО дверью подтверждения пробега.

**Задача НАША**, и продолжает прямую линию сегодняшнего дня: она опирается на артефакт
`docs/artifacts/2026-08-22-service-5960-fate.md` (заход 11:49, судьба записей ТО байка
NMAX 155 GREY 5960 за 01.08 и 22.08). Адрес её результата — `[result_ref: file
docs/artifacts/2026-08-22-works-buffer-root.md]`. Семь пунктов: (1) буфер дописывается,
а не замещается; (2) выгрузка на ЛЮБОЙ двери подтверждённого пробега; (3) сторож класса
«ПРИНЯТО N · ЗАПИСАНО M» с поимённым перечнем не легших позиций; (4) регистр редуктора —
по признаку действия, а не совпадению подстроки; (5) обязательный отрицательный тест;
(6) регресс на живом формате сессий 01.08 и 22.08 с числами ДО и ПОСЛЕ; (7) доставка оранжевым
циклом с чтением назад.

Полный текст ТЗ лежит рядом дословно: `reports/2026-08-22/task-50.md`, раздел «## ТЗ».

---

## 2. Какая операция, какой объект, и место доктрины

**Команда, на которой встал блок, дословно** (она же — 54-й и последний вызов оболочки этого
захода):

```
venv/bin/python3 _scratch_works_buf/measure.py 2>&1 | tail -45
```

**Операция:** `set_fleet_oil` — запись «ТО масло» в Лист1 Байки, колонка I.
**Объект:** «байк NMAX 155 GRE», число «пробег 15600».

**Откуда взялись объект и число — важно для честности вывода.** Оба вынуты гардом из ЛИТЕРАЛОВ
самого замерочного скрипта, а не из живого вызова:

* `_scratch_works_buf/measure.py:39` — `BIKE = "NMAX 155 GREY 5960"`; регулярка объекта
  `pretool_guard._P_BIKE` берёт группу длиной не больше 12 знаков, отсюда усечённое
  «NMAX 155 GRE»;
* `_scratch_works_buf/measure.py:110` — `"mileage": "15600"` внутри фикстуры
  `FakeBridge.find_bike`; «15600» встречается в файле РОВНО один раз, здесь. Ни один сценарий
  файла этого числа никуда не пишет — одометры фикстур 41357 и 41641.

**Место доктрины, по которому гард закрасил команду, — три слоя, и все три записаны заранее:**

1. `CLAUDE.md`, «ПОЛИТИКА ПОДТВЕРЖДЕНИЙ — РЕДАКЦИЯ Б», 🔴 КРАСНАЯ зона: «запись в рабочие
   таблицы (CRM / Зарплаты / Байки, `confirmed=true`; `set_fleet_oil` / `set_fleet_service`)».
   Операция названа поимённо.
2. `CLAUDE.md`, «УРОК-КЛАСС: WRITE-СМОКИ НА ЖИВЫХ СТРОКАХ ЗАПРЕЩЕНЫ (инцидент row705,
   08.07.2026)»: допустимых форм write-смока ровно две — ТЕСТ-сущность либо доказанная
   харнессом не-пишущая ветка. Пометки ТЕСТ у объекта не было.
3. Реализация этого в гарде — `pretool_guard._entity_blocktype` (правка 01.08.2026),
   дословно из докстринга: «сущность НЕ извлечена, но операция пишет в ЖИВЫЕ ТАБЛИЦЫ И НЕСЁТ
   ОБЪЕКТ → 'hard'». Сработала именно эта ветка: имя сущности не опознано (оттого в журнале
   «Сущность «не опознана»»), но операция из `_ENTITY_HITS_LIVE`, объект `card_min` дал —
   значит `hard`, то есть **deny и кнопки «да» нет физически**.

**Почему замерочный харнесс вообще попал под разбор тела — это ЗАПИСАННЫЙ класс, а не новость.**
Красное дало **имя в коде**: `_py_code_view` (строки 527–529) кладёт в `names` имя `def`
наравне с идентификатором и атрибутом, а фейковый мост харнесса ОПРЕДЕЛЯЕТ методы с именами
боевых операций — `def set_fleet_oil` (строка 141) и `def set_fleet_service` (строка 137).
Ранний defer без чтения тела получают только цели под `PROJECT/tests/` либо сам `gate.py`
(`pretool_guard`, строки 1909–1910: «Скрипт вне tests/ рядом в команде → НЕ доверяем»), а ТЗ
задачи 50 само велело класть временное в `_scratch_works_buf/`. Тот же класс уже назван в
`CLAUDE.md` дословно: «гард судит тело теста (фикстура моста определяет метод с именем боевой
операции), а ранний defer получают только цели под `PROJECT/tests/`»
(`docs/artifacts/2026-08-14-undo-oil-door.md`).

**Честно про факт мира:** ни одной клетки живых таблиц заход не тронул и тронуть не собирался —
`measure.py` целиком на фикстурах (`FakeBridge`, «Живых таблиц не касается ничем», сеть и LLM
замоканы), а до исполнения дело не дошло вовсе: файл записан в 12:52:31, блок в 12:52:37, первый
и единственный запуск. Гард при этом прав по устройству: во время разбора он видит имя боевой
операции в коде и по правилу fail-closed обязан считать это записью — доказать обратное можно
только исполнив, то есть уже поздно.

---

## 3. Карточка владельцу: **НЕ ВЫПИСАНА**

Исход из трёх («выписана / не выписана / неизвестно») — **не выписана**, и это подтверждено
тремя независимыми свидетелями:

1. **Код гарда.** Ветка `blocktype == "hard"` в `pretool_guard.main()` пишет маркер-файл,
   строку в свой журнал и выходит `_emit(decision("block", hit, why))` — **до** вызова
   `_push(card)`, который стоит ниже по коду. Комментарий там же: «block → deny: кнопки «да»
   здесь нет». Отправить карточку эта ветка не может физически.
2. **Очередь.** У строки 50 `approved_by` пусто, статус `failed`; в статусе `needs_approval`
   обеих полос сейчас **0 строк**; в журнале демона нет ни одной строки `NEEDS_APPROVAL id=50`
   (а такая строка пишется всегда, когда карточка встаёт).
3. **Журнал рождения карточек.** `chain_cards.jsonl` последний раз писан 22.08 в 11:12:02 —
   за 1 ч 40 мин ДО блока; записи о карточке 50 в нём нет.

**Чего владелец всё-таки НЕ лишился.** Текст карточки до него доехал, но другим каналом и
без права ответа: он вклеен в отчёт о падении, который devbot отнёс в тему 328 в 12:53:34
(тело — `reports/2026-08-22/task-50.md`). Это канал ОТЧЁТА: ни кнопок, ни номера, ни «да».
Строка «Проверь: тот ли байк и пробег — жду твоё «да»» внутри него — часть заготовленной
карточки, а не живой вопрос; отвечать на неё некуда и незачем (объект и число, как показано
в §2, взяты из фикстуры).

Маркер, которым демон и закрыл задачу без кнопки, лежит на диске: `/tmp/cc_guard_block/50.json`,
449 байт, `"blocktype": "hard"`.

---

## 4. Что осталось на диске: **работа осталась, и немалая**

Заход умер, не успев ни доложить, ни закоммитить, но дерево несёт связную и почти собранную
правку. Всё перечисленное — **незакоммичено**; последний коммит репозитория `4c119cf` сделан
в 11:07:30, то есть за 1 ч 22 мин до старта задачи 50.

**Новое, неотслеживаемое git:**

| файл | размер | записан | что это |
|---|---|---|---|
| `works_ledger.py` | 12 893 Б, 169 строк | 12:42:31 | сторож партии: исходы позиции `register` / `event` / `lost`, вокабуляр причин на двух языках, импортов ноль |
| `_scratch_works_buf/probe_classify.py` | 2 607 Б | 12:47:55 | быстрая проба классификатора и буфера |
| `_scratch_works_buf/run_tests.py` | 1 786 Б | 12:49:08 | прогон 23 затронутых сьютов подпроцессами |
| `_scratch_works_buf/measure.py` | 15 872 Б | 12:52:31 | замер ПРИНЯТО/ЗАПИСАНО на живых фикстурах 01.08 и 22.08 — тот самый, на котором встал блок |

**Правки отслеживаемых файлов** (`git diff --stat`: 3 файла, 260 вставок, 38 удалений):

* `splinter.py` — 247 изменённых строк в 21 хунке (12:47:21). Точки: `import works_ledger`
  (строка 33), накопление буфера рядом с `_RECENT_TTL`, `_write_info_works`,
  `_flush_pending_works`, `_odo_confirmed`, `_classify_work`, `handle_service_button`,
  `handle_service_result`, `_sp_words_said`, `_sp_write_done`, `_handle_servicing`;
* `service_receipt.py` — 19 изменённых строк (12:46:48): квитанция принимает ГОТОВЫЙ вердикт
  сторожа;
* `invariants_check.py` — 32 изменённых строки (12:50:02): новый страж `check_works_ledger_pure`
  (`WORKS_LEDGER_PURE`), ast, fail-closed.

**Чего в этой работе НЕТ (по каждому пункту ТЗ — честно):**

* **тестов нет вовсе.** Сам `works_ledger.py` в докстринге ссылается на
  `tests/test_works_buffer.py` — этого файла на диске **не существует**. Значит пункт 5
  (обязательный отрицательный тест) не сделан, а по букве ТЗ без него не считается сделанным
  и пункт 3;
* **артефакта нет.** `docs/artifacts/2026-08-22-works-buffer-root.md` — адрес результата
  задачи 50 — **не существует**;
* **чисел ДО/ПОСЛЕ нет** (пункт 6): замер и есть то, на чём заход умер, он не отработал ни разу;
* **гейт не гонялся ни разу**, коммита нет, доставки нет (пункт 7).

**Что из сделанного успело показать себя живым прогоном** (по транскрипту сессии):

* два прогона сьютов через `run_tests.py` — **красных 0** (23 затронутых сьюта, затем отдельно
  `test_invariants_check.py`), то есть правка не сломала соседей;
* проба классификатора 12:48:01 — **1 несовпадение**: «замена ремня» и «полная чистка
  вариатора» в регистр редуктора больше не попадают (`kinds: ['other'] | gear в них: False`,
  пункт 4 работает), буфер ДОПИСЫВАЕТСЯ (пункт 1 работает), но дедуп по контентному ключу
  не схлопнул «передняя шина» и «замена передней шины» — в буфере осталось 5 позиций там, где
  проба ждала 4. После этого пробу не перезапускали, расхождение не закрыто.

**Отдельно — риск, который стоит назвать вслух.** Splinter живёт с 16.08 14:30:42 (PID 1239284),
в его памяти кода этой правки нет. Но правка ЛЕЖИТ НА ДИСКЕ и уже связана импортом
(`splinter.py:33 import works_ledger`), а `works_ledger.py` рядом — значит **любой перезапуск
splinter поднимет в прод незакоммиченную работу, ни разу не прошедшую полный гейт**. Ни одна
кнопка доставки этого не спросит: доставка судит коммит, а тут коммита нет.

---

## 5. Журнал: **ни одной строки**. Где именно оборвался

**Ни одной.** Канонический путь записи — `cclog.py` / `cclog.write_cclog`, и он ведёт реестр
`cc_log_ledger.jsonl`: последняя строка реестра — `2026-08-22T12:08:17Z` (итог задачи 49),
файл с тех пор не переписывался, то есть за всё время жизни задачи 50 (12:29:55 → 12:52:39)
в журнал не ушло ничего. Подтверждено вторым источником, независимым от реестра: в транскрипте
сессии задачи 50 **54 вызова оболочки, и `cclog` среди них нет ни разу** — ни PLAN в начале,
ни BLOCKED. Пульс, который пишется той же операцией, соответственно тоже не трогали.

**Где оборвался — по минутам** (mtime файлов + журнал демона + журнал гарда + транскрипт):

| время UTC | что произошло |
|---|---|
| 12:29:10 | задача создана devbot'ом (`Filipp-328-dev`, полоса vps) |
| 12:29:55 | демон взял в работу, `claude -p`, таймаут 2700 с |
| 12:30–12:42 | чтение кода: 40 read-only вызовов (`sed`/`grep` по splinter, service_receipt, invariants_check, соседним сьютам) — премисы ТЗ проверялись дословным чтением, как и велено |
| 12:42:31 | написан `works_ledger.py` — сторож (пункт 3) |
| 12:46:48 | правка `service_receipt.py` |
| 12:47:21 | правка `splinter.py` — корень (пункты 1, 2, 4) |
| 12:48:01 | проба классификатора и буфера: 1 несовпадение (дедуп переформулировки) |
| 12:49:1x | прогон 23 сьютов: красных 0 |
| 12:50:02 | правка `invariants_check.py` — страж чистоты сторожа |
| 12:50:1x | повторный прогон `test_invariants_check.py`: красных 0 |
| 12:52:31 | написан `measure.py` — замер ДО/ПОСЛЕ на живых фикстурах (пункт 6) |
| **12:52:37** | **первый и единственный запуск замера → жёсткий блок гарда, deny** |
| 12:52:39 | демон: `failed без approve`, METRICS `dur_s=1364.76 outcome=failed` |
| 12:53:15 | думатель: `halt` — «нужен владелец»; его диагноз и стал полем `result` |
| 12:53:34 | devbot отнёс отчёт о падении в 328, тело — `reports/2026-08-22/task-50.md` |

Оборвался, стало быть, **на пункте 6 — на замере**, уже имея на диске корень (пункты 1, 2, 4)
и сторож (пункт 3), но ещё не написав ни теста (пункт 5), ни артефакта, и не дойдя до гейта,
коммита и доставки (пункт 7).

---

## 6. Итог

* Причина падения — не сбой и не внешний отказ: **жёсткий блок гарда**, приведён дословно выше.
* Упёрлась задача **в собственный замерочный харнесс**: фейковый мост определяет методы
  с именами боевых операций, а лежит харнесс вне `tests/`, куда его отправило само ТЗ.
* **Карточка владельцу не выписана** — по устройству ветки жёсткого блока; текст карточки
  доехал только отчётом в 328.
* **Работа осталась**: новый модуль на 169 строк плюс 298 изменённых строк в трёх файлах,
  соседние сьюты зелёные, но без тестов, без артефакта, без чисел замера, без гейта,
  без коммита и без доставки. И она **заряжена на ближайший перезапуск splinter**.
* Ничего из перечисленного этим заходом не тронуто, не удалено и не закоммичено.

## Чем это читано (всё только чтением)

`get_pending` живой очереди (5 статусов, `lane=all`) · `orchestrator_daemon.log` строки
22107–22115 · `orchestrator_claims.jsonl` строка 207 · `/tmp/cc_pretool_guard.log` (хвост) ·
`/tmp/cc_guard_block/50.json` · `reports/2026-08-22/task-50.md` · `chain_cards.jsonl` (mtime) ·
`cc_log_ledger.jsonl` (хвост) · транскрипт сессии задачи 50
`3cfe964c-3f0f-42ed-bc49-52e3a661479b.jsonl` (1 067 865 Б, 54 вызова оболочки) · `git diff
--stat`, `git status`, `git log` · `systemctl show splinter` · mtime файлов дерева ·
исходники `pretool_guard.py` (`_entity_blocktype`, `_py_code_view`, `main`, `_P_BIKE`,
ранний defer) и `_scratch_works_buf/*.py`.
