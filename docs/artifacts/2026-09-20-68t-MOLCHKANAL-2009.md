68t MOLCHKANAL 2009

# Два внешних канала молчат по-разному: один отвечает отказом по формату, второй закрывает задачу молча

дата: **2026-09-20** · полоса: ПК · ключ Штаба 68t.2009

FACT: read-only разведка. Ни один канал не дёрнут, наружу не ушло **0** пакетов, проб **0**.
Всё ниже прочитано с диска: реестр очереди `review_outbox_queue_state.json` (225 585 б),
здоровье каналов `review_auto_state.json`, 291 карточка лотка `docs/review_inbox/`,
протоколы последних попыток `tmp/review_send/codex_stderr.txt` (23 806 б, 20.09 12:19:13)
и `tmp/review_send/manus_protocol.json` (30 541 б, 20.09 12:33:17). Временное место захода —
`tmp/razbor_molchaniya/` (пусто: ни одного файла не потребовалось). Ничего не удалялось,
не чинилось, состояния не правились. Время везде **по Пхукету** (UTC+7).

---

## 0. Признак сделанности

Файла `docs/artifacts/2026-09-20-68t-MOLCHKANAL-2009.md` на диске **не было**
(`ls` → No such file). Заход начат с нуля, отвечены все пункты.

## Премиса Штаба — проверена первым действием

| что утверждает премиса | вердикт | чем |
|---|---|---|
| каналов **два** | **ВЕРНА** | `review_send.CHANNELS = ("codex", "manus")` (`review_send.py:47`); третьего программного канала нет по комментарию там же |
| за 20.09 **заходов три**, ответов **ноль**, ответило **0 из 2** | **ВЕРНА КАК ПОКАЗ ВИТРИНЫ; как счёт попыток — занижена втрое** | витрина считает СТРОКИ ЛОТКА с `отправлено: 2026-09-20` (`contour_digest.external_stats`, `attempts = len(rows)`): их ровно **3** (codex 1, manus 2), ответов 0, каналов ответило 0 из 2. Живых попыток за тот же день по реестру — **10** (codex 4, manus 6): повтор перезаписывает файл того же пакета и канала |
| канал отвечал при одной модели и перестал при другой, наш запрос не менялся | **ВЕРНА для обоих, но модель тут РАЗНАЯ и роль у неё разная** | manus: `model` в теле GET сменился на `manus-1.6-adaptive`, ответов с тех пор 0. codex: их сервер отвечает кодом 400 на модель `gpt-6-astra`, которую выбирает САМ CLI v0.151.0 на нашей машине; наш код модель не задаёт (в адресе канала нет `-m`) |

Два дня последних ответов, прочитаны по шапкам лотка: **codex — 11.09**, **manus — 03.09**.

## 1. Названо по адресу: два канала, две двери

### Канал 1 — `codex`: локальный CLI по подписке

| что | адрес |
|---|---|
| дверь | исполняемый файл из PATH, резолв `review_send_run.resolve_codex` (`review_send_run.py:220-230`, `shutil.which("codex")` → на Windows берёт `codex.cmd`) |
| способ запроса | `subprocess.run` (`review_send_run.py:274`), argv собирается на `review_send_run.py:265`: `codex exec --sandbox read-only --color never --output-last-message <workdir>/codex_last_message.txt -`. Пакет уходит **стдином** (кириллица в argv на этой полосе коверкается), предел `DEFAULT_TIMEOUT = 900` с (`review_send_run.py:94`) |
| ответ целиком на диске | `tmp/review_auto/codex_last_message.txt` (заход демона) или `tmp/review_send/codex_last_message.txt` (ручной заход и повтор ступени F); рядом `codex_stdout.txt`, `codex_stderr.txt` (`review_send_run.py:258, 298-299`). Дословный текст ответа — в карточке лотка `docs/review_inbox/<день>-<пакет>-codex.md`, раздел «ОТВЕТ КАНАЛА (дословно)» |
| состояние на 20.09 | файла `codex_last_message.txt` **нет ни в одном из двух рабочих каталогов** (канал его не отдал), `codex_stdout.txt` — **0 байт**, `codex_stderr.txt` — 23 806 б |

### Канал 2 — `manus`: HTTP-API

| что | адрес |
|---|---|
| дверь | `https://api.manus.ai` (`review_send_run.DEFAULT_MANUS_BASE`, `:66`); создание — `POST /v1/tasks` (`:67`), забор — `GET /v1/tasks/{task_id}` (`:75`) |
| ключ | берётся процессом отправщика из окружения по имени `MANUS_API_KEY` (`DEFAULT_KEY_ENV`, `:64`); кладётся в заголовки `API_KEY` и `Authorization: Bearer` (`_manus_headers`, `:319-332`) и больше никуда — ни в протокол, ни в дочерний процесс (`child_env`, `:184`). **Сам ключ этим заходом не читался и здесь не называется** |
| способ запроса | `urllib.request.urlopen` (`manus_http`, `:335-370`). Пакет режется на части по `MANUS_CHUNK_CHARS = 2400` (`:151`): часть 1 создаёт задачу, части 2…N досылаются в ту же задачу; готовность опрашивается раз в `DEFAULT_MANUS_POLL = 10` с до `DEFAULT_MANUS_WAIT = 1800` с (`:103-104`) |
| ответ целиком на диске | `tmp/review_auto/manus_protocol.json` / `tmp/review_send/manus_protocol.json`, поле **`body`** держит тело GET **дословно**; там же `create_body`, `part_status`, `part_replies`, `poll_note`. Карточка лотка — `docs/review_inbox/<день>-<пакет>-manus.md` |

### Кто ходит в эти двери

* **Ступень A** — демон `review_auto_run.py` (импортирует `review_send_run`, `:38`), рабочий каталог `tmp/review_auto` (`DEFAULT_WORKDIR`, `:46`). Лежачий канал пробуется не чаще раза в `CHANNEL_PROBE_AFTER_SEC = 6 ч` (`review_auto.py:104`).
* **Ступень F** — повтор очереди: `review_outbox_queue_run.retry_argv` (`:263-277`) зовёт **того же** отправщика `review_send_run.py --pack … --channel …`, рабочий каталог `tmp/review_send`.
* Реестр попыток — `review_outbox_queue_state.json` (99 записей «пакет×канал»), здоровье каналов — `review_auto_state.json` → ключ `channels`.

## 2. Последние десять попыток к каждому каналу

Время — **момент записи вердикта в лоток** (`review_outbox_queue_run._file_at`: реестр берёт
mtime файла лотка, а не «сейчас»). Записи `expired` из счёта убраны: это пометка нашего архива
по возрасту, а не заход в канал.

**codex** (в реестре всего 77 попыток, все с одной причиной `channel_error`):

| # | время (Пхукет) | пакет | чем кончилась |
|---|---|---|---|
| 1 | 19.09 10:52:59 | chain-pc-2026-09-17-70 | `channel_error` |
| 2 | 19.09 10:53:05 | …-70 | `channel_error` |
| 3 | 19.09 17:43:45 | …-70 | `channel_error` |
| 4 | 19.09 23:44:11 | …-72 | `channel_error` |
| 5 | 19.09 23:52:56 | …-72 | `channel_error` |
| 6 | 19.09 23:53:02 | …-72 | `channel_error` |
| 7 | 20.09 06:02:24 | …-72 | `channel_error` |
| 8 | 20.09 12:12:27 | …-74 | `channel_error` (проба ступени A) |
| 9 | 20.09 12:19:08 | …-74 | `channel_error` (повтор F) |
| 10 | 20.09 12:19:13 | …-74 | `channel_error` (повтор F, текущий mtime карточки) |

**manus** (в реестре всего 193 попытки):

| # | время (Пхукет) | пакет | чем кончилась |
|---|---|---|---|
| 1 | 19.09 11:21:53 | …-70 | `channel_idle` |
| 2 | 19.09 11:24:33 | …-70 | `channel_idle` |
| 3 | 19.09 17:46:25 | …-70 | `channel_idle` |
| 4 | 19.09 23:48:23 | …-72 | `channel_idle` |
| 5 | 20.09 00:04:16 | …-72 | `channel_idle` |
| 6 | 20.09 00:08:29 | …-72 | `channel_idle` |
| 7 | 20.09 06:06:36 | …-72 | `channel_idle` |
| 8 | 20.09 12:16:05 | …-74 | `channel_idle` (проба A) |
| 9 | 20.09 12:29:38 | …-74 | `channel_idle` |
| 10 | 20.09 12:33:17 | …-74 | `channel_idle` (текущий mtime карточки) |

### Раскладка по пяти видам

Два числа в клетке там, где они расходятся: **слева — как числит наш реестр, справа — по
содержанию их собственного ответа**. Расхождение и есть главная находка п.4.

| вид | codex (10) | manus (10) |
|---|---|---|
| 1. пришло тело | **0** | **0** |
| 2. пустой завершённый ответ | 0 | **10** (`channel_idle`: задача `completed`, сообщений ассистента 0) |
| 3. отказ их сервера | **10 по реестру / 0 по содержанию** | 0 |
| 4. таймаут или обрыв | 0 | 0 |
| 5. отказ по формату (наша вина) | **0 по реестру / 10 по содержанию** | 0 |

Почему у codex клетка двоится: реестр зовёт `channel_error` внешним («канал упал у себя, код
возврата ≠ 0», `review_outbox_queue.py:132`), а их сервер на самом деле **ответил** — кодом
**400 `invalid_request_error`**: «The 'gpt-6-astra' model requires a newer version of Codex».
Это отказ **по формату запроса**, и корень его на нашей машине (CLI v0.151.0), а не на их
стороне. Наш код при этом модель не выбирает: в адресе канала карточки стои́т
`codex.CMD exec --sandbox read-only` без `-m`.

### Весь корпус реестра (для сравнения с последней десяткой)

* **codex — 77 попыток: `channel_error` ×77.** Ни одной другой причины: канал ломается ровно одним способом.
* **manus — 193 попытки:** `channel_idle` 122 · `answer_lost` 48 · `http_400` 15 · `accepted_no_answer` 5 · `http_500` 2 · `no_credentials` 1.
  По пяти видам: пустой завершённый **127** (122+5) · их сервер **2** · таймаут/обрыв **48** · наша вина **16** (15 раз формат 400 + 1 раз не было ключа) · тело — в реестр не попадает по построению (ответ закрывает запись).

## 3. Чем вычисляется признак «канал лежал» — и почему он негоден как приговор их стороне

Признак живёт **в трёх местах, и ни одно из них не меряет их сторону**.

| # | адрес | что меряет ДОСЛОВНО |
|---|---|---|
| A | `review_auto.channel_down` (`review_auto.py:529`) → поле `down_since`, ставится в `note_channel` (`:500-526`) при `strikes >= CHANNEL_DOWN_STRIKES = 2` (`:100`); страйк даёт `channel_failed` (`:489-497`) | «две попытки подряд кончились НЕ словом `answered`, и причина не из трёх наших (`CHANNEL_OUR_FAULT = {outbound_guard, sender_crashed, no_credentials}`, `:109`)» |
| B | `review_outbox_queue.origin()` (`:189-221`) → `external` → шапка `_HEAD_EXTERNAL` «КАНАЛ ЛЕЖАЛ, ПАКЕТ НЕ ДОЕХАЛ» (`:560`), уезжает в тему Аудит через `audit_text` (`:569`) | «слаг причины лежит в словаре `_EXTERNAL` из пяти слов (`:129-145`), либо это `http_5xx`, либо `http_408/425/429`» |
| C | витрина полосы: `contour_digest.external_stats` → ключ `down` (`contour_digest.py:452-453`) → строка `vitrina_pc.py:848` | «канал назван хотя бы в одной шапке лотка за КАЛЕНДАРНЫЙ день и ни в одной из них нет «ОТВЕТ ПОЛУЧЕН»» |

**Поднимается ли он без события? Да, всеми тремя дорогами — говорю прямо: признак негоден
как доказательство «их сторона лежала».**

1. `channel_unreachable` рождается, когда `shutil.which("codex")` вернул `None` — **бинаря нет
   в PATH**, наружу не ушло ничего (`review_send.py:347-348`; комментарий самого словаря:
   «адрес канала недоступен — наружу не ушло ничего», `review_outbox_queue.py:130`). Origin у
   него `external` → в теме Аудит «КАНАЛ ЛЕЖАЛ», и две такие попытки ставят `down_since`.
2. `timeout` — это НАШ будильник на 900 с (`review_send.py:352-353`), а не их молчание;
   `answer_unreadable` — наш диск. Оба дают страйк.
3. Витрина (C) поднимает «КАНАЛ ЛЕЖАЛ» даже на НАШИХ причинах `outbound_guard` (страж задержал
   пакет у нас) и `no_credentials` (ключа нет в окружении): шапка с именем канала в лотке уже
   появилась, «ОТВЕТ ПОЛУЧЕН» в ней не стои́т — условия `down` выполнены при нулевом выходе
   наружу.
4. **Живая иллюстрация 20.09:** codex числится лежачим с `2026-09-12T07:22:53Z`, страйков 33, а
   их сервер в этот самый момент **отвечает** — кодом 400. «Лежит» здесь означает «мы не
   получили ответа», и читать признак можно только так.

**Два побочных дефекта счёта, замеренные сегодня** (называю, не правлю — не предмет задания):

* Повтор ступени F перезаписывает файл того же пакета и канала, поэтому **10 живых попыток за
  20.09 витрина показывает как 3**.
* Повтор запускает отправщика **без `--date`** (`retry_argv`, `:274-276`), и день карточки берётся
  «сегодняшний» — но карточка `2026-09-19-…-72-codex.md` переписана **20.09 в 06:02:24** и
  осталась в дне 19.09: эта попытка в витрине за 20.09 не видна **вовсе**. День витрины ≠ день
  попытки.

## 4. Вина числом: 20 попыток

| счёт | наша сторона | их сторона |
|---|---|---|
| **как числит наш реестр** (`origin`) | **0 из 20 — 0 %** | **20 из 20 — 100 %** (все 20 записей `origin: external`) |
| **по содержанию их собственных полей** | **10 из 20 — 50 %** (codex: их код 400 `invalid_request_error` на модель, которую выбирает CLI **на нашей машине**; это отказ по формату запроса) | **10 из 20 — 50 %** (manus: приняты все части кодом 200, задача закрыта `completed`, квитанция 1, сообщений ассистента 0) |

Оговорка внутри «их» половины, чтобы число не читалось шире, чем стои́т: режим и профиль задачи
manus мы не шлём ни одним полем, модель выбирает их сторона, а что стои́т в кабинете — с полосы
ПК не читается. Это **НЕИЗВЕСТНО**, а не «наша вина»: наш запрос по форме `{"prompt"[, "taskId"]}`
за весь период не менялся.

Для полноты, весь корпус реестра (270 попыток), счёт **по слагам реестра**: наша сторона
**16** (5.9 %) — все у manus (`http_400` ×15, `no_credentials` ×1); их сторона **254** (94.1 %).
Разбор по содержанию полей на весь корпус не делался: протоколы прошлых попыток затёрты (п.5).

## 5. Сохраняем ли мы их поля ответа: модель, код, причину

**Да — но только у последней попытки, только во временном каталоге, и по-разному у двух каналов.**

### manus, последняя попытка 20.09 12:33:17 — `tmp/review_send/manus_protocol.json`

Тело GET лежит **дословно** в поле `body`. Служебные поля (только они, тел и переписки здесь нет):

```
"object":"task", "status":"completed", "model":"manus-1.6-adaptive",
"credit_usage":1, "created_at":1789882180, "updated_at":1789882381,
"output":[ 9 × role="user" ], сообщений ассистента 0
```

Полей `error`, `failed`, `stopped` в теле **нет ни одного**. Коды досылок: `part_status` =
`[200 ×9]`, `part_error` пуст, `part_replies` — 8 непустых тел. Задача `Qhgo9ws8v26APsmEg8ishZ`.
**В карточке лотка модели нет ни одним полем** — там исход `unknown`, причина `channel_idle`,
цена «1 request» и служебная строка `idle_error`.

### codex, последняя попытка 20.09 12:19:13 — `tmp/review_send/codex_stderr.txt`

```
стр. 1:   OpenAI Codex v0.151.0
стр. 4:   model: gpt-6-astra
стр. 204: warning: Model metadata for `gpt-6-astra` not found…
стр. 205: ERROR: {"type":"error","status":400,"error":{"type":"invalid_request_error",
          "message":"The 'gpt-6-astra' model requires a newer version of Codex…"}}
стр. 206: та же строка второй раз
```

`codex_stdout.txt` — **0 байт**, файла ответа нет. В карточку лотка попадает **код возврата 1**
и **последние 300 знаков** хвоста (`review_send.py:370`). **Отдельного поля «модель» у нас нет
нигде**: имя `gpt-6-astra` видно в карточке только потому, что их текст ошибки называет модель
сам и напечатан дважды — второе упоминание влезло в обрезок. Уберут они модель из текста
ошибки — и разбирать станет нечего.

### Общий дефект обеих дорог

Протокол — **один файл на рабочий каталог** и перезаписывается каждой попыткой
(`codex_stderr.txt`, `manus_protocol.json`); каталоги `tmp/` вне git. Значит **истории полей
не существует**: разобрать можно только последнюю попытку каждого канала и только до
следующей. Карточка лотка, которая живёт в git, модели не хранит **ни для одного канала** — а
именно модель оказалась тем полем, по которому обе немоты и различаются.

## 6. Граница вслух

**Прочитано по адресу** (файл и строка названы выше): каналов 2 · последние ответы 11.09 и
03.09 · попытки codex 77 и manus 193 с разбивкой по причинам · последние 10 у каждого с
временами · строк лотка за 20.09 — 3 · страйки 33 и 50 · `down_since` 12.09 и 05.09 · все поля
тела manus и все строки шапки codex · размеры и mtime протоколов · все константы и номера строк.

**Вывод, а не чтение:** раскладка причин по пяти видам задания (слаги наши, виды — Штаба);
отнесение codex-400 к «нашей вине по содержанию» (их код — факт, «корень на нашей машине» —
вывод из того, что модель выбирает CLI, а наш argv её не задаёт); счёт «10 живых попыток за
20.09» (сложены записи реестра за календарный день Пхукета); фраза «признак негоден» (вывод из
трёх названных сценариев, ни один из них сегодня не воспроизводился).

**Чего заход НЕ делал:** ни одного пакета наружу, ни одного обращения к codex или manus, проб
0; `.env` и конфиги не читались (имя `MANUS_API_KEY` взято из кода `review_send_run.py:64`);
ключ, токены и содержимое конфигов не печатались; тела ответов, клиентские реплики и переписка
не печатались; ни один процесс не тронут, ни одно состояние не правилось, ничего не удалялось;
`--status` очереди не запускался (он пишет реестр); кабинет Manus не открывался; CLI не
обновлялся; **ничего не чинилось** — заход устанавливает факт.

**Осталось НЕИЗВЕСТНЫМ:** какая модель работает с codex CLI v0.151.0 и откуда CLI берёт
`gpt-6-astra` (его настройка — конфиг, не читаем); в каком режиме идут задачи manus в кабинете
и есть ли в API поле, возвращающее `manus-1.6-agent`; что происходило в попытках, чьи протоколы
уже затёрты (это все, кроме последней у каждого канала).

## 7. Бюджет

Единица одна, потолок 2 700 с, доля 0.7 = **1 890 с**. Старт захода — метка очереди
1789892991 (20.09 15:29:51 Пхукет), написание артефакта — 15:41 (epoch ≈ 1789893660):
**≈ 670 с**, то есть 0.25 потолка и 0.35 доли. Урезать чтение не потребовалось: прочитаны все
10 последних попыток к каждому каналу (20 из 20, как просило задание) плюс весь корпус реестра
в 270 записей. Точные секунды с коммитом и журналом назовёт итоговый отчёт.
