# Тело записи журнала — 2026-09-22 10:07 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1269 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-22 10:07:06 |
| символов в строке | 1269 |
| порог | 600 |

---

DONE Dispatch 17:10: 70w RYCHAGLEGKIY — выбор учётки строителей полосы ПК переехал из постоянной переменной пользователя (реестр не тронут) в файл рабочего дерева claude_profile_choice.txt, читаемый profile_choice.py при сборке окружения ребёнка ОБЕИМИ ветками спавна (_run_task_impl:2931 и _thinker_exec:5977). Три исхода: ОСНОВНОЙ → ключ CLAUDE_CONFIG_DIR удаляется; путь → ставится он; нет файла/пусто/мусор/путь не существует → окружение не трогается вовсе + строка WARNING в журнал. Пустая переменная не ставится ни на одной дороге. Живой замер: при ОСНОВНОЙ ребёнок докладывает CHILD_SEES=None, claude doctor rc=0 и 0 маркеров отказа входа из 4; пустая переменная попала в окружение 0 раз на 9 случаях; мутант ветки испорченного значения краснит 16 отказов в 4 методах, контроль 0, боевой файл не тронут (sha256 до=после). Клиентское замыкание 50 файлов, клиентских в коммите 0 из 4 — ворота не задержат. Тесты: test_profile_choice 19 OK, test_pc_orchestrator 1152 OK, test_pc_local_dec 49 OK, сторож реестров зелёный. Коммиты 22fc57d9 (код) и 692a2db0 (артефакт), оба запушены. Применение по строке самообновления ЕЩЁ НЕ НАСТУПИЛО: демон бежит на 2297a7e7, между ним и HEAD 6 коммитов, строк про 22fc57d9 в логе 0, грязных файлов реестра 0.
