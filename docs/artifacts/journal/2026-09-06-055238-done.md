# Тело записи журнала — 2026-09-06 05:52 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **3353 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-06 05:52:38 |
| символов в строке | 3353 |
| порог | 600 |

---

DONE Dispatch 06.09 задание Штаба 00-g-lessons-read-live.0906 (полоса ПК): в момент ответа продукт читает ДЕЙСТВУЮЩИЕ уроки из базы lesson_store (разрез «актив»), плоская книга manager-bot/docs/playbook.md стала снимком. Коммиты 8bfb55b и eae6240. ГДЕ ВИДНО ОДНОЙ СТРОКОЙ: suggest.py:748 return _put_learned_section(text, bullets) — слева книга ещё источник, справа уже снимок. Правки в userbot_listen.py и moderation_bot.py НЕ потребовалось: воронка чтения одна (suggest.load_playbook), её зовут все три боевые точки сборки промпта. КАНДИДАТЫ В ОТВЕТ НЕ ПОПАДАЮТ: отбор единственным lesson_store.active, своего определения «действующего» в пути ответа нет и завестись молча не может — тест по дереву исходника запрещает candidates, STATE_CANDIDATE и литералы состояния в suggest.py. КОНТРФАКТ ТРЕМЯ ВХОДАМИ НА ЖИВЫХ ДАННЫХ (маркер — урок №9, 49 символов; боевые книга и таблица только читались, sha256 обоих до и после совпали): действующий урок в промпте ЕСТЬ; после withdraw по копии таблицы — НЕТ, прочие 8 из 8 на месте; правка книги руками — «РУКОПРАВКА-В-КНИГЕ» в промпт не попала, промпт байт-в-байт прежний. КОНТРОЛЬ, без которого третий вход был бы тавтологией: тот же прибор с откатом LESSON_BASE_READ_OFF=1 — рукоправка в промпте ПОЯВЛЯЕТСЯ. ТРЕТИЙ ИСХОД РАЗВЕДЁН С ПУСТОТОЙ: active_lesson_bullets отдаёт пару, ((), True) значит «база прочитана, действующих ноль» (секции нет, книжные буллеты её НЕ подменяют), ((), False) значит «источник не прочитан» (нет файла / неразбор / модуль бросил / откат) и тогда в промпт идёт книга-снимок. ЧИСЛО ПО КОРПУСУ: затронуто 16 из 16 кейсов trainer_cases.json (trainer_run.generate:294 зовёт load_playbook безусловно), весь набор НЕ гонялся; содержательная дельта измерена — тексты правил совпали 9 из 9 посимвольно, разошёлся только дата-префикс (0 из 9), книга в промпте 2843 → 2882 символа, +39. ПОСЫЛКА СОСЕДЕЙ: павших от правки тестов ДВА, ослаблен ни один. (1) TestBotNotWired запрещал suggest.py импорт хранилища доводом «иначе кандидат дойдёт до клиента» — довод стоял на подмене (импорт как признак утечки); список WIRED расширен ПОИМЁННО с объяснением, как требовал сам тест, а взамен добавлены ДВА прямых замка — по дереву исходника и по поведению (три состояния в одной таблице). (2) Сторож реестров deps_registry_check заблокировал коммит: suggest лениво тянет lesson_store, тот — anonymize_corpus; обход --no-verify отклонён (перенёс бы красное на self-update демона молча), оба внесены в _ORCH_LAZY_UNCOVERED со строкой «чем опасна его грязь». list_playbook_rules и find_playbook_conflict переведены на load_playbook_file, чтобы нумерация показа /rules совпадала с файлом, который remove_playbook_rule мутирует; ЦЕНА НАЗВАНА В КОДЕ: удаление правила через /rules больше не гарантирует, что бот перестанет по нему отвечать. РЕГРЕСС: до правки 1110 тестов / 6 падений, после — 1763 теста / 7 падений, седьмое (test_trainer_run.test_raner_ne_v_klientskom_konture) НЕ моё и доказано контрфактом на дереве git archive HEAD, где оно уже True; цепочка trainer_run ← lesson_regress ← trainer ← userbot_listen, коммит a42c8ee. Вклад в храповик неразбора измерен: 0 точек у всех моих файлов. Новых зелёных 20, новых красных 0. Контрольный прогон после правки реестра — 1145 тестов OK. ОТКАТ: LESSON_BASE_READ_OFF=1. Временное: _scratch_lesson_read_0906/, ничего не удалялось.
