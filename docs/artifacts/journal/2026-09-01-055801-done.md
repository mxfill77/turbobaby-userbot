# Тело записи журнала — 2026-09-01 05:58 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1543 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-01 05:58:01 |
| символов в строке | 1543 |
| порог | 600 |

---

DONE Dispatch 01.09 задача 72: разведка класса «тест меряет живое дерево вместо своего», обе полосы, правок ноль. Корень класса найден НЕ в тестах, а в проде — 6 констант с литералом корня: ПК pc_orchestrator.py:98 REPO и pretool_guard.py:281 PROJECT, VPS orchestrator_daemon.py:33 REPO, :34 BRIDGE_GS, pretool_guard.py:78 PROJECT, guard_replay.py:36 PROJECT; тесты наследуют корень импортом, поэтому починка одних тестов косметична. Оба образца из задания подтверждены и оказались симметричны: три ПК-теста надзора за зависимостями демона (test_pc_orchestrator.py:822, :10703, :10733) имеют зеркало из трёх сканеров исходника демона на сервере (test_claim_event.py:242, test_guard_marker_ttl.py:216, test_settings_allowlist.py:42), а хардкод корня в гард-тесте есть на обеих полосах (VPS tests/test_guard_cli_args.py:27, там же :166 создаёт tempfile в КОРНЕ живого репо). Числа VPS по снимку tmp/srv (HEAD 7298787 от 01.08, месяц отставания): 149 файлов test_*.py, 125 кладут абсолютный корень в sys.path[0]. Ложно-зелёный разобран по семи видам; у вида «и код, и данные из живого дерева» тест структурно неспособен покраснеть от правки копии. Правило класса: корень есть ответ на вопрос «где я», а не константа — сначала прод, потом тесты, замок инвариантом; операционный критерий «какая правка В КОПИИ красит этот тест». Артефакт → docs/artifacts/2026-09-01-abs-root-class-in-tests.md, коммит 54cb9d1. Не измерено: живая полоса VPS (мерил снимок), D:\foreign не читался (вне проекта, разрешения нет), прогонов не было.
