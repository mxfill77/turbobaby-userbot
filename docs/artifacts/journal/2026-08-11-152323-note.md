# Тело записи журнала — 2026-08-11 15:23 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-11 15:23:23 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #500 → done · **Срок карточки (1800с) был короче таймаута прогона (2700с) — теперь он не назначается, а вычисляется: пол `TASK_TIMEOUT+POLL_SEC`=2760с, ручка может поднять, но не опустить. Замок из 11 тестов краснеет при возврате класса. Гейт 853 OK, коммит d7f247e запушен.** **FACT:** commit `d7f247e` в `git log` (push `1df7785..d7f247e` в origin/main) · живая константа: `APPROVAL_TTL = 2760 (floor 2760)`, `ratio ok: True` · проба возврата класса → красный замок · журнал: `OK: записано в мозг` → `docs/artifacts/journal/2026-08-11-152220-done.md` **1) Два числа строкой кода — откуда каждое** ``` pc_orchestrator.py:110 TASK_TIMEOUT = int(os.getenv("PC_TASK_TIMEOUT","2700")) # жёсткий рез run_claude pc_orchestrator.py:111 APPROVAL_TTL = int(os.getenv("PC_APPROVAL_TTL","1800")) # ← БЫЛО: ниже прогона ``` 2700 — собственный предел полосы, им же `run_claude` рубит headless. 1800 — пришло **первым коммитом порта с VPS** (`1eaa79c`) вместе с чужой посылкой и на своей полосе не мерилось никогда. Демон исполняет задачу **синхронно** внутри `poll_once`, очередь в это время не читает, а на первом свободном витке проверка срока стои́т **перед** запуском — значит ряд старел за занятость исполнителя. **2) Правка (одна): срок ВЫВОДИТСЯ, а не назначается** ``` APPROVAL_TTL_FLOOR = approval_ttl_effective(0) # 2760 с = прогон + виток APPROVAL_TTL = approval_ttl_effective(APPROVAL_TTL_ASKED) ``` Обоснование одной строкой: **дольше одного прогона демон слепым быть не может по построению** (`run_claude` режет …обрезано
