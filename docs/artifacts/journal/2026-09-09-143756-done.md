# Тело записи журнала — 2026-09-09 14:37 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2866 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-09 14:37:56 |
| символов в строке | 2866 |
| порог | 600 |

---

DONE Dispatch 21:55: заход 239, задание ящика Штаба 14-a-nabor-snyatie-dver.0909 — ДВЕРЬ СНЯТИЯ НАБОРА УРОКОВ. Код 20c375b (8 файлов, +1466/-27), артефакт 4fd7924 → docs/artifacts/2026-09-09-НАБОР-СНЯТИЕ-ДВЕРЬ-ОБЪЕКТ-ЧИСЛО-0909.md. Премисы П1-П4 проверены живым кодом, все верны; добавка к П2 — возврата набора не было ВОВСЕ (restore/unwithdraw = 0 имён), то есть недоставало половины механизма, а не только двери. ДВА ШАГА: batch_card показывает ключ/сколько уйдёт/когда залит/кем; batch_withdraw снимает только по подтверждению с КЛЮЧОМ И ЧИСЛОМ, и проверка числа стоит В ХРАНИЛИЩЕ, а не только в двери. Число, а не хранимый «он уже смотрел»: хранимое состояние протухает между показом и снятием, а число сверяется с живым файлом. batch_restore возвращает СЫРЫЕ байты состояния из следа — действующий станет действующим, кандидат кандидатом, снятый раньше другим разрезом останется снятым тем разрезом; всё или ничего. След СВОЙ (.batch): в следе переводов ключ — номер урока, и снятие набора заставило бы rollback(N) врать «уже откачен». СВОЙ ДЕФЕКТ, найден и закрыт внутри захода: движение опознавалось секундным штампом, и снял/вернул/снял внутри одной секунды ломало возврат («снятия в следе нет» при живом снятом наборе) — теперь номер движения, тест с прибитыми часами. Роутер ОДИН (lesson_word расширен до 4-кортежа, голдены обновлены дословно, не срезом), дверей две по числу объектов, право одно (may_write_rule, fail-closed). ЖИВАЯ ПРОБА на КОПИИ боевой таблицы: боевая дверь ОТКАЗАЛА по праву на всех пяти вызовах (fail-closed жив на этой машине); с правом в памяти — 4 отказа (без числа, чужое число 99, чужой ключ, пустая причина), ни один не тронул таблицу; снятие 4 строк, файл 14→14 строк; возврат ПОБАЙТНО равен, sha 591921ff9a981ee3 до и после; 9 строк без источника остались действующими. Боевой lesson_store.tsv не изменился: a399e51cc63b5739. ТЕСТЫ: набор 26 OK, уроки 441 OK, тренажёр+агент 360 OK, дерево 6981 при 9 падениях — БЫЛО 9, красных не прибавилось. Вклад в храповик неразбора +2 → 0 (зрячая форма isinstance, взята у promote, а не подгонка под детектор). Замыкание клиентского контура 78 до захода и 78 после, вошло 0 вышло 0 — доказано распаковкой git archive HEAD~1, а не унаследовано на слово. Переписан ОДИН чужой тест и это названо: test_long_command_truncated мерил не то, что называл — комментарий про обрезку эха, а утверждение про длину всего ответа, то есть про перечень команд агента (715 символов при 14 формах, запас около сотни на все будущие двери); теперь эхо мерится отдельно, у перечня свой потолок от предела сообщения Telegram. Запреты соблюдены: ничего не удалено (гард остановил os.remove в первой редакции пробы — по делу), временное место названо явно tmp/batch_door_0909, .env не читан, процессы не тронуты, боевой вызов и проверка различаются веткой --path, владельцу не ушло ничего.
