# Тело записи журнала — 2026-09-12 09:54 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **3109 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-12 09:54:39 |
| символов в строке | 3109 |
| порог | 600 |

---

DONE Dispatch 2026-09-12: задание 55-a (ОТКУДА kalendar-cena-nalichie 1209) закрыто РЕШЕНИЕМ С ДОКАЗАТЕЛЬСТВОМ, read-only, кода не правлено. ЦЕНА клиенту считается НЕ в Календаре бронирования, а по файлу на диске ПК price_source.json (built_on 2026-09-02): price_source.reprice (price_source.py:475, числа ставят строки 544/545/552), адрес источника — КОНСТАНТА модуля price_source.PATH, ручка env PRICE_SOURCE_SEASON (знак перевёрнут — выключает), врезка suggest._safe_quote_for_model:3649. Дословная строка столбца J листа СНИМАЕТСЯ (out.pop("text"), price_source.py:546). НАЛИЧИЕ приходит живой дверью моста: pricing.quote_for_model:311 читает поле available ответа, действие названо env PRICING_ACTION (pricing.py:43), парк — литерал "fleet" (:202); фразу «свободен на эти даты» дописывает КОД в suggest._client_price:3711/3730 только под if q.get("available"). ИСТОЧНИК ЧИСЛА ЦЕНЫ И ИСТОЧНИК НАЛИЧИЯ РАЗНЫЕ (файл против двери), но дверь у наличия и у входной котировки ОДНА. Премисы: П1 верна (фразу печатает exam_show.py:602 через reason_line:401 из shot["note"], рождается в suggest._class_offer_note — на коммите 1c2ccee строка 4523, живьём 4661); П2 верна с поправкой имени (не «сетка цен», а записанное правило-ФАЙЛ); П3 НЕВЕРНА в написанной форме — наличие приходит ТЕМ ЖЕ ответом ТОЙ ЖЕ двери. Строка «ПОЧЕМУ так» про свой источник ВРЁТ — это дефект ИМЕНИ, а не ход в закрытый лист: ложных имён 4 (suggest.py:4647/4653/4661; moderation_card.py:282 «строка листа ДОСЛОВНО» — обещает объект, снятый pop("text"); pricing.py:3 и :9-10; moderation_card.py:284), и префикс «ЦЕНА из Календаря бронирования» — ЯКОРЬ пост-чека suggest._PRICE_OK_MARKS:7199 и reviewer.py:68, поэтому переименование без них погасит цену клиенту. В закрытый лист ПК-код не ходит НИ ОДНОЙ строкой: прямой двери к ячейке у прода нет (price_freshness.py:18, разведка 15.08 — 27 кандидатов, все unknown_action), ручка сезона снимается полем season.global_discount той же двери quote_price (литерал в price_freshness_run.py:100, 9 GET, окно 15-16.09.2026); create_booking вызовов в боевом коде 0 (совпадения только в списке токенов pretool_guard). ТРЕТИЙ ИСХОД назван: какие диапазоны листа читает САМ мост — НЕИЗВЕСТНО с полосы ПК, исходников моста в репо нет (git ls-files по .gs → 0); вторично известно, что цена лежит на листе «цены альт», а из закрытого листа мост берёт блок кепок Z3:AB15. СРЕЗ ЧИСЛОМ: боевых py в корне 132; влияющих модулей контура 26 (39 загруженных минус 13, замер 07.09); открыто поимённо 11 файлов, из них в контуре 9; непросмотренных 17, и предел признания назван — 6 читателей цены (trainer_run, noprice_gate, season_gate, delivery, pricing_advisor, booking_draft) на второй источник числа не мерены. Бюджет: 24 единицы × ~5 с = ~120 с против потолка 2700 (0.044); мина замерена — полнодеревный grep не уложился в 120 с, обход шёл только по git ls-files. Правок кода, перезапусков, показа владельцу, открытия таблиц и вызовов живой двери в заходе НЕТ; наружу не ушло ни одного сообщения. Артефакт: docs/artifacts/2026-09-12-ОТКУДА-kalendar-cena-nalichie-1209.md, коммит 97d0c0d.
