# Тело записи журнала — 2026-08-17 10:30 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1570 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-17 10:30:19 |
| символов в строке | 1570 |
| порог | 600 |

---

NOTE Orchestrator: задача #9 → done · КЛИЕНТУ НЕ ПИШЕТ ВОВСЕ: 0 сообщений за всё время (470 черновиков, 03.07–17.08). Гипотеза штаба верна по ФАКТУ и неверна по ПРИЧИНЕ — ветка прямой отправки клиенту в коде ЖИВА (`suggest.py:6796`, единственная из 12 выходов), держит её флаг `SUGGEST_TEST_MODE`, у которого дефолт в коде ОБРАТНЫЙ. Артефакт + коммит `aeea8ef`. **FACT:** - commit `aeea8ef` в `git log` — `docs/artifacts/2026-08-17-bot-output-path.md`, 231 строка, 1 file changed - журнал: `OK: записано в мозг, символов: 408848` (строка-индекс 532 симв., порог 600 не задет — авто-выноса нет) - боевая БД открыта **только** `mode=ro`, наружу ушли схема и счётчики; тел сообщений и `client_id` не выведено ни одного - живые процессы сняты наблюдением, не трогались: userbot **PID 7828**, модербот **PID 11968** --- **1. Все ли пути ведут в модерацию — НЕТ.** Выходов 12, клиентский среди них **ровно один**: `suggest.send_to_client` → `client.send_message(client_id, text)` (`suggest.py:6796`), два вызывающих — `poll_and_send` (:6892) и `on_moderation_reply` (:7177). Остальные 11 — модер-группа (`post_draft`, `post_mod_note`, `_ack`, `pc_orchestrator:2599`), «Входящие брони» (`poll_and_post_intake`, `collect_booking:213`), группа тренажёра (`_trainer_send:247`), владелец (`pc_agent` ×3, `dispatch_notify` ×5), модербот ×32. Модербот клиента достать **не может по устройству**: приватного хендлера нет (`filters.ChatType.GROUPS`), `client_id` ни разу не адресат. Полнота — три среза: git-дерево → обходные примитивы (`respond`/`reply …обрезано
