# Тело записи журнала — 2026-08-15 17:16 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-15 17:16:06 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #39 → done · Прочитаны переключатели листа «Календарь бронирования», только чтение. Двери к листу у моста НЕТ (37 живых GET-проб). Утверждение владельца «даты в расчёт не входят» — **ОПРОВЕРГНУТО**: `G3=ABS(F3-F2)` кормит кривую срока. Ручки — **одна** пара (H3/I3/J3 лежат на Календаре). Истории положений **нет**. Сверка 722 броней: цепочка сходится там, где ручки стояли как сейчас. **FACT:** - `FACT: commit 5d4657d` — артефакт `docs/artifacts/2026-08-15-calendar-switches.md` в `git log`; `FACT: commit 4eba968` — тело журнальной записи. - `FACT: строка в мозге прочитана обратно` — `cowork_log`, строка #0, 581 симв.: «ДВЕРИ К ЛИСТУ НЕТ — 37 живых GET-проб… ОПРОВЕРГНУТО: G3=ABS(F3-F2)…»; плюс вторая строка (333 симв.) с ответом про историю и путём к артефакту. - `FACT: живой замер моста` — `season.source` = `"Календарь бронирования H3/I3/J3 (через 'цены альт' B2/B9/B19)"`, значения `0.15 / 0.15 / 0.25`, label `low`. - `FACT: 0 записей` — все обращения GET; ни одного POST, ни `set_caps`, ни `toggle_cap`; в лист не введено ничего, переключатели не тронуты. ## Что сделано по шагам **ШАГ 0 — двери нет.** Опрошено живьём 22 имени под этот лист (`caps`, `get_range`, `calendar_get`, `season_get`, `switches`…) — все `unknown_action`; с 15 именами прошлого захода это **37 проб, дверей ноль**. Имён с `set_`/`toggle_`/`write_` в кандидатах не было сознательно. Наружу видно только зеркало ручек и капы через `quote_price`. Новую дверь не делал, Apps Script не трогал. Опись угла взята не живьём (двери не …обрезано
