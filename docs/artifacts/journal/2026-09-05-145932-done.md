# Тело записи журнала — 2026-09-05 14:59 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1482 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-05 14:59:32 |
| символов в строке | 1482 |
| порог | 600 |

---

DONE Dispatch 05.09 разведка заявки #33 (вид УПРОЩАЕМО, канал codex): посылка проверена кодом, не отчётом. ГЛАВНОЕ ОПРОВЕРГНУТО — отдельного предиката повтора у тренера НЕТ: trainer._repeat_of берёт getattr(suggest,'_rules_similar'), своих определений в trainer.py 0. ЖИВО: CLAUDE.md:195 (suggest.py:689 PLAYBOOK_FILE = manager-bot/docs/playbook.md, git ls-files 0, mtime 2026-07-23 01:26); книга 15 буллетов, набор append 9, набор тренажёра 14, разница 5 правил; _DUP_SHARE_MIN=0.5 жив (trainer.py:319, коммит 9a1e96b). ЧЕГО НАХОДКА НЕ УЧЛА: (1) замок нужен ровно из-за широкого набора — вызывающий его буллет лежит в разделе Стиль (playbook.md:7), которого append не видит вовсе; (2) у общего предиката 3 боевых места вызова (suggest.py:754 append, :867 replace, :947 remove-селектор) — замок внутри него правит клиентский контур и живую книгу ВНЕ git; (3) наборы разные по предмету, а два уточнения разнонаправлены (набор строже, замок мягче) и осмысленны только парой. ЗАКРЫВАЕТ находка: порог 0.5 действительно не измерен (обоснован одним промахом), и второй источник истины есть — но это ПАРСЕР буллета (trainer.py:312 vs suggest.py:727), а не предикат. НЕИЗВЕСТНО: сколько гипотез отсеклось бы иначе (last_dropped в KB_trainer_log не пишется) и сколько тестов покраснеет (тесты НЕ запускал — test_trainer.py:1140 пишет в живой moderation_ipc). Боевого кода не правил. Артефакт → docs/artifacts/2026-09-05-разведка-zayavka-2282ccc8431d.md, коммит 316c0f7
