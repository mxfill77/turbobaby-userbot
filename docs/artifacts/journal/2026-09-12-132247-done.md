# Тело записи журнала — 2026-09-12 13:22 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1720 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-12 13:22:47 |
| символов в строке | 1720 |
| порог | 600 |

---

DONE Dispatch 20:26: Врезка 60-d — слушатель ЧИТАЕТ присланный снимок, но ещё не в живом процессе. Место поимённо: userbot_listen.on_trainer_group, строки 432-458, импорт trainer_photo строкой 42. Связка одна — trainer_photo.intake: скачивание СРАЗУ в docs/vhodyashchie-snimki (download получил target=), тут же РОВНО одна строка индекса через photo_inbox.commit_save, и ТОТ ЖЕ файл читается головой в asyncio.to_thread (синхронный subprocess на 90 с иначе заморозил бы весь слушатель). Строка пишется ДО чтения: сорвись чтение, факт доставки всё равно у Штаба. Первой строкой trainer.bump_seq() — чтение 78..88 с против дебаунса 8 с, иначе бот ответил бы на подпись «пришлите фото» за 8 с. Переиспользованы обе половинки без переписывания: 60-a (download/describe/transcript_body/human_note) и 60-b (plan_save/commit_save/sender_label/_fix_extension) — второй папки и второго индекса не заведено. Тесты без сети и без живого процесса: 21 новый в test_photo_intake — путь целиком (загрузок 1, кругов головы 1, строк 1), без вложения 0 строк и 0 чтений и индекс даже не создан, повтор даёт ПОБАЙТОВО тот же индекс и 0 загрузок, сорванная загрузка — НЕ СКАЧАН с причиной словами и 0 кругов головы; врезка судится РАЗБОРОМ AST, а не подстрокой. 209 соседних тестов зелены, вклад в храповик неразбора 0 очков. ПРИМЕНЕНИЯ НЕТ и это не провал: живой userbot PID 10892 (старт 13:10:01) не тронут и не перезапущен, Python модуль на лету не перечитывает — врезка включится при следующем подъёме процесса. Наружу не ушло ни одного сообщения, живых кругов головы ноль. Коммиты e99451e (код и тесты) и 79d4c36 (артефакт docs/artifacts/2026-09-12-ВРЕЗКА-slushatel-chitaet-1209.md). ВРЕЗКА slushatel-chitaet 1209
