# Тело записи журнала — 2026-08-13 06:16 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-13 06:16:19 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #507 → done · Класс 497 **был уже закрыт** коммитом `d7f247e` — задача переотправлена. Переделки не делал: проверил фактами, доказал что замок краснеет, дособрал недостающее (артефакт-основание лежал вне git). Полный гейт: 2 красных — доставшиеся, не мои. **FACT:** `approval_ttl=1800s` (до `d7f247e`) → `2760s` (после) в живом баннере демона `pc_orchestrator.log:6111/6159`, в проде `a8d0b39`. Новые коммиты `3321233` и `7e5b036` в `origin/main`, `git rev-list origin/main...main` → `0 0`. **1) Оба числа строкой кода** - `pc_orchestrator.py:110` — `TASK_TIMEOUT = int(os.getenv("PC_TASK_TIMEOUT","2700"))`, жёсткий рез `run_claude`, своя полоса. - `APPROVAL_TTL` был литералом `1800` — пришёл первым коммитом порта с VPS (`1eaa79c`) вместе с чужой посылкой, на своей полосе не мерился никогда. Теперь `:163` — не литерал, а `approval_ttl_effective(APPROVAL_TTL_ASKED)`. **2) Способ** — пол `TASK_TIMEOUT + POLL_SEC` = 2760с: окно, в котором демон физически не читает очередь (исполняет синхронно), плюс виток на замечание ответа. **3) Замок кусается** — не декларация. Проба возвращает срок к 1800: ``` AssertionError: 1800 not greater than 2700 : срок карточки короче таймаута прогона — вернулся класс 497 (74 и 78: «да» пришло вовремя и сгорело за занятость исполнителя) ``` `test_floor_follows_timeout` доказывает **выводимость** (600/2700/5400/9000), а не совпадение двух чисел сегодня. Ручка `PC_APPROVAL_TTL` поднимает, но не опускает; мусор → пол; клемма шумит в баннере. **4) Забор цел** — `test_fence_st …обрезано
