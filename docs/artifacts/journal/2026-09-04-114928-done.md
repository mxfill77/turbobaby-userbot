# Тело записи журнала — 2026-09-04 11:49 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1428 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-04 11:49:28 |
| символов в строке | 1428 |
| порог | 600 |

---

DONE Dispatch 19:05: суточный потолок ящика Штаба поднят 8 -> 40 в ОБОИХ экземплярах разом (shtab_box.DAILY_BUDGET + умолчание pc_orchestrator.SHTAB_BOX_BUDGET), коммит f89d8dd. Премиса проверена своими командами: живое чтение обеих полос дало у ПК за 2026-09-04 РОВНО 8 маркеров и budget_left=0 при 18 лежащих документах (лог с 17:57 трижды «документов 18, взято 0, отложено 20»); экземпляра действительно два — бой держит второй, сводка печатает первый; сигнал А сработал сегодня ДВАЖДЫ (метки 545c9d90e1aa, 779dfa3b3b1f), сигнал Б ОДИН раз (fb943361760b). Обоснование переписано: 40 — ловушка на разгон, а не делёж; физпредел полосы измерен и он МАКС 30 заходов/сут, плюс одна задача за виток при паузе 600с = 6.7 часа одних пауз на сорок взятых. Замок против предсмертного взгляда усилен: умолчание демона сверяется теперь С КОНСТАНТОЙ, а не с литералом, — проверено делом, откат одной копии в 8 красит набор. ОТРИЦАТЕЛЬНЫЙ ТЕСТ на 50 блоков: внутри витка держит «не больше одной за виток» со ВТОРОГО блока, между витками потолок захлопывается на блоке №41, взято 40, осталось лежать 10, следующие сутки полоса снова берёт. Не тронуты ни строкой: одна за виток, дедуп, отзыв переименованием, замок владельца, сигнальные остановки, приёмка, ворота приёма. Тесты набора ящика: test_shtab_box + _accept + _signals = 245, OK. ARTIFACT -> docs/artifacts/2026-09-04-суточный-потолок-ящика-поднят-до-сорока.md
