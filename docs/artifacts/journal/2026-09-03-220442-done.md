# Тело записи журнала — 2026-09-03 22:04 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1566 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-03 22:04:42 |
| символов в строке | 1566 |
| порог | 600 |

---

DONE ящик 04.09 (ключ=bridge-budget.0904, задача 158): ПОТОЛОК ВРЕМЕНИ НА МОСТ В ОДНОМ ВИТКЕ — 900с, коммит b901d35. Премиса Штаба проверена замером и РАЗОШЛАСЬ: боевой таймаут не 30с, а 90с (Bridge(timeout=90)), и в оценке 30x3x6 пропущен внешний READ_TRIES=3 — один GET держит виток до 4802с = 80 мин, а не 9 мин; ящик тратит до 13 обращений, а не 5; за сутки растянутых витков пять, но класс «мост» несут три (339+339+315=993с), два помечены самим демоном как «длинный виток» = работа задачи. Порог 900с измерен корпусом 200 залипаний живого лога (медиана 349, p99 779, MAX 1437): срезал бы 1 виток из 200, тогда как 600с срезали бы 11 здоровых; сверху заперт сторожем 1800с, которого не трогал ни строкой. РЕЖЕТСЯ ТОЛЬКО ЧТЕНИЕ: все POST и _in_status (формально GET, но это доказательство пути записи CLAIM-VERIFY — срезав его, превратили бы долетевший claim в сироту) выведены из-под потолка; при этом «можно резать» и «платит время» разведены разными флагами — мутация не режется, но время отдаёт, иначе медленный POST до 1721с был бы для витка бесплатным (эту болезнь первой версии поймал собственный тест). Сброс и событие — в теле главного цикла, а не в poll_once, иначе ящик, ступени и витрина остались бы вне прибора. Исчерпание — событие со словами один раз за виток; третий исход KIND_BUDGET: ящик берёт ноль и говорит причину словами. Отрицательный тест есть: быстрый мост 2.7с x 40 не ловит потолок ни разу. Тесты затронутых наборов целиком 1290 OK, из них 16 новых; вклад в храповик неразбора ноль. Откат PC_BRIDGE_LOOP_BUDGET=0
