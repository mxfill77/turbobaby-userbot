# Тело записи журнала — 2026-08-01 10:06 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-01 10:06:44 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #165 → done · **Задача уже выполнялась сегодня (коммит 4c5daa3, артефакт `docs/artifacts/2026-08-01-platform-memory-recon.md`); этот прогон — независимая перепроверка + новые факты, дописаны в §6 (коммиты 884e30c, 3390d6f, запушены). Направление вердикта не изменилось, но появились две отсечки, которых не было: индекс памяти рвётся за 6,8 часа, и приёмник вправе отказать в записи.** ## 1. Что платформа даёт из коробки — четыре поверхности, не одна | поверхность | хранилище | версии/аудит | пригодность | |---|---|---|---| | **memory tool** `memory_20250818` | **бэкенд пишешь ты** | нет вовсе | это наш же `cowork_log_append.py`, только чужой схемой | | **Managed Agents memory stores** (β `managed-agents-2026-04-01`, только 1P API + AWS) | `memstore_…`/`mem_…` | полные (§2) | единственный настоящий кандидат | | **Files API** | 500 МБ файл, 100 ГБ на орг. | нет | дописывания нет, только перезалив | | **«Synced project memory» / team memory внутри самого Claude Code** (найдено в этом прогоне) | файловая память → общий стор, `.md/.txt/.json/.jsonl` | те же серверные (общий контракт) | важна тем, что **не требует API-ключа рабочего пространства**; здесь **выключена** | Лимиты (извлечено дословно из CLI 2.1.218): **102 400 байт на документ** (`content_too_large`), путь ≤1024 байт и ≤20 сегментов, **макс. 8 сторов на сессию** (привязка только при создании сессии), TTL нет — живёт до удаления, `archive` без разархивации. **Поиска по содержимому на стороне хоста нет**: `ListMemories` фильтрует по `pa …обрезано
