# Тело записи журнала — 2026-08-04 18:01 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-04 18:01:10 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #283 → done · Разбор класса сделан замером, а не грепом: перехват записей показал, что зелёный гейт «2936 OK» портил **два** боевых файла (`cowork_log.ledger` — вытеснение 13 настоящих записей журнала; `client_watch.json` — снимок надзора заменён фикстурой). Правило поставлено в саму константу (`log_setup.state_path`), проверено гейтом-2 и побайтным бэкапом. Коммиты `1724eb5`, `921c9c6`. ## FACT-отчёт **ЗАМЕР.** Потеряно суммарно **31** находка: 19 до спула (замер 01.08 по журналу) + 12 съедены гейтом 05.08. **В спуле сейчас 0** — `[]`, 2 байта, mtime 05.08 00:07:47, не менялся с момента порчи. **Поправка к артефакту инцидента:** состав тех 12 восстановим **полностью** по логу — 4 (пост-релиз/чеки, окна 244119252 и 1226243168) + 8 (клиентский контур, строка демона называет число прямо) = 12. Артефакт считал «4+7=11, одна неопознана». Заодно видно, что второй замок `6d41c37` отработал: `since=` у прогонов 14:27 и 20:28 одинаков — метка не сдвинулась. **Вопрос 1–2 — что гейт трогает, факт по перехвату** (`open`/`os.replace`/`sqlite3.connect` с привязкой к тесту; команда гейта осталась канонической через `PYTHONPATH`): | путь | было | статус | |---|---|---| | `cowork_log.ledger` | `open:w`, 9 тестов, **104697 → 101052 б** — кольцо на 500 строк вытолкнуло 13 настоящих записей за 01.08 (задачи #173–#176) | **закрыто** | | `pc_orchestrator.client_watch.json` | `os.replace`, **268 → 64 б**, фикстура `ts=1301.0, children={}`; читает отдельный процесс `pc_agent status` | **закрыто** | | `trainer_ru …обрезано
