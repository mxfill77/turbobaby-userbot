# Тело записи журнала — 2026-08-23 13:05 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1424 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-23 13:05:39 |
| символов в строке | 1424 |
| порог | 600 |

---

DONE Dispatch 20:06: проверка разгона ЦП замером (read-only). РАЗГОН ЕСТЬ: i7-8700K держит 4701 МГц при ВСЕХ ШЕСТИ активных ядрах против паспортных 4300 — Multi-Core Enhancement платы MSI Z390I; 13 наблюдений из 25 при полной загрузке всех 12 потоков, без исключений. 20 замеров в покое: MIN=MAX=AVG=4701, разброс 0 — но это объясняется планом «Высокая производительность» с минимальным состоянием 100% и разгона САМО ПО СЕБЕ не доказывает; доказывает поведение под шестиядерной нагрузкой. Прошивка объявляет 4701 номиналом, C-состояний одно, Speed Shift не задействован. Vcore штатно НЕ читается (бит 7 снят) — главный незакрытый факт. BIOS 1.A0 от 10.10.2020: не старше ЦП (тот 2017), но старая на 5 лет 10 мес. Жалоб на питание/сброс частоты в журналах нет; зато 13 из 17 падений БЕЗ стоп-кода вовсе. СНЯТ факт, названный прошлым заходом неснятым: все 13 WHEA — банк MCA 0 = блок выборки инструкций; в банках памяти и TLB ноль, 10 из 13 на одном ядре. ПОПРАВКА ПРЕМИСЫ: ошибок TLB нет ни одной (вместо них 2 ошибки внутренней чётности), но «внутри процессора» верно. Версии: В1 разгон > В2 деградация > Б планки (почти закрыта чтением) > А перегрев. Раздел дописан в живую карту, узел master по имени KB_MASTER из живого реестра, 418492→428266 симв., расписка ok. Артефакт → docs/artifacts/2026-08-23-cpu-overclock-check.md. Ничего не ставилось, нагрузки не гонялись, BIOS не трогался, коммитов нет.
