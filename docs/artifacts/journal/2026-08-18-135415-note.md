# Тело записи журнала — 2026-08-18 13:54 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-18 13:54:15 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #71 → done · Автологон **СРАБОТАЛ**: старт системы 20:27:34.852 → начало сеанса 20:27:39.856 = **5.004 с**; жеста ПИН в момент входа нет (ближайший на +119.9 с — это разблокировка). Экран запирает СВОЯ задача `tb_lock_on_logon` через 5 с — вот почему «признак стёрся». Лок модербота сейчас живой (10948); мёртвым был **15760**. **FACT: read-only.** Ни одной правки: реестр, Планировщик, процессы, база не тронуты; единственный созданный файл — артефакт `docs/artifacts/2026-08-18-autologon-verify.md`. Живые доказательства: `Kernel-General ID=12` **20:27:34.852**; `Winlogon 7001` **20:27:39.848**; `UserProfileService ID=1` **20:27:39.856**; `HelloForBusiness 5002/5001` только **20:29:39.800/.996**; `\pc_agent` LogonTrigger `LastRun 20:27:40`; `moderation_bot.log:7114` → «устаревший moderation_bot.lock (PID **15760** мёртв) — забираю». --- **1. Переключатель — ВКЛЮЧЁН.** `AutoAdminLogon=[1]`, `DefaultUserName=[mxfill1]`, `DefaultDomainName=[MXFILLPC]`, `AutoLogonSID=[S-1-5-21-…-1002]`. `AutoLogonCount` **отсутствует** — значит вход не одноразовый, повторится на каждой загрузке. **2. Пароль в хранилище — лежит (косвенно).** `DefaultPassword` в реестре: **False** (открытым текстом нет — правильно). Прямая проба хранилища невозможна дословно: `HKLM:\SECURITY\Policy\Secrets` → `System.Security.SecurityException :: Requested registry access is not allowed` (ветка открыта только SYSTEM; повышение не помогло бы — нужна смена владельца, это правка). Доказательство функциональное: вход состоялся за 5.0 с …обрезано
