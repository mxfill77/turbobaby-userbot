# Тело записи журнала — 2026-08-01 09:52 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-01 09:52:20 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #164 → done · Разведка read-only завершена: кандидат один — memory stores (Managed Agents, бета). Аудит/откат у платформы сильнее нашего, но 100 KB на документ против 280 KB живого `cowork_log`, а Штаб читает Drive-коннектором, к memory stores коннектора нет. Артефакт: `docs/artifacts/2026-08-01-platform-memory-recon.md`, коммит `4c5daa3`. Обе строки журнала ушли в спул — мост отвечает таймаутом. ## 1. Что предлагает платформа Три разные вещи, путать их — ложный диагноз: | | механизм | хранит ли платформа | версии/откат | |---|---|---|---| | **Memory tool** `memory_20250818` (ГА) | каталог `/memories`, команды view/create/str_replace/insert/delete/rename | **нет — бэкенд твой** | нет вовсе | | **Memory stores** (Managed Agents, бета `managed-agents-2026-04-01`) | `memstore_…` → `mem_…` (≤100 KB) → `memver_…`; FUSE-монтирование `/mnt/memory/<store>/` в контейнер сессии + REST `/v1/memory_stores/…` | **да** | **да** | | **Files API** (бета) | 500 MB/файл, 100 GB/орг, операции бесплатны | да | нет | Кандидат один — **memory stores**. Срок хранения: TTL нет, живут до удаления; `archive` делает стор read-only **без разархивации**. Макс. 8 сторов на сессию, привязка только в момент создания сессии. Доступность: **β**, только первопартийный API и Claude Platform on AWS (Bedrock/Vertex/Foundry — ❌), нужен API-ключ рабочего пространства. **Поиска по содержимому host-side нет** — только фильтр по `path_prefix`/`depth`; `grep` живёт лишь внутри контейнера сессии. ## 2. Аудит и откат — главное | вопро …обрезано
