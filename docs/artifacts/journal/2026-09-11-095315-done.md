# Тело записи журнала — 2026-09-11 09:53 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2698 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-11 09:53:15 |
| символов в строке | 2698 |
| порог | 600 |

---

DONE ПК 16:50: задание Штаба 32-a-imya-ryada.1109 — у закрытого ряда очереди появилось УСТОЙЧИВОЕ ИМЯ, и реестр судьи хранит его вместо переиспользуемого номера. Премисы П1-П3 подтверждены прогоном первым действием: реестр — плоский {номер: вердикт} на 158 ключей (перепись мерила 157), поля дня нет ни одного; переиспользование номера доказано 4 инверсиями seq против at из 7 пар; считающая функция серии одна, боевых вызовов ровно два. ИМЯ = ключ маркера плюс номер, из первой строки САМОГО ряда (ключ несут 10 закрытых строк из 10). Не «номер+день»: два сброса нумерации в одни сутки 10.09 он не различает; не «ключ» один: переотправка сохраняет ключ. Дня в имени нет потому, что взять его нечем — часы судье запрещены тестом TestPurity. Третий вопрос назван вслух: имя поднимается БЕЗ события (ряд несёт ключ с минуты создания), поэтому событие доказывает не имя, а ЗАПИСЬ под ним, которую делает только судья. Сшивок переведено две и обе названы: писатель done_judge_pc.note (ключ = имя, поданное параметром — своего разбора маркера у судьи нет, обратного импорта тоже) и читатель contour_digest.judged_of/row_name; точка вызова pc_orchestrator:4030. Остальных 43 мест переписи не трогал. Миграция боевого реестра: перенесено 7, имя восстановить нечем у 151 (исход НЕИЗВЕСТНО, а не догадка — дата из ПРИЧИНЫ покрыла бы 144 записи и была бы догадкой в 144 случаях), вердиктов потеряно 0, тела 158 из 158 совпали байт-в-байт. 151 — не потеря: закрытая половина слепка это окно СУТОК, и счёт серии старше суток не читает. Доказано числами одного слепка: до правки proved 5 blind 1 unjudged 3, без миграции новый код дал бы proved 0 unjudged 9, после миграции снова proved 5 blind 1 unjudged 3. Серию руками не обнулял: streak 0 во всех трёх состояниях. Откат побайтный: копия tmp/pc_report/judged_before_32a.json, способ — copy поверх без удалений, отпечатки сверил ОТДЕЛЬНЫЙ процесс (sha256 8adbc500 до = после откатa в песочнице). Отрицательный тест TestRowNameNotNumber, 12 проверок: на HEAD 3 падения и 7 ошибок, после правки 12/12 OK; контроля обратной стороны два и оба зелены ДО правки (честная цепочка считается, серия растёт на трёх рядах). Три прогона затронутого класса 165-165-165 OK, класс показа неизвестности 116 OK (f5dc9ab цел), смежные 1301 OK. Своё расхождение назвал: первый вариант теста занял уже занятое имя помощника _closed и уронил 28 чужих тестов — тот же класс поймал автора на своём поле. Коммиты ff8b149 (код, 4 файла +239/-8) и 0f74f1c (артефакт). Потрачено 1253 с из потолка 2700 при замере 131 с на единицу. Наружу не ушло ничего, процессов не трогал, .env не читал, ничего не удалял — гард остановил rmtree песочницы в миграции, вызов снят.
