# Тело записи журнала — 2026-08-02 13:11 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1352 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-02 13:11:01 |
| символов в строке | 1352 |
| порог | 600 |

---

DONE Dispatch 02.08 21:05: пять мёртвых посылок (Д-1..Д-5) починены, коммит fb239df запушен (165c6cb..fb239df). Две прикрывали ТИХОЕ ИСПОЛНЕНИЕ красного: Д-3 обычный вид без объекта уходил в журнал (замер по полю строки лога: 22 события ветки за 22.07-01.08, все на kill и schtasks, ноль после 31.07 — ветка лишилась и посылки, и входа); Д-4 _stays_red отдавала py_write в defer, когда гард КОДА НЕ ВИДЕЛ — 121 живой запуск из 136 проходил молча, включая os.remove и боевую проводку через heredoc, при том что тот же снос через rm даёт карточку. Причины Д-4 механические: posix-shlex съедает обратные слэши в Windows-путях, ветка stdin выходила до сбора скан-текста. Сперва научили гард ВИДЕТЬ код, потом «не видел» = красное; обратная половина проверена — из 9 штатных команд остановлено 0. Д-1 PREAMBLE обещала кнопку, которой нет (инверсия: послушный исполнитель не оставляет маркера и карточки владельцу нет вовсе) — текст приведён к правде, тест сверяет промпт с конвейером. Д-2 fail-closed не ослаблен, но причина теперь берётся из _reply_channel и закреплена тестом. Д-5 две мёртвые функции сняты, класс закрыт сторожем _EXTERNAL_API (сироты выводятся по AST). Четыре зелёных теста, охранявших мёртвые обещания, приведены к правде. Гейт полный: 2826 тестов OK. Разбор → docs/artifacts/2026-08-02-five-dead-premises-fixed.md
