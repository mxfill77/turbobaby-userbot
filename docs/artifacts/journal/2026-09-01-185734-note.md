# Тело записи журнала — 2026-09-01 18:57 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-01 18:57:34 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #99 → done · Разведка закрыта артефактом. Причина http_400 названа числом: пакет уходил ОДНИМ сообщением, а канал держит 5000 estimated tokens — граница на корпусе дня (7689, 11394] знаков. Класс уже закрыт коммитом 76b4447 (деление на части): 10 отказов ДО, 5 ok ПОСЛЕ, исключений 0. Повод верен как факт и протух как задача — второй такой подряд. **FACT:** коммиты `1783fd4` (артефакт + скрипты замера) и `352091e` (тело журнальной записи) в `git log`; замер воспроизводим из git: `venv/Scripts/python.exe docs/artifacts/2026-09-01-разведка-manus-http-400/measure.py` → «сверено хешей : 19 из 19». ## Что установлено **Причина числом.** Отправленный текст восстановлен `review_send.build_prompt` и сверен с хешем, который отправщик сам записал в ответ — **19 из 19 совпали**. Значит числа описывают ровно то, что видел канал: | исход | n | знаков (min…max) | |---|---|---| | `http_400` | 10 | 11 394 … 14 408 | | `ok` | 5 | 7 470 … 12 218 | | `accepted_no_answer` | 3 | 6 981 … 7 689 | Самое длинное принятое целиком — 7 689, самое короткое отбитое — 11 394. Граница 5000 estimated tokens лежит в **(7689, 11394]** знаков. **Ловушка, которую пришлось обойти:** размеры `ok` и `http_400` перекрываются (пакет 97 на 12 218 знаках доехал, пакет 78 на 11 394 был отбит). По размеру вывод получился бы ложным — делит не размер, а эпоха прибора. Все 10 отказов легли до коммита `76b4447` (01.09 22:26:51), все 5 `ok` — после. **Прибор или полоса.** Предел 5000 токенов — свойство ПОЛОСЫ (дословное тело `{"code":3,"me …обрезано
