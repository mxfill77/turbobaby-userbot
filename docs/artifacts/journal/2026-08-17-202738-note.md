# Тело записи журнала — 2026-08-17 20:27 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1570 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-17 20:27:38 |
| символов в строке | 1570 |
| порог | 600 |

---

NOTE Orchestrator: задача #41 → done · Committed as `a9599a3`. Final report: Прибор О2 крикнул «демон не даёт оборота», пока демон штатно исполнял задачу 40 (39 мин, done). Причина: штамп занятости ищется по полю `at`, а оно на повторно выданном id очереди не обновляется — свежайшей осталась отметка задачи 34, закрытой 2 ч 43 мин назад. Сторож демона на ТОМ ЖЕ файле читает mtime и в ту же минуту отвечает верно. Дефект воспроизведён живым на этой же задаче. **FACT:** commit `a9599a3` в `git log` → `docs/artifacts/2026-08-17-false-alarm-turn.md`. Живое доказательство: предсказание `probe4.py` в 20:15 UTC («тик 20:24:29, ключ `o2t|1786997057`») сбылось дословно — `tmp/expect_pc/state.json`: `{"o2t|1786997057": {"first": 1786998269.4459536, "kind": "o2_pc_turn"}}`, `silent=1200.0002`. Демон PID 5540 жив; его сторож в ту же минуту: «идёт объявленный заход (1020с из 4500с) — демон работает». 1. **Оборот = виток `poll_once`.** Время — файл `pc_orchestrator.heartbeat` (`expectations_pc_run.py:80,150`), пишет `_write_heartbeat()` (`pc_orchestrator.py:966`) ровно один раз последней строкой витка (`:3755`). Порог 20 мин (`expectations_pc.py:301`), сравнивается накопленная тишина бодрствования. 2. **Знает — и не спросил.** `busy_facts` (`expectations_pc_run.py:295-319`) берёт максимум по полю `at`; `os.stat` там нет. Сторож демона `_wd_busy_age` (`pc_orchestrator.py:8853-8871`) на том же файле берёт `st_mtime` — один syscall на уже открытом файле. Разрыв: `at`=16:24:16 против mtime=20:06:02 = **3.70 ч**. Второй непрочитанный …обрезано
