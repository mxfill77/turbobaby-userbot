# Тело записи журнала — 2026-09-06 06:28 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1806 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-06 06:28:26 |
| символов в строке | 1806 |
| порог | 600 |

---

DONE Dispatch 13:45: правило порога заведено и применено ко всему списку разом (задание Штаба 00-h-threshold-rule.0906, коммиты e4bbe13 + 6c6cdb2). Диагноз Штаба проверен и верен: правило «порог сверяется с замером» в каноне есть, но его не проверяло ничто — два ложных порога прожили 24 и 16 суток, и нашла их авария, а не прибор. Продукт: машиночитаемый ПАСПОРТ ПОРОГА рядом со значением (терпит/замер/снят/делится/предел-единицы/род), судья threshold_ledger.py + руки threshold_ledger_run.py, 22 юнита. Краснеет на: записи нет, паспорт битый, замер протух, файл не прочитан; честное «замера нет» не красное (менять такое значение запрещено), замок — храповик счёта. Пять отрицательных тестов, главный снимает НАСТОЯЩИЙ паспорт с ЖИВОГО price_gate.py и требует красного, с контрфактом по звену на том же файле. Открытие ищет ФОРМУ, а не список имён, и нашло 2 порога сверх списка премисы. Горизонт протухания сам снят с замера полосы (протухания 16 и 24 сут → половина короткейшего = 8), основание n=2 названо слабым в самом паспорте, судья покрывает сам себя. След срабатывания: дедлайн сетки и дедлайн подбора по классу теперь говорят ЧИСЛО выброшенного (у второго в живом журнале было РОВНО НОЛЬ строк — следа не было вовсе), срез отказов проб перестал молча терять хвост. ЧИСЛА: 21 порог в охвате, замер у 8, «замера нет» у 12, «не порог» 1, свой предел единицы у 2; вне охвата названо числом — pc_orchestrator.py 71 кандидат. ЗНАЧЕНИЯ НЕ ТРОНУТЫ НИ У ОДНОГО ПОРОГА. Соседи: 9 наборов зелены (test_pc_orchestrator 1061 OK), 2 павших разобраны контрфактом на дереве HEAD из git archive — оба падают ДО правки (замыкание pc_agent от чужого aa0de0e; E2E-смоук без канала), ослаблять ничего не потребовалось. Артефакт: docs/artifacts/2026-09-06-правило-порога-запись-замера-06.09.md
