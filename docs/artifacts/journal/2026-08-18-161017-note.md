# Тело записи журнала — 2026-08-18 16:10 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1570 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-18 16:10:17 |
| символов в строке | 1570 |
| порог | 600 |

---

NOTE Orchestrator: задача #1 → done · Разведка read-only завершена: строки о закрытии теряются гонкой перезаписи целого дока — журнал `cowork_log` перечитывается и записывается ЦЕЛИКОМ без замка, писатели спавнятся fire-and-forget, одна запись длится 10.8–40.2 с, и начавшего раньше затирает следующий уже ПОСЛЕ того, как тот подтвердил себя обратным чтением. За сутки: взято 20, закрыто 19, запись об исходе есть у 18, нет у #71. **FACT:** commit `313bfba` в git log · артефакт `docs/artifacts/2026-08-18-close-notes-gap.md` на диске · строка ARTIFACT в живом доке проверена повторным чтением (`DONE 2026-08-18 16:08 UTC: ARTIFACT почему записи о закрытии…`, первая строка дока) · read-only: кода не правил, очередь не трогал, процессы не трогал, гейт не гонял. **Временный каталог:** `_scratch_closenotes_0818/` (снимки доков и скрипты сверки, вне git). --- **1. Два разных пути, не один.** Взятие — `pc_orchestrator.py:2975`, сразу после `claim_task`, точка **одна**. Исход — `pc_orchestrator.py:3052` и ещё **20 точек** (рычаг, смоук, approved, TTL, сирота, самопочинка, цепь, урок, ревизор). Обе руки — `_cowork` (`:783`), который **не пишет сам**, а спавнит `cowork_log_append.py` через `Popen` с потоками в DEVNULL: исхода вызова родитель не узнаёт никогда. **2. Ветки потери.** Главная — гонка целого дока: `compose`+`write_doc` кладут весь текст (`cowork_log_append.py:331-345, 457`), замка нет, `readback_landed` (`:348`) видит строку только в свой момент, `ledger_add` (`:475`) отмечает её успешной. Отсюда: **реестр врёт полож …обрезано
