# Тело записи журнала — 2026-09-03 08:41 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1763 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-03 08:41:38 |
| символов в строке | 1763 |
| порог | 600 |

---

DONE Dispatch 15:55: ящик Штаба переехал с блоков в узле на ОТДЕЛЬНЫЕ ДОКУМЕНТЫ папки мозга (коммиты f7cbe20 + 9a93091). Премиса живым вызовом 03.09: list_brain_folder ok, детей 50, shtab_box_probe в списке, ключа в реестре у него нет (без ключа 15 из 49) — и читается такой документ ТОЛЬКО по file id, на имя мост отвечает unknown_name, второй двери не существует. Префикс shtab_task_ (константа в одном месте; замер: shtab_box→2 попадания, shtab_box_→1 проба, shtab_task_→0 — чистое пространство). Порядок разбора по имени, сортируем САМИ: у файла в ответе ровно три поля (id/mime/name), времени нет вовсе. Мина усечения с числом: limit=3 на 49 файлах вернул НЕ три первых по алфавиту — мост сортирует уже обрезанный кусок, поэтому truncated=True это НЕИЗВЕСТНО и ноль постановок, слово «пусто» не звучит. Отзыв переименованием shtab_task_<ключ>.СНЯТО (4 разделителя, регистр не важен); метка, а не снятие префикса, потому что без префикса документ пропадает МОЛЧА. Узел shtab_box остался человеческой шапкой и держит метки снятия сигналов, источником задач быть перестал, блок старой формы в нём НЕ БЕРЁТСЯ но ДОКЛАДЫВАЕТСЯ словами. Все шесть замков и четыре сигнала перепроверены ЗАНОВО на новом источнике, шесть отрицательных тестов с положительными контролями. Инвариант «только чтение» переведён на белый список читателей плюс тест, что новая дверь brain_writer.list_folder сама ничего не пишет. Честная цена переезда записана тестом: отказ ворот теперь стоит одного чтения done, зато уже взятое задание не стоит чтения тела вовсе — меняем растущую цену на постоянную. Тесты набора 159, трижды одной командой 159/159/159; гейт демона 1017 зелёных; живая проба --status прошла весь путь. Откат: стоп-файл pc_orchestrator.shtab_box.off.
