# Тело записи журнала — 2026-09-02 14:46 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1570 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-02 14:46:08 |
| символов в строке | 1570 |
| порог | 600 |

---

NOTE Orchestrator: задача #9 → done · Разведка заявки #3 (`47b3131c052c`, вид ПЕРЕУСЛОЖНЕНО) закрыта артефактом. Посылка находки по `contour_digest.py:76` **жива**, но вывод канала подтверждён лишь частично и не своей причиной: одна из трёх меток мертва по вложению префикса, а два главных довода находки опровергнуты замером. Боевой код не правился, обе правки названы словами. **FACT:** commit `32edb8d` (артефакт, 234 строки) и `e6fa5e5` (тело журнала) в `git log`; строки ARTIFACT и DONE легли в мозг (`OK: записано в мозг, символов: 813325`). ## Что проверено фактом **Посылка ЖИВА.** `contour_digest.py:473` → `read_only_marks()` сегодня возвращает `('[заявка-ревью', '[разведка', '[разведка-заявка')` — три метки из двух чужих модулей, ровно как описывает находка. Заявка держит `premise: alive`, чтение это подтверждает. **Что находка ЗАКРЫВАЕТ** (единственное, и найдено не по её доводу): `ASK_MARK = "[разведка-заявка"` начинается с `TASK_MARK = "[разведка"`, поэтому в `any(startswith)` решить ничего не может. ``` ЗАМЕР E · метки, поглощённые префиксом другой метки: 1 — ['[разведка-заявка'] ЗАМЕР F · без метки '[разведка-заявка' исход меняется у 0 строк (из 41 живой строки слепка) ``` **Чего находка НЕ учитывает — три вещи, каждая числом:** - «плюс git» — конфляция: git живёт в `contour_digest_run.git_moves:226` и питает раздел «оси», метки — в чистой логике и питают одно число `moved`. `series()` про git не знает. - «маркеры держатся ради одного агрегата» — неверно: владельцы `recon_auto.py` (4) и `review_intake.py` …обрезано
