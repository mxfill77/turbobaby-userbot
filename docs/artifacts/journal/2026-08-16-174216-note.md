# Тело записи журнала — 2026-08-16 17:42 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1570 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-16 17:42:16 |
| символов в строке | 1570 |
| порог | 600 |

---

NOTE Orchestrator: задача #25 → done · **FACT: commits `bb9c569` + `8822917` в git log; артефакт `docs/artifacts/2026-08-16-dialog-link-labels.md` на диске; две строки в мозге (391038 симв.)** Гипотеза штаба **ОПРОВЕРГНУТА**, но составно: диагноз верен (15.08 правда сравнивал несравнимое), лекарство невозможно. Функция обезличивания воспроизводима и секрета не требует — я пересобрал **все 710 меток живого корпуса из ВЫДУМАННОГО входа**. Именно это её и хоронит: метка есть функция **номера строки**, а не человека. ## Как это доказано числом | проба | результат | |---|---| | метки корпуса, пересобранные функцией из синтетического телефона/ника | **710 из 710** (username, name, peer_id, phone) | | 1000 разных выдуманных лиц при **одном** номере строки | **1** метка → константа по личности | | одно лицо при номерах строки 1..710 | **710** меток | Корпус подтверждает по всем записям: `peer_username` = `профиль_<номер строки>` 710/710, `peer_id` **затёрт номером строки** 710/710 (уничтожен идеальный telegram-ключ), `peer_phone` пуст 710/710. Модуль говорит это прямо (`anonymize_corpus.py:13–16`): между диалогами устойчивости нет **сознательно**. Посылка гипотезы — «один человек всегда получает одну метку» — для этого обезличивателя ложна. ## Три исхода сцепки - **по нику:** ОДНОЗНАЧНАЯ **0** · НЕОДНОЗНАЧНАЯ **все 403** (кратность 710 у каждого) · НЕ НАЙДЕНА **0** - **по телефону:** цель пуста **710/710** → ОДНОЗНАЧНАЯ **0** · НЕОДНОЗНАЧНАЯ **0** · НЕ НАЙДЕНА **все 644** Против 15.08 разница содержательная: тогда провал …обрезано
