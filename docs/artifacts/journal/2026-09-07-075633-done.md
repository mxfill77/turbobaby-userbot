# Тело записи журнала — 2026-09-07 07:56 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2143 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-07 07:56:33 |
| символов в строке | 2143 |
| порог | 600 |

---

DONE Dispatch 15:05: разбивка цены ПО ДНЯМ поставлена в код по ответу владельца (ключ 01-ca-price-split-code.0907). Сначала правило: ответ «полная скидка что бы разница была конечно же не такая» вписан в KB_business_rules дословно с датой и каналом, вопрос 2 (месячный потолок) помечен ПЕРЕОТКРЫТЫМ — рекомендация Штаба отозвана как ошибочная; узел 78939 → 80862 → 82640 символов, обратное чтение отдельным процессом LEN=82640. Потом код: price_source.term_total — множитель периода у КАЖДЫХ суток, ступень срока ОДНА и от ПОЛНОЙ длительности брони, округление ПОСУТОЧНО (округление в конце разошлось бы с сегодняшней ценой на 2 бата), итог = сумма по суткам; врезано в reprice; откат словом PRICE_SPLIT_DAYS_OFF=1. Коммиты a8c9f57 (код) и 0306a84 (артефакт). ЧТО НЕ СЛОМАНО ЧИСЛОМ: срок целиком внутри одного периода — 22680 строк корпуса (365 дат × 7 сроков × 14 моделей), СОВПАЛО 22680, РАЗОШЛОСЬ 0, тот же sha256 1e55fe07 до и после. КОНТРФАКТ с двух сторон: с разбивкой разошлось 11536 из 13090 строк границы, при откате — 0 из 13090, дословно прежний хеш cbaff41d. ТРЕТИЙ ИСХОД пережил и УСИЛЕН: период спрашивается у каждого дня, дыра в середине срока теперь видна (была невидима), 4 теста включая контроль фикстуры. ГЛАВНЫЙ ЗАПРЕТ ИСПОЛНЕН: условие применения потолка не тронуто ни битом, меняется только подаваемая ему сумма. ЧТО СЛЫШИТ КЛИЕНТ (названо, не решено): на точечной котировке 0 бат — сторож границы возвращается ДО счёта (292 окна корпуса); на прайс-сетке 3332 строки из 15330 (21.7%); 135 строк меняют фразу на «аренда от кепки» из-за новой суммы. При двух ставках day_price СНИМАЕТСЯ, среднее не подставляется; какого поля не хватает — названо (day_price_by_period), своё не заведено. ГОЛДЕНЫ dlg120 пересчитаны честно: живой случай сам лежит через границу P4-P5, 12880 → 13441, и разбивка увела его от уплаченных денег на 561 бат — сказано, а не спрятано. ТЕСТЫ: ценовой куст 155 OK, test_price_source 58 OK (16 новых); 8 красных в test_suggest/test_pricing/ledger/trainer — ЧУЖИЕ, доказано поимённым совпадением множеств при подмене price_source версией HEAD. Кругов тренажёра 0 из разрешённых 4.
