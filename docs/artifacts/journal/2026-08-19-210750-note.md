# Тело записи журнала — 2026-08-19 21:07 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-19 21:07:50 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #51 → done · Разведка read-only выполнена. Диагноз ставит **не упавший заход** — формально это демон и реаперы, но у господствующего исхода `done` весь тест родителя = `rc==0` + регексп `RESULT:`, то есть самооценка с проверкой орфографии. Из 95 закрытий за 7 суток: успех доказан **0**, законная остановка **0**, отказ доказан **2**, внешний отказ **0**, неизвестно **93 = 97.9 %**. **FACT:** артефакт `docs/artifacts/2026-08-19-outcome-taxonomy.md` создан (8 разделов); журнал принял две строки — `ARTIFACT` (484468 симв.) и финальный `DONE` (485063 симв., тело вынесено в `docs/artifacts/journal/2026-08-19-210701-done.md`). Кода не правил, процессов не трогал, гейт не гонял — `git status` по трекаемым файлам чист, изменён только новый артефакт. ## Ответы по пунктам **1. Коды.** Статусы очереди — `done` / `failed` / `needs_approval` / `approved` (`rejected` статусом не является — отказ владельца лежит в `failed` с префиксом). Причины провала — семь, `pc_orchestrator.py:1010–1022`: `approval_timeout`, `heartbeat_timeout`, `run_timeout`, `model_refusal`, `exec_error`, `unbacked_red`, `network_outage`. Едут в текст как `[причина=<код> · <имя>]`, разбираются обратно `FAIL_CODE_RE` (`:1046`). **2. Кто ставит — главный вопрос.** Ребёнок не ставит никогда: у него нет ни клиента моста, ни токена. Ставят трое — тот же процесс демона синхронно (`:1971`), позднейший виток/другой процесс демона (реаперы `:3732`, TTL `:3266`), и чужая полоса. Живое доказательство третьего: вердикт от **19.08 21:39:34** вын …обрезано
