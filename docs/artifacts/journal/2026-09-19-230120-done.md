# Тело записи журнала — 2026-09-19 23:01 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2303 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-19 23:01:20 |
| символов в строке | 2303 |
| порог | 600 |

---

DONE Dispatch 06:02: 68f PARTREZH 2009 — обе дыры захода 68e закрыты в хранилище уроков, коммиты 48ed3019 (код) и 8487645e (артефакт). ПАРТИЯ: десятая колонка, пятый разрез CUT_BATCH, ключ снятия стал парой (источник, партия); карточка называет заливки ЧИСЛОМ до движения; set_batch растит хвост лежащим строкам приёмом 11.09, чужих байтов не переписывая. РЕЖИМ: одиннадцатая колонка, ВЫЧИСЛЯЕТСЯ из источника и не является аргументом НИ ОДНОЙ из 10 дверей записи (замер: 0 дверей принимают его входом, одно присваивание в модуле); экспорт_переписки жёстко в бой, двусмысленный экзамен честно даёт НЕИЗВЕСТНО; вписанное рукой обучение права не даёт (mode_conflict, is_training fail-closed). Числа на копии живой таблицы: снята заливка №1 (3 строки) — заливка №2 цела 2 из 2, чужой набор 9 из 9, физических строк 15 на 15; КОНТРФАКТ без номера снял обе, 5 из 5. Режим: 8 дорог записи боевого источника, легло с обучением 0, с боем 8. Совместимость: ширин три (8,9,11), десятой нет, строка старого вида читается и даёт НЕИЗВЕСТНО, а не обучение — обе живые таблицы девятиколоночные, 24 строки из 24. Регресс 132 OK до правки и 132 OK после; с новым test_lesson_partiya (31 проверка) 163 OK; все test_lesson* 516 OK. Шесть упавших — пины прежней редакции формата, обновлены до новой с теми же точными числами, ни один не ослаблен. Полный прогон репо 7922 теста, 41 падение — все чужие: восемь модулей не импортируют изменённое и падают в одиночку теми же числами (30), девятый test_exam_show в одиночку ЗЕЛЁНЫЙ (11 падений от утечки EXAM_SET по порядку). ПОПУТНО замерена и починена мина, заданием не заказанная: batch_withdraw спрашивал часы ДВАЖДЫ, и снятие набора, переступившее границу секунды, становилось НЕВОЗВРАТНЫМ — 1 срыв на 25 прогонов; контрфакт на сам фикс проведён (вернул строку — покраснело, вернул фикс — позеленело). Боевые таблицы НЕ ТРОНУТЫ: sha256 A 7d19b1db и B 9b147b6c до и после совпали; копии п.0г лежат в tmp/zalivka_hvost_zapisi/. Откат: git revert 48ed3019, таблицы возвращать нечего. Бюджет: 2198 с до коммита правки, итог около 0.95 потолка — в порог 0.7 НЕ влез, причина названа (полный прогон репо 362 с и охота за флейком). ОСТАТОК вслух: развести тренажёрный и живой экзамен разными источниками — правка exam_show.py, вызывающего, здесь не сделана.
