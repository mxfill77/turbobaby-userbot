# Тело записи журнала — 2026-09-01 19:14 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-01 19:14:06 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #100 → done · [V0 судит done: сделано · V0: PROVEN / all_gates_passed по адресу «docs/artifacts/2026-09-02-почему-исчерпано-14-без-повторов.md»] Премиса подтверждена дословно; счётчик НЕ унаследован (1 из 3 у всех 14) — исчерпание рождается при `kind="none"` в `_schedule`, до диска и до повторов. Главное: посылка вердикта `ours` умерла за 1 ч 38 мин ДО бутстрапа очереди. Артефакт записан, коммит 4886653, журнал закрыт. Боевой код не правился. FACT: commit `4886653` в `git log` (`Разбор: 14 пакетов исчерпаны без повторов…`) · артефакт `docs/artifacts/2026-09-02-почему-исчерпано-14-без-повторов.md`, 225 строк · две строки в мозге (ARTIFACT → 738571 симв., DONE → 739071 симв., обе с обратным чтением писателя) · реестр очереди читался читателем и НЕ переписывался. ## 1. Премиса (п.1) — подтверждена дословно `pc_orchestrator.log:10226` → `2026-09-02 01:38:53,253 INFO ступень F: взято 0, повторено 0, исчерпано 14, в очереди 0`. Это не строка мозга, а `log.info` из `pc_orchestrator.py:9415`. Строка стои́т **8 раз подряд** (00:11:42 … 02:00:18, UTC+7) — картина держится, а не мигнула. ## 2. С каким счётчиком пришли (п.2) — числом **Счёт начался с 1 из 3. Не унаследовано ничего.** У 14 из 14 записей: `attempts=1`, `max_attempts=3`, `len(history)=1`, `next_at=None`, `kind=none`, `state=exhausted`. Единица — сознательный инвариант `new_record` («первая отправка состоялась»), а не чужие падения: отправщик пишет файл лотка по (пакет, канал, день) и повтор того же дня перезаписывает его — истории заходо …обрезано
