# Тело записи журнала — 2026-09-07 02:32 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2086 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-07 02:32:04 |
| символов в строке | 2086 |
| порог | 600 |

---

DONE Dispatch 09:45: задание Штаба 01-a-live-code-observed.0907 — класс назван и ИЗМЕРЕН, правок кода/карт/ворот ноль. п.0: 15 артефактов 07.09 открыты, ни один не отвечает на «сколько дверей мимо запуска» и ни один не даёт контракта из 4 частей — продолжено с нуля. п.1 ПЕРЕПИСЬ: T=19 модулей прочитано при СТАРТЕ, F=78 может быть прочитано за жизнь процесса, F−T=59 читаются с диска ПОСЛЕ старта; дверей первого эшелона 23 = 13 ленивых импортов + 10 путей данных (6 горячих), второго эшелона 68, разряд «динамическое исполнение» (exec/eval/importlib.reload/loader) — 0 по всем 78 модулям; ленивых импортов внутрь уже загруженного 15, дверьми не являются. п.2: от четырёх боевых входов достижимо 5 дверей из 13 (граф грубый и fail-open: 5 — потолок, 8 недостижимых — крепко) плюс 5 горячих файлов данных; восемь недостижимых прямо пишут ровно те данные, что читает путь ответа. ЭКСПОЗИЦИЯ снята с локов, которые боты написали сами, процессов не касались: старт 03.09 05:02:13 и 05:03:59, живы; из 19 стартовых модулей сменилось на диске 12 (весь ценовой узел 06–07.09), из 59 ленивых — 35, данных — 3; HEAD дерева ea19aca 07.09 08:57. ГЛАВНОЕ: у живого процесса НЕТ ОДНОГО КОММИТА — он исполняет смесь трёх возрастов (стартовые байты 03.09, ленивые байты неизвестного момента, ТЕКУЩЕЕ содержимое горячих данных), коммит есть свойство дерева, а не процесса; три ночные находки — следствия одного отсутствующего прибора. п.3 исход НАБЛЮДАЕМО ПОСЛЕ ПРАВКИ: боты пишут о себе PID, время старта, путь образа и строки журнала, grep по замыканию на rev-parse/__version__/commit/sha — ноль; child_raise_commit.json есть намерение (берёт HEAD дерева, пишется даже при неудавшемся подъёме, покрывает 2 двери из 4, at=03.09 12:11 при локах 05:02/05:04). п.4 контракт четырьмя частями, четвёртая — три отрицательных теста, главный ОТ-3: все модули совпали с коммитом, а ответ породили playbook.md и lesson_store.tsv вне git плюс горячий price_source.json. Артефакт docs/artifacts/2026-09-07-какой-код-исполняет-живой-бот.md, коммит d2c2afb, временное _scratch_livecode_0907/.
