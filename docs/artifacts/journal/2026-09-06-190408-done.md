# Тело записи журнала — 2026-09-06 19:04 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2825 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-06 19:04:08 |
| символов в строке | 2825 |
| порог | 600 |

---

DONE Dispatch 03:10: критерий ворот доведён до варианта F — кейсам класса «обязательное упоминание» три круга с зачётом большинством 2 из 3, остальным прежние два и все зелёные; коммит b4e32bc. Класс кейс получает ИЗ КОРПУСА (непустой require_any), а не списком номеров: список протух бы на первом же новом кейсе с чеком прозы головы, и протух бы молча. Вердикт теперь НАЗЫВАЕТ применённое правило и число кругов у каждого кейса — семь новых полей (criterion, criterion_id, runs_by_case, runs_line, runs_max, tolerated, tolerated_why) плюс две строки в stdout и две в отчёте: живой вывод на корпусе даёт «КРУГОВ ПО КЕЙСАМ: 1:2 2:2 3:2 4:2 5:3 6:3 7:2 8:2 9:2 10:3 11:2 12:2 13:2 14:2 15:2 16:2». Границы послабления не расширены ни одной: «неизвестно» большинством не лечится, прощается только красное меньшинство у зачтённого кейса, чеки прощённого круга не идут НИ в зачёт, НИ в провалы (35 кругов по 2 чека дали в счёте 68 из 68 — два чека прощённого круга ушли из обоих знаменателей), а при runs ниже порога ворот лишних кругов нет вовсе, поэтому разведка и lesson_regress не подорожали ни на круг. КОНТРФАКТ живыми кругами: кейс с require_any на слово, которого нет ни в одном языке, красен 3 из 3 (40с, 36с, 33с), оба критерия отвечают КРАСНЫЙ, прощено ничего, failed содержит все три круга пофамильно; черновики головы при этом здоровые — красное чистое свойство критерия, а не сломанного продукта. ЦЕНА полного прогона: 32 кейс-круга становятся 35, это счёт по живому корпусу с замком в тесте, +139.4 с (сумма ИЗМЕРЕННЫХ средних именно тех трёх кейсов: 41.36 + 48.86 + 49.18), то есть ≈1632 с против прежних ≈1492 с — плюс 9.4%; доля точна, абсолютные секунды наследуют разброс цены круга (сегодняшний контрфакт дал 36.5 с/круг против вчерашних 46.6). Посылка соседнего места: run_corpus отдаёт седьмое значение, живой потребитель один (lesson_regress, две распаковки) и оба стенда тестов приведены к новому контракту; test_lesson_regress 57, test_client_contour 105, test_lesson_store 41, test_pc_orchestrator 1061 — все OK; test_trainer_run 86, красный ОДИН и ПРЕЖНИЙ (test_raner_ne_v_klientskom_konture), доказано HEAD-блобами: все три ребра замыкания userbot_listen→trainer→lesson_regress→trainer_run есть в HEAD, а мой дифф не добавил ни одной строки import — тест не ослаблен и чинить его надо отдельной задачей. Отклонённый Штабом вариант D замкнут тестом: порог класса обязан быть строго больше единицы и расти с числом кругов. Названо вслух: большинство даётся кейсу целиком, а не отдельному чеку, поэтому кейс 10 платит третий круг за чек, который с 8a10449 снят, и получает большинство для остальных восьми — честный путь к этому направление G, в этом заходе не делалось. Полный набор не гонялся по запрету задания, корпус и уроки не тронуты, реестр вердиктов не открывался на запись.
