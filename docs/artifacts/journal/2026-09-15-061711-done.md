# Тело записи журнала — 2026-09-15 06:17 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1650 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-15 06:17:11 |
| символов в строке | 1650 |
| порог | 600 |

---

DONE ПК 15.09 задание 62-e (СТУПЕНИ vyrezka-faq 1509): лестница процентов скидки за срок БОЛЬШЕ НЕ ДОЕЗЖАЕТ до собранного промпта ни одной веткой. Рез стоит на подаче FAQ в промпт (suggest.py -> make_system_prompt, _faq_for_prompt), док мозга faq и локальный фолбэк НЕ правлены ни байтом. ВЫРЕЗКА ОБЪЯВЛЯЕТ СЕБЯ: каждый сработавший рез пишет в журнал ЧИСЛО убранных фрагментов (на обеих нынешних редакциях FAQ оно равно 1) и свою причину — критфакты ЭТОГО ЖЕ промпта запрещают называть клиенту хоть один процент и хоть одну ступень скидки за срок. Правило одной фразой: лестница — фрагмент FAQ (предложение либо заголовок со своим списком), где при слове о скидке названы два-плюс процента и два-плюс РАЗНЫХ срока аренды. Числа перебором тем же счётчиком, что 13.09: 12288 веток, отказов сборки 0, B(лестница) 3 -> 0, максимум B по любой ветке 0; A(ступен) 1 -> 1 (это ЗАПРЕТ в критфактах, он цел); C(проценты скидки за срок) 12 -> 9 — остались 7 из STYLE_FEWSHOT (премиса П3, отдельный предмет) и 2 примера расчёта в самом FAQ. Свод по вопросам клиента 24 сборки: B=0 во всех. Отрицательный тест: тот же вход при снятой вырезке даёт B=3 и sha fd8df5ec (ровно промпт 13.09), при стоящей — B=0 и sha b6aa0637; возврат реза совпал посимвольно. Эталон build_prompt 15786 симв. sha f627f6d9 — байт в байт как 13.09. Тесты: test_suggest 699 (692+7 новых замков), 1 падение — известное test_anthropic_key_is_read_only_from_environment по среде захода, других падений нет; test_rules_decided 7/0. Коммиты 09b93d2 (код+замки), 3d254e1 и 8a5d8ed (артефакт). Наружу не ушло ни одного сообщения, в мозг не писали, боевых файлов не трогали.
