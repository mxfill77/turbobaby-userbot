# Тело записи журнала — 2026-08-04 17:59 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1489 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-04 17:59:49 |
| символов в строке | 1489 |
| порог | 600 |

---

DONE Dispatch 01:20: находки ревизора — разбор класса и правка. ЗАМЕР: потеряно суммарно 31 находка (19 до спула + 12 съедены гейтом 05.08), в спуле сейчас 0. Состав тех 12 восстановлен ПОЛНОСТЬЮ по логу (4 чек-лист/#93 + 8 клиентского контура) — поправка к артефакту, считавшему одну неопознанной. Гейт замерен ПРЕДМЕТОМ (перехват записей с привязкой к тесту, команда каноническая): зелёный гейт 2936 OK испортил ДВА боевых файла, грепом не находимых — cowork_log.ledger (кольцо 500 строк вытолкнуло 13 НАСТОЯЩИХ записей за 01.08, это вытеснение, а не засорение) и pc_orchestrator.client_watch.json (268→64 б, снимок надзора заменён фикстурой ts=1301.0; его читает отдельный процесс pc_agent status). Оба той же формы, что съеденный спул: у живой функции параметр пути ЕСТЬ, тест его не передал. Правило поставлено в саму константу: log_setup.state_path — близнец уже боевого log_path, решает по СПОСОБУ ЗАПУСКА процесса; на него переведены 11 констант pc_orchestrator + LEDGER_PATH. Голден от инцидента упал бы на коде до правки. Проверено тем же прибором: гейт-2 2940 OK, записей в оба файла НЕТ, 10 файлов состояния совпали с побайтным бэкапом. Коммит 1724eb5, запушен. Остатки: trainer_rules.json и pc_ask_*.marker вне границ; гейт демона самообновления гоняет тесты без test_isolation — боевая moderation_ipc.db там открыта, тривайр мёртв; на VPS спула нет вовсе, но memory.DB_PATH — тот же класс. Разбор → docs/artifacts/2026-08-05-test-writes-to-live-state.md
