# Тело записи журнала — 2026-08-18 21:56 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **668 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-18 21:56:45 |
| символов в строке | 668 |
| порог | 600 |

---

ARTIFACT база обучения тренажёра: на чём строить -> docs/artifacts/2026-08-19-training-base-recon.md: правила бота - ОДИН md-файл playbook.md (9 правил, вне git) + сайдкар trainer_rules.json (6 ключей, 1 сирота); узла мозга нет ('rules' про агентов, 'business_rules' клиентский контур НЕ читает - 0 боевых чтений); версии нет, автора НЕТ НИКОГДА (сток берёт голую строку); одно правило откатывается, но номер плывёт, /rules в живой путь не заведена, отката по источнику нет; книга читается на КАЖДЫЙ запрос без кэша, переигровки хода в тренажёре нет; кап 100 молча стирает, возвращая added (замер 120->100), три процесса пишут файл БЕЗ ЗАМКА
