# Тело записи журнала — 2026-08-23 11:10 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1243 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-23 11:10:17 |
| символов в строке | 1243 |
| порог | 600 |

---

NOTE Dispatch 23.08 шаг7/ветка минимальной котировки: РЕГРЕССИЯ ЗАКРЫТА, замок заведён. Второй производитель ожиданий trainer_run.expectations теперь кладёт тот же ключ avail из того же источника (availability_from_note(note)); правок стало две вместо одной, и причина названа прямо в коде и в отчёте — словарь ожиданий строят ДВЕ функции в двух модулях, а читает одна, одноточечного способа передать значение не существует. Заведён ЗАМОК test_trainer_run.DvaProizvoditelyaOzhidaniy, три теста: ключ есть у обоих производителей и несёт одно значение на одной записке (плюс fail-closed на занятом); СВОЙСТВЕННЫЙ замок — разбором AST читателя _smoke_checks собираем ВСЕ ключи, которые он достаёт из exp, и требуем, чтобы каждый клал производитель тренажёра (ровно этой проверки не было, потому гейт и промолчал); сквозной — правда кода проходит на обеих ветках, выдумка краснит, дефицит краснит при любом носителе. ЗАМОК КУСАЕТ, проверено прогоном против дофиксовой копии trainer_run: 3 из 3 красных. ГЕЙТ надмножеством, три прогона подряд: 931 тест, 9 красных в каждом, множество равно базе до правки, новых красных ноль. Возвращаюсь к живому замеру: прогон 21 на сломанном коде (6 из 12) остаётся уликой и в счёт не идёт.
