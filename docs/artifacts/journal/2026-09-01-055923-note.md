# Тело записи журнала — 2026-09-01 05:59 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-01 05:59:23 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #72 → done · Разведка класса «тест меряет живое дерево вместо своего» по обеим полосам закрыта. Корень класса — не в тестах, а в проде: 6 констант с литералом корня. Оба образца из задания подтверждены и оказались симметричны (у каждой полосы есть и три теста надзора, и хардкод в гард-тесте). Правок кода ноль. Артефакт записан и запушен. **FACT:** - commit `54cb9d1` («Перепись класса абсолютного корня в тестах на обеих полосах») и `3c8df49` (тело журнальной записи) в `git log`, запушены: `77f5ac5..3c8df49 main -> main`, `git status -sb` → `## main...origin/main` (ahead 0). - Артефакт: `docs/artifacts/2026-09-01-abs-root-class-in-tests.md` - Журнал: две строки легли («OK: записано в мозг, символов: 682526» и «683063 | тело вынесено в docs/artifacts/journal/2026-09-01-055801-done.md»). - Правок кода/тестов: **ноль** (`git show --stat 54cb9d1` — один новый .md). --- ## 1. Корень класса: хардкод в ПРОДЕ, а не в тестах Главный вывод, который переворачивает постановку. Литерал корня живёт в продовых модулях, тесты наследуют его через `import` — поэтому починка одних тестов косметична. | полоса | файл:строка | константа | |---|---|---| | ПК | `pc_orchestrator.py:98` | `REPO = r"D:\turbobaby-bot"` | | ПК | `pretool_guard.py:281` | `PROJECT = r"D:\turbobaby-bot"` | | VPS | `orchestrator_daemon.py:33` | `REPO = "/root/turbobaby-manager-bot"` | | VPS | `orchestrator_daemon.py:34` | `BRIDGE_GS = "/root/turbobaby-bridge-gs"` | | VPS | `pretool_guard.py:78` | `PROJECT = "/root/turbobaby-manager-bot"` | …обрезано
