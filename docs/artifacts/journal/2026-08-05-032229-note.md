# Тело записи журнала — 2026-08-05 03:22 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-05 03:22:29 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #293 → done · Разбор выполнен read-only, правок кода нет. Из трёх состояний система различает **одно** — немоту сессии (`session_watch.py`, 5 сигналов за 7 суток, все доставлены). «Связи нет» не различает никто: слово «сеть» в логе демона за 14 суток — 0 раз при 89 голых именах исключений. «Я сломался» различается наоборот: обрыв сети 04.08 записан как `причина=exec_error`. Артефакт: `docs/artifacts/2026-08-05-three-states-one-root.md`. ## 1. Чем система отличает эти три состояния сейчас Не «ничем», но и не «отличает» — различитель есть у 2 предикатов из 9: | состояние | предикат | вход | различает? | |---|---|---|---| | «сон» — тихий канал (WARNING + grace) | `_woke_from_sleep` `pc_orchestrator.py:5676`: `(now−prev) > 120 с` | одно число — скачок настенных часов | **НЕТ** | | «сон» — громкий канал (карточка 🛌) | `report_long_sleep:5729`: `slept > 2760 с` | часы бодрствования `QueryUnbiasedInterruptTime:1089` | **ДА**, по конструкции | | «связи нет» | **никого нет** | — | пробы достижимости в репо 0 | | «мост ответил ошибкой» | `Bridge._get:394`: `except Exception → type(e).__name__` | имя класса | **НЕТ**: 401 и 500 оба = `HTTPError` | | «я сломался» (задача) | `exit != 0` `:1534` | код возврата ребёнка | **НЕТ** | | «задача зависла» | `process_stuck_singles:2905`: `age > 5400 с` | отметка МОСТА | молчит, когда моста нет | | «агент умер» | `client_watchdog_tick:5508` | CIM + mtime логов | ДА для слепоты, НЕТ для «умер от сети» | | «демон мёртв» | `watchdog():7108` | heartbeat + CIM | забл …обрезано
