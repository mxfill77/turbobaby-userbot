# Тело записи журнала — 2026-08-17 04:48 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1570 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-17 04:48:53 |
| символов в строке | 1570 |
| порог | 600 |

---

NOTE Orchestrator: задача #46 → done · **Гипотеза штаба НЕ подтвердилась: на честном знаменателе (15 пар из 26) линия не выросла — модель 16.7 % против 15.4 %, цена 0.0 % против 3.8 %. Но 18 промахов из 71 (25.4 %) постановка снимает. Боту подавали ТОЛЬКО речь клиента, срезанную дважды: медиана 7 реплик из 27, обрыв на 11-м сообщении диалога из 107. Настоящих уклонений 36.** **FACT:** коммит `ad0443a` в `git log` — `docs/artifacts/2026-08-17-harness-fairness.md` (артефакта не было на диске, защита от повтора пройдена). Журнал: `OK: записано в мозг, символов: 401164`. Бот НЕ запускался — замок: `trainer_run` в `sys.modules` после разбора → **НЕТ**, голова недостижима. Вход восстановлен дословно — `len(tr)`/число строк против записанных `tr_chars`/`tr_lines` **совпало по всем 26 парам**. ## Основание проверено `2026-08-17-regression-miss-anatomy.md` на диске, коммит `52aa8a1` в `git log`, §6 держит «65 из 71». НЕИЗВЕСТНО не наступило. ## 1. Что подавалось на вход — как есть Ни один из четырёх вариантов постановки не верен. Подавалась **речь клиента, срезанная дважды**: только `who=="client"` (**0 строк `[менеджер]:` против 348 `[клиент]:`**), день ≤ дня начала аренды, и обрыв на якоре — `cl[anchor-49:anchor+1]`, потолок 50. Формат `[клиент]: <текст>`, перевод строки → ` ⏎ `. Ходы менеджера вырезаны **намеренно** — дословно из `run_bot.py:20-24`: «именно в них лежит записанная модель/дата/цена, и с ними мерка мерила бы списывание». ## 2. Правило среза и ход обрыва Якорь = **последняя реплика клиента до старта**, где …обрезано
