# Тело записи журнала — 2026-08-11 09:19 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-11 09:19:41 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #491 → done · О3 поднят: судит ПРОДУКТ пятисекундного тика модербота (запись в `moderation_ipc`), а не живой PID. Порог 15 мин снят собственным замером бота, три исхода покрыты тестом в обе стороны, наблюдатель — уже стоящая задача SYSTEM вне модербота. Коммит `2239c65` запушен, полный гейт красен двумя тестами храповика, и они не мои — доказано замером HEAD-блобов. **FACT:** commit `2239c65` в `git log` (+`4740ef4` тело журнала), оба на `origin/main` (`c6e2254..2239c65`). Стоящий наблюдатель уже исполняет О3: его тик 16:14:41 записал блок `mod` в `tmp/expect_pc/state.json` (295 байт против 160 до правки), живой прогон — `модербот (работа, не жизнь): делает работу`. **1. «Жив» против «делает работу».** ЖИВ = в системе есть процесс с номером из `moderation_bot.lock`; ДЕЛАЕТ РАБОТУ = его тик `job_heartbeat` реально коммитит запись в `moderation_ipc` — тот продукт, по которому userbot решает, отдавать ли ему черновики. Залипший event-loop остаётся в списке процессов, лог модербота молчит по устройству, и контур-вотчдог демона такой процесс считает здоровым: он судит существование, а не продукт. **2. Порог 15 мин — своим замером, не от О1/О2.** 849 пауз тика в его логе: медиана 5.000с, max законной **5.022с**; 26 промахов планировщика за 39 суток: худший **4.97с**; живая проба продукта 11.08 (900 замеров, 189 шагов mtime за 15.8 мин): max **5.02с**; сам контур зовёт его мёртвым через 15с. 900с = 180 пропущенных тиков и, главное, больше одного периода наблюдателя (600с) — заметка требует двух п …обрезано
