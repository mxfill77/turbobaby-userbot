# Тело записи журнала — 2026-09-12 10:07 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **3078 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-12 10:07:05 |
| символов в строке | 3078 |
| порог | 600 |

---

DONE ПК 17:10: задание 56-a (ОТБОР primery-blizhajshie 1209) — отбор ближайших живых примеров ДОКАЗАН числами, включения в записку головы не делал (пункт 7). Признак сделанности: работа 51-b нашлась целиком в 5 коммитах (7996e3b style_examples.py, 95c1a5b suggest.py, c58f526 exam_show.py, 4b6160e 27 проверок, c856904 паспорт порога), дерево по отслеживаемым файлам чисто — переписывать готовое не пришлось; недостающим было ровно доказательство. Близость ОДНОЙ фразой: доля смысловой массы вопроса (сумма idf общих токенов / сумма idf токенов вопроса), модели канонизированы детектором бота model_name, незнакомое слово весит максимум шкалы, а не ноль. База client_chats.anonstable.jsonl отпечаток 36d68c71d6019fe2, пар 5558, сборка индекса 443.5 мс раз на процесс. ДЕСЯТЬ ВОПРОСОВ, разрез A (первая реплика каждого из первых 10 диалогов): примеров 30, с примерами 6 из 10, max 10.5 мс. Разрез B (буквально первые 10 клиентских реплик) считан отдельно ЗАМЕРОМ, а не по вкусу: клиентских реплик в первых десяти диалогах [8,4,604,206,6,188,5,2,107,313], то есть 8 из 10 первых реплик принадлежат диалогу 0 — примеров 10, с примерами 2 из 10, max 11.6 мс. Две строки разреза B показали ЗАМКИ живьём: кандидат с оценкой 0.6239 выше порога всё равно дал НОЛЬ, потому что он один, а правило всё-или-ничего требует трёх. ОТРИЦАТЕЛЬНЫЙ ТЕСТ: 6 вопросов, которых в базе нет вовсе, вернули 0 примеров и 0 кандидатов выше порога; ноль доказан не сам собой, а ЗАПАСОМ — порог снят в -1.0, максимум чужого домена 0.1547 против порога 0.2235 (запас 0.0688), пять из шести не собрали даже двух общих значимых слов ни с одной из 5558 пар, а свой домен на том же приборе даёт 0.4624..0.6589. ПОРОГ ВРЕМЕНИ НАЗВАН: самый долгий одиночный отбор за заход 11.6 мс, дольше 2000 мс 0 отборов из 26, подгонять было нечего. АРИФМЕТИКА БЮДЖЕТА до первой рабочей команды: 11 единиц, цена замером на двух первых 10.6 и 10.7 мс, плюс сборка 631.4 мс — 0.75 с против потолка 2700 с, это 0.0003 потолка. Набор test_style_examples: Ran 27 tests, OK. СВЕРХ ЗАДАНИЯ назван числом пробел, который вскрыл отрицательный замер: англоязычный вопрос обслуживается неровно — латиницей в индексе 179 вопросов и 808 смешанных, из трёх англовопросов один дал 5 примеров (0.4475), второй 0 при 26 кандидатах (лучший 0.1963 под порогом), третий 0 кандидатов из 5558 вовсе; правкой не закрывал по пункту 7. НАЙДЕНА МИНА В СВОЁМ ЖЕ ПРИБОРЕ: similar(..., limit=1) обрезает ВЫДАЧУ, и первый замер печатал кандидатов=1, где единица была моим пределом, а не числом кандидатов — исправлено на limit=999 до всякого вывода. Наружу за заход не ушло ни одного сообщения, строк клиентской переписки нет ни в артефакте, ни в отчёте, Календарь бронирования и брони не открывались, ничего не удалялось. Временное место названо: _scratch_otbor_1209 (probe2.py, run10.py, margin.py, lang.py, msg.txt). Коммит 91a691a; его сообщение переписано amend — первый заход 54e1c53 получил субъектом литерал @, потому что PowerShell-here-string уехал в Bash. АРТЕФАКТ docs/artifacts/2026-09-12-ОТБОР-primery-blizhajshie-1209.md
