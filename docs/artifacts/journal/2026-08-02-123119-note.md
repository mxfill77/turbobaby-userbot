# Тело записи журнала — 2026-08-02 12:31 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-02 12:31:19 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #191 → done · Оба дефекта карточки задачи 189 закрыты и запушены (`2c8ded0`, `165c6cb`); гейт полный — 2818 OK. ## FACT-отчёт **Дефекты были подтверждены, но не чинились** — артефакт `2026-08-02-channel-registry-readonly-audit.md` §4 говорит прямо: «Два новых дефекта (только названы, не чинились)». Заход не закрывающий, а чинящий. **Д-1 — откат называл другой файл.** `_ROLLBACK["edit_claude"]` был статичным литералом: замер до правки дал одну и ту же строку `git checkout -- .claude/settings.json (файл под git)` для **всех пяти** объектов, включая пустой. Теперь откат собирается по объекту (`_claude_cfg_tracked`): под git → команда с тем же путём; под `.gitignore` → честное «git его НЕ вернёт»; файла не нашли → не утверждаем ничего. Базовое имя от Write/Edit разворачивается в путь от корня — иначе предложенная команда git не нашла бы цели. Сверх того замок у рождения карточки (`_rollback_conflicts` в `_card`): строка отката, назвавшая файл, отличный от объекта, в карточку не попадает никогда. **Развилку решил внутри правки:** «карточка не должна выписываться» исполнено как «неверная СТРОКА не выписывается», а не «карточка снимается» — `defer` это ПРОПУСК операции (`main`, `sys.exit(0)` без вывода), снятие карточки на красном виде было бы дырой. На это есть отдельный голден. **Д-2 — разряд по имени в тексте.** Две ноги: (1) `_seg_runs_stdin_as_code` спрашивал «есть ли где-нибудь `-`» раньше, чем «названа ли `.py`-цель», а `-` здесь сентинел самого писателя журнала — теперь решает первый токе …обрезано
