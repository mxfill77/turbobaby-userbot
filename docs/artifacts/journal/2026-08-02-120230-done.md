# Тело записи журнала — 2026-08-02 12:02 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1885 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-02 12:02:30 |
| символов в строке | 1885 |
| порог | 600 |

---

DONE Dispatch 19:15: read-only перепроверка реестра каналов, ничего не менялось и не коммитилось. ФАКТ 1: 81e524e matcher НЕ трогал (в диффе только гард, тест, разбор; matcher из 7 имён последний раз правился e769a82) — заменена последняя строка decide: вместо defer любому чужому имени теперь ask/unknown_tool, реестр _TOOL_CHANNELS 7 гардованных + 2 зелёных, поддержан _stays_red и _HARD_CARD; сторож TestChannelRegistry 6 тестов выводит список из живого matcher и из ast исходника. f18c744 — только тело журнальной записи, 868 симв при пороге 600. ФАКТ 2: defer это ПРОПУСК — main (pretool_guard.py:4045) делает sys.exit(0) без вывода, ни _emit_ask, ни _emit_deny не зовутся, решает glob-слой прав; не вопрос и не блок. ФАКТ 3: гард в проде (блоб файла = блоб HEAD 067b8c2, хук — новый процесс на каждый вызов), но демон PID 18892 стартовал 12:38:32 на 321dc3d и держит в памяти гард БЕЗ нового кода (маркеров 0 против 5 в живом файле); self-update следит только за блобом pc_orchestrator.py, а его коммиты реестра не касались. Живых решений unknown_tool в логе 0 — имя вне matcher до гарда не доходит. ДВА ДЕФЕКТА ПОДТВЕРЖДЕНЫ, не чинились: Д-1 карточка задачи 189 назвала объектом локальный файл прав, а откат отдала статичным литералом про отслеживаемый файл настроек с припиской «файл под git», хотя объект стоит под .gitignore:8 — тест сторожит только префикс «Откат: », а во всех тестах объектом стоит лишь отслеживаемый файл; Д-2 вид edit_claude судит по подстроке скан-текста без разделения упоминание/обращение (у env оно заведено 01.08), а тело heredoc не вырезается, потому что _seg_runs_stdin_as_code проверяет токен «-» РАНЬШЕ именованной .py-цели: замер True против False на одной команде с «-» и без; «-» при этом сентинел самого писателя журнала, предписанный для кириллицы. Разбор → docs/artifacts/2026-08-02-channel-registry-readonly-audit.md
