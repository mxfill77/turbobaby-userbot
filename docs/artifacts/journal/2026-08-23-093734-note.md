# Тело записи журнала — 2026-08-23 09:37 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1301 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-23 09:37:34 |
| символов в строке | 1301 |
| порог | 600 |

---

NOTE Dispatch 23.08 шаг1/боевой пояс наличия: премисы разбора 09:14 UTC ПРОВЕРЕНЫ пробой _scratch_availbelt_0823/probe_holes.py. Дыра 1 порядок слов: _PC_AVAIL_RE требует «свободен\s+на», на «на эти даты свободен» даёт False. Дыра 2 беглая гласная: стем «свободн\w*» в _pc_maybe не ловит «свободен» (свобод-Е-н), тайбрейк не запускается. Дыра 3: guard_availability вызовов в боевом коде 0 (suggest/moderation_core/moderation_bot/userbot_listen/client_contour/pricing/trainer/trainer_run). Забор Честертона: НЕ отключали — не подключали никогда. Доказательство историей: git log -S guard_availability по всем веткам даёт 9 коммитов, вне docs/ и тестов правился ТОЛЬКО 9c45837 (15.07, шаг4/6 родителя 365) — он ВНЁС определение + юниты и не тронул ни generate_draft, ни regenerate_draft, ни moderation_core; ни один коммит вызова не удалял. Три документа фиксируют то же состояние без решения владельца: 2026-08-17-fleet-availability.md:259 «готовый замок лежит рядом с дверью и не заперт», 2026-08-20-class-suggest-behavior.md:169 «в боевом пайплайне не вызывается ни одной строкой — это состояние тоже не менялось», 2026-08-21-goldens-update.md:229 столкновение с канонической строкой цены помечено как ОТКРЫТАЯ развилка владельца, «в работу не брал». Кода не тронуто ни строкой.
