# Тело записи журнала — 2026-09-07 08:27 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2395 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-07 08:27:06 |
| символов в строке | 2395 |
| порог | 600 |

---

DONE Dispatch 07.09 WA-2b разведка (задание Штаба 01-f-wa2b-recon.0907): решение с доказательством перед постройкой, кода транспорта не писал, живой адрес очереди не дёрнут ни разу. ГЛАВНОЕ п.4: записи ОСТАЮТСЯ ДО РАСПИСКИ — двери GET /wa-queue/pull и POST /wa-queue/ack построены серверной полосой 06.09, держат аренду 5 минут (LEASE_SECS), удаления нет ни одного (доказано ast-стражем по реально уходящему SQL). ПОПРАВКА К УЗЛУ: KB_WA_PLAN §7 УСТАРЕЛ — он всё ещё пишет «НЕТ pull/ack» и «НЕТ wa_send», хотя обе двери сделаны в тот же день; доказано тем, что спорные строки живого узла и его git-копии совпадают на номерах 130 и 133, то есть узел не обновляли. Заход, поверивший §7, ответил бы «НЕИЗВЕСТНО» и был бы неправ. п.2: транспортно-нейтральной ТОЧКИ ВХОДА НЕТ — suggest.on_client_message не принимает сообщения вовсе (текст восстанавливается обратным разбором транскрипта, а клиент и отправитель — живые объекты Telethon). Но путь НЕ задваивается: шея есть, и это СТРОКА ОЧЕРЕДИ moderation_ipc.drafts, ниже которой код транспорта не знает. п.3: ключ контекста сегодня client_id INTEGER (Telegram user_id), номер телефона не примет — худшая из трёх поломок в том, что send_to_client отправит черновик Telethon-ом постороннему человеку, и спасает сегодня только флаг SUGGEST_TEST_MODE. п.6 НАХОДКА: видов записи ЧЕТЫРЕ, а не три (квитанции доставки заданием не названы), и в выдаче pull семь полей — echo и history среди них НЕТ, поэтому эхо менеджера неотличимо от входящего клиента; в Coexistence это дало бы карточку на каждое сообщение менеджера. п.7: неверный секрет даёт 404, неотличимый от неизвестного URL по устройству двери — значит 404 нельзя читать как «нет данных». ПРЕМИСЫ ШТАБА: узел есть, но две другие не подтвердились — moderation_card и dialog_card в боевом пути не участвуют вовсе (dialog_card не импортирует никто), а WA_PULL_SECRET принадлежит полосе VPS, а не ПК, и не заведён вообще (дверь fail-closed отвечает 404 всегда, ключ вносит владелец). Своя ошибка названа: фоновый поиск прочитан как «0 совпадений» до его завершения, а завершившись он дал совпадение, которое перевернуло главный вывод. ОСТАЁТСЯ НЕИЗВЕСТНЫМ и блокирует постройку: фильтрует ли pull эхо и историю — закрывается одним чтением живого wa_webhook.py с СЕРВЕРНОЙ полосы. Коммит d62fece, артефакт 507 строк → docs/artifacts/2026-09-07-wa2b-вход-whatsapp-в-путь-клиента.md
