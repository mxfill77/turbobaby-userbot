# Тело записи журнала — 2026-08-09 18:15 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-09 18:15:47 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #423 → done · Инбокс разведён с потоком итогов: адрес выбирает один признак «ждёт ли сообщение ответа» (`dispatch_notify.deliver`/`awaits_reply`), замок на кнопку и высшую карточку §7 стоит перед признаком. За всё наблюдение в 1160 ушло 184 сообщения, ответа ждали 9; 175 карточек «Code-сессия завершена» уходят в 328. Гейт 3171 OK, коммиты b1c38db + e265f98 на origin/main. ## FACT-отчёт **1. Место выбора адреса — названо до правки.** Адрес выбирал **вид** сообщения, в пяти ветках `main()` (нумерация по `git show HEAD:dispatch_notify.py`): ``` 169:def send_critical(text): 193:def send_topic(text, thread_id=None): 214: return send_critical(text) 515: channel, ok = send_critical(text) ← --critical 527: channel, ok = send_topic(text, tid) ← --topic 549: channel, ok = send_critical(text) ← --hook session_end (дефект) 578: channel, ok = send_critical(text) ← --hook notification 591: channel, ok = send(text) ← прямой текст → личка ``` Дефект виден прямо здесь: `session_end` вёл в инбокс, потому что так решили про **вид**, а не потому, что карточка финала чего-то ждёт. Перечень уже протух в другую сторону — `--card` (карточка **с кнопками**) уезжала мимо инбокса, в личку. **2. Признак один.** `deliver()` (:314) — единственное место выбора; `awaits_reply()` (:293) — единственный признак: замок → заявление отправителя → названное ожидание владельца → закрытый исход → «ни того, ни другого = ждёт». Слова обоих признаков сняты с корпуса 2179 живых отправок, а не выдуманы. `--topic <id>` остался **адресо …обрезано
