# Тело записи журнала — 2026-09-11 20:16 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2516 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-11 20:16:57 |
| символов в строке | 2516 |
| порог | 600 |

---

DONE Dispatch 03:20: задание 49-a (ВОПРОС podstanovka-dat 1209) — карточка экзамена показывала владельцу СЫРУЮ строку корпуса вместо вопроса клиента: в снимке кейса 1 лежало «XMAX 300 {when_sloppy}», а в ответе головы стояли сами даты «с 6 по 11 октября». Подстановка была одна и жила в прогонщике (placeholders + format в build_transcript), а показ её не звал вовсе. Сделано: подстановка вынесена в trainer_run.substitute и зовётся ОБОИМИ (прогон и показ, второй копии нет — проверено исходником); exam_show.freeze считает окно дат РОВНО ОДИН раз и отдаёт его голове и записанному вопросу (тест с разным окном на каждый вызов: счётчик обязан остаться 1); сырая строка не потеряна — ключ question_raw; снимок exam_shots/case-1.json починен разово, окно взято по дате сборки 11.09, поля draft/note/commit/corpus/rules/built_at сверены обратным чтением и не тронуты, копия ДО правки сохранена. Доказательство, что подставлено виденное головой: окна 11.09 и 12.09 равны побайтно во всех 5 формах, месяц «октября» стои́т в самом ответе. ЧИСЛА: фигурных скобок в карточке ДО 1 → ПОСЛЕ 0; в корпусе шаблон несут 10 кейсов из 17, в карточке не остаётся ни одного на всех 17; кругов головы 0 из 0 запланированных (потолок 2700 с, машинного времени ≈26 с); test_exam_show 55 (было 43, +12), соседи test_trainer_run+test_pc_agent+test_dispatch_notify 263, всё OK; вклад в храповик неразбора 0. ПЕРЕПОКАЗ: ровно одно сообщение наружу, message_id=1921 в группу -5193185299, шапкой названо, что это перепоказ и чем отличается; прежняя карточка 1920 висит нетронутой. Старые кнопки проверены живьём, а не предположены: exam:ok:1 разбирается живым pc_agent в ('ok','1'), exam_show.tap пишет строку КАНДИДАТ (журнал подменён на временный, боевого exam_verdicts.tsv не было до пробы и нет после). Порядок захода задан замком свежести ловца: правка exam_show.py делает файл новее старта агента и показ отказал бы сам себе, а агента перезапускать запрещено — поэтому сперва починка снимка и показ, потом правка кода. Перепоказ получил свой файл exam_reshow.py ещё и потому, что анти-проба dispatch_notify разрешает живую отправку только с точки входа в корне репозитория: разовым скриптом из tmp/ показ не сделать вовсе (получен честный отказ). Найдено рядом и НЕ чинено: в журнале тапа версия правил выходит как «НЕ ЗАПИСАНО» — rules_version кладёт список, а _evidence ждёт строку. Коммиты d2b951d, cc43f9e, 10e81b8, запушены (6ff82b5..10e81b8). Артефакт: docs/artifacts/2026-09-12-ВОПРОС-podstanovka-dat-1209.md
