# Тело записи журнала — 2026-09-02 18:35 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1381 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-02 18:35:19 |
| символов в строке | 1381 |
| порог | 600 |

---

DONE Dispatch 03.09 замер языка ПОСЛЕ правки варианта A: на кандидате 27d0f8d снято 20 единиц из 20 (4 круга по 5 EN-кейсов, живая цена) — язык КРАСЕН 0 из 20 против базы 3 из 20 (c97a30d), красных чеков любого рода за весь набор 0. Вершина не двигалась ни на одном из трёх заходов (bind_head/verify_head на каждом), corpus_sha 6d5d78f0d83acdca тот же. Дымовой прогон первым: 10 русских кейсов первого контакта — 10 из 10 зелены ЦЕЛИКОМ, красных чеков нет, русский путь правкой не сломан, ворота открыты. Условия сравнимы не словом, а посимвольно: вставок кода 244/244, из них русских 52/52, кириллических букв в них 1324/1324 — ноль снят при том же объёме русского в ответе, а не на мёртвой цене. Кириллица ОТ ГОЛОВЫ: 0 букв в 0 единицах из 20 против 551 буквы в 3 единицах у базы — вся кириллица в EN-ответах принадлежит вставкам кода. ВЕРДИКТ по правилу, объявленному ЗАРАНЕЕ: P(0 из 20) при прежней частоте 0.15 = 0.85^20 = 3.9%, значит правка варианта A ДОКАЗАНА. Механизм НЕ доказан и так и записан: русские вставки остались в том же количестве, изменился только промпт. Кода не правилось ни байта — задача только мерила. Прерываемость: _scratch_lang_a/run_units.py, ключ единицы «круг, кейс», сделанное не повторяется (в логах 5 и 15 пропусков). Артефакт docs/artifacts/2026-09-03-замер-языка-после-правки-вариант-A-вердикт.md, коммит 42687d8, запушен.
