# Тело записи журнала — 2026-08-20 08:39 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **933 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-20 08:39:51 |
| символов в строке | 933 |
| порог | 600 |

---

DONE Dispatch 20.08: карточка модерации переведена на единицу РАЗГОВОР — dialog_card.py (одна карточка на диалог, сжатая история, темы клиента с тремя исходами), moderation_ipc.supersede_open (устаревшее предложение неотправляемо: статус superseded вне CLAIMABLE_FROM, замок в СУБД), card_load.py (замер нагрузки надзора, числа копятся и владельцу не показываются). Четыре отрицательных теста устояли: никто не нажал — не ушло ничего; клиент дописал — устаревшее отправить нельзя (проверено шестью дорогами, включая гонку и контрфакт порядка); восемь нажали разом — ушло ровно одно; не могу посчитать — «не считаю», а не число. Гейт 3947 тестов (было 3881, +66), красных 2 — те же два храповика неразбора, totals 994 без изменений, вклад в храповик ноль. Коммиты 651f436 и 7ca7883 запушены. На живых ботов НЕ выкачено, moderation_bot и userbot не тронуты. Артефакт → docs/artifacts/2026-08-20-card-per-dialog.md
