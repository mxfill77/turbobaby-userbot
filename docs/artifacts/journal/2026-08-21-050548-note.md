# Тело записи журнала — 2026-08-21 05:05 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-21 05:05:48 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #78 → done · Сетка парка переведена на записанное правило: один источник цены на оба пути. Три отрицательных теста устояли, новых красных ноль, живой повтор случаев 4 и 7 дал другие числа (пик NMAX 337→424 ฿/сут). Артефакт `docs/artifacts/2026-08-21-sheet-source-switch.md`, коммиты запушены. ВАЖНО: живой сторож свежести сегодня отказывает — в проде бот молчит по цене на обоих путях. **FACT:** commit `6877ae8` (код) и `79b0125` (артефакт) и `f64bc3c` (тело журнала) в `git log`, все запушены в `origin/main` (`e081ba4..6877ae8`, далее). Живой прогон: `_scratch_sheetrule_0821/case_04.json` — NMAX 155 сутки **424 ฿** против **337 ฿** прогона `ab16dcd`. --- **Первое действие (доложено):** дерево `ab16dcd` (2026-08-21 10:47:20 +0700), время 11:31:17. **Признак сделанности:** `docs/artifacts/2026-08-21-sheet-source-switch.md` — отсутствовал, работа сделана с первого захода. ## Забор Честертона (одной строкой) Сетка отвечает на каталог-вопрос, и врезка 20.08 сознательно оставила её на живом листе, назвав причину дословно — «у неё нет дат клиента (якорь — завтра)». **Посылка верна ровно наполовину:** дат нет у 37 % черновиков (172 из 470), значит у 63 % они ЕСТЬ и у сетки те же самые (`hints["iso_start"]`) — случай 4 ровно такой, якорь `2026-12-20`, чистый пик. Опирались на прежний путь ещё четверо, проверено поимённо: занятость, депозит и имя юнита остаются словом живой двери (правило их не трогает); подбор по классу и подвыборки берут те же `rows` и едут следом; `model_claim_mismatch` судит имена …обрезано
