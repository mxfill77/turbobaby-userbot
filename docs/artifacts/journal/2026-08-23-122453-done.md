# Тело записи журнала — 2026-08-23 12:24 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **3035 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-23 12:24:53 |
| символов в строке | 3035 |
| порог | 600 |

---

DONE Dispatch 23.08 19:25: версия перегрева проверена ЗАМЕРОМ, разбор → docs/artifacts/2026-08-23-overheat-version-check.md. П.1 ответ ПРЯМОЙ: температуры процессора штатно НЕ ЧИТАЮТСЯ — MSAcpi_ThermalZoneTemperature отказывает в доступе, Win32_TemperatureProbe даёт два LM78A с пустым CurrentReading, единственная живая цифра \_TZ.TZ00 = 27.85 C оказалась КОНСТАНТОЙ: 17 замеров за 4 мин 51 с при загрузке ЦП 1-16% дали разброс 0.00. Стороннего не ставил, обход не выдумывал. П.3: термальных событий за 30 суток НОЛЬ — id 37/38/39/40/86/87/88 ноль, Kernel-Power 86-89/140 ноль, провайдера ACPI нет, перебор всех 4240 записей System по семи словам дал 169 совпадений и ВСЕ до одного — текст загрузочной записи id 55 про Throttle (T) States. Оговорка: механизм и не должен был сработать — схема Высокая производительность, PercentofMaximumFrequency=100 во всех 12 замерах, 4701 МГц при загрузке 0-4%. П.5 вердикт: картина перегрев НЕ ПОДДЕРЖИВАЕТ — падения 0x7F в 05:13:25 и 0x50 в 06:51:39 в самые прохладные часы, первому предшествовали 5 ч 40 м холостой работы; пять WHEA из восьми вне дневной жары; наработка до падения не растёт и не убывает (5ч40м/47м/3ч12м/5ч18м). Но сказано именно НЕ ПОДДЕРЖИВАЕТ, а не «перегрева нет»: температуры за сутки не мерились ни разу и штатно неизмеримы. ГЛАВНОЕ ПО ФАКТУ: все восемь WHEA за 25 суток — БАЙТ-В-БАЙТ одна ошибка, MCi_STATUS=0x9400004000040150, MCACOD=0x0150 = memory hierarchy / IRD ВЫБОРКА ИНСТРУКЦИИ / INSTRUCTION / L0, секция PROCESSOR GENERIC errtype=CACHE ERROR, а секция PLATFORM MEMORY пуста (ValidBits=0) во всех восьми — планка не названа ни разу. Два числа локализуют дефект: 7 ошибок из 8 на ОДНОМ физическом ядре (лог. CPU 2 и 3), и 3 из 8 по РОВНО ОДНОМУ младшему адресу 0x1bf486 в ТРЁХ разных загрузках при рандомизированной базе ядра (случайность одной пары — 1 к 2 097 152). В каждой из четырёх загрузок WHEA и последующее падение легли в один образ ядра (10-11 общих hex-знаков, 0.22-7.50 МБ), три падения из четырёх в пределах 12 минут после WHEA. Порядок версий по силе: процессор > планки > перегрев, у каждой назван ОДИН отделяющий замер (сутки с кондиционером / прогон по одной планке / достать номер банка MCA — последнее чистое чтение). Поправки к премисам: WHEA сегодня ЧЕТЫРЕ, а не три (05:01:59, 06:42:23, 10:26:56, 14:37:48); после смены частоты памяти случилось только ОДНО падение из четырёх (окно BIOS 12:56:00-13:04:42), три предыдущих до неё. Режим строго read-only: ничего не установлено, тестов памяти и нагрузки не запускал, BIOS не трогал, живых процессов не снимал, дампы и журналы не чистил, клиентского контура не касался, в фоне ничего, ничего не удалено, КОММИТОВ НЕ ДЕЛАЛ. Временное место названо: сессионный scratchpad (скрипты разбора WHEA) и _scratch_thermal_0823/ (скрипт дозаписи), оба untracked. Раздел добавлен сверху в узел master живой карты, адрес по имени KB_MASTER из живого реестра: 410415 -> 418492 символов, расписка ok, бэкап tmp/brain_backup_thermal_0823_20260823-192332.txt, прежние разделы не тронуты.
