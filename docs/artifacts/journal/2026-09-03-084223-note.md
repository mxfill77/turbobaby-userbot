# Тело записи журнала — 2026-09-03 08:42 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-03 08:42:23 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #68 → done · [V0 судит done: сделано · V0: PROVEN / all_gates_passed по адресу «docs/artifacts/2026-09-03-ящик-берёт-задания-из-документов-папки.md»] Ящик Штаба переехал с блоков в узле на отдельные документы папки мозга. Премиса подтверждена живым вызовом до единой строки кода. Префикс `shtab_task_`, порядок по имени, отзыв переименованием `.СНЯТО`. Все 6 замков и 4 сигнала перепроверены заново на новом источнике. Тесты 159, трижды — 159/159/159. **FACT:** коммиты `f7cbe20` (код), `9a93091` (артефакт), `327faef` (тело журнала) в `git log`. Тесты набора **159/159/159** трижды одной командой. Гейт демона (`test_pc_orchestrator` + `test_pc_local_dec`) — 1017 зелёных. Живая проба `shtab_box_run.py --status` на боевом мосту прошла весь путь. **Признак сделанности (первым действием):** артефакта по адресу не было — работа с нуля. **1. Премиса — жива.** `list_brain_folder` → `ok=True`, детей **50**, `shtab_box_probe` в списке, ключа в реестре у него **нет** (без ключа 15 из 49). Развилка, решившая устройство: такой документ читается **только по file id** — на имя мост отвечает `unknown_name`. Двух дверей не будет не по нашему самоограничению, а потому что второй не существует. **2. Префикс `shtab_task_`** — константа `shtab_box.TASK_PREFIX`, в руках литералом не набрана. Обоснован замером: `shtab_box`→2 (затянуло бы саму шапку), `shtab_box_`→1 (документ-проба, дословно «Заданий не несёт»), `shtab_task_`→**0** — чистое пространство имён. Ключ = имя минус префикс; форма маркера очереди не менялас …обрезано
