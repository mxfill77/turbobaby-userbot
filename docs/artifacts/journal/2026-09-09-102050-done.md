# Тело записи журнала — 2026-09-09 10:20 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2244 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-09 10:20:50 |
| символов в строке | 2244 |
| порог | 600 |

---

DONE Dispatch 09.09 заход 231: ЯЩИК-ОСТАНОВКА-НЕЗНАЮ-ЗАМОК-0909 — остановка ящика вида «не знаю» звала владельца нажимать кнопку, которой нет. По коду: holdable требует determinate (shtab_box_signals.py:1238), signal_a/signal_b при rows_ok=False дают determinate=False (:656, :726), arm фильтруется тем же holdable (:1368) — остановка живёт РОВНО ОДИН виток. Доказано поведением: 09:53:34 UTC остановка «Снимается ТОЛЬКО словом владельца» с двумя метками → 10:04:02 UTC ящик САМ взял #231 (разрыв 10 мин 28 с), меток f7e7b567826c/ac33f71008b9 в pc_orchestrator.shtab_box_hold.jsonl НЕТ ни строкой. Владельцу ушла ТОЛЬКО строка журнала (pc_orchestrator.py:11701); Telegram не было ни одного — tell = untold(hold + arm), arm пуст по построению, значит notify_hold и release_buttons не звались, и обещанной кнопки не существовало вовсе. Корпус не пришёл из-за BridgeTransportError: второе плечо моста (echo) бьёт обратно в наш /exec, ключ расписки мёртв, дальше был бы ложный «Invalid or missing token»; источник корпуса — Queue.rows(("failed","done")) → bc.get_pending(lane='all') (shtab_box_run.py:635, зов :867). Ложных признаков прочитанности за местные сутки — 1 из 88 витков (1.1%); второй промах моста в 06:12 в счёт не входит, там упала живая очередь и сигналы не считались (sig.evaluate под if marks_asked, :1006). РАЗВИЛКА решена в пользу правки текста: stop_words получил свою ветку для determinate=False («ЖДЁМ ПРИБОР, А НЕ ВАС … уйдёт САМА … нажимать нечего … чинить надо ЕГО»), journal_line кладёт такую остановку NOTE, а не ASK. Тормоз НЕ ослаблен: запрет брать читает непустую ФРАЗУ (shtab_box.select, shtab_box.py:1206), ветки определённых остановок не тронуты ни символом, дверь снятия у слепой цела (merge_held примет метку) — убрана ПРОСЬБА, а не право. Отрицательный тест зелёный: определённая остановка называет BY_OWNER, метку, узел и кнопку И даёт armed + hold_notices. Тесты 137 (было 134) / 176 / 1111 OK, плюс потребители фразы vitrina_pc 93, close_msg_pc 78, shtab_box_accept 30 — красных нет. Коммиты 0af66e9 (код+тесты) и 0acdf99 (артефакт). Из проверки НЕ ушло ничего: замер новой фразы — чистые функции, тесты на заглушке двери, живой инбокс не тронут. Мост не чинился — чужая полоса, названа.
