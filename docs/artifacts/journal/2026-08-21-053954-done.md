# Тело записи журнала — 2026-08-21 05:39 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1363 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-21 05:39:54 |
| символов в строке | 1363 |
| порог | 600 |

---

DONE Dispatch 13:30: длинная аренда починена — на сроке от полугода бот цену НЕ называет, а зовёт человека. Коммиты 1459f96 (код+11 тестов) и eaec1ae (артефакт), запушены в origin/main. Живой прогон экзамена ПОСЛЕ правки: случай 7 «полгода на скутере» — цифр расчёта НОЛЬ (было 47 чисел + обещание скидки), ответ дословно: «Такой длительный срок мы, конечно, сдаём, но считается он отдельно — цену и скидку на полгода согласует менеджер, я передам ему ваш запрос»; случай 1 не задет — 298/2980/3000 до бата. Корень: _parse_term НЕ ЗНАЕТ слова «полгода», term_days был null — длинный срок доезжал до цены неотличимым от недельного. Замок стоит ПЕРВОЙ веткой _build_pricing_note_core, раньше всех трёх ценовых, поэтому молчат ВСЕ строки списка, а не часть. ГРАНИЦЫ ЧИСЛОМ В ПРАВИЛЕ НЕТ — порог 182 взят из уже записанной в suggest.py равнозначности _PAST_ROLL_MAX_DAYS, не назначен; нужно РЕШЕНИЕ ВЛАДЕЛЬЦА, где между двумя неделями и полугодом лежит настоящая граница. Регресс: test_suggest 591/10 красных — имена те же, что в базе; набор пути цены 176/20 — множество ИДЕНТИЧНО замеру без правки (снималось стэшем, дерево вернулось байт-в-байт). Новых красных ноль. Голдены не правились, весь гейт не запускался, клиентам не отправлено ничего, ботов не перезапускали, выкатки не делали. Разбор → docs/artifacts/2026-08-21-long-rent-silence.md
