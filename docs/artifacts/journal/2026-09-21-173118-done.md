# Тело записи журнала — 2026-09-21 17:31 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1599 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-21 17:31:18 |
| символов в строке | 1599 |
| порог | 600 |

---

DONE Dispatch 00:35 (70i KVITANCIYADVAISHODA 2209): квитанция тренажёра говорит по КАЖДОМУ уроку номер, состояние словом и дословную строку включения — коммиты 92ede2b9 (код) и 9b1797f7 (артефакт). ПРЕМИСА Штаба подтвердилась ЧАСТЬЮ: развилка одна и внутри записи урока (exam_show.py:1844), но меряет непустую ПРИЧИНУ И РЕЖИМ, а не наличие наблюдения; оба признака поднимаются БЕЗ события (env EXAM_LESSON_MODE; текст наблюдения в своём снимке), поэтому состояние ЧИТАЕТСЯ из базы, а не выводится из условия записи. Форма слова и его АДРЕС переехали в одно место (новый lesson_word_forms.py, promote_phrase сверяет образец тем же разбором; pc_agent.LESSON_ON_RE — ссылка на него). Живой роутер слова ровно один (pc_agent, тема 205), карточка экзамена висит в другом месте — образец без адреса послал бы владельца говорить слово туда, где его никто не разбирает. Живой прогон на своей базе tmp/kvitanciya_2209: один тап → #1 актив + #2 кандидат РАЗЛИЧЕНЫ в одном сообщении (1/1/1/0). МУТАНТ (состояние подменено в базе, запись не тронута): кандидат 1→0, актив 1→2, слово включения 1→0. ТРЕТИЙ ИСХОД проверен ПЯТЬЮ намеренно созданными состояниями — везде «НЕ ЗНАЮ» с причиной, «действует» не сказано ни разу. ДЛИНА при трёх уроках 771 символ против потолка 4096 (trainer.TG_MSG_LIMIT), не режется, влезает ещё 31 ждущий урок. Тесты: 13 новых в test_lesson_receipt.py + 2 замка в test_exam_show.py; 529 проверок уроков OK, test_pc_orchestrator 1152 OK. Наружу 0 байт (отправитель 2, агент 2, прибор 3 — все заглушки). Боевая база уроков не открывалась ни на запись, ни на чтение.
