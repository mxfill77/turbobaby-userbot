# Тело записи журнала — 2026-09-03 04:28 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2095 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-03 04:28:05 |
| символов в строке | 2095 |
| порог | 600 |

---

DONE Dispatch 11:45: заявка, которой отбор не дал карточку, больше не висит «ждёт владельца» — ряд закрывается сам третьим исходом «снято отбором» (коммиты 7d3ce44 правка, ec57b7c артефакт). ПРЕМИСА замерена живой очередью: 10 рядов needs_approval — 6 заявок-ревью, 4 разведзаявки ступени E, 0 карточек гарда; карточка уходила по 0 из 6 (в задании «по пяти нет» — расхождение в ту же сторону и сильнее, доложено числом). ИСХОД назван своим словом: не «принято» и не «отклонено», статус done со своей меткой, разрез не пересекается с _REJECT_PREFIX; в тексте исхода и в журнальной строке прямой фразой стоит, что решил ОТБОР (машина), а не владелец, и что согласием это не является. ЧЕТЫРЕ ЗАМКА отрицательными тестами: клиентский дефект не закрывается ни одной веткой (и сверх потолка тоже); карточка уходила → ряд ждёт ОТВЕТА; отбор недостоверен (граф не построился ИЛИ не спрошен) → ряд остаётся ОТКРЫТЫМ; нет номера — закрывать нечего; плюс отказ Моста оставляет ряд открытым и вне обоих чисел. НАЙДЕНА И ЗАКРЫТА СЛЕПОТА ОТБОРА: живой адрес премисы несёт номер строки (model_name.py:30), а client_contour его не срезает — проба дала is_client(model_name.py)=True при is_client(model_name.py:30)=False, то есть условие «предмет виден клиенту» было слепо к КАЖДОМУ адресу в коде, и живой ряд #32 — настоящий клиентский дефект — уезжал в сводку; порог отбора НЕ тронут, починен разбор адреса, голден переведён с выдуманного формата на живой. РАЗВЕДЗАЯВКИ ступени E под наш отбор не попадают (их судил бы не тот вопрос, разбирать их нечем, свой отбор у них стои́т раньше — на входе); поправка к заданию по коду: «да» им захода полосы НЕ стоит — pc_orchestrator:3451 закрывает их done «принято к сведению» без прогона. ЧИСЛА ПОСЛЕ сухим ходом живой очередью: закрыто отбором 5, ждёт владельца 1 (дефект #32 карточкой), под чужим отбором 4; сводка несёт оба числа ВРОЗЬ и больше не врёт строкой «каждая заявка осталась открытой». Тесты 214 OK трижды одной командой (было 189, +25). Боевую очередь руками не трогал — закрывает демон своим оборотом. Откат ZAYAVKI_PC_NO_CLOSE=1.
