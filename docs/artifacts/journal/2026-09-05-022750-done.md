# Тело записи журнала — 2026-09-05 02:27 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2203 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-05 02:27:50 |
| символов в строке | 2203 |
| порог | 600 |

---

DONE Dispatch 09:50: задача 238 — полоса исполняет ДВЕ задачи одновременно, commit 0142532 (пуш прошёл). ПРЕМИСА проверена своими числами: пол паузы ящика 600 с при медиане 642 с (тик не ограничитель), 33 CLAIM за сутки при TASK_TIMEOUT=2700, MAX_CLAUDE_PROCS=2 при взятии ровно одного ряда, медиана захода 999 с по 490 строкам METRICS. ПАРАЛЛЕЛЬ: тело process_new (взять ряд → headless → закрыть) в двух потоках, join ВНУТРИ process_new — снаружи виток остался одним витком, _write_heartbeat() по-прежнему последняя строка (О2/О4 целы). Потолок два держится кодом: parallel_lanes()=max(1,min(ручка,2,MAX_CLAUDE_PROCS)), мусор в ручке читается как 1. ЗАМОК КОММИТОВ — git_serial_pc.py, байтовый замок ЯДРА (msvcrt/fcntl), а не свой флажок: держатель умер — ядро снимает само, стухнуть нечем (класс ночи 03.09 закрыт по построению, файл git.lock не удаляется ни одной веткой). ОТРИЦАТЕЛЬНЫЙ ТЕСТ на двух настоящих процессах: без замка laneA теряет коммит целиком (rc=128 index.lock), под замком оба коммита на месте, git fsck=0, index.lock не остался. СМЕРТЬ ОДНОГО: исключение ловится у корня потока, сосед доработал (done), ряд упавшего закрыт честным failed вместо 90-минутного висения; мост промолчал о статусе — ряд НЕ закрывается по догадке. МИНА ПОПУТНО: _executor_verdict вынес бы приговор СИРОТА живому соседу — лечится множеством _RUNNING_TIDS и приписыванием PID ребёнка своему потоку. ЗАМЕР: устройство даёт ×2.00 (две задачи за то же стенное время витка), полоса на своей истории ×1.15 — МЕНЬШЕ полутора раз, и разницу съела ПУСТАЯ ОЧЕРЕДЬ: в 85% переходов закрыл→взял следующая задача ещё не пришла, медиана ожидания 695 с. Узкое место измерено неверно: не рука одна, а задачи приходят по одной. ТЕСТЫ: test_pc_orchestrator 1009 (было 993), красных ровно те же 2, что и на чистом дереве через git stash; test_git_serial_pc 15 зелёных; test_selfupdate_gate+test_deps_registry_check 10 зелёных; полный набор полосы не гнал. ОСТАТОК НАЗВАН: у ребёнка обёртка — инструкция (env GIT_SERIAL_PC плюс абзац преамбулы), а не решётка; решётка требовала бы правки .claude/settings.json, класс edit_claude, туда не ходил. ОТКАТ: PC_PARALLEL_TASKS=1. FACT: commit 0142532 в git log
