# Тело записи журнала — 2026-08-21 05:41 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-21 05:41:02 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #79 → done · Случай 7 экзамена починен: на сроке от полугода бот цену не называет ни поштучно, ни в списке, а говорит «считается отдельно» и зовёт менеджера. Случай 1 не задет (298/2980/3000 до бата). Новых красных ноль. Границы числом в правиле НЕТ — нужно решение владельца. **FACT:** коммиты `1459f96` (код+11 тестов), `eaec1ae` (артефакт), `93b8f22` (тело журнала) в `git log`, все три в `origin/main`. Живой прогон экзамена ПОСЛЕ правки: случай 7 — `цифры расчёта: []`; случай 1 — 298/2980/3000. Регресс: `test_suggest` 591/10, набор пути цены 176/20 — множества красных идентичны замеру **без** правки. --- ## 1. Первое действие (доложено) `f64bc3c` 2026-08-21 12:04, локальное время 12:11. Артефакта `2026-08-21-long-rent-silence.md` **не было** → делал с нуля. ## 2. Правило — из живого реестра по имени ``` ИМЯ KB_business_rules -> name:business_rules [канон business_rules] ПРОЧИТАНО: символов 60966, строк 1155 ``` Раздел «РЕАЛЬНЫЕ СРОКИ АРЕНДЫ», строки 391–396: «ДЛИНА В ПОЛГОДА: сдаём, но полгода обсуждается отдельно — цена такой аренды не берётся из расчёта, её согласует человек… Величина скидки не фиксирована и правилом НЕ становится: в расчёт не идёт, бот её не называет». ## 3. Забор Честертона — одной строкой **`_parse_term` не знает слова «полгода» вовсе** — у случая 7 в живом прогоне стояло `term_days: null`, и полугодовая аренда доезжала до цены неотличимой от недельной. Молчание ничего не ломает чуть ниже границы: 181 сутки, «на месяц» и двухмесячный пик случая 4 считаются как прежд …обрезано
