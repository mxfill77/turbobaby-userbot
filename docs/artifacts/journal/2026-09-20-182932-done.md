# Тело записи журнала — 2026-09-20 18:29 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1789 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-20 18:29:32 |
| символов в строке | 1789 |
| порог | 600 |

---

DONE Dispatch 01:30 (db714569): 69j перепись кнопок группы обслуживания — ЗАМЕР, кода не тронуто ни строки, в группу не отправлено ничего, ни одна кнопка не нажата. Число Штаба 19 НЕ СОШЛОСЬ, верю своему счёту: в группе -1002751134848 живут 13 кнопок по callback / 17 по надписи, слепых из них ДВЕ (info:card и svc:km), полуслепая одна (svc:mok), полный след у 5 (fix/sodo/done/codo/undo), неполный у 5. Разница с 19 разобрана тремя источниками: (1) 67u мерил ЧИСЛО нажатий, а не след — ветка, печатающая своё последствие, след оставляет; (2) другая популяция — в те 25 входят o3:* и svc:undogo, 13 кнопок ВНЕ группы (O3_TEST_MODE=True и NARYADY_TOPIC=None уводят наряды в HQ, undogo уезжает в инбокс 1160); (3) три кнопки числились без прибора, а прибор есть и ноль у них настоящий — маркеры fix-btn/sodo-кнопка/верхняя-граница внесены 16.07, 24.07 и 14.08, то есть ВНУТРИ окна лога, а grep -ac по 415190 строкам даёт 0. Главный дефект не там, где ждали: svc:col (×3) и svc:oil пишут в боевой Лист1, а подтверждают человеку ТОЙ ЖЕ сводкой _emit_summary, которую он видит после svc:km, где не записано ничего — признак поднимается без события, значит негоден; у col нет даже кнопки отмены, различающей сводку масла. svc:km — единственная ветка без единого log.*, и она закрывает цикл. Сквозной шов: отказ недоверенному не логируется ни у одной из шести кнопок, где он есть, хотя образец закрытого шва лежит в том же файле (svc:fix, сторож Б). Мина грепа снята: svc:undogo построен КОРТЕЖЕМ, по слову callback_data не находится вовсе. Локальные копии splinter.py читать было нельзя — свежайшая 7649 строк против живых 9688, якоря 67u не совпадают ни в одной. Все адреса строк перепроверены грепом перед публикацией. → docs/artifacts/2026-09-21-69j-SLEPYEKNOPKI-2109.md
