# Тело записи журнала — 2026-08-06 13:09 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-06 13:09:55 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #377 → done · Откат снят и проверен, замена НЕ сделана: текста рамки 06.08 нет ни в строке задачи, ни в дереве (grep «06.08.2026» → 0 файлов), ни в каналах — сочинять копию инструкций Штаба нельзя. Живой док не тронут (10202 симв., версия 04.08), доказано чтением назад другим процессом ПО ИМЕНИ. Готова одна команда на момент, когда текст придёт. Коммиты 60f97c3, f9c2271, 64a06c3. ## FACT-отчёт **1. Адрес сошёлся** (живой Bridge через `brain_writer`, только чтение). Реестр: **33 ключа**, `shtab_frame` → `1DiXr3jxoZYxkWaS7k4X4SR0u2xQKUTCi` — совпадает с id задания; на этот файл ссылается **ровно один** ключ; `resolve_name("KB_shtab_frame")` → `name:shtab_frame`; чтение по id и по имени — **10202 символа оба раза, посимвольно равны**. **2. Откат снят ДО всего остального, в двух местах** — `tmp_shtab_rewrite_20260806_n4/shtab_frame_2026-08-06_before.txt` и в git `docs/artifacts/2026-08-06-shtab-frame-before-rewrite.txt`: по **17 423 байта, 10202 символа, sha256 `67f7767cc96651b0…`**, побайтно равны друг другу и посимвольно — живому доку. Заодно снята мина: при `core.autocrlf=true` копия вернулась бы из чекаута с CRLF и перестала быть побайтной — заведён `.gitattributes` ровно на `docs/artifacts/*-before-rewrite.txt` (`git check-attr`: у копии `text: unset`, у соседнего `.md` и `README.md` — `unspecified`). **3. Почему замены нет.** Текста рамки 06.08 нет: в строке задачи (демон отдаёт её без обрезки) — только оговорка «пришлю следующим сообщением»; в дереве — `grep «06.08.2026»` даёт **0 файло …обрезано
