# Тело записи журнала — 2026-07-31 07:23 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **883 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-07-31 07:23:41 |
| символов в строке | 883 |
| порог | 600 |

---

ASK Dispatch 14:42: признак «человек или машина» на ответе с ПК замком НЕ является — замерено, что автономный процесс получает вердикт human ОДНОЙ командой (снял метки демона через env -u, терминал у его оболочки есть) и подтверждает своё же красное без рубильника и без билета. Прежний комментарий в коде утверждал обратное — исправлен на фактическое поведение. Чинить локально нечем: env, tty, происхождение процесса, файл — всё это агент достаёт, недостижимого локального признака не существует. РАЗВИЛКА ВЛАДЕЛЬЦА: (а) подтверждение вне машины — кнопка в Telegram с owner-gate по uid, как у devbot, тогда признак становится настоящей границей; либо (б) оставить признак сигналом для дежурного и не полагаться на него как на замок. Пока PC_ANSWER_* в окружении нет, дыра не эксплуатируется. Разбор → docs/artifacts/2026-07-31-pc-approve-channel.md (6719c9c).
