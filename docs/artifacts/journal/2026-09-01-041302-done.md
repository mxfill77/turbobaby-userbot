# Тело записи журнала — 2026-09-01 04:13 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **997 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-01 04:13:02 |
| символов в строке | 997 |
| порог | 600 |

---

DONE Dispatch 11:35: класс ложных карточек гарда закрыт — вид назначает ЭФФЕКТ, а не подстрока (коммит 3b911e5, запушен). Три места полосы ПК: слово secret в ИМЕНИ файла, git rm --cached (индекс, не диск), имя секрета в ДОКСТРИНГЕ скрипта — последняя карточка родилась 01.09 10:31:57 на фразе, эффект ОТРИЦАЮЩЕЙ. Прогон до правки 8 расхождений из 20, после 0; регресс TestEffectNotSubstring 17 проверок, 6 из них держат защиту; база красного на HEAD 7, после правки 6 — своих ноль. Послабление идёт с усилением: тело записи теперь судится на настоящее присваивание секрета, поэтому значение ловится в файле с ЛЮБЫМ именем (до правки проходило молча). Вторая полоса: по снимку tmp/srv от 01.08 двух классов там нет вовсе, третий закрыт trust-by-origin — зеркалить нечего, точки касания названы. Честная граница замера: 70 строк лога из 94 обрезаны писателем на 300 символах и не пересчитываются. Разбор и оба прогона → docs/artifacts/2026-09-01-класс-подстроки-гарда-закрыт.md
