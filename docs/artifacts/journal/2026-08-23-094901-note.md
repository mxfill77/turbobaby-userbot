# Тело записи журнала — 2026-08-23 09:49 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1566 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-23 09:49:01 |
| символов в строке | 1566 |
| порог | 600 |

---

NOTE Dispatch 23.08 шаг3/боевой пояс наличия: ПРАВКА ВНЕСЕНА в два боевых файла, бэкап до правки _scratch_availbelt_0823/suggest.before.py. suggest.py: (1) _PC_AVAIL_RE — снята связка порядка слов «свободен\s+на», утверждающие формы взяты словом по правилу word_hit: (?<!\w)свобод(?:ен|на|но|ны)\b, (?<!\w)доступ(?:ен|на|но|ны)\b, (?<!\w)в\s+наличии\b; (2) _pc_maybe — стем «свободн\w*» заменён настоящим корнем (?<!\w)свобод\w* (беглая гласная свобод-Е-н), левые границы добавлены и остальным токенам; (3) _AV_POS_RE — левые границы плюс формы свободно/свободны/доступно/доступны, чтобы «недоступен» не читался утверждением по куску строки; (4) новый носитель availability_from_note(pricing_note) — True только по дословной фразе кода _AVAIL_CODE_PHRASE «свободен на эти даты», иначе None, fail-closed; (5) guard_availability ВКЛЮЧЁН последним шагом generate_draft, перегенерация даётся обязательно (regenerate_draft гарда в себе не несёт, рекурсии нет). moderation_core.py: guard_availability включён в _default_regen — та же сборка, что держит сквозной голден TestEndToEndMax2stix. ТРИ ОТРИЦАТЕЛЬНЫХ ТЕСТА (_scratch_availbelt_0823/neg_tests.py) ЗЕЛЁНЫЕ: при занятом юните пояс ловит все три формы (прямой, обратный, беглая гласная) обоими слоями; при свободном юните все три честных ответа проходят байт-в-байт с source=draft; корень внутри чужого слова не ловится на живых словах корпуса «безналичным», «в безналичной форме», «освободится», а «недоступен» остаётся отрицанием, а не утверждением. Гейта ещё не было, сверка 12 кейсов впереди.
