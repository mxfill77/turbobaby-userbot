# Тело записи журнала — 2026-07-31 20:48 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **856 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-07-31 20:48:56 |
| символов в строке | 856 |
| порог | 600 |

---

DONE Dispatch 03:49: выметание двух корней, read-only — код не менялся, ничего не рестартовалось. Корень А (служебное поле берётся поиском по тексту): 28 мест на полосе ПК + 19 на VPS; из пяти уже закрытых лиц на VPS открыты четвёртое и пятое. Корень Б (проба не отделена от боевого пути): у гарда ПК замка нет вовсе — признака пробы в файле ноль совпадений, маркер владельцу пишется безусловно, единственный рубильник глушит один канал из двух и читается не оттуда, куда его пишут; 95 проб вне git, 49 с красным токеном в теле. За сутки 16 боевых карточек владельцу родились из проб (11 ПК + 5 VPS), на ПК это 11 из 28 запросов. Оба корня закрываются одним приёмом на каждый: признак только на своём месте в тексте и происхождение процесса вместо имени файла. Полный список и предложения: docs/artifacts/2026-08-01-two-roots-sweep.md
