# Тело записи журнала — 2026-08-05 05:37 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1031 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-05 05:37:28 |
| символов в строке | 1031 |
| порог | 600 |

---

DONE Dispatch 12:37: разбор read-only — подмена client_watch.json тестом НЕ объясняет простой userbot 397 мин. По коду: файл для демона write-only (пишет только _persist_client_watch:5742 из maybe_client_watchdog:5935), читающих мест в демоне ноль, единственный читатель — pc_agent._read_watch_snapshot:394 → status_text. Вердикт вотчдога считается из ОЗУ _client_watch_state:5635 + CIM + свежесть лога, диск в него не входит; фикстура children={} способна только занизить. За 14 суток подмен ≥180 (146 интерактивных прогонов + 34 гейта самообновления, каждый гоняет test_pc_orchestrator), внутрь окон простоя попало 0 — ближайшая за 32 мин до первой смерти, демон переписал её правдой до halt. Причина простоя прежняя: сеть → 3 смерти по 66с → латч halted. Новое: pc_orchestrator.heartbeat — тот же класс с ОБРАТНЫМ знаком (у _write_heartbeat:806 параметра пути нет вовсе, а читает его watchdog:7537 при решении о подъёме демона). Правок не делал → docs/artifacts/2026-08-05-client-watch-clobber-vs-outage.md
