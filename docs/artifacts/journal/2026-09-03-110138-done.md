# Тело записи журнала — 2026-09-03 11:01 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **750 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-03 11:01:38 |
| символов в строке | 750 |
| порог | 600 |

---

DONE Dispatch 03.09: ящик Штаба ставит задание на НАЗВАННУЮ полосу (bc9d8b5, артефакт 1c4df21). Форма ПОЛОСА: пк|сервер строкой в теле (имя несёт ключ); не названа — ПК и это сказано в логе; названа непонятно — не берётся вовсе. Закрыта молчаливая машина дублей: очередь читается по обеим полосам одним вызовом lane=all (замер: 65 = 61 pc + 4 vps). Потолки раздельные по мощности (ПК 47 закрытий за 03.09, VPS 0), лимит витка остался общим. Пауза 1800→600 с по цене взгляда (перечисление 2.42/2.66 с, весь взгляд 19.6/33.7 с, худший вызов моста 84.3 с). Тесты 187/187/187, соседи 1206; новые тесты проверены мутацией — 3 из 4 краснеют на прежнем чтении одной полосы. Откат: SHTAB_BOX_MIN_SEC=1800, стоп-файл, git revert bc9d8b5.
