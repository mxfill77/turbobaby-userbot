# Тело записи журнала — 2026-08-01 15:40 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-01 15:40:47 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #173 → done · Готово. Рамка Штаба легла в мозг **новым** документом (не дублем) — с регистрацией, дословной сверкой и открытым хвостом, о котором надо знать. **FACT-отчёт** **1. Создавать или дописывать — решено фактом ДО записи.** `create_brain_plain` неидемпотентен, а перечислить файлы папки Brain канал не умеет, поэтому «прошлый заход» искался по четырём независимым следам: | проба | результат | |---|---| | живой реестр `--list-brain` | 32 ключа, похожих на рамку — **ни одного** | | `resolve_name("KB_shtab_frame")` | **отказ**: канон `shtab_frame`, ключа нет, похожих нет | | живой `cowork_log` (212 309 симв.) | `shtab_frame` — **0 вхождений** | | живой `pulse` | следов **0** | Документа не существовало → создание. **2. Что легло (числа живого прода).** ``` ok=True id=1DiXr3jxoZYxkWaS7k4X4SR0u2xQKUTCi key=shtab_frame name=KB_shtab_frame chars=4669 chars_back=4669 verified=True fffd=0 ``` Проверка **отдельным вызовом после записи**: `--resolve KB_shtab_frame` → `name:shtab_frame` (канон), `--probe --name KB_shtab_frame` → 4669 симв., 104 строки, U+FFFD 0; реестр **32 → 33** ключа. Кодировка проверена **до** отправки: BOM нет, `═` 6, `«` 6, `»` 6, `→` 1, `≠` 1, `ё` 7, `№` 1 — в живом доке U+FFFD тоже 0. **3. ОТКРЫТО — текст источника оборван.** Присланный текст кончается посреди фразы: последняя строка живого дока — `разведка закрыта, миграции нет. Этап 3`. Записал **дословно, как прислано**; досочинять описание этапа 3 не стал, своей пометки в тело дока не ставил (хвост придёт дозаписью в …обрезано
