# Тело записи журнала — 2026-09-07 07:11 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2352 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-07 07:11:51 |
| символов в строке | 2352 |
| порог | 600 |

---

DONE ПК 07.09 14:20: ПРИБОР БАЙТОВ ОТВЕТА ПОСТРОЕН И ПРИНЯТ ОТРИЦАТЕЛЬНЫМ ТЕСТОМ. Коммиты 40cea88 (proc_bytes.py 816 строк + test_proc_bytes.py 483, 28 голденов) и e646ed9 (артефакт). Контракт взят готовым из §6 d2c2afb, все четыре части, расхождений нет. Два регистратора внутри процесса: свой sys.meta_path-искатель пишет имя, путь, sha256 и МОМЕНТ загрузки модуля (момента не было нигде — это и делало ленивую дверь ненаблюдаемой), аудит-ловушка на событии open видит чтение файла данных БЕЗ единой правки на месте вызова. Провенанс тремя словами и не повышается: чтение > кэш (заголовок .pyc сверен со stat исходника) > добор (вошёл ДО прибора — сверка честно зовёт «не проверено», а не «совпало»). ОХВАТ ЧИСЛОМ, живыми импортами и живым open: разряд A — путь+sha 13 из 13 рёбер, момент открытия 13 из 13, семь различных целей, второй эшелон +36 модулей поимённо; разряд B — 9 из 9 существующих путей плюс lesson_store.tsv вне git; разряд C — 0. Итого 22 из 23 названных дверей; двадцать третья suggest_pairs.jsonl — файла на диске нет, читать нечего. Слепых среди реально открывавшихся — 0 из 22. ТРИ ОТРИЦАТЕЛЬНЫХ ТЕСТА ЖИВЫЕ: ОТ-1 дверь не открывалась → «нет в отпечатке», итог не зелёный; ОТ-2 отпечаток старше ответа отвергается отдельным исходом, живая половина настоящими байтами даёт v1 против v2 и вердикт РАЗОШЛОСЬ; ОТ-3 тест ПОКАЗЫВАЕТ буквой OK, что прибор без файлов данных зеленеет, и потому такой не принят, а полный называет playbook.md и lesson_store.tsv как «вне git», price_source.json как «разошлось». У самого прибора поймано два ложных числа: счётчик разряда C дал 865 (в exec идёт штатный запуск каждого модуля), потом 24 — потому что abspath("<string>") разрешает выдуманное имя от рабочего каталога, а он у демона равен корню репо, ложных 24 из 24; после правки 0, совпало с артефактом. Оба промаха закрыты голденами. Соседние тесты 177 зелены, сторож реестров зависимостей совпал. ЧТО НЕ ЗАКРЫТО И НАЗВАНО: прибор в живые боты НЕ поставлен — установка требует их подъёма, а это решение владельца; проводка выписана дословно двумя строками (install() первым действием во входном скрипте, emit() рядом со строкой отправки). Процессов не трогали и не поднимали, кругов тренажёра не гоняли, --write не звался, ничего не удалено, конфиги не читаны. Временное: _scratch_bytes_0907/ и tmp/proc_bytes_probe/.
