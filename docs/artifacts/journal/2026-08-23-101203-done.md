# Тело записи журнала — 2026-08-23 10:12 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1580 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-23 10:12:03 |
| символов в строке | 1580 |
| порог | 600 |

---

DONE Dispatch 23.08 17:12: три дыры боевого пояса наличия закрыты, коммит 166a81a, разбор → docs/artifacts/2026-08-23-avail-belt-holes.md. Забор Честертона отвечен историей: гард не отключали, а не подключали никогда — вне docs/ и тестов guard_availability трогал ровно один коммит 9c45837 от 15.07, он внёс определение и юниты и не тронул сборку; удалявших вызов нет. Порядок слов и беглая гласная починены уже введённым правилом слова word_hit, цена левой границы измерена живым корпусом переписок (безналичный ×15, освободится ×31, недоступен ×20). Гард включён в generate_draft и moderation_core._default_regen на НОСИТЕЛЕ — дословной фразе кода из ценовой записки (availability_from_note, fail-closed); выбор носителя посчитан: из quote-блока 1 ложное срабатывание из 84, из записки 0. Три отрицательных теста зелёные: занятый юнит — все три формы пойманы, свободный — все три честных ответа прошли байт-в-байт, корень внутри чужого слова не ловится. Сверка 12 кейсов: гард изменил 0 из 84 черновиков, мягкий пояс 1 из 84 — ровно тот клейм обратного порядка, который задача велит ловить; откат не делал, решение и его цена названы в артефакте, откат стои́т одной строкой. Гейт затронутым набором три прогона подряд: 766 тестов, 9 красных в каждом, множество равно базе до правки, новых красных ноль, тестов 757→766. Пустое значение проверяющему осталось в ОДНОМ боевом месте — suggest.py:7483, это чек прибора, чинить запрещено заданием. Раздел добавлен сверху в узел master живой карты (399893→406383 символов, расписка ok), прежние разделы не тронуты.
