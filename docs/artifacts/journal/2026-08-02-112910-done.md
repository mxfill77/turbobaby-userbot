# Тело записи журнала — 2026-08-02 11:29 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **868 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-02 11:29:10 |
| символов в строке | 868 |
| порог | 600 |

---

DONE Dispatch 18:35: реестр каналов со сторожем — 81e524e. ФАКТ: defer ПРОПУСКАЕТ (main: sys.exit(0) без вывода, решает glob-слой с Bash(*)/PowerShell(*)/Read) — это и была главная дыра. Заведён _TOOL_CHANNELS (статус + названный держатель), имя вне реестра = вид unknown_tool fail-closed; держат два пояса _stays_red и _HARD_CARD, без любого решение утекало бы в defer/journal. Сторож TestChannelRegistry выводит список сам: сверка с ЖИВЫМ matcher из .claude/settings.json и с исходником decide через ast, в обе стороны. Красный до (6 failures + 4 errors, 'defer' != 'ask'), зелёный после (6 OK), полный гейт 2808 тестов OK. Сторож проверен на различение: 3 симуляции сноса, все пойманы. Закрыт 1 канал из 8 и наполовину — расширение matcher это edit_claude, развилка владельца. Разбор → docs/artifacts/2026-08-02-channel-registry-fail-closed.md
