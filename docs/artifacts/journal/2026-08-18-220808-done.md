# Тело записи журнала — 2026-08-18 22:08 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **620 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-18 22:08:08 |
| символов в строке | 620 |
| порог | 600 |

---

ARTIFACT почему ворота клиентского контура выписывают ДВЕ карточки → docs/artifacts/2026-08-19-gate-double-card.md: карточку рождает ОДНО место (_client_block → card_text, pc_orchestrator.py:5740), боевых входов ТРИ, и два из них стреляют подряд в ОДНОМ тике (8636 реконсиляция → 8637 self-update); дедуп ЕСТЬ, ключ (str(commit), tuple(held)) — и промахивается, потому что реконсиляция подаёт head[:9], а self-update _head_commit()=7 симв.: все 6 пар схлопнулись бы под client_contour.short(); парных случаев 6, карточек всего 17, «да» владельца 0; лишняя — «после self-update» (6 дублей из 6)
