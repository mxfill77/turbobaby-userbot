# Тело записи журнала — 2026-09-06 07:13 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1611 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-06 07:13:18 |
| символов в строке | 1611 |
| порог | 600 |

---

DONE ПК 14:15: отмена урока по номеру подключена к отзыву в базе (коммит b963236). Было: «отмени урок N» резала буллет плоской книги и отвечала «Отменил», а бот по уроку продолжал отвечать — с 8bfb55b ответ читается из базы. Стало: trainer.cancel_lesson → lesson_store.withdraw(number=N), шесть исходов своими словами (withdrawn/already/not_found/no_store/denied/no_author), право — moderation_core.may_write_rule, то же самое, что у перевода кандидата, fail-closed. Контрфакт на ОТВЕТЕ одной песочницей: ДО правки исход removed и урок в промпте до=True после=True (плюс снят ЧУЖОЙ буллет — нумерации книги и базы разные), ПОСЛЕ правки исход withdrawn, до=True после=False, промпт посимвольно равен прежнему. Мутационная проба: тело «как было» роняет 19 из 21 теста, тело «право снято» — ровно 5 правовых. Регрессия в обе стороны: добавление 100 из 100 зелёных, отмена 34 из 34, прибор регрессии 57 из 57, весь набор 6562 при 20 красных и нулевом моём вкладе в них. Отменённый урок ОСТАЁТСЯ с отметкой снят(урок;штамп), lines_before == lines_after. Соседи разобраны по существу: пять тестов команды в test_trainer переписаны на новое место правды, текстовый замок регрессии починен убиранием имён из комментария, а не смягчением. Своя порча названа: мутационная проба старым телом стёрла живую запись бокового списка trainer_rules.json (заметил чужой test_lesson_migrate_book), файл восстановлен из индекса, дыра закрыта в пробе и в наборе. Остаток: текстовая команда без кнопки отказывает по имени, лечится строкой who=username в userbot_listen.py — правка userbot этому заходу запрещена.
