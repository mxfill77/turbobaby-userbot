# Тело записи журнала — 2026-08-18 08:29 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-18 08:29:48 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #47 → done · Пересчёт сделан на рабочем диапазоне 7–14 суток: 420 броней = 33.8 % корпуса и 26.3 % денег, ±15 % = 62.7 % по всей истории и 81.1 % на свежем окне при медиане промаха 8.1 %. Гипотеза владельца про кепку подтвердилась только в низком сезоне; в высоком опровергнута числами. Артефакт и журнал записаны, база не тронута. **FACT:** commit `fcb64b1` в git log (артефакт), `794e70b` (тело журнальной записи); журнал — две строки в мозг, `OK: записано в мозг` ×2; контрольные суммы четырёх читаемых файлов — `ИТОГ: все суммы совпали, база не тронута`. ## Признак сделанности `docs/artifacts/2026-08-17-price-accuracy-working-range.md` на диске **не было** → работа выполнена. Основание (`2026-08-17-price-accuracy-wide.md`, корпус 1243) на диске **есть** → «НЕИЗВЕСТНО» не применяется. ## Замки перед счётом (все пройдены) - **Прибор тот же.** `price_source.json` сменил sha (`6134182c…` → `dd382ac9…`), но блок за блоком: все счётные блоки (`base/season/term/formula/sign_rule/low_season_discount`) **совпали побайтно**; разошёлся один добавленный `freshness` (`e8995e9`). Расхождений по базе моделей — 0. - **Числа те же.** Нарезка по корзинам файла воспроизвела §11 замера 17.08 до знака: медианы `+0.0 / +2.7 / +6.2 / +0.4 / −7.5`, доли ошибки `3.7 / 6.4 / 18.2 / 24.2 / 47.4 %`. - **Кривая срока прайса** взята из прод-кода `QuotePrice.js` (не из прозы) и воспроизвела слепок 15.08 — **0 расхождений из 14**. ## 1. Корзины срока (прогон B) ``` корзина n доля судимо деньги ฿ доля медиана ±15 % ±5 % |о …обрезано
