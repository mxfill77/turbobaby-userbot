# Тело записи журнала — 2026-09-05 00:19 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2199 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-05 00:19:44 |
| символов в строке | 2199 |
| порог | 600 |

---

DONE Dispatch 07:35: Задача 231 (полоса ПК, ось бизнес) — урок перестал быть выстрелом вслепую. Премиса проверена своими командами: прогонщик trainer_run.py 805 строк НАЙДЕН, запись урока в книгу НАЙДЕНА, прогона корпуса после урока в коде ОТСУТСТВУЕТ (grep trainer_run из пути урока — ноль). Заведён lesson_regress.py (639 строк): spawn отсоединённым Popen без wait/communicate — нажатие владельца не удлиняется ни на прогон; одиночка O_EXCL; замер = скрининг 16 кейсов на 1 прогон плюс подтверждение ТОЛЬКО красных; сравнение с последним известным ЧИСЛОМ; одна короткая строка владельцу тремя голосами (ничего не сломалось / СЛОМАЛОСЬ N кейсов с номером и САМИМ чеком — имя, ждали, факт / прогон не состоялся и потому НЕИЗВЕСТНО), плюс четвёртый оттенок эталона-не-было. Шов в trainer._apply_lesson только на added, fail-safe: падение прибора урок не отменяет. Эталон в pc_orchestrator.lesson_regress.json под существующей маской gitignore, первый берётся ЧТЕНИЕМ реестра ворот при совпавшем corpus_sha; несостоявшийся замер эталон не переписывает. Вердикт ворот не пишется — имени write_verdict в модуле нет, сверяется тестом по телу файла; книга правил не правится и урок не откатывается ни одной веткой. Цена числом: 26с на кейсо-прогон (замер 05.09, 832с на 32), скрининг 16 на 1 = 7 мин, худшее 14 мин; уроки подряд СКЛЕИВАЮТСЯ в один прогон по итоговой книге (очередь отвечала бы трижды устаревшим, пропуск терял бы ответ), потолок 4 пересчёта, строка называет все склеенные уроки и при красном признаёт, что не знает какой сломал. Тесты своего набора: test_lesson_regress 57 OK, три отрицательных класса — правило ломает известный кейс даёт красный а не тишину, недоступная сеть даёт НЕИЗВЕСТНО а не всё хорошо, запись урока при упавшей регрессии всё равно происходит. Соседи test_trainer 114 OK, test_lesson_urok 14 OK; полный набор полосы не гнался. Живая проба механизма на двух кейсах: кейс 1 = 69с, кейс 4 = 13с, 20 из 20 чеков, четвёртый голос сработал по-настоящему — реестр ворот держит запись 02.09 на СТАРОМ 12-кейсовом корпусе и эталоном не взят. Артефакт: docs/artifacts/2026-09-05-после-урока-корпус-прогоняется-и-говорит-исход.md. Коммит a42c8ee запушен.
