# Тело записи журнала — 2026-08-21 21:11 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-21 21:11:40 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #22 → done · Опись тренажёра снята чтением; раздел лёг в узел карты master. Ключевой ответ: тренажёров ДВА — `trainer_run.py` гоняет код рабочего дерева в своём процессе, `trainer.py` живёт внутри живых детей. Две премисы из трёх оказались неверны: прогон 31.07 дал 10 ЗЕЛЁНЫХ из 12 (не красных), а 50000 — порог комфорта, а не ёмкость. **FACT:** - Запись в мозг: `записано в name:master: 230407 → 237788 символов`, обратное чтение — «текст дословно на месте (вхождение 1 раз)», бэкап `tmp\brain_backup_trainer_recon_0822_20260822-040639.txt`. Адрес взят из живого реестра моста (`--list-brain` → ключ `master`), id не хардкодился. - Журнал: `OK: записано в мозг, символов: 539953 | досланы отложенные: 1`. - Код не менялся: HEAD как был — `0be1ea6`, `git diff --name-only HEAD` пуст (правок 0). Тренажёр не запускался, гейт не гонялся. --- ## 1. ЧЕМ ГОНЯЕТ — ключевой вопрос, ответ первым **Тренажёров ДВА, и ответ у них разный.** **1) `trainer_run.py` — код РАБОЧЕГО ДЕРЕВА, свой процесс, живых детей не трогает.** Доказано кодом запуска: `REPO = os.path.dirname(os.path.abspath(__file__))` (`trainer_run.py:70`) + `import client_contour / suggest / trainer` (`:89-91`) — Python грузит модули с диска, потому вердикт и требует чистое дерево и несдвинутый HEAD (`:148-155`). Telethon не импортируется вовсе, `userbot.lock` не берётся, боевая `moderation_ipc.db` уводится в temp до первого обращения (`isolate_ipc`, `:111-127`), логи в temp (`:87`). Наружу не уходит ничего. **Оговорка, которую нельзя терять:** « …обрезано
