# Тело записи журнала — 2026-09-02 14:00 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1570 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-02 14:00:49 |
| символов в строке | 1570 |
| порог | 600 |

---

NOTE Orchestrator: задача #7 → done · [V0 судит done: сделано · V0: PROVEN / all_gates_passed по адресу «docs/artifacts/2026-09-02-ящик-берёт-задания-из-папки-по-префиксу.md»] ОСТАНОВЛЕНО НА П.1 — самим заданием предписано. Перечисление документов папки мозга демону НЕДОСТУПНО тем путём, каким он ходит в мозг: у моста нет ни одного такого экшена. Кода не менялось ни строкой; артефакт с меню решений для Штаба написан, закоммичен, запушен. **FACT:** коммиты `f44e7d3` (артефакт) и `699c65b` (тело журнальной записи) в `git log`, оба запушены в `origin/main`. Живой замер прода 02.09: `read_doc&name=roadmap_v2` → `{"ok":false,"error":"unknown_name","message":"Нет имени \"roadmap_v2\" в манифесте"}` при живом файле; контроль `read_doc&name=shtab_box` → `ok, 39 символов`. ## Что измерено (четыре независимых замера, все живым вызовом) | замер | результат | |---|---| | список POST-экшенов, который мост отдаёт сам | **67 экшенов**, перечисления файлов папки нет ни одного | | `?action=help` | **лжив**: легаси-список из 9 имён, в нём нет даже `list_brain` — поэтому GET опрошен вызовом, а не списком | | 31 кандидат GET-имени перечисления | **31 из 31 → `unknown_action`**, транспорт не отказал ни разу | | `list_brain` | **37 ключей + `folder_id`** — это МАНИФЕСТ (реестр зарегистрированных имён), а не содержимое папки | | именной путь `read_doc&name=` | загорожен манифестом, запасного хода в папку нет — мост говорит это про себя дословно | **Круг, который полоса ПК не рвёт:** чтобы документ Штаба попал в манифест, нужен `registe …обрезано
