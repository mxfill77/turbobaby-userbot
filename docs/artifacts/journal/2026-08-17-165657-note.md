# Тело записи журнала — 2026-08-17 16:56 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-17 16:56:57 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #36 → done · Знаменатель поднят с 15 пар до **N=1243**. Сезонное знание стои́т **1 241 848 ฿** недобора (57.7 %; за последний год 421 069 ฿), но полосу ±5 % источник не держит ни в одном прогоне — 11.6 % против 22.4 %. Отрицательная батарея прогнана ДО замера и устояла (0 провалов). Контрольные суммы базы до и после совпали, в сеть не ходили. **FACT:** - `commit c13aa54` в git log — артефакт `docs/artifacts/2026-08-17-price-accuracy-wide.md` (40 230 байт) - `commit 2b65ae9` — тело журнальной записи (авто-вынос писателя) - две строки в мозге: `OK: записано в мозг, символов: 422727` и `423230` - контрольные суммы **до = после**: `rows.json 78f7287a…fc7f3e`, `price_source.json 6134182c…2fe8b` — база не тронута - бот не запускался, живая дверь `quote_price` не звалась **ни разу** (в сеть не ходили) **Признак сделанности:** артефакта на диске не было — заход состоялся полностью. **Порядок соблюдён именно тот.** (1) Правило отбора записано дословно в шапку `sweep.py` **до** первого прогона; отброшено 35 из 1278 по пяти названным причинам (статус 18 · уплачено 7 · имя юнита 5 · дата 4 · срок 1). (2) **N=1243** — сходится с независимой переписью 15.08 до единицы (1278−18−17). (3) Цена единицы на двух первых — 0.000755 с; 1243 × 0.000755 = **0.94 с против потолка 1890 с**, влезает целиком, сделаны все, не осталось ни одной. (4) Запись с `flush`+`fsync` после каждой единицы. **Отрицательный тест устоял, и по дороге нашлось два факта.** Девять периодов **тайлят год целиком** (0 непокрытых суток из 3 …обрезано
