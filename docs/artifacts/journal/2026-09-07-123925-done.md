# Тело записи журнала — 2026-09-07 12:39 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **3614 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-07 12:39:25 |
| символов в строке | 3614 |
| порог | 600 |

---

DONE Dispatch 19:45: ПОЧИНЕН ПРОДУКТ, а не сторож — живая карточка владельцу перестала терять число. Задание Штаба 01-j-card-duty-number.0907, полоса ПК. Признак сделанности смотрел содержимое: из 26 артефактов за 07.09 предмета касаются два, и оба говорят дословно «чинить надо код — отдельная задача»; не сделано, заход с нуля. Премисы Штаба подтверждены все три: место card_duty.py:229, причина — чтение числа СМЕЩЕНИЕМ lines[i+1] («_card печатает их подряд»), а печатать подряд перестали 03.09 — коммит 39fb7e4 вставил между объектом и числом строку последствия «да» (CONSEQ_LINE_PREFIX); позиционная связка у соседнего поля «Объект» уже ломалась 01.08 (там свободным поиском, 18/20), и лечили её тем же лекарством — чтением С МЕСТА. Правка efe2657: заведён card_duty._num_index() — число ищется тем же устройством, каким его кладёт печатающая сторона (блок объекта, между ними ровно одна необязательная строка последствия), а не позицией и не свободным поиском; довод строгости взят у гарда — ошибка строгости даёт владельцу прочерк, ошибка вольности даёт правдоподобное ЧУЖОЕ число под честным объектом. Молчание снято прибором, а не вседозволенностью: сверка читателя с _card_fields по ВСЕМ 20 видам живого словаря плюс замок литерала строки последствия зеркалом гарда — следующая вставленная строка покраснит гейт, а не потеряет число у владельца. ОТРИЦАТЕЛЬНЫХ входа два (чужая строка «Число:» в теле карточки; число СОСЕДНЕГО блока многоблочного маркера), оба собраны живым _card и оба с зубами: наивный «вольный» фикс отдаёт на них '99 целей' и '1 цель', читатель места — пусто. КОНТРФАКТ двумя прогонами на одном входе: до — num='', после — num='1 цель'; модуль 41 тест при 1 красном → 45 зелёных, ни один тест не удалён и не ослаблен. ЗАМЕР по всему словарю: число печатали 2 вида из 20 (delete «1 цель», clasp_deploy «деплой …»), терялись ОБА, то есть всё, что было; после правки сошлось 20 из 20. ПОЛНЫЙ НАБОР один раз в конце, 103 модуля живым списком gate_selective.all_test_modules: Ran 6807, failures=6, skipped=11, 464.998 с — против Ran 6803, failures=7, skipped=11 у замера предыдущего захода. Красных по всему набору 7 → 6, девятнадцать не стали двадцатью; арифметика сходится без остатка: 6803+4 новых теста = 6807, skipped как было 11, так и осталось. Сравнение корректно без повторного прогона «до»: git log 9d4baa4..HEAD содержит РОВНО один коммит (мой efe2657), отслеживаемые файлы чисты — дерево между замерами не менялось ничем другим, хотя рядом шёл второй заход. Оставшиеся шесть красных все в test_suggest (5 ждут слова владельца о судьбе инварианта «строка J дословно», 1 судит среду запуска) — их не касался ни строкой. ЧЕСТНЫЕ ПРЕДЕЛЫ: дежурный по-прежнему выключен по умолчанию (CARD_DUTY), поэтому вклад — готовность к включению и к регрессу, а не «снято N живых случаев»; живой переписи «сколько боевых карточек несли число» нет — тела карточек в лог гарда не пишутся, мерил корпусом регресса и за бой его не выдаю; прогон занял 464.998 с против 370.744 с у предыдущего замера, разницу приписываю загрузке машины и содержательно объяснять не буду. Путь до человека боевым вызовом НЕ трогал ни разу: все карточки собраны в памяти проверочного процесса, ни маркер не писался, ни _notify_topic/deliver не звались; ни одного сообщения владельцу или в группы. Ничего не удалял, место для временного одно и названо явно — tmp/card_num_0907/; процессов не трогал, .env и конфиги не читал; кругов тренажёра не гонял, --write не звал; красных операций не потребовалось ни одной. Артефакт: docs/artifacts/2026-09-07-card_duty-число-в-карточке.md (коммит cf9024a).
