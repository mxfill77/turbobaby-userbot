# Тело записи журнала — 2026-08-23 09:44 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1435 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-23 09:44:49 |
| символов в строке | 1435 |
| порог | 600 |

---

NOTE Dispatch 23.08 шаг2/боевой пояс наличия: ЗАМЕРЫ ДО ПРАВКИ, кода ещё не тронуто. (1) Корпус живых переписок client_chats.anon.jsonl (710 записей) назвал реальные слова класса «корень внутри чужого слова»: «безналичный» ×15 держит корень налич (прямой близнец «безопасным» ⊃ «опасн», тот же префикс без-), «освободится/освободиться/освободился» ×31 держит корень свобод, «недоступен/недоступна/недоступны/нетдоступна» ×20 держат корень доступ. В 84 сохранённых живых черновиках 12 кейсов таких слов НЕТ ни одного — отрицательный тест берём с живого корпуса, а не выдумываем. (2) Носитель наличия найден и он ОДИН: фразу «свободен на эти даты» печатает _client_price ровно под if q.get("available") в двух точках (suggest.py:3458 и :3477), и она приезжает в ЦЕНОВУЮ ЗАПИСКУ; в 49 записках из 84 она есть. (3) Контрфакт включения гарда посчитан на тех же 84 черновиках: если брать наличие из quote-блока (как это делает ПРИБОР на suggest.py:7416) — гард срабатывает 1 раз из 84 и ровно на правдивой фразе кейса 10 прогона 3 «на эти даты свободен» (обратный порядок, quote-блока в ветке минимального срока нет); если брать наличие из ценовой записки по дословной фразе кода — 0 срабатываний из 84, текст 12 кейсов не двигается. Значит включать гард надо на носителе-записке, а не на quote-блоке. Скрипты замеров: _scratch_availbelt_0823/roots_live_corpus.py, measure_guard_on_84.py, measure_guard_note_carrier.py.
