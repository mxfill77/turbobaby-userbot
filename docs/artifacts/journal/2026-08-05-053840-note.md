# Тело записи журнала — 2026-08-05 05:38 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-05 05:38:40 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #304 → done · **Подмена снимка тестом вчерашний простой НЕ объясняет и объяснить не могла: для демона `client_watch.json` — write-only витрина для `pc_agent`, вердикт вотчдога живёт в ОЗУ. За 14 суток подмен ≥180, внутрь окон простоя попало 0. Причина простоя прежняя: сеть → 3 смерти → латч `halted`. Найден однофамилец с обратным знаком — `pc_orchestrator.heartbeat`.** ## 1. Что хранит и кто читает Схема (`pc_orchestrator.py:5755`): `{ts, cooldown, max_deaths, children:{имя:{deaths, halted, last_raise}}}`. Живой файл 268 б. - **Пишет** — `_persist_client_watch` (`:5742`), единственный касающийся файла код (`open "w"` + `os.replace`, `:5757-5761`), из одной точки `maybe_client_watchdog` (`:5935`), троттлинг 300 с (`:132`). - **Читает** — **ровно один**: `pc_agent._read_watch_snapshot` (`pc_agent.py:394`) → `_supervision_label` (`:405`) → `status_text` (`:433`) → телеграм-хендлер (`:731`). `pc_agent` — отдельный процесс, ОЗУ демона ему недоступна. - **Читающих мест в демоне — ноль** (греп по дереву: объявление `:211`, обёртка `_state` `:267`, запись `:5747`). ## 2. Мог ли обмануть вотчдог — НЕТ, по построению 1. `_client_watch_state = {}` (`:5635`) — модульный dict в ОЗУ; докстринг `:5632`: «Состояние в памяти демона… рестарт его сбрасывает». 2. `client_watchdog_tick`: `state = _client_watch_state if state is None` (`:5822`); `_client_watch_step` (`:5724`) считает `alive|raise|cooldown|halt_now` только из `state.get(name)`. 3. Живость — из CIM (`:5645`) и свежести лога (`:5716`), файл в верд …обрезано
