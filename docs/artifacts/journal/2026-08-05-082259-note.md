# Тело записи журнала — 2026-08-05 08:22 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-05 08:22:59 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #325 → done · Реапер молчал **не** из-за нечитаемой полосы — замер это опроверг: у всех 6 сирот за 7 суток отказов чтения после истечения порога **ноль**. Причина — условие: реапер мерил молчание `updated`, а молчит и живой прогон. Починено: сиротство доказывается исполнителем, доказанная сирота снимается за 300с вместо 5400с. Красный до — 12/19, зелёный после — 19/19, гейт 3040 OK, запушено (88d53de, 2a77206). ## 1. Почему реапер не увидел сироту — место Не полоса, не права, не «его не зовут». **Условие.** `pc_orchestrator.process_stuck_singles`, строка `if age <= PC_SINGLE_STALE: continue` — где `age` это возраст `updated`, то есть **молчание**, а порог 5400с обязан быть большим, потому что живой прогон молчит так же (heartbeat во время прогона не тикает вовсе). Кандидата «полоса не читается» (`pc_orchestrator.py:3365`, 13 таких тиков за неделю) разделяет с настоящей причиной один вопрос: **сколько отказов чтения пришлось на момент, когда порог уже истёк** — только такой отказ и стоит задаче времени. Ответ по всем шести сиротам — **0**. У 305 пошагово: первый же тик после claim (12:45) полосу прочитал (возраст ~120с), шесть отказов 13:34–14:11 легли на возраст 3100–5330с (ниже порога в любом случае), тик 14:15:53 прочитал и снял. **Нечитаемая полоса не стоила 305 ни одной секунды.** ## 2. Замер: 6 сирот, 10,0 ч, 90% из них — сам порог | id | висела | не запускалась / умер исполнитель | отказов чтения после истечения порога | |---|---|---|---| | 61 | 6169с | запускалась, COMPLETE не лёг | …обрезано
