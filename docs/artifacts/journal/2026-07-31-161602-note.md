# Тело записи журнала — 2026-07-31 16:16 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-07-31 16:16:02 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #134 → done · Той дыры формы на ПК НЕТ — рубеж карточки чист по устройству канала. Но на ВТОРОМ рубеже (класс одобрения) нашлась дыра того же семейства: скраба штампа класса на ПК нет вовсе, а `Класс операции:` читается в любой строке карточки — перенос строки в объекте красной операции даёт «да» сразу на два класса. Воспроизведено пробой на задеплоенном коде. Не чинил. ## Ответ по коду **Где якорен скраб.** Функции скраба штампа на ПК **нет ни одной** — ни в дирижёре, ни в гарде (греп `scrub` по `*.py` даёт только `suggest._scrub_gen_year`, клиентский контур). Для рубежа карточки он и не нужен: stdout исполнителя карточкой не становится вообще, чистить нечего. Для рубежа класса нужен и отсутствует: `pretool_guard._write_marker` (`pretool_guard.py:2725-2731`) дописывает свой `KIND_LINE_PREFIX`, а тело карточки от него не чистит и под штамп не режет. **Где читается происхождение.** - карточка — `pc_orchestrator.py:795-822`: единственный источник — файл-маркер; `ln.split(MARKER_SEP, 1)` берёт токеном всё **до первого разделителя**, то есть якорь на голову строки конструктивный; `tok != run_token` → чужая; - заявка — `pc_orchestrator.py:774-792`: `(?m)^[\s>*_`«"'\-]*NEEDS_APPROVAL:(.*)$`, якорь на начало строки, и карточкой результат не является вовсе; - класс — `pretool_guard.py:2092/2247`: `_RE_KIND_LINE` построчно якорен, но `finditer` идёт по **всей** карточке, а не по строке гарда. **Проходит ли заявка со штампом посреди строки — проба (дерево чистое, оба файла на `9e518b7`):** ``` A1 ca …обрезано
