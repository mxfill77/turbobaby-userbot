# Тело записи журнала — 2026-08-05 03:43 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-05 03:43:36 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #296 → done · Спул ревизора вчерашним фиксом **ПОКРЫТ** (константа + голден; гейт самообновления тоже — дискриминатор смотрит способ запуска, а не `TESTING`); снаружи остался его адресат `moderation_ipc.db`. userbot за 7 суток: **6 эпизодов простоя, 479.8 мин**; 5 из них — необработанный `ConnectionError` (входы 485 и 541). Надзор снаружи снимает 370.6 мин из 480 без единого клиентского файла. --- ## 1. Спул ревизора — внутри замка | факт | где проверено | |---|---| | `REVIZOR_SPOOL_FILE = _state(...)` | `pc_orchestrator.py:5968`; `_state` → `log_setup.state_path` (`log_setup.py:163`) | | `REVIZOR_STATE_FILE` — тем же приёмом | `pc_orchestrator.py:5770` (закрыты оба конца: находки и «докуда дошли») | | голден называет спул поимённо | `test_log_setup.py:669`; прогнан здесь: `Ran 4 tests in 0.070s → OK` | | гейт самообновления накрыт | `_gate_unittests` (`pc_orchestrator.py:3017`) зовёт `-m unittest`; `TESTING` там НЕ взведён, но `_started_as_test_runner` смотрит `__main__.__spec__` — срабатывает без флага | | фикс в бою | демон на `commit=921c9c6` с 05.08 01:01; спул `[]`, 2 б, mtime 05.08 00:07:47 — не тронут | **Снаружи остался сосед:** `REVIZOR_DB = os.path.join(REPO, "moderation_ipc.db")` (`pc_orchestrator.py:5765`) — **без `_state()`**. Это и источник окон ревизора, и адресат доставки находок из спула; его изоляция держится на `TESTING`, которого в гейте самообновления нет. **Что нужно:** одна строка — `env["TESTING"]="1"` в `subprocess.run` строки 3017. Не чинил. ## 2. Падения userbot …обрезано
