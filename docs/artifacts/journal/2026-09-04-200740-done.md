# Тело записи журнала — 2026-09-04 20:07 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1833 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-04 20:07:40 |
| символов в строке | 1833 |
| порог | 600 |

---

DONE Dispatch 03:10: задача 220 — тренер перестал советовать то, чего в бизнесе нет. Премиса проверена своими командами, все 3 пункта: hypotheses_prompt(incoming, answer) = 2 параметра, system 308 симв., весь запрос 508 симв.; KB_trainer_log (14941 симв.) отдал живую строку 05.09 02:29 TEST-11 с четырьмя гипотезами, из них №1 (город и остров подачи) и №4 (точное время подачи) против бизнеса. Правка ОДНОГО файла trainer.py: в запрос вошли действующая книга правил целиком (2843 симв., suggest.load_playbook — тот же файл, куда пишет «Применить»), факты о компании из констант КЛИЕНТСКОГО промпта (CRITICAL_FACTS + 6 жёстких правил + парк из локального снимка Лист1 + состав брони, 6365 симв.) и НАКОПЛЕННЫЙ транскрипт тренажёра (потолок 3000, режется с головы). Запрос 508 → 10969 симв. типично (потолок 13766), в 21.6 раза, вызов LLM по-прежнему ОДИН. Повтор правила книги снимается детерминированно предикатом suggest._rules_similar плюс замок соразмерности 0.5 (без него слово «коротко» умирало о правило стиля — промах поймал живой тест панели). Стоп-листа слов нет ни одного. Секция ОТСЕЯНО владельцу не показывается, виден счётчик. Канон недоступен → «Советую ВСЛЕПУЮ» первой частью, а не молчаливый откат. Сети в месте сборки нет сознательно: hypotheses_prompt зовётся синхронно в обработчике модербота без concurrent_updates. moderation_bot.py и userbot_listen.py не тронуты ни строкой. Тесты: 3 отрицательных плюс 4 страхующих, набор тренажёра целиком 231 OK. Вклад в храповик неразбора trainer.py 47 → 58 точек, линию не поднимал. НЕ ИЗМЕРЕНО ЧЕСТНО: живого прогона новой головы на входе 05.09 не было (вызов модели — сеть вне контура задания), число «сколько из четырёх снимет живьём» не называю. Коммит 9a1e96b запушен. Артефакт docs/artifacts/2026-09-05-гипотезы-тренажёра-видят-книгу-и-канон.md
