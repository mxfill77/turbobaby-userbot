# Тело записи журнала — 2026-09-05 08:17 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1351 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-05 08:17:03 |
| символов в строке | 1351 |
| порог | 600 |

---

DONE Dispatch 15:35: задание Штаба 00-lock-path.0905 (полоса ПК). Премиса проверена своими командами: путь лока собирался ВЕРНО, резалось СООБЩЕНИЕ — обрезание по длине str(e)[:60] в expectations_pc_run._read_lock, где 57 символов из 60 съедал сам путь через repr с удвоенными разделителями Windows. Обрезаны были ВСЕ ЧЕТЫРЕ имени полосы (mod/pc_/pc_/use), причём pc_agent.lock и pc_orchestrator.lock давали неразличимый огрызок «pc_». Правка одна и в одном месте: имя файла берётся у пути одним куском (os.path.basename) и стоит ДО среза, режется только причина (_why_short, ERR_TAIL=80); заведён единственный источник имён LOCK_FILES + lock_path(), из которого теперь берут путь MOD_LOCK_FILE, KID_FILES и CODE_LOCKS (литерал pc_agent.lock был написан руками дважды). Третий исход и честность наблюдателя не тронуты: expectations_pc.py в коммитах нет, при непрочитанном локе состояние по-прежнему «неизвестно» и пометка стоит. Живьём: pc_agent — замыкание 7 файлов считается, pid=25688, code_state «код с диска»; три отрицательных случая дали «неизвестно + заметка», «свежий → закрытие o6u|pc_agent», «старый → o6_pc_code_late». Тесты своего набора целиком: 186 OK (было 178, +8 в новом классе TestLockNameIsNeverCut). Коммиты d79f556 (код+тесты) и 74681f8 (артефакт docs/artifacts/2026-09-05-имя-лока-перестало-обрезаться.md).
