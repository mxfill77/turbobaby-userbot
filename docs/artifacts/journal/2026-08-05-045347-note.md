# Тело записи журнала — 2026-08-05 04:53 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-05 04:53:47 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #299 → done · Класс закрыт: обрыв связи теперь называется «связи нет» (не exec_error), задача от него не считается ни зависшей, ни провалом исполнителя, детектор сна молчит при нулевом сне. Коммит 831b69f запушен, полный гейт 2991 OK, строка в cowork_log легла. ## FACT-отчёт **1. «Связи нет» распознаётся и называется своим именем** - `bridge_http.error_kind/explain` — отказ различается по вопросу «дошли ли до моста ВООБЩЕ». `HTTPError` и `BridgeTransportError`/`BridgeReceiptLost` = **связь есть** (последних в логе **354** за 02–05.08 при живой сети — считать их обрывом значило бы 354 ложняка); `URLError`/`TimeoutError`/`ConnectionError` = связи нет. Код HTTP больше не теряется (401 ≠ 500). - `LinkWatch` — вердикт по трём бесплатным свидетелям (мост / github / api модели), **0 новых запросов**. Подтверждение любого канала гасит отказы всех; «связи нет» — только при двух молчащих; свидетельство троично (`None` = не измерил). - Голое имя исключения в логе заменено разбором: `get_pending(new) ошибка: URLError — СВЯЗИ НЕТ: до моста не дошли — [Errno 11001] getaddrinfo failed [URLError, предел ожидания 90с]`. - В статусе задачи и в журнале: `📡 провал [причина=network_outage · связи нет]`, карточка владельцу — «СВЯЗИ НЕ БЫЛО (внешний обрыв, исполнитель ни при чём)» вместо «❌ провалена». **2. Задача от обрыва не зависшая и не провал исполнителя** - Реапер одиночек не судит непроверяемую полосу (пишет это строкой, а не тишиной) и **вычитает окна обрыва из возраста**: `провисела 6000с, но 4516с из н …обрезано
