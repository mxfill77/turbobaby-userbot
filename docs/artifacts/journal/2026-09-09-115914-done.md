# Тело записи журнала — 2026-09-09 11:59 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **3136 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-09 11:59:14 |
| символов в строке | 3136 |
| порог | 600 |

---

DONE Dispatch 19:20: заход 236 (ящик Штаба 11-a-urok-kandidat-deystvuyushchiy.0909) — у перевода урока из кандидата в действующие появилась ДВЕРЬ, и она зовётся живьём. Коммиты 2af45de (код+тесты) и 8051c6f (артефакт). П1 подтверждена замером: promote жил с 05.09 и его не звал ни один файл дерева, кроме тестов — вердикт владельца правилом не становился ни при каком его старании. П3 поправлена: обратная функция withdraw опирается на CUTS (урок/день/автор), а не на список полей показа. ПРАВО не изобретено, а найдено и процитировано — moderation_core.may_write_rule:530, fail-closed (пустой список прав = НИКОМУ), тот же, что судит запись урока и отмену. Дверь тремя слоями: lesson_store.promote/rollback (примитив, требует причину И имя автора, пишет след), trainer.promote_lesson/rollback_promotion (гейт права, карточки словами, fail-safe), lesson_promote.py — CLI, плюс слово владельца в теме 205 через pc_agent (урок кандидаты / урок включи N: причина / урок откати N / урок след). Нажатие вердикта не изменилось ни веткой и по-прежнему только пишет кандидата — заперто поведением и сторожем на исходнике. След из четырёх частей в lesson_store.tsv.promote: автор, время, номер, откат; откат по номеру возвращает прежнее состояние ПОБАЙТНО (sha256 таблицы до перевода равен sha256 после отката, в тесте стои́т чужая экранированная последовательность). Откат — не снятие: withdraw кладёт третье, новое состояние, откат возвращает прежнее. Порядок следа выбран, а не случился: перевод пишет след ДО правки, откат ПОСЛЕ — из двух неполных исходов всюду выбран тот, где лишняя строка следа, а не потерянный откат. Отрицательных тестов три и они зелены: без причины отказ и ничего не подставлено; откат побайтный; при снятом праве тот же вход даёт ДРУГОЙ ответ (сравниваются два ответа между собой, а не слово одного). Числа: база уроков 381 OK, тренажёр+агент 207 OK, модерация и соседи 289 OK, новый набор 14 OK, всё дерево 6920 при 9 падениях — все разобраны как не мои: client_contour доказан сравнением с git archive HEAD (78 файлов и там, и в рабочем дереве, вошло 0), suggest — живой прайс, храповик неразбора красен и без меня четырьмя чужими файлами. Свой вклад в храповик не прячу: trainer +3 точки, pc_agent +6, итого +9 из общего роста +65; первая редакция давала +12, три точки убраны честно, линию НЕ поднимал. Пункт 5 ЧТЕНИЕМ и без правки: поля источника набора у урока НЕТ (колонок восемь), откатить набор одного источника целиком нечем — разрезов три, четвёртого нет ни веткой; вместо поля живёт соглашение в свободном тексте (перенос 05.09 в почему, автор владелец), и оба ложных пути отката по нему названы. Чего не хватает: девятая колонка, четвёртый разрез, заполнение источника всеми вызывающими — это следующий шаг, здесь не сделан. Боевая база уроков не тронута: её mtime 06.09 04:54, файла следа рядом с ней не существует, все прогоны шли на копиях во временном месте tmp. userbot_listen.py и модербот не правлены ни строкой — последний вершок (слово в самой группе тренажёра и кнопка под карточкой) назван несделанным в артефакте §8 вместе с отсутствующей регрессией корпуса на перевод.
