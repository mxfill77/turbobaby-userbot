# Тело записи журнала — 2026-09-06 03:51 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1963 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-06 03:51:35 |
| символов в строке | 1963 |
| порог | 600 |

---

DONE Dispatch 11:05: звено потери цены названо и доказано. ПЕРВОЕ звено, где число есть на входе и нет на выходе — suggest._safe_quote_for_model, ветка сторожа (suggest.py:3401-3407): pricing.quote_for_model уже вернул status=ok total=1685, после чего price_gate.allow() отказал и число выброшено в {status:error,quote:None}, а _resolve_model_price превратил это в текст 22 провалов «точная цена из Календаря сейчас недоступна». Улика не по аналогии, а из САМОГО прогона корпуса: лог %TEMP%/turbobaby_TESTING_pc_orchestrator.trainer_run.log несёт 20 строк отказа в окне 09:47:11-10:07:57, все с одной причиной «не снялись ручки: J3» (12 NMAX + 4 XMAX + 4 прайс-сетка); 4 отказа сетки сходятся ровно с 4 провалами чека «сетка прайса дословно» кейсов 2 и 14. КОНТРФАКТ снят на одном процессе и одном входе: такт B при кэше отказа даёт «недоступна» при том же total=1685 на входе, такт C после price_gate.reset() даёт [QUOTE]. Гипотеза Штаба про две среды ОПРОВЕРГНУТА тремя замерами: дверь и сборка спрошены в ОДНОМ процессе и дали одинаковый ответ, name_filter для NMAX равен None, TURBOBABY_TEST_LOGS ценового пути не касается. Побочная находка, державшая вопрос сутки: тот же флаг уводит логи прогона в %TEMP% под именем pc_orchestrator.trainer_run.log, поэтому pricing.log в репо стои́т с 31.07 и выглядит мёртвым. Причина отказа: девять проб сторожа делят бюджет 90с, а хвост живой двери сегодня 80.13с при медиане 4.18с (128 GET) — одна медленная дверь съедает бюджет, последняя ручка J3 голодает; кэш отказа на 30 мин отравил весь прогон 23.8 мин, отсюда и совпадение кругов 16 из 16. Цена захода 426.1с + 127.5с против потолка 2700с = доля 0.21. ПРАВКА НЕ СДЕЛАНА сознательно: смена измеренного 90с, пересбор price_source.json (запрещён заданием) и сужение кэша отказа — решения владельца с ценой в ожидании клиента. Объяснение «Календарь молчит» отменяется: молчит сторож по бюджету времени. Боевых файлов не изменено, коммит 5eb3c5f.
