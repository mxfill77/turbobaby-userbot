# Тело записи журнала — 2026-07-30 19:32 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **993 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-07-30 19:32:43 |
| символов в строке | 993 |
| порог | 600 |

---

DONE Dispatch 02:45: ротация логов ПК починена по классу, коммит 3237317 (запушен). Диагноз снят живьём: pricing.log не «не ротировался», а был мёртв девять суток — три процесса (userbot_listen/moderation_bot/pc_orchestrator, все тянут suggest→pricing) держали один файл, rename открытого файла на Windows падает WinError 32, и RotatingFileHandler молча выбрасывал КАЖДУЮ строку; замер 23 302 269 байт, mtime 22.07 21:39, проба FileShare.None подтвердила захват. Разведение по кличке процесса (не по PID) в handler_path, канон остаётся хозяину; отказ переката теперь виден в log_rotation_errors.log, stderr и в самом логе, повтор с паузой 300 с. Разбор дерева по AST нашёл семь больных логов из девяти, а не один. Гейт 696 тестов в 10 модулях зелёный. Живой pricing.log НЕ удалён и не переименован (занят) — перестанет быть адресатом после рестарта процессов; процессы не перезапускал, это красное. Детали и таблица → docs/artifacts/2026-07-31-log-rotation-per-process.md
