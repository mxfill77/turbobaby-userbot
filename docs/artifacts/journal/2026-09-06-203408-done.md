# Тело записи журнала — 2026-09-06 20:34 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2415 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-06 20:34:08 |
| символов в строке | 2415 |
| порог | 600 |

---

DONE Dispatch 03:45 (ключ 00-r-trainer-resume.0907): у прогона тренажёра заведена КОНТРОЛЬНАЯ ТОЧКА — таймаут теперь стоит ОДИН КЕЙС, а не весь заход. Коммиты ee40b67 (точка + 25 голденов, 9 из них test_otr_*), bc1268f (причина сброса показывает два РАЗНЫХ значения — дефект найден живым замером, а не чтением), ceeac02 (артефакт). Единица записи — ЗАКРЫТЫЙ КЕЙС (run_one_case), а не круг: критерий F решает исход после ВСЕХ кругов, и половина кругов исходом не является. Основание точки целиком: коммит, корпус+sha, runs, критерий (ид И текст), плюс сверх задания tree_key (sha git diff HEAD — на грязном дереве код коммитом не опознаётся) и ph_key (даты уезжают сами). Расхождение → набор ЗАНОВО и вслух (stdout + поле point_reset вердикта); «?» совпадением не считается. Прежний набор НЕ стирается — наборы лежат по ключу основания рядом. ЖИВОЕ ДОКАЗАТЕЛЬСТВО: заход А убит по таймауту на 130-й секунде с одним закрытым кейсом (2 круга: 100с OK, 31с UNK), заход Б заплатил за него НОЛЬ кругов и прогнал только кейс 2 — «взято из точки кейсов 1 (кругов 2 — голову не звали ни разу) · прогнано живьём кейсов 1 (кругов 2)»; в вердикт доехал весь замер кейса 1 целиком, включая его «неизвестно». Живой отрицательный: та же точка при --runs 3 отвергнута с тремя названными расхождениями, закрытых кейсов 0, полных кругов потрачено 0. АРИФМЕТИКА: полный вердикт — 38 кругов; штабная цена круга 43.7–46.6с ⇒ 1661–1771с (помещается в 2700 одним заходом), сегодняшняя живая цена по 4 кругам 87.0с среднее / 179с максимум ⇒ 3306с и в один заход НЕ помещается; делится на ТРИ захода по кейсам (1–6 = 14 кругов, 7–12 = 13, 13–17 = 11, последний без --only доберёт остаток и соберёт вердикт), каждый в потолке даже при худшем круге. Цена таймаута: было — весь заход до 2700с и ноль сохранённого, стало — не больше одного кейса (2–3 круга, ≤210с штабной ценой) и всё закрытое сохранено. --write НЕ звался ни разу, реестр не менялся (last по-прежнему 4aafbab red 06.09), полный набор не гонялся. ОСТАТКИ ЧЕСТНО: test_raner_ne_v_klientskom_konture красен и ДО моей правки — с коммита a42c8ee (05.09) lesson_regress импортирует trainer_run, и раннер, объявленный шапкой «вне клиентского контура», теперь ВНУТРИ замыкания (доказано на HEAD-копии); +28 слепых точек храповика на моих строках, линию не поднимал. Тесты: test_trainer_run 127 (1 красный — тот самый), соседи 299 OK, test_pc_orchestrator 1061 OK.
