# Тело записи журнала — 2026-09-08 10:15 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **3562 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-08 10:15:47 |
| символов в строке | 3562 |
| порог | 600 |

---

DONE Dispatch 17:35: заход 224, задание Штаба 04-a-baza-obucheniya.0908 — БАЗА ОБУЧЕНИЯ тренажёра описана чтением, кода не писал. Артефакт docs/artifacts/2026-09-08-БАЗА-ОБУЧЕНИЯ-ВЕРСИЯ-ИСТОЧНИК-ОТКАТ-0809.md, коммит 4cb929c (330 строк). ПРЕМИСЫ: П1 ВЕРНА (условие 3 из 4 не выполнено; поправки — «не выполнено» не значит «не начато», а записанного списка всех четырёх условий в дереве по-прежнему нет, как и 21.08); П2 ВЕРНА — KB_trainer_log жив (живое чтение моста: 14941 символ, 42 непустых строки, шапка «TRN LOG v1»), но это ЖУРНАЛ СОБЫТИЙ тренажёра (client/bot/btn/lesson) плюс чужой блок разведки «ЧЕРНОВИКИ ДЛЯ ВЕРДИКТА 17.08», а НЕ набор уроков: вхождений «почему»/«состояние»/«кандидат»/«актив»/«снят(» — ноль. ВОПРОС 1: сущность ЕСТЬ — lesson_store.py + lesson_store.tsv (9 уроков, все актив, все оптовый перенос книги 06.09, живых уроков из тренажёра ноль). Из четырёх свойств есть ОДНО с половиной: АВТОР+ВРЕМЯ+НОМЕР есть (COL_WHO/COL_WHEN/COL_NUM, :130-136); ВЕРСИИ набора нет вовсе (в модуле ноль вхождений «верси»/version, шапка файла — только имена колонок, таблица вне git по .gitignore:150, снимка таблицы нет ни одного, .bak одно поколение и на диске его нет); колонки ИСТОЧНИК нет (колонок ровно 8), фактический источник живёт свободным текстом в графе «почему» — lesson_migrate_book.MIGRATION_WHY, и по нему же опознаёт своё migrated_keys(); ОТКАТА набором одного источника нет — withdraw знает ровно три разреза урок/день/автор (CUTS :173). ВОПРОС 2: вердикт владельца (в коде он зовётся уроком — слова «вердикт» в trainer.py ноль) идёт кнопкой «Обучить» либо командой «урок:» → trainer.apply_lesson :1530 → lesson_candidate :1430 → lesson_store.add_candidate: ложится строкой в состоянии КАНДИДАТ, причина пустая и НЕ подставляется, плоская книга не пишется ни байтом. На следующий ответ НЕ влияет: suggest.draft_from_transcript :8781 → load_playbook :8839 → _learned_from_base :857 берёт только active(), а active() сравнивает с литералом «актив». Отменить МОЖНО — «отмени урок N» → cancel_lesson :1710 → withdraw, кандидат снимается наравне с действующим. ГЛАВНОЕ: включить нельзя — lesson_store.promote :629 не зван НИ ОДНОЙ боевой веткой (вызовов вне тестов ноль, в trainer.py только упоминание в комментарии :1408), а CLI хранилища объявлен «чтение» и ключей --promote/--withdraw не имеет. Петля: записать можно, отменить можно, ВКЛЮЧИТЬ нечем. ВОПРОС 3: самый маленький вариант — одна колонка «источник» плюс четвёртый разрез отката по ней, версией становится пара (источник, дата загрузки); новых файлов не нужно. Готово в дереве: сам lesson_store с дозаписью и fsync, механика разрезов (CUTS/withdrawn_state/parse_state/_matches/withdraw), правка полей по месту _rewrite с .bak и os.replace, ГОТОВЫЙ мигратор оптовой загрузки lesson_migrate_book.py (405 строк, идемпотентен, текст не правит, источник только на чтение) — прямой образец под полтора года переписки менеджеров, обезличивание scrub_lesson без выключателя, замок «бот берёт только актив», состояние «кандидат» с тестами, ёмкость 50000 уроков замером, дисциплина датированных снимков docs/lesson_base. Цена: 9 колонка = МИГРАЦИЯ девяти лежащих строк (load объявляет битой строку с иным числом полей), и промоушен всё равно надо подключать отдельно. Предел ответа назван в артефакте отдельной строкой: заход описывает устройство, петлю обучения проектирует Штаб. Уроков не создавал, не менял и не снимал; в группу тренажёра ничего не слал; календаря и броней не касался. Временное — _scratch_baza_obuch_0908 (одна проба чтения моста), не удалялось.
