# Тело записи журнала — 2026-09-20 09:14 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1794 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-20 09:14:26 |
| символов в строке | 1794 |
| порог | 600 |

---

DONE Dispatch 16:20: заход 68u OTKUDAPROBEG 2009 — читатель парка называет происхождение пробега. Премиса подтверждена своим замером 38 из 38 (29/29 current_km == max(I..L), 9/9 нет). Заведено поле current_km_source с тремя словами: правило переехало в splinter._odo_current_src (2395), _odo_current (2384) стал дверью с прежней подписью — копии правила нет; словарь SOURCES в park_verdict (70-73); park_read отдаёт поле у каждого байка, у каждого регистра и в сводке by_current_km_source. Вердикт на подставленном пробеге больше не «в норме»: третий исход с причиной «пробег подставлен» плюс remaining_km_upper_bound (park_verdict 203-206), а просрочка остаётся просрочкой и объявляется нижней границей (201). Фоллбэк НЕ выпилен. Мутанты в памяти: контроль 53 зелёных 0 красных, выключение третьего исхода 2 красных, снятие нижней границы 1, обрыв происхождения в читателе 5; контрфакт на HEAD тем же входом даёт «в норме» и remaining_km 4000 без единого поля об источнике. Регресс 252 файла тестов 0 красных до (156.8 с) и после (155.8 с), старые тесты не правились, в test_park_read 41 -> 53. Пересчёт парка read-only (снимок 2026-09-20T09:08:14Z, 38 строк, all_read_only=True): было 19 просрочено 17 НЕИЗВЕСТНО 2 в норме -> стало 19 / 19 / 0; поимённо из «в норме» ушли NMAX 155CC GREY PHUKET 5960 и NMAX 155CC RACE PHUKET 7532; из 19 просрочек 12 объявлены нижней границей, 7 стоят на своём одометре. Код на сервере коммит 7b1d3af (никуда не пушен), артефакт docs/artifacts/2026-09-20-68u-OTKUDAPROBEG-2009.md коммит 1d5ea5f2 запушен. Процессы не трогались — живой бот подхватит правку только при следующем рестарте; в рабочие таблицы не записано ни клетки, пишущих вызовов моста нет, временного места на сервере не создавалось, временное на ПК tmp/metka_istochnika/.
