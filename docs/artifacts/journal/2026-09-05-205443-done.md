# Тело записи журнала — 2026-09-05 20:54 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2085 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-05 20:54:43 |
| символов в строке | 2085 |
| порог | 600 |

---

DONE Dispatch 03:55 (ящик night-policy.0904): ограничитель ящика переехал с календаря на ИСХОД. Премиса проверена своими числами: смещение +7:00, lane_day=06.09 против UTC 05.09 (премиса «сутки по UTC» протухла), DAILY_BUDGET=40 и умолчание ручки 40 (премиса «потолок 3» протухла), 03.09 — 21 сессия, 274 367 596 tokens_in, медиана 1018с, max 2643с, ящик дал 3 (#93,#100,#101) — все пять чисел Штаба верны дословно. Из ДВУХ слотов, названных сгоревшими на чужом API, признак Штаба («умер до первой команды либо без следов работы») засчитывает ОДИН: #101 — 529 Overloaded, следов 0/0; #100 — та же API Error, но коммит 14cb105 в окне, значит наш ряд и слот потрачен. Расхождение названо, а не подогнано. Сделан п.5–7 (пп.3–4 сделаны 04.09 и не переделывались): ОДИН различитель в одном месте — shtab_box_signals.external_refusal, четыре условия, признак берётся ТОЛЬКО из части демона (daemon_part режет по первому «[причина=»), потому что черновик доклада пишет наш же исполнитель и задача про этот класс цитирует чужой отказ дословно. Потребители зовут одно правило: суточный счёт (shtab_box.billable, два числа — взято и съедено, дедуп не тронут ни строкой) и сигнал А (вычерк ДО хвоста). Сигнал Б тоже — это нашёл собственный тест: три чужих отказа суть один код exec_error, Б вставал рядом с Д и требовал слова владельца ЗА ПОГОДУ. Своя остановка — сигнал Д «три внешних отказа подряд»: другое имя, NOTE вместо ASK, снимается САМ со сменой суток, корпус — ряды текущих суток (обещание держится формой, а не намерением). Отрицательный тест главный и он бьёт: наш черновик цитирует «API Error: 529» и «Следов работы… нет», все прочие условия совпадают — различитель НЕ клюнул. Реплей на дословных итогах 03.09: живая остановка 20:42:15 не встаёт, взято 3 / съедено 2. Тесты 408 OK, полный набор полосы не гнался. Коммит 05db8c6. Честные остатки: россыпь из трёх чужих отказов за сутки не ловит никто (Б вычеркнул, Д ищет подряд); сводка контура видит слепок без статусов и считает ВЗЯТО, а не съедено. Временное — _scratch_box_policy_0904/, ничего не удалялось.
