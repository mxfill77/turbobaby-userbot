# Тело записи журнала — 2026-08-19 05:19 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-19 05:19:13 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #28 → done · Экран считает пустоту **одним булевым вентилём** `_queue_open_count` над 4 статусами очереди — цели куратора он не видит ни одной строкой, хотя плашка в том же файле их смотрит из того же снимка. Корень глубже: демон закрывает задачу ДО консультации куратора, и в окне ≤180 с у открытой цели строки в очереди нет вообще. Артефакт записан. **FACT:** read-only — `git status --porcelain --untracked-files=no` пуст (ни одного отслеживаемого файла не тронуто), HEAD `4e3465c` не сдвинут; единственная запись — новый артефакт `docs/artifacts/2026-08-19-queue-empty-lies.md` (untracked). Признак сделанности: файла `docs/artifacts/2026-08-19-queue-empty-lies.md` НЕ было (`NOT_FOUND`) — отвечено заново. **Оговорка о базе:** строки экрана нет в ПК-репо (`git grep 'Очередь пуста'` → 0). Код полосы VPS взят из снимка внутри проекта `tmp/srv_e2e_20260814/` (файлы от **14.08**, живой VPS вне проекта = красное). Снимок дословно содержит обе наблюдённые 19.08 строки, копии от 01.08 — не содержат вовсе. **1. Где считается.** `devbot.py:1633-1653` `_announce_idle`. Три числа в тексте — **литералы f-строки**, переменных за ними нет. Вентиль один: `_queue_open_count` (`:1616-1630`) над `_OPEN_STATUSES = ("new","in_progress","needs_approval","approved")` (`:1611`). Слагаемых 4: `by["new"]`, `by["in_progress"]`, `by["needs_approval"]` минус `_revizor_silenced`, `by["approved"]` — все из одного снимка, фильтр `QUEUE_FROMS` (`:169-170`). НЕ берёт: `done`/`failed`; хвост «Закрыто:» — не сутки, а список `_c …обрезано
