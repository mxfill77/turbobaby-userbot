# Тело записи журнала — 2026-08-18 21:57 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-18 21:57:48 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #19 → done · Разведка закрыта: правила бота живут одним md-файлом вне git плюс write-only сайдкар источника; версии и автора нет вовсе, откат есть только для одного правила по плывущему номеру, а на росте ломается не промпт, а тишина — кап 100 молча стирает уроки, возвращая «added». Артефакт записан, боевые файлы правил не тронуты (sha совпал до и после). **FACT:** - Артефакт на диске: `docs/artifacts/2026-08-19-training-base-recon.md`, 34220 байт, 406 строк (`ls -la` + `wc -l`). - Read-only подтверждено хешом боевых наборов правил ДО и ПОСЛЕ всех проб — идентичны: `88e731788784b1fe9315d84d42c60c257cd30b82fdbc697ed6fb08fcc8ffc6dd manager-bot/docs/playbook.md` `bee2d75f81b916ab83796442a70b7350cf348e498865a2fdbe5a09d68a1494e4 trainer_rules.json` `git status --short` по обоим — пусто. - Журнал: `OK: записано в мозг, символов: 456865 | тело вынесено в docs/artifacts/journal/2026-08-18-215645-done.md` - Живая проба канала мозга (только чтение): `brain_writer.py --probe --name rules|business_rules|trainer_log` → `PROBE ok` 16857 / 26636 / 14192 символов. **Ответы по пунктам** **1. Где живут правила.** Один markdown-файл `manager-bot/docs/playbook.md`, секция `## Выученные правила`, строка = `- (ГГГГ-ММ-ДД) <текст>` (`suggest.py:668,687,736`). Замер: 2844 симв. всего, 9 правил, 1918 симв. в секции. Сбоку — `trainer_rules.json`, плоский dict `{нормализованный текст: "тренажёр"}` (`trainer.py:63-64,279`), 6 ключей. **Узла мозга под правила бота нет**, и два имени соблазняют ошибиться: `rules` — эт …обрезано
