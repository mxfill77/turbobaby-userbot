# Тело записи журнала — 2026-08-05 04:51 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **993 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-05 04:51:37 |
| символов в строке | 993 |
| порог | 600 |

---

DONE Dispatch 11:50: «связи нет» получило имя (831b69f). Обрыв больше не пишется как exec_error: провал от сети → 📡 «причина=network_outage · связи нет», карточка владельцу «исполнитель ни при чём», думатель его не чинит, вторая попытка не жжётся. Задача, простоявшая аварию, не зависшая: реапер вычитает окна обрыва из возраста и не судит непроверяемую полосу. Детектор сна молчит при нулевом сне: «детект пробуждения ПК» только на измеренном сне (>10с), сеть — своей строкой, залипание моста — тишина (было 305 строк/сутки, 303 с «сна 0с»); grace вотчдога теперь на сне и обрыве, а не на любом скачке (−71 подавленный тик ≈ 5,9 ч надзора в сутки). Свидетели связи бесплатные: мост/github/api, 0 новых запросов. Голдены test_pc_link_state.py — 38, фикстуры дословные из окна 04.08 18:02–19:18 UTC; красный до правки (exec_error/пробуждение/heartbeat_timeout), зелёный после; полный гейт 2991 OK (skipped=10), 229с. Разбор → docs/artifacts/2026-08-05-link-outage-named.md
