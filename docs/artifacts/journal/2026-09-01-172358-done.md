# Тело записи журнала — 2026-09-01 17:23 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **721 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-01 17:23:58 |
| символов в строке | 721 |
| порог | 600 |

---

DONE Dispatch 02.09: детерминизм очереди исходящих проверен без потери пакетов. Реестр отодвинут переименованием в tmp/queue_determinism_0902/ и возвращён той же sha256 ab130de3, 14 пакетов до и после. Три прогона с чистого состояния: 107 тестов OK, полный вывод байт в байт; после правки 114 OK, снова байт в байт. Ждут повтора НОЛЬ пакетов: 10 http_400 (наш формат), 3 accepted_no_answer (оплачено, id задачи нет), 1 no_credentials. Отрицательная проба нашла дефект: порча реестра не называлась и на пустом лотке очередь уходила в ноль молча - закрыто коммитом a0f6939 (read_state_why + state_why в отчёте + 7 регрессов). Разбор: docs/artifacts/2026-09-02-детерминизм-очереди-без-потери-пакетов.md
