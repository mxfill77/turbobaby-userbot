# Тело записи журнала — 2026-08-23 10:26 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1580 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-23 10:26:54 |
| символов в строке | 1580 |
| порог | 600 |

---

NOTE Dispatch 23.08 шаг1/ветка минимальной котировки: премисы ПРОВЕРЕНЫ пробой _scratch_minquote_0823/probe_minquote.py (только чтение, шим моста дословно как в живом голдене TestEndToEndMax2stix). Контраст сутки/пять дней снят четырьмя случаями. Сутки, юнит СВОБОДЕН: quote-блока НЕТ, проверяющий получает None, при этом фраза кода «свободен на эти даты» В ЗАПИСКЕ ЕСТЬ и чтение из записки даёт True — это и есть потеря. Пять дней, юнит СВОБОДЕН: quote-блок ЕСТЬ, проверяющий получает True. Сутки и пять дней при ЗАНЯТОМ юните: обе дороги дают None, fail-closed цел. Место потери названо дословно: suggest.py:5143 marker_mode = (kind == "ok" and not pct) — служебный quote-блок кладётся ТОЛЬКО на ветке ok (suggest.py:5170), а ветка min (suggest.py:3526-3528) печатает ту же фразу _client_price(q) прямо в текст записки и блока не выпускает; читатель suggest.py:7333 берёт j_line = _quote_block_from_note(note), поэтому на min получает пустоту, и она едет в проверяющего на suggest.py:7483. ЗАБОР ЧЕСТЕРТОНА ОТВЕЧЕН ИСТОРИЕЙ: значение не убирали, его НИКОГДА тут не было. git log -L по строке проверяющего даёт РОВНО ДВА коммита за всю историю: c895751 (рождение чека, шаг3/6 родителя 92) — строка родилась как avail=None ЛИТЕРАЛОМ, и 92dd754 (22.08) — литерал заменён значением из quote-блока. git log -L по строке-производителю j_line даёт ОДИН коммит — то же рождение c895751. Ни один коммит значения не удалял; ветка min пуста не потому, что кто-то убрал, а потому что выбранный 22.08 носитель на ней физически не существует. Кода не тронуто ни строкой.
