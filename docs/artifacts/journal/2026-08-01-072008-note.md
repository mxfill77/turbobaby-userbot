# Тело записи журнала — 2026-08-01 07:20 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-01 07:20:08 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #159 → done · Разбор закрыт фактами: механизм — два словаря имён без переводчика (мост знает только ключи реестра: 28 ключей, заглавных 0, префикса `KB` 0; рамка зовёт доки титулами Drive). 13 из 14 имён ведут в ЖИВОЙ док. KB_NORTH_STAR жив вне реестра. Одно правило закрывает 16 обращений из 27. Ничего не чинил, не коммитил, гейт не гонял; `.env` не открывал. ## МЕХАНИЗМ **Два словаря имён, переводчика между ними не было.** Мост понимает ТОЛЬКО ключи реестра (`index`, `infra`, `cc_userbot_log`); рамка Штаба, инструкции и код зовут доки ТИТУЛАМИ файлов Drive (`KB_MASTER`, `KB_INFRA`). Спрашивали в чужом словаре. Замер живого реестра сегодня (`--list-brain`): **28 ключей-доков + `folder_id`, с заглавной буквой — 0, с префиксом `KB` — 0, вне `[a-z0-9_]` — 0.** Ни один ключ не может совпасть с `KB_*`-титулом ни при каком регистре. **Где имя превращается в id — дословно:** - сторона моста (исходника в этом репо нет, судим по живому ответу): `{"ok":false,"error":"unknown_name","message":"Нет имени \"KB_ROADMAP_MASTER\" в манифесте…"}` — `docs/artifacts/2026-07-29-brain-registry-liveness.md:116`; - сторона ПК, вход в разрешение — `brain_writer.py:181` → `if (… r.get("error") == "unknown_name" and resolve and addr.get("name")):`; - само правило — `brain_writer.py:367-371` (`canon_name`: casefold → не-`[a-z0-9]` в `_` → долой ведущий `kb_`) и сопоставление с живым реестром `brain_writer.py:421-428`. **Промах живой, а не исторический:** `--probe --name KB_INFRA_UNIFY_FUTURE` сегодня → «ИМЯ НЕ РАЗРЕШ …обрезано
