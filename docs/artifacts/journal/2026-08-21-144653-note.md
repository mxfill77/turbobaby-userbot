# Тело записи журнала — 2026-08-21 14:46 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-21 14:46:53 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #12 → done · Разделение потерялось одной регуляркой в счётчике базы: `model_of()` разбирает имя юнита только до `CC`, а слово `NEW` стоит правее — оба поколения падают в ключ `XMAX 300`. Источник на диске поколения РАЗВОДИТ. Премиса «790/939 бат в сутки» неточна: это колонка B листа, цена суток — 593/704. Разделение измерено впервые: OLD 560 / NEW 641. **FACT: commit `3ee6c6b` в git log** — `docs/artifacts/2026-08-21-xmax-generations.md`, 487 строк, 1 файл. Кода не тронуто (`git status -- '*.py'` → только преждесущие `_scratch_*`, все untracked). Замер воспроизведён: повтор счётчика по сохранённому срезу дал слитую медиану **623 = ровно как в файле**, значит счёт верен. **Премиса 0:** артефакта `2026-08-21-xmax-generations.md` не существовало — работа выполнена. **1. Записей про XMAX 300 в правиле — три, в двух блоках.** `base.models`: **ОДНА** — `"model":"XMAX 300", "n_bookings":44, "base_thb_per_day":623, "p25_p75":[500,701]`, поля `generation` нет (0 из 13 строк). `low_season_caps.models`: **ДВЕ** — `generation "2020-2022"`/`"2023-"`, `sheet_model "YAMAHA XMAX300 2020-2022"`/`"YAMAHA XMAX 300 NEW 2023-"`, капы 8900/9900. Базы у капов нет. **Файл сам себе противоречит:** блок капов пишет «поколение брони различается словом NEW в имени байка», блок остатков — «имя байка поколения не несёт». **2. Источник найден, поколения ТАМ разведены.** `_scratch_basePrice_0816/rows.json` (494 588 Б, untracked, 1278/1243 — совпадает с `provenance` правила дословно). Поле `bike`: `XMAX 300CC NEW BLACK P …обрезано
