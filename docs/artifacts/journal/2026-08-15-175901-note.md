# Тело записи журнала — 2026-08-15 17:59 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-15 17:59:01 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #41 → done · Эталона нет: сигнал в диалогах есть (354 из 710 с датами клиента), запись есть (356 завершённых броней в окне корпуса), но связать их нечем — 0 однозначных сцепок из 710. Вердикт: **НЕ СУДИМО**, пригодных пар 0 при пороге 30. Только чтение, мутаций ноль, необезличенный корпус не открывался. **FACT:** commit `b3ebda2` в `git log`, файл `docs/artifacts/2026-08-15-dialog-intent-pairs.md` в `git ls-files`; две строки в мозге (журнал вырос 370467 → 371061 символов); живой GET `clients` вернул 1278 строк, срез на диске без ПДн (`name`/`contacts`/`note`/`booking_id` — 0 строк из 1278). **Защита от повтора:** файла-признака на диске не было — задача выполнялась впервые. **ШАГ 1 — потолок метода (356 из 1260, 28.3%).** Окно корпуса 2025-06-01…2026-05-31 (замер по самому корпусу). Завершённых броней 1260: в окне **356**, раньше окна **884**, позже **16**, без даты **4**. Край: 2/5/13 броней начинаются в пределах 7/14/30 суток после окна — их разговор мог попасть в корпус. Семь броней из десяти вне окна просто по датам, и это ДО потерь на WhatsApp. **ШАГ 2 — сцепка: 0 / 0 / 710.** Искал в данных, живым GET: | признак | база | корпус | исход | |---|---|---|---| | `@ник` | 632 строки из 1278, **403 разных** | сверка всех 403 → **0 совпадений** (и голой подстрокой тоже) | мёртв | | телефон | **644** хвоста по 9 цифр | **0**; `peer_phone` пуст у 710 из 710 | мёртв | | имя | 1277 из 1278 | 14 форм, но 5 живут в 4–24 диалогах (обычные слова); по одному диалогу дают 9 форм → **9 диалогов** | н …обрезано
