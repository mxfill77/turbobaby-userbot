# Тело записи журнала — 2026-09-07 15:33 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **3207 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-07 15:33:30 |
| символов в строке | 3207 |
| порог | 600 |

---

DONE Dispatch 22:45: класс отказа серверной полосы НАЗВАН, и он не один — их ДВА, коммит 474cb7c. Сервер только читался (8 ssh-заходов с ConnectTimeout=10/ServerAliveInterval=15/ServerAliveCountMax=4/BatchMode=yes), ни одной записи, ни одного рестарта. ЦЕНА ЕДИНИЦЫ замерена первым действием на 172: 1 GET моста (2.8с, все 5 строк failed lane=vps сразу) + 1 ssh-окно журнала = 2.8 тыс. символов на задачу; 3 единицы = 2.1% потолка захода, порог 0.7 не близко. ТРИ ЗАДАЧИ: 144 (from=Filipp-328, взята 06.09 23:06:26, закрыта 23:16:28, 600.16с, run_timeout), 159 (from=Filipp-328, 07.09 06:07:20 -> 06:12:00, 255.37с, exec_error exit=-9), 172 (from=Filipp-shtab, 07.09 09:24:53 -> 09:34:56, 600.07с, run_timeout). Коммитов сервера в окнах 0/0/0. РАБОТА 172 — ЕСТЬ: в рабочем дереве сервера незакоммиченное ровно в окне 09:27-09:34 — wa_kind.py и tests/test_wa_kind.py git-у неизвестны вовсе (новые файлы захода), wa_webhook.py и invariants_check.py изменены на 75 вставок/13 удалений, плюс запись журнала PLAN 09:28. Переделывать 172 вслепую нельзя. КЛАССЫ: А (172 и 144) — потолок выбран по ЯРЛЫКУ ОТПРАВИТЕЛЯ, а не по работе: orchestrator_daemon._task_timeout даёт 2700с только при from, оканчивающемся на -dev/-dec, иначе 600с; ящик Штаба (Filipp-shtab) и тема 328 под суффикс не попадают и получают быстрый потолок 10 минут на полноразмерные ТЗ; обе смерти ровно на потолке. Б (159) — OOM внутри cgroup юнита, а не таймаут: ядро назвало жертву дословно (Killed process claude.exe anon-rss 3869664kB, oom_memcg=orchestrator-daemon.service), MemoryMax=4 ГиБ, смерть на 255с при неизрасходованных 344с времени; строка получен сигнал 15 стоит на 54 мс РАНЬШЕ exit=-9 и читается как рестарт, но рестарт тут следствие OOM, а не причина. В (побочный, он и породил премису Штаба) — status_truth.fail_result собирает улики только из коммитов и реестра журнала, рабочее дерево не смотрит ни одной веткой, поэтому у 144 и 159 в очередь ушло ложное СЛЕДОВ РАБОТЫ не найдено, работа не начиналась — при том что файлы _recon_ci_0907 лежат на диске с mtime внутри обоих окон. Общей правки быть не может: таймаут не спасёт 159, память не спасёт 172 и 144. ПОРОГ ПРОТИВ МЕДИАНЫ: сшивка потолок x длительность по 3194 строкам ИСПОЛНЕНИЕ и 564 METRICS, сшито 564 из 564. Медиана законной завершённой работы того же демона под нерезающим потолком 2700с = 742.0с (N=388) против порога 600с — порог НИЖЕ медианы на 19 процентов, то есть гарантированная ложь. Прямая улика: под потолком 600с провалов 6 из 18 (33.3%), под 2700с — 27 из 434 (6.2%), разница 5.4 раза. Оговорка: у самого потолка 600с законных done всего 11 с медианой 5.0с — выборка отобрана выживанием, судить порог по ней нельзя. Для 159 порог памяти с медианой НЕ СВЕРЕН: METRICS память не пишет ни одним полем — честное НЕИЗВЕСТНО. Премисы Штаба: 159 и 144 подтверждены дословно; 172 не закрыта — неверно по букве (status=failed, COMPLETE 09:34:56), слова НЕ ЗАКРЫТА это первые слова текста result, а не статус; из Штаба сервер не читается — про полосу ПК не подтвердилось, все 8 заходов ответили. Ничего не чинилось: TASK_TIMEOUT, MemoryMax и детектор следов не тронуты. Временное — tmp/srvclass_0907 на ПК, на сервере не создано ничего.
