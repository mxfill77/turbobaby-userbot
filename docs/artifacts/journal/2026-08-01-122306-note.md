# Тело записи журнала — 2026-08-01 12:23 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-01 12:23:06 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #167 → done · Мост врал не о записи, а о доставке расписки. Механизм назван фактом, правка есть, гейт зелёный, запушено. ## ШАГ 1 — механизм (read-only) Три звена, каждое проверяемо: 1. **Тело POST-а Apps Script отдаёт вторым плечом.** Ответ собирается `ContentService.createTextOutput(...)` (`tmp/bridge_v75/Bridge.js:495-497`), но `/exec` на POST отвечает **302** на `script.googleusercontent.com/macros/echo`. `urlopen` идёт по редиректу **внутри себя** — падение второго плеча приходит **тем же исключением**, что и падение первого. Снаружи неразличимо. 2. **Мутация фиксируется ДО отправки расписки.** `writeDoc_` зовёт `brainTextWrite_(id, text)` и только **потом** собирает `{ok:true}` (`tmp/bridge_v75/ReadDocs.js:352-354`). Когда 404 родился на втором плече, док уже перезаписан. 3. **Флакость измерена раньше нас** — `docs/artifacts/2026-07-28-journal-write-fixes.md` §1, при совпавших адресах и токенах ПК/VPS. **Место, делавшее из этого ложь** — `brain_writer.apply()`: `except` вокруг POST был коротким замыканием, обратное чтение ниже **не запускалось вовсе**, а CLI печатал `НЕ ЗАПИСАНО.` — утверждение о факте, которого код не проверял. Именно эта строка и толкает на повтор. **Это класс, а не случай:** `create_plain` (создание **неидемпотентно**, id приходит только с распиской → слепой повтор кладёт второй файл), `register_doc`/`move_into_brain`/`unregister_doc` (`unregister` **односторонний**: «упал» читалось как «ключ на месте»), `cowork_log_append.py` (спулит по коду ответа — известный «л …обрезано
