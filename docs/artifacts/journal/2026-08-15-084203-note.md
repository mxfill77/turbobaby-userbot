# Тело записи журнала — 2026-08-15 08:42 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-15 08:42:03 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #11 → done · Собраны экзаменационные сценарии шагов «приход» и «бронь»: 13 штук (шаг 1 — 4, шаг 2 — 9), у каждого назван отрицательный тест наблюдения. Read-only: ничего не запускалось, кода не менялось, БД не открывалась. Найдены две ложно-зелёные ветки в коде. Артефакт сохранён, журнал записан. **FACT:** артефакт `docs/artifacts/2026-08-15-exam-scenarios-intake-booking.md` создан (untracked, коммита нет по запрету); в мозг легли две строки — `OK: записано в мозг, символов: 353325` (ARTIFACT) и `OK: записано в мозг, символов: 353882` (DONE). Правок кода 0, `git status` по трекнутым файлам не менялся. **Дословный вывод команд (решающие):** ``` --- A: ветка ложного зелёного --- 6963: mid = getattr(sent, "id", None) 6964: moderation_ipc.mark_intake(r["id"], "posted", posted_msg_id=mid) --- B: возрастной сторож intake --- suggest.py:0 moderation_bot.py:0 --- C: тесты intake --- 30 ``` ``` === posted_msg_id в тестах === test_intake_bridge.py:128: self.assertEqual(row["posted_msg_id"], 555) test_intake_bridge.py:378: self.assertEqual(row["posted_msg_id"], 9) ``` ``` === TTL/реапер для intake pending === ./moderation_ipc.py:182:def fetch_pending_intake(path=None): ./suggest.py:6939: rows = moderation_ipc.fetch_pending_intake() ./userbot_listen.py:224: await suggest.poll_and_post_intake(client) === возраст/stale у intake === (пусто) ``` **Сводка:** 1. **Ожидаемый результат назван проверяемо**: шаг 1 — 5 условий по `drafts` (`status`, `incoming` дословно, `card_msg_id` существующего сообщения, `d …обрезано
