# Тело записи журнала — 2026-08-01 12:20 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **849 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-01 12:20:51 |
| символов в строке | 849 |
| порог | 600 |

---

DONE Dispatch 19:25: ответ моста больше не факт записи (7a65fa3, запушен). Механизм задачи 166: Apps Script отдаёт тело POST-а вторым плечом (/exec -> 302 -> googleusercontent), падение этого плеча неотличимо от падения первого, а writeDoc_ фиксирует мутацию ДО сборки ok:true — потому 404 пришёл при ЛЁГШЕЙ записи. Класс, а не случай: create_plain (неидемпотентен) и админ-посты реестра судили так же. brain_writer.apply теперь ВСЕГДА читает назад и судит по чтению: не записано говорится только по прочитанному доку, легло при отказе моста → успех с receipt=lost, оба канала молчат → written=None и запрет слепого повтора. Идемпотентность append не ослаблена. Регресс TestReceiptIsNotTheFact: 5 errors + 1 failure на старом коде, 55/55 после; гейт полный 2752 OK. Разбор → docs/artifacts/2026-08-01-bridge-receipt-not-fact.md
