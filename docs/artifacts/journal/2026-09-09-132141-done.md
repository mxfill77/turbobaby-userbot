# Тело записи журнала — 2026-09-09 13:21 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2916 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-09 13:21:41 |
| символов в строке | 2916 |
| порог | 600 |

---

DONE Ящик 09.09 (задание 12-a-urok-istochnik-nabora.0909, метка УРОК-ИСТОЧНИК-НАБОРА-РАЗРЕЗ-ОТКАТ-0909): у урока появился ИСТОЧНИК НАБОРА — необязательный хвост формата, четвёртый разрез снятия и откат набора целиком. Коммиты 0afe886 (код и тесты) и 226b031 (артефакт). Премисы П1-П4 проверены живым кодом первым действием и ВСЕ ЧЕТЫРЕ верны: колонок было 8 и слова «источник» среди них 0, CUTS были три, перенос помечал набор текстом причины «перенос 05.09» и именем «владелец», сайдкар ключуется текстом правила (6 записей). Сделано: девятая колонка стои́т ПОСЛЕДНЕЙ, индексы восьми прежних не сдвинулись (IDX_STATE==7 в обеих ширинах), load знает обе шапки и обе ширины, лежащие строки не переписаны ни одной веткой; строка без хвоста отвечает третьим исходом «источник неизвестен» и не попадает ни в один разрез набора; CUT_SOURCE лёг в тот же кортеж CUTS, регулярка состояния теперь СОБИРАЕТСЯ из CUTS вместо литерала, у _matches появился явный хвост False вместо «всё, что не урок и не день — автор»; запись без источника отказывает словами, значения перечислены одним списком SOURCES (тренажёр, перенос_книги, экспорт_переписки), живых вызывающих двое и оба называют свой источник. ПОБАЙТНЫЙ ЗАМЕР на КОПИИ боевой базы: «то, что видит бот» после снятия набора совпало ПОБАЙТНО с тем, что было до его заливки (sha256 8a35f6ab8eb4db03), файл без строк набора совпал с файлом до заливки (a399e51cc63b5739), между «набор залит» и «набор снят» разошлась РОВНО одна колонка «состояние» и только в строках набора, строк было 16 стало 16. Боевой lesson_store.tsv не тронут ни байтом. Контрфакт: 4 поломки из 4 дают красное, эталон зелёный — четвёртая поймала слабость первой редакции замера и он переписан на сверку колонка за колонкой. Тесты: дерево 6954/9 падений против 6920/9 до правки — падений НЕ ПРИБАВИЛОСЬ, все девять вне уроков и тренажёра (test_suggest 6, test_nonparse_ratchet 2, test_client_contour 1); уроки+тренажёр 529 OK; вклад в храповик неразбора +0/+0 на всех трёх правленых файлах. Два чужих теста покраснели ЗАКОННО и переписаны, а не удалены: вчерашний замок «колонок восемь, разрезов три» и сверка «всё кроме последнего поля» (последнее поле теперь источник, а не состояние — та же мина, что в _replace_state). Границы названы: двери в Telegram у снятия набора нет и задание её не просило; «источник неизвестен» разрезом снятия не сделан; чужое значение источника читается, но снятием не достаётся; шапка боевого файла останется восьмиколоночной. Цена отката ЗАМЕРЕНА: код из HEAD на девятиколоночной строке видит 0 уроков и 4 битых (НЕРАЗБОР), на сегодняшней боевой таблице 9 и 0 — значит revert чист ровно до первой новой записи в боевую таблицу, а сегодня таких строк ноль. Дефект захода назван сам: у коммита 0afe886 первой строкой сообщения стоит одинокий @ (PowerShell-here-string через Bash), тело и метка целы, не переписывал из-за второго захода на общем дереве.
