# Тело записи журнала — 2026-08-15 15:04 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-15 15:04:51 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #35 → done · **Экзамен этапа 3, шаг «бронь»: различитель НАЙДЕН** — таблица `intake`, поле `status`. Строка рождается при СБОРКЕ (`status='draft'`), ДО отправки: выходов между сборкой и записью **0** против 7 у «прихода». Воронка 286→2 подтвердилась точно, но источник истины даёт 458→2. Замок на боевую базу устоял: `mode=ro`, размер неизменен, записей 0. **FACT:** - `FACT: commit 0e51533 в git log` (`git log --oneline -1` → артефакт `docs/artifacts/2026-08-15-booking-discriminator.md`) - `FACT:` две строки в мозг через `cowork_log_append.py` — «OK: записано в мозг» дважды, 363933 → 364525 символов (DONE = 592 симв., под `LINE_MAX` 600, авто-выноса не было) - `FACT:` база read-only — `size=1699840` неизменен во **всех 20+ замерах**, окна чтения согласованы (`sha ДО == sha ПОСЛЕ: True`), `ls moderation_ipc.db*` → один файл, `-wal/-shm` сняты живым писателем - `FACT:` вердикт гарда снят ЗАРАНЕЕ: `decide` и `decide_for_role` → `('defer','','')` в обеих формах пути **Вердикт: НАЙДЕН.** Причина — не удача, а **место записи**. У «прихода» строка рождается ПОСЛЕ продукта, за 7 `return None`, поэтому провал не оставляет ничего. У «брони» `save_intake_candidate` (`moderation_bot.py:235`) пишет строку **в момент сборки, до всякой отправки**, со статусом `'draft'`, и она не удаляется никогда (`sqlite_sequence = count = 2`, удалений 0). Выходов, способных оставить собранную карточку без строки, — **0**; единственный `return` между сборкой и записью (`:229`) — это `except` самой сборки, то есть состоян …обрезано
