# Тело записи журнала — 2026-09-08 06:11 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2029 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-08 06:11:21 |
| символов в строке | 2029 |
| порог | 600 |

---

DONE ПК 08.09 задание 01-y-granica-mesyaca.0908 (ящик Штаба): заход только читал код, живых вызовов расчёта и обращений к листу «Календарь бронирования» — ноль. Признак сделанности: артефакта с адресными словами не было (6 файлов за 08.09, совпадений 0) → работа с нуля. П1 — ВЕРНА для двери quote_price (pricing.py:82 — чистый проброс поля season без вычислений), но НЕВЕРНА как описание сегодняшнего пути цены: с переключения 20.08 сумму затирает price_source.reprice (врезка suggest.py:3564), а там коэффициент выбирают ДАТЫ, а не ячейка. П2 — ВЕРНА дословно: low_season=(season==1.0) считается от period_of(doc, start), цепочка price_source.py:339→350→359, ворота потолка :548. ВОПРОС 1: коэффициент попадает в расчёт в price_source.py:422 (применение :427) внутри цикла по каждым суткам; входы term_total названы поимённо, дат в них две ручки — start и days. ВОПРОС 2: на сроке 20.10 + 30 суток код срок ДЕЛИТ, но не на две части, а посуточно — P2 ОКТЯБРЬ 12 суток ×1.000 и P3 НОЯБРЬ 18 суток ×1.161 скутер / ×1.101 мото; ступень срока ОДНА и от полной длительности (корзина 30+, ×0.53); суточная ставка клиенту не называется вовсе, потому что их две (reprice :544). ВОПРОС 3 и главный ответ захода: потолок применится ОДИН раз и выбран по ОКТЯБРЮ, то есть по месяцу даты начала — делить срок по месяцам код умеет для КОЭФФИЦИЕНТА и НЕ умеет для ПОТОЛКА; у cap_for дат во входах нет вовсе, а _cap_applies (suggest.py:3576) — булев предикат с порогом 30 суток, умножения кепки на месяцы в коде ноль. Следствие названо: чем полнее разбивка вносит ноябрьскую надбавку, тем вероятнее сумма перерастёт потолок и будет стёрта ценой низкого сезона, право на который дал один октябрьский день старта. НЕИЗВЕСТНО: положение ручек PRICE_SOURCE_SEASON и PRICE_SPLIT_DAYS_OFF в боевом окружении (.env не читал, читал код, который их читает), cap_active живой котировки, ячейки B2/B9/B19. Бюджет: единица замерена на вопросе 1 ≈500 с, все три ≈800 с = 0.42 от рабочей доли 1890 с. Коммит ef9f00b, 1 файл, 335 строк.
