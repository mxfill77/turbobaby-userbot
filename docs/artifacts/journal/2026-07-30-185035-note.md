# Тело записи журнала — 2026-07-30 18:50 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-07-30 18:50:35 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #76 → done · Журнал стал индексом: строка длиннее **600** символов теперь кладёт тело в `docs/artifacts/journal/`, а в мозг уходит итог (рез по границе фразы) + путь + полная длина. Порог не назначен, а **измерен**: 81 живая строка уже правильного вида «итог + путь» даёт максимум **588** — 600 ближайшее круглое выше, поэтому вынос не режет ни одной образцовой записи. Коммит `0fb0348` отгружен, гейт 2452 теста зелёный. **Итог 5 строк** 1. **Замер (`journal_measure.py`, воспроизводим одной командой):** снимок живого `cowork_log` + `cowork_log_archive` через `brain_writer.read_text` — 2058 записей за 19.06–30.07, 880 209 символов. 78.2% записей ≤600 держат 25.5% объёма; остальные 21.8% — 74.5%. Журнал уже на три четверти состоял из тел. 2. **Обоснование порога:** опора — 81 запись вида «итог + путь» (`ARTIFACT …`), их `p50=177, p90=218, максимум=588`. `LINE_MAX=600` стоит выше максимума → формат сам себе не противоречит. Инвариант закреплён тестом `588 < LINE_MAX < 1000`. 3. **Механика:** `spill()` пишет тело в `docs/artifacts/journal/ГГГГ-ММ-ДД-ЧЧММСС-<тип>.md` в **исходном** виде (с переносами — `main()` держит `raw` отдельно от схлопнутого `msg`), возвращает голову + ссылку. Стоит **до** развилки «нет конфига», иначе отложенная простыня обошла бы порог через спул. FAIL-SAFE: файл не записался → строка уходит целиком (потерянная запись хуже длинной). Существующие записи не тронуты — `compose()` только добавляет. 4. **Проверено по факту, а не по сводке:** финальный `DONE` этой сессии сам ок …обрезано
