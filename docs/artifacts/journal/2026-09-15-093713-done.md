# Тело записи журнала — 2026-09-15 09:37 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2216 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-15 09:37:13 |
| символов в строке | 2216 |
| порог | 600 |

---

DONE ПК 15.09 62-i: величина процента скидки за срок снята в ПРИМЕРАХ — в собранном боевом промпте их НОЛЬ. Счётчик C того же замера 13.09: 9 → 0 (было 7 в примерах формата и 2 в примерах расчёта подаваемого FAQ); B лестницы 0 → 0, A запрета 1 → 1. Перебор 12288 веток: отказов 0, максимум C собственного текста промпта 0; ветка подобранных живых примеров 24 сборки — тоже 0 (15.09 показывала до 10). Примеры формата остались примерами ФОРМАТА: строение, поля, порядок, прайсовые числа и тон целы, ушла только величина — на её месте «N%», тем же словом, каким зовёт это место шапка блока ЦЕН ПО МОДЕЛЯМ. Два примера расчёта FAQ решены РЕЗОМ, а не оставлены: оба стоят под словами «Формат ответа, который ты используешь», то есть тот же класс. Правило расширено явно и двумя действиями: лестница (обещание) режется фрагментом целиком, одиночный процент (формат) теряет только величину; маска стоит на ОБОИХ входах текста в промпт — на подаче FAQ и на подаче примеров, второй нужен ради базы переписок, править которую полоса ПК не смеет. Первая редакция правила (окно ±80 плюс богатый словарь сроков) ОТКАЧЕНА ЗАМЕРОМ до коммита: она съедала «скидка категории 25%/15%» в живом доке, а прайса эта полоса не правит; окно и словарь взяты дословно у счётчика. Блок ЦЕНА свой живой процент СОХРАНЯЕТ — строка J Календаря по конкретным датам, отдельный замок. Премиса П2 ОПРОВЕРГНУТА: объявление примеров иллюстрацией в промпте ОДНО, а не два. Премиса П3 подтверждена и долг закрыт замером: test_moderation прогнан — 71 тест, 2 падения, оба НЕ наши (доказано снятием правки в памяти: падают и так; предмет — расхождение цены двери 939 и файла 662, заведено не этим заходом). Отрицательный тест на трёх местах по очереди: каждое даёт C=0 при правке и C=1 без неё, ответ разный, возврат посимвольный. Тесты: test_suggest 718/1 (известное падение по среде, в счёт не взято), test_rules_decided 7/0, test_moderation 71/2. Док мозга faq и локальный фолбэк не правлены ни байтом (sha256 фолбэка совпал), наружу не ушло ни одного сообщения. Коммиты ac866d6 (правка, 12 новых замков), bb5a5cd (артефакт), e2900da (хеш артефакта в сам артефакт). Артефакт: docs/artifacts/2026-09-15-ПРИМЕРЫ-procenty-nol-1509.md
