# Тело записи журнала — 2026-08-12 00:19 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **920 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-12 00:19:36 |
| символов в строке | 920 |
| порог | 600 |

---

DONE Dispatch 14:25: сторож pc_orchestrator судит ПРОДУКТ (оборот poll_once), а не живой PID — коммит a8d0b39, запушен. Живой PID больше не вердикт: процесса нет → подъём как прежде, процесс жив без оборота → карточка владельцу БЕЗ подъёма, проверить нечем → «неизвестно». Пороги измерены своей полосой: тишина 1800с (1005 пауз витка, MAX законной 605с, выше потолка вызова моста ≈28 мин), занятость 4500с (193 окна прогона, MAX 2700с). Реплей живым кодом по 856 реальным тикам: ложных карточек 0, подъёмов живого процесса 0; из восьми зависаний поймал бы 0 — честный ноль, там ломалась строка очереди, а не демон. Тесты 3312, красных 2 — оба test_nonparse_ratchet, красные и на HEAD до правки (934 точки против 933 с правкой). Живой факт: сторож уже тикает новым критерием — «продукту 2021с, но идёт объявленный заход (1914с из 4500с)». Разбор: docs/artifacts/2026-08-12-watchdog-judges-product.md
