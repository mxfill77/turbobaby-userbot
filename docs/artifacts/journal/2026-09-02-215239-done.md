# Тело записи журнала — 2026-09-02 21:52 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **3300 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-02 21:52:39 |
| символов в строке | 3300 |
| порог | 600 |

---

DONE Dispatch 04:5x: судья закрытия больше не зачитывает ЧУЖОЙ файл по адресу. Признак сделанности: по адресу за 03.09 файла не было — работали с нуля. ПРЕМИСА ОПРОВЕРГНУТА НАПОЛОВИНУ: вердикт задачи 26 действительно доказан по адресу артефакта про карточку гарда, но ряд #26 полосы ПК И БЫЛ задачей про карточку гарда (расписка pc-2026-09-02-26.json, черновик task26-16068-1788381036297-26.md), а его артефакт родился 04:08:27 ВНУТРИ окна захода 03:30:36→04:14:43 — файл заходу не чужой; коммитов после db97ebc не «нет», лежит 53e5fa1. Откуда взялось «задача 26 = доставка»: номер задачи полоса ПЕРЕИСПОЛЬЗУЕТ — за 02–03.09 из 38 номеров повторились 2 (id=4, id=5), а реестр вердиктов и пакеты судьи ключуются номером. ГИПОТЕЗА ШТАБА «слова адреса слишком свободные» ОПРОВЕРГНУТА ЗАМЕРОМ: 20 адресов × 500 файлов = 10 000 пар, чужих совпадений 0, у каждой задачи ровно один ответ и он свой; обратное ужесточение запрещено числом — 11 из 20 живых адресов несут слова ТОЛЬКО ИМЕНЕМ файла, и правило «слова в теле» перекрасило бы 11 честных закрытий (на это поставлен замок-тест). НАСТОЯЩИЙ МЕХАНИЗМ лежит не в словах, а в адаптере: (1) отвечающих словам файлов, изменённых за заход, могло быть несколько, и брался ПЕРВЫЙ ПО АЛФАВИТУ — тронутый заходом чужой файл уходил в бандл вместо продукта, и V0 такого не ловит ничем, ему подают один путь; (2) в режиме measured любое изменение sha означало «родилось за заход», поэтому переименованная или скопированная сюда чужая работа считалась продуктом — файл появился, а содержимое старше начала захода. ПРАВКА (06fe5d9): демон снимает ДО захода ВТОРОЙ, широкий снимок всей адресной папки (baseline.prior = sha256→путь); два отвечающих файла и больше → «неизвестно» с перечнем вместо выбора; sha изменённого кандидата сверяется с prior → совпал → «неизвестно» с именем двойника. Часов не прибавлено ни строкой — «старше начала захода» доказано тем, что содержимое ЛЕЖАЛО тут до захода; инвариант чистоты зелёный. Роль V0 не урезана: труп прежнего прогона по-прежнему ловит он через changed_scope_mismatch. ЦЕНА КЛАССА за 02–03.09: судья судил 37 закрытий, с названным адресом 19, доказано 19, зачтено ЧУЖИМ файлом 0 — честный ноль. ЦЕНА ПРАВКИ: обе новые ветки перекрасили бы 0 из 20 живых закрытий, опорный снимок подорожал на 67 мс (505 файлов, 15 МБ). ОТРИЦАТЕЛЬНЫХ ШЕСТЬ: чужой файл с частичным совпадением слов → не доказано (text_condition_failed); по адресу пусто → не доказано с причиной; файл появился, но старше начала захода → не доказано с именем двойника; два отвечающих файла за заход → не доказано; плюс два положительных контроля (свой файл отвечает; слова только в имени). ТРИ ЧИСЛА: test_done_judge_pc 43·43·43 одной командой, OK трижды; смежные test_contour_digest 69 OK, TestStageCJudgeWiring 9 OK. Строгость судьи НЕ менялась: DONE_JUDGE_PC не переключался ни разу, правило «нет адреса — нет зелёного» не тронуто, гард/карточки/ворота/клиентский контур/боевая очередь не тронуты, реестр задним числом не переписан. Артефакт с разделом «вопрос внешним каналам» (что делает признак завершения неподделываемым, когда исполнитель и судья живут в одном дереве) → docs/artifacts/2026-09-03-судья-зачёл-чужой-артефакт-механизм-и-число.md. Коммиты 06fe5d9 и bed6762, запушены (073cee6..bed6762). Откат: DONE_JUDGE_PC=off.
