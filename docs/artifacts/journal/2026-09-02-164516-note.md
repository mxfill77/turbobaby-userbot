# Тело записи журнала — 2026-09-02 16:45 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-02 16:45:16 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #13 → done · [V0 судит done: сделано · V0: PROVEN / all_gates_passed по адресу «docs/artifacts/2026-09-02-доставка-правок-считается-замыканием-импортов.md»] Пометка «пора перезапуститься» теперь рождается из вычисленного замыкания импортов, а не из рукописной карты. Премиса подтверждена замером: замыкание pc_agent — 5 файлов, карта знает 2. Заведено О6 в слое ожиданий: состояние, а не событие — говорится каждый прогон, пока живо. Ничего не перезапускалось. **FACT:** коммиты `6f4deb4` (код), `b314a7d` (артефакт), `c97a30d` (тело журнала) в `git log`, запушены в origin/main. Тесты 1177 OK трижды одной командой. ## Что сделано **П.1 — премиса верна, и вот числа.** `pc_orchestrator._dep_files` (:4091) считает своё замыкание через `client_contour.closure(..., cut=())` — 59 файлов. За агентом и детьми следит `_FILE_PROCESS_RULES` (:5781), 13 лямбд. Замер `client_contour.closure` от каждой входной точки: | процесс | замыкание | карта знает | не знает | |---|---:|---:|---| | **pc_agent** | **5** | **2** | `io_utf8.py`, `proc_identity.py`, `selfupdate_gate.py` | | userbot | 63 | 8 | 55 | | moderation_bot | 64 | 9 | 55 | | rc_supervisor | 4 | 0 правил вовсе | — | Коммит только в `io_utf8.py` не помечал агента никогда — `_procs_for_file` возвращал пустое множество. **П.2** — `_agent_hit()` решает вычислением; карта оставлена **запасной** (граф не построился → решает она); расхождение карты и вычисления пишется строкой лога поимённо. **П.3** — О6: вердикт пересчитывается **каждый прогон** наблюдателя …обрезано
