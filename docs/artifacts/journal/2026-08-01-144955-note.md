# Тело записи журнала — 2026-08-01 14:49 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-01 14:49:55 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #171 → done · Сводка: ревизор больше не выбрасывает находки — недоставленное уходит в спул на диске, а метка прогона («докуда дошли») двигается только по факту доставки. Замер по живому журналу: 4 отсрочки за всё время, 19 находок (2 task + 17 owner), текст не сохранён нигде — восстанавливать нечего. Гейт 2777 OK, коммиты 6d41c37 + 00c41dc запушены, журнал записан и прочитан назад. ## FACT-отчёт **Корень (по коду, а не по рапорту).** `_revizor_route` держал находки только в кадре (`task_f`/`owner_f`); на `_loc_fetch_items() is None` писал в журнал «отложены до след. прогона» и возвращался. `maybe_revizor` ставил метку **всегда** (дословный комментарий: «метку ставим ВСЕГДА»), поэтому следующий прогон брал `since` = эту метку и те же окна в отбор не попадали. Две лжи в одном месте: «отложены» без хранилища = выброшены; «отработал» при недоставленном результате — класс `7a65fa3`. **Числа (ответ на вопрос владельца).** Считаны **чтением доков** `cowork_log` и `cowork_log_archive` через `brain_writer` (read-only, `tmp_revizor_spool_20260801_s1/count_deferrals.py`), не по ответу моста и не по локальным дампам: | когда | задач | owner | источник | |---|---|---|---| | 23.07 ~23:24 (окно 8725114073) | 0 | 3 | archive | | 31.07 ~19:41 | 2 | 0 | archive | | 01.08 07:05 UTC (окно 929753577) | 0 | 4 | cowork_log | | 01.08 13:07 UTC (окна 929753577, 8629287224) | 0 | 10 | cowork_log | | **итого** | **2** | **17** | **4 отсрочки, 19 находок** | Второй источник сходится: в `pc_orchestrator.log` (вся исто …обрезано
