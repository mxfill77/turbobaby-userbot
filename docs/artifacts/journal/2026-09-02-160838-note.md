# Тело записи журнала — 2026-09-02 16:08 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-02 16:08:38 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #12 → done · [V0 судит done: сделано · V0: PROVEN / all_gates_passed по адресу «docs/artifacts/2026-09-02-какие-процессы-крутят-старый-код-разведка.md»] Разведка закрыта: 5 живых процессов ПК, 4 разных механизма доставки правки, премиса подтверждена посекундно. Прибор для расхождения «диск новее памяти» в репозитории есть ровно один и покрывает только 2 из 5 процессов. Ничего не чинилось, ни один процесс не тронут. **FACT:** коммит **`605310b`** «Разведка: какие живые процессы ПК крутят старый код и чем это увидеть» в `git log`, запушен (`075d70c..605310b`); тело журнала — `73c025e`. Артефакт: `docs/artifacts/2026-09-02-какие-процессы-крутят-старый-код-разведка.md`. **Признак сделанности.** Первым действием открыт адрес: в `docs/artifacts` за 02.09 лежал 31 файл, ни одного со словами задания → писал с нуля. **1. Премиса — верна, вот её секунды.** 22:25:34 коммит `a209241` кладёт в `pc_agent.py` ветку `zayavka:(yes|no):(\d+)` → 22:35:30 демон видит правку → 22:35:58 обновляет **себя** за 28 с и пишет пометку про агента **один раз** (`pc_orchestrator.log:10843`) → 22:44:23/37/53 три тапа владельца дают `action=None ok=False` (`pc_agent.log:4190-4194`) → 22:56:03 новый агент PID 11224 забирает **стухший** лок PID 7092 (старт по телу улики — 31.08 19:38:10). **2. Процессы и доставка.** Демон 26064 — самообновление по отпечатку import-замыкания (59 файлов, 9 раз за сутки, задержка 28 с). Агент 11224 — **только рука** («обновись» из 205 либо подъём Планировщиком при смерти). userbot 13284 / мод …обрезано
