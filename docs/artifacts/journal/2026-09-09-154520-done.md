# Тело записи журнала — 2026-09-09 15:45 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2030 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-09 15:45:20 |
| символов в строке | 2030 |
| порог | 600 |

---

DONE Dispatch 22:44: заход 241, задание ящика Штаба 16-a-pravo-vladeltsa-dveri.0909 — ПРЕДПОЛЁТ ПРАВА ВЛАДЕЛЬЦА У ДВУХ ДВЕРЕЙ УРОКОВ (включение урока и снятие набора). Заход НИЧЕГО НЕ ЧИНИЛ: ни строки кода, ни символа настройки прав. Ответ: ПРИМЕТ обе двери. Живой список прав НЕ пуст — 5 элементов (значение нигде не печаталось), значит fail-closed не срабатывает; право пропускает 5 из 5 элементов и 2 из 2 имён, которыми владелец уже действует в живом следе модерации (moderation_bot.log, 4 строки авторства). Обе двери на этом праве: promoted и batch_withdrawn; сквозным путём тем же CLI-субпроцессом, какой собирает pc_agent._lesson_cli, — коды 0/0/0 (включение, снятие, возврат), постороннее имя — код 1 и отказ словами. Премиса П2 поправлена фактом: вчерашние пять отказов давал НЕ пустой список, а чужое имя — ветка пустого списка не бралась вовсе. Список читается suggest.py:93 (_csv_set, разбор :86-89), право судит moderation_core.py:530-549, членство suggest.py:1875-1879, гейт обеих дверей trainer.py:1420-1422 (вызовы :1809 и :1959); окружение наполняют load_dotenv в moderation_bot.py:29 и pc_agent.py:100, а порождённая агентом дверь наследует его без env=. Регистр и собака (одна и две) право не различает — 5 из 5 форм ПРОПУСК; пробел по краям напрямую ОТКАЗ, но дверь его снимает (trainer._actor_for). ДЕФЕКТ НАЗВАН, НЕ ПРАВЛЕН: текст отказа по праву («Нет прав на перевод уроков…» / «Нет прав — набор … НЕ снят…») не объясняет человеку ПРИЧИНУ и не говорит, что ничего не изменено; формулировка предложена в артефакте. Тесты: 441 (15 модулей test_lesson_*) OK, 133 (test_moderation, test_pc_agent) OK, из них 81 прямо про право дверей. Боевая таблица уроков только читалась, sha256 a399e51c… до и после совпал; наружу не ушло ни одного сообщения. Вспомогательная проба про источник ключа остановлена гардом как красный класс env — обход не искался. Временное место: tmp/prava_dveri_0909/. Артефакт: docs/artifacts/2026-09-09-ПРАВО-ВЛАДЕЛЬЦА-ДВЕРИ-УРОКОВ-ПРЕДПОЛЁТ-0909.md, коммит acca18c.
