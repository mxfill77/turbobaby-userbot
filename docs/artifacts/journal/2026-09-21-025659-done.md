# Тело записи журнала — 2026-09-21 02:56 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1660 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-21 02:56:59 |
| символов в строке | 1660 |
| порог | 600 |

---

DONE Dispatch 69n PRICHINAPOLEM 2109 (коммит c231a1da): перепись 7 путей сообщения о провале и словаря поля причины → docs/artifacts/2026-09-21-69n-PRICHINAPOLEM-2109.md. ГЛАВНОЕ: пять коротких показов провала человеку (карточка devbot, _human:3074, слепок queue_state_pc:340, витрина vitrina_pc_run:401, журнал _cowork:4544) берут ПЕРВУЮ СТРОКУ поля result, а её с 04.09 занимает шапка close_msg_pc со словами СПРАШИВАЛИ + ЦЕЛЬ задания. Механизм: FAIL_REASONS[exec_error] - единственный код БЕЗ маркера (:1465), NO_HEAL_PREFIXES (:6064) держит только маркеры а не код, close_msg_pc.prepend (:744-756) ставит шапку нулевой строкой. Замок 04.09 защищает гейты самопочинки, а не читателя-человека. ЧИСЛА окна 14-21.09: провалов 15 - exec_error 6, unbacked_red 4, heartbeat_timeout 2, approval_timeout 1, вердикт судьи done_judge_pc 2 (у них строки FAIL причина= нет вовсе - второй словарь). Цель вместо причины назвали 12 из 15, а по штатной дороге process_new - 12 из 12; три правильных заголовка пришли из веток с рукописной фразой, а не из поля. П3: лимит от поломки НЕ различаем - usage limit / rate limit / 429 в коде демона 0 совпадений, stderr думателя выброшен целиком (:5909), у ветки rc!=0 своей строки лога нет; причина установима по записям ПК у 0 из 6 exec_error, думатель падал exit=1 в 4 из 6. ПОПРАВКА К ПРЕМИСЕ: личка о провале снята 05.08.2026, за окно таких отправок 0 - чинить надо не личку. ПРАВИЛО: причина это ПОЛЕ а не первая строка, разбор берётся один (shtab_box_signals.reason_class, единственное место где уже правильно), применяется в 5 местах. Кода не правлено ни строки, владельцу не ушло ни одного сообщения.
