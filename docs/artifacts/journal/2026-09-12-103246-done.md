# Тело записи журнала — 2026-09-12 10:32 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2079 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-12 10:32:46 |
| символов в строке | 2079 |
| порог | 600 |

---

DONE Dispatch 17:32: задание 52-d — замок свежести ловца держит теперь ТОТ факт, ради которого заведён: «живой pc_agent разбирает КАЖДЫЙ callback_data этой карточки». exam_show.py убран из сравнения по времени (замер 51-c: агент его не импортирует вовсе, тап спавнит дверь отдельным процессом pc_agent.py:1077 — ребёнок всегда свежий), расхождение кнопок ловится СОДЕРЖИМЫМ: card_callbacks() отдаёт колбэки живого снимка, agent_parse() вынимает EXAM_CB_RE из исходника агента текстом без import. pc_agent.py оставлен в сравнении по времени как УСЛОВИЕ законности сверки — разбор читается с диска, а разбирает тап код в памяти; порядок жёсткий. Граница названа вслух: правка exam_show вне markup, живость процесса сейчас, второй слой exam_args, сохранённый mtime. Три исхода числами: НЕТ по содержимому — прежний разбор не принимает 5 из 6 колбэков (exam:h1/h2/h3/own/go); НЕТ по времени — pc_agent.py новее старта на 1000 с; ДА на живом — pid 13740, 6 из 6 разобраны. Набор test_exam_show 114 OK (8 в классе замка, среди них контракт двух ЖИВЫХ файлов: markup при HINTS_MAX против EXAM_CB_RE живого pc_agent.py), соседи test_pc_agent 62 OK и test_trainer_run 143 OK, храповик неразбора 0 новых слепых точек. Коммиты adaf906 + 35fb4ae ДО показа, артефакт e0aaf39. Кейс 1 из слота case-1@37c290f.json показан: подсказок 3, примеров 3, фигурных скобок 0, кнопок 6 в 3 рядах (Верно / 1-2-3 тумблеры / своё + Применить), message_id=1923 в чат -5193185299, отправок за заход РОВНО 1. Живое доказательство снятия класса: exam_show.py правлен ПОЗЖЕ старта агента, а замок отвечает ДА — прежним кодом это был бы третий отказ подряд. Кнопки не нажимал и не имитировал, pc_agent.py не правлен ни строкой, процессы не тронуты, голова 0 кругов. Остатки названы: заголовок adaf906 несёт лишний символ @ (here-string PowerShell в Bash; тело цело, шапку общей ветки при живом соседе не переписывал), docstring exam_reshow.py:6-8 всё ещё пишет прежний состав замыкания — второй боевой файл заданием запрещён. Артефакт → docs/artifacts/2026-09-12-ЗАМОК-po-sushchestvu-1209.md
