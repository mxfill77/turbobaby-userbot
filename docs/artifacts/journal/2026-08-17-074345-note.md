# Тело записи журнала — 2026-08-17 07:43 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1570 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-17 07:43:45 |
| символов в строке | 1570 |
| порог | 600 |

---

NOTE Orchestrator: задача #3 → done · Докорм линию НЕ поднял: на честном знаменателе модель 16.7→0.0, даты 13.3→0.0, «+6.7» по цене — переезд одного попадания между парами. Вход вырос вдвое (медиана 27 реплик против 7, доля речи 43.4→94.4%). 26 пар из 26 прошли, sha256 правил совпали до и после. **FACT:** commit `b52be53` в git log (артефакт) + `df162ee` (тело журнала); `docs/artifacts/2026-08-17-regression-fullinput.md` на диске; строка легла в мозг («OK: записано в мозг, символов: 403049»); 26 записей в `_scratch_fullinput_0817/drafts_full.jsonl`. ## Замок повтора Файла-признака на диске **не было** — заход законный. Опора `2026-08-17-harness-fairness.md` (`ad0443a`) на месте. В каталоге лежали заготовки прошлого захода и **10 уже прогнанных пар** (полным входом, 13:57–14:03) — прогон я **продолжил**, а не начал заново: прогонщик дописывает после каждой пары и пропускает лежащие. ## Прогон — синхронно, в этом ходу Двумя кусками по 480с внутреннего стопа (потолок одного вызова — 600с): 10 → 17 → **26 из 26**. Медиана 46.9с на пару, сумма 1337с. ## Замки до прогона - sha256 **ДО**: `88e73178` / `bee2d75f` — оба совпали с опорными. Копия обоих файлов — в `rules_backup_before/`. - **Отрицательный тест пройден**: дальняя подмена модели 4 промаха из 4, ближняя 4 из 4, сдвиг брони 3 из 3, ложных попаданий 0. - **Замок доверия**: тот же код воспроизвёл все опубликованные числа 17.08 (линия 26, знаменатель А из 15 пар списком, уклонения 36, промахи 71/65). Разница — это разница подачи, а не двух счётчиков. ## Числа **Ре …обрезано
