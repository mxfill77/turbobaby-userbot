# Тело записи журнала — 2026-08-18 22:11 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-18 22:11:05 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #20 → done · Разведка окончена: карточка рождается в одном месте, боевых входов три, два стоят подряд в одном обороте демона (`:8636` реконсиляция → `:8637` self-update). Дедуп есть и промахивается только на длине хеша — 9 символов против 7. Парных карточек за окно — 6. **FACT:** read-only. Живые доказательства: `pc_orchestrator.log` (24 строки ворот, 17 боевых), `dispatch_notify.log` (17 доставок — сверка сошлась), `pc_agent.log` (PID 13688 / 13736). Артефакт создан: `docs/artifacts/2026-08-19-gate-double-card.md`. Строка ARTIFACT в мозг легла («OK: записано в мозг, символов: 458119»). Кода не правил, ворот не трогал, процессов не трогал. **Признак сделанности:** `docs/artifacts/2026-08-19-gate-double-card.md` отсутствовал — задача не была отвечена ранее, разведка проведена заново. **1. Мест, рождающих карточку — одно.** `client_contour.card_text` (`client_contour.py:464`), единственный вызов — `pc_orchestrator.py:5740` внутри `_client_block` (`:5709`). Боевых точек входа три: `:5852` «авто-обновление после задачи» (0 срабатываний), `:5966` «после self-update» (6), `:6233` «на новый коммит» (11). Плюс 7 строк проб с `where`=«живая проверка ворот» и `PID [99999]` — не боевой код. **2. Оба пути дословно.** `main_loop:8636` → `reconcile_children_tick:6233` подаёт `short = head[:9]`; `main_loop:8637` → `maybe_self_update:3985` → `_selfupdate_restart_children:5966` подаёт `new_commit = _head_commit()` = `git rev-parse --short HEAD` (7 симв.). Одновременность не случайна: `suggest.py`, `price_ …обрезано
