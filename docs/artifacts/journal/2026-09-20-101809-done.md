# Тело записи журнала — 2026-09-20 10:18 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **3429 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-20 10:18:09 |
| символов в строке | 3429 |
| порог | 600 |

---

DONE Dispatch 17:30: заход 68w VESIPOROG 2009 — решение с доказательством по отбору находок внешнего контура, коммит c6794bd4. Вес разложен по адресу: слагаемых ДВА (STOP_WEIGHT=2 review_audit.py:88, AGREEMENT_WEIGHT=2 :94) при пороге HIGH_MIN=2 (:95), плюс НЕ слагаемое, а жёсткие ворота премисы (:159,173); максимум по построению 4, при одном отвечающем канале ФАКТОМ 2 — замер 78 заявок позже 04.09 (последние сутки с manus), у всех 78 источников ровно один, веса на отрезке {0:56, 2:22}. Премиса Штаба подтвердилась в слове «мёртв» и ОПРОВЕРГНУТА в причине: код каналов не смотрит вовсе (count=len(claim['sources']), :165), один канал давал два и более источника 14 раз, до шести у codex (c9b39939842c), и a7fbb33479b4 от 01.09 — когда manus в реестре нет вовсе; значит мёртво по ТЕМПУ, а не по построению, и формулой это не чинится. Порог при этом ДОСТИЖИМ — 10 из 78 поздних заявок высокие одним стоп-видом (12.8%), замка с недостижимым условием пропуска на полосе НЕТ; но вес выродился в булево «стоп-вид И живая премиса»: на отрезке ровно два значения веса, шкалы для rank() больше не существует. Поведение при отказе названо молчаливым ПРЯМО: ряд очереди needs_approval есть у 243 из 243 заявок включая все 183 снятых (review_intake_run.py:428), но живёт он APPROVAL_TTL=46 мин (пол 2760с, pc_orchestrator.py:183-185), а в теме Аудит текстом названы лишь 25 из 183 — те, что попали в тройку сводки СВОИХ суток; 158 снятых (86.3%) не названы ни одной строкой текста и ни одним путём никогда, backlog не пересматривается ни одной веткой. п.4 пересчитан на КОПИЯХ реестров (tmp/ves_otbora/, боевые review_*_state.json не тронуты ни одной пишущей командой; сверка моей арифметики с живой review_audit.weight() — 0 расхождений из 243): убрать ровно одно недостижимое слагаемое даёт на одноканальном отрезке 10 → 10, прирост НОЛЬ — мёртвое слагаемое никого не держит, держит порог; на полном корпусе 243 то же действие даёт 60 → 38, МИНУС 22 (там согласие было честно заработанным: 44 заявки с 2+ источниками, 30 двухканальных); единственная правка, что пропускает — снятие ворот премисы, 60 → 95 (+35), заход её НЕ предлагает. Два предмета на прочитанной выборке 20: про наш КОД — 7, про форму нашего ОТЧЁТА — 13 (65%); годного разделителя в данных НЕТ ни одного — kind поднимается НАПИСАНИЕМ СЛОВА (kind_of берёт слово канала раньше номера пункта, review_intake.py:206-213) и числом не делит, число источников тоже написанием (склейка по Жаккару значимых слов, MERGE_JACCARD=0.145), .py в тексте — 2 из 7 полноты; единственное СОБЫТИЙНОЕ поле (проба премисы) стоит воротами, а не слагаемым, и делит лишь отрицательно (7/7 кодовых alive, но alive и у 6 отчётных). Дороже всего: все 5 из 5 стоп-находок выборки — про форму отчёта, про код ни одной, то есть единственная живая ручка поднимает наверх ненужный предмет. Цена обеих сторон: снимает 183 из 243 = 75.3%, из них 19 высокие по сегодняшнему весу и не показаны ни разу; экономит 183 сообщения и режет поток с 20.3 заявки/сут (пик 03.09 — 102 за сутки) до ровно 3.0 сообщения/сут, в 6.8 раза. Честное НЕИЗВЕСТНО: реестр не хранит вес на момент решения, отсюда 19 показанных сегодня не высоких и 19 снятых сегодня высоких — причина не установлена, и это ограничение моего же п.4; судьба 183 рядов очереди не читалась — на сервер заход не ходил вовсе. Кода отбора не тронуто ни строкой, порогов и весов не менял, в реестры не писано, наружу ни пакета.
