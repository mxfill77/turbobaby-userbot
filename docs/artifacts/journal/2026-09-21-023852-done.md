# Тело записи журнала — 2026-09-21 02:38 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1443 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-21 02:38:52 |
| символов в строке | 1443 |
| порог | 600 |

---

DONE Dispatch 69m ZAPROSOTVET 2109 (c9b7ad8e): таблица полей канала manus сдана. ШЛЁМ 3 поля и только их — prompt (части ≤2400), taskId у досылок, agentProfile (пуст у всех трёх дверей); модели и режима не шлём нигде, поля model в запросе нет ни строкой. ВОЗВРАЩАЕТСЯ 9 полей GET-тела, из них model НЕ ЧИТАЕТ НИ ОДНА строка кода и не сохраняется нигде; в git (карточка лотка) не уезжает ни одно поле канала, сырое тело живёт только в tmp/review_send/manus_protocol.json до следующего захода, а на УСПЕШНОМ заходе подменяется текстом ответа (review_send_run.py:1143) — разбирать чужое молчание нечем, починка начинается с сохранения. КОРПУС: граница молчания после 03.09 (не 01.09, как считалось — карточек лотка 127: половина A 01-03.09 даёт 40 ok из 57, половина B 04-21.09 — 0 из 70 при 48 channel_idle). Тел на диске 5: A — model=manus-1.6-agent (1 из 1, ассистент говорил 2 раза), B — manus-1.6-adaptive (4 из 4, ассистента 0). Набор полей НЕ менялся: ни одно не появилось и не исчезло, значение сменило РОВНО ОДНО — model. CODEX: модель и режим задаём мы (-m gpt-5.6-terra :234, --sandbox read-only), и 20.09 это подняло канал с нуля до 17 ответов; у manus та же ручка пуста и заполнить нечем. ВЫВОД: строки, задающей модель manus, нет — выбирает их сторона; премиса Штаба верна в первой половине (agent→adaptive при неизменном запросе с 01.09), причинность НЕ доказана. Кода не тронуто, в API не ходили, кредитов 0.
