# Тело записи журнала — 2026-09-11 08:36 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1812 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-11 08:36:50 |
| символов в строке | 1812 |
| порог | 600 |

---

DONE Dispatch 15:37: задание 30-a «ряд не личность» — перепись полосы ПК, кода не правил. ОСМОТРЕНО 59 мест, номер подменяет личность в 45 (номер ряда 20 · PID 8 · счётчики 17), закрыто или обезврежено 14. П1 ПОДТВЕРЖДЕНА живыми файлами: vitrina_pc_run.py:328 берёт час прихода Штаба из pc_orchestrator.task_started.json ПО НОМЕРУ РЯДА — claims[4] = 04:40:17 UTC (взятие 29-a), а closed[4] слепка = ключ 23-a; открытая половина слепка поля at не несёт вовсе, потому при пустом claims закрытая чужая строка побеждает живую (vitrina_pc_run.py:338). Дефект жив сейчас: номер 5 одновременно в open (30-a) и в closed (24-a). П2 подтверждена по факту двух показов, но причина ДРУГАЯ: счётчик один (contour_digest.series), расходятся правила показа неизвестности — сводка при протухшем слепке печатает «серия 0/30» вместо «неизвестно» (contour_digest.py:1035, report.get(series) or {}), витрина гейта свежести не имеет вовсе (vitrina_pc_run.py:608-610), хотя её собственный текст его обещает (vitrina_pc.py:785); предел источника 3600 с. Сам источник истины болен тем же: реестр судьи плоский {номер: вердикт} без дня, живой замер 157 ключей от 1 до 245. П3 принята с чужого замера, своей проверки нет. ПРАВИЛО: переиспользуемый номер — адрес, а не имя; рядом с ним обязано стоять неповторяемое имя события (ключ задания, образ плюс момент старта, день), а показ без имени говорит «неизвестно» словом, а не нулём. Артефакт docs/artifacts/2026-09-11-ПЕРЕПИСЬ-ryad-ne-lichnost-1109.md, коммит 16a0a7f. Бюджет: цена единицы замерена на двух первых местах — 357 с на два = 178 с/место, произведение 50x178=8900 с против потолка 2700 с не влезает, поэтому три прохода пущены параллельно (292 с на 57 мест = 5.1 с/место); перепись не резалась. Наружу не ушло ничего, процессов не трогал, кода не правил.
