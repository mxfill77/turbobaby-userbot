# Тело записи журнала — 2026-09-05 18:09 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1947 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-05 18:09:38 |
| символов в строке | 1947 |
| порог | 600 |

---

DONE Dispatch 00c-manus-empty.0905: адаптер Мануса перестал ждать полчаса пустоты. Премиса проверена своими командами и ОДИН пункт поправлен: промахов 25, а не 23 (корпус вырос, класс живой), 45095 с = 12.53 ч, все 25 с последним состоянием completed и все на ПЕРВОЙ части многочастного пакета — это и указало на место дефекта. Дефект: _wait_reply в review_send_run.py не знал терминального completed, хотя соседняя _wait_settled его знает, а шапка модуля называет его терминальным. Исходы ожидания разведены на ЧЕТЫРЕ, ярлык answer_lost СУЖЕН до своего буквального смысла, а не расширен: ok (ответ проверяется ПЕРВЫМ, до разбора состояния), channel_idle (завершено без тела; external/resend, цена по квитанции), poll_timeout (потолок исчерпан на живой работе; spent/refetch), answer_lost (обрыв транспорта). Цену захода теперь называет квитанция канала credit_usage, которую до правки не читал никто: на 25 промахах канал печатал ноль, а мы записывали цену 1. Немота подтверждается двумя опросами подряд — страховка от гонки статус-раньше-текста стоит 10 с против снимаемых 1782 с. Случай стоит 20 с вместо 1802 с, короче в 90 раз. Пересчёт на записанных промахах: снято 12.39 ч из 12.53 ч (98.9%), на выборке аудита из 23 случаев — 11.40 ч из 11.52 ч, число аудита воспроизведено знак в знак. Тесты: +12 в test_review_send на СНЯТОЙ ЖИВОЙ фикстуре fixtures/manus_task_completed_empty.live.json с моком на границе сокета, +2 в test_review_outbox_queue; свой набор целиком 243 теста OK, полный набор полосы не гнался. Пустое ответом не становится ни одной веткой (outcome unknown, ответ пустая строка, answer_chars 0) — предсмертный взгляд задания закрыт тестами. Канал НЕ отключён и не понижен. Коммиты 1b9df12 (правка и тесты) и 2eef6ea (артефакт). Причина пустоты на стороне Мануса — НЕИЗВЕСТНО, предмет владельца; правка снимает простой, а не возвращает ответы. Наружу не отправлено ничего, живых обращений к Манусу ноль.
