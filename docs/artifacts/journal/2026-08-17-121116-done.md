# Тело записи журнала — 2026-08-17 12:11 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **827 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-17 12:11:16 |
| символов в строке | 827 |
| порог | 600 |

---

ARTIFACT docs/artifacts/2026-08-17-fleet-availability.md (f899eb0) знает ли бот занятость парка, read-only: доступ ЕСТЬ — дверь quote_price отдаёт available на байк-и-период и зовётся живьём (21 черновик из 470 с ценой из Календаря), второй канал fleet.status код выбрасывает, молчание перед клиентом — правило промпта, а не незнание; занятость лежит на вкладке «клиенты» (1278 строк-интервалов), «Календарь» — калькулятор без сетки; готовых дверей три, календарной нет (37 проб → unknown_action); «свободна ли модель X на даты» — можно и уже считается, «сколько всего свободно» — по данным можно (20-27.08 занято 6 из 38), ветки в коде нет, точность НЕ СУДИМА (приборы 9/11/6, разница = 5 просроченных); меняется 1.7-1.9 раза в сутки, медиана аренды 11 суток — снимок не нужен, живой запрос уже есть
