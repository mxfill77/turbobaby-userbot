# Тело записи журнала — 2026-09-05 12:57 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2524 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-05 12:57:57 |
| символов в строке | 2524 |
| порог | 600 |

---

DONE Dispatch 20:05: сборка пакета внешнему ревьюеру перестала выглядеть смертью демона. ЗАМЕР 03-05.09 по pc_orchestrator.log: 25 объявлений «демон жив процессом, но НЕ РАБОТАЕТ» при НУЛЕ подъёмов и НУЛЕ смертей; из 25 демон дал оборот в ближайшие 5 минут ОДИН раз, в остальных 23 — позже (медиана +1200с, max +2268с), 1 не наблюдаем. Причина устройственная: сборок с пакетом наружу 39, их медиана 1841с при пороге тишины 1800с, max 1916с — каждая успешная сборка обязана была позвать владельца, 25 из 39 позвали. Живой эпизод премисы подтверждён: 17:44:28 старт сборки, 18:13:02 тревога, 18:15:19 пакет (1851с). СДЕЛАНО: (1) ступень A объявляется сторожу ТЕМ ЖЕ способом, что объявленный заход — отметка pc_orchestrator.stage_started.json, годная лишь пока новее последнего оборота; потолок ВЫВЕДЕН из жёстких пределов ступени (300 codex + 1800 manus + 1800 остаток витка = 3900) и клеммирован 4500с headless-прогона; порог тишины НЕ поднят ни на секунду — молчание без объявления звенит через прежние 1800с; объявление звучит только перед каналами, иначе зависание в любом другом месте витка купило бы чужую грацию. Контрфакт по корпусу: снимет 25 тревог из 25, останется 0. (2) уступка витка работе владельца (new/approved, судит _is_owner_work демона) внутри оборота — после повода, до сборки пакета, попытка не списана. ЧЕСТНЫЙ НОЛЬ: на видимом корпусе уступка сработала бы 0 раз из 39 — лог хранит момент постановки только рядов, которые ставит сам демон (n=55, медиана ожидания клейма 171с, max 334с), а ряд владельца из Telegram момента появления не оставляет вовсе. (3) ОТРИЦАТЕЛЬНЫЕ ТЕСТЫ: остановленный демон без объявления звенит в срок (девять пятиминутных тиков, живой watchdog по фактам на диске, мокнуты только CIM и schtasks — красные); открытое объявление НЕ заслоняет труп (restarted, /Run один); пережившая свой потолок ступень звенит по имени. Тесты: test_pc_orchestrator 1050/1050, test_review_auto 86/86, test_pc_local_dec 49/49. МИНА ЗАМЕРА, названная в артефакте: в pc_orchestrator.log пишут ДВА процесса, и по «предыдущей строке лога» длительность сборки вышла max 288с при живом факте 1851с. ВТОРАЯ МИНА: очередь переиспользует номера — наивная пара enqueue-claim дала ожидания в 208-237 тыс. секунд. ОБЩИЙ ИНДЕКС: между моим git add и git commit закоммитил сосед, и правка легла в коммит 3d627d0 с ЧУЖИМ сообщением; принадлежность проверяется содержимым (_wd_stage_busy 3, yield_fn 4), историю не переписывал — там же чужая работа; факт записан в артефакт коммитом 232edd3.
