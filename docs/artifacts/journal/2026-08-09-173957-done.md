# Тело записи журнала — 2026-08-09 17:39 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1299 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-09 17:39:57 |
| символов в строке | 1299 |
| порог | 600 |

---

DONE Dispatch 00:52: ревизор — порог доказанной пользы проверок (коммит 20dcf1b, запушен). Проверка = класс|предмет без окна; счёт законных/ложных/не разобрано; порог «разобранных ≥20 И законных 0» уводит проверку из потока владельцу в ленту. Замок двойной и оба под голденом: находки выбывшей продолжают считаться (раздел checks реестра + улика в лог + имя проверки в ленту), первая ЗАКОННАЯ возвращает её в поток тем же прогоном (статус вычисляется, а не хранится). Записан вердикт «ложная» пяти ключам группы «депозитная лестница» #92 (основание — разбор 411); две улики из пяти выведены, не процитированы — промах существа безопасен, находка доедет как новая. ЧИСЛОМ: выбывает сейчас 0 проверок, в счёте 1 (#92|депозит: разобрано 6 при пороге 20), у 14 счёт пуст, карточек порог убирает 0. 27 голденов, красная проба 5/5, полный гейт 3157 OK, храповик nonparse 320 без роста. Сам чек #92 и suggest.py не тронуты. ОТДЕЛЬНО, не чинил: 22 находки неразбираемы — улика рождается в _revizor_postrelease_findings (pc_orchestrator.py:7808) и _revizor_ipc_findings (:7865), а в лог маршрута (:7897, :7906) идёт только СЧЁТ; текст живёт лишь в карточке на Bridge и в спуле, который очищается доставкой (:8028). Разбор: docs/artifacts/2026-08-10-revizor-check-usefulness-threshold.md
