# Тело записи журнала — 2026-09-06 11:40 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2791 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-06 11:40:42 |
| символов в строке | 2791 |
| порог | 600 |

---

DONE Dispatch 18:47: дедлайн сборки прайс-сетки назначен ПО ЗАМЕРУ, а сработавший перестал прятать потерю под чужой причиной (задание Штаба 00-m-sheet-deadline.0906, полоса ПК). Коммиты 1bebd37 и fd8554e. ЗАМЕР: живой лист «Календарь бронирования» не открывался ни разу — прямой запрет задания, а одна сборка это 111 обращений к нему; поэтому мерили БОЕВУЮ price_sheet_status при СНЯТОМ дедлайне на боевой кардинальности (111 quote, 13 продуктов, пул 8), дверь воспроизводила 54 живых замера quote_price того же дня. 12 повторов плохого окна: 99.4 · 101.3 · 102.7 · 103.1 · 103.1 · 104.1 · 104.7 · 108.4 · 112.7 · 113.4 · 116.9 · 118.4с, медиана 104.7, макс 118.4. Стенд сверен с живым: те же 12 повторов на обычном окне дают медиану 72.9с против живой 64.9с (56 сборок журнала), расхождение 12 процентов. ЗНАЧЕНИЕ: 118.4 умножить на два = 236.8, это 2.26 медианы плохого окна и 3.65 живой медианы; и НИЖЕ аналитического потолка сборки 280с (потолок круглым вверх 111 на 8 очередей умножить на PRICING_TIMEOUT 20с) — выше 280 порог был бы мёртвым кодом. ПОЧЕМУ НЕ ЖУРНАЛ: живой журнал цензурирован тем самым потолком, который судим, — 4 сборки из 56 записаны ровно как 120.0с, и сколько им было нужно, журнал не знает по построению. СЛЕД С ЧИСЛОМ виден в suggest.log уровнем WARNING по префиксу SHEET ПОРОГ СРАБОТАЛ; этим заходом закрыта ЛОЖНАЯ ПРИЧИНА — модель, у которой дедлайн срезал ВСЕ котировки, уходила в журнал словами занятость НЕ ПРОВЕРЕНА, то есть разбирающий чинил бы дверь вместо порога; теперь называется дедлайн и число срезанных котировок, плюс разрез моделей целиком и частично в сводке. КОНТРФАКТ парой: сработал даёт ровно одну сводку с числом (18 quote из 48, моделей целиком 1 из 4), уложившийся на боевом значении не оставляет ни строки и пишет нули. ЦЕНА КЛИЕНТУ словами: обычная сборка не подорожала ни на секунду (медиана 64.9с как была), худшее ожидание выросло со 120 до 236.8с и платит его клиент в плохом окне двери (4 случая из 56, 7 процентов) — взамен он перестаёт получать прайс, из которого молча выброшено 40 процентов котировок (замерено 44 и 41 из 111). РЕЕСТР зелёный: с замером 9 было 8, без замера 11 было 12, красных 0; храповик замера ЗАТЯНУТ с 12 до 11. СОСЕДНЯЯ ПОСЫЛКА: своего красного ноль — test_suggest 664 павших 6, и все шесть красны на suggest.py ИЗ HEAD (доказано прогоном attribute.py head ALL: 19 павших, те же шесть плюс десять чужих плюс три моих); test_expectations_pc падает одним тестом замыкания pc_agent от чужого коммита aa0de0e, suggest.py в то замыкание не входит вовсе. Ослабление теста не рассматривалось ни разу. Общее дерево: в коммит ушли ТОЛЬКО свои ханки (сосед правит те же suggest.py и test_suggest.py), отбор скриптом mine_only.py с проверкой маркеров. Временное в _scratch_sheet_deadline_0906.
