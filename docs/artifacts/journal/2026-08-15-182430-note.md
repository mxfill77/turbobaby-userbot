# Тело записи журнала — 2026-08-15 18:24 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-15 18:24:30 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #43 → done · Опись выполнена: 1278 строк, 23 поля. Даты оформления НЕТ, отметки предоплаты НЕТ, `booking_id` пуст у 1278 из 1278 (подтверждено). Шаги 5 и 6 — **НЕ СУДИМО**. Из трёх причин одной подписи полем обеспечена ровно одна (сезон); две другие в базе ненаблюдаемы. Мутаций ноль, ПДн наружу не выносились. **FACT:** - `FACT: commit 311d63c в git log` — артефакт `docs/artifacts/2026-08-15-booking-fields-census.md` - `FACT: commit e88175c в git log` — тело журнальной записи (авто-вынос по LINE_MAX) - `FACT: read-only` — единственный экшен `GET clients filter=all`, три захода; `create_booking`/`close_booking`/`add_transaction` не звались; в лист ни одного ввода - `FACT: журнал` — писатель вернул `OK: записано в мозг, символов: 372710`, код возврата 0 - `FACT: живые выводы` — `_scratch_bookfields_0815/{probe_output,meaning_output,control_output}.txt` ## Ответы по шагам **ШАГ 1 — 23 поля на 1278 строк.** Имена как есть: `auto_cancel, bike, booking_id, contacts, date_end, date_start, days_overdue, debt, deposit, deposit_raw, duration, helmets, is_active, is_fresh, is_overdue, name, note, paid, pay_per_day, row, status, to_pay, type`. Заполненность по каждому — таблица в артефакте §1. Оговорка, которую я держу явной: **пять ключей** (`row`, `is_active`, `is_fresh`, `is_overdue`, `days_overdue`) колонками не подкреплены — производные; `deposit`/`deposit_raw` — два вида одной колонки. За колонками стои́т ~17 ключей из 23. **ШАГ 2 — даты оформления НЕТ.** Признак «похоже на дату» гонялся по всем …обрезано
