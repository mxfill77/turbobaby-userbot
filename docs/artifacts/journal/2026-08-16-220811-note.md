# Тело записи журнала — 2026-08-16 22:08 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-16 22:08:11 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #40 → done · Из двух файлов правил под git встал **один**: `trainer_rules.json` (коммит `95077e4`), содержимое не тронуто ни байтом. `manager-bot/docs/playbook.md` взять **нечем** — путь за границей вложенного репозитория, и обойти это можно только удалением `manager-bot/.git` (красная операция, решение владельца). Секретов в обоих файлах нет, но у книги правил есть пустой раздел под реквизиты. **FACT:** - `FACT: commit 95077e4 в git log` — `trainer_rules.json`, 1 file changed, `git ls-files trainer_rules.json` → 1 - `FACT: commit 82fb3db в git log` — `docs/artifacts/2026-08-16-rules-under-git.md`, 130 строк - `FACT: sha256 bee2d75f…94e4` — на диске до add, после add, после кругового рейса и после отката; `cmp` молчит - `FACT: sha256 88e73178…c6dd` (playbook.md) не менялся — файл не трогался вовсе - `FACT: журнал — «OK: записано в мозг, символов: 398705»`, ledger `2026-08-16T22:06:26Z`, строка 541 символ, легла целиком ## Защита от повтора и основание `docs/artifacts/2026-08-16-rules-under-git.md` на диске **отсутствовал** → работа не сделана, иду дальше. Основание `2026-08-16-regression-probe10.md` (коммит `164cea9`) существует и содержит оба замера — `88e73178…c6dd` и `bee2d75f…94e4`. Оба совпали с живым диском **до** любых действий. ## Замок A — содержимое не изменилось Риск был настоящий: `core.autocrlf=true`, файл несёт 7 CRLF и **не имеет завершающего перевода строки**. Git хранит блоб в **1901 байт** против **1908** на диске — наивная сверка «sha блоба против sha диска» показала бы …обрезано
