# Тело записи журнала — 2026-08-08 13:17 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-08 13:17:56 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #411 → done · Разбор класса #92 закрыт фактами: чек `_smoke_deposit_conflict` объект не группирует — сравнивает суммы по всему тексту (`suggest.py:6512`), а шесть сумм карточки 401 дословно равны полному набору депозитных ступеней прайс-блока кода на 20 моделей ⇒ **находка ложная**. Маршрут: признак клиентского контура стоит только в ветке `act=="task"`, а #92 рождается `action="owner"` с пустым `task_text` — ворота его не видят. Правок нет. **Артефакт (весь дословный вывод команд — §6):** [`docs/artifacts/2026-08-08-class92-deposit-false-finding.md`](docs/artifacts/2026-08-08-class92-deposit-false-finding.md) ## 1. Группирует ли по объекту — НЕТ `suggest.py:6512-6514` (вся ветка «две суммы»): ```python deps = sorted({int(m.group(1).replace(" ", "")) for m in _SMOKE_DEP_AMOUNT_RE.finditer(s)}) if len(deps) > 1: return "разные суммы депозита: " + ", ".join(f"{v} ฿" for v in deps) ``` `finditer(s)` — по **всему** тексту; ни модели, ни брони, ни `client_id` в функции нет вовсе. Понятие «в одной фразе» в этом же коде уже есть, но применено только к ветке «паспорт»: `clause = _clause_at(s, m.start())` (`:6516`), поиск суммы по `clause` (`:6517`). Асимметрия и есть корень класса. Вход — весь клиентский текст одним куском: `client = client_facing_text(draft)` (`:6527`). Живой замер того же разрыва, `moderation_bot.log:6225-6245` (дословно): ``` проба: NMAX 155 2026-08-06..2026-08-11 зона Раваи (590 ฿) — [депозит без противоречий] ожидание: депозит один: сумма ЛИБО паспорт, без двух значений факт: …обрезано
