# Тело записи журнала — 2026-07-31 02:47 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2179 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-07-31 02:47:55 |
| символов в строке | 2179 |
| порог | 600 |

---

DONE Dispatch 10:05 (31.07.2026), 4ecbb5b: гард судит по ДЕЙСТВИЮ, а не по форме команды. Перебраны ВСЕ 14 строк таблицы красных признаков — девять судили чистой подстрокой (delete x2, kill x3, schtasks, git_force x3), пять уже имели структурный разборщик, но таблица этого не записывала. Поставлен ЗАМОК из трёх поясов: пара (regex,kind) больше не собирается — только _red() с обязательным verbs (имя в КОМАНДНОЙ позиции) либо probe, иначе ValueError ПРИ ИМПОРТЕ; генеративный тест строит текстовые пробы из verbs каждой строки сам, поэтому новое правило проверяется тестом, которого для него не писали; реестр _ACTION_CHECK сверяется с ИСХОДНИКОМ _decide_bash — мимо таблицы признак тоже не завести. Замок сработал на своём авторе: алиас ri стоял в _DEL_CMDS, а в признаке удаления его не было — ri файл не краснел ВООБЩЕ, дыра закрыта. Починены три текстовые ошибки, каждая — живой случай владельца: рекурсией считался любой флаг с буквой r (-Force делал удаление ОДНОГО файла массовым); в списке шаблонных флагов стояли булевы -n и -E, из-за чего имя файла секретов ВНУТРИ шаблона поиска становилось обращением к секрету; скобка не снималась, и проверка наличия в круглых скобках не опознавалась. ЗАМЕР реплеем журнала 29-31.07 одним конвейером на обеих чашах: верная необрезанная часть 3403 строки — карточек 48 → 46, НОВЫХ 0; весь корпус 4139 — 73 → 70, новых 0; по колонке журнала владелец за эти сутки видел 137, шесть классов из семи чинились в те же сутки. Корпус случаев test_guard_action_not_text.py: ложных краснело 4 из 22 → 0 из 22, настоящего красного проехало 0 из 24 в ОБОИХ замерах — настоящее удаление, запись в базу, правка конфига и обращение к секрету краснеют по-прежнему. Границы соблюдены: deny, hard-блок, секреты, высший вид карточки и клиентский контур не тронуты; прозрачность лога сохранена видом word_<kind>. Гейт 2545 тестов OK. НЕ сделано и записано в остатки: правка не перенесена на полосу VPS (зеркальная таблица там с тем же дефектом), запись вне репо не уважает санкционированные зоны шелла, журнал гарда искажает реплей обрезкой и склейкой переводов строк. Артефакт: docs/artifacts/2026-07-31-guard-action-not-text.md
