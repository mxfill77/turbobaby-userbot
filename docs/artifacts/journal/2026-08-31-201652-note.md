# Тело записи журнала — 2026-08-31 20:16 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1570 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-31 20:16:52 |
| символов в строке | 1570 |
| порог | 600 |

---

NOTE Orchestrator: задача #63 → done · V0 научился отличать свежий результат от следа прежнего прогона: введён гейт `V0_RUN_BINDING` — прогон называет задача, бандл сверяет, каждая улика несёт имя прогона сама. Фикстура 2 закрыта (C2a/C2b: PROVEN → DISPROVEN), фикстура 1 осталась красной теми же байтами. Коммит `1c6f314`, пуша нет. **FACT:** - `FACT: commit 1c6f314 в git log` — «V0 отличает свежий результат от следа прежнего прогона», 3 файла, +335/−4 - `FACT:` тесты V0 — `Ran 29 tests ... OK (skipped=1)`, было 18 - `FACT:` фикстуры — `requirement violated in 0/12 case(s)` (замер «до» на том же наборе: 6/12) - `FACT:` двойной прогон разными процессами — `compared 15 file(s) run1 vs run2, differing: 0` - `FACT:` журнал — две строки приняты мозгом (668317 → 668870 символов) ## Что сделано **1. Работа от артефакта, премиса сверена числом.** Открыт `docs/artifacts/2026-09-01-v0-negative-test.md`. Замер «до» снят на модуле с sha256 `178af445…` — ровно тем, который назван в отрицательном тесте. То есть «до» здесь не пересказ документа, а тот же прибор. **2. Привязка свежести — по личности, не по времени.** Гейт `V0_RUN_BINDING` стоит последним содержательным (после `V0_TEST_EVIDENCE`), три проверки: - пакет **задачи** обязан нести `run_id` (зеркало уже принятой в V0 асимметрии «гейты называет задача, не исполнитель») → иначе `UNKNOWN/task_run_id_missing`; - бандл обязан назвать тот же прогон → иначе `DISPROVEN/run_binding_mismatch`; - **каждая** улика обязана нести имя прогона сама: JSON — полем `run_id`, текст/питон — …обрезано
