# Тело записи журнала — 2026-08-20 22:36 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1570 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-20 22:36:31 |
| символов в строке | 1570 |
| порог | 600 |

---

NOTE Orchestrator: задача #75 → done · Разведка read-only закончена. Главное: посылка задачи неверна — правило и код **сошлись 16 из 16 до бата**; расходятся ГОЛДЕН и (правило=код). Корней пять, 13 из 16 расчёта цены не касаются вовсе. Сезон и ступень срока не оспорены ни разу. За 10 % на живых деньгах — одно место: XMAX New Gen −11.5 %. **FACT:** - `FACT: commit c6e7887` — артефакт `docs/artifacts/2026-08-21-goldens-deferred.md` в `git log`; `FACT: commit 9dc1f5d` — тело журнальной записи. `git status --porcelain --untracked-files=no` → **пусто** (кода не тронуто ни строки). - `FACT: Ran 836 tests in 190.801s / FAILED (failures=16)` — живой прогон 05:22, те же 16 поимённо. - `FACT: СОШЛОСЬ ДО БАТА: 16 из 16` — вывод `_scratch_deferred_0821/code_vs_rule.py`. - `FACT: журнал` — две строки легли: `OK: записано в мозг, символов: 512837` (ARTIFACT) и `513388` (DONE, тело вынесено в `docs/artifacts/journal/2026-08-20-223533-done.md`). - **Не пушено** (задача запрещает выкатку) — коммиты лежат локально на `main`. --- ## Сводка **1. Правило против кода — не расходятся нигде.** Независимый счёт (`rule16.py`, умышленно **не** импортирует `price_source.py`, читает только данные `price_source.json`) против `price_source.day_price`: 16 из 16 совпали до бата. Расходятся **голден** и **(правило = код)**. Это доказывает, что код применяет записанное правило точно; верность самих данных — отдельный вопрос, на него отвечает сверка с деньгами. **2. Пять корней, а не один.** Г1 снятое поле `text` — 5 (№2–6); Г2 протухшие **ручные** …обрезано
