# Тело записи журнала — 2026-08-01 15:58 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-01 15:58:23 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #174 → done · **СВОДКА:** Хвост рамки дописан в КОНЕЦ того же `KB_shtab_frame` (id `1DiXr3jx…`): 4669 → 6150 симв., 104 → 134 строки, U+FFFD 0, «конец рамки» ровно 1 раз, старый текст стоит префиксом дословно (4668 симв.). Второго дока нет — реестр как был 33 ключа. Коммит `f130f61` запушен, обе строки в журнале. ## FACT-отчёт **1. Числа до и после** (один и тот же канал, живой Bridge): | замер | ДО | ПОСЛЕ | |---|---|---| | символов | **4 669** | **6 150** | | строк | 104 | 134 | | U+FFFD | 0 | **0** | | маркер `═══ конец рамки ═══` | 0 | **ровно 1** | Арифметика объясняет каждый символ: 4 668 (старый текст без хвостового `\n`) + 2 (пустая строка-разделитель) + 1 480 (тело) = 6 150. **2. Документ один — проверено ДО записи.** `РЕЕСТР: ключей 33; shtab_frame -> 1DiXr3jxoZYxkWaS7k4X4SR0u2xQKUTCi; совпадает с id задания: True`; `resolve_name(KB_shtab_frame) -> name:shtab_frame`; тело и маркер в доке — **0 раз** (значит не повтор). Ключей после записи столько же: канал дозаписи доки не создаёт, `create_plain` не звался. **3. Существующее не тронуто — префиксом, а не обещанием:** `старый текст стоит префиксом дословно: True (4668 символов)`. Первые 4 668 символов посимвольно те же; всё новое строго после них. Бэкап снят писателем до записи: `tmp/brain_backup_shtab_frame_tail_20260801-225406.txt`. **4. Судит чтение назад, а не мост.** Расписка пришла `ok`, но фактом не считалась. Писали **по file id**, проверяли **по имени** через реестр, **отдельным процессом**: `6150 символов, 134 строк, U+FF …обрезано
