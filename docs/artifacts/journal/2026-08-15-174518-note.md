# Тело записи журнала — 2026-08-15 17:45 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-15 17:45:18 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #40 → done · Экзамен шага «бронь» прогнан. Отрицательный тест УСТОЯЛ; корневой сценарий S8 — 4 из 6 пунктов ПРОШЛО; слом — ОТКАЗ экзамена. Артефакт `docs/artifacts/2026-08-15-exam-booking.md` создан, коммит `ebf87db`, строка-индекс в журнале. Три нуля замка выдержаны. **FACT:** commit `ebf87db` в `git log` (290 строк, `docs/artifacts/2026-08-15-exam-booking.md`); журнал — `OK: записано в мозг, символов: 369678`; прогоны живые, выводы в `_scratch_exam_booking_0815/exam_booking_neg_output.txt` и `exam_booking_run_output_pass2.txt`. ## Защита от повтора (первое действие) `docs/artifacts/2026-08-15-exam-booking.md` на диске **отсутствовал** → заход законен. Основание проверено вторым действием: discriminator-артефакт есть (29 732 б, коммит **`0e51533`**, дополнен `2fbfdac`, вердикт «НАЙДЕН: `intake.status`»); артефакт сценариев есть (19 876 б) — но **untracked в git** (`??`), назвал честно. ## Номера сценариев — из артефакта > **Шаг 2 «бронь» — это S5–S13 (девять).** §4 артефакта дословно: «шаг 1 — 4 (S1–S4), шаг 2 — 9 (S5–S13)». Прежнее расхождение S1↔S10 объяснено: S1 живёт в шаге 1 и судит `drafts`, S10 — в шаге 2 и судит `intake`. ## ШАГ 1 — отрицательный тест: **УСТОЯЛ** Состояние «строка есть, `status='draft'`, дальше ничего»: прибор → **НЕ ПРОШЛО**, наивный контрфакт («строка есть → заявка оформлена») → **ПРОШЛО**. Отличие от шага 1 существенное: там честным исходом было «не судимо», здесь различитель есть, поэтому честный исход — отказ. ## ШАГ 2 — корневой S8, шесть пунктов | # | исхо …обрезано
