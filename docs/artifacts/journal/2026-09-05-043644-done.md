# Тело записи журнала — 2026-09-05 04:36 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1260 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-05 04:36:44 |
| символов в строке | 1260 |
| порог | 600 |

---

DONE Dispatch 11:40: задача 241 — под заморозкой ворота не спрашивают владельца, коммит e788e08 (запушен). Премиса верна вся: за 05.09 25 отказов ворот, 21 подавлен, 4 карточки владельцу. Корень: 25 отказов дали 5 форм (1/3/7/8/10 файлов), 4 новые за сутки — форма включает состав клиентских файлов, а он растёт каждой задачей ночи, т.к. _last_child_commit при отказе не двигается. Правка одна: client_contour.gate_route — под заморозкой ВСЕГДА лента, независимо от формы, состава, коммита и успеха записи реестра; счёт форм вынесен в _seen_bump, стал бухгалтерией. Живой прогон по тому же корпусу при живом флаге: 0 карточек, 25 в ленту; контроль без заморозки — 25 карточек. Признак заморозки явный: client_contour.frozen() → os.path.exists(<репо>/pc_orchestrator.contour_frozen), лежит с 21.08 19:36; отмена — удалить файл, без перезапуска демона, под тестом. Fail-closed, полнота отказа в журнале и чужие классы карточек не тронуты. Тесты 1113 OK. Остаток: текст самого флага описывает прежнее правило и врёт — не правил, перезапись сменит mtime = эпизод заморозки. Красный test_raner_ne_v_klientskom_konture старше задачи (проверено на дереве HEAD). Артефакт docs/artifacts/2026-09-05-под-заморозкой-ворота-не-спрашивают-владельца.md
