# Тело записи журнала — 2026-08-18 07:38 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-18 07:38:51 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #44 → done · Штамп занятости О2 переведён с поля `at` на mtime реестра — тот же предмет, что у верно отвечающего соседа. Два читателя одного файла сошлись посекундно на живом замере (1096 с против 1096 с; прежний предмет дал бы 15.13 ч и тревогу). Исходов стало три; оба отрицательных теста устояли на живой хронологии 17.08. Гейт 3738 при тех же двух храповиках. **FACT:** commit `3f1f930` (код) и `a2ecfcc` (артефакт) в `git log`, оба запушены (`59c3c8a..3f1f930`, `3f1f930..a2ecfcc`). Живой замер 18.08 07:32 UTC: прибор 1096 с · сосед `_wd_busy_age` 1096 с → **СОШЛИСЬ**; прежний предмет на том же тике — 54477 с (15.13 ч) при потолке 3300 → тревога. Боевой `--status`: «оборот демона: даёт оборот (молчание оправдано работой, объявленной 17 мин назад)». ## Что сделано **Предмет.** `expectations_pc_run.busy_facts` больше не перебирает поля `at`, а берёт `os.stat().st_mtime` — тем же одним вызовом, что и `pc_orchestrator._wd_busy_age`. Вместе с предметом перенесён замок соседа «отметка позже последнего оборота». **Три состояния** (`BUSY_RUN` / `BUSY_IDLE` / `BUSY_UNKNOWN`): работа идёт → молчание; оборот свежий → молчание (штамп даже не спрашивается); работы нет и оборота нет → **тревога**. Прочитать не удалось → «неизвестно», причём «реестра нет» (`FileNotFoundError`) и «реестр не прочитан» разведены дословно как в `pc_orchestrator._task_started_read`. **Забор Честертона.** `at` читали ради момента CLAIM и **номера задачи** для карточки. Момент mtime отдаёт точнее; номер **теряем безвозвратно** …обрезано
