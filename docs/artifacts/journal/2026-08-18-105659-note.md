# Тело записи журнала — 2026-08-18 10:56 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-18 10:56:59 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #52 → done · РАЗВЕДКА READ-ONLY завершена. Артефакта не было — задача выполнена целиком. Причина простоя названа: дыра не в дате 2099, а в принципале `InteractiveToken`. Забор объяснён журналом: 2099 — протез вместо `ONSTART`, на который не хватило прав. Автологон, выбранный владельцем 07.08 как решение, так и не вооружён. Ничего не менял. **FACT:** read-only. Единственная запись на диск — `docs/artifacts/2026-08-18-pc-autostart-why.md` (создан) и `_scratch_autostart_0818/raw-evidence.txt` (сырые выгрузки). Контроль неизменности состояния: `pc_orchestrator` `LastRun=18.08 14:13:03 Result=267009 Next=01.01.2099` и `pc_orchestrator_watchdog` `NextRun=17:48:00` — те же, что при первом замере; `AutoAdminLogon=0` как было; ни одной команды `schtasks`, `Set-*`, `New-*`, `Stop-*` не исполнялось. **Ответы на 8 вопросов — по живым фактам:** 1. **Выгрузка.** Единственный триггер — `TimeTrigger StartBoundary=2099-01-01`, без повтора. Принципал — `mxfill1`, **`LogonType=InteractiveToken`** («только для вошедших»), `RunLevel` не задан. Питание: `DisallowStartIfOnBatteries=true`, `StopIfGoingOnBatteries=true` (инертны — десктоп). Сети в условиях нет (`RunOnlyIfNetworkAvailable` отсутствует → false). `RunOnlyIfIdle` отсутствует → весь блок idle мёртв. `StartWhenAvailable` отсутствует → пропуск не догоняется. `AllowStartOnDemand` по умолчанию true — задача существует ради `/Run`. 2. **История.** Заведена **04.07.2026 23:24:44**, `created == modified` посекундно — **2099 стоит с рождения и не правилось ни …обрезано
