# Тело записи журнала — 2026-08-16 21:55 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-16 21:55:48 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #39 → done · No split file appeared — the line went into the brain whole. Заход закрыт. --- Пары 11–36: прошли **ВСЕ 26 из 26** за 1312с (предел 2700). Набор правил **ЦЕЛ** — оба sha256 совпали до и после. Линия: модель 15.4%, обе даты 7.7%, цена 3.8%, отказов 5. Сведено с пробой — 36 пар. «100% по модели» проверено: это **ОТБОР, не умение**, ходов менеджера во входе 0. **FACT:** - `FACT: commit e9231b3 в git log` — `docs/artifacts/2026-08-16-regression-part2.md`, 1 файл - `FACT: 26 строк в _scratch_regpart2_0816/drafts_part2.jsonl`, прогонщик вышел сам (exit 0, 1312.1с), сироты не оставил - `FACT: sha256 ДО = ПОСЛЕ` — `88e731788784b1fe…c6dd` (playbook.md) и `bee2d75f81b916ab…94e4` (trainer_rules.json), mtime не сдвинулись - `FACT: журнал — «OK: записано в мозг, символов: 397470»`, строка легла целиком (файла-выноса не создано) **ШАГ 0 — целость (до запуска бота).** Оба sha256 совпали с якорями `88e73178` / `bee2d75f`. Копия набора правил положена ДО прогона в `_scratch_regpart2_0816/rules_backup_before/` (файлы вне git, отката у них нет) — восстановление руками: положить обратно поверх. **Запись после каждой пары.** Место: `_scratch_regpart2_0816/drafts_part2.jsonl`. Это не добавка захода, а свойство прогонщика — `run_bot.py:228` пишет на дописывание сразу после пары, `:169` пропускает уже прогнанное, поэтому обрыв стоил бы одну пару, а не заход. Свои часы (стоп 2050с) не понадобились. **Секунды.** Медиана 47.9с · худшая 100.2 · лучшая 18.8. Предсказание по медиане пробы (1297с) разошлос …обрезано
