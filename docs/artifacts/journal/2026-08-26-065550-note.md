# Тело записи журнала — 2026-08-26 06:55 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-26 06:55:50 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #41 → done · Модербот 26.08 **не падал** — он дважды стартовал и **выходил сам** по своему синглтон-гарду: `moderation_bot.lock` держит номер 18200 с 23.08, а после ручного ребута 11:47:04 этот номер достался `PinWin.exe`. `_pid_alive` проверяет только «номер занят», без имени и времени старта. Вотчдог при этом не соврал ни разу. Разбор записан артефактом, коммит b6df27f. **FACT:** commit `b6df27f` в `git log` (артефакт `docs/artifacts/2026-08-27-moderbot-three-failed-starts.md` + тело журнальной записи). Три строки в журнал ПК — rc=0 каждая. Живая проба: `moderation_bot.py` в `Win32_Process` **отсутствует**; PID 18200 существует и это `PinWin.exe` (старт 26.08 11:48:14, PPID 3624); `LastBootUpTime` = 2026-08-26 11:47:04. --- ## ШАГ 4 — ответ (первым, как просили) **Версия (а) — процесс стартует и немедленно выходит — ДОКАЗАНА. Причина в логе дословно:** ``` 2026-08-26 11:53:54,696 | moderation_bot уже запущен (PID 18200) — второй не поднимаю, выхожу. 2026-08-26 12:27:33,032 | moderation_bot уже запущен (PID 18200) — второй не поднимаю, выхожу. ``` Стухший лок + переиспользованный Windows'ом номер: - `od -c moderation_bot.lock` → ровно 5 байт `18200`, mtime **23.08 18:29:02.030612200** (миллисекунда в миллисекунду со строкой «забираю» в логе); - настоящий модербот PID 18200 прожил 23.08 18:29 → 26.08 11:46:54 и погиб **вместе с машиной** (ручной ребут: Id=1074 `StartMenuExperienceHost.exe` в 11:46:47, Id=13 в 11:46:54, Id=12 в 11:47:05). `release_lock()` при выключении не отрабатывает — ф …обрезано
