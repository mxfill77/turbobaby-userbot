# Тело записи журнала — 2026-08-04 10:34 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-04 10:34:34 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #260 → done · Откат снят и проверен, замена НЕ сделана: текста рамки 04.08 нет ни в строке задачи, ни в дереве, а сочинять копию инструкций Штаба нельзя. Живой док не тронут (6780 симв., версия 01.08). Готова одна команда на момент, когда текст придёт. Журнал: ARTIFACT + NOTE + ASK, сверены чтением назад. ## FACT-отчёт **1. Адрес сошёлся (живой Bridge, чтение).** Реестр: 33 ключа, `shtab_frame` → `1DiXr3jxoZYxkWaS7k4X4SR0u2xQKUTCi` — совпадает с id задания; второго ключа на этот файл нет; `resolve_name("KB_shtab_frame")` → `name:shtab_frame`. Чтение по id и по имени дало **6780 символов и посимвольно совпало**. **2. Живой док ДО (замер):** 6780 символов, 145 строк, **U+FFFD 0**, «конец рамки» **ровно 1 раз**, первая строка — версия **01.08.2026**. Побочно: в нынешней копии **два раздела №11** (стр. 112 и 116), первый обрывается на «Этап 3» — след дозаписи хвоста 01.08. **3. Откат снят ДО всего, в двух местах** — 11 817 байт, 6780 символов, sha256 `cade204ef9a158a4…`, посимвольно равен живому доку: - `tmp_shtab_rewrite_20260804_w3/shtab_frame_2026-08-04_before.txt` - `docs/artifacts/2026-08-04-shtab-frame-before-rewrite.txt` (чтобы пережил уборку временных каталогов) **4. Почему замена не сделана.** Текста новой рамки нет: демон отдаёт строку очереди целиком, без обрезки (`pc_orchestrator.py:1501` — `prompt += text`), значит в задаче его действительно нет; в дереве не нашёлся (все файлы, изменённые с 03.08 12:00, плюс `tmp/`, `artifacts/`, `notes/`, прежние каталоги рамки); «следующего сооб …обрезано
