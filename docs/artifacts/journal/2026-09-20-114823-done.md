# Тело записи журнала — 2026-09-20 11:48 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1357 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-20 11:48:23 |
| символов в строке | 1357 |
| порог | 600 |

---

DONE 68z MODELPOLE 2009 18:48: ручка модели канала codex заполнена ОДНИМ значением в одном месте — review_send_run.py:234 DEFAULT_CODEX_MODEL='gpt-5.6-terra' и :285 model = model or DEFAULT_CODEX_MODEL; этого хватает на все три двери (демон pc_orchestrator.py:11293 → review_auto_run.py:566,590 с codex_model=None; повтор review_outbox_queue_run.py:274-276 без флага; ручной вызов :1333), потому что строку запуска канала собирает РОВНО одно место review_send_run.py:282 и других сборщиков codex exec в репо нет. Числом: до правки -m в argv 0 раз (длина 9), после 1 раз (длина 11). Имя НЕ выдумано: gpt-5.6-terra — голова последнего успешного ответа канала (шапка живого прогона 01.09, docs/artifacts/2026-09-01-разведка-каналов-ревью.md:53; последний успех лотка 11.09), и она лежит строкой в самом codex.exe 0.151.0 (5 совпадений), а выбираемого CLI gpt-6-astra в бинаре НЕТ вовсе (0). Отдельного списка моделей в пакете нет — сказано прямо. Замок: 5 тестов test_review_send.CodexModelHandle. Регресс 263/2 падения (оба новый замок) → 263/0 OK; два лишних падения TestNoBuildWhileNobodyRides оказались дефектом окольного стенда (порядок импорта), тест не ослаблен. Живой пробы НЕТ: красное network плюс бюджет 1420с из порога 1890 при пределе пробы 900с — наружу 0 пакетов, тело 0 знаков, карточка не легла. Коммит 336a6099, запушен.
