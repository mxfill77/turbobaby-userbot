# Тело записи журнала — 2026-09-05 10:39 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1418 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-05 10:39:32 |
| символов в строке | 1418 |
| порог | 600 |

---

DONE Dispatch 17:45: задание 15 «сигнальная остановка ящика 2026-09-05 16 35». ИСХОД: НАЙДЕНО — остановка ДЫРЯВАЯ (объявление верное, запрет шёл мимо). Доказано монотонностью: метка e0140bfc78dc объявлена ДВАЖДЫ (12:58:21 и 16:35:17), а снятый сигнал в active() не входит и второй раз объявиться не может; между объявлениями ящик взял 5 заданий (14:35, 14:50, 15:02, 15:22, 15:55) и одно после (16:45 #13). Причина: запрет — чистая функция от корпуса закрытых рядов живой очереди, у которой номера идут по кругу (замер того же дня: #241 в 11:19, «номер переиспользован» в 11:44, 7 рядов вместо 250), а памяти у запрета не было; долгую память в тот день получил ДЕДУП (e46c638), но не остановка. Гипотеза про мост ПОДТВЕРЖДЕНА: нечитаемая шапка читалась как «меток снятия нет», и ящик просил метку, которую владелец уже дал. Правка 0ffc63e: замок pc_orchestrator.shtab_box_hold.jsonl (дозапись строкой), один список сигналов merge_held даёт И фразу владельцу, И запрет брать; запирается только ОПРЕДЕЛЁННАЯ остановка со снятием словом владельца, «не знаю» держит виток и уходит с прибором; снятие помнится; при нечитаемой шапке фраза говорит НЕИЗВЕСТНО и метку не просит. Отрицательный тест: корпус пропал — взято 0, тот же оборот без замка БЕРЁТ; 12 новых проверок, 1602 теста затронутых наборов зелёные. Артефакт: docs/artifacts/2026-09-05-сигнальная-остановка-ящика-держит-или-не-объявляется.md
