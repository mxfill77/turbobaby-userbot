# Тело записи журнала — 2026-07-30 20:12 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1418 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-07-30 20:12:20 |
| символов в строке | 1418 |
| порог | 600 |

---

DONE Dispatch 03:10: ротация pricing.log закрыта по факту, коммит d352655 (запушен). Проход 1 (3237317) развёл логи по владельцу и сделал отказ громким, но записал "неустранимый отказ" как факт — замер его снял: os.replace на боевом файле → WinError 32, а copy2+truncate(0) → успех (Python отдаёт FILE_SHARE_WRITE, но не FILE_SHARE_DELETE). Живой pricing.log перекачен БЕЗ рестарта процессов: история в pricing.2026-07-22.log (23 302 269 байт, побайтово равна), боевой — 0 байт, НЕ удалён. copytruncate стал второй ступенью переката у SafeRotatingFileHandler и у _rotate (гард); громко теперь только если сорвались обе, обходной путь виден как ROTATE-NOTE. Стенд на штатном RotatingFileHandler доказал: до усечения строка теряется, после — ложится, то есть застрявший сосед оживает без kill. 4 теста прохода 1 покраснели честно (их фикстура ломала только rename) — контракт разделён, не подогнан: +TestCopytruncateRescue. Гейт: test_log_setup 34 OK, полный прогон 2471 OK, 0 красных. ВАЖНО: 14 красных при первом полном прогоне — ложные, их даёт PRETOOL_APPROVED_KINDS этой сессии, который тесты гарда читают из живого os.environ (та же болезнь "тесты читают боевое окружение", но в гарде; точка касания pretool_guard.py:1879, в остатки). Живым котировочным трафиком воскрешение ещё НЕ подтверждено — файл 0 байт, трафика ночью нет. Разбор → docs/artifacts/2026-07-31-log-rotation-copytruncate.md
