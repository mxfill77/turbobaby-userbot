# Тело записи журнала — 2026-09-04 10:23 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1367 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-04 10:23:19 |
| символов в строке | 1367 |
| порог | 600 |

---

DONE Dispatch 17:35: ПОТОЛОК МОСТА СНЯТ ВРЕМЕННО РУЧКОЙ ОТКАТА (коммит 1a28519). Снято ЗНАЧЕНИЕМ: умолчание PC_BRIDGE_LOOP_BUDGET 900→0 в pc_orchestrator.py:685 — код прибора цел весь (класс, три исхода, замок пути записи, событие со словами), вернуть литералом или PC_BRIDGE_LOOP_BUDGET=900 в среде демона. Премиса подтверждена: 10 срезанных витков ящика 13:37:41→16:06:55, полоса не брала задач 12:09:49 (#189) → 15:13:01 (#195) = 3 ч 03 мин, порог 900 = 1.16x p99. ЗАМЕРОМ НАЙДЕНА НАСТОЯЩАЯ ПРИЧИНА, и она не в числе: демон живёт как __main__, а ящик и ступени берут мост через import pc_orchestrator — ВТОРОЙ объект модуля со своим _bridge_budget, которого reset() главного цикла не достаёт, и счётчик той копии не обнуляется НИКОГДА. Отпечаток: 10 срезов и НОЛЬ строк ПОТОЛОК МОСТА, очередь выздоровела сама в 15:13, а ящик — только рестартом 16:08. Живая проба той же дорогой (_scratch_budget_off_0904/prove_off.py, только чтение): с потолком ok=False kind=budget ms=0 (сети не касались), без потолка ok=True, очередь прочитана, живой GET 11.3 с. Гейт self-update 1033 теста OK. ВЕРНУЛИ ВСЛУХ: один зависший вызов моста снова тормозит оборот целиком — до 50 мин по вчерашнему замеру, до 4802 с по инъекционному. ВРЕМЕННО, а не решение: правило придёт разведкой оборота. Артефакт: docs/artifacts/2026-09-04-bridge-ceiling-off-temporarily.md
