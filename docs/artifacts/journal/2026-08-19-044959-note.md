# Тело записи журнала — 2026-08-19 04:49 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-19 04:49:59 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #24 → done · Ключ дедупа ворот починен одной строкой: коммит в подписи сверяется `client_contour.short()` с сырым фолбэком. Реплей шести живых пар: было 0/6 схлопнутых, стало 6/6. Оба отрицательных теста устояли, красная проба валит ровно (а). Гейт 3773 теста против прежних 3770, красных 2 — те же два храповика. Коммиты 70635de (правка) и 4e3465c (артефакт) запушены. **FACT:** - `FACT: commit 70635de в git log` — правка ключа (`pc_orchestrator.py | 14 +++-`, `test_pc_orchestrator.py | 47 +++`), запушен `a2ecfcc..70635de main -> main` - `FACT: commit 4e3465c в git log` — артефакт `docs/artifacts/2026-08-19-gate-dedup-fix.md` (216 строк), запушен `70635de..4e3465c main -> main` - `FACT: гейт после правки — Ran 3773 tests, FAILED (failures=2, skipped=10)`, красные те же: `test_no_growth_against_baseline`, `test_totals_do_not_grow` (`test_nonparse_ratchet.TestRatchet`) --- ## Признак сделанности `docs/artifacts/2026-08-19-gate-dedup-fix.md` **не существовал** → работа не была сделана. Сделана в этой сессии. ## Что изменено — ровно ключ `pc_orchestrator.py` → `_client_block`: ```python - key = (str(commit), tuple(held)) + key = (client_contour.short(commit) or str(commit), tuple(held)) ``` плюс комментарий над строкой. Больше в воротах — ничего: ни логики, ни текстов карточки, ни числа входов, ни `held`. Нормализатор не заводился, он уже лежал (`client_contour.py:319`) и его докстринг написан ровно про этот класс. Фолбэк `or str(commit)` обязателен: на не-хеше `short()` отдаёт пустую строку, и …обрезано
