# Тело записи журнала — 2026-09-21 19:57 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2050 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-21 19:57:00 |
| символов в строке | 2050 |
| порог | 600 |

---

DONE Dispatch 03:20: 70l ARHIVPISHETSNOVA 2209 — ВКЛЮЧЕНО. Премиса Штаба НЕ подтвердилась: путь приёма userbot действительно клиентский (client_contour.closure = 50 файлов, userbot_listen.py внутри), но вызов обязан стоять НЕ там — захват пакетный (замер 2.167с и 43104 сообщения за проход), на входящем это полное перечитывание архива НА КАЖДОЕ СООБЩЕНИЕ. Настоящее место — pc_orchestrator.poll_once, строка _chatlog_capture('poll') после _srv_delivery() и ДО _write_heartbeat(); файл ВНЕ клиентского замыкания, замыкание от правки не выросло (50 → 50, спавн а не импорт). Вызова не было НИКОГДА: f991db9f (05.09 18:46) родил три модуля одним коммитом и вызывающего не внёс, git log --all -S chatlog_ingest вне артефактов пуст. Написано: tick()/due() с чистым решением о частоте (360 мин, откат CHATLOG_TICK_OFF=1), штамп заявляется ДО захода, файловый замок O_EXCL с перехватом брошенного, ветка ошибки несёт только имя класса исключения (str(e) цитирует разбираемый документ, то есть переписку). Тесты 52 → 77 OK (+25), гейт self-update 1201 OK. МУТАНТЫ: 10 запущено, 10 поймано; M4 сначала ВЫЖИЛ — тест зеленел на закомментированном вызове, переписан на разбор исполняемых строк. ФОТО: только отметка kind+note, имени файла и ссылки НЕТ (проверено перебором ключей). reply_to только у выгрузок, признака 'от бота' нет вовсе. Темп 0.059 МБ/сут (16 суток: 1549 уник. сообщ., 994293 Б), хватит на ~1610 лет при 34 ГБ; ротации НЕТ по построению (4 теста) — с включением часов архив растёт постоянно и не обрезается никогда. ВРЕД, НАЗВАННЫЙ ЧИСЛОМ: первый прогон гейта доехал до живого poll_once и запустил ДВА настоящих захвата — в боевой архив легло 1587 законных строк и 1118 ДУБЛЕЙ в 16 файлах дня, 2 файла побайтно испорчены; дедуп защищает от ПОВТОРА, а не от ОДНОВРЕМЕННОСТИ. Закрыто двумя замками, повторный прогон 1389 тестов архив не тронул. Порчу НЕ убираю (запрет 'ничего не удалять') — решение владельца. Коммиты 4852c138 и 5dbf970a, запушены. Наружу не ушло ничего, процессов не тронуто, строк переписки в отчёте 0.
