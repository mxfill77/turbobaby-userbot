# Тело записи журнала — 2026-09-12 07:58 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **2372 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-12 07:58:22 |
| символов в строке | 2372 |
| порог | 600 |

---

DONE Dispatch 15:05: разведка f8403651faf0 — повод «отказ канала codex: channel_error» ПОДТВЕРЖДЁН и недосказан. Причина числом: прибор codex-cli 0.151.0 (codex --version) запросил модель gpt-6-astra, сервер ответил HTTP 400 «requires a newer version of Codex» — 2 раза внутри ОДНОГО запуска, stdout 0 байт (живой tmp/review_send/codex_stderr.txt, 16865 байт). ПРИБОР отделён от ПОЛОСЫ: в адресе канала нет суффикса ' -m ', а review_send_run.py:268 печатает его только при заданной модели — значит gpt-6-astra выбрал прибор, а не мы; у двух пакетов разные sha256 (73dbf76a…, b34cd441…), ошибка побайтово одна — от содержимого не зависит. Корпус: 143 вердикта codex = 141 ok (98.6%) + 2 channel_error (1.4%); по суткам ступенька — 11 суток 100% ответов (01.09–11.09), затем 2 из 2 отказа 12.09, ни одного смешанного дня. Окно поломки 12 ч 22 мин 29 с (11.09 19:53:45 → 12.09 08:16:14); второй отказ через 6 ч 31 мин — устойчив, не мигает. ДЕФЕКТ ПОЛОСЫ найден: review_send.py:371 схлопывает любой ненулевой код CLI в одну причину channel_error, review_outbox_queue.py:132 кладёт её в _EXTERNAL → resend, MAX_ATTEMPTS=3, паузы 300+900 — каждый пакет покупает 3 запуска за 20 мин, 2 из них заведомо обречены; тот же 400 по HTTP ушёл бы в ours без повтора (review_outbox_queue.py:212-216 дословно «повтор воспроизвёл бы ту же ошибку»). Асимметрия замерена: у manus 10 вердиктов http_400, у codex те же 400 — 2 channel_error. Кровотечение ограничено CHANNEL_DOWN_STRIKES=2. КРУПНЕЕ ПОВОДА: те же 2 пакета в manus дали channel_idle — 4 отправки, 0 ответов; последний ok у manus 03.09, с 05.09 подряд 20 channel_idle, канал мёртв 9 суток. Значит 04.09–11.09 внешнее ревью вёз ОДИН codex (84 ответа при нуле от соседа), и 12.09 контур встал в ноль — резерва не было неделю, и этого не говорит ни один отдельный вердикт. НЕИЗВЕСТНО (законный третий исход): откуда взялся gpt-6-astra — в .codex/config.toml репо ключа модели нет, а ~/.codex вне проекта, читать не стал (красное outside, обхода не искал). Правки боевого кода НЕ делал — разведка кончается артефактом: названы словами обновление CLI (красное, outside/network, владельцу), пин --codex-model (ручка уже есть, review_send_run.py:1169), и главная — научить classify_codex видеть "status":4xx в хвосте и отдавать http_4xx вместо плоского channel_error. Артефакт закоммичен 3484d4c, 1 файл, 226 строк.
