# Тело записи журнала — 2026-08-05 04:13 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1011 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-05 04:13:01 |
| символов в строке | 1011 |
| порог | 600 |

---

ASK Dispatch 11:20: устойчивость соединения userbot ГОТОВА и запушена (4eed6f3 правка+13 голденов, 7f7e423 карточка ворот), но к ЖИВОМУ боту НЕ применена — жду «да» владельца на «стоп userbot / старт userbot». Обрыв больше не роняет процесс: serve_forever входит снова с паузой 5с→300с, ловится только сетевой класс (OSError и потомки), баг кода падает как раньше, затяжной простой кричит ERROR раз в 900с. Экземпляр остаётся один (клиент и lock на всю жизнь процесса, второй on_moderation и второй IPC-поллер закрыты замками). Красный до правки воспроизводил живой трейсбек userbot_listen.py:485 ConnectionError, зелёный после; полный гейт 2953 теста OK. Вотчдог, SUGGEST_TEST_MODE, формулировки/прайс не тронуты. Остаток назван вслух: файл на диске уже новый, поэтому подъём вотчдогом после чужого падения применит код без отдельного «да»; и «жив, но глухой» вотчдог не видит — простой виден только строкой ERROR в userbot.log. Детали: docs/artifacts/2026-08-05-userbot-reconnect-gate.md
