# Тело записи журнала — 2026-09-01 21:38 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-01 21:38:52 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #104 → done · Причина кейса 8 названа и доказана числами: язык ответа задаётся ОДНОЙ фразой (0.7% промпта) внутри промпта, который на 89.4% написан кириллицей и на первом контакте прямо велит начать «Здравствуйте! …», не глядя на язык клиента. Проверки языка готового ответа в боте нет вовсе. Не чинили — три варианта отданы владельцу. Артефакт закоммичен. **FACT:** commit `393b4d0` в `git log` — `docs/artifacts/2026-09-02-кейс-8-язык-ответа-причина.md`; commit `8983967` — тело журнальной записи. Живой замер: `venv/Scripts/python.exe _scratch_case8_lang_0902/probe_prompt_lang.py` → системный промпт EN-кейса **14305 симв., кир=9311 (89.4%), лат=1107 (10.6%)**. Клиентские файлы: `git status --short` по `suggest.py userbot_listen.py moderation_bot.py moderation_core.py model_name.py trainer*.py` → **пусто**. **Признак сделанности.** Первым действием открыт адрес результата — артефакта не было (`ls` → No such file), работа продолжена. **п.1 Премиса подтверждена.** Красная клетка ровно одна: кейс 8, прогон 1, «язык ответа» 8/9; прогон 2 — 9/9. Факт `ДЗСХабвгде` — это все 4 заглавные кириллические буквы прозы (**З**дравствуйте, **С**пасибо, **Д**ля/**Д**епозит, **Х**отите): ответ был русским целиком, а не с вкраплением. **п.2 Сырьё.** Побайтовая сверка post-hoc **невозможна — и это честное НЕИЗВЕСТНО с названной причиной**: тренажёр входы в модель не сохраняет нигде (`run_case` держит черновик в памяти, на диск идут вердикт-json и отчёт с обрезом 1200 симв., `trainer_run.py:720`). Сверка сделана * …обрезано
