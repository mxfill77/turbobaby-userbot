# Тело записи журнала — 2026-08-02 15:54 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1571 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-02 15:54:46 |
| символов в строке | 1571 |
| порог | 600 |

---

NOTE Orchestrator: задача #194 → done · **Мост чинён по факту, не по расписке.** Три ПК-клиента переведены на durable-транспорт `bridge_http` (форма с VPS), `claim`/`complete` при отказе перечитывают статус. Живая петля echo сработала в тот же день и вскрыла два дефекта уже в правке — оба закрыты. Гейт полный 2855 OK, 5 коммитов запушены, обе строки в мозге, спул пуст. ## FACT-отчёт **1. Транспорт (`bridge_http.py`, новый).** Форма взята с VPS `bridge_client._exchange`: без авто-редиректа, ручной обход цепочки GET-ом по `Location` (≤6 хопов), повтор ВТОРОГО плеча на 404/429/5xx/таймаут. Перешли все три: `pc_orchestrator.Bridge`, `cowork_log_append.get/post`, `brain_writer._get/_post`. Контракт функций сохранён дословно (JSON либо исключение) — спул, `with_retry`, `_retry_read`, обратное чтение работают как работали. Посылка VPS «unauthorized ⇒ действие не исполнено» **не взята сознательно** — замер 02.08 её опровергает; POST не пересылается никогда, отпечаток `doGet` в ответе на POST = `BridgeReceiptLost` («судить обязан факт»). **2. Факт вместо расписки.** `claim_task` — задача в `in_progress` своей полосы ⇒ claim долетел (случай 193). `complete_task` — задача в ЗАПРОШЕННОМ статусе ⇒ запись долетела (положительная проверка: «её больше нет в in_progress» неверно, complete зовут и для approved). Отказы по существу перечитывание не дёргают; не прочитали → прежнее поведение. **3. Класс воспроизведён механически** (`tmp/bridge-redirect-probe-0802a/`, read-only): голый `urlopen` даёт плечи `POST /exec (тело) → GET echo …обрезано
