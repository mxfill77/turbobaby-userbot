# Тело записи журнала — 2026-09-01 21:37 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1430 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-01 21:37:47 |
| символов в строке | 1430 |
| порог | 600 |

---

DONE Dispatch 04:40: разведка провала кейса 8 тренажёра (EN-клиент, язык ответа) на кандидате 0c91a72. Причина: язык ответа задан ОДНОЙ фразой (suggest.py:5240 → :5471, 0.7% промпта) в промпте, который замером даёт 14305 симв. и 89.4% кириллицы против 10.6% латиницы, а на первом контакте ЕЩЁ И прямо велит начать русским литералом «Здравствуйте! …» (:5241–5245, ветка lang не смотрит вовсе); few-shot 18 пар = 15 RU / 3 EN, и провальный черновик — дословные цитаты пар №1 и №6, причём у №6 есть EN-близнец №17. Языковая часть входа обоих прогонов одинакова ПО ПОСТРОЕНИЮ (ph один раз, trainer_run.py:563; detect_lang_from_client — чистая функция, проба даёт en), голова зовётся без temperature и без seed (_cli_llm :5713) — исход решает сэмплинг. Класс: проверки языка ГОТОВОГО ответа в боте НЕТ (все три боевых вызова detect_lang_from_client стоят на ВХОДЕ, _CYR_RE в боевых модулях 0, оба ретрая generate_draft :7429/:7434 язык не судят) — тренажёр ловит то, чего бот сам не проверяет. Не чинили: три варианта с ценой и риском отданы владельцу. НЕИЗВЕСТНО: побайтовая сверка входов post-hoc невозможна (тренажёр входы в модель не сохраняет), язык живого FAQ не замерен, какая голова отработала в каждом прогоне (при живом --fallback-model прогоны могли идти РАЗНЫМИ головами). Артефакт docs/artifacts/2026-09-02-кейс-8-язык-ответа-причина.md, коммит 393b4d0 — только артефакт, клиентские файлы не тронуты.
