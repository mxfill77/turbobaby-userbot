# Тело записи журнала — 2026-08-01 05:09 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **1572 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-08-01 05:09:28 |
| символов в строке | 1572 |
| порог | 600 |

---

NOTE Orchestrator: задача #154 → done · Из 78 мест про **разрешения — 34** (ПК 25, VPS 9); остальные 44 — данные/поведение 33, косметика 11. Деление 36/42 резало по механизму починки, а не по цене: «одно правило» накрывает 23 из 34. **Десять мест поиска по тексту работают КАК ЗАДУМАНО — жёсткое правило их сломает.** Артефакт: `docs/artifacts/2026-08-01-root-a-permission-split.md` Проверял по живому дереву (`6d72804`), не по каталогу: гард вырос 3341 → 3742 строк, после сводов легли `53bc936` и `2ec0481`, номера строк каталога устарели — места искал по именам символов. ## Группа «РАЗРЕШЕНИЯ» — 34 места Критерий строгий: место влияет на (a) родится ли карточка, (b) её класс, (c) что откроет ответ владельца, (d) пройдёт ли красное. Ключ к счёту: `card_object()` → `_card_fields()` теперь то самое значение, с которым `approval_covers()` сверяет «да» (`:2979`) — **извлекатели объекта стали кодом разрешений**. ### Гард ПК (19) | # | суть | правка меняет? | как задумано? | |---|---|---|---| | **A-29** | **ЖИВОЕ ×2.** Обвинитель читает `scan`, смягчитель `_env_reach(cmd)` — листинг каталога становится «обращением к секретам» | **ДА** — #141 и #144 станут зелёными | нет | | **A-1** | три зелёных шортката стоят ВЫШЕ единственного сканера тела питона | **ДА** | нет | | **A-3** | зелёное по имени `test_*.py`; канон `_is_repo_tracked` в той же строке | **ДА** | нет | | **A-2** | карве-аут инвертирован: `"memory.db" not in blob` снимает красное со всех SQL-записей | **ДА** | **детектор — ДА** | | **A-30** | боевой токен в теле; о …обрезано
