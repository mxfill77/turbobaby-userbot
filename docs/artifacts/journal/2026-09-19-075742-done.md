# Тело записи журнала — 2026-09-19 07:57 UTC

Вынесено АВТОМАТИЧЕСКИ писателем `cowork_log_append.py`: строка журнала была **3120 символов** при пороге **600** (журнал — индекс, а не хранилище тел).
В `cowork_log` ушёл итог и ссылка на этот файл.

| поле | значение |
|---|---|
| записано | 2026-09-19 07:57:42 |
| символов в строке | 3120 |
| порог | 600 |

---

DONE Dispatch 15:15: 67k REVIZOR tik 1909 — РЕШЕНИЕ: ожидание на тик ревизора НЕ НУЖНО. Премисы: П1 ДА (expectations_pc.py — «ревизор» только в докстринге, строки 197/208/213/220; KINDS:534 его не знает; попутно номер О7 уже ЗАНЯТ — «цена клиенту числом», 05.09, следующий свободный О8). П2 НЕТ, и это главная поправка: О4 (life_state:1159, pulse_due:1191) берёт фактом heartbeat через turn_state (О2), строк ревизора не читает ни одной веткой — докстринг:230 «О4 переиспользует факт О2»; строки ревизора там названы доказательством ЗАДНИМ ЧИСЛОМ (корпус 14.08). Доказательство оборота у О4 РОВНО ОДНО и к ревизору отношения не имеет; слепоты не наследует, направление обратное — вставший ревизор делает поток следов реже, значит пульс звучит ЧАЩЕ. П3 ДА С ПОПРАВКОЙ: pc_orchestrator.review_audit_tick.json есть (26 байт, одно поле ts), но это метка ступени D (REVIEW_AUDIT_MIN_SEC=600с, :11464), а тик ревизора живёт в pc_orchestrator.revizor_state.json (REVIZOR_HOURS=6, :9798) — разница периодов 36x. п.1 предмет: не находки (при молчащих клиентах окон ноль ПО УСТРОЙСТВУ), а ЗАМКНУТЫЙ ПРОГОН ОТБОРА — продвижение last_run (_revizor_write_state:10028, advance=delivered:11061) плюс строка «ревизор: since=… → окон N» в логе. п.3 замер: 225 тиков 23.07→19.09 (58.2 сут), 224 интервала, дыр >=24ч ноль; медиана 6ч01м21с, p90 6ч17, p95 6ч34, p99 11ч51м52с, min 6ч00м00с, max 23ч06м05с (05–06.08, простой самого демона); законный максимум 11ч52м14с, единственный законный порог 13 ч (на 6 ч краснеет 223 из 224). п.4 отрицательный тест «свежий след покойника» (каденция живая, last_run заморожен, порог у обоих 13 ч): судья по возрасту метки краснеет 0 из 117, различитель по продвижению last_run — 116 из 117, первое покраснение ровно через 13.00 ч. Второй тест на боевом коде: ступень D пишет метку ДО бегуна (:11528 против :11531) и оставляет свежей при падении (:11536–11539 «метка уже сдвинута») — продукт есть у 1.6% следов (41 строка исхода против <=2549 тиков с 01.09). п.5 цена решения: класс «ревизор мёртв при живом обороте» остаётся непокрытым СТРУКТУРНО (heartbeat в poll_once:5273, maybe_revizor зовётся ПОСЛЕ на :13357, исключение глотает except :13378), частота класса на замере 0 за 58.2 сут, grep «метку прогона НЕ двигаем» по трём логам → 0. Судья по продукту запрещён числом: 28 ложных из 28 на здоровом ряду, макс. пауза между непустыми прогонами 126ч29м, пустых тиков подряд до 20. Живой повод разгадан: 19.09 13:50 минус 15ч14 = 18.09 22:35:46 с точностью 14 с — показ мерил последний НЕПУСТОЙ прогон, а тик был свежий (3ч14 при периоде 6ч), то есть ожидание на возраст тика 19.09 промолчало бы. НЕИЗВЕСТНО честно: какой именно показ Штаба дал 15ч14 (формы «тик старый» на ПК нет, revizor_age.py — файл сервера; реконструкция, не прочитанный источник) и снимался ли когда-нибудь DIALOG_REVIZOR (.env читать запрещено; по косвенному следу дыр в 224 интервалах нет). Ревизора не будил, не перезапускал, не чинил; кода демона не правил; боевых состояний не писал; наружу ничего не слал. FACT: commit bc798724 в git log, запушен 2a8a1561..bc798724 main -> main, 1 файл 262 строки.
