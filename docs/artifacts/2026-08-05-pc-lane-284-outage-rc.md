# ПК-полоса: разбор простоя вокруг задачи 284 (RC-заход, read-only)

**Замер:** 2026-08-04 19:19–19:30 UTC (локально 2026-08-05 02:19–02:30, TZ = UTC+7).
**Режим:** только чтение. Процессы не тронуты, ничего не поднято и не погашено, `.env` не открывался,
боевых записей нет кроме финальной строки журнала.

**Заход исполнил ИМЕННО пункты 1–5 задания владельца** (перекличка / 284 / мост / ворота / клиентский бот).

---

## 0. Главное одной строкой

Полоса стояла **не из-за моста и не из-за ворот**, а из-за того, что **сеть ПК лежала 76 минут**
(18:02:55 → 19:18:11 UTC). Задача 284 при этом **не висит** — она закрыта `failed` ещё в 18:18:48 UTC.
Единственное, что осталось лежать после восстановления сети, — **`userbot_listen`**: контур-вотчдог
залатчен по нему на «3 смерти подряд» и сам его больше не поднимет.

---

## 1. Перекличка (МЁРТВЫЙ — первым)

### МЁРТВ: `userbot_listen.py` — 0 экземпляров

| факт | значение |
|---|---|
| экземпляров сейчас | **0** |
| последний долгоживущий процесс | работал с `2026-07-31T09:07:18Z`, остановлен `2026-08-04T18:13:16Z` |
| подъём 1 (вотчдог) | PID 16412, старт `18:18:50Z` → стоп `18:19:56Z` (**66 с**) |
| подъём 2 (вотчдог) | PID 21600, старт `18:38:47Z` → стоп `18:39:53Z` (**66 с**) |
| PID 16412 / 21600 сейчас | оба МЁРТВЫ (проверено `Get-Process`) |
| простой клиентского слушателя | **73 мин** (с 18:13:16Z на момент замера) |
| коммит процесса | последние два запуска — с дерева на `921c9c6` (HEAD на 18:38Z) |

Дословно из `pc_orchestrator.log` (время в логе локальное, +7):

```
2026-08-05 01:19:00,741 WARNING контур-вотчдог: userbot МЁРТВ (лежал 331с, смерть 1/3) → подъём ok=True, PID после=[16412]: userbot запущен (PID 16412).
2026-08-05 01:38:58,394 WARNING контур-вотчдог: userbot МЁРТВ (лежал 1129с, смерть 2/3) → подъём ok=True, PID после=[21600]: userbot запущен (PID 21600).
2026-08-05 01:58:44,589 ERROR   контур-вотчдог: userbot умер 3 раз подряд — СТОП попыток, нужен разбор
```

Почему умирал — из `userbot.log`, каждый запуск одинаково: 6 попыток коннекта к `91.108.56.105:443`,
все `Attempt N at connecting failed: TimeoutError`, затем `--- userbot_listen ОСТАНОВЛЕН ---`.
То есть умирал он **от той же сетевой дыры**, а не от своего дефекта.

### Живые

| процесс | PID | экз. | старт (UTC) | аптайм | коммит процесса |
|---|---|---|---|---|---|
| `pc_orchestrator.py` | 6376 | 1 | 2026-08-04 18:01:25 | 85 мин | **921c9c6** — из собственного стартового баннера процесса |
| `pc_agent.py` | 12124 | 1 | 2026-07-31 05:58:42 | 6562 мин ≈ 109.4 ч | **e6e6a31** (HEAD на момент старта) |
| `moderation_bot.py` | 10080 | 1 | 2026-07-31 05:58:50 | 6561 мин | **e6e6a31** (ребёнок pc_agent, ни разу не перезапускался) |
| контур-вотчдог | — | — | — | — | **не процесс**: поток внутри PID 6376 (`CLIENT_WATCH_SEC` = 5 мин) |
| `rc_supervisor.py` (+ `claude rc`) | 12444 / 11840 | 1 / 1 | 2026-07-30 17:26:07 | — | RC-полоса, жива |

Коммит демона — не вывод из дерева, а его собственная запись в память процесса при старте:

```
2026-08-05 01:01:25,386 INFO === ДЕМОН СТАРТ (lane=pc, poll=60s, task_timeout=2700s, approval_ttl=1800s, claude=C:\Users\mxfill1\.local\bin\claude.EXE, commit=921c9c6) ===
```

Почему `pc_agent`/`moderbot` остались на старом коммите — сказал сам демон:

```
2026-08-05 01:01:24,997 INFO self-update дети: pc_agent изменён — ЖДЁТ РУЧНОГО рестарта (Планировщик/сам подхватит), чужими руками не трогаю
```

**Латч вотчдога.** `pc_orchestrator.py:5332` — `_client_watch_state = {}  # name -> {"last_raise", "deaths", "halted"}`,
состояние **в памяти демона**. `CLIENT_MAX_DEATHS = 3`. Сбрасывается только тем, что процесс снова
окажется живым, либо рестартом демона. Пока `halted=True` — вотчдог userbot **не тронет**, даже
теперь, когда сеть вернулась.

---

## 2. Задача 284

**Не висит. Закрыта `failed` в 18:18:48 UTC — 61 мин до замера.** Дословно:

```
2026-08-05 01:04:32,013 INFO    CLAIM id=284 in_progress
2026-08-05 01:04:32,519 INFO    RUN id=284 (timeout=2700s, попытка 1/2)
2026-08-05 01:15:05,582 WARNING FAIL причина=exec_error окно=04.08 18:04–18:15 UTC следы: коммитов=0, записей журнала=0
2026-08-05 01:15:05,582 INFO    METRICS task=284 lane=pc type=read mode=prod src=pc_orchestrator.py model=claude-opus-5 effort=xhigh start=2026-08-04T18:04:32+00:00 end=2026-08-04T18:15:05+00:00 dur_s=633.56 outcome=failed attempts=1 selfheals=0 tokens_in=na tokens_out=na
2026-08-05 01:18:48,174 INFO    COMPLETE id=284 status=failed
```

Причина провала — дословно из текста, который демон передал думателю самопочинки:

> `провал [причина=exec_error · ошибка выполнения]: claude exit=1: API Error: Unable to connect to API (ConnectionRefused). Следов работы в окне 04.08 18:04–18:15 UTC нет (коммитов 0, записей журнала 0).`

Самопочинка тоже не отработала — по той же причине (её `claude` не достучался):

```
2026-08-05 01:18:06,065 WARNING task-selfheal: думатель не отработал (Command '[...claude.EXE, -p, ...]' timed out after 180 seconds) — fail-safe
```

**Живого дочернего исполнителя 284 нет** — процесс умер вместе с задачей. Единственный живой
ребёнок демона сейчас — PID 23004, но это **задача 290** (см. §6).

**Что успела сделать: ничего.** Проверено тремя независимыми способами:
- `git status --porcelain -uno` → пусто; tracked-изменений **0**, untracked **178** (это накопленные `docs/artifacts/` и `artifacts/`, к 284 отношения не имеют);
- коммитов в окне 18:04–18:20 UTC — **0** (`git log --since/--until`), `git reflog` последним держит `921c9c6` от 18:00:04 UTC;
- файлов, изменённых в окне (кроме логов/pyc), — **один**: `pc_orchestrator.autofetch.json` в 18:19:22 UTC, его пишет сам демон, не задача.

Задача 284 была «отправитель ленты на ПК-полосе» — заметки о старте/стопе `userbot_listen` и
`moderbot` в тему 829. Иронично ровно то, что она не доехала именно в тот час, когда `userbot_listen`
пять раз стартовал и умер молча — то есть ради этого события её и заводили.

---

## 3. Мост: claim проходит СЕЙЧАС

**Да, проходит.** Живой факт — демон взял новую задачу через минуту после возврата сети:

```
2026-08-05 02:20:47,874 INFO CLAIM id=290 in_progress
2026-08-05 02:20:48,339 INFO RUN id=290 (timeout=2700s, попытка 1/2)
```

Ошибок `get_pending` после 19:20:47 UTC — **0**. Живой исполнитель PID 23004 подтверждает, что claim
не только записался, но и был прочитан назад (иначе `RUN` не начался бы).

Собственной пробы claim я не делал: claim — боевая запись, а заход read-only. Факт взят у живого
демона, который сделал её штатно.

**Это НЕ повтор класса 193.** По 284 расписка действительно отбилась — но запись **долетела**, и
демон это проверил обратным чтением, дословно:

```
2026-08-05 01:04:32,012 WARNING CLAIM-VERIFY id=284: расписка claim — отказ (BridgeReceiptLost), а задача in_progress на моей полосе → claim ДОЛЕТЕЛ, работаю штатно (сироты нет)
```

Разница с 193 принципиальная: тогда запись не проходила и задача висела **без исполнителя**. Здесь
исполнитель был взят и запущен, а убила его сеть уже внутри работы.

### Настоящая причина простоя полосы — сеть ПК лежала 76 минут

Окно: **18:02:55 → 19:18:11 UTC**. Пять независимых свидетелей:

| канал | дословно |
|---|---|
| Мост | `01:02:55,487 WARNING get_pending(in_progress) ошибка: TimeoutError` … `02:18:11,117 WARNING get_pending(new) ошибка: URLError` — **19** отказов `in_progress` + **18** отказов `new` |
| GitHub | `01:19:22,524 WARNING авто-фетч: git fetch origin не удался — tal: unable to access '…': Failed to connect to github.com port 443 after 21115 ms: Could not connect to server` |
| Telegram (moderbot) | `02:18:12,177 \| сетевая ошибка (не падаю, PTB переподключится): NetworkError: httpx.ConnectError: [Errno 11001] getaddrinfo failed` — падал **DNS** |
| Telegram (userbot) | 6× `Attempt N at connecting failed: TimeoutError` на `91.108.56.105:443` в каждом из 3 запусков |
| API Anthropic | `claude exit=1: API Error: Unable to connect to API (ConnectionRefused)` — этим убило 284 |

**Момент возврата сети зафиксирован точно:** сокеты `pc_agent` (PID 12124) и `moderbot` (PID 10080)
к `149.154.166.110:443` (api.telegram.org) созданы в **19:18:19 и 19:18:20 UTC**. Первый успешный
claim — через 2.5 минуты.

### Побочный ложный диагноз, который стоит знать

Всё окно демон писал:

```
2026-08-05 02:16:23,577 WARNING детект пробуждения ПК: скачок wall-clock 186с (>60+60; из них сна 0с) — grace вотчдога 120с
```

**ПК не спал** — сам детект честно говорит «из них сна 0с». Цикл блокировался на сетевых таймаутах
(два `get_pending` по ~87 с каждый), и это выглядело как пробуждение. Строка ~24 раза за окно.
Родственник класса #171: разбор, начатый с этой строки, уедет в сон вместо сети.

---

## 4. Ворота контура: 2 раза, оба в 18:01, повторов 0

**Числом: сработали ровно 2 раза, оба в 18:01 UTC. С тех пор — 0 повторов за 85 минут / 18 полных
циклов опроса.** Дословно обе строки:

```
2026-08-05 01:01:14,016 ERROR ворота контура (реконсиляция детей на новый коммит): применение 921c9c6fa к «userbot,moderbot» ОСТАНОВЛЕНО — клиентские файлы: log_setup.py
2026-08-05 01:01:25,012 ERROR ворота контура (реконсиляция детей после self-update): применение 921c9c6 к «userbot,moderbot» ОСТАНОВЛЕНО — клиентские файлы: log_setup.py
```

**Каждый цикл это НЕ повторяется, и это не «отвалилось» — это разовое событие на смену коммита.**
Реконсиляция запускается при изменении HEAD; авто-фетч всё окно падал (github недоступен), HEAD не
двигался — значит и повторяться было не на чем. Всего строк `ворота контура` за всю историю лога — **24**.

**Решения оно не ждёт.** Карточки/кнопки ворота не рождают: `ERROR` отработал, ветка закрылась.
Практическое следствие: `pc_agent` и `moderbot` остались на **e6e6a31**, а в дереве новее лежат их
общие зависимости — `log_setup.py` (05.08 00:50 лок.), `suggest.py` (31.07 19:24), плюс
`bridge_http.py`, `brain_writer.py`, `card_duty.py`, `pretool_guard.py`, `reviewer.py`,
`cowork_log_append.py`. Собственные модули детей не менялись (`pc_agent.py` — 30.07, `moderation_bot.py` — 23.07).

Поведение штатное и по замыслу: `client_contour.py:75` — `CLIENT_ENTRIES = ("userbot_listen.py", "moderation_bot.py")`,
`log_setup.py` лежит в их транзитивном import-замыкании, признак fail-closed. Ворота сделали ровно то,
ради чего заведены: не выкатили клиентскому контуру коммит, тронувший клиентский файл.

---

## 5. Клиентский бот сейчас — живым откликом

### `moderbot` — ДОСТУПЕН

Не по PID, а по отклику:

- ESTABLISHED-сокет PID 10080 → `149.154.166.110:443`, создан **19:18:19 UTC**;
- `moderation_bot.log`, 19:18:58 UTC: `Run time of job "job_poll_new (trigger: interval[0:00:03], next run at: 2026-08-04 19:18:59 UTC)" was missed by 0:00:01.882413` — опрос идёт с интервалом 3 с и уже нагоняет;
- `moderation_ipc.db` записан в **19:25:57 UTC** — петля модерации пишет прямо сейчас.

### `userbot_listen` — НЕ ДОСТУПЕН

0 процессов, простой **73 мин** с 18:13:16 UTC, вотчдог залатчен. Последнее принятое живое сообщение
клиента — **18:07:06 UTC** от `@PHUKET_NICK` (запрос на cbr650r на 3–4 месяца с 15 августа).
Всё, что писали в личку после 18:13:16 UTC, ПК **не слушал**. Telethon добирает историю при старте,
но проверить это можно только подъёмом — а он под запретом до слова владельца.

---

## 6. Отдельно: параллельный заход демона (задача 290)

В 19:20:48 UTC демон запустил PID 23004 с задачей **290** — это почти дословно та же read-only
перекличка ПК (284/перекличка/очередь/ворота). Он уже положил свой артефакт
`docs/artifacts/2026-08-05-pc-rollcall-290.md` (19:27 UTC) и напишет **свою** строку в `cowork_log`.
То есть по одному инциденту в журнале будет **две** записи — моя (RC) и его. Это не дубль-дефект,
а два независимых захода: владелец поставил диагностику и в RC-канал, и в очередь моста.

---

## РАЗВИЛКА для владельца (ничего не сделано, жду слова)

Сеть вернулась в 19:18 UTC. Полоса моста ожила сама. **Сам не оживёт только `userbot_listen`** —
его держит латч `halted` в памяти демона.

| вариант | что делает | цена |
|---|---|---|
| **А. Поднять только userbot** тем же штатным путём, каким его поднимает вотчдог (venv-spawn через pc_agent) | клиентская личка снова слушается за ~1 мин | латч `halted` в памяти демона **останется** — если userbot снова умрёт, вотчдог опять не поднимет, пока не оживёт сам процесс |
| **Б. Рестарт `pc_orchestrator`** | сбрасывает `_client_watch_state` целиком, вотчдог снова работает штатно и сам поднимет userbot | обрывает **живую задачу 290** (PID 23004) — она уйдёт в `failed`/сироту |
| **В. Ничего не делать сейчас** | дождаться конца 290, потом Б | клиентская личка не слушается ещё ~N минут |

Отдельным пунктом, **не срочно**: `pc_agent` и `moderbot` живут на `e6e6a31` уже 109 часов и ждут
ручного рестарта — но их рестарт сейчас (а) уронит moderbot, который единственный держит клиентский
контур, (б) применит к ним новый `log_setup.py`, который ворота сознательно не пустили.
Это отдельное решение, не часть аварии.

**Мой выбор, если спросят: В → потом Б.** Дать 290 доработать (она read-only, ничего не сломает),
затем перезапустить дирижёра — это единственный вариант, который чинит и userbot, и залатченный
вотчдог одним движением, не оставляя мину на следующую смерть.
