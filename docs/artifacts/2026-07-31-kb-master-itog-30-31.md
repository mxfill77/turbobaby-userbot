# Итог 30–31.07.2026 записан в KB_MASTER — факты записи и обратного чтения

**Дата:** 31.07.2026, 14:45 (локальное время ПК)
**Канал:** `brain_writer.py` (единственный легальный писатель в Brain-доки с ПК)
**Док:** `name:index` (KB_MASTER — единая карта проекта)
**Команда:**

```
PYTHONIOENCODING=utf-8 venv/Scripts/python.exe brain_writer.py \
    --name index --place top --backup-tag index_itog_3031 - < tmp/kb_master_itog_3031.txt
```

Текст подан **из файла в stdin**, а не через argv: argv на Windows коверкает кириллицу
(класс мохибейка 29.07, из-за него пять строк легли в мозг «С‚РёРї РўРЎ»). Файл проверен
до отправки: 6580 байт / 3912 символов, перевод строки только `\n` (CR=0), BOM нет,
знаков подмены `U+FFFD` — 0, не-ASCII символы только ожидаемые: `« » – — → ─`.

## Что легло

Раздел вставлен **в самое начало** дока, существующее не тронуто. Блок начинается линией-
разделителем (40× `U+2500`) — тем же, каким док уже отделяет верхний блок «КАРТА МИГРАЦИИ»
от тела KB_MASTER. Нижней границей блока служит **уже стоявший** разделитель дока, поэтому
дублирующей линии не появилось; структура дока читается как раньше:

```
──────  ← новый разделитель
=== ИТОГ 30–31.07.2026 ===
…
──────  ← разделитель, стоявший в доке ДО записи
=== КАРТА МИГРАЦИИ, зафиксировано 29.07.2026 ===
…
──────
KB_MASTER — ЕДИНАЯ КАРТА ПРОЕКТА TurboBaby…
```

## Обратное чтение (по живому доку, не по локальной склейке)

| проверка | факт |
|---|---|
| длина ДО | **86 661** символ (853 строки) |
| длина ПОСЛЕ | **90 573** символа |
| прирост | **+3912** = 3911 символов блока + один `\n` — **ровно длина вставки, ни символа сверх** |
| блок дословно | `back.startswith(body + "\n")` → **True**, вхождений в доке — **1** |
| старый текст цел | `back[3912:] == бэкап` → **True**, первое расхождение — **none** |
| кириллица | **50 632** кириллических символа в доке, читаются глазами (см. фрагмент обратного чтения) |
| знаки подмены `U+FFFD` | **0** (и до записи было 0 — сверено на том же чтении) |
| знаки `?` | **18**, все до единого **из старого текста** (`Q_OLD=18`, `Q_BLOCK=0`) — то есть ни один символ не выродился в «?» |

Верификация писателя (встроенная, до моей): гард усыхания, сверка длины отправленного
с длиной в доке, дословный поиск текста, счёт вхождений цельными строками = 1.
Плюс независимая проверка отдельным чтением дока — числа выше из неё.

**Бэкап старого текста (откат = запись этим файлом):**
`tmp/brain_backup_index_itog_3031_20260731-144515.txt` (86 661 символ).

## Дословный текст вставки

```
────────────────────────────────────────
=== ИТОГ 30–31.07.2026 ===

СДЕЛАНО (все с живым доказательством):
- Реестр мозга: 28 записей, мёртвый roadmap_master снят, два спека
  зарегистрированы (49d08df). Документы лежали в Brain/_archive.
- Класс кодировки закрыт на трёх полосах разом (fd5388f), поставлен
  страж — он сразу поймал пропущенное место.
- Доставка одобрений починена (aed7a08, e19a4ba). Цена класса:
  из 10 нажатий владельца доходили 3, семь задач умерли впустую.
  Попутно закрыта дыра: чтение секретов через Select-String -Path
  проезжало гардом зелёным.
- Ворота клиентского контура (a8f8822): признак — транзитивное
  замыкание импортов от userbot/moderbot, не список имён. Fail-closed.
  Удержали коммиты цепей 4 и 44.
- Тренажёр выдаёт вердикт (6478a39, 1318c37). Красный держит коммит —
  доказано живьём, 10/12 кейсов. Чужой коммит не засчитывается.
- Честные статусы обеих полос (5f2be1c, 1597cbe, 7de305d): код причины
  провала, следы работы в окне. Формулировка «в окне есть работа»,
  а не «работа выполнена» — окно не равно авторству.
- Зомби-проба канала RC (a773df7, 3a097a9): порог 43200с выведен из
  живых чисел, было 33 ложных гашения в сутки, стало 0.
- Шум карточек: четыре группы разведены по действию, затем закрыт
  КОРЕНЬ (4ecbb5b, 2444073) — перебраны все 14 красных признаков,
  поставлен замок: правило по старому образцу даёт ошибку ПРИ ИМПОРТЕ.
  Второй проход нашёл дыру в самом замке.
- Два вида карточки по цене ошибки (ec8358d): высший — живые таблицы,
  деньги, клиентский контур, выкатка прода, удаление вне tmp, снос
  процессов — НЕ открывается коротким «да N», нужен ответ с объектом.
  Без объекта карточка высшего вида не выписывается вовсе.
- Fable убран везде, включая нормализатор алиасов. Основная opus-5,
  запасная opus-4-8 на обеих полосах.
- Сон ПК: корень — ручное усыпление из меню Пуск, не автоматика.
  Клиентский бот был недоступен 12ч10м за неделю.
- Ротация pricing.log (3237317, d352655): файл был МЁРТВ 9 суток,
  каждая строка молча выбрасывалась.
- Журнал стал индексом (0fb0348): тело >600 символов уходит в артефакт,
  за неделю 268К → 84К символов.
- Документы: две сверки и две починки (d14d450, a1d7824, dac7cd5,
  8e4d5d8) — около 153 расхождений. Вторая сверка перепроверила первую
  адверсариально и нашла в ней 19 неточностей, 12 из них меняли вердикт.
- САМОПОЧИНКА ШАГА ВКЛЮЧЕНА (caa28e6, 910f4b8, d95315e, 0038a1f,
  c336193, aee6fb4, e6e6a31). Подтверждена на живом процессе:
  демон PID 17568, баннер STEP_SELFHEAL=1. Выключение — стоп-файл
  pc_orchestrator.step_selfheal.off, одна строка без рестарта.
  На живых задачах ещё НЕ срабатывала ни разу.

ОТКРЫТО:
- PLAN_ADAPT выключен — включать после наблюдения за самопочинкой.
- Уборка мозга: 20 документов никто не читает, 7 дублей, 7 со
  смешанными ролями. Это остаток этапа 1.
- Дежурный на ПК: из 127 карточек бесспорных 29. Главный упор —
  ответить «да» с ПК было невозможно, чинится сейчас.
- Самообновление демона утаскивает соседей: pc_agent умер и не
  вернулся 17 минут, сигнала не было.
- Бизнес-список отложен до слова владельца: 10 классов дефектов
  черновиков, сводка ревизора id=6, ТО у MT-03.
- Этап 2 карты — пилот памяти платформы на журналах.

УРОКИ, вошедшие в правила:
- Окно с коммитом НЕ равно авторству работы.
- Класс-фикс кладётся на обе полосы сразу, иначе зеркальная дыра
  вылезет через неделю (проверено на зомби-пробе: сервер починили
  24.07, канал забыли).
- Перезапуск живого процесса — ОТДЕЛЬНОЕ разрешение, общее «да»
  его не покрывает. Наступали трижды за сутки.
- Досыл к задаче становится НОВОЙ задачей. Не досылать — отправлять
  заново целиком.
- Перед повтором упавшей задачи — сначала статус очереди.
- Гипотезы Штаба за сутки ошиблись четырежды (креды вместо пробы,
  сервер вместо локального реапера, «работа сделана» по совпадению
  окна, разошедшиеся копии документа). Гипотезу проверять командой,
  а не аргументом.
```

## Что НЕ делалось

Текст владельца не редактировался, не сокращался и не переформулировался — это
запись, а не сочинение. Ни одна существующая строка дока не изменена и не удалена
(доказательство — побайтовое равенство хвоста бэкапу).
