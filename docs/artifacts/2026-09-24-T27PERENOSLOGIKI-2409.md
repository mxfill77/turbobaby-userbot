# Перенос логики TurboBaby в gHashTag/turbobaby-user-bot — что сделано 24.09.2026

Задача владельца (сессия Claude Code на ПК, 23–24.09.2026): «наша задача — основную всю нашу
логику перенести туда, посмотреть, как она перенесена, и чтобы там всё работало… делай всё то,
чего не хватает». Репозиторий — `gHashTag/turbobaby-user-bot`: Rust axum + Dioxus wasm Mini App,
Telegram-бот и корпус контрактов `.t27`. Всё ниже — замеры с командой или ссылкой, без пересказа
на слово.

## 1. Что влито в `main` (PR от имени gHashTag, каждый после зелёного CI)

| PR | коммит влития | суть |
|---|---|---|
| #55 | 2d76554 | гейт 3: 66 → 168 привязок контракт↔код |
| #56 | 393891d | семьи статусов, отмены и денег; **утечка Telegram ID в таблице лидеров закрыта** (`public_display_name` на чтении и на записи); dist пересобран |
| #57 | f0640f8 | e2e и мониторинг смотрят на свой origin `turbobaby-bot-production.up.railway.app` |
| #58 | 68d5bcc | `scripts/postdeploy-smoke.sh` (только GET) и `docs/ROLLBACK.md` |
| #59 | 27ddcfc | CLICK 125 → только NMAX 155 (из парка); нет «Свободно N из M» из посева; admin Add — успех только после 2xx |
| #60 | 5880c80 | **18 зон доставки Пхукета по решению владельца 24.09** (миграция 087; Аэропорт 690; минуты не показываются нигде; 4 зоны Ко Пангана деактивированы, не удалены); dist — бандл `a41a77c967b58518` |
| #61 | f3fb38f | нет QR PromptPay на сумму < 1 сатанга (422); залог в заказе обязан совпасть с опубликованным залогом модели, за байк |
| #62 | f49c372 | гигиена контрактов: цитаты мозга перечитаны, один владелец на факт (перепись миграций — schema-provenance), привязки ride.js, отрицательный контроль гейта посева, страж словаря каннабис-эпохи, полы гейта 1 = замер; handover обновлён |

## 2. Числа на итоговом дереве (main после #62, прогон 24.09)

- 4 гейта: 45 контрактов; **9859/9859** утверждений исполнено; **227** привязок (41 контракт, 78 файлов
  исходников; 214 пар из 5484 объявленных констант = 3.9 %); посев OK. Утром было 9535 и 198.
- `cargo test --features backend -j 2`: **2347 passed, 0 failed**, 133 ignored (интеграционные с БД).
- clippy (backend в форме CI и wasm32) и fmt — чисто; CI каждого PR — 17 из 17.
- Смок собранного dist в встроенном браузере: бандл монтируется; в консоли только ожидаемые ошибки
  Telegram CloudStorage и 404 на `/api` (без бэкенда); строки «30-45» в новом wasm 0 (в прежнем 1).

## 3. Чего НЕТ у клиентов (честно)

**Прод не задеплоен.** Merge в `main` не деплоит ничего (`deploy.yml` пропускает себя без
`RAILWAY_TOKEN`/`RAILWAY_SERVICE`). Последний read-only смок: прод отдаёт сборку `4a5aa72`
(бандл `c3a91ae8dc97ab9e`). Значит, у клиентов сейчас: зоны Ко Пангана с минутами 30-45, таблица
лидеров с утечкой ID, CLICK 125 с чужими моделями. Всё это исправлено в `main` и ждёт деплоя.

## 4. Решения владельца, которые нужны (подробно — `docs/t27-handover.md`, раздел 3, в репо)

- **Деплой `main`** — ручной `railway up` из чистого дерева (`docs/ROLLBACK.md` §3B) или токен Railway для CI.
- Политика бэкапа базы `woody` (не записана нигде; миграция 083 необратима).
- Источник цены (ценовая дверь — заглушка) и доступ к Bridge; правило вместимости и глубина брони.
- Залог: за байк или за строку; залог в USD/EUR; сниженный менеджером залог; событие фрода на отказ.
- Имя зоны «Аэропорт» (мозг: сам аэропорт не обслуживается, только отели рядом); пояс 1490 и отсечка 17:30.
- Каталог: фраза «сейчас все заняты» из снимка; `bike_card.rs`; строки CLICK 125 в `knowledge_base`.
- Владелец цены nmax-155 (pricing-honesty или rental-terms); копирайт для мёртвых ключей меню.

## 5. Мозг

- Записано этой сессией (через `brain_writer`, с бэкапом и обратным чтением): решение владельца
  24.09.2026-1 о зонах Mini App — `business_rules`, строки 1712–1734 (прочитано назад 24.09).
- Кроме журнальных строк `cowork_log`, в мозг ничего не писалось; `knowledge_base` про CLICK 125 не тронут — это текст владельца.

## 6. Как проверено отсутствие мусора в PR

- `dist/version.txt.br` в коммиты не попал ни разу (untracked, так и оставлен).
- Ветки перед влитием перебазированы с `core.autocrlf=false`; ссылки `файл:строка` сверены
  инструментом «цитата → дерево, где написана → строка сейчас» (scratchpad сессии, `citecheck2.py`).
- Резервные локальные ветки до сворачивания коммитов: `backup/orders-hardening-a206c37`,
  `backup/spec-hygiene-ae6bd16` (в `D:/turbobaby-bike-bot`, не пушились).
