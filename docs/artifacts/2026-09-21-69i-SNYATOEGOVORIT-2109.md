69i SNYATOEGOVORIT 2109

# Снятая находка должна говорить: где она выбывает и что при этом записывается

**Полоса:** ПК · **Заход:** 69i из ящика заданий Штаба (ключ `69i.2109`) · **Дата:** 21.09.2026
**Вид работы:** РАЗВЕДКА. **Исход: ЗАКОННАЯ ВЕТВЬ задания — мест выбывания БОЛЬШЕ ОДНОГО (восемь),
и потому не починено ни одно.** Перечень с адресами — п.2, остановка — там же.

**FACT (живое доказательство).** Два собственных read-only счётчика, прогнанные живым кодом
контура (`review_intake_run.read_records` + `review_intake.merge` + `review_audit.rank`) на
коммите `acffb823`, дерево ПК, 21.09.2026:
`files_in_inbox=293 · skipped_files=112 · records=543 · claims=247 · B_claims_placed=243 ·
D_shown=60 · claims_never_shown=187 · snyato_by_registry_B=183 · named_by_text=4 · silent=183`.
Выводы счётчиков лежат рядом с ними: `tmp/snyatoe_govorit/schet_69i.out.txt` и
`tmp/snyatoe_govorit/schet_nazvany_69i.out.txt`.

**Кода не правлено ни строки.** Ни `review_intake.py`, ни `review_intake_run.py`, ни
`review_audit.py`, ни `review_audit_run.py`, ни `pc_orchestrator.py`. Порогов не менял, состояний
(`review_intake_state.json`, `review_audit_state.json`) не трогал, в очередь не ставил, наружу не
ушло ни одного пакета и ни одного сообщения, владельцу из проверки не уходило НИЧЕГО, клиентского
контура не касался, процессов не трогал, `.env` и конфиги не читаны, ничего не удалено.

**Место для временного названо явно:** `tmp/snyatoe_govorit/` — создано, НЕ удалено. В нём ровно
четыре файла: два счётчика (`schet_69i.py`, `schet_nazvany_69i.py`) и два их вывода.

---

## п.0 — ПРИЗНАК СДЕЛАННОСТИ: ПРОВЕРЕН ПЕРВЫМ ДЕЙСТВИЕМ

`ls docs/artifacts/2026-09-21-69i-SNYATOEGOVORIT-2109.md` → `No such file or directory`.
За 21.09 в каталоге лежал единственный файл — `2026-09-21-69g-DEVYATKORZIN-2109.md`, к этой задаче
отношения не имеющий. Пункты 2–4 не отвечены нигде. **Работа с нуля**; этот файл — первая редакция.

---

## КОРОТКИЙ ОТВЕТ

| вопрос задания | ответ |
|---|---|
| премиса «183 / 158» | **сошлась наполовину.** «183 снятых» — ДА, это ровно `243 поставленных − 60 показанных`. «158 без слова» — **НЕТ, занижено**: живым воспроизведением тройки сводки молчат **183 из 187** снятых (по реестру ступени B — **179 из 183**) |
| сколько мест снятия | **ВОСЕМЬ**, и в семи из восьми причина НЕ записывается никуда |
| одно ли место | **НЕТ** → законная ветвь: перечень и остановка, заплаты нет |
| есть ли след наполовину | **ДА.** Оба реестра уже умеют писать снятое СО СЛОВОМ (`state["held"][key]["why"]`), но пишут ровно ОДИН РАЗ — на бутстрапе. Достраивать надо это, а не заводить второй след рядом |
| что записывается сейчас | ступень B — только ЧИСЛО отложенных в лог демона и только когда что-то поставлено; ступень D — **НИЧЕГО, даже числа** |

---

## п.1 — ПРЕМИСА ЧИСЛОМ, ПЕРЕСЧИТАНА НЕЗАВИСИМЫМ ПРОХОДОМ

Считано не по чужому отчёту, а своим проходом по лотку живыми функциями контура. Корпус —
`docs/review_inbox` (293 файла), реестры — `review_intake_state.json` и `review_audit_state.json`.

### За всё время корпуса (01.09 — 11.09 по полю `отправлено:`, показ по 20.09)

| ступень | число | адрес факта |
|---|---|---|
| файлов в лотке | **293** | `docs/review_inbox` |
| файлов, из которых находок не взято | **112** | `review_intake_run.read_records` → `skipped` |
| находок-источников (строк ответа) | **543** | `review_intake.split_findings` |
| заявок после склейки дублей | **247** (склеено 296 записей) | `review_intake.merge` |
| ступень B поставила заявкой в очередь | **243** | `review_intake_state.json → claims` |
| ступень B не поставила | **4** (в реестре `held` — 5 ключей бутстрапа) | там же, `held` |
| ступень D показала владельцу немедленно | **60** | `review_audit_state.json → shown` |
| **ступень D СНЯЛА** | **187** по корпусу · **183** по реестру ступени B | разность наборов ключей |
| из снятых названы ТЕКСТОМ хотя бы раз | **4** | воспроизведение тройки сводки |
| **из снятых НЕ названы текстом НИКОГДА** | **183 из 187** (97.9%) · **179 из 183** по реестру | там же |

Разбивка 112 пропущенных файлов по причине (причина берётся из самого файла, не из догадки):

| причина пропуска | файлов |
|---|---|
| исход канала «НЕИЗВЕСТНО» (`channel_idle`) | 45 |
| «КАНАЛ ОТКАЗАЛ» (`channel_error`) | 26 |
| «НЕИЗВЕСТНО» (`answer_lost`) | 25 |
| «КАНАЛ ОТКАЗАЛ» (`http_400`) | 10 |
| «НЕИЗВЕСТНО» (`accepted_no_answer`) | 3 |
| «КАНАЛ ОТКАЗАЛ» (`no_credentials`) | 1 |
| не файл ответа (`not_an_answer_file`) | 2 |

Премиса ступени B по поставленным заявкам: `alive` **141**, `unknown` **66**, `stale` **36**.

### Отдельно за последние семь суток

Здесь два честных окна, и путать их нельзя.

| окно | пришло находок | заведено заявок | дошло до владельца | снято |
|---|---|---|---|---|
| **календарные 14.09–21.09** (окно задания) | **0** | **0** | **21** (7 суток × 3, всё из backlog) | 0 новых |
| **последние семь суток КОРПУСА 05.09–11.09** | 192 находки-источника | **78** заявок | **13** | **65** |

**Новых находок нет с 11.09 — одиннадцатые сутки.** Это и есть то, о чём премиса Штаба говорит
«сегодня ночью каналы должны ожить»: пока канал молчит, ступень D живёт исключительно backlog'ом
и выдаёт ровно по 3 показа в сутки — `2026-09-01…2026-09-20`, двадцать суток подряд, ни одного
исключения (`review_audit_state.json → shown`, поле `day`). Потолок `IMMEDIATE_BUDGET = 3`
(`review_audit.py:102`) упирается КАЖДЫЕ сутки.

### Сошлось ли с числами Штаба

**«183» сошлось точно, и я нашёл, откуда оно:** это `243 − 60` — снятые, считая по РЕЕСТРУ
ступени B. Мой счёт по КОРПУСУ даёт 187, и разница ровно 4 — те самые заявки, которых ступень B
не ставила вовсе. Оба числа верны, каждое в своей единице; складывать их нельзя.

**«158» НЕ сошлось, и я верю своему.** Прежний замер
(`docs/artifacts/2026-09-20-68w-VESIPOROG-2009.md:120`) моделировал тройку суточной сводки по полю
`placed_at` реестра. Живая сводка считает сутки ПО `send_date` источника
(`review_audit.py:414,422`), и воспроизведение тем же полем, каким судит код, даёт не 25 названных,
а **4**. Причина различия называется без гадания: тройку выбирает `rank()`
(`review_audit.py:482`), ключ которого — вес, тот же самый, что решает немедленный показ. Поэтому
**тройка сводки почти всегда повторяет уже показанное**: из 33 занятых мест тройки за 20 суток
снятым досталось четыре. Сводка, которая должна была ловить снятое, ловит показанное.

**Вывод по премисе: она не преувеличена, а ПРЕУМЕНЬШЕНА.** Молчат не 158 из 183, а 183 из 187.

---

## п.2 — ГДЕ СНИМАЕТСЯ: ВОСЕМЬ МЕСТ ПОИМЁННО. ЭТО ЗАКОННАЯ ВЕТВЬ

Задание: «Мест больше одного — уходишь на законную ветвь: список, и остановка». Мест **восемь**.
Ниже все, с адресом, условием и ответом на главный вопрос — **что записывается в момент события**.

| # | место | адрес | условие выбывания | что ЗАПИСЫВАЕТСЯ в этот момент |
|---|---|---|---|---|
| 1 | разбор лотка, файл не прочитан | `review_intake_run.py:156-158` | `OSError` при чтении | причина складывается в `skipped` (память). На диск — **ничего** |
| 2 | разбор лотка, не файл ответа | `review_intake_run.py:159-163` | `parse_answer` кинул `ReviewIntakeError` | то же: `skipped`, на диск **ничего** |
| 3 | разбор лотка, исход канала не «ответ» | `review_intake_run.py:164-166` | `not header["ok"]` — отказ/молчание канала | то же: `skipped` с причиной канала, на диск **ничего** |
| 4 | разрез ответа на пункты | `review_intake.py:329-330` | кусок между номерами пуст (`not quote`) | **НИЧЕГО ВООБЩЕ** — ни причины, ни счётчика. Единственное место, где даже слова нет в коде |
| 5 | склейка дублей по смыслу | `review_intake.py:596-599` | `same_meaning(anchor, rec)` — находка становится ИСТОЧНИКОМ чужой заявки | ЧИСЛО живёт: `merged` в суточной сводке (`review_audit.py:435,476`). Имён склеенных — нет |
| 6 | **ступень B, отбор постановки** | `review_intake.py:704-713` (3 причины: `уже поставлена` · `сверх лимита захода` · `бюджет суток исчерпан`) → `review_intake_run.py:531-532` | любая из трёх | причина есть СЛОВОМ в `report["held"]` и **умирает вместе с процессом**. В лог демона уходит только ЧИСЛО (`pc_orchestrator.py:11559-11561`) и только при `report["acted"]` |
| 7 | **ступень D, отбор показа — ГЛАВНОЕ МЕСТО** | `review_audit.py:226-240` (5 причин: `уже показана` · `не высокой важности` · `отложена бутстрапом` · `сверх лимита захода` · `бюджет немедленных суток исчерпан`) → `review_audit_run.py:368` | любая из пяти; ворота важности — `alive and score >= HIGH_MIN` (`review_audit.py:173`) | **НИЧЕГО. Ни причины, ни числа.** `line()` (`review_audit_run.py:434-445`) про `held` молчит, лог демона (`pc_orchestrator.py:11675-11678`) тоже. Причина видна ровно одним способом: ручной `--status`/`--dry` в stdout (`review_audit_run.py:468-476`) |
| 8 | суточная сводка, отсечка тройки | `review_audit.py:482` (`[:TOP_N]`, `TOP_N = 3`) | находка не в трёх самых весомых СВОИХ суток | ЧИСЛО живёт одни сутки («высокой важности: N · показано немедленно: K», `review_audit.py:478`). Назавтра — ничего: сводка считает свои сутки, backlog не пересматривается |

**Два места-исключения, где причина ПИШЕТСЯ на диск словом — и оба одноразовые:**

| место | адрес | что пишет | почему не считается |
|---|---|---|---|
| бутстрап ступени B | `review_intake_run.py:502` → `:543-546` | `state["held"][key] = {"seen_at", "kind", "keys", "why": "бутстрап: ответ лежал в лотке до рождения ступени B — не ставили"}` | ветка живёт РОВНО ОДИН раз за жизнь контура (`state["bootstrap_at"]` ставится тут же, `:546`). Записей — 5 |
| бутстрап ступени D | `review_audit_run.py:344` → `:411-413` | `state["held"][key] = {"seen_at", "keys", "why": "бутстрап: ответ лежал в лотке до рождения ступени D — не показывали"}` | то же, `:414`. Записей — 32 |

**Два хвоста, снятием не являющиеся, но названные, чтобы список был честным:** сбой постановки в
очередь (`review_intake_run.py:518`) и сбой отправки в тему Аудит (`review_audit_run.py:358`) —
это `failed`, а не `held`: находка не снята, она вернётся следующим заходом, и число «не встало N»
в строку исхода попадает.

**И одно мёртвое поле:** `built["header_skipped"]` (`review_audit_run.py:291`) заполняется и
**не читается ни одной строкой** — пропуски шапок лотка на ступени D исчезают совсем.

### Почему это именно законная ветвь, а не отговорка

Заплата в одном месте здесь не просто недостаточна — она **соврала бы числом**. Если завести
запись только в месте №7, журнал скажет «за проход снято 187», и это будет прочитано как «весь
отсев контура — 187». А до ступени D находка уже прошла три молчаливых сита (№1–3: 112 файлов),
одно вовсе бессловесное (№4) и одну склейку (№5: 296 записей). Полный отсев корпуса — не 187 из
247, а **543 → 60**. Правило про след обязано писаться на все восемь мест сразу и одной формой,
иначе первое же число нового следа станет born-false.

**Поэтому п.4 (завести след) и п.5 (мутант) этим заходом НЕ ВЫПОЛНЯЛИСЬ — так и говорю прямо:
замок не поставлен, падения числом не показано, потому что кода я не трогал вовсе.** Это
предписанный задании исход, а не пропуск.

---

## п.3 — ЧТО УЖЕ ЕСТЬ: СВОЙ СЛЕД ЧИТАЕТСЯ ПРЕЖДЕ ЧУЖОГО

Прочитано ДО всякой мысли о новом следе. Ответ на вопрос «нет ли его уже»: **есть, наполовину.**

| носитель | что в нём про СНЯТОЕ | годен ли как след |
|---|---|---|
| `review_intake_state.json → held` | 5 ключей, у каждого `why` СЛОВОМ, `seen_at`, `kind`, `keys` | **форма ровно та, что нужна.** Но пишется только бутстрапом |
| `review_audit_state.json → held` | 32 ключа, у каждого `why` СЛОВОМ, `seen_at`, `keys` | то же самое |
| `review_intake_state.json → claims` | 243 поставленных: `placed_at`, `queue_id`, `kind`, `premise`, `sources`, `channels` | след ПОСТАВЛЕННОГО, не снятого |
| `review_audit_state.json → shown` | 60 показанных: `day`, `at`, `message_id`, `kind`, `score` | след ПОКАЗАННОГО, не снятого |
| `review_audit_state.json → digests` | 20 суток: числа суток, `placed`, `message_id` | числа есть, имён снятого нет |
| `docs/review_inbox` (293 файла) | полный текст всегда, бессрочно | текст жив, но **адрес его владельцу не называют**, а поиска по нему нет |
| `docs/zayavki_lotok` | 19 файлов заявок, ушедших мимо ряда | про снятое ступенью D не говорит |
| журнал `cowork_log` | строки-индексы ступеней B и D (`review_intake.index_line`, `review_audit.index_line`) | пишутся ТОЛЬКО на поставленное/показанное |
| `pc_orchestrator.log` | ступень B: «заявок поставлено N, не встало M, отложено K» (`:11559`); ступень D: про отложенное **ни слова** (`:11675`) | у B — число без причины и только при `acted`; у D — пусто |

**Вывод п.3 одной строкой: второй след заводить НЕЛЬЗЯ — надо достроить существующий `held` в
обоих реестрах, сняв с него условие «только на бутстрапе».** Форма записи уже придумана, уже
обкатана на 37 записях и уже несёт `why` человеческими словами. Это и есть «есть наполовину —
достроить, а не писать второй рядом».

**Проверка задания «можно ли поднять этот признак, не совершив события?»** применена к
существующей форме заранее: `held[key]["why"]` рождается ВНУТРИ `select`/`select_immediate`, то
есть в самом месте события, и берёт причину из той же ветки `if`, которая и снимает находку.
Поднять его, не сняв находку, нельзя — **признак годен**. Негодным был бы пересчёт снятых задним
числом по разности реестров (ровно то, чем считал этот заход): он воспроизводится кем угодно и
когда угодно, событию не свидетель.

---

## п.6 — ГРАНИЦА ВСЛУХ

**Прочитано ПО АДРЕСУ (число или строка взяты из файла/кода, а не из чужого отчёта):**
`review_intake.py:307-334` (разрез), `:585-614` (склейка), `:691-716` (отбор B), `:526-546`
(запись источника); `review_intake_run.py:143-169` (пропуски лотка), `:284-300` (сборка),
`:470-568` (оборот, бутстрап, `_line`), `:573-599` (`_render`); `review_audit.py:85-243` (веса,
пороги, отбор D), `:395-499` (сутки и сводка); `review_audit_run.py:109-144` (шапки), `:277-291`
(сборка), `:310-445` (оборот и строка исхода), `:451-477` (`_render`); `pc_orchestrator.py:11540
-11563` и `:11650-11680` (что демон пишет в лог). Реестры `review_intake_state.json`,
`review_audit_state.json` — прочитаны целиком своим счётчиком.

**Выведено (мой счёт, а не запись в файле):** 543/247/296 склеенных; 187 и 183 снятых (разность
наборов ключей); 4 названных текстом и 183 молчащих (воспроизведение `rank()[:3]` по дням
сводок); 112 пропущенных файлов и их разбивка; окна 14.09–21.09 и 05.09–11.09; «183 = 243 − 60» —
восстановление происхождения чужого числа, не его цитата.

**Чего заход НЕ делал:**
- не правил ни одной строки кода и ни одного порога; нового следа не заводил, п.4 и п.5 не
  выполнял СОЗНАТЕЛЬНО — законная ветвь задания;
- не слал наружу ничего: ни пакета, ни сообщения в тему Аудит, ни карточки владельцу; проверочных
  вызовов пути «сообщение владельцу» не совершал вовсе;
- не трогал состояний контура, очереди, моста; не ставил и не снимал заявок;
- не ходил на сервер и не судил судьбу 183 рядов очереди (сколько дожило, сколько сгорело по
  `APPROVAL_TTL`) — исход **НЕИЗВЕСТНО**;
- не перепроверял премисы заявок живым `build()` (полный проход по дереву дорог): исходы
  `alive/stale/unknown` в п.1 взяты полем реестра;
- не читал текстов самих находок — этот заход про механизм, а не про правоту канала (тексты
  читал заход 68s, 20 штук);
- не трогал `.env`, конфиги, боевую базу, денежные пути, процессы, клиентский контур; ничего не
  удалял, включая своё временное.

**Чего этот замер НЕ доказывает.** «4 названных текстом» получено воспроизведением тройки
СЕГОДНЯШНИМ корпусом: сводка каждых суток собиралась на корпусе ТОГО дня, и если файл ответа
приехал в лоток позже своей даты `отправлено:`, живая тройка тех суток могла отличаться от моей.
Направление ошибки при этом одно — **в пользу премисы, а не против**: поздний файл мог только
ДОБАВИТЬ снятому шансов попасть в тройку задним числом, значит настоящее число названных ≤ 4 либо
чуть выше, но никак не 25.

---

## п.7 — БЮДЖЕТ

Потолок 2700 с, порог 1890 с. Заход уложился без урезания: п.0–п.1 (признак сделанности, два
счётчика, сверка с чужим числом) ≈ 0.45 бюджета, п.2 (восемь мест по адресам) ≈ 0.30, п.3
(существующий след) ≈ 0.10, запись артефакта, коммит и журнал ≈ 0.15. Арифметика урезания не
потребовалась — законная ветвь сняла с захода самые дорогие пункты 4 и 5.

---

## ЧТО ЭТО ЗНАЧИТ ДЛЯ НОЧИ, КОГДА КАНАЛЫ ОЖИВУТ

Одной строкой, без предложений по починке (их запрещает законная ветвь): сегодня поток находок
равен нулю, и восемь молчащих мест не стоят полосе ничего. Как только канал заговорит,
`IMMEDIATE_BUDGET = 3` и `HIGH_MIN = 2` начнут снимать в том же темпе, что 01.09–11.09 —
**75.7% всего, что придёт**, — и по-прежнему без слова у 97.9% снятого. Правило про след, когда
его будут писать, обязано покрыть все восемь мест разом и одной формой; готовая форма уже лежит
в обоих реестрах (`held[key]["why"]`) и ждёт снятия условия «только на бутстрапе».
