# ВОСЕМЬ sutok zhivaya dver 1709 — живая дверь на 8 сутках XMAX 300 нового поколения: 655, а не 662 (задание 62-q)

**ГЛАВНОЕ ОДНОЙ СТРОКОЙ. Живая дверь `quote_price` на 16.07.2026 → 24.07.2026 (8 суток) отдала
`day_price` = 655 и `total` = 5238, `text` «скидка за срок 7%». Число голденов 662 из записанного
правила с дверью НЕ СХОДИТСЯ: на 7 сутках дверь даёт 662, то есть база подтверждена, а расходится
ОДНА часть правила — ступень срока.** В правиле корзина 7–14 суток ровная, множитель 1.000, а у
двери ступень меняется каждые сутки: 7 сут → 6%, 8 → 7%, 9 → 8%. Разница на восьми сутках: 7 ฿ в
сутки и 58 ฿ за срок, правило дороже. Ни правило, ни ожидание теста, ни фикстура не правились:
вывод этой единицы — знание, а не правка.

| предмет | значение |
|---|---|
| заход | ПК-полоса #55, задание создано 17.09.2026 00:48:03 (+07); вызовы двери 00:52:14 → 00:56:07 |
| место для временного | `_scratch_vosem_sutok_1709/`: прибор `door.py` и сырьё `calls.jsonl`. Ничего не удалялось |
| вызовов двери | **8**, отказов 0; все GET `quote_price`, только чтение |
| записей | ни одной: ни в таблицы, ни в мозг, ни в чаты; код, тесты и фикстуры не тронуты |
| замер секунд (п.6) | [`2026-09-17-zamer-sekund-dveri-quote-price-1709.md`](2026-09-17-zamer-sekund-dveri-quote-price-1709.md) |

---

## 0. Тело задания и признак сделанности

**0.** Тело задания вижу от «[от Штаба дата=2026-09-17 ключ=62-q-vosem-sutok-dver.1709]» до
«АДРЕС РЕЗУЛЬТАТА: файл в docs/artifacts за 17.09 со словами ВОСЕМЬ sutok zhivaya dver 1709».
Длина **≈4300 символов. Это оценка на глаз, а не замер**: дословной копии тела на диске этот заход
не делал. Хвост цел, адрес результата виден.

**0б.** За 17.09 до этого захода лежал один артефакт: `docs/artifacts/2026-09-17-slovar-chuzhogo-otkaza.md`.
Он про различитель чужого отказа (`exec_error`), цены и двери в нём нет. **Вопросы п.2–п.4 не
отвечены нигде**, поэтому работа продолжена.

## Премисы — да/нет с адресом

| # | исход | адрес и замер |
|---|---|---|
| **П1** | **ДА** | `docs/artifacts/2026-09-15-ЦЕНА-dver-protiv-fayla-1509.md:89-92` («ответ живой двери РОВНО на восьми сутках не снимался … на этом единственном незамеренном звене стои́т переход 7 → 8») и `:193-194` («Живую дверь не звал ни разу …») |
| **П2** | **ДА** | `venv/Scripts/python.exe -m unittest test_moderation` → **Ran 71 tests in 40.511s, OK** (17.09 00:5x). Ожидание — `test_moderation.py:372` `for num in ("662", "7000")`; `git log -S'for num in ("662", "7000")' -- test_moderation.py` → **один коммит `cab37f0` (31.08.2026)**, позже ни знаком не менялось. Фикстура `NEW = (662, 7000, True, 9900)` (`:296`) — коммит `405a100` (16.09) |
| **П3** | **ЧАСТЬЮ** | Сам `price_freshness_run.py` на 8 сутках спросить нельзя: окно зашито константой `WINDOW = ("15.09.2026", "16.09.2026")` (`:48`), юниты — `PROBES` (`:40-44`). Поэтому взят его **путь**, а не скрипт: тот же клиент моста, одолженный у демона (`:73-90`), тот же GET `quote_price`. «9 вызовов примерно за 2 минуты 13.09» — да, `docs/artifacts/2026-09-13-ПОЛЗУНКИ-zamer-dveri-1309.md:28` |
| **П4** | **ЧАСТЬЮ — НЕ ТО ПОКОЛЕНИЕ** | «XMAX 300CC BLUE PHUKET 5773» — это **старое поколение**: дверь зовёт его `YAMAHA XMAX300 2020-2022`, ставка 557 (`docs/artifacts/2026-09-01-xmax-pokoleniya-razdeleny.md:40,44`). Проба 13.09 доказывает, что дверь отвечает по XMAX, но не по новому поколению. Новое поколение — юнит `XMAX 300CC NEW BLACK PHUKET 8969` (там же `:41,45`; 12.09 — `_scratch_cena_tri_0912/unit2_door.json`). Этот заход спрашивал его, и дверь ответила `model` = `YAMAHA XMAX 300 NEW 2023-` все 8 раз |

---

## 1. Цена единицы (п.1)

Первыми сделаны два вызова: **5.45 с** (8 суток) и **4.50 с** (7 суток).
Арифметика: 8 вызовов × 5.45 с = 43.6 с против 0.7 потолка = 1890 с → **влезает**, остановки не было.
Факт: 8 вызовов заняли **218.08 с** (11.5 % от 1890 с). Три из восьми были медленными (41.62 · 64.91 ·
84.77 с), и это названо в файле замера.

## 2. Живая дверь: 7 / 8 / 30 суток и форма перехода (п.2, п.5)

Юнит `XMAX 300CC NEW BLACK PHUKET 8969`. Каждое число взято из названного поля ответа, дословно:
сырьё — `_scratch_vosem_sutok_1709/calls.jsonl`, поле `answer`.

| окно | `days` | `day_price` | `total` | из `text` дословно | `deposit` | `cap_price` / `cap_active` | `available` / `conflicts` |
|---|---|---|---|---|---|---|---|
| 16.07–23.07 | 7 | **662** | **4632** | «скидка за срок 6%, 662 в день» | 7000 | 9900 / true | true / 0 |
| **16.07–24.07** | **8** | **655** | **5238** | «скидка за срок 7%, 655 в день» | 7000 | 9900 / true | true / 0 |
| 16.07–25.07 | 9 | 648 | 5829 | «скидка за срок 8%, 648 в день» | 7000 | 9900 / true | true / 0 |
| 16.07–30.07 | 14 | 577 | 8082 | «скидка за срок 18%, 577 в день» | 7000 | 9900 / true | true / 0 |
| 16.07–31.07 | 15 | 570 | 8547 | «скидка за срок 19%, 570 в день» | 7000 | 9900 / true | true / 0 |
| 16.07–15.08 | 30 | **458** | **13728** | «скидка за срок 35%, 458 в день» | 7000 | 9900 / true | true / 0 |
| 21.09–29.09 | 8 | 655 | 5238 | «скидка за срок 7%, 655 в день» | 7000 | 9900 / true | true / 0 |
| 16.07–24.07 (повтор) | 8 | 655 | 5238 | «скидка за срок 7%, 655 в день» | 7000 | 9900 / true | true / 0 |

У всех восьми: `ok` = true, `model` = «YAMAHA XMAX 300 NEW 2023-», `season` =
`{"label": "low", "global_discount": 0.25, "source": "Календарь бронирования H3/I3/J3 (через 'цены альт' B2/B9/B19)"}`.

**Форма перехода по двери.** Ступень срока растёт на 1 процентный пункт в сутки: 6 → 7 → 8 % на 7/8/9
сутках. Между 9 и 14 сутками она прыгает до 18 % (сколько на 10–13 сутках, живьём не снято), на 15
сутках даёт 19 %, на 30 — 35 %. Опорной «ровной корзины» у двери нет.

**Итог двери не равен ставке, умноженной на срок:** 655 × 8 = 5240, а в поле `total` пришло 5238;
на 7 сутках 662 × 7 = 4634, а в `total` 4632.

*Справка, не основание решения.* Все шесть сроков сходятся до бата с формулой из снимка кода моста
от 14.08: `round(704 × (1 − скидка))` и `round(704 × дни × (1 − скидка))`, скидка скутера
`d≤5 → 0; 6 → 5%; 7…13 → 6% + (d−7)·1%; 14…30 → 18% + (d−14)·1.0625%`
(`tmp/srv_e2e_20260814/bridge_prod/QuotePrice.js:108-113`, `:223-225`). Число 704 в ответе двери
не приходит, оно выведено арифметикой: 939 × 0.75 ≈ 704, как в артефакте 12.09. Поэтому в
решение ни 704, ни «формула снимка» не идут, решение стоит только на полях таблицы выше.

## 3. Окно дат (п.3)

**Окно голденов — 16–24 июля 2026, и дверь его посчитала.** Прошлые даты она не отвергла: `ok` =
true, `days` = 8 (вызовы #1 и #8). Окно не подменялось.

Дополнительно, не взамен, спрошено окно **21.09–29.09.2026**: та же ступень срока (8 суток) и тот же
сезон по правилу (P1 «ИЮНЬ-СЕНТЯБРЬ», множитель 1.000 — ответ правила ниже). Дверь дала
**побайтно тот же ответ**: 655 / 5238 / 7 % / `global_discount` 0.25. Отсюда граница, которую надо
назвать: **у цены двери нет оси даты.** «Июльская» цена двери — это положение ручки сегодня
(`season.global_discount` = 0.25), а не то, что стояло 16.07. Истории положений у ручки нет
(`docs/artifacts/2026-09-13-ПОЛЗУНКИ-zamer-dveri-1309.md` §2.1). Сравнение с правилом всё равно
законно: правило тоже зовёт и июль, и конец сентября одним периодом P1 с сезоном 1.000, а его база
приведена к низкому сезону. Именно это значение ручки дверь и показывает сегодня (`label` = low).
Какой была ручка 16.07.2026, отсюда **НЕИЗВЕСТНО**.

## 4. Сравнение с 662 и с частями правила (п.4)

**Правило:** `price_source.json:49` →
`price_per_day = base[model] * season[period(start_date)] * term[bucket(days)]`.
Числа правила сняты живым кодом без сети: `price_source.term_total` через
`_scratch_vosem_sutok_1709/door.py --rule`, поле разбора `day_prices`. Для всех окон: `base` =
662.0, `period` = P1, `season` = 1.0.

| сут | правило: `bucket` / `term` | правило: цена суток / итог | дверь: `day_price` / `total` | правило − дверь, ฿/сут | правило − дверь, за срок |
|---|---|---|---|---|---|
| 7 | 7-14 / 1.0 | 662 / 4634 | 662 / 4632 | **0** | +2 (округление итога) |
| **8** | **7-14 / 1.0** | **662 / 5296** | **655 / 5238** | **+7** | **+58** |
| 9 | 7-14 / 1.0 | 662 / 5958 | 648 / 5829 | +14 | +129 |
| 14 | 7-14 / 1.0 | 662 / 9268 | 577 / 8082 | **+85** | **+1186** |
| 15 | 15-29 / 0.82 | 543 / 8145 | 570 / 8547 | −27 | −402 |
| 30 | 30+ / 0.53 | 351 / 10530 | 458 / 13728 | −107 | −3198 |

Разбор по частям правила:

1. **База `base[XMAX 300, 2023-]` = 662 — ПОДТВЕРДИЛАСЬ.** На опорной точке, 7 сутках, дверь дала
   `day_price` = 662. Это та же точка, на которой база снималась 26.08 и сверялась 01.09. Оговорка:
   662 — это уже цена двери со скидкой за срок 6 %. База файла равна «недельной» цене двери, а не
   цене без скидки: на 5 сутках, где скидка 0 %, дверь 12.09 давала 704.
2. **Сезон `season[P1]` = 1.000 — не опровергнут, но дверью независимо не проверяем.** У двери нет
   оси даты (§3). Совпадение значит одно: сегодняшняя ручка — низкий сезон (`global_discount` 0.25,
   `label` low), то есть то положение, к которому приведена база.
3. **Ступень срока `term[bucket(8)]` = 1.000 — РАСХОДИТСЯ.** У правила корзина 7–14 ровная
   (`price_source.json:885-892`, «ОПОРА … скидка за срок начинается с пятнадцатых»). У двери на 8
   сутках `text` «скидка за срок 7%» против «6%» на 7 сутках. Отношение 655/662 = 0.989, а не 1.000.
   Внутри корзины расхождение растёт с каждыми сутками и на 14 сутках доходит до 85 ฿/сут. На 15 и 30
   сутках знак меняется: правило становится дешевле двери. Прыжок у двери стоит между 13 и 14
   сутками (по снимку кода; 13 сутки живьём не сняты), у правила — между 14 и 15.
4. **Ожидание голденов 662 на 8 сутках — это число правила, а не число двери.** Решение владельца
   12.09 (клиенту называется число двери; цитата — `docs/artifacts/2026-09-15-ЦЕНА-dver-protiv-fayla-1509.md:55-57`)
   на этих датах дало бы клиенту **655 ฿/сут и 5238 за срок**. Отсюда две записанные фразы, которые
   живая дверь опровергает. Обе названы, **не исправлены**:
   - артефакт 62-m, `:12-14`: «Ожидание теста (662) переживает пересчёт по НОВОМУ канону». Это
     верно на 7 сутках и неверно на 8;
   - комментарий фикстуры `test_moderation.py:331-333`: «ставка равна снятой дверью, а итог ей
     кратен». На 8 сутках ставка двери 655, итог 5238 ставке не кратен.
5. **30 суток: кап.** И дверь (13728), и правило (10530) дают итог выше `cap_price` 9900. По коду
   клиентского пути (`suggest.py:3776`, `_cap_applies`) клиент в обоих случаях услышал бы «аренда от
   9900 ฿/мес». Этот путь **живьём не прогонялся**, это чтение кода.

## 5. Поля (п.5)

Каждое число решения выше стоит рядом со своим полем: `day_price`, `total`, `days`, `text`,
`deposit`, `cap_price`, `cap_active`, `available`, `conflicts`, `season.global_discount`,
`season.label`, `model`. У правила — `base`, `period`, `season`, `bucket`, `term`, `day_prices` из
разбора `term_total`. Числа без поля (704, формула снимка кода) помечены как справка и в решение
не входят.

## 6. Секунды (п.6)

Отдельный файл: [`2026-09-17-zamer-sekund-dveri-quote-price-1709.md`](2026-09-17-zamer-sekund-dveri-quote-price-1709.md).
8 вызовов, медиана **5.65 с**, мин 4.50, макс 84.77, разброс 80.27 с; медленных 3 из 8.

## 7. Чего замер НЕ видит (п.7)

**Путь двери и закрытое место — говорю прямо.** Мои команды не открывали ни лист «Календарь
бронирования», ни брони, ни одну таблицу. Сама дверь на стороне моста их **читает**. По снимку кода
14.08 (`QuotePrice.js`) это:
- лист «цены альт» — строка модели, колонки C и H, ячейка ручки B19 (`:213-218`);
- лист «клиенты» — статус, байк и даты всех строк; из них считается `conflicts` (`:227-246`);
- блок капов Z3:AB15 листа «Календарь бронирования» — оттуда `cap_price` (`:154-158`).

Живой ответ это подтверждает: в нём есть поля `conflicts`, `cap_price`, а `season.source` называет
«Календарь бронирования». Мне вернулись только числа (конфликтов 0, кап 9900), содержимое броней
не приходило. Запрет «не открывать ни одной командой» я прочёл как запрет на **мои** команды, так
же, как его читали заходы 12.09 и 13.09 с той же формулировкой. Если Штаб считает внутреннее
чтение двери закрытым местом, **этот замер его переступил**, и решать об этом Штабу. Прогнать
дверь мимо этих чтений нельзя: у `quote_price` нет режима «без наличия».

**Кэш:**
- ПК: в клиенте кэша нет. `Bridge._get` каждый раз строит новый запрос (`pc_orchestrator.py:993-995`),
  в `bridge_http.py` слова cache нет ни одного. Есть лестница повторов целиком нового GET
  (`READ_TRIES = 3`, `:80`): медленный ответ мог быть вторым или третьим запросом, и отсюда это не
  видно.
- Мост: в снимке `QuotePrice.js` от 14.08 нет `CacheService` (grep → 0), каждое чтение идёт через
  `getRange`. **Живой код моста с тех пор этим заходом не сверялся**: по числам он совпал со
  снимком на 6 сроках из 6, по тексту — НЕИЗВЕСТНО.
- Лист: колонка C «цены альт» сама формула (B × (1 − ручка)). Посчитана ли она заново к моменту
  чтения или отдана последним пересчётом таблицы, отсюда не видно. Связано ли зеркало B19 с J3
  формулой или числом, переписанным руками, НЕ установлено (`docs/artifacts/2026-08-15-calendar-switches.md`).
- Повтор #8 дал побайтно ответ #1. Отличить «посчитано заново» от «взято из кэша» этим нельзя,
  детерминированный счёт выглядел бы так же. Разные сроки дали разные числа в пределах секунд, то
  есть ступень срока считается на каждый запрос.

**Нетронутые ветки цены:**
- одна модель и один юнит — кривая `scooter`; кривые `moto1`, `moto1_xsr`, `moto2` не спрашивались;
  старое поколение XMAX (557) не спрашивалось;
- сроки 1–6, 10–13, 16–29 и больше 30 суток не сняты. Прыжок 13 → 14 известен только по снимку кода;
- высокий сезон (`global_discount` = 0 → `label` high) не встретился: ручка сегодня 0.25;
- ветка «занят» (`available` = false, «⚠️ ЗАНЯТ») не встретилась, у всех окон `conflicts` = 0;
- отказы `bike_not_resolved`, `model_not_priced`, `bad_period`, `price_read_failed` не вызывались;
- подмена капом на клиентском пути (`_cap_applies`) — только чтение кода, без прогона;
- у правила не тронута ветка срока через границу периодов (`split`): все окна лежат в одном P1.

## 8. Бюджет (п.8)

Единица одна. Задание создано 00:48:03, вызовы двери 00:52:14 → 00:56:07, артефакты около 01:05
местного. Итого **≈17–20 мин ≈ 1100 с < 1890 с** (0.7 от 2700). Из них на дверь 218 с.

## Чего заход НЕ сделал

- Не правил ни правило (`price_source.json`), ни ожидание теста, ни фикстуру, ни комментарий
  фикстуры, ни артефакт 62-m, хотя две записанные фразы опровергнуты (§4.4).
- Не менял цен, прайса и ползунков, не писал в рабочие таблицы, не открывал своими командами лист
  «Календарь бронирования» и брони. Что читала сама дверь, названо в §7.
- Не читал `.env` и конфиги, не печатал секретов: клиент моста берёт их сам.
- Не трогал и не перезапускал процессы, не поднимал клиентских детей, наружу не отправил ничего.
- Не запускал полного гейта. Прогнан только `test_moderation` (71 OK) для П2.
- Не выяснил, почему три вызова шли 42–85 с (файл замера, раздел «Чего замер НЕ видит»).
- Временное место `_scratch_vosem_sutok_1709/` не убирал и в git не заводил.

ВОСЕМЬ sutok zhivaya dver 1709
