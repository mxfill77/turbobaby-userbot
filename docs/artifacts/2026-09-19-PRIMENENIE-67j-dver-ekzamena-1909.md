# Правка 67i у двери экзамена ПРИМЕНЕНА, и не временем это доказано: дверь — свежий процесс на каждый тап

PRIMENENIE 67j dver ekzamena 1909

**Дата:** заход 19.09.2026 (начат 14:04:51 местного) · **Полоса:** ПК · **Задание:** ящик Штаба,
ключ `67j-primenenie-dveri.1909` (док `shtab_task_67j-primenenie-dveri.1909`, file id
`1QBfNB3Ka2Fn6u2uzllGmmnJnXQahCDILstSTrnxzZQI`) · **Тело задания:** 3859 символов ·
**Временное место:** `_scratch_kod_v_pamyati_zamer/` (вне git; ничего не удалялось — обе копии
пред-67i кода оставлены на месте как предмет отрицательного теста).

**Заход РАЗВЕДОЧНЫЙ, и это отпечаток, а не обещание** (п.0д — ничего не чинить). Ни один боевой
файл состояния в заходе не писался, и проверяется это mtime против минуты начала захода:

| файл | mtime | писан в заходе? |
|---|---|---|
| `lesson_store.tsv` (книга головы / база тренажёра) | 11.09 18:58:14 | НЕТ |
| `exam_live/lessons.tsv`, `exam_live/verdicts.tsv`, `exam_live/desk.json` | 19.09 03:27:55 | НЕТ |
| `exam_session.json` (стол тренажёра) | 12.09 18:10:02 | НЕТ |
| `manager-bot/docs/playbook.md` (книга головы на диске) | 23.07 01:26:26 | НЕТ |
| `pc_agent.lock` | 19.09 01:26:39 | НЕТ (читан) |
| `pc_orchestrator.contour_frozen`, `…client_trainer_green.json` | 21.08 19:36:25 / 06.09 14:44:19 | НЕТ (читаны) |

Кода не менялось ни строкой, процессов не поднималось и не трогалось ни одного, живой прогон
корпуса не запускался (п.0г): путь «сообщение владельцу» в заходе не вызван ни разу.

---

## ОТВЕТ ОДНОЙ СТРОКОЙ

**ПРИМЕНЕНО — но не потому, что «совпало по времени», а потому, что у двери экзамена НЕТ живого
процесса, который мог бы нести старый код:** тап спавнит `exam_show.py` отдельным процессом, и
ребёнок читает файл с диска заново каждый раз (`pc_agent.py:1120`; настоящих `import exam_show` в
агенте — **0**, единственное вхождение имени в строке импорта лежит в комментарии `pc_agent.py:1023`).
Готовность и применение названы порознь: **готовность** — коммит `c9273ce8` 19.09 13:09:35 +0700;
**применение** — то, что по argv-пути двери лежит блоб `7e2bd67e…`, байт в байт равный `HEAD:exam_show.py`.
Наивный признак «файл не старше коммита» на этом предмете **инвертирован** и краснил бы правду:
живой `exam_show.py` на **568 с СТАРШЕ** своего коммита, а пред-67i копия — на **3833 с моложе** его.

## п.0в — ПРИЗНАК СДЕЛАННОСТИ: что лежит в `docs/artifacts` за 19.09

Файлов за эту дату восемь. Вопросы п.1–п.4 задания 67j **не отвечены ни в одном**; ближайший —
артефакт самого 67i, и он честно говорит о себе, что мерил стенд.

| файл | о чём | отвечает п.1–п.4? |
|---|---|---|
| `2026-09-19-REGRESSIYA-znaet-dver-ekzamena-1909.md` | прибор регрессии у двери экзамена (67i) | **нет**: назвал строки вызова и мутанта, но слова «живой процесс», PID и коммита в нём нет; все замеры под `TESTING=1` |
| `2026-09-19-PETLYA-zhivogo-nabora-razomknuta-1909.md` | у петли живого набора нет читателя | нет (предмет — книга головы) |
| `2026-09-19-EKRAN-67i-zamykanie-zhivogo-nabora-1909.md` | экран задания 67i владельцу | нет (показ) |
| `2026-09-19-KONTEKST-komnaty-obhodit-bazu-1909.md` | ответ ТЕСТ-бота мимо базы | нет |
| `2026-09-19-FRAZA-kartochki-vypryamlena-1909.md` | фраза карточки живого набора | нет |
| `2026-09-19-POKAZ-kejs-1-…`, `…-PEREPOKAZ-kejs-14-…` | показ и перепоказ кейсов | нет |
| `2026-09-19-YASHIK-ne-vidit-lezhashchih-dokumentov-1909.md` | ворота ящика и длина документов | нет |

## ПРЕМИСЫ — каждая да/нет, с адресом и числом

**П1. Правка 67i лежит в коммите — ДА, `c9273ce833b9dba39606207e90ac5472f12d52fd`, автор-время и
коммит-время одно: 2026-09-19 13:09:35 +0700.** Он же **вершина**: `git rev-parse HEAD` =
`c9273ce8…`. Состав коммита — 5 файлов, 1104 вставки / 37 удалений: `exam_show.py` (+88),
`lesson_regress.py` (+298/-37), `test_lesson_regress.py` (+372, новый), `test_exam_show.py` (+119),
артефакт 67i (+264). Рабочее дерево по трём названным файлам **чистое**
(`git status --porcelain -- exam_show.py lesson_regress.py test_lesson_regress.py` → пусто), и
чистота эта подтверждена не только статусом, а СОДЕРЖИМЫМ (см. п.1, сверка блобов).

**П2. Процесс, обслуживающий кнопки двери экзамена, жив — ДА: `pc_agent.py`, PID 4660, старт
19.09.2026 01:26:38 местного (18.09 18:26:38 UTC).** Два независимых источника совпали:
`pc_agent.lock` (`{"pid": 4660, "started": 1789755998.397, "script": "pc_agent.py"}`) и живая
таблица процессов (`Win32_Process`: PID 4660, `CreationDate 19.09.2026 1:26:38`, командная строка
`venv\Scripts\python.exe pc_agent.py`). Рядом живут и другие, и путать их с дверью нельзя:
`userbot_listen.py` PID 5484 (18.09 21:39:03), `moderation_bot.py` PID 10012 (18.09 21:52:11),
`pc_orchestrator.py` PID 18848 (19.09 01:27:02), `rc_supervisor.py` PID 10516 (18.09 21:38:10).
**Агент старше коммита 67i на 11 ч 42 мин 57 с** — и это ровно то место, где наивное чтение
объявило бы «НЕ ПРИМЕНЕНО» (см. п.1, почему оно неверно).

**П3. `lesson_regress.py` входит в клиентское замыкание — ДА, обеими мерами; `exam_show.py` —
НЕТ.** Спрошен живой признак (`client_contour`, только чтение):

| замыкание | ok | файлов | `lesson_regress.py` | `exam_show.py` | `trainer.py` | `trainer_run.py` | `pc_agent.py` |
|---|---|---|---|---|---|---|---|
| ворота ВЫКАТКИ ДЕТЕЙ (`cut=()`, срез снят 07.09) | True | **88** | **True** | False | True | False | True |
| дефолт (`cut=FOREIGN_ENTRIES`, ворота входа) | True | **50** | **True** | False | True | False | False |

То есть доставка правки судится воротами **из-за `lesson_regress.py`**, а не из-за двери: файл
двери для признака внутренний (в замыкание живых ботов он не входит ни одной дорогой), и это не
случайность — `pc_agent.py:1022–1028` называет цену вслух: `import exam_show` там затащил бы в
охраняемое множество прогонщик корпуса, поэтому стол читается литералом, а не импортом.

---

## п.1 — КАКОЙ КОД ИСПОЛНЯЕТ ЖИВОЙ ПРОЦЕСС ДВЕРИ: **ПРИМЕНЕНО**

Основание из трёх звеньев; ни одно не опирается на совпадение времён.

**Звено 1 — у двери нет памяти, которую можно было бы застать старой.** Тап приезжает в живой
`pc_agent` (PID 4660), тот роутит его по виду кнопки (`pc_agent.py:1335` — `exam`, `:1336` —
`exam_live`) в `_exam_cli`, а `_exam_cli` **спавнит дверь отдельным процессом**:

```
pc_agent.py:1119   r = subprocess.run(
pc_agent.py:1120       [str(VENV_PY), str(REPO_DIR / "exam_show.py")] + args + ["--who", str(who or "")],
```

Число, которым это держится: настоящих импортов `exam_show` в `pc_agent.py` — **0** (`grep -c
'import exam_show' pc_agent.py` → 1, и это вхождение целиком лежит внутри комментария
`pc_agent.py:1023`; исполняемых строк импорта нет). Значит кода двери в памяти PID 4660 нет
вовсе, и возраст процесса о содержимом двери не говорит **ничего** — что и записано в самом файле
двери замером 51-c/52-c (`exam_show.py:1150–1153`).

**Звено 2 — по argv-пути лежит именно 67i.** Сверка не по mtime и не по `git status`, а по
содержимому, руками git (фильтры те же, что при коммите):

| предмет | объект |
|---|---|
| диск `exam_show.py` (`git hash-object`) | `7e2bd67ed2925a2b7ab883cb0a1f7fffb610737f` |
| `HEAD:exam_show.py` (`git rev-parse`) | `7e2bd67ed2925a2b7ab883cb0a1f7fffb610737f` — **РАВНЫ** |
| диск `lesson_regress.py` | `b5821264d49db464254ea9b3b375224f5f4896a4` |
| `HEAD:lesson_regress.py` | `b5821264d49db464254ea9b3b375224f5f4896a4` — **РАВНЫ** |

Плюс маркеры правки в файле по argv-пути: имя `lesson_regress` — **6** вхождений, `spawn_many` —
**2**, `REGRESS_SAID`/`regress_said` — **2/3**; в `lesson_regress.py` — `SET_TRAINER` **9**,
`SET_LIVE` **4**, `spawn_many` **1**.

**Звено 3 — тап доезжает, и разбор кнопки в памяти агента тот же, что на диске.** Замок времени
кнопки (`exam_show.agent_carries_button`, замыкание `BUTTON_CLOSURE = ("pc_agent.py",)`):
mtime `pc_agent.py` = 01:08:30 против старта 01:26:38 — **−1087 с, файл СТАРШЕ процесса**, замок
зелёный. И правка 67i этого замка не касается по построению: в диффе `exam_show.py` ни одного
вхождения `markup`, `card_callbacks`, `EXAM_CB_RE`, `inline_keyboard` — **0** (пять хунков,
все в теле `_lesson_write`…`own_take`: `@@ -1775`, `@@ -1810`, `@@ -1827`, `@@ -1887`, `@@ -1949`),
а `pc_agent.py` коммит не трогает вовсе. Колбэки карточки прежние — значит разбор живого агента
принимает их ровно как принимал до 67i.

**Строка самообновления существует, и она здесь НЕ ПРИ ДЕЛАХ.** У агента есть само-рестарт:
`pc_agent.py:1428` (`text in ("обновись", "перезапустись", "обнови себя", "restart")`) →
`pc_agent.py:596` (`git pull` → спавн помощника → снять лок → жёсткий выход). Он обновляет **код
самого агента**, которого 67i не менял ни строкой; для двери он не требуется и её применения не
даёт и не отнимает. Демон (`pc_orchestrator`, PID 18848) обновляется сам и к предмету не относится.

**Чего этот исход НЕ покрывает, вслух.** «ПРИМЕНЕНО» сказано про **дверь экзамена** — кнопки
«✔ Применить» и «✍️ своё» карточки экзамена (и тренажёрного набора, и живого). Про
**вторую дверь того же прибора** — кнопку «🎓 Обучить» модербота (`trainer.py:1428` →
`lesson_regress.spawn`) — исход **НЕИЗВЕСТНО**: импорт там ленивый, и если ветку звали после
старта PID 10012 (18.09 21:52:11), в памяти модербота лежит пред-67i модуль; узнать это, не
трогая процесс, нечем. Цена этого незнания **нулевая**, и это измеримо: 67i менял у `spawn`
только добавленный `set_name` с прежним умолчанием, а прогон корпуса всё равно живёт в
ОТСОЕДИНЁННОМ ребёнке `lesson_regress.py --after-lesson` (дифф: `argv = [VENV_PY, SELF,
"--after-lesson"]`), то есть меряет всегда свежий файл с диска.

## п.2 — ЖИВОЙ ВЫЗЫВАЮЩИЙ (не тест)

Прибор зовётся из **боевого** пути двумя строками, обе внутри функций, которые исполняет живой тап:

| адрес | функция | какая кнопка | как файл попадает в живой процесс |
|---|---|---|---|
| **`exam_show.py:1897`** | `apply_marked` (объявлена `:1835`) | «✔ Применить» → CLI `--apply` (`exam_show.py:2143`, аргумент собирает `pc_agent.exam_args`, `pc_agent.py:1087–1088`) | `pc_agent.py:1120` спавнит `venv\Scripts\python.exe exam_show.py --apply --case N --who <имя>` — новый процесс, чтение с диска |
| **`exam_show.py:2024`** | `own_take` (объявлена `:1958`) | «✍️ своё» → CLI `--own-text` (`:2147`, ветка `:2220`, вызов `own_take(sys.stdin.read(), a.who)` на `:2224`) | тот же спавн, текст урока едет через stdin |

Обе строки — `regress_said(_regress_call(…))`, то есть один и тот же прибор и одна и та же
очерёдность: сперва вердикт и уроки на диск, потом замер. Дальше цепочка внутри процесса двери:
`_regress_call` (`:1816`) → `_default_regress` (`:1803`) → ленивый `import lesson_regress`
(`:1809`) → **`lesson_regress.spawn_many`** (`:1813`), а тот поднимает отсоединённый
`lesson_regress.py --after-lesson [--set live]`. Тестами зовутся другие входы (`regress_fn`
инъекцией), и работой они не являются — боевой путь именно этот, инъекция в нём `None`.

**Мина, на которой этот пункт легко соврать, с числом.** Имя `lesson_regress.py` встречается в
рабочем дереве **34** раза, `exam_show.py` — **25** (копии деревьев в `_scratch_*` и `tmp/*`), а в
git — по **одной** (`git ls-files` даёт 2 совпадения на маску, второе — файл теста). Дверь-ребёнок
видит ровно корневую: её путь назван в argv абсолютным, и `sys.path[0]` ребёнка — корень репо.
Поэтому все числа выше сняты поимённо с корневых файлов, а не грепом по дереву.

## п.3 — ВОРОТА: держат, законно, и НЕ то, о чём спрашивают

**Держат — ДА, но подъём ДЕТЕЙ, а не дверь.** Ворота клиентского контура зовутся ровно из
`pc_orchestrator._client_block` (`pc_orchestrator.py:7878`), и это, дословно из шапки ворот
(`:7746`), «ТОЛЬКО рестарт детей-ботов»: self-update самого демона и его эстафета ворот не
спрашивают ни строкой (`:7770` — «`_client_block` зовётся ТОЛЬКО с трёх дорог рестарта ДЕТЕЙ»).
Путь двери экзамена в этом перечне не стои́т вовсе: её поднимает `pc_agent` субпроцессом на тап,
и ни одна ветка ворот на этом пути не спрашивается — поэтому применение двери воротами не
задерживается ни на секунду.

**Основание удержания названо и законно:** контур **ЗАМОРОЖЕН** — флаг
`pc_orchestrator.contour_frozen` лежит с **21.08.2026 19:36:25** (1184 байта), и живой прибор
отвечает дословно: `freeze_holds_release()` → `(True, 'контур ЗАМОРОЖЕН … — зелёный вердикт
тренажёра ворот НЕ открывает; открыть может только поимённое «да» владельца на этот коммит')`.
Второе основание снято целиком: запись тренажёра в реестре есть (`pc_orchestrator.client_trainer_green.json`,
mtime 06.09 14:44:19), но под заморозкой `release_reason` до неё не доходит (`client_contour.py:743–749`).
Клиентский файл в коммите 67i ровно один — `lesson_regress.py` (`exam_show.py` признак держит
внутренним), и его правка ждёт рестарта детей законно.

**Объявляю вслух, чтобы прибор не кричал ложно:** удержание правильное и чинить его нечем и не
надо. «Ворота держат» здесь значит «модербот и userbot продолжают работать на прежнем коде до
решения владельца», а НЕ «правка 67i не применена». Смешение этих двух предметов — готовый ложный
диагноз: у двери экзамена применение уже состоялось, у детей — нет, и это два разных факта.

## п.4 — ОТРИЦАТЕЛЬНЫЙ ТЕСТ: старый код при зелёном признаке

**Собранное состояние.** В названное временное место положены пред-67i версии обоих файлов —
блобы родителя коммита (`c9273ce8^` = `cba76d1f`), взятые `git show`:
`_scratch_kod_v_pamyati_zamer/exam_show.py` (171050 байт против живых 178418) и
`…/lesson_regress.py` (47181 против 66609). Ничего боевого при этом не тронуто: живые файлы не
открывались на запись, копии лежат в стороне и оставлены на месте.

**Наивные признаки применения на этом состоянии — ЗЕЛЁНЫЕ, все три:** коммит 67i есть и он вершина
(`HEAD = c9273ce8`); `git status` по трём файлам чист; «файл не старше коммита» → **+3833 с** и
**+3834 с** (копии созданы 14:13:28/14:13:29 против коммита 13:09:35). Код при этом **пред-67i**.

**Мой способ различения (сверка СОДЕРЖИМОГО по argv-пути) краснеет числом:**

| предмет | ЖИВОЙ диск | СТАРАЯ копия | вердикт способа |
|---|---|---|---|
| git-объект `exam_show.py` | `7e2bd67e…` = `HEAD` | `36915e9271f21fdb…` ≠ `HEAD` | **КРАСНЫЙ** |
| git-объект `lesson_regress.py` | `b5821264…` = `HEAD` | `d5c606004429f4d3…` ≠ `HEAD` | **КРАСНЫЙ** |
| маркер `lesson_regress` в `exam_show.py` | **6** | **0** | **КРАСНЫЙ** |
| маркер `spawn_many` в `exam_show.py` | 2 | 0 | КРАСНЫЙ |
| маркеры `regress_said` / `REGRESS_SAID` | 3 / 2 | 0 / 0 | КРАСНЫЙ |
| маркеры `SET_TRAINER` / `SET_LIVE` / `spawn_many` в `lesson_regress.py` | 9 / 4 / 1 | 0 / 0 / 0 | КРАСНЫЙ |

**И то же измерение показало, что НАИВНЫЙ способ врёт в ОБЕ стороны — это главное число дня.**
Признак «файл моложе коммита, значит правка внутри» на живых, честно применённых файлах
**красный**: `exam_show.py` старше своего коммита на **568 с**, `lesson_regress.py` — на **596 с**
(правку писали до 13:00, коммит сделан в 13:09:35). То есть одно и то же наивное правило даёт
**ложное «НЕ ПРИМЕНЕНО» на правде и ложное «ПРИМЕНЕНО» на пред-67i коде** — ровно поэтому задание
и запретило считать совпадение времён основанием.

## п.5 — ОДНО ДЕЙСТВИЕ

Применение двери есть, и для неё действий не требуется ни одного. Единственное отсутствующее
применение — **у детей** (userbot PID 5484 / moderbot PID 10012 держат пред-67i `lesson_regress`
в памяти на ленивом импорте), и действие для него **ровно одно и оно ВЛАДЕЛЬЦА**: поимённое «да»
воротам на коммит `c9273ce8` — слово-рычаг **«выкати»** там, где ворота отвечают
(`client_contour.ANSWER_AT`, отказ — «не выкатывай»); под заморозкой это единственное, что ворота
открывает (`client_contour.py:743`). **Цена:** демон применит коммит целиком и перезапустит обоих
детей (обрыв обслуживания на секунды рестарта), а заморозка контура, лежащая с 21.08, на этом
коммите будет разово обойдена решением человека. Наше в этом действии — ноль строк: ни код, ни
процессы этого не дают.

## п.6 — БЮДЖЕТ

Единиц две, потолок 2700 с каждая, порог 0.7 = 1890 с. Замер первой единицы: заход начат
**14:04:51** (минута взята не на глаз — из id задачи), все премисы и п.1–п.5 закрыты и артефакт
записан к **14:18:27** — **816 с, 0.30 единицы**, то есть **втрое ниже порога**; вторая единица не
понадобилась. Остаток называю вслух: п.1–п.5 отвечены полностью, ничего из задания на вторую
единицу не перенесено, и остаток первой единицы — **1074 с до порога**.

---

## ОСТАТКИ И МИНЫ, НАЗВАННЫЕ ЧЕСТНО (не чинились — п.0д)

1. **Докстринг `exam_reshow.py:6–8` описывает СНЯТОЕ поведение** и готов родить ложный диагноз
   «правка `exam_show.py` ломает показ»: он утверждает, что `agent_carries_button` сравнивает mtime
   файлов замыкания «`pc_agent.py`, `exam_show.py`», тогда как живое `BUTTON_CLOSURE` —
   `("pc_agent.py",)` (`exam_show.py:1181`), и замена сверки времени на сверку содержимого сделана
   12.09 с числами (`exam_show.py:1150–1176`). Замер сегодня: mtime `exam_show.py` на **+41608 с**
   новее старта агента — по старому правилу показ отказал бы себе, по живому замок зелёный.
   Правка докстринга — код, в этом заходе не делалась.
2. **Журнала прибора на диске ещё нет ни одного:** `pc_orchestrator.lesson_regress.json` и
   `…live.json` отсутствуют оба. Это согласуется с «применено, но ни один тап после правки не
   состоялся» и НЕ является признаком неприменения — первый же тап создаст файл набора.
3. **Модербот: исход НЕИЗВЕСТНО по построению** (см. конец п.1). Способа узнать состав памяти
   живого процесса, не трогая его, у полосы нет; предмет закрывается рестартом детей, то есть тем
   же одним действием владельца из п.5.
