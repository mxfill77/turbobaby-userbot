# Задача 16: спасение остатка и арифметика смерти

**Дата разбора:** 2026-08-17 (задача 17, окно `13:23 UTC →`)
**Предмет:** задача #16 `ПОДКЛЮЧИТЬ БОТА К price_source.json И ЗАМЕРИТЬ. Промпт НЕ трогать.`
**Исход:** `failed`, причина `run_timeout`, окно `17.08 12:27:47 – 13:12:47 UTC` (2700.67с).
**Заход разбора:** только чтение. Код не правился, дерево не менялось, ничего не удалялось.

---

## Сводка (5 строк)

1. **Работа задачи 16 ЖИВА и НЕ ПОТЕРЯНА** — три файла в дереве (`suggest.py` изменён,
   `price_source.py` и `test_price_source.py` untracked) плюс полный результат замера
   в `_scratch_pricewire_0817/`. Диff `suggest.py` снят патчем, дерево оставлено как было.
2. **Остаток ПРИНАДЛЕЖИТ задаче 16** — основание не время, а СОДЕРЖАНИЕ: диff дословно
   исполняет ТЗ (`import price_source` → `price_source.reprice`), а имя флага в диффе
   совпадает с единственным объявленным отличием прогона (`run26.py`).
3. **Замер СОСТОЯЛСЯ и его результат цел:** цена по честному знаменателю **0/15 → 2/15**,
   оба известных занижения закрыты (dlg 179: 317→437 при 464; dlg 120: 577→710 при 798),
   модель не сдвинулась. Файл `_scratch_pricewire_0817/score_fixed.txt` дописан в **13:12:45**,
   таймаут наступил в **13:12:47** — задача умерла через **2 секунды** после своего результата.
4. **По построению закрыться МОГЛА:** 26 единиц × 50с = 1300с при окне 2700с (48%). Замер
   цены единицы в журнале ЕСТЬ. Убил не объём единиц, а второй, исправляющий прогон,
   на который в окне уже не было бюджета.
5. **Куратор («следов нет») считает ТОЛЬКО коммиты и записи журнала** — незакоммиченное
   и untracked в его счёт НЕ входит по построению. Тот же процесс через 5 секунд написал
   «рабочая копия грязная … файлы: suggest.py». Это дефект прибора, не отчёт о безделье.

---

## 1. Дерево: что лежит и куда снят патч

```
$ git status -s --untracked-files=no
 M suggest.py

$ git diff --stat
 suggest.py | 27 +++++++++++++++++++++++++--
 1 file changed, 25 insertions(+), 2 deletions(-)

$ git status --porcelain --untracked-files=all -- price_source.py test_price_source.py suggest.py
 M suggest.py
?? price_source.py
?? test_price_source.py
```

**Патч снят в явно названное место** (файл в дереве НЕ тронут — не откачен, не закоммичен,
не спрятан в stash):

```
_scratch_task16_0817/suggest.py.task16-residual.patch
  3498 байт · sha256 f5e8db821a9b6a23c198e71f4bc67d8ec1b9af64ae54f7b5cf393392f9b9c62b
```

Остаток задачи 16 — **не один файл, а три**. Два из трёх (`price_source.py` 18297 б,
`test_price_source.py`) **untracked**, то есть не видны даже команде `git diff`:

| файл | состояние | размер | mtime (UTC) |
|---|---|---|---|
| `suggest.py` | tracked, изменён | 543346 б | 12:37:45 |
| `price_source.py` | **untracked** | 18297 б | 13:09:12 |
| `test_price_source.py` | **untracked** | — | 13:10:19 |
| `price_source.json` | **в git** (`83f1591`) | 53610 б | 09:47 — вне окна, чужой (задача 19/21) |

Диff `suggest.py` целиком — единственная точечная котировка `_safe_quote_for_model`, врезка
за флагом `price_source.enabled()`, при снятом флаге путь побайтно прежний; сбой ветки гасит
цену, а не роняет ответ.

---

## 2. Чей остаток: ПРИНАДЛЕЖИТ ЗАДАЧЕ 16

Исход из трёх — **принадлежит 16**. Совпадение по времени доказательством НЕ считаю;
основания ниже, по силе.

**Основание 1 (сильное) — содержание диффа исполняет текст ТЗ.** ТЗ: «ПОДКЛЮЧИТЬ БОТА
К `price_source.json` И ЗАМЕРИТЬ». Диff добавляет ровно `import price_source` и вызов
`price_source.reprice(...)` в единственной точке котировки. Совпадение предметное, не временнóе.

**Основание 2 (сильное) — имя флага сходится с объявленным отличием прогона.**
`_scratch_pricewire_0817/run26.py` (докстринг): «Отличие от `e9231b3` РОВНО ОДНО и оно
объявлено: в окружении процесса поднят флаг **PRICE_SOURCE_SEASON**». В диффе новая ветка
исполняется только при `price_source.enabled()` — тот же флаг, та же семантика.

**Основание 3 (среднее) — штамп claim, а не стенные часы.**
`pc_orchestrator.task_started.json`: `"16": {"at": "2026-08-17T12:27:47.058918+00:00",
"pid": 1748, "proc": "1748-1786965920", "child": 16700}`. Правка `suggest.py` (12:37:45)
и оба untracked-файла (13:09, 13:10) лежат внутри окна именно этого захода.

**Контрпроверка «а не чужая ли параллельная сессия»** (карточка демона сама предупреждает:
«в окно попадают и параллельные сессии ПК»):
- задача 14 закрыта в 12:12 UTC — раньше правки;
- задача 17 (этот заход) взята в 13:23:06 UTC — позже правки, и я ничего не правил;
- коммитов в окне 0, то есть иной сессии, оставившей след, в окне не было.
Полностью исключить незакоммиченную третью сессию нельзя — но она должна была бы написать
именно код под именем `price_source` с флагом `PRICE_SOURCE_SEASON`, чего у неё нет причины делать.

---

## 3. Текст ТЗ 16 — дословно, и граница дословности

Из очереди, через локальный слепок `queue_snapshot_pc.state.json` (`closed["16"]`):

```json
{"id": 16,
 "at": 1786972370.737,
 "goal": "ПОДКЛЮЧИТЬ БОТА К price_source.json И ЗАМЕРИТЬ. Промпт НЕ трогать.",
 "outcome": "failed",
 "why": "⏱ провал [причина=run_timeout · таймаут прогона]: таймаут 2700s — headless прерван, задача не завершилась. Сл…"}
```

**ЧЕСТНАЯ ГРАНИЦА: это ПЕРВАЯ СТРОКА ТЗ, а не всё ТЗ.** Слепок хранит `goal` обрезанным
(≈60 символов, обрыв на границе строки), полное тело задания живёт в ряду очереди на мосту.
Прочитать его отсюда нечем без чтения секретов моста (`bridge_http.py` — транспорт, токена
и адреса не знает; единственные читатели очереди сами грузят конфиг). Полный текст ТЗ 16 —
**НЕИЗВЕСТНО**; первая строка — выше, дословно.

Из того же слепка — соседи по суткам, для контекста арифметики:

```
  упало:
    #36 · БАЗОВАЯ ЛИНИЯ БОТА: прогнать 62 строгие пары эталона и посчитать · причина: ⏱ НЕ ЗАКРЫТА, но В ОКНЕ ЗАДАЧИ ЕСТЬ РАБОТА [причина=run_timeout · таймаут прогона]: таймаут 2700s — headless …
    #16 · ПОДКЛЮЧИТЬ БОТА К price_source.json И ЗАМЕРИТЬ. Промпт НЕ трогать. · причина: ⏱ провал [причина=run_timeout · таймаут прогона]: таймаут 2700s — headless прерван, задача не завершилась. Сл…
```

Обе упали одинаково по причине — и получили **РАЗНЫЕ вердикты о работе**. Почему — §5.

---

## 4. Арифметика смерти

### 4.1 Сколько единиц и есть ли замер цены единицы

**Единиц: 26 пар.** Корпус объявлен в `run26.py` («те же 26 пар, ТЕМ ЖЕ способом, что
в `e9231b3`») и подтверждён числом строк результата: `drafts_wired.jsonl` — 26,
`drafts_fixed.jsonl` — 26.

**Замер цены одной единицы В ЖУРНАЛАХ ЕСТЬ** — два независимых:

| источник | число |
|---|---|
| коммит `164cea9` (задача 38, «проба мерки на 10 парах») | «62 пары × **50с** = 3091с при пределе 2700» |
| `docs/artifacts/2026-08-17-regression-fullinput.md:71` | «Секунд на пару: медиана **46.9** (было 47.9) · худшая 138.4 · **сумма 1337с**» на тех же 26 парах |

### 4.2 Умножение

```
плановая цена:   26 × 50с   = 1300с   из 2700с  → 48.1% окна
измеренная ранее: сумма      = 1337с   из 2700с  → 49.5% окна
фактическая в 16: 12:40:23 → 13:05:05 = 1482с   из 2700с  → 54.9% окна (57.0 с/пара, +14%)
```

### 4.3 Могла ли закрыться ПО ПОСТРОЕНИЮ

**ДА, могла.** И это прямая противоположность задаче 36, которая по построению закрыться
НЕ МОГЛА: у неё 62 × 50 = 3091с при пределе 2700 — окно было мало ещё до старта
(это записано в `164cea9` теми же словами). У задачи 16 замер съедал половину окна,
вторая половина — 1400с — оставалась на всё остальное.

**Убил не объём единиц, а ВТОРОЙ прогон.** Восстановление хода по mtime (реконструкция,
не лог — так и помечено):

```
12:27:47  CLAIM #16
12:29:09  первый скрипт захода          (dump_source.py)          ← 82с до первого действия
12:33:29  снят бэкап правил              (rules_backup_before/)
12:34:48  замок A: батарея фикстур ДО    (probeA.py)
12:37:37  гейт ДО правки                 (gate_before.txt, 3628 тестов, 229с прогона)
12:37:45  ПРАВКА suggest.py              ← подключение
12:37:57  замок A после, флаг снят       (побайтно равно ДО)
12:38:01  замок A после, флаг поднят
12:40:23  запуск прогона 26 пар          (run26.py, свой бюджет 2200с)
13:05:05  прогон завершён 26/26          (drafts_wired.jsonl)      ← 1482с
13:06:40  счёт                           (score_out.txt)
13:07:12  разбор веток                   (why.py)
13:09:12  ПРАВКА price_source.py         ← найден дефект, чинится
13:10:19  test_price_source.py           ← тест на исправление
13:10:48  перепрогон 2 пар               (rerun2.py)
13:12:40  перепрогон завершён            (drafts_rerun2.jsonl = 2, drafts_fixed.jsonl = 26)
13:12:45  ИТОГОВЫЙ СЧЁТ                  (score_fixed.txt, verdicts_both.json)
13:12:47  ТАЙМАУТ 2700s → failed         ← через 2 СЕКУНДЫ
```

Баланс окна: подготовка 756с (28.0%) + прогон 1482с (54.9%) + разбор/починка/перепрогон 462с
(17.1%) = 2700с ровно. На коммит, артефакт и журнал осталось **0 секунд**.

**Прямой ответ.** Арифметика замера была подъёмной (1300–1594с из 2700). Непосильным окно
стало потому, что в него, кроме 26 единиц, обязаны были уместиться: полный гейт (229с только
прогона тестов), новый модуль на 18 КБ, тест к нему, две батареи фикстур, **исправляющий
второй прогон** и — сверх всего — коммит, артефакт и журнальная строка. Запаса на цикл
«померил → нашёл дефект → починил → перемерил» в бюджете не было ни секунды, а именно этот
цикл и случился.

---

## 5. Детектор: по какому признаку куратор решил «следов нет»

### 5.1 Место в коде — словами

- `pc_orchestrator.py:1246` — `fail_result(code, detail, since, now)`, текст итога провала.
  Три формы по трём состояниям знания; интересующая — вторая.
- `pc_orchestrator.py:1269` — сам литерал:
  `"%sпровал %s: %s. Следов работы в окне %s нет (коммитов 0, записей журнала 0)."`
- Улики собирает `pc_orchestrator.py:1228` — `_work_evidence(since, until)`, и возвращает
  он ровно два списка: `{"commits": …, "journal": …}`. Третьего источника нет.
  - `pc_orchestrator.py:1184` — `_git_commits_between`: запускает
    `git log --no-merges --reverse --since=… --until=…`.
  - `pc_orchestrator.py:1202` — `_journal_writes_between`: читает реестр `COWORK_LEDGER`
    (`cowork_log.ledger`), берёт строки, реально ушедшие в журнал в окне.

### 5.2 Входит ли незакоммиченное в его счёт — НЕТ

**Не входит по построению.** `git log` перечисляет ИСТОРИЮ; рабочее дерево ему не предмет.
Ни `git status`, ни `git diff`, ни обход файловой системы в `_work_evidence` не зовутся ни
одной строкой. Следствие в двух шагах:

1. изменённый tracked-файл (`suggest.py`, 25 добавленных строк) — **не улика**;
2. untracked-файлы (`price_source.py` 18 КБ, `test_price_source.py`) — **не улика тем более**.

Живой вывод в логе, дословно:

```
2026-08-17 20:12:47,749 WARNING FAIL причина=run_timeout окно=17.08 12:27–13:12 UTC следы: коммитов=0, записей журнала=0
```

### 5.3 Самое острое: тот же процесс через 5 секунд говорит обратное

```
2026-08-17 20:12:47,750 INFO METRICS task=16 … outcome=failed …
2026-08-17 20:12:50,609 INFO COMPLETE id=16 status=failed
2026-08-17 20:12:52,058 INFO авто-фетч: рабочая копия грязная — git pull пропущен; файлы: suggest.py
```

Демон **видит** правку — но другой веткой кода (`pc_orchestrator.py:6096`, `_dirty_tracked`
внутри авто-фетча), и эта ветка с `fail_result` не связана ничем. Две ветки одного процесса
за 5 секунд отвечают на вопрос «была ли работа» противоположно.

И даже эта, зрячая, ветка увидела бы только треть остатка: на `pc_orchestrator.py:6091`
строки `porcelain`, начинающиеся с `??`, отфильтровываются — `price_source.py` и
`test_price_source.py` не попали бы и туда.

### 5.4 Проверка признака на соседе

Задача 36 упала тем же `run_timeout` в те же 2700с — и получила
«**НЕ ЗАКРЫТА, но В ОКНЕ ЗАДАЧИ ЕСТЬ РАБОТА**». Разница не в объёме сделанного, а
исключительно в том, что в её окно попали коммиты. Признак различает не работу, а её
**фиксацию**.

**Не правил.** Точка касания — `_work_evidence` (`pc_orchestrator.py:1228`) и её два
источника; лечится добавлением третьего (грязь дерева, включая untracked). В остатки.

---

## 6. Что именно спасено: результат замера задачи 16

Файл `_scratch_pricewire_0817/score_fixed.txt`, дописан 13:12:45 UTC, дословно (сокращено
до разделов 1–3; попарная таблица 26 строк — в файле):

```
ВХОД: базовая линия 26 пар (drafts_part2.jsonl) · с флагом 26 пар (drafts_fixed.jsonl)
СПИСОК ПАР СОВПАЛ ПО ВСЕМ 26.
ЗАМОК ВХОДА: лент разошлось между прогонами 0 · записанное≠пересобранное 0 OK

1. ЦЕНА
  БЫЛО (e9231b3)        линия 1/26 =  3.8% · ЧЕСТНЫЙ ЗНАМЕНАТЕЛЬ 0/15 =  0.0% · назвал цену 8 из 21 ответивших (молчаний 13) · отказов 5 · витрин 5
  СТАЛО (флаг поднят)   линия 3/26 = 11.5% · ЧЕСТНЫЙ ЗНАМЕНАТЕЛЬ 2/15 = 13.3% · назвал цену 10 из 21 ответивших (молчаний 11) · отказов 5 · витрин 5
  знаменатель А до/после: 15 / 15 — ТОТ ЖЕ СПИСОК
  попали по цене: было [258] · стало [120, 148, 179]

2. МОДЕЛЬ И ДАТЫ — ОБЯЗАНЫ НЕ СДВИНУТЬСЯ
  модель       было попал 4 / не попал 0 / не судимо 17 / отказ 5
               стало попал 4 / не попал 0 / не судимо 17 / отказ 5
  дата начала  было попал 3 / не попал 3 / не судимо 15 / отказ 5
               стало попал 6 / не попал 2 / не судимо 13 / отказ 5
  дата конца   было попал 4 / не попал 2 / не судимо 15 / отказ 5
               стало попал 5 / не попал 3 / не судимо 13 / отказ 5
  ПАР С ИЗМЕНИВШИМСЯ ВЕРДИКТОМ по модели/датам: 6 [108, 139, 155, 179, 197, 273]

3. ДВА ИЗВЕСТНЫХ ЗАНИЖЕНИЯ (179: назвал 317 при 464 · 120: назвал 577 при 798)
  dlg=179  БЫЛО  уплачено/сут 464 · полоса таблицы 436 · мерка увидела ничего
  dlg=179  СТАЛО уплачено/сут 464 · полоса таблицы 436 · мерка увидела [437, 3059]
  dlg=120  БЫЛО  уплачено/сут 798 · полоса таблицы 731 · мерка увидела ничего
  dlg=120  СТАЛО уплачено/сут 798 · полоса таблицы 731 · мерка увидела [710, 9940]
```

Что это значит владельцу, без прикрас:

- **Цель достигнута:** оба живых занижения, ради которых заводился `price_source.json`,
  закрылись — 317→437 (уплачено 464) и 577→710 (уплачено 798).
- **Побочного вреда по модели нет:** 4/0/17/5 до и после, ни одной пары не потеряно.
- **Даты сдвинулись** (начало 3→6 попаданий), хотя правка их не касается — это ОТДЕЛЬНОЕ
  наблюдение, требующее объяснения, а не победа.
- **Есть регресс:** `dlg=258` был попаданием по цене и перестал им быть. Прирост 1/26 → 3/26
  честнее читать как «+3 −1».
- **Гейт ДО правки был красен на 2** (`gate_before.txt`: `Ran 3628 tests … FAILED
  (failures=2, skipped=10)`) — это известный храповик `main`, не вклад задачи 16.
  Гейта ПОСЛЕ правки в остатке НЕТ — задача до него не дожила.

**Ничто из этого не было сказано владельцу ни разу:** карточка задачи 16 сообщила
«следов работы нет».

---

## 7. Остатки — списком, ничего не сделано

1. **Прибор куратора слеп к незафиксированной работе.** `_work_evidence`
   (`pc_orchestrator.py:1228`) знает два источника из трёх. Точки касания: `:1184`,
   `:1202`, `:1228`, `:1269`; зрячая, но не подключённая к вердикту ветка — `:6091`, `:6096`
   (и она сама фильтрует `??`). Класс общий с полосой VPS — **на ту полосу не проверено**.
2. **Остаток задачи 16 не закоммичен и не отвергнут.** Три файла в дереве. Решение
   «доводить или откатывать» — владельца; мой заход их не трогал.
3. **Гейт после правки не прогонялся** — зелёность подключения НЕ ДОКАЗАНА ни одним прогоном.
   Утверждать «правка безопасна» нельзя.
4. **Регресс `dlg=258` не объяснён.**
5. **Сдвиг вердиктов по датам (6 пар) при правке, датам не адресованной,** — не объяснён.
6. **Полный текст ТЗ 16 недоступен с этой полосы** — слепок хранит только первую строку.
   Если он нужен для суда о полноте исполнения, читать его должен владелец из очереди.
7. **Бюджет окна не учитывает цикл починки.** Задача с прогоном на половину окна не имеет
   запаса на «померил → починил → перемерил». Это устройство, а не невезение: так же
   умерла бы любая такая задача.
