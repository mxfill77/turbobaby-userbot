# Реапер молчал не потому, что не видел полосу — замер и правка (05.08.2026)

Повод: задача 305 провисела `in_progress` без исполнителя **5569с** и была снята только
90-минутным таймаутом. Перекличка Dispatch
([`2026-08-05-task305-rollcall.md`](2026-08-05-task305-rollcall.md)) назвала причиной молчания
реапера нечитаемую полосу. **Замер эту причину не подтвердил.**

---

## 1. Почему реапер не увидел сироту — место и число

Реапер — `pc_orchestrator.process_stuck_singles`. У его молчания есть ровно два кандидата, и оба
надо было разделить числом.

**Кандидат А — «полоса не читается»** (`pc_orchestrator.py:3365`): отказ `get_pending(in_progress)`
→ строка «реапер молчит: не прочитал полосу — не сужу задачи» → выход. За 7 суток таких тиков
**393** (см. поправку ниже; в первой редакции здесь стояло «13, все 05.08» — число было измерено
неверно).

> **ПОПРАВКА 05.08 (независимая сверка, тот же день).** «13 тиков, все 05.08» — число
> **born-false**, и родилось оно ровно тем классом, от которого предостерегает CLAUDE.md: считался
> не сам отказ, а **фраза в логе**. Счётчик `mute` в `measure_orphans.py:135` матчил строку
> «реапер молчит», а эту формулировку внёс коммит **831b69f — сегодня же, 05.08**. Поэтому
> семидневное окно физически не могло дать ничего, кроме сегодняшних строк, и «все 05.08» было не
> выводом, а **тавтологией счётчика**. Отказы по правильному признаку (`BAND_ERR`,
> `get_pending(in_progress) ошибка`) за то же окно:
>
> | 29.07 | 30.07 | 31.07 | 01.08 | 02.08 | 03.08 | 04.08 | 05.08 | итого |
> |---|---|---|---|---|---|---|---|---|
> | 3 | 1 | 4 | 16 | 46 | 119 | 97 | 107 | **393** |
>
> Полоса не читается **не изредка и не впервые сегодня**: отказы идут каждый день и с 01.08 выросли
> на два порядка (сейчас ~100/сут ≈ 7% витков; в живом логе это `BridgeTransportError` — второе
> плечо моста бьёт обратно на свой же `/exec`). **Вердикт §1 от этого НЕ меняется:** он стои́т не на
> этом счётчике, а на подушевой колонке таблицы ниже, а она с самого начала считалась по
> правильному `BAND_ERR` — у всех шести сирот после истечения порога отказов действительно **0**.
> Меняется другое: «нечитаемая полоса» — живая и растущая болезнь соседнего узла, а не разовый
> шум, и с коротким порогом она стала значить БОЛЬШЕ (см. §3).

**Кандидат Б — условие** (было `if age <= PC_SINGLE_STALE: continue`): возраст `updated`, то есть
МОЛЧАНИЕ задачи, против порога **5400с**.

Разделяет их один вопрос: **сколько отказов чтения пришлось на момент, когда порог УЖЕ истёк** —
только такой отказ и стоит задаче лишнего времени. Ответ для всех шести сирот недели:

| сирота | висела | порог истёк | снята | отказов чтения ПОСЛЕ истечения порога |
|---|---|---|---|---|
| 61 | 6169с | 30.07 21:11:12 | 21:24:01 | **0** |
| 162 | 5410с | 01.08 17:37:08 | 17:37:18 | **0** |
| 193 | 5472с | 02.08 21:46:38 | 21:47:50 | **0** |
| 284 | 5682с | 05.08 02:34:00 | 02:38:42 | **0** |
| 291 | 7653с | 05.08 11:16:34 | 11:54:07 | **0** |
| 305 | 5569с | 05.08 14:13:04 | 14:15:53 | **0** |

Для 305 это видно по логу пошагово: первый же тик реапера после claim (12:45) полосу прочитал
(отказа в логе нет) — но возраст был ~120с; шесть отказов 13:34–14:11 пришлись на возраст
3100–5330с, то есть **ниже порога в любом случае**; тик 14:15:53 полосу прочитал и снял задачу.
Нечитаемая полоса не стоила задаче 305 **ни одной секунды**.

**Вердикт: реапер молчал ПО УСЛОВИЮ.** Он видел сироту, признавал её живой и обязан был молчать,
потому что мерил тишину, а тишина 90 минут — это норма для полосы, где живой прогон тоже молчит
(`TASK_TIMEOUT` = 2700с, heartbeat во время прогона не тикает). Прав, доступа, вызова — всё на
месте: реапер стои́т первым в `poll_once` и отрабатывает каждый виток.

## 2. Замер: 6 сирот за 7 суток, 10,0 ч висения, 90% из них — сам порог

Скрипт замера: `tmp_orphan_20260805_v7/measure_orphans.py` (read-only по `pc_orchestrator.log`,
окно 29.07–05.08).

```
сирот: 6, суммарно провисели 35955с = 10,0 ч, в среднем 5992с = 100 мин
  вклад САМОГО порога:            32400с (9,0 ч) = 90%
  простой демона/чтение сверх него: 3555с (1,0 ч) = 10%
```

Две РАЗНЫЕ группы, и обе — сироты в одном смысле «нет процесса»:

- **не запускались на ПК ни разу — 3** (162, 193, 305): claim лёг на мосту, расписка потерялась,
  прогон не начался. В реестре отметок старта (`pc_orchestrator.task_started.json`, 105 ключей)
  ключа 305 нет вовсе — при том, что 315 и 317 там есть;
- **запускались, исполнитель умер, задача осталась `in_progress` — 3** (61, 284, 291): у 284 и
  291 в логе есть и `RUN`, и `COMPLETE status=failed`, а `updated` на мосту так и остался равен
  моменту claim — то есть запись финала не легла, а расписка об этом промолчала.

Просрочка сверх порога (1,0 ч на шестерых) — это не чтение полосы, а простой: демон одноворкерный,
и пока он 45 минут ведёт другую задачу, реапер не работает вовсе (61: 12,8 мин, 291: 37,5 мин —
там между истечением порога и реапом лёг self-update-рестарт).

## 3. Правка: сироту опознаём по исполнителю, а не по тишине

Молчание — плохая улика: живой прогон молчит так же, как мёртвый. Хорошая улика — **процесс**.

Добавлен `_executor_verdict(tid)` (`pc_orchestrator.py:3294`) — трёхзначный ответ
**есть / ДОКАЗАННО нет / не знаю**:

| улика | вывод |
|---|---|
| `_RUNNING_TID == tid` — задачу ведёт этот процесс прямо сейчас | ЕСТЬ |
| реестр отметок пуст/нечитаем | **не знаю** (потерянный реестр неотличим от «не запускалась») |
| отметки задачи нет, а реестр есть | НЕТ: прогон не начинался ни разу *(случай 305)* |
| в отметке записан PID ребёнка | судим по нему: жив → ЕСТЬ, мёртв → НЕТ *(случай 284/291)* |
| отметку делал ЭТОТ процесс, а исполняем не её | НЕТ: демон синхронный, его дети живут внутри `run_task` |
| отметку делал другой процесс, PID ребёнка не записан | **не знаю** (тот демон мог умереть, а headless — остаться) |

Чтобы четвёртая строка работала, `run_claude` пишет PID порождённого claude в реестр **сразу после
спавна** (`pc_orchestrator.py:1464`) — это единственный момент, когда PID вообще известен, и
единственная улика, по которой СЛЕДУЮЩИЙ процесс демона отличит работающий headless от мёртвого.
Формат отметки расширен со строки до словаря `{at, pid, child}`; старую голую строку читатели
понимают по-прежнему (окно работы не теряется), но исполнителя по ней назвать нельзя → «не знаю».

Дальше решает порог: **ДОКАЗАННАЯ сирота — `PC_ORPHAN_STALE` = 300с** (5 витков поллинга),
всё остальное — прежние `PC_SINGLE_STALE` = 5400с. Плюс безусловный замок: задачу, которую демон
ведёт прямо сейчас, реапер не трогает никаким возрастом (возраст `updated` растёт по стенным
часам, а бюджет прогона — только по бодрствованию; проспавший ПК делает живую задачу «старой»).

Что это дало бы на неделе замера, при тех же отказах моста и том же простое демона:

```
id=61   6169с → ~1069с      id=284  5682с → ~582с
id=162  5410с →  ~310с      id=291  7653с → ~2553с
id=193  5472с →  ~372с      id=305  5569с →  ~469с
суммарная экономия ≈ 30600с = 8,5 ч из 10,0
```

**Поправка к этой оценке (та же сверка 05.08).** Цифры выше переносят на новый порог просрочку,
замеренную у СТАРОГО, и тем самым молча считают, что в новом окне полоса читается всегда. Это не
так: окно `[старт+300с, снятие]` в 17 раз шире прежнего, и отказы чтения в него попадают — в
отличие от окна порога 5400с, где их было 0. Замер по тому же логу
(`tmp_reaper_verify_20260805_k9/verify_new_window.py`, read-only):

| сирота | 61 | 162 | 193 | 284 | 291 | 305 |
|---|---|---|---|---|---|---|
| отказов чтения в окне нового порога | 1 | 2 | 1 | **18** | 4 | 6 |
| честный ПОЛ новой задержки | ≥360с | ≥420с | ≥360с | ≥1380с | ≥540с | ≥660с |

То есть для 305 честнее «**≥660с**», а не «~469с». **Вывод правки это не отменяет** — 10,0 ч
превращаются в ~1,0 ч, экономия порядка 9 ч остаётся, и даже худшая сирота (284) снимается за
23 минуты вместо 95. Но обещание «5 минут» верно лишь при читаемой полосе: **фактический потолок
скорости реапа задаёт теперь не порог, а здоровье моста**. Это и есть настоящий остаток задачи —
не в реапере, а в транспорте (`BridgeTransportError`, ~100 отказов/сут и растёт).

Итог владельцу теперь называет факт, а не догадку: вместо «ПК был выключен, либо прогон
застрял/оборвался» — «СИРОТА: живого исполнителя нет — прогон на этом ПК не начинался ни разу».
Код причины прежний (`heartbeat_timeout`) — его читает серверная полоса.

**Граница проведена сознательно:** живой PID ребёнка отменяет только БЫСТРЫЙ реап. Задача с живым,
но зависшим ребёнком мёртвого демона по-прежнему добивается на 90-й минуте — иначе правка закрыла
бы один класс вечного зависания и открыла другой.

## 3-бис. Дефект В САМОЙ правке, найденный сверкой: молчание пробы выдавало себя за смерть

Правка §3 научила реапера судить по процессу — и тем самым поставила ВСЮ тяжесть решения на пробу
`_lock_pid_alive`. А проба была **двузначной и глотала собственный отказ**:

```python
    except Exception:
        return False        # ← «tasklist не ответил» неотличимо от «процесса нет»
```

Для синглтона (её прежний и единственный читатель) это осознанно: не поднять второго демона хуже,
чем поднять. Но у реапера `False` означает **ДОКАЗАННУЮ смерть исполнителя**, а доказанная смерть
включает короткий порог 300с. Итог: **один таймаут `tasklist` снимал ЖИВУЮ задачу** — ту самую,
у которой ребёнок жив и работает. Это ровно **класс #171 своей полосы**: проба процессов на
пробуждении ПК отвечает не сразу, и её молчание уже однажды выдало себя за смерть (ложная смерть
вотчдога, `f89da43`). Правка §3 внесла тот же класс слоем ниже — и нарушила собственное правило,
записанное в её же докстринге: «„не знаю“ не смеет выдавать себя за „нет“».

Показано ЗАПУСКОМ, а не рассуждением: на модуле из `2a77206` живая задача с живым ребёнком при
таймауте пробы получает `'failed' != 'in_progress'`.

**Правка.** Проба разведена на две по контрактам, а не скопирована:
`_pid_alive_probe` — трёхзначная (`True | False | **None — проба не ответила**`), её зовёт реапер;
`_lock_pid_alive` — прежняя двузначная обёртка над ней (`... is True`), её контракт для синглтона
не изменился ни на бит. `_executor_verdict` на `None` отвечает «не знаю» → длинная мерка 5400с →
работа цела.

**Замер, без которого правка была бы хуже дефекта:** `tasklist` на несуществующий PID отвечает на
этой машине **`rc=0`** и строкой «задачи не найдены». Значит разделять «мёртв» и «сбой пробы» надо
по СОДЕРЖИМОМУ, а не по коду возврата — иначе `rc != 0→ None` тихо отключило бы ветку «ребёнок
мёртв» (случаи 284/291), то есть половину §3.

## 3-тер. Ещё два места, где правка §3 казнила по недоказанному

Общий корень у всех трёх (§3-бис и двух ниже) один: правка §3 повысила **цену** старых, вполне
терпимых неточностей. Пока сирота ждала 90 минут, любое «показание» успевало быть перепроверенным
десятками витков; с порогом в 300с ошибка первого же витка стала приговором. Поэтому проверять
надо было не только вывод, но и КАЖДОГО свидетеля, на которого вывод опирается.

**(1) Один потерянный реестр делал сиротами ВСЮ полосу сразу.** `_task_started_read` возвращал `{}`
на любой сбой чтения — и молча. Ветка «пусто → не знаю» защищала лишь до первого claim'а: демон
тут же пересоздавал файл **с одним ключом**, реестр переставал быть пустым, и все ОСТАЛЬНЫЕ живые
`in_progress` полосы попадали в ветку «отметки нет» = «прогон не начинался ни разу» → снятие за
5 минут. Запись реестра не атомарна (`open("w")` + `json.dump`), так что оборванная запись — ровно
тот случай, ради которого реестр и заведён.
**Правка:** отсутствие ключа — показание только если реестр **застал** старт задачи
(`_task_started_covers`: самая старая отметка ≤ момента, с которого задача молчит). Реестр моложе
задачи → «не знаю» → длинная мерка. Плюс сбой чтения теперь виден в логе, а не глотается.
Случай 305 при этом снимается быстро как прежде (реестр помнил дни до задачи) — на это стои́т
отдельный тест `test_registry_older_than_task_still_proves_orphan`, иначе правка съела бы §3.

**(2) `pid == os.getpid()` — не личность процесса.** Улика 5 («отметку делал Я, а веду не её»)
сравнивала голый PID, который Windows переиспользует. После self-update-рестарта совпадение номера
достижимо, и уводило оно **в опасную сторону**: чужая ЖИВАЯ задача объявлялась своей брошенной.
Стандарт этого же файла — PID + время рождения (`_is_descendant`, «Windows переиспользует PID…»);
на единственное сравнение, решающее судьбу работы, он перенесён не был.
**Правка:** в отметку пишется `proc` — метка запуска (`_PROC_TOKEN`, PID + момент старта, считается
раз при импорте); улика 5 сравнивает ЕЁ. Отметки без метки (старый формат) уходят в «не знаю».

Обе найдены не рассуждением, а **воспроизведены запуском** на модуле `2a77206` (см. §4).

**Остаток, НЕ закрытый здесь** (назван, но не правлен — чтобы не расширять правку молча): урок в
законном 24-часовом ожидании защищён от реапера ровно одним файлом состояния
(`_lesson_waits_read` → `{}` при любом сбое чтения). Раньше поверх этой защиты лежал запас в
90 минут, теперь — 5. И сравнение `if tid in waiting` сопоставляет сырой id моста с множеством
`int`, тогда как весь остальной новый код нормализует через `str()`; фейковый мост в тестах отдаёт
`id` числом, поэтому голден этот разрыв не увидит (класс «мок ≠ живой формат»).

## 4. Регресс

`test_pc_orchestrator.TestOrphanNoExecutor` — 19 тестов: задача без исполнителя снимается
(четыре разных доказательства отсутствия), живая не снимается (текущая задача, живой PID ребёнка,
ждущий урок, чужая полоса, обрыв связи, «не знаю»).

- **красный ДО правки: 12 из 19** — модуль из `HEAD` подменён в `tmp_orphan_20260805_v7/red/`,
  старому модулю доклеены только ЛЕСА (имена `PC_ORPHAN_STALE`, `_RUNNING_TID`,
  `_task_started_child/_rec`, которыми тесты готовят состояние; вердикта `_executor_verdict` там
  нет) → падения поведенческие: сирота не снимается, живая задача не защищена;
- **зелёный ПОСЛЕ: 19 из 19**;
- **полный гейт: 3040 тестов, OK** (skipped=10).

**Сверка 05.08 (независимый повтор обоих прогонов + правки §3-бис и §3-тер).** Красный ДО
воспроизведён заново на модуле из `88d53de^` — те же **12 из 19**; зелёный ПОСЛЕ — 19 из 19;
полный гейт 3040 OK. Дальше — регресс на три новых дефекта, прогнанный против `2a77206`
(`tmp_reaper_verify_20260805_k9/red_probe/`), **25 тестов, красных 6**:

| красный ДО | почему красный | дефект |
|---|---|---|
| `test_probe_silence_is_not_death` | **живая задача снята**: `'failed' != 'in_progress'` | §3-бис |
| `test_registry_reborn_after_loss_does_not_orphan_the_lane` | **живая задача снята** после потери реестра | §3-тер (1) |
| `test_pid_reuse_by_another_daemon_is_not_proof` | **живая задача снята** при совпавшем PID | §3-тер (2) |
| `test_verdict_without_since_never_claims_proof` | `False is not None` — приговор без свидетеля | §3-тер (1) |
| `test_probe_failure_modes_are_unknown_not_dead` | трёхзначной пробы в старом модуле нет | §3-бис |
| `test_verdict_tri_state` | параметра `since` в старом модуле нет | §3-тер (1) |

Четыре из шести — **поведенческие**, то есть старый код не «не знал имени», а СНИМАЛ ЖИВУЮ ЗАДАЧУ;
две — честное «возможности не было». Остальные 19 тестов на старом модуле **зелёные**: артефактных
падений (когда тест краснеет лишь оттого, что подменяет несуществующее имя) в наборе не осталось —
подмена делается по живому шву `subprocess.run` с настоящим форматом вывода `tasklist`, а не по
внутреннему имени пробы. **Зелёный ПОСЛЕ — 25 из 25.**

## 5. Заодно, без правки: покрыт ли claim класса «расписка моста — не факт»

**Покрыт.** И это не форма: `Bridge.claim_task` (`pc_orchestrator.py:619`) на отказе расписки,
не входящей в `_CLAIM_SEMANTIC` (а `BridgeReceiptLost` в неё не входит), ПЕРЕЧИТЫВАЕТ статус через
`_in_status` и, увидев `in_progress`, работает штатно. Заведено **cc8d008** (02.08, «claim и
complete судят по перечитанному статусу, а не по расписке») — это ОТДЕЛЬНЫЙ коммит от **ab5106d**
того же дня, который чинил транспорт (не идти по 302). Страховка живая, сегодня она сработала
дважды: `CLAIM-VERIFY id=284` в 01:04:32 и `CLAIM-VERIFY id=297` в 10:46:11.

**Дыра — не в отсутствии страховки, а в её однократности и в канале.** `_in_status` спрашивает ТОТ
ЖЕ мост ТЕМ ЖЕ чтением РОВНО ОДИН РАЗ, а свой честный ответ «не прочитал» (`None`) отдаёт наверх,
где он сливается с «не легло»: цикл пропускается (fail-safe). На 305 в 12:44:01 именно это и
произошло — расписка потерялась, перечитать не вышло, `CLAIM-VERIFY` в логе нет, задача осталась
сиротой на мосту. Ровно так же не спасла бы она 162 (`HTTPError`, 01.08) и 193 (`unauthorized`,
02.08) — обе старше cc8d008.

Кандидаты в правку (здесь НЕ делались): повтор перечитывания с паузой (мутация не переотправляется,
это чтение); и/или локальный след «claim мог лечь, tid=N» — сегодня демон знает id несостоявшейся
сироты в момент её рождения и молча его забывает. **Сама по себе эта дыра больше не стоит полутора
часов:** правка §3 снимает такую сироту за ~5 минут вместо 90.

---

Замер: `tmp_orphan_20260805_v7/measure_orphans.py`, красный прогон: `tmp_orphan_20260805_v7/red/`.
Боевых записей не делалось, процессы не трогались, `.env` не открывался.
