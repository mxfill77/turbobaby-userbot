# Второе применение второго ключа ворот — ключ ПОВЁРНУТ, ворота ОТКРЫЛИСЬ, дети НЕ поехали

**Дата факта: 26.08.2026, 03:05–03:52 (+07).** Задание просило заголовок с датой 25.08; машинные
часы, журнал событий Windows, штамп прогона (`when: 2026-08-26 03:35:35`) и время демона дают
**26.08** — в заголовок поставлена дата ФАКТА, расхождение названо, а не подогнано. Ось: бизнес.

## ИТОГ ОДНОЙ СТРОКОЙ

**Ворота ОТКРЫЛИСЬ впервые за всю свою жизнь** (`release_reason('74be777a8') -> 'trainer'`, первая
зелёная запись в ящике вердиктов за всю историю), **но дети остались на `9f6360c`** — за воротами
стои́т ВТОРОЙ барьер, гейт затронутых тестов, и он красный семью ПРЕЖНИМИ голденами. **Снято 0
коммитов отставания из 4.** Откат не понадобился ни по одному из трёх поводов.

---

## 1. Состояние машины (замер, повторён после всех действий)

| что | значение |
|---|---|
| падения за сутки (Kernel-Power 41 · BugCheck 1001 · EventLog 6008) | **0** |
| ошибки WHEA за сутки | **0** |
| контроль на ложное молчание запроса: те же Id за 7 суток | **12** падений и **6** WHEA, последнее — 23.08 18:28:09 и 23.08 19:53:04 |
| аптайм | **57.4 ч** (загрузка 23.08 18:27:46) — механически исключает перезагрузку за сутки |
| память | 2×8 ГБ, `Speed` и `ConfiguredClockSpeed` = **2667 МГц** обе планки, 1.2 В |
| процессор | i7-8700K, номинал 3700 МГц, `MaxClockSpeed` 4701 |
| частота под нагрузкой | **4701 МГц** (три выборки подряд, `Processor Frequency`); `% Processor Performance` = **127 %** от номинала |
| частота на простое | **4701 МГц** — та же |

Две честные оговорки, обе — незакрытые вопросы, а не находки этой сессии:

* **память работает НИЖЕ своей планки.** Комплект `CMR16GX4M2Z3200C16` рассчитан на 3200 МГц,
  живёт на 2667 — профиль XMP не применён. На вердикт это не влияет ничем, но «2667» само по
  себе не означает «норма для этих планок».
* **процессор держит 4701 МГц ПОСТОЯННО — и под нагрузкой, и на простое.** Множитель не
  опускается вовсе: это ровно тот авто-разгон платой, который владелец держит открытым вопросом.
  Троттлинга нет (частота не проседает), но и штатного сброса на простое тоже нет.

Падений за сутки не было → правило «падения были — остановиться» не сработало, работаем дальше.

## 2. Вершина дерева и её чистота

```
74be777a833568bcf96a2a2edbf30ccd1fbfd18d
2026-08-25 17:25:52 +0700  стилевая половина правила об опыте поднята до жёсткой: ветка A.2 …
git status --porcelain --untracked-files=no  →  ПУСТО
```

Вершина **равна** `74be777`, отслеживаемое дерево **чистое** — оба условия входа выполнены.
Untracked есть (рабочие каталоги `_scratch_*`), но `tree_dirty` продукта их не считает, и это
та же граница, что у `git diff --name-only HEAD`.

**Премисы проверены первым действием — три из четырёх подтвердились дословно, четвёртая уточнена:**

1. **правило об опыте поднято до жёсткого коммитом `74be777`** — да. Половин у правила стало две
   (`a3` прежняя, `a2` новая); граница взята самая узкая из трёх посчитанных («первый контакт +
   про опыт не говорили + модель не названа + прайс не просят + **клиент НАЗВАЛ СРОК**»): 8
   черновиков под веткой из 96, дописок в корпусе 0. Отвергнутые — широкая (80 под веткой, 72
   дописки) и средняя (16/8, все 8 садятся на кейс 4);
2. **три отрицательных теста зелёные** — да, они в коммите поимённо:
   `test_negativ_a2_chernovik_bez_upominaniya_poyman`,
   `test_negativ_a2_chernovik_s_upominaniem_prohodit_netronutym`,
   `test_negativ_a2_klient_nazval_opyt_pereprosa_net`;
3. **дети работают на `9f6360c`** — да, посчитано парой функций самого демона
   (`_proc_started_at` → CIM-время старта, `_commit_at_time` → вершина на тот миг);
4. **«живой прогон после правки красных не дал»** — верно ДОСЛОВНО и требует уточнения, важного
   для бизнеса: тот прогон (25.08 ~17:44) был **не зелёным, а НЕИЗВЕСТНЫМ** — 8 кейсов из 12,
   чеков 171/171, **красных ноль**, но **6 кейсо-прогонов «голова промолчала»**. Вердикта он не
   писал (`--no-write`), в ящике его нет. То есть на входе в сегодняшний заход про `74be777` было
   известно «красных не видели», но НЕ «набор зелёный».

## 3. Ворота ДО вердикта — дословно

```
==============================================================================
РЕШЕНИЕ ВОРОТ — ДО ВЕРДИКТА (сессия 26.08 03:0x)
==============================================================================
вершина          : 74be777a833568bcf96a2a2edbf30ccd1fbfd18d
в ворота идёт как: 74be777a8   (реконсиляция подаёт head[:9])
дерево грязное   : нет (чисто)
контур заморожен : True

release_reason(74be777a8) -> None
  owner_approved  -> False
  trainer_enabled -> True
  trainer_verdict -> (False, 'прогона не было')

ЯЩИК ВЕРДИКТОВ pc_orchestrator.client_trainer_green.json:
  зелёных записей: 0
  красных записей: 3 ['55aad22', '6478a39', '91d244e']

корпус на диске  : sha=98ad5e3e2c256542
порог            : кейсов>=12, прогонов>=2
```

## 4. Один живой прогон набора — штатным каналом, синхронно

Командная строка — **ровно штатная**, без ключей отбора, без `--no-write`, без пина:

```
D:\turbobaby-bot\venv\Scripts\python.exe trainer_run.py --report D:\turbobaby-bot\_scratch_key2_0826\run_live_1.md
```

| факт | значение |
|---|---|
| старт → финиш | **2026-08-26 03:22:03 → 03:35:35** |
| время | **812.0 с = 13 мин 31 с** |
| код выхода | **0** (0 — зелёный · 1 — не зелёный · 2 — прогон не состоялся) |
| коммит, зафиксированный на входе | `74be777a833568bcf96a2a2edbf30ccd1fbfd18d` |
| **кейсов** | **12 из 12** |
| **чеков** | **224 из 224** |
| прогонов внутри захода | **2** (порог ворот) |
| **состав красных** | **ПУСТ**: `failed: []` — ни одного |
| **число «неизвестно»** | **0** (`unknown: 0`, `unknown_why: []`) |
| дерево на прогоне | чистое (`clean: true`, `tree_dirty: false`, `dirty_paths: []`) |
| вершина за время прогона | **неподвижна** (`head_moved: ""`, `head_after` = тот же хеш) |
| корпус | `trainer_cases.json`, sha `98ad5e3e2c256542` — совпал с диском |
| прогонов снято | **ОДИН.** Он годен (судить было чем: «неизвестно» ноль), поэтому по правилу «первый годный решает» дополнительных не добирал |

**Промахи границ — 0, и это измерено кейсами, а не заявлено.** Новая узкая ветка `a2` обязана
включаться ровно на кейсе 6 и не включаться больше нигде:

* **сработала там, где должна**: кейс 6 «опыт не упомянут (A.2)» — 🟢 **оба прохода**, чек
  «обязательное упоминание» зелёный (24 с и 23 с). Вчера ровно этот чек был единственным красным;
* **не сработала там, где не должна**: кейсы 1–5 и 7–12 — 🟢 оба прохода каждый. В частности
  кейс 4 «только про депозит — контрпример прайса» 🟢🟢 (на него садилась отвергнутая средняя
  граница) и кейс 5 «новичок + мощный байк (A.3)» 🟢🟢 (соседняя половина правила).

Единственное снятие чека за весь заход — прежнее и документированное: кейс 2, «депозит без
противоречий» (в сетке по парку депозит СВОЙ у каждой модели — это прайс, а не самопротиворечие).
Оно было и вчера, ветка не новая.

Отчёт прогона таблицей: `_scratch_key2_0826/run_live_1.md`, полный stdout —
`_scratch_key2_0826/run_stdout_1.txt`.

О синхронности: заход шёл 13 мин 31 с, а один вызов инструмента живёт не дольше 10 минут. Прогон
запущен **один раз, одним процессом (PID 7008)**, и на него ЖДАЛИ — ожидание разбито на два
последовательных блокирующих ожидания того же PID, между ними не делалось ничего. Параллельного,
фонового и повторного здесь нет.

## 5. Вердикт — что записано и куда

Записан **штатным каналом** (`trainer_run.write_verdict`), файл
`D:\turbobaby-bot\pc_orchestrator.client_trainer_green.json`, время файла 26.08 03:35. Дословно:

```json
{
  "commit": "74be777a833568bcf96a2a2edbf30ccd1fbfd18d",
  "result": "green",
  "unknown": 0,
  "unknown_why": [],
  "checks_passed": 224,
  "checks_total": 224,
  "cases": 12,
  "cases_total": 12,
  "runs": 2,
  "clean": true,
  "head_moved": "",
  "head_after": "74be777a833568bcf96a2a2edbf30ccd1fbfd18d",
  "tree_dirty": false,
  "tree_known": true,
  "dirty_paths": [],
  "corpus": "trainer_cases.json",
  "corpus_sha": "98ad5e3e2c256542",
  "runner": "trainer_run.py",
  "ts": 1787690135.2370048,
  "when": "2026-08-26 03:35:35",
  "failed": []
}
```

Последние две строки stdout раннера:

```
вердикт: D:\turbobaby-bot\pc_orchestrator.client_trainer_green.json
ворота клиентского контура откроются на этот коммит по основанию «trainer»
```

## 6. Ворота ПОСЛЕ вердикта — дословно

```
==============================================================================
РЕШЕНИЕ ВОРОТ — ПОСЛЕ ПРОГОНА (26.08 03:36)
==============================================================================
вершина          : 74be777a833568bcf96a2a2edbf30ccd1fbfd18d
в ворота идёт как: 74be777a8   (реконсиляция подаёт head[:9])
дерево грязное   : нет (чисто)
контур заморожен : True

release_reason(74be777a8) -> 'trainer'
  owner_approved  -> False
  trainer_enabled -> True
  trainer_verdict -> (True, 'зелёный: кейсов 12/12, чеков 224/224, прогонов 2, коммит совпал (2026-08-26 03:35:35)')

ЯЩИК ВЕРДИКТОВ pc_orchestrator.client_trainer_green.json:
  зелёных записей: 1 ['74be777']
  красных записей: 3 ['55aad22', '6478a39', '91d244e']
```

**Слово ворот изменилось `None → 'trainer'`.** Это первая зелёная запись в ящике **за всю его
историю**: вчера ключ повернулся впервые и сказал «нет», сегодня — впервые сказал «да».

## 7. Команда отката — записана ДО применения

Записана в `_scratch_key2_0826/ROLLBACK.md` в 03:2x, **до** прогона и, значит, до того, как
ворота могли применить коммит. Дословно:

```
git -C D:/turbobaby-bot revert --no-edit 74be777
D:/turbobaby-bot/venv/Scripts/python.exe D:/turbobaby-bot/_scratch_key2_0826/rollback_children.py
D:/turbobaby-bot/venv/Scripts/python.exe D:/turbobaby-bot/trainer_run.py
```

Первая строка снимает правку с ДЕРЕВА; вторая возвращает ЖИВЫХ детей той же механикой, которой их
поднимает сам демон (`git revert` правит дерево, а процессы остаются на том коде, с которым
стартовали); третья снимает зелёную запись из ящика — не-зелёный прогон сносит прежнюю зелень
этого коммита (`write_verdict`, fail-closed). Альтернатива третьему шагу — аварийный рубильник
второго основания (`client_contour.trainer_enabled`), гасящий основание целиком.

**Откат не понадобился:** ни один из трёх поводов (дети пишут клиентам / цена разошлась / дверь
закрылась) не наступил.

## 8. Применение — что сделал демон и где встало

Демон (`pc_orchestrator.py`, PID 14060) увидел вердикт **через 8 секунд** после записи и открыл
ворота, а затем упёрся во второй барьер. Строки его лога дословно:

```
2026-08-26 03:35:43,278 INFO  ворота контура (реконсиляция детей на новый коммит):
                              основание пропуска «trainer» — коммит 74be777a8 применяем к userbot,moderbot
2026-08-26 03:38:37,043 ERROR реконсиляция детей userbot:  гейт КРАСНЫЙ (… Ran 633 tests in 173.177s
                              FAILED (failures=7)) — рестарт отложен
2026-08-26 03:41:46,308 ERROR реконсиляция детей moderbot: гейт КРАСНЫЙ (… Ran 633 tests in 188.680s
                              FAILED (failures=7)) — рестарт отложен
```

**Что это за барьер.** После того как ворота пропустили коммит, `reconcile_children_tick` гоняет
`_affected_test_modules(['suggest.py'])` → `['test_suggest']` и требует от
`_gate_test_modules` кода возврата 0. Замер тем же вызовом до прогона:
`python -m unittest test_suggest` → **633 теста, 7 красных**. Поимённо:

| класс | тест |
|---|---|
| `TestRunLiveSmoke` | `test_green_full_data_passes` |
| `TestRunLiveSmoke` | `test_green_paraphrase_with_correct_numbers_now_passes` |
| `TestRunLiveSmoke` | `test_red_delivery_missing_fails` |
| `TestRunLiveSmoke` | `test_red_j_line_not_verbatim_fails` |
| `TestRunLiveSmoke` | `test_red_violations_flagged_with_diff_card` |
| `TestPriceSheetMinAcrossVariants` | `test_pointwise_pair_xmax_explicit_old_gen_both` |
| `TestPriceSheetMinAcrossVariants` | `test_pointwise_xmax_explicit_old_gen_shows_both` |

**Множество РАВНО дофиксовой базе** (замер 25.08 подменой файлов: 613 тестов/7 красных на
`1ded105`, 626/7 на `91d244e`, те же имена). Тестов стало 633 — плюс 7 новых из самого `74be777`.
Значит **правка не добавила ни одного красного**: это разошедшиеся голдены смены источника цены,
а не сегодняшняя регрессия. Гейт я **не ослаблял**, тестов **не трогал**, детей руками **не
двигал** — их двигают ворота, и они честно не сдвинули.

Побочный эффект по коду: демон поставил `_child_reconcile_rejected = 74be777…` и этот HEAD больше
проверять не будет — рестарт придёт только с НОВЫМ коммитом (тем, что починит голдены).

**ЖИВОЙ ПРОЦЕСС — на каком коммите каждый ребёнок сейчас** (посчитано парой функций демона,
`children_probe.py`, и сверено с CIM-листингом):

| ребёнок | номер (PID) | время старта | командная строка | коммит | отставание |
|---|---|---|---|---|---|
| userbot | **13844** | 2026-08-23 18:28:15 | `D:\turbobaby-bot\venv\Scripts\python.exe D:\turbobaby-bot\userbot_listen.py` | **`9f6360c`** | 4 коммита |
| moderbot | **18200** | 2026-08-23 18:29:01 | `D:\turbobaby-bot\venv\Scripts\python.exe D:\turbobaby-bot\moderation_bot.py` | **`9f6360c`** | 4 коммита |

Номера и время старта **не изменились** с начала сессии — доказательство, что рестарта не было
вовсе, а не что он был и откатился. Из четырёх коммитов отставания клиентский файл один:
`suggest.py`.

## 9. Отрицательный тест ПОСЛЕ: дети клиентам не пишут

| проверка | результат |
|---|---|
| банер ЖИВОГО userbot (последний в логе, 2026-08-23T11:28:18 UTC = старт PID 13844) | `SUGGEST ВКЛЮЧЁН (TEST_MODE=True, mod_group=-5031790861, bot-режим=да, лимиты 6/ч 15/д)` |
| следы фактической отправки клиенту («✅ …Отправлено…») за всю жизнь лога (75 682 строки) | **0 совпадений** |
| то же с момента запуска живого PID (строка №72712) | **0 совпадений** |
| `client_contour.frozen()` | **True**, флаг-файл `pc_orchestrator.contour_frozen` на диске есть |
| ревизор демона, последняя запись 26.08 01:17:41 | «окон с активностью 0 (всего строк drafts 523)» |

**Честная оговорка о силе доказательства.** Маркеров БЛОКИРОВКИ (`SUGGEST[TEST_MODE] …`) в логе
тоже **0** — значит путь отправки за всю жизнь лога ни разу не дошёл до развилки, и «блокировка
сработала» я утверждать не могу. Утверждаю ровно то, что измерено: **фактических отправок ноль**,
живой процесс поднят с `TEST_MODE=True`, активных клиентских окон ноль. Отката не потребовалось.

## 10. Пять прежних дверей и цена

**Пять дверей — открыты все пять** (живая проба, `doors_probe.py`):

| дверь | результат |
|---|---|
| 1 · мост-прайс `pricing.fleet()` | ОТКРЫТА за 2.44 с — 38 моделей |
| 2 · мост-зоны `delivery.get_delivery_zones()` | ОТКРЫТА за 2.39 с — 20 зон |
| 3 · чтение доков мозга | ОТКРЫТА за 3.20 с — 7665 символов |
| 4 · playbook | ОТКРЫТА — 2843 символа |
| 5 · голова (LLM через CLI) | ОТКРЫТА за 20.56 с — ответ есть |

Шестая дверь, сторож свежести цены: `state='СВЕЖЕЕ'`, `may_quote=True`, `call_owner=False`,
возраст **8.0** суток при пороге **14.0**, сошлись H3/I3/J3, расхождений ноль. Вчера было 7.0 —
сутки прибавились ровно на одни, запас до 01.09 сходится.

**Цена — расхождений НЕТ НИ ОДНОГО, все девять числовых пар сошлись с колонкой «стало»**
разбора цены удержания ворот (`docs/artifacts/2026-08-22-gate-hold-price-cost.md`, §3), до бата:

| кейс | было (18.08) | стало по разбору (22.08) | сейчас (замер) | сходится |
|---|---|---|---|---|
| низкий сезон, NMAX 155, 7 сут | 317 ฿/сут · 2217 | 298 ฿/сут · 2086 | 298 · 2086 | ДА |
| **ПИК**, NMAX 155, 7 сут | 317 ฿/сут · 2217 | 437 ฿/сут · 3059 | 437 · 3059 | ДА |
| через границу сезонов, NMAX, 7 сут | 317 ฿/сут · 2217 | 346 ฿/сут · 2422 | 346 · 2422 | ДА |
| низкий, XMAX 300 New, 14 сут | 577 ฿/сут · 8078 | 511 ฿/сут · 7154 | 511 · 7154 | ДА |
| **ПИК**, XMAX 300 New, 14 сут | 577 ฿/сут · 8078 | 749 ฿/сут · 10486 | 749 · 10486 | ДА |
| XMAX 300 старое поколение, 7 сут | 557 ฿/сут · 3899 | 623 ฿/сут · 4361 | 623 · 4361 | ДА |
| XMAX 300 New Gen, 7 сут | 662 ฿/сут · 4634 | 623 ฿/сут · 4361 | 623 · 4361 | ДА |
| месяц, низкий, NMAX 155 | кепка 5000 ฿/мес | 4740 | 158 ฿/сут · 4740 | ДА |
| месяц, **ПИК**, NMAX 155 | кепка 5000 ฿/мес | 6960 | 232 ฿/сут · 6960 | ДА |

**Аренда от 180 суток** — ожидание «ни одного числа цены» держится: 181 сутки и ровно 180 →
`long_term=True` и записка «Цену НЕ называй ВООБЩЕ»; контроль 179 суток → `long_term=False` и
прежняя формулировка (соседнее поведение не задето). Второй, независимый вход признака — слова:
«на полгода» / «на год» / «for 6 months» → да; «брал байк полгода назад» / «на 3 месяца» → нет.
`_LONG_TERM_MIN_DAYS = 180`.

Замер сделан на ТЕКУЩЕМ дереве `74be777`; дети остались на `9f6360c`, но из ценовых файлов между
этими коммитами не изменился НИ ОДИН — дифф `9f6360c..74be777` это ровно `suggest.py`,
`test_suggest.py` и четыре файла в `docs/`, а правка в `suggest.py` цены не касается.

## 11. Одной строкой

**Ворота ОТКРЫЛИСЬ** (основание «trainer», впервые за всю их жизнь), **дети остались на
`9f6360c`** (PID 13844 и 18200, старт 23.08 18:28) — их удержал второй барьер, гейт затронутых
тестов с семью ПРЕЖНИМИ красными, **снято 0 коммитов отставания из 4**.

## 12. Что осталось открытым

1. **Голденовые семь красных `test_suggest`** — пока они красны, ни один клиентский коммит до
   детей не доедет, сколько бы зелёных вердиктов ни было. Это и есть настоящая цена удержания
   сегодня. Классы известны: `TestRunLiveSmoke` (ждут дословную строку столбца J, а продукт
   отдаёт пересчёт) и `TestPriceSheetMinAcrossVariants`.
2. **Вторая половина жёсткого правила** — «просит мощный/крупный байк БЕЗ подтверждённого опыта»
   — по-прежнему не покрыта (остаток назван в самом `74be777`).
3. **Авто-разгон платой и память ниже планки** — вопрос владельца остаётся открытым: 4701 МГц без
   сброса на простое, память 2667 при паспортных 3200.
4. **Флап головы.** 25.08 в 17:44 тот же набор дал 6 «голова промолчала» из 24 кейсо-прогонов, а
   26.08 в 03:22 — ноль из 24. Разброс годности захода реальный, и «первый годный решает» его не
   отменяет: следующий заход может снова не дать вердикта не по вине кода.

Временное место сессии названо явно: **`_scratch_key2_0826/`** (untracked, ничего не удалялось).
