# SPLINTERVOIDDERZHIT2609 — держится ли пометка void из #85 на живом Splinter (задание Штаба 0047-74p.2609)

**Сводка. ДЕРЖИТСЯ.** Пометка стоит: позиция «подшипники» байка 4685 (тема 76, 09:51:04 UTC) в живом
`works_persist_state.json` имеет исход `void`. Файл байт-в-байт тот, что оставил `uborka.py` в 15:37:11 UTC:
sha `a5e3bcb2…`, mtime совпадает до наносекунды. Прочие 4 позиции равны копии `orig.json`. Служба та же:
PID 2470428, старт 13:50:17 UTC, NRestarts 0. С момента правки файл она не переписывала. Новых классов
ERROR, CRITICAL и Traceback после 15:37:11 нет, строк по этой позиции — 0.

Не проверено одно: живой проход службы по снятой позиции. За окно не было ни старта, ни приёма работ, ни
выгрузки по пробегу, поэтому `_wl_tick` не звался ни разу. Код HEAD при следующей записи `void` сохранит (п.5).

Хвост #85 упал не на проверке, а на выходе процесса. Проба через 25 с была зелёной. 10-минутная проба не
запускалась: заход закончился в 15:38:14 UTC без строки RESULT.

```
FACT: read-only. Замеры 26.09 16:11:28, 16:12:11 и 16:12:47 UTC. Живое не менялось:
      HEAD 09587d944760e287af85f0326b0536126029d5bc на main, отслеживаемых изменений 0.
FACT: works_persist_state.json: sha256 a5e3bcb2b64b74593a95ca222085c33ef1e91685e52586f53eb3778bed88bd32 = «после» #85;
      1628 байт; mtime 2026-09-26 15:37:11.060147174 UTC = миг apply в p3.out #85; inode 293921; *.tmp рядом нет.
FACT: позиция key -1002751134848:76 / слот «подшипники» / at 09:51:04.777: state=void, closed_at 15:37:11.059,
      km пусто, причина — строка WHY_VOID скрипта #84. Позиций 5 → 5: новых 0, исчезнувших 0; 4 written
      равны копии полями; исходы {written: 4, void: 1}.
FACT: /tmp/uborkavoid2609/orig.json sha256 e652e9471caf0deb1e85696c315305aa2b8c5e813007fe069090f7d5a193ded1 = «до» #85,
      mtime 13:50:22.484; копия на ПК tmp/uborka_void_2609/orig.json — тот же sha.
FACT: живой модуль HEAD (works_persist.py blob 52127710…) по живому файлу: report '' · open 0 · expire_hit 0 ·
      counts {waiting 0, lost_no_km 0, lost_other 0, written 4}.
FACT: splinter active/running, MainPID 2470428, ExecMainStart 13:50:17 UTC, NRestarts 0 (все три замера);
      journalctl: стартов после 13:50:17 — 0, окно с 15:37:10 — «-- No entries --».
FACT: splinter.log, окно 442693..442810 (15:37:11,400 → 16:12): новых классов ERROR/CRITICAL/Traceback 0;
      Traceback 0; [ERROR] 7 строк — все классы базы (мост); новый класс WARNING 1 — эхо-слой моста
      (в логе с 07.07, до базы 6609 раз); строк журнала принятых работ 0; строк с 4685 / «подшипник» / ключом темы — 0.
FACT: временное — ПК tmp/void_check_2609/ (c1.sh, c1.lf.sh, c1.out, c2.sh, c2.lf.sh, c2.out, c3.out);
      сервер /tmp/voidcheck2609/ (c1.sh, c2.sh, works_persist.py, splinter_head.py, base_now.log, base_jctl.log,
      win.log, win2.log, win_jctl.log, void_judge.py). /tmp/uborkavoid2609/ только читался. Ничего не удалялось.
```

## 0. Признак сделанности

- Артефакта SPLINTERVOIDDERZHIT2609 за 26.09 до захода не было. Артефакта #85 (`2026-09-26-SPLINTERVOID2609.md`)
  нет вовсе: #85 его не написал. Начато с нуля в 16:09 UTC.
- Способ замера взят у #85, чтобы вердикт был сравним: база классов — та же. `/tmp/uborkavoid2609/base.log`
  (splinter.log 442352..442692) сверен с живым логом, `cmp` дал равенство. Судья — тот же `void_judge.py`,
  sha `f91c492e…` на сервере и на ПК. На сервер скопирован в `/tmp/voidcheck2609/`, из папки #85 только читался.

## 1. Что сделал #85 и на чём упал хвост

**Одной строкой:** в 15:37:11 UTC `uborka.py apply` на живом файле дал rc 0, sha `e652e947…` → `a5e3bcb2…`,
исход `lost_no_km` → `void`. Проба через 25 с (метка t60, 15:37:36) зелёная. 10-минутная проба не начата,
артефакта нет. Заход кончился в 15:38:14 UTC записью демона `insufficient_output (нет «RESULT:») → failed`:
причина `exec_error`, 323 с, коммитов 0, записей журнала 0.

Детали по следам:

| что | где | значение |
|---|---|---|
| sha до / после | черновик #85 `tmp/pc_report/task85-25472-1790436772602-85.md`, `tmp/uborka_void_2609/p3.out` | `e652e947…` / `a5e3bcb2…` |
| правка | `p3.out` | 15:37:11.041 UTC, `apply OK · state=void`, rc 0; чтение назад: изменена только ЭТА (`closed_at`, `state`, `why`), 4 прочие — нет |
| база классов | `p3.out`, `/tmp/uborkavoid2609/edit_line.txt` | splinter.log 442352..442692, 10 классов |
| проба после правки | `tmp/uborka_void_2609/p4_t25.out`, сервер `cand_t60.log` 15:37:37 | ЗЕЛЕНО: красных 0, новых WARNING 0, строк журнала работ 0 |
| 10-минутная проба | сервер `/tmp/uborkavoid2609/` | следов нет: последний файл — `cand_t60*` 15:37:37 |
| конец захода | `pc_orchestrator.log` 22:38:14–22:38:38 местного | нет RESULT → failed, `exec_error`; думатель: halt «нужен человек» |

**Почему процесс замолчал без RESULT — неизвестно.** Транскрипт захода лежит вне проекта и не читался.
Гипотеза, не доказанная: заход закончился через 37 с после пробы, а до 10-минутной пробы оставалось почти
10 минут. Похоже, заход завершил ход на ожидании. Для боевой части это ничего не меняет: правка легла и
проверена.

## 2. Файл сейчас

| проверка | ожидалось (#85) | сейчас (16:11:28 и 16:12:47 UTC) | вердикт |
|---|---|---|---|
| sha256 живого файла | `a5e3bcb2…` | `a5e3bcb2b64b7459…bed88bd32` | **равен** |
| размер · mtime | 1628 · 15:37:11.060147174 | 1628 · 15:37:11.060147174 | **равны** |
| ЭТА позиция: state | `void` | `void`; `closed_at` 15:37:11.059, `km` пусто | **стоит** |
| ЭТА: поля против копии | изменены `closed_at`, `state`, `why` | те же три, прочие поля равны | как при правке |
| прочие позиции | 4 × written, как в копии | 4 × written, против копии изменений нет (по индексу и по ключу+`at`) | **как были** |
| состав | 5 позиций | 5; новых 0, исчезнувших 0 | как был |
| sha256 `orig.json` | `e652e947…` («до») | `e652e9471caf0deb…d5a193ded1`, mtime 13:50:22.484 | **равен** |
| `after_apply.json` #85 | — | `cmp` с живым: равны | — |
| живой модуль HEAD по файлу | report пуст, open 0 | report `''`, open 0, expire_hit 0, lost_no_km 0 | **позиция не открыта** |

## 3. Служба

| поле | ожидалось | сейчас (три замера) |
|---|---|---|
| ActiveState / SubState | active | active / running |
| MainPID | 2470428 | **2470428**; `ps`: старт Sat Sep 26 13:50:17 2026 |
| ExecMainStartTimestamp | 13:50:17 UTC | **13:50:17 UTC** |
| NRestarts | 0 | **0** |
| старты по journalctl с 13:40 | Stopped/Started 13:50:17 | те же две строки 13:50:17, других нет |

## 4. Логи после 15:37:11 UTC

- **splinter.log:** окно — строки 442693..442806 на 16:11:28, до 442810 на 16:12:47. Первая строка окна —
  15:37:11,400. Строк `STARTING` в окне 0.
- **Классы окна против базы 13:50:17 → 15:37:11** (судья #85, правило 0044-74m):

| класс | в окне | в базе | вердикт |
|---|---|---|---|
| `[ERROR] bridge_client: Bridge request error` | ×2 | ×2 | есть в базе |
| `[ERROR] bridge_client: Bridge timeout` | ×5 | ×10 | есть в базе |
| `[WARNING] apscheduler.scheduler: Execution of job` | ×6 | ×12 | есть в базе |
| `[WARNING] bridge_client: … get_pending: request_failed / backoff-ретрай отменён`, `echo-ретрай отменён`, `Bridge` | ×2 / ×5 / ×5 / ×1 | есть | есть в базе |
| `[WARNING] bridge_client: Bridge: echo-слой сбоит` | ×4 | нет | **новый WARNING, не красное** |
| Traceback / CRITICAL | 0 | 0 | — |

- **Новый WARNING к правке отношения не имеет.** Это ретрай эхо-слоя моста (`bridge_client.py:475` на HEAD).
  В splinter.log он с 07.07.2026, до старта 09587d9 встретился 6609 раз, в сутки 47–313 раз за 19–25.09.
  В узкую базу 13:50–15:37 он просто не попал.
- **Новых классов ERROR / CRITICAL / Traceback: 0.** Вердикт судьи: ЗЕЛЕНО, красных 0, rc 0 — в обоих замерах.
- **Строк по позиции после 15:37:11: 0.** Счёт такой:
  - «журнал принятых работ» — 0, «Принятые работы» — 0;
  - «4685» — 0, «подшипник» — 0, ключ темы — 0;
  - «в журнал принятых работ добавлено» — 0, «инфо-работы отложены» — 0.
- **Журнал службы:** окно с 15:37:10 — `-- No entries --`. В базе 4 строки, из них Stopped/Started 13:50:17.

## 5. Переписывает ли служба файл

- **С момента правки — нет.** mtime `15:37:11.060147174` равен мигу `apply` из `p3.out` до наносекунды,
  sha равен sha «после». Рядом нет `works_persist_state.json.tmp`, который оставила бы оборванная запись
  `_wl_save`. Службе и не было повода писать: в окне нет ни старта, ни `_wl_*`-строк. `_wl_tick` зовётся
  только на старте, при приёме работ и при выгрузке по пробегу.
- **Когда служба запишет — `void` устоит.** Это прочитано в коде HEAD 09587d9, не проверено живым проходом.
  - `_wl_load` отдаёт словарь с диска как есть: нормализации исходов при чтении нет.
  - `_wl_save` пишет `json.dump(works_persist.prune(state))` атомарно.
  - `prune` выбрасывает только `written` (`works_persist.py:254–270`).
  - `settle`, `lose`, `accept` трогают только `OPEN_STATES` = (waiting, lost_no_km, lost_other) (строки
    93, 135, 165, 192), `expire` — только `waiting` (209).
  - `void` не входит ни в одно из этих множеств, так что ни одна ветка его не перепишет и не выбросит.
  - То же на копии показала проба #84 (`2026-09-26-SPLINTERUBORKA2609.md` §3).

## ВЫВОД

**Держится.** Все три условия выполнены:
1. `void` стоит на месте, sha и mtime — от правки, прочие позиции целы.
2. Служба файл не переписала: та же служба, PID 2470428, NRestarts 0.
3. Новых классов ошибок нет, строк по позиции 0.

**Что предлагается (без правок):**
- Закрыть #85 как «убрано». Откатывать нечего, повторять `apply` нельзя: скрипт ответит STOP, и это правильно.
- Последний живой признак — первый `_wl_tick` службы после правки. Это ближайший старт, приём работ или
  выгрузка по пробегу. Тогда в splinter.log вместо `[ERROR] … журнал принятых работ` должна появиться INFO
  «Принятые работы без пробега: открытых нет», а `void` должен остаться в файле. Сверить это можно тем же
  `void_judge.py judge` на окне после 442692.
- Копия для отката есть только во временных местах: `/tmp/uborkavoid2609/orig.json` на сервере и
  `tmp/uborka_void_2609/orig.json` на ПК, sha `e652e947…`. Копии `works_persist_state.json.bak-uborka-2609`
  рядом с файлом, которую советовал #84 §4, НЕТ: `ls works_persist_state*` показал один файл. Если откат
  может понадобиться, копию стоит положить на диск рядом с файлом. Это запись в боевой каталог, так что
  отдельным заданием.

SPLINTERVOIDDERZHIT2609
