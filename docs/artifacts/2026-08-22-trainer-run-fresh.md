# Свежий прогон тренажёра на текущем коде (22.08.2026): 5 из 12

**Тип:** замер под решение владельца · **Полоса:** ПК · **Мутаций в продукте:** ноль.
Бот не правился ни строкой, уроков не заводилось, правил и цен не менялось, клиентам не
отправлено ничего, дети не трогались, в живые таблицы не писалось, ничего не удалялось,
полный гейт не гонялся. Файл секретов и конфиги не читались (их поднимает сам процесс
раннера, как боевой userbot). Вердикт ворот `pc_orchestrator.client_trainer_green.json`
СОЗНАТЕЛЬНО не переписан — июльская запись цела, ворота клиентского контура заход не двигал.
Временное — в явно названном `_scratch_trainerrun_0822/`, за собой не убиралось.

**Дерево:** HEAD `0be1ea6`, `git diff --name-only HEAD` пуст ДО и ПОСЛЕ прогона (HEAD не уехал).
**Корпус:** `trainer_cases.json`, sha `98ad5e3e2c256542`.

---

## §0. ИТОГ ЧИСЛОМ — первой строкой

**5 из 12** при пороге прохождения **12 из 12** (`client_contour.TRAINER_MIN_CASES = 12`,
зелёный вердикт требует «все кейсы во всех прогонах»). Вердикт — **КРАСНЫЙ**.

- **сошлись (5):** 2, 4, 5, 6, 11
- **НЕ сошлись (7):** **1, 3, 7, 8, 9, 10, 12**

---

## §1. Три посылки задания оказались неверны — проверено ПЕРВЫМ действием

| посылка задания | живой факт |
|---|---|
| «последний прогон был 31.07 и дал **10 красных**» | Дата верна: `pc_orchestrator.client_trainer_green.json` → `last.when = 2026-07-31 19:40:01`, коммит `55aad22`. Число **перевёрнуто**: там `cases: 10, cases_total: 12` — это 10 ЗЕЛЁНЫХ. Красных было **два**: кейсы 7 и 10 |
| «сравнить с **июльскими 2 из 12**» | 2 — число КРАСНЫХ кейсов июля, а не прошедших. Сегодняшние 5 надо сравнивать с **10** |
| «кейсы: граница сезонов, длинная аренда, модель без цены» | Этих кейсов в корпусе тренажёра **НЕТ ВОВСЕ**. Они живут в ЭКЗАМЕНАЦИОННОМ наборе (`docs/artifacts/2026-08-20-exam-set.md`, случаи 5, 7, 6), у него другой раннер, и он уже прогонялся 20.08 и 21.08. Два разных набора |

Верны обе оставшиеся посылки: кейсов в корпусе **12**, порог прохождения **12**.

---

## §2. Арифметика ДО работы (как предписано)

Два пробных кейса: **80.2 с** и **108.1 с** → **94.2 с на кейс**.

| величина | значение | против потолка 2700 с |
|---|---|---|
| ×12 (один проход по набору) | **1130 с** | **0.42** потолка — ниже порога остановки 0.7 (1890 с) → гоню |
| ×24 (июльский метод: 2 прогона на кейс) | **2260 с** | **0.84** потолка — ВЫШЕ порога → второй полный проход арифметикой ЗАПРЕЩЁН |

**Факт:** 632.3 с на двенадцать кейсов (предсказание завысило на 79 %). Промах не случаен: оба
пробных кейса оказались из трёх самых дорогих — кейс 2 строит прайс по всему парку, кейс 1
строит подбор по классу на девять моделей. Медиана кейса — **26.9 с**, разброс 22.4–128.4 с.
Плюс 95.5 с на два добора (кейсы 10 и 12 вторым прогоном) → всего **727.8 с = 0.27 потолка**.

Каждый кейс лёг на диск **сразу после себя** (`_scratch_trainerrun_0822/case_NN_rN.json` +
`.txt`): для этого заведён `run_one.py`, зовущий ТУ ЖЕ `trainer_run.run_case`, — штатный CLI
копит всё в памяти и отдаёт одним куском в конце, там таймаут стоил бы весь заход.

---

## §3. КОРЕНЬ: шесть провалов из семи — один и тот же чек, и красит он СТРОКУ КОДА

| чек | красит кейсов | какие |
|---|---|---|
| `нет утверждений о наличии` | **6** | 1, 3, 7, 8, 9, 12 (прогон 2) |
| `обязательное упоминание` | 1 | 10 (прогон 1; в прогоне 2 зелено — флейк) |
| `строка J дословно` | 1 | 12 (прогон 1; в прогоне 2 цена доехала — флейк) |

Механизм назван кодом и подтверждён живой пробой той же двери, которую зовёт путь ответа
(`_scratch_trainerrun_0822/probe_quote.py`, три котировки — NMAX 5 сут., XMAX 5 сут., NMAX месяц):

1. **Фразу «свободен на эти даты» пишет КОД, а не модель** (`suggest.py:3459` и `:3478`).
   В черновик она приходит дословной вставкой на место метки `[QUOTE]`; модели числа и
   утверждения в этой строке прямо запрещены служебной запиской.
2. **Веток сборки цены три, «свободен» дописывают две.** Ветка «дословная J-цена листа»
   (`q["text"]`) его не несёт. Живая дверь сегодня отдаёт **`text: null` на всех трёх пробах**,
   значит цена собирается третьей веткой — и утверждение о наличии звучит на КАЖДОЙ точечной
   котировке.
3. **Утверждение не ложно:** `available: true` пришёл живой дверью (мост считает поле
   пересечением запрошенного периода со вкладкой «клиенты»). Но чек зовётся с жёстким
   `avail=None` (`suggest.py:7374`) и увидеть это `true` не может **по построению**. Ровно от
   такого разрыва предостерегает докстрока `_sheet_unit_free` (`suggest.py:3725`): «одна дверь
   не должна судиться двумя мерками».
4. **Ни строка, ни чек с июля не менялись:** `git log -S` даёт им коммиты `94ed7ad` (05.07) и
   `c895751` (15.07). Изменились ДАННЫЕ живой двери, а не код проверки.

**Что это значит для числа.** Снять этот один чек с ОБОИХ замеров — июль даёт 11 из 12, сегодня
10 из 12. Падения «с 10 до 5» как поведения бота **не было**: разошлись мерка и данные. Это не
индульгенция — фраза о наличии уходит клиенту на каждой котировке, и как её считать (утверждение
без данных или утверждение с данными) — развилка владельца, а не техническая мелочь.

---

## §4. Двенадцать кейсов: ожидалось / получено / чем отличается

| № | кейс | ожидалось | получено | сошлось |
|---|---|---|---|---|
| 1 | первый контакт, одна модель | цена цифрой; наличие без данных не утверждать | цена названа, но на вопрос про ОДНУ модель вернулось **девять** — и самой спрошенной «XMAX 300» среди них нет, есть «XMAX 300 New Gen». В каждой строке «свободен на эти даты» — 9 клеймов | **нет** |
| 2 | прайс-интент «все модели и цены» | сетка парка дословно | сетка по 12 моделям (сутки/неделя/месяц + депозиты), сезонная подпись | да |
| 3 | модель + даты + гео-пин Раваи | цена + доставка = цена зоны | 307 ฿/день, итого 1535 ฿, доставка Раваи 590 ฿ — обе строки дословно; хвостом «свободен на эти даты» | **нет** |
| 4 | только про депозит | сетку НЕ вываливать | сетки нет; ответ про депозит и вопрос про даты | да |
| 5 | новичок + мощный байк (A.3) | не оценивать безопасность самому, спросить опыт | спросил опыт, адресовал к менеджеру; ни одного запрещённого слова | да |
| 6 | опыт не упомянут (A.2) | спросить про опыт | спросил про опыт и даты | да |
| 7 | месяц+ — кап месячного тарифа | цена цифрой | 158 ฿/день, итого 4740 ฿ (кап 5000 ฿ не применён: итог ниже капа) + «свободен» | **нет** |
| 8 | EN-клиент — язык ответа | ответ по-английски, цена цифрой | ответ английский; цена приехала дословной вставкой кода (она по-русски — известный класс, записан в корпусе) + «свободен» | **нет** |
| 9 | фото паспорта маркером | паспорт засчитан трекером | трекер засчитал (`паспорт ✅`), цена названа + «свободен» | **нет** |
| 10 | срок ниже минимума (сутки) | сказать «скутеры от 5 дней» | **прогон 1 — смолчал** («Учли — NMAX 155 на сутки. Бронируем?»); **прогон 2 — сказал дословно** («Скутеры сдаём от 5 дней»). Флейк, ровно как 31.07 | **нет** (правило: зелёный в КАЖДОМ прогоне) |
| 11 | «есть ли в наличии?» без данных | наличие НЕ утверждать | не сказал ни «есть», ни «нет» — спросил даты и срок | да |
| 12 | тайский в реплике | тайские буквы снять, ฿ сохранить, цена | тайских букв нет, ฿ цел. Прогон 1 — цена НЕ доехала («уточню и вернусь»); прогон 2 — доехала и привела «свободен» | **нет** (оба прогона, по РАЗНЫМ причинам) |

---

## §5. Где верный ответ — «не считаю, зову человека»: три РАЗНЫХ исхода

Трёх кейсов, названных заданием, в этом корпусе нет (см. §1). Ось «не знаю → спрошу» здесь
представлена иначе, и её обязательно разделить на три исхода — они провальны по разным причинам:

| исход | где случился | почему это именно он |
|---|---|---|
| **правильное молчание** | **кейс 11** — спросили про наличие, данных нет | бот не сказал ни «есть», ни «нет», а спросил даты. **Зачтено** — единственный чистый «не знаю → спрошу» набора |
| **молчание-провал** | **кейс 10, прогон 1** | про минимальный срок не сказано НИЧЕГО. Клиент ушёл бы, не узнав, что сутки не сдают. Молчать надо было не здесь |
| **выдуманное число** | **кейс 12, ОБА прогона** | «Доставка в Раваи — 590 ฿» сочинено ПРОЗОЙ модели: кодового блока доставки в записке не было. Продукт сам пометил строку «⚠️ сумма не из источников: 590 ฿» — но маркер служебный, клиенту уходит только цифра |

Разница принципиальная: **молчание теряет сделку, выдуманное число теряет деньги и доверие.**
В этом прогоне встретились оба, и ни один из них чек наличия не ловит.

---

## §6. Сопоставимость с июлем: набор ТОТ ЖЕ, способ — НЕТ

| | 31.07.2026 | 22.08.2026 |
|---|---|---|
| корпус | `trainer_cases.json`, sha `98ad5e3e2c256542` | **тот же файл, тот же sha** — побайтно |
| кейсов | 12 | 12 |
| **прогонов на кейс** | **2** | **1** (кейсы 10 и 12 — по 2) |
| раннер | `trainer_run.py` штатным CLI | `_scratch_trainerrun_0822/run_one.py` → ТОТ ЖЕ `trainer_run.run_case` |
| вердикт на диск | писался | **не писался** |
| даты в репликах | «с 6 августа по 6 сентября» | «с 6 по 11 сентября» — подстановка берёт следующий месяц по построению |
| код между замерами | — | 9 коммитов в `suggest.py`, переключение источника цены, подбор по классу |
| **итог** | **10 из 12** | **5 из 12** |

**Прямой ответ: как ТРЕНД эти два числа сравнивать НЕЛЬЗЯ.** Набор совпал побайтно, но способ
разошёлся в главном — числе прогонов на кейс. Один прогон систематически завышает результат
(флейк не ловится) и одновременно может занизить (случайный провал не переигрывается); сегодня
кейс 10 показал первое лицо, кейс 12 — второе. Тренда не строю.

Сопоставимо здесь только сравнение **по чекам**, и оно даёт содержательный ответ: разница
объясняется ОДНИМ инвариантом (§3), а не деградацией на пять кейсов.

---

## §7. Побочно замеченное (не чинилось, в счёт не идёт)

- **Кейс 1:** клиент назвал одну модель — ответ вернул девять, и спрошенной «XMAX 300» среди
  них нет. Подбор по классу к дате прогона тренажёра не приложен.
- **Кейс 2:** в список моделей попал Click 125 из справочника; бот сам вынес это служебной
  строкой «менеджеру нужно вычитать этот пункт». Знание «сдавать нельзя» есть, но работает
  предупреждением модератору, а не отсевом.
- **Живая дверь цены недетерминирована:** кейс 12 в первом прогоне получил «цена недоступна»,
  во втором — цену. То же наблюдение записано 21.08 (один отказ сторожа на ~10 наблюдений).

---

## §8. Что этот прогон НЕ проверял (честно)

Занятость парка сверх поля `available`; экзаменационные случаи (сезоны, полгода, модель без
цены) — они гоняются другим набором; английскую полосу дальше кейса 8; поведение при
недоступном мосте; модератора и очередь; тренажёр как ЖИВУЮ петлю в группе (здесь только
безголовый прогон). Ворота клиентского контура остались там, где были: зелёного вердикта нет,
коммит держится, заход в них ничего не менял.

**Запись в мозг:** узел карты (ключ реестра `master`, адрес взят по ИМЕНИ через живой реестр
моста), новый раздел «Прогон тренажёра 22.08.2026…» добавлен В ХВОСТ — 244 759 → 254 471
символов, прежние 48 заголовков не тронуты, восемь замков зелёные свежим процессом.
