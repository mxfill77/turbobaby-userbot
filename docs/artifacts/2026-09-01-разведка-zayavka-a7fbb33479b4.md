# Разведка заявки #87 — находка внешнего канала «НЕ ДЕЛАТЬ ВОВСЕ» по `review_pack.py:788`

ключ разведки: **2026-09-01-разведка-zayavka-a7fbb33479b4**
дата: 2026-09-01 · вид: разведка (только чтение, счёт, артефакт) · полоса: ПК
повод поставила себе САМА полоса (ступень E), источников находки — 2

**Находка — МНЕНИЕ внешнего канала, а не задание.** Ни одна её строка не исполнена;
боевой код не тронут. Разведка кончается этим файлом, а не сменой поведения.

---

## ВЕРДИКТ В ОДНОЙ СТРОКЕ

**Посылка ЖИВА, претензии ЛОЖНЫ.** Механика, на которую находка указывает пальцем,
существует ровно там, где названо (`review_pack.py:788`), и промолчать она может.
Но оба конкретных обвинения — «пакет заявил полное покрытие» и «пакет скрыл, что
расписок 3 из 6» — **опровергаются самими пакетами**: они говорят это вслух, один
из них ещё и хешированным числом. Находка прочитала ОДИН раздел и приняла его за
всё заявление пакета.

---

## ЧТО ГОВОРИТ НАХОДКА (дословно, из двух источников)

| источник | претензия вида «НЕ ДЕЛАТЬ ВОВСЕ» |
|---|---|
| `docs/review_inbox/2026-09-01-2026-09-01-chains-2026-08-31-codex.md` | «не заявлять "слепых пятен не заявлено" и **полное покрытие дня**. s1 сознательно исключена, следовательно покрытие суточных закрытых цепочек неполно» |
| `docs/review_inbox/2026-09-01-2026-09-01-digest-2026-09-01-codex.md` | «не заявлять "ничего не опущено" и "слепых пятен не заявлено", когда **из шести закрытых цепочек приложены расписки только для трёх**. Для `#77`, `#78`, `#80` содержание … неизвестно» |

---

## ЗАМЕРЫ (число + команда, которой оно получено)

### Замер A — сколько мест вообще наполняют `unknowns`

```
$ grep -n "unknowns = list\|unknowns\.append" review_pack.py
392:    unknowns = list(inner.get("unknowns") or [])
409:            unknowns.append(
```
**Число: 2.** Больше входов у раздела «НЕИЗВЕСТНО» нет.

* `392` — то, что **объявил вызывающий** (случай), код сам ничего не выводит;
* `409` — единственный автовывод, и он срабатывает **ровно на одном условии**:
  источник ЧИСЛА не доехал до пакета (`review_pack.py:393-417`).

### Замер B — становится ли исключение/опущение слепым пятном

```
$ grep -c 'excluded.*unknown\|unknown.*excluded' review_pack.py   →  0
$ grep -c 'omitted.*unknowns\.append' review_pack.py              →  0
```
**Число: 0 и 0.** Ни `excluded` (запрет имени), ни `omitted` (источник не влез)
не порождают записи в `unknowns` **ни одной строкой**. Третий такой же молчаливый
путь — расписка без источника: `review_pack.py:437` кладёт
`"source_sha256": src["source_sha256"] if src else None` и `unknowns` не трогает.

### Замер C — как часто строка 788 реально печатается

```
$ ls docs/review_outbox/*.md | wc -l                                    →  14
$ grep -l "слепых пятен не заявлено" docs/review_outbox/*.md | wc -l    →  13
```
**13 из 14 пакетов** печатают «— слепых пятен не заявлено».

### Замер D — а в скольких из них пакет тут же показывает дыру

Цикл по всем пакетам: печатает ли файл «слепых пятен не заявлено» И при этом
имеет НЕпустой раздел ОПУЩЕНО или ИСКЛЮЧЕНО.

```
$ for f in docs/review_outbox/*.md; do
    grep -q "слепых пятен не заявлено" "$f" || continue
    grep -q "ничего не опущено" "$f" && grep -q "запрещённых имён в манифесте не было" "$f" || echo "$f"
  done
docs/review_outbox/2026-09-01-chains-2026-08-31.md
```
**Число: 1 из 13.** Единственный такой пакет — тот самый, на который жалуется находка.

### Замер E — претензия о «разных схемах» (третий пункт того же ответа канала)

```
$ grep -o 'turbobaby\.result_packet/v[0-9.]*' docs/review_outbox/2026-09-01-chains-2026-08-31.md | sort | uniq -c
      1 turbobaby.result_packet/v1
```
**Число: 1 (одна-единственная строка схемы, вариантов ноль).** Канал написал
«`turbobaby.result_packet/v1` против `turbobaby.result_packet/v1` с точкой» —
это **две дословно одинаковые строки**, поданные как различие. Претензия
самоопровергается и вдобавок не подтверждается файлом.

---

## ЧТО ЕЙ ОТВЕЧАЕТ В КОДЕ ПОЛОСЫ

`review_pack.py:782-789` — раздел «НЕИЗВЕСТНО»:

```python
lines.append("## НЕИЗВЕСТНО (unknowns)")
if p["unknowns"]:
    for rec in p["unknowns"]:
        lines.append("- `%s` · проверка `%s` · %s" % (rec["source"], rec["check"], rec["blind_spot"]))
else:
    lines.append("%s слепых пятен не заявлено" % _NA)   # ← строка 788
```

Адрес назван находкой **верно**: строка 788 жива сегодня и печатается в
else-ветке пустого `unknowns`. С учётом замеров A и B это значит:
**исключённый по имени источник и неприложенная расписка проходят мимо раздела
«НЕИЗВЕСТНО» молча.** Это и есть живая посылка находки.

Смягчает её ровно одно, и это надо назвать честно: слово в строке — «не
**ЗАЯВЛЕНО**», а не «нет». Оно описывает состояние объявления, а не мира, и
буквально исполняет объявленный инвариант модуля (`review_pack.py:20-23`):
«Ничего не выдумывать. Сводка, статусы, числа — это то, что вызывающий ОБЪЯВИЛ».
Строка не лжёт — но читатель может принять «никто не объявил» за «ничего нет».

---

## ЧЕГО НАХОДКА НЕ УЧИТЫВАЕТ (по фактам обоих пакетов)

**1. Дайджест говорит «3 из 6» сам — дважды, и одно из двух хешировано.**
`docs/review_outbox/2026-09-01-digest-2026-09-01.md`, раздел СВОДКА:

> «Полнота списка держится ИНДЕКСОМ дня; **расписок приложено 3 из 6** — остальные
> цепочки названы в индексе строкой, но своей расписки в этом пакете не имеют.»

и там же в таблице ЧИСЕЛ строка `дайджест · расписок приложено | 3 |
docs/review_receipts/2026-09-01-digest-index.md | 92e3ea094b63 | hashed`.
То, что канал предъявляет как **скрытое**, стои́т на лице пакета числом
с sha256 источника. Претензия ложна.

**2. `«ничего не опущено»` — не про расписки, канал смешал два разных слова.**
`omitted` в этом коде значит «источник, НАЗВАННЫЙ в манифесте, в пакет не влез»
(`review_pack.py:16-19`). Расписок `#77`/`#78`/`#80` в манифесте не было вовсе —
они не «опущены», они не заявлялись. Строка «ничего не опущено» по своему
определению верна; читать её как «ничего не отсутствует» — ошибка канала, а не
пакета.

**3. Пакет цепочек НЕ заявляет полного покрытия дня.**
```
$ grep -n "покрыт\|полнот\|все цепочки\|ПОКРЫТИЕ" docs/review_outbox/2026-09-01-chains-2026-08-31.md
67:## ПОКРЫТИЕ ПОЛОС
```
Единственное упоминание покрытия — **ПОКРЫТИЕ ПОЛОС** (`pc — checked`,
`hq — checked`), то есть по ПОЛОСАМ, а не по суткам. А про s1 пакет говорит
прямым текстом в строке 23 сводки:

> «Пятая закрытая цепочка дня (s1 …) в пакет ВОЙТИ НЕ МОЖЕТ: её расписка лежит по
> пути со словом secret … **это не пропуск, а названное ограничение**»

плюс раздел ИСКЛЮЧЕНО с точным путём и сработавшим токеном `secret`.
Обвинение «заявили полное покрытие дня» ложно: пакет назвал дыру трижды.

**4. Мимо находки прошёл БОЛЕЕ ОПАСНЫЙ путь того же класса.** Канал заметил
исключение по имени (замер D: 1 случай, и тот громко объявлен). Он НЕ заметил,
что `review_pack.py:437` молчит про расписку без источника: `source_sha256`
становится `None`, а в раздел «НЕИЗВЕСТНО» не попадает ничего. Сегодня это
безвредно — дайджест объявил «3 из 6» руками, — но держится на добросовестности
составителя случая, а не на коде.

---

## ЧТО ПОВОД ЗАКРЫВАЕТ, ЧТО ОСТАЁТСЯ, ЧТО НЕИЗВЕСТНО

**ЗАКРЫВАЕТ (проверено числом):**
* адрес `review_pack.py:788` назван верно, строка жива на 2026-09-01;
* механика молчания реальна: `excluded` и `omitted` не порождают `unknowns`
  (замер B: 0 и 0), автовывод один и только для чисел (замер A: 2 места);
* обе конкретные претензии канала — ложны (пункты 1–3 выше, с цитатами файлов);
* претензия о «разных схемах» самоопровергается (замер E: вариантов 1).

**ОСТАЁТСЯ (правка НЕ сделана — разведка, решение владельца):**
Назвать словами, как условлено, не трогая код:
> Сделать так, чтобы строка 788 могла напечататься только когда прятать нечего:
> при непустом `excluded`, при непустом `omitted` и при расписке без источника
> (`review_pack.py:437`, `source_sha256 is None`) автоматически класть запись в
> `unknowns` — с `check` вида `excluded_source` / `omitted_source` /
> `receipt_missing`. Тогда «слепых пятен не заявлено» перестаёт быть достижимым
> при известной дыре, а не полагается на добросовестность составителя случая.

Цена и риск правки в этом заходе НЕ мерялись: она сдвинет рендер и, значит,
sha256 уже отправленных пакетов — а инвариант модуля требует побайтовой
пересборки (`review_pack.py:12-15`). Это довод ЗА решение владельца, а не за
самостоятельную правку.

**НЕИЗВЕСТНО (законный третий исход, гадать не буду):**
* был ли получен owner approval по цепочке h1 — канал спрашивает, в пакете этого
  нет, и по пакету это не проверяется; не смотрел вне пакета;
* сколько пакетов прошлых суток попали бы под предложенную правку — корпус
  сегодня 14 файлов (замер C), исторического среза не снимал;
* принял ли валидатор что-либо по схемам h1 — вопрос канала повис на
  самоопровергающейся цитате (замер E), проверять было нечего.

**Красного не встретил:** ни одной операции из запретного списка не потребовалось
— только чтение, счёт и этот файл.
