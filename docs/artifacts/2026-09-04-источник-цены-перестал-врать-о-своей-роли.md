# Источник цены перестал врать о своей роли

**Дата:** 2026-09-04 · **Полоса:** ПК · **Задание:** `02-price-selfdesc.0904` (ящик Штаба,
документ `shtab_task_02-price-selfdesc.0904`, file id `1eBAvFnXuJFSL1BBm1IgbG19Rum4U-Evv`)
**Ось:** бизнес — правило «данные не врут». **Единица:** одно поле данных + перепись читателей.

## §0. Признак сделанности (первым действием)

Артефакт по адресу результата **ОТСУТСТВОВАЛ**: `ls docs/artifacts | grep 2026-09-04` дал
**9 файлов** за 04.09, ни один не про самоописание источника цены. Вопросы задания не были
отвечены нигде — заход шёл с нуля, а не дожимал чужую работу.

## §1. Премиса: три пункта, свои команды, числа

Штаб замерил 04.09 около 02:15 местного. Проверено заново, **сходится 3 из 3**.

| # | утверждение Штаба | чем проверено | вердикт |
|---|---|---|---|
| 1 | поле вердикта говорит «ДАННЫЕ. НЕ ПОДКЛЮЧЕНО И НЕ ЧИТАЕТСЯ НИКЕМ» | `price_source.json:6`, дословно | **СОШЛОСЬ** |
| 2 | признак включённости по умолчанию ДА; выключает опознанное слово; пустое и мусор → ВКЛЮЧЕНО | `price_source.py:64-70` | **СОШЛОСЬ** |
| 3 | значит утверждение неверно с 20.08 и неверно молча | коммит `45a38cf` | **СОШЛОСЬ** |

**Пункт 2 дословно.** `price_source.py:70`:

```python
return (os.getenv(ENV_FLAG) or "").strip().lower() not in _FALSE
```

`ENV_FLAG = "PRICE_SOURCE_SEASON"` (`:54`), `_FALSE` — **6 слов**: `0`, `false`, `no`, `off`,
`нет`, `выкл` (`:58`). Пусто → `"" not in _FALSE` → **True**. Мусор → **True**. Выключает
ровно одно из шести слов. Это не чтение вслух докстроки: то же самое **заперто гейтом** —
`test_price_source.py:103` (ручки нет → `assertTrue`), `:108` (каждое слово `_FALSE` →
`assertFalse`), `:115` (мусор → `assertTrue`).

**Пункт 3 — дата, названная честно.** Переключение внёс коммит
`45a38cf` «источником цены стало ЗАПИСАННОЕ ПРАВИЛО вместо живого листа; сторож свежести включён
вместе с переключением», `git log --date=iso` → **2026-08-21 01:53:23 +0700**, то есть
**2026-08-20 18:53 UTC**. Расхождение «20.08 или 21.08» разрешается часовым поясом, а не спором:
по UTC — 20.08, по местному — ночь на 21.08. В файле и докстроке названо 20.08; менять эту дату
поводов нет. Молчаливость доказана тем же: за **15 суток** (20.08 → 04.09) ни один читатель не
споткнулся о ложный вердикт, потому что вердикт — проза, и его никто не исполняет.

## §2. Перепись читателей: поимённо и числом

Предсмертный взгляд задания назван прямо: провал — это греп по имени файла и вывод
«ОТСУТСТВУЕТ» там, где на деле «НЕИЗВЕСТНО». Поэтому искали четырьмя способами: импорт модуля,
путь, собранный из кусков (`os.path.join`), зеркала чисел без чтения, и не-python (JS, `.bat`,
`.ps1`, `.vbs`, `.xml` планировщика, хуки `.claude`, промпты). Ниже — **числа, а не слово
«читается»**.

### A. Боевой путь ответа клиенту — 3 читателя + 1 ревизор

| читатель | строки | что делает |
|---|---|---|
| `price_source.py` | `:52` `PATH`, `:87` `io.open`, `:88` `json.load` | сам `load()`, кэш по `(путь, mtime, размер)` |
| `suggest.py` | `:38` импорт, `:3398` и `:3817` `reprice`, `:3791` `period_of(load())`, `:3873` `enabled()` | заменяет числа живой котировки счётом по файлу |
| `price_freshness_run.py` | `:34` путь, `:57` `io.open`, `:58` `json.load` (`read_rule`) | **ВТОРОЙ, независимый от модуля читатель того же файла** |
| `pc_orchestrator.py` | `:10673` → `suggest._smoke_expectations` → `suggest.py:7609` | ревизор читает тем же кодом, что клиентский ответ |

Живой вход — `userbot_listen.py:229`: `await asyncio.to_thread(suggest.build_pricing_note, ...)`.
Второй читатель приходит не через `import price_source`, а через
`price_gate.py:136` `price_freshness_run.read_rule()` → `price_gate.allow()` ←
`suggest.py:3390`. Греп по `import price_source` его бы **не нашёл** — ровно та ловушка, о
которой предупреждало задание.

### B. Инструменты руками — 4

`price_revouch.py:340,382` (единственный, кто может **писать** в файл, только за `--apply`,
с резервной копией `tmp/price_revouch/`); `price_snapshot_collect.py:55,308,387`;
`price_snapshot_publish.py:411` — и у него замок `:402-403`: писать в файл с именем
`price_source.json` ему **прямо запрещено**; `price_freshness_run.py` как CLI (тот же файл,
что и боевой читатель A3 — функция двойного назначения).

### C. Зеркала (числа скопированы, файл НЕ читается) — 2

`pricing_advisor.py:112-125` (`GEN_STATES` — зеркало `price_source._pick_generation`,
`SNAPSHOT_MAX_AGE_DAYS_DEFAULT = 14.0` — копия `price_freshness.MAX_AGE_DEFAULT`);
`price_freshness.py:77` (`QUENCH` — намеренная копия литерала `price_source.reprice`,
«сторож не импортирует того, за чем следит»).

### D. Тесты — 14 файлов

`test_client_contour`, `test_deploy_voice`, `test_isolation`, `test_moderation_card`,
`test_pc_orchestrator`, `test_price_freshness`, `test_price_gate`, `test_price_revouch`,
`test_price_snapshot_collect`, `test_price_snapshot_publish`, `test_price_source`,
`test_pricing`, `test_pricing_advisor`, `test_suggest`.

### E. Третий исход назван — что осталось НЕИЗВЕСТНО

Три места, где «поиск не нашёл» — это **НЕИЗВЕСТНО**, а не **ОТСУТСТВУЕТ**:

1. **`moderation_card.py:46,213,219`** файл действительно читает (`import price_source`,
   `load()`, `period_of`). Единственный импортёр — `dialog_card.py:50`, а у него
   прод-вызывающего на 04.09 не нашлось (только тесты и `_scratch_*`). Это «на дату среза
   вызывающего не видно», а не «читателя нет»: динамический импорт по строковому имени поиском
   такого рода не ловится.
2. **Облачный Apps Script моста.** Исходников `.gs` в репозитории нет вовсе (есть только
   `clasp_prod_pins.json` с id проекта) — код моста живёт в облаке Google. Читает ли он что-то
   похожее на своей стороне, локальным чтением **проверить нельзя**.
3. **Живой планировщик Windows.** В дереве репозитория регистраций нет (проверены `*.bat`,
   `*.ps1`, `*.vbs`, `pc_remote_control.task.xml`, хуки `.claude/settings.json` — только
   `dispatch_notify.py` и `pretool_guard.py`), но сам `schtasks` не опрашивался: это красная
   операция, и заход её не делал.

Отдельно закрыт вопрос про **`manager-bot/`** — снимок VPS-кода вне git, куда боевой `suggest.py`
ходит за тремя фолбэками (`suggest.py:263` FAQ, `:514` парк, `:689` playbook). Проверены все
5 файлов `manager-bot/docs/*.md` и каталог целиком: `price_source` — **0 совпадений**. Фолбэк
даёт FAQ и парк, но не цену из этого файла.

**Итог переписи одной строкой:** боевых читателей **3** (+1 ревизор тем же кодом),
инструментов **4**, зеркал **2**, тестов **14**, неизвестных мест **3**.

## §3. Что переписано

### Правка 1 — поле вердикта, `price_source.json` → `status`

Было (строки 6–7): `"ДАННЫЕ. НЕ ПОДКЛЮЧЕНО И НЕ ЧИТАЕТСЯ НИКЕМ."` плюс в `note`: «Ни бот, ни
его правила, ни промпты, ни книга правил этот файл не читают и не знают о нём».

Стало: `"ПОДКЛЮЧЁН И ЧИТАЕТСЯ. ИСТОЧНИК ЦЕНЫ БОТА ПО УМОЛЧАНИЮ с 20.08.2026."`, а `note` теперь
называет **роль** (записанное правило, по которому бот называет цену), **читателей поимённо со
строками** (§2), **условие выключения** (`PRICE_SOURCE_SEASON`, шесть слов `_FALSE`, пустое и
мусор = включено) и **дату с коммитом**. Названо и то, что из прежнего самоописания осталось
верным: это по-прежнему **НЕ утверждённый прайс**, ни одна цифра отсюда никуда не записана.
Прежний вердикт не стёрт молча — он процитирован с пометкой, что описывал состояние до 20.08.

### Правка 2 — второе ложное самоописание в шапке модуля-читателя

`price_source.py:2` открывался словами **«Подключение на ФЛАГЕ»** — и это противоречило
**собственной строке 19 того же докстринга**: «ИСТОЧНИК ПЕРЕКЛЮЧЁН 20.08.2026 — ветка живёт
БЕЗ ФЛАГА». Файл спорил сам с собой на расстоянии 17 строк, и читающий сверху вниз принимал
решение по первой фразе. Заменено на «ПОДКЛЮЧЕНО ПО УМОЛЧАНИЮ с 20.08.2026» с явным «ручка
ВЫКЛЮЧАЕТ, а не включает».

### Правка 3 — третье ложное самоописание, найденное поиском «нет ли второго рядом»

Ключ `low_season_caps.for_consumers_if_ever_connected` — «для потребителей, **если когда-нибудь
подключат**». Проверено: блок **уже подключён** — `price_source.py:158` `_caps` →
`:328` `cap_for` → `:406` внутри `reprice`, то есть на живом пути. Величина кепки берётся
**отсюда** (`cap_thb_per_month`), а положение ручки — из живой котировки, ровно как требует
текст самого поля. Ключ переименован в `for_consumers`, впереди добавлен факт подключения с
номерами строк. **Само правило («кап нельзя применять по календарю») не изменено ни словом.**
Переименование безопасно: читателей у этого ключа **0** (греп по `*.py` и `*.md`).

### Чего НЕ трогали

**Ни одной цифры.** Ни `base`, ни `season`, ни `term`, ни `low_season_caps` числами; ни
пересчёта, ни перезамера, ни похода за живым прайсом. Диффом это видно: правка описательная.
Также не тронут `pricing_advisor.py` — его комментарии про зеркало (`:52-55`) честны и уже
называют себя зеркалом, а не источником.

## §4. Тесты набора

Полный набор не гнался — только набор, которому принадлежит правка.

```
python -m unittest test_price_source test_price_gate test_price_freshness \
                  test_price_revouch test_price_snapshot_publish test_price_snapshot_collect
Ran 166 tests in 0.267s — OK
```

Перед этим `python -m json.tool price_source.json` → **JSON OK** (файл не сломан правкой прозы).

Соседний круг (`test_pricing_advisor`, `test_moderation_card`, `test_deploy_voice`,
`test_client_contour`, `test_pricing`) — **365 тестов, 1 падение**:
`test_client_contour.py:137` «brain_writer.py обязан быть внутренним».

**Это падение НЕ от правки, и это проверено, а не предположено.** Оба файла убраны в
`git stash`, тот же набор прогнан на чистом HEAD → **62 теста, то же самое 1 падение**. Затем
`git stash pop` вернул правку. Класс — чужой (граф клиентского замыкания зацепил
`brain_writer.py`), к источнику цены отношения не имеет; заводить его чинить в этом заходе
не входило в единицу.

## §5. Арифметика захода

Потолок 2700 с, целились в 900. Единица не разрослась: одно поле данных плюс перепись, из
переписи выросли ещё две описательные правки (шапка модуля и ключ блока капов) — это тот же
предмет «файл говорит о себе правду», а не второй. За пределы захода вынесено: чужое падение
`test_client_contour`, живой планировщик ОС, облачный Apps Script.

## §6. Что теперь неверно в чужих местах (честно)

Формулировка «`price_source.json` — данные, к боту не подключены» могла разойтись по другим
документам, написанным до 20.08. Этот заход правил **источник**, а не всех, кто его пересказал;
перепись таких пересказов не делалась и в единицу не входила.

**Временное захода:** `_scratch_price_selfdesc_0904/` (заведён, пуст — разведка обошлась
чтением) и черновик доклада `tmp/pc_report/task185-28424-1788495065976-185.md`.
