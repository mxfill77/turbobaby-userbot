# Дубли зарегистрированных сред RC (D:\turbobaby-bot) — развилка для владельца

Дата: 2026-07-24 ~17:05. Контекст: в пикере каталога приложения три одинаковые записи
`D:\turbobaby-bot` — следы повторных стартов `claude rc` 24.07.

## Факты (read-only)

- **Живая среда одна**: `env_0135pmoZ79usEybo2JXD78A7` — зарегистрирована 24.07 16:50:06
  (лог `rc_server_debug.log`: `POST /v1/environments/bridge -> 200 environment_id=env_0135pmoZ79usEybo2JXD78A7`,
  `Registered, server environmentId=…`, bridgeId `c3e9caae-8e92-4483-a722-f75d1ab28db3`).
  Её держит rc-server PID 6272 (старт 16:50:05) под сторожем rc_supervisor PID 10020.
- **Дубли — мёртвые регистрации** прежних стартов rc-server: как минимум 16:36:13 (pid 21088,
  убит перезагрузкой 16:49) и более ранний запуск(и) 24.07. Их процессы завершены,
  environment_secret жил только в памяти процесса — с ПК ими управлять нечем.
- **bridgeId НЕ персистится**: `c3e9caae` отсутствует и в репо, и в `~/.claude.json`, и в
  `~/.claude\*` (совпадения — только транскрипты сессий). Каждый старт `claude rc` ⇒ НОВАЯ
  среда на сервере. Это и есть механизм накопления дублей (у сторожа рестарт ветки = новая
  регистрация).
- **CLI 2.1.218 не умеет управлять средами**: `claude rc --help` и полный `--help` не содержат
  команд list/delete/unregister для environments. Локального штатного способа удаления НЕТ.
- `rc_server_debug.log` пересоздаётся на каждый старт — старые environment_id в логах ПК не
  сохранились.

## Развилка (выбор владельца)

1. **Удалить дубли в UI** — claude.ai/code или мобильное приложение: список сред/каталогов,
   убрать две мёртвые записи `D:\turbobaby-bot` (живую отличить по статусу online/активности;
   оффлайновые = мёртвые). Браузер на ПК уже открыт на claude.ai/code.
2. **Оставить как есть** — мёртвые записи не поллят сервер и не мешают работе; риск только
   в путанице пикера (можно выбрать мёртвую среду и получить ошибку создания сессии).
3. **Системно (на будущее)** — если дубли раздражают: просить Anthropic/следить за версиями
   CLI на предмет персистентного bridgeId или команды `environments` (сейчас нет).

Проверка пункта «в пикере одна запись» возможна ТОЛЬКО с телефона/веба владельца — с ПК
серверный список сред не читается (нет API-канала без выкапывания OAuth-секретов, что
запрещено классом 328).
