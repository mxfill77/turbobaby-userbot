# Что успел создать упавший заход по хранилищу уроков (задача 41)

**Дата разведки:** 2026-08-19, 13:13–13:2x UTC (20:13–20:2x локального, +07).
**Режим:** read-only. Ничего не создано, не изменено, не удалено, не запущено, кроме этого файла.
**Признак сделанности проверен первым действием:** `docs/artifacts/2026-08-19-lesson-store-state.md`
на момент старта **отсутствовал** (`ls` → `No such file or directory`), поэтому разведка шла заново.

---

## 0. Вывод одной строкой (пункт 6 задания)

**НАЧАТО ЧАСТИЧНО, и продвинуто далеко:** модуль `lesson_store.py` (633 строки) и
`test_lesson_store.py` (527 строк, 39 тестов) написаны ЦЕЛИКОМ и не обрезаны, но **не
закоммичены, не досведены и не проверены** — сам файл таблицы `lesson_store.tsv` не заведён,
коммитов ноль, а последняя правка модуля оставила **три висячих обещания**, одно из которых
делает уже написанный тест `test_store_file_is_outside_git` **красным прямо сейчас**.

**Повторять вслепую нельзя, но и не нужно: двух хранилищ не будет — существующее одно, и оно
почти готово.** Правильный следующий заход — не «построить», а «досведить и закоммитить»
(§7).

---

## 1. Хронология окна 12:48–13:07 UTC (= 19:48–20:07 локального)

Границы подтверждены строкой METRICS хука `session_end` (`dispatch_notify.log:7681`):

```
METRICS task=9c34673a lane=session type=code mode=prod src=sdk-cli model=<synthetic> effort=xhigh
        start=2026-08-19T12:48:11+00:00 end=2026-08-19T13:06:21+00:00
```

18 мин 10 с. И реестром журнала `cowork_log.ledger` (хвост, дословно):

```
{"ts": "2026-08-19T12:48:20.168769+00:00", "line": "NOTE 2026-08-19 12:48 UTC: Orchestrator: взял задачу #41 (in_progress)"}
{"ts": "2026-08-19T13:06:39.641282+00:00", "line": "DONE 2026-08-19 13:06 UTC: ✅ Code-сессия завершена: API Error: Server error mid-response. The response above may be incomplete."}
{"ts": "2026-08-19T13:07:27.554265+00:00", "line": "NOTE 2026-08-19 13:07 UTC: Orchestrator: задача #41 → failed (думатель: halt) · Причина — транзиентная серверная API-ошибка в середине ответа, а не дефект формулировки (задача самодостаточна, путей/команд чинить нечего); следы в окне принадлежат чужим сессиям 15.08 и о фактическом состоянии нового ф"}
{"ts": "2026-08-19T13:13:12.829850+00:00", "line": "NOTE 2026-08-19 13:12 UTC: Orchestrator: взял задачу #42 (in_progress)"}
```

> **Отдельно:** запись 13:07 утверждает, что «следы в окне принадлежат чужим сессиям 15.08».
> Это **неверно** — и ровно поэтому разведка и понадобилась. Следы в окне принадлежат
> задаче 41, что показано ниже поминутно (§2): три файла в `_scratch_lessonstore_0819/`,
> `test_lesson_store.py` и `lesson_store.py` записаны 19:58–20:06 локального, то есть внутри
> окна 12:48–13:07 UTC. Ни один из них не датирован 15.08.

---

## 2. Пункт 1 — файлы, созданные или изменённые между 12:48 и 13:07 UTC

Полная выборка по дереву (исключены `.git/`, `venv/`, `manager-bot/`), команда:

```
find . -newermt "2026-08-19 19:48:00" ! -newermt "2026-08-19 20:07:00" -type f \
  -not -path "./.git/*" -not -path "./venv/*" -not -path "./manager-bot/*" \
  -printf "%TY-%Tm-%Td %TH:%TM:%TS  %10s  %p\n" | sort
```

Дословный вывод (37 файлов; `__pycache__` сгруппирован для читаемости, остальное — как есть):

```
2026-08-19 19:48:13.9227910000        4771  ./tmp/brain_backup_queue_snapshot_pc_20260819-194813.txt
2026-08-19 19:51:14.6795589000       26991  ./__pycache__/bridge_http.cpython-312.pyc
2026-08-19 19:51:14.6815599000       38601  ./__pycache__/card_duty.cpython-312.pyc
2026-08-19 19:51:14.6835595000       21072  ./__pycache__/card_terminal_log.cpython-312.pyc
2026-08-19 19:51:14.6875585000       39133  ./__pycache__/client_contour.cpython-312.pyc
2026-08-19 19:51:14.6925580000       32166  ./__pycache__/delivery.cpython-312.pyc
2026-08-19 19:51:14.6995625000       42869  ./__pycache__/dispatch_notify.cpython-312.pyc
2026-08-19 19:51:14.7005633000        7423  ./__pycache__/gate_selective.cpython-312.pyc
2026-08-19 19:51:14.7015627000        3350  ./__pycache__/io_utf8.cpython-312.pyc
2026-08-19 19:51:14.7230710000      111570  ./__pycache__/lesson_router.cpython-312.pyc
2026-08-19 19:51:14.7270714000       30958  ./__pycache__/log_setup.cpython-312.pyc
2026-08-19 19:51:14.7290728000       18141  ./__pycache__/moderation_ipc.cpython-312.pyc
2026-08-19 19:51:14.7300719000        8045  ./__pycache__/parse_outcome.cpython-312.pyc
2026-08-19 19:51:14.7400709000       58441  ./__pycache__/pc_agent.cpython-312.pyc
2026-08-19 19:51:15.0559320000      622233  ./__pycache__/pc_orchestrator.cpython-312.pyc
2026-08-19 19:51:15.0859316000      302486  ./__pycache__/pretool_guard.cpython-312.pyc
2026-08-19 19:51:15.0889298000       22802  ./__pycache__/price_source.cpython-312.pyc
2026-08-19 19:51:15.0919297000       16434  ./__pycache__/pricing.cpython-312.pyc
2026-08-19 19:51:15.0939304000       15302  ./__pycache__/result_ref.cpython-312.pyc
2026-08-19 19:51:15.0959308000       17372  ./__pycache__/result_spill.cpython-312.pyc
2026-08-19 19:51:15.0989289000       13900  ./__pycache__/reviewer.cpython-312.pyc
2026-08-19 19:51:15.0999305000        3287  ./__pycache__/selfupdate_gate.cpython-312.pyc
2026-08-19 19:51:15.1029307000       30906  ./__pycache__/session_watch.cpython-312.pyc
2026-08-19 19:51:15.2035688000      474741  ./__pycache__/suggest.cpython-312.pyc
2026-08-19 19:51:15.2065696000       13561  ./__pycache__/task_metrics.cpython-312.pyc
2026-08-19 19:51:23.2474772000          53  ./tmp/probe_iso_sop28zbr/fx_book.py
2026-08-19 19:51:23.2484779000          20  ./tmp/probe_iso_sop28zbr/tmp/claude/sess/scratchpad/fx_void.py
2026-08-19 19:54:06.3677072000        1908  ./trainer_rules.json
2026-08-19 19:58:31.3352179000        3822  ./_scratch_lessonstore_0819/smoke.py
2026-08-19 19:59:24.2737154000        4671  ./_scratch_lessonstore_0819/bench.py
2026-08-19 20:04:16.5798169000       31711  ./test_lesson_store.py
2026-08-19 20:04:20.7102769000       52069  ./__pycache__/test_lesson_store.cpython-312.pyc
2026-08-19 20:04:20.7172776000       39534  ./__pycache__/lesson_store.cpython-312.pyc
2026-08-19 20:04:51.5713645000        1202  ./_scratch_lessonstore_0819/probe_scrub.py
2026-08-19 20:06:16.2154483000       38716  ./lesson_store.py
2026-08-19 20:06:22.7689854000      801385  ./dispatch_notify.log
2026-08-19 20:06:39.6652841000       30984  ./cowork_hook.log
```

### Что из этого — работа задачи 41, а что чужое

| файл | размер | время (лок.) | чей след |
|---|---|---|---|
| `lesson_store.py` | **38 716 б** | 20:06:16 | **задачи 41** — главный предмет, последняя правка перед обрывом |
| `test_lesson_store.py` | **31 711 б** | 20:04:16 | **задачи 41** — тесты |
| `_scratch_lessonstore_0819/probe_scrub.py` | 1 202 б | 20:04:51 | **задачи 41** — проба чистки персонального |
| `_scratch_lessonstore_0819/bench.py` | 4 671 б | 19:59:24 | **задачи 41** — замер ёмкости |
| `_scratch_lessonstore_0819/smoke.py` | 3 822 б | 19:58:31 | **задачи 41** — живой смоук |
| `__pycache__/lesson_store.cpython-312.pyc` | 39 534 б | 20:04:20 | **задачи 41** — след ИМПОРТА (см. §5.3) |
| `__pycache__/test_lesson_store.cpython-312.pyc` | 52 069 б | 20:04:20 | **задачи 41** — след ПРОГОНА тестов |
| `trainer_rules.json` | 1 908 б | 19:54:06 | mtime в окне, но **содержимое совпало с HEAD** — см. ниже |
| `__pycache__/*.pyc` (24 модуля) | — | 19:51:14–15 | **чужое**: импортная волна гарда/демона. `lesson_store` среди них НЕТ |
| `tmp/probe_iso_*`, `tmp/brain_backup_*` | 20–4 771 б | 19:48–19:51 | **чужое**: изоляция проб гарда и бэкап слепка очереди |
| `dispatch_notify.log`, `cowork_hook.log` | — | 20:06:22/39 | **хвост обрыва**: хук `session_end` |

**Про `trainer_rules.json`.** Файл tracked, mtime попал в окно, но
`git status --porcelain trainer_rules.json` вернул **пусто** → содержимое байт-в-байт равно
HEAD. То есть в окне он был переписан тем же содержимым либо тронут живым тренажёром;
**правкой задачи 41 он не является**. Содержимое — 6 правил (дословно приведено в §5.4
как источник замера длины строки для `bench.py`).

---

## 3. Пункт 2 — незакоммиченные изменения в рабочем дереве

```
$ git status --porcelain lesson_store.py test_lesson_store.py trainer_rules.json
?? lesson_store.py
?? test_lesson_store.py
```

**Оба файла — untracked (`??`), то есть НОВЫЕ и никогда не попадавшие в git.**
Изменённых tracked-файлов в дереве **нет ни одного**: в полном `git status --porcelain`
нет ни одной строки с ` M`, вывод начинается сразу с untracked (`?? _tmp_final.py`).

```
$ git stash list
(пусто)
```

Отложенной работы в stash нет.

Дословный дифф показать нечем — у untracked-файла нет базы для сравнения; вместо диффа
их содержимое разобрано пофункционально в §5.

---

## 4. Пункт 3 — коммиты за окно

```
$ git log --since="2026-08-19T12:48:00+00:00" --until="2026-08-19T13:07:00+00:00" --oneline --all
(пусто)
```

```
$ find ./.git -newermt "2026-08-19 19:48:00" ! -newermt "2026-08-19 20:07:00" -type f
(пусто)
```

**Внутри `.git` в окне не изменился НИ ОДИН файл** — ни объект, ни индекс, ни `ORIG_HEAD`.
Это сильнее, чем пустой `git log`: заход не сделал ни одной git-операции вовсе, включая
`git add`.

Последняя запись reflog — `55a5ba0 HEAD@{18:02:24}` (11:02 UTC), то есть **до** окна, от
предыдущей задачи (числа вотчдога). HEAD репозитория окно не пережил и не сдвинулся.

**Коммитов за окно: 0.**

---

## 5. Пункт 5 — новый код записи и чтения уроков

### 5.1 `lesson_store.py` — 633 строки, untracked, mtime 20:06:16

Файл **не обрезан**: заканчивается полным блоком

```python
if __name__ == "__main__":
    sys.exit(main())
```

Заголовок (строка 3): `lesson_store.py — ХРАНИЛИЩЕ УРОКОВ ТРЕНАЖЁРА. Отдельная таблица; бот
её пока НЕ зовёт.`

Зависимости все на месте: `anonymize_corpus.py` (44 854 б), `io_utf8.py` (3 368 б),
`parse_outcome.py` (7 344 б).

**Константы схемы (строки 110–148):**

```
110:HERE = os.path.dirname(os.path.abspath(__file__))
111:STORE_NAME = "lesson_store.tsv"
112:STORE_PATH = os.path.join(HERE, STORE_NAME)
113:BACKUP_SUFFIX = ".bak"
114:TMP_SUFFIX = ".tmp"
119:COL_NUM = "номер"
120:COL_QUESTION = "вопрос_клиента"
121:COL_BOT = "ответ_бота"
122:COL_RIGHT = "как_правильно"
123:COL_WHY = "почему"
124:COL_WHO = "кто_записал"
125:COL_WHEN = "когда"
126:COL_STATE = "состояние"
128:COLUMNS = (COL_NUM, COL_QUESTION, COL_BOT, COL_RIGHT, COL_WHY, COL_WHO, COL_WHEN, COL_STATE)
129:HEADER_LINE = "\t".join(COLUMNS)
132:REQUIRED_FIELDS = (COL_QUESTION, COL_BOT, COL_RIGHT, COL_WHY, COL_WHO, COL_WHEN)
136:SCRUBBED_FIELDS = (COL_QUESTION, COL_BOT, COL_RIGHT, COL_WHY)
144:STATE_ACTIVE = "актив"
145:CUT_ONE = "урок"   146:CUT_DAY = "день"   147:CUT_WHO = "автор"
546:COMFORT_LESSONS = 50000
```

**Функции (полный перечень со строками):**

| строка | имя | назначение |
|---|---|---|
| 153 | `withdrawn_state(cut, stamp)` | сборка состояния `снят(<разрез>;<штамп>)` |
| 160 | `parse_state(value)` | разбор состояния обратно |
| 182 / 190 | `esc` / `unesc` | экранирование `\t`, `\n`, `\r`, `\\` — держит «строка = урок» байтами |
| 218 | `class LessonRejected(ValueError)` | отказ записи с названным полем и причиной |
| 238 | **`scrub_lesson(...)`** | чистка персонального в 4 текстовых полях через `anonymize_corpus` |
| 266 | `now_stamp` / 272 `day_of` / 277 `_leading_number` | штампы и разбор номера |
| 286 | **`load(path=None)`** | **ЧТЕНИЕ** всей таблицы → `st.lessons`, `st.reading`, `st.exists` |
| 316 | `norm_who(who)` | ключ автора без `@` и регистра |
| 321/325 | `is_active` / `active` | фильтр активных |
| 330/336 | **`by_author` / `by_day`** | разрезы чтения |
| 345 | `validate(...)` | проверка шести обязательных полей |
| 362 | `_next_number(store)` | номер, который никогда не переиспользуется |
| 376 | `_append_row(...)` | дозапись строки |
| 386 | **`add(...)`** | **ЗАПИСЬ** урока: валидация → чистка → дозапись + fsync |
| 423–447 | `_matches`, `_replace_state`, `_count_lines`, `_rewrite` | перезапись по СЫРЫМ строкам |
| 473 | **`withdraw(number=/day=/who=)`** | снятие тремя разрезами, строка НЕ удаляется |
| 553 | `capacity(path=None)` | ёмкость числом (`свободно`/`тесно`) |
| 574 | `integrity(path=None)` | целостность |
| 591/597 | `_render`, `main(argv)` | CLI `--status` / `--list` / `--list --all` |

**Заявленные в докстринге гарантии** (строки 40–50): только дозапись + `fsync`; ни `os.remove`,
ни `os.truncate`, ни `os.unlink`; единственный усекающий `open(..., "w")` — по ВРЕМЕННОМУ файлу;
снятие меняет последнее поле, рядом кладётся `.bak`. Отказ от CSV / JSON / sqlite обоснован
там же (sqlite назван красной операцией контура и потому отвергнут).

### 5.2 `test_lesson_store.py` — 527 строк, untracked, mtime 20:04:16, **39 тестов**

Файл **не обрезан**, оканчивается `unittest.main()`. Классы:

| строка | класс | тестов |
|---|---|---|
| 48 | `_Base` | — |
| 70 | `TestWhyIsMandatory` | 5 |
| 133 | `TestWithdrawKeepsRow` | 10 |
| 239 | `TestPersonalDataNeverLands` | 7 |
| 323 | `TestNothingEverDeletes` | 4 |
| 397 | `TestOneLineOneLesson` | 3 |
| 427 | `TestReaderSaysNonparse` | 5 |
| 472 | `TestCapacityNeverLoses` | 3 |
| 501 | `TestBotNotWired` | 2 |

### 5.3 Пробы в `_scratch_lessonstore_0819/` (3 файла, untracked)

* `smoke.py` (3 822 б, 19:58) — живой смоук в СВОЙ временный файл: запись → сырые байты →
  чтение → отказ без «почему» → снятие → ёмкость.
* `bench.py` (4 671 б, 19:59) — замер ёмкости на 1 000…200 000 уроков, длины полей взяты с
  живых правил `trainer_rules.json` (сознательно не на синтетике `a\tb\tc`).
* `probe_scrub.py` (1 202 б, 20:04) — проба чистки на фразе «Меня зовут Сергей, мой номер
  +66 81 234 5678».

**Сохранённых выводов этих проб на диске нет** — их результаты обрыв унёс.

### 5.4 Бот к хранилищу НЕ подключён — и это по замыслу шага

```
$ grep -rn "lesson_store" --include=*.py --include=*.md --include=*.json .
  (кроме самих lesson_store.py, test_lesson_store.py и _scratch_lessonstore_0819/ — пусто)
```

Ни `trainer.py`, ни `moderation_bot.py`, ни `userbot_listen.py`, ни `suggest.py`,
ни `lesson_router.py` модуль не импортируют. Докстринг называет это границей шага явно
(строки 12–15), и это заперто тестом `TestBotNotWired.test_no_bot_module_imports_the_store`.
**Клиентский контур не тронут.**

---

## 6. Пункт 4 — таблица: заведена ли

**Файла таблицы НЕТ.**

```
$ ls -la lesson_store.tsv lesson_store.tsv.bak lesson_store.tsv.tmp
ls: cannot access 'lesson_store.tsv': No such file or directory
ls: cannot access 'lesson_store.tsv.bak': No such file or directory
ls: cannot access 'lesson_store.tsv.tmp': No such file or directory
$ ls -la *.tsv
ls: cannot access '*.tsv': No such file or directory
```

Ни одного `.tsv` в корне репозитория нет. Это **не поломка**: файл по устройству заводится
лениво, первым `add()`, а `load()`/`capacity()`/`integrity()` честно говорят «ещё не заведён»
(`main()`: `"есть" if store.exists else "ещё не заведён"`).

Поэтому на подпункты отвечаю по СХЕМЕ, заданной кодом, а не по живому файлу:

* **имя:** `lesson_store.tsv` (`STORE_NAME`), в корне репозитория рядом с модулем;
* **листов:** понятия «лист» у формата нет — это плоский TSV, один файл = одна таблица;
* **колонок: 8** — `номер`, `вопрос_клиента`, `ответ_бота`, `как_правильно`, `почему`,
  `кто_записал`, `когда`, `состояние`;
* **строк: 0** (включая заголовок) — файла не существует.

Ничего в таблице не менялось и не могло быть изменено: менять нечего.

---

## 7. Что именно осталось недоделанным — три висячих обещания

Последняя правка модуля (20:06:16) **новее** последнего прогона тестов (20:04:20) и вписала
в докстринг три ссылки, которым нечего адресовать. Это и есть точка обрыва.

**(а) Обещан тест, которого нет.** Строки 81–82 `lesson_store.py`:

```
81:  разбор — в docs/artifacts/2026-08-19-lesson-store.md §6; поведение заперто тестом
82:  `TestPersonalDataNeverLands.test_greeting_position_overmasks_by_design`.
```

```
$ grep -n "greeting_position_overmasks_by_design" test_lesson_store.py
(пусто)
```

В классе `TestPersonalDataNeverLands` семь тестов, и такого среди них нет. Докстринг
утверждает «заперто тестом» про замок, которого не существует.

**(б) Обещан артефакт, которого нет.** Ссылки на `docs/artifacts/2026-08-19-lesson-store.md`
стоят дважды — строка 81 (§6) и строка 524 (§5):

```
$ ls -la docs/artifacts/2026-08-19-lesson-store.md
ls: cannot access ... No such file or directory
```

(Не путать с ЭТИМ файлом — `…-lesson-store-state.md` — он про состояние, а не разбор
устройства.)

**(в) `.gitignore` не дополнен — и это делает уже написанный тест КРАСНЫМ.**
`test_lesson_store.py:519` требует буквально:

```python
def test_store_file_is_outside_git(self):
    """Файл таблицы не под git: `git checkout HEAD --` молча откатил бы накопленные уроки."""
    with open(os.path.join(HERE, ".gitignore"), encoding="utf-8") as f:
        self.assertIn(LS.STORE_NAME, f.read(),
                      "таблица уроков не занесена в .gitignore — откат дерева её сотрёт")
```

```
$ grep -n -i "lesson\|tsv" .gitignore
(пусто)
```

Строки `lesson_store.tsv` в `.gitignore` нет. **Этот тест падает на текущем дереве.**
(Довод, ради которого он написан, в докстринге назван по чужому коду: `pc_orchestrator`
возвращает дерево к HEAD, строки 2389 и 2491, а авто-фетч встаёт на грязной копии, строка 6161.)

**(г) Итоговая правка не проверена ни одним прогоном.** `__pycache__/lesson_store.cpython-312.pyc`
записан 20:04:20, а `lesson_store.py` — 20:06:16: исходник **новее** байткода, значит с момента
последней правки модуль ни разу не импортировался. Синтаксис финальной редакции здесь **не
проверялся сознательно** — `py_compile` создал бы файл, а разведка read-only. Хвост файла цел,
но это не то же самое, что «компилируется».

Порядок событий читается по минутам и объясняет обрыв: тесты записаны (20:04:16) → прогнаны
(20:04:20) → написана проба чистки (20:04:51) → в модуль внесено объяснение перемаскировки
(20:06:16) → API оборвался (20:06:21).

---

## 8. Сводка

1. **Двух хранилищ не будет.** Существует ровно одно, `lesson_store.py` (633 стр.) +
   `test_lesson_store.py` (527 стр., 39 тестов), оба untracked и оба целые, не обрезанные.
2. **В git не ушло ничего:** коммитов за окно 0, `.git` в окне не тронут ни одним файлом,
   stash пуст, изменённых tracked-файлов нет.
3. **Файла таблицы `lesson_store.tsv` нет** — по устройству он заводится первым `add()`;
   схема из 8 колонок задана в коде, строк ноль.
4. **Бот не подключён ни одной веткой** — грep по `lesson_store` вне самих файлов пуст,
   клиентский контур не тронут.
5. **Осталось четыре хвоста:** нет теста `test_greeting_position_overmasks_by_design`; нет
   артефакта `2026-08-19-lesson-store.md` (обещан дважды); нет строки `lesson_store.tsv` в
   `.gitignore` — из-за чего `test_store_file_is_outside_git` красный; финальная правка модуля
   не проверена прогоном.

**Следующий заход должен быть «досведить и закоммитить», а не «построить».** Строить заново —
значит выбросить 1 160 строк готового кода.

---

*Разведка read-only. За её время не создано, не изменено и не удалено ничего, кроме этого
файла; ни один процесс не тронут, `.env` и конфиги не читались, гейт не гонялся, тесты не
запускались.*
