# Подсказки критика перестали пропадать молча: три исхода вместо одного, сырой ответ сохраняется, причина названа числом — обрезка, а не промпт и не разбор

КРИТИК tri-ishoda 1209

**Дата:** 12.09.2026 · **Полоса:** ПК · **Задание:** ящик Штаба, ключ `52-b-kritik-tri-ishoda.1209`
(док `shtab_task_52-b-kritik-tri-ishoda.1209`, file id `1J6RDShl2Bw5OrqpkgAsbUYbm1nP9Y-ou`)
**Коммиты:** `37c290f` (три исхода, сохранение ответа, свой потолок) · `3cd17b5` (исход названный
собирающему) · `88838eb` (кейс 1 пересобран) · этот артефакт — четвёртым.
**Временное место захода, названо явно:** `tmp/exam52b/` (проба `critic_probe.py`, сырые ответы
`raw/case-{1,2,3,7,9}.txt`, результаты `probe-case-*.json`, логи) и `tmp/pc_report/` (черновик
доклада). Боевой код сохраняет ответы критика в `tmp/exam_critic/` — тоже вне git. Ничего не
удалялось, включая уборку за собой.

---

## FACT — живые числа

| что доказано | число / факт |
|---|---|
| **причина пустых подсказок названа** | **ОБРЕЗКА.** 5 ответов из 5 — в правильной форме; разбор вынул **15 гипотез из 15**; книга правил (14 правил) отсеяла **0**. Промпт и разбор оправданы числом |
| **чем именно резало** | круг критика длиннее прежнего потолка у **4 замеров из 5**: 61.0 · 121.8 · 122.5 · 128.3 с против **60 с** |
| **потолок был ОБЩИЙ с черновиком** | `suggest.CLI_TIMEOUT = 60` (`suggest.py:244`) питал оба круга единицы; разведён — `exam_show.CRITIC_TIMEOUT` |
| **новый потолок — по замеру** | **257 с** = удвоенный максимум замера (128.3 × 2 = 256.6). Медиана пяти кругов 121.8 с, то есть прежние 60 с были **вдвое меньше медианы** |
| **почему отказ был молчалив** | `suggest._cli_llm` на таймауте возвращает **пустую строку и НЕ бросает исключения** → ветка «критик подсказок не дал» не срабатывала ни разу |
| **три исхода в снимке** | поле `critic.outcome` ∈ `hints` / `silent` / `unparsed`; слова у (б) и (в) разные, оба говорят «НЕИЗВЕСТНО, почему» |
| **кейс 1 пересобран** | слот `case-1@37c290f.json`: подсказок **3** (было **0**), исход `hints`, ждали **71.3 с** при пределе 257 |
| **под прежним потолком тот же круг снова дал бы ноль** | 71.3 с > 60 с |
| **старых слотов тронуто** | **0** — `git status` по `exam_shots/` до коммита: 0 изменённых, 1 новый |
| **проверок в наборе** | **98 → 109** (+11), `test_exam_show` **109/109**; соседние `trainer`/`trainer_run`/`trainer_log`/`trainer_pin`/`pc_agent`/`style_examples` — **401/401** |
| боевых файлов правлено | **1** (`exam_show.py`, 1273 → 1464 строки) + его набор; потолок задания — два |
| наружу ушло сообщений | **0** (`--show`, `--reach`, `dispatch_notify` не звались ни разу) |
| сырой ответ в снимке владельцу | **нет ни одной веткой** — в снимке живут длина (684) и отпечаток (`12364dc6cc7373d4`), сам текст во временном месте |

---

## §1 Признак сделанности — по СОДЕРЖИМОМУ, первым действием

Артефактов за 12.09 на входе — **15**. Слов адреса `КРИТИК tri-ishoda 1209` не несёт ни один,
вопросы задания в них не отвечены → заход продолжен. HEAD на входе `908f4d2`, трекаемое дерево
чисто.

## §2 Свой файл прежде чужого

Прочитаны и **не переделаны**: [`2026-09-12-СНИМКИ-odin-devyat-1209.md`](2026-09-12-СНИМКИ-odin-devyat-1209.md)
(разбор расхождений, §Р1: 2 подсказки из потолка 36, обе в кейсе 8; таймаут напечатан у кейсов 4,
5, 6; у 1, 2, 3, 7, 9 критик ответил, а разбор не вынул ничего, и «что именно вернула голова —
НЕИЗВЕСТНО: ответ критика нигде не сохраняется») и раздел про критика в
[`2026-09-12-ЭКЗАМЕН-obrazets-iyul-1209.md`](2026-09-12-ЭКЗАМЕН-obrazets-iyul-1209.md) (`6f709fb`
— парный режим, `split_pair` fail-safe в обе стороны, боевой путь модербота не изменился ни на
символ). Замеры оттуда взяты ссылкой.

## §9 Арифметика бюджета — ДО первой строки кода

Кругов головы не больше четырёх, цена круга по замеру 52-a — **185 с**; произведение
**4 × 185 = 740 с против потолка 2700 с = 0.274 потолка**. Порог пройден с запасом → работа начата.

Фактически заплачено меньше расчётного, и вот чем. Пробы платили **только за круг критика**:
вопрос и черновик уже лежали в снимках 52-a, и второй раз за них никто не платил.

| что | стенных секунд |
|---|---|
| пять проб критика (кейсы 1, 2, 3 подряд; 7 и 9 парой) | 353 |
| пересборка кейса 1 полным кругом (черновик + критик + метки) | ≈ 490 |
| **итого** | **≈ 843 с = 0.31 потолка** |

Расчёт 740 с был верхней оценкой четырёх ПОЛНЫХ кругов; фактические 843 с включают круг
пересборки и пять критик-кругов, то есть шесть обращений к голове ценой меньше двух полных единиц
каждое. Оценка проверялась ДО работы, а не подгонялась после.

## §3 Три исхода вместо одного — что изменилось в словах

До сегодня снимок без подсказок выглядел ОДИНАКОВО во всех случаях. Теперь исходов три, и у
каждого свои слова (`exam_show.no_hints_line`):

| исход | когда | что говорит владельцу |
|---|---|---|
| **(а)** `hints` | подсказки вынуты | «🔎 ЧТО УЛУЧШИТЬ — подсказки критика», нумерованный список, **число** подсказок в легенде кнопок |
| **(б)** `silent` | ответа нет вовсе (таймаут, отказ, исключение) | «ПОДСКАЗОК НЕТ: критик **НЕ ОТВЕТИЛ** — ждали N с при пределе M с. Почему именно — **НЕИЗВЕСТНО**: ответа нет вовсе, разбирать нечего» |
| **(в)** `unparsed` | ответ есть, гипотез в нём разбор не нашёл | «ПОДСКАЗОК НЕТ: критик **ОТВЕТИЛ** — K знаков, сохранён в `<путь>`, — но разбор не вынул ни одной. Почему именно — **НЕИЗВЕСТНО**» |

Оба нулевых исхода говорят «неизвестно, почему» **прямо**, а не «подсказок нет»: у бота нашлось бы
что улучшить — механизм этого не донёс, и выдавать незнание за ответ здесь запрещено веткой.
Снимок старого образца (ключа `critic` нет вовсе) показывается РОВНО как прежде — проверено
живьём на настоящем `case-1@4b6160e.json`: карточка 1334 знака, слова «подсказок критика в этом
снимке нет» есть, новых слов нет ни одного.

Собирающему исход называется СРАЗУ (`--freeze` печатает `critic_line`), в ту минуту, когда ещё
можно позвать круг заново, — а не сутки спустя при показе.

## §4 Сырой ответ сохранён, причина названа ЧИСЛОМ

**Адрес временного места:** проба — `tmp/exam52b/raw/case-{1,2,3,7,9}.txt`; боевой код —
`tmp/exam_critic/case-<кейс>-<штамп>.txt` (`exam_show.CRITIC_RAW_DIR`, каталог под `.gitignore`).
В снимок владельцу сырой ответ НЕ кладётся: там живут только длина и отпечаток.

Пять ответов сняты на УЖЕ ЗАМОРОЖЕННЫХ вопросах и черновиках 52-a (то есть на том же материале,
на котором подсказки пропали), потолок вызова на время пробы поднят локально — иначе замер упёрся
бы в сам потолок и померил бы его, а не работу:

| кейс | секунд | длина ответа | маркер `ГИПОТЕЗЫ:` | разделителей `\|\|` | строк в секции | вынуто разбором | отсеяно книгой |
|---|---|---|---|---|---|---|---|
| 1 | **61.0** | 618 | есть | 3 | 3 | **3** | 0 |
| 2 | **122.5** | 881 | есть | 3 | 3 | **3** | 0 |
| 3 | **44.9** | 621 | есть | 3 | 3 | **3** | 0 |
| 7 | **121.8** | 1076 | есть | 3 | 3 | **3** | 0 |
| 9 | **128.3** | 763 | есть | 3 | 3 | **3** | 0 |
| **итого** | медиана 121.8 | — | **5 из 5** | по 3 в каждом | 15 | **15 из 15** | **0** |

**Причина в ОБРЕЗКЕ, и это сказано числом, а не предположением.**

- **Не в промпте:** форму соблюли **5 ответов из 5** — маркер секции на месте, каждая строка двумя
  частями через `||`, секция отсева заполнена осмысленно. Просить формат иначе незачем: его и так
  соблюдают.
- **Не в разборе:** `parse_hypotheses` вынул **15 гипотез из 15** предложенных. Замок повтора по
  книге правил (14 действующих правил) выбросил **0** — то есть подозрение «книга съедает
  гипотезы» опровергнуто числом, а не отведено словами.
- **В обрезке:** круг критика длиннее прежнего потолка 60 с у **4 замеров из 5**. Механизм резал
  ответ ДО того, как разбор его видел, и резал в ПУСТУЮ СТРОКУ: `suggest._cli_llm` на
  `TimeoutExpired` возвращает `""` и **не бросает исключения** — поэтому ветка «критик подсказок не
  дал» (`exam_show.freeze`) не срабатывала ни разу, и снимок молча ложился с нулём.

Живое подтверждение диагноза на продукте: пересобранный кейс 1 дал круг **71.3 с** — под прежним
потолком 60 с он снова получил бы ноль подсказок, под новым получил **три**.

**Чего замер НЕ доказал — сказано честно.** Исход (в) на пяти свежих ответах **не воспроизвёлся ни
разу**: у кейсов 3, 7 и 9 в 52-a таймаут в лог не печатался (проверено: логи `f3.log`, `f7.log`,
`f9.log` не содержат ни строки `таймаут`, ни строки `rc=`; логгер `suggest` без обработчика отдаёт
предупреждения в stderr, и у кейсов 4, 5, 6 они в логах есть — значит их отсутствие означает
отсутствие отказа, а не потерю строки). Ответ тогда был, и разбор дал ноль — почему, сегодня
сказать нечем, потому что тот ответ не сохранён. **Ровно для этого случая ответ теперь и
сохраняется:** следующий такой эпизод будет разбираем числом, а не догадкой, и в карточке он
получит СВОИ слова.

## §5 Потолок вызова критика — по замеру, и он больше не общий

**Он БЫЛ общим, и это названо прямо:** `suggest.CLI_TIMEOUT` (60 с, `suggest.py:244`) питает
`_cli_llm` — единственный вызов головы и у черновика (`trainer_run.run_case`), и у критика
(`_default_critic` → `suggest.default_llm_caller`). Предел мерили черновику, а упирался в него
критик, у которого материал заметно больше: системная часть запроса — **10752 знака**.

Замер — на трёх кейсах ПОСЛЕДОВАТЕЛЬНО (параллельная нагрузка испортила бы число) плюс пара:

| набор | кейсы | секунды | медиана | максимум |
|---|---|---|---|---|
| последовательный | 1, 2, 3 | 61.0 · 122.5 · 44.9 | **61.0** | **122.5** |
| парный (под двойной нагрузкой) | 7, 9 | 121.8 · 128.3 | 125.1 | 128.3 |
| **все пять** | — | 44.9…128.3 | **121.8** | **128.3** |

Новое значение — **`CRITIC_TIMEOUT = 257 с`**, удвоенный максимум замера (128.3 × 2 = 256.6,
вверх до целой секунды). Не круглое намеренно: число выведено из замера, а запас «вдвое хуже
худшего измеренного» назван правилом, а не взят на глаз. Ручка `EXAM_CRITIC_TIMEOUT` рядом —
отменить можно не правя код.

Разведение сделано ТОЧЕЧНО и с возвратом (`try/finally`): `CLI_TIMEOUT` — модульная глобаль
`suggest`, и оставить её поднятой значило бы молча поднять потолок ЧЕРНОВИКА тоже, а его предел
меряли не мы. Замок на это — тест `test_the_critic_ceiling_is_its_own_and_is_given_back_after_the_call`:
проверяет, что круг критика шёл под своим потолком И что потолок черновика после круга прежний.

## §6 Правка — по результату разбора, коммиты по зелёным наборам

Боевых файлов правлено **один** — `exam_show.py` (потолок задания два). `trainer.py` не тронут ни
строкой сознательно: разбор оправдан числом (15 из 15), и правка невиновного здесь была бы вредом.

| коммит | что | набор |
|---|---|---|
| `37c290f` | три исхода, сохранение сырого ответа, свой потолок по замеру | 108/108 |
| `3cd17b5` | исход круга назван собирающему сразу при `--freeze` | 109/109 |
| `88838eb` | пересобранный кейс 1 в новом слоте | — (данные) |

Каждый — через замок `git_serial_pc.py` (рядом шёл второй заход, индекс один на двоих).
`.git/index.lock` не встретился ни разу, чужого замка не удалялось.

## §7 Отрицательные тесты — числом

Новых проверок **11**, набор вырос **98 → 109**, все зелёные. Четыре из пункта 7 задания:

| требование задания | тест | итог |
|---|---|---|
| фикстура ответа в правильной форме — гипотезы вынимаются | `test_an_answer_in_the_right_form_yields_the_hints` | ok |
| пустой ответ — исход (б) | `test_an_empty_answer_is_the_silent_critic` | ok |
| ответ, из которого разбор не вынул ничего, — исход (в), а не (а) и не (б) | `test_an_answer_that_yields_nothing_is_the_third_outcome` | ok |
| снимок старого образца без ключа подсказок — как прежде | `test_an_old_shot_without_the_critic_key_is_shown_as_before` | ok |

Остальные семь: разные слова у (б) и (в) и слово «НЕИЗВЕСТНО» у обоих; сырой ответ лежит во
временном месте и не едет в снимок; свой потолок и возврат чужого; исход едет В ФАЙЛ снимка;
упавший критик назван словом и снимка не отменяет; исход назван собирающему; fail-safe строки без
разделителя.

**Расхождение с буквой пункта 7 — названо, а не обойдено.** Задание просит, чтобы «ответ без
разделителя» давал исход (в). Живой код даёт (а), и это НЕ дефект: `trainer.split_pair`
СОЗНАТЕЛЬНО fail-safe — строка без `||` целиком идёт ПРАВИЛОМ, потому что правило есть то, что
ложится уроком, и терять его из-за несоблюдённого формата нельзя (обратный выбор дал бы урок с
пустым текстом, по которому бот не ответит ничего). Сломать этот fail-safe ради буквы пункта
значило бы выбросить работу оплаченного круга. Поэтому: исход (в) проверяется на ответе, из
которого разбор действительно не вынимает ничего (модель забраковала всё сама), а на строку без
разделителя поставлен ЗАМОК — `test_a_line_without_the_separator_still_becomes_a_hint`, — чтобы
«развести исходы» однажды не сломало fail-safe заодно.

## §8 Кейс 1 пересобран — числом

Слот **`case-1@37c290f.json`**, один круг, новый файл рядом со старыми.

| что | было (52-a, `@4b6160e`) | стало (`@37c290f`) |
|---|---|---|
| подсказок | **0** | **3** |
| исход снимка | ключа `critic` нет вовсе | **`hints`** |
| ждали / предел | не измерялось | **71.3 с** / 257 |
| длина ответа критика | неизвестна (не сохранялся) | **684** знака, отпечаток `12364dc6cc7373d4` |
| кнопок в карточке | 2 | **6** (три номера-тумблера) |
| раздел карточки | «подсказок критика в этом снимке нет» | «🔎 ЧТО УЛУЧШИТЬ», три пары «наблюдение → правило» |

**Старые слоты не тронуты:** у кейса 1 их на диске четыре (`case-1.json`, `@df03e80`, `@4b6160e`,
`@37c290f`), и `git status` по каталогу до коммита дал **0 изменённых** и 1 новый. Отказ по
коммиту работает по построению — перезаписать занятый слот механизм не умеет ни одной веткой.

## §Запреты задания — исполнены

Наружу не ушло **ни одного** сообщения (`--show`, `--reach`, `dispatch_notify` не звались).
`pc_agent` и демон не перезапускались, процессов не трогали. Клиентских детей не поднимали.
Боевых файлов правлено **один** плюс его набор. Сырой ответ критика в снимок владельцу не положен
ни одной веткой — только длина и отпечаток. Старых снимков не удаляли и не перезаписывали (0
изменённых по git). В базу уроков `lesson_store.tsv` и в журнал вердиктов `exam_verdicts.tsv` не
писали. Модербота и `userbot_listen.py` не правили. Броней и листа «Календарь бронирования» не
открывали; живой таблицы периодов проба не касалась вовсе (`cross_window` подменён заглушкой, а у
кейсов 1…9 плейсхолдера `{when_cross}` нет ни одного — проверено по корпусу). `.env` и конфиги не
читали, значений токенов не печатали. Текста клиентских диалогов в этот артефакт не выведено —
только имена кейсов, числа и длины.

**Средовое красное, замеченное и не чиненное:** `test_suggest.test_anthropic_key_is_read_only_from_environment`
падает в ПОЛНОМ прогоне `test_suggest` (692 теста) и зеленеет в одиночном прогоне своего класса.
К правке отношения не имеет: `test_suggest.py` не упоминает `exam_show` **ни разу** (0 совпадений),
а `suggest.py` заход не трогал ни строкой. Класс — порядок импортов набора, не дефект дня.

## §Что осталось следующему заходу

1. **Исход (в) на живом материале** — сегодня не воспроизведён ни разу. Сохранение ответа заведено
   ровно ради него: первый же случай теперь будет разбираем числом.
2. **Кейсы 2…9 на новом коде** — их слоты по-прежнему на `4b6160e`, с нулём подсказок и без поля
   исхода. Пересобирать их этот заход не платил: задание называло кейс 1.
3. **Потолок черновика** (60 с) остался прежним и не мерился — он вне предмета задания; у кейсов
   4, 5, 6 таймаут 52-a был у КРИТИКА, а черновики собрались.
