# Разведка заявки внешнего канала — ключ a38b18117bfd (06.09.2026)

Адрес результата (слова задания): **2026-09-06-разведка-zayavka-a38b18117bfd**.

Ступень E, вид работы — **разведка**: читал, считал, написал. Операционного состояния не
трогал ни одной строкой: процессов, очереди, боевых баз, денег, детей, выкаток и удалений
здесь нет. Боевой код НЕ правлен — найденное названо словами, решение за владельцем.

| что | значение |
|---|---|
| заявка | очередь **#107**, вид **ПЕРЕУСЛОЖНЕНО**, источников 1 |
| ключ заявки | `a38b18117bfd` |
| предмет | `shtab_box_run.py` + AST-инвариант в `test_shtab_box.py` |
| источник находки | `docs/review_inbox/2026-09-06-2026-09-06-chain-pc-2026-09-06-104-codex.md` (канал codex) |
| пакет, на который отвечали | `docs/review_outbox/2026-09-06-chain-pc-2026-09-06-104.md` |
| HEAD на момент разведки | `33f1383` (2026-09-06 21:37:17 +0700), Python 3.12.5 |
| место для временного | `tmp/recon_a38b/` — три скрипта замера, только чтение, ничего не удалялось |

Текст находки — **МНЕНИЕ внешнего канала**, а не задание: ни одной строки оттуда не
исполнялось, задач не порождено, в очередь ничего не клалось.

---

## 0. Какая именно находка носит этот ключ — посчитано, а не угадано

Ответ канала содержит ТРИ находки, и по одному их виду заявку не опознать. Ключ считает
живой код разбора (`review_intake.claim_key` — вид плюс мера смысла), поэтому его и звали:

```
venv/Scripts/python.exe tmp/recon_a38b/measure.py
```

| находка | вид | ключ |
|---|---|---|
| #1 — `--by` и запись личности в замке | УПРОЩАЕМО | `eae2a347d209` |
| #2 — отдельный второй реестр дедупликации | НЕ ДЕЛАТЬ ВОВСЕ | `a31ad2d559ed` |
| **#3 — прежний AST-инвариант на весь `shtab_box_run.py`** | **ПЕРЕУСЛОЖНЕНО** | **`a38b18117bfd`** ← эта |

Дословная посылка находки #3:

> Переусложнён прежний AST-инвариант, запрещавший любые двери доставки и кнопки во всём
> `shtab_box_run.py`: он охранял один вид извещения чрезмерно широкой областью и конфликтовал
> с новым, отдельным сообщением. Его сужение выглядит необходимым; достаточно ли новый тест
> привязан именно к извещению о взятии, из пакета неизвестно.

## 1. Жива ли посылка СЕГОДНЯ по адресу `shtab_box_run.py` — нет, и это главный итог

Посылка распадается на три утверждения, и они имеют РАЗНУЮ судьбу.

**(а) «прежний инвариант обходил ВЕСЬ модуль» — правда, и это проверено, а не принято на слово.**

```
git show 2cb08e0^:test_shtab_box.py   # строки 2307–2331
```

В той редакции тест `test_the_notice_carries_no_buttons_and_never_goes_to_the_inbox` делал
`tree = ast.parse(_src("shtab_box_run.py"))` и шёл `for node in ast.walk(tree)` — без области
вовсе. Посылка исторически ВЕРНА.

**(б) «его сужение выглядит необходимым» — сужение УЖЕ СОСТОЯЛОСЬ, и сделал его тот самый
коммит, который ревьюер рецензировал.** На диске сегодня:

* `test_shtab_box.py:2344` — `_door_names(inside=…, outside=…)`, область названа доводом;
* `test_shtab_box.py:2373` — `test_the_TAKE_notice_…` зовёт `outside=HOLD_DOOR`;
* `test_shtab_box.py:2402` — парный `test_the_STOP_notice_…` зовёт `inside=HOLD_DOOR`.

**Значит по предмету посылка на 06.09 МЕРТВА: находка описывает состояние ДО рецензируемого
коммита и советует ровно то, что в нём уже сделано.** Работы она не открывает.

**(в) «достаточно ли новый тест привязан именно к извещению о взятии — неизвестно» —
единственная живая часть, и она измерима.** Ответ: **тест НЕ привязан к извещению о взятии,
и это сознательно; привязка, которой ждал ревьюер, была бы ОСЛАБЛЕНИЕМ.**

```
venv/Scripts/python.exe tmp/recon_a38b/measure.py          # область и имена дверей
venv/Scripts/python.exe tmp/recon_a38b/outside_funcs.py    # что теряет пофункциональная привязка
```

| замер | число |
|---|---|
| `shtab_box_run.py` | 1808 строк, 32 `FunctionDef`, **9398** узлов AST |
| `notify_hold` (единственное исключение) | строки 1376–1411, **91** узел |
| область живого TAKE-теста = модуль минус исключение | **9307** узлов; под полным запретом **31 функция из 32** |
| `notify_taken` (то, к чему ревьюер предлагал привязаться) | строки 1334–1373, **111** узлов |
| во сколько раз такая привязка была бы у́же | **в 84 раза** |
| упоминает ли TAKE-тест имя `notify_taken` | **НЕТ** (проверено обходом AST самого теста) |

Живой прогон предмета — зелёный:

```
venv/Scripts/python.exe -m unittest test_shtab_box.TestTakeNotice -v
→ Ran 19 tests in 2.342s   OK
```

## 2. Чего находка НЕ учитывает — три вещи, каждая с числом

**2.1. «Чрезмерно широкая область» — не избыток, а несущая часть конструкции.** Модуль
дважды кладёт дверь В ПЕРЕМЕННУЮ, и пофункциональная привязка этих строк не увидела бы вовсе:

```
shtab_box_run.py:1371  door = sender if sender is not None else dispatch_notify.send_topic_strict
shtab_box_run.py:1409  door = sender if sender is not None else dispatch_notify.send_critical
```

Сверх того **11** узлов `Call`/`Attribute` живут ВНЕ любой функции (модульный уровень:
`Queue`, `main`, `exit`, `OPEN_STATUSES`, `path`, `abspath`, `dirname`) — область «весь модуль
минус одна функция» покрывает и их, а «внутри `notify_taken`» не покрывает ни одного.
Реально названные в модуле двери (по AST, мимо докстрингов): `send_topic_strict` →
только `notify_taken`; `send_critical` → только `notify_hold`; `send_topic`, `deliver`,
`awaits_reply`, `send` → нигде.

**2.2. Неизвестность ревьюера родилась в САМОМ ПАКЕТЕ, а не в работе.** Ответ на его вопрос
стои́т в артефакте цепочки `docs/artifacts/2026-09-06-box-stop-notice-answer.md` строками
**99–104** («запрет остался ПОЛНЫМ для всего модуля, кроме одной поимённо названной функции…
область покрытия включает и код вне функций… незапрещённого места в модуле не осталось ни
одного»). Пакет вырезал строки **95–123** — из **43** невошедших строк (из 123) это **29**.
То есть находка меряет слепое пятно упаковщика, а не дыру в коде; и это ПОВОД смотреть на
выборку пакета, а не на тест.

**2.3. Находка не заметила настоящего дефекта, лежащего ровно в том месте, о котором она
говорит: область у `_door_names` отбирается по совпадению `id(node)`, и она ПРОТЕКАЕТ.**

`ast` делит один объект узла на весь модуль у **13** типов (`Load`, `Store`, `Add`, `And`,
`Eq`, `In`, `Is`, `IsNot`, `Mod`, `Not`, `NotEq`, `NotIn`, `Or`) — таких вхождений **3242**
из 9398. Из-за этого отбор «по id» уводит **3170** узлов не в ту область:

```
venv/Scripts/python.exe tmp/recon_a38b/leak.py
```

| область | живой способ (по `id`) | честный (по поддереву) | разница |
|---|---|---|---|
| `outside=notify_hold` | 6137 узлов | 9307 узлов | **−3170** |
| `inside=notify_hold` | **3261** узел | **91** узел | **+3170** |

Иными словами, «внутри `notify_hold`» живой тест считает 34.7% модуля вместо 0.97%.

**КОНТРФАКТ — и он оправдывает: вердикт от протечки НЕ меняется.** Тот же скрипт считает
имена дверей и доводы обоими способами и сверяет:

```
--- outside=notify_hold ---  имена дверей совпали: ДА ; доводы совпали: ДА
--- inside=notify_hold  ---  имена дверей совпали: ДА ; доводы совпали: ДА
способ A (живой)     TAKE-инвариант: зелён ; STOP-инвариант: зелён
способ Б (честный)   TAKE-инвариант: зелён ; STOP-инвариант: зелён
```

Причина инертности названа числом, а не надеждой: среди 13 делимых типов **нет ни
`Attribute`, ни `Call`, ни `keyword`** — протекают только узлы без полей `attr`/`func`/
`keywords`, то есть ровно те, из которых `_door_names` не берёт ничего. **Дефект реален,
но сегодня безвреден.**

## 3. Что повод ЗАКРЫВАЕТ, что оставляет, что остаётся НЕИЗВЕСТНЫМ

**ЗАКРЫВАЕТ.**

* Вопрос «достаточно ли новый тест привязан именно к извещению о взятии» — закрыт числом:
  привязан не к нему, а к модулю минус одно поимённое исключение, что в 84 раза шире
  предложенного; 31 функция из 32 под полным запретом, 32-ю судит парный обратный инвариант.
* Вопрос «не ослабление ли сужение» — закрыт: незапрещённого места в модуле не осталось,
  оба инварианта зелены и живым прогоном (19 тестов), и независимым пересчётом обоими
  способами отбора области.
* Сам предмет находки — закрыт коммитом `2cb08e0` ДО того, как находка была написана.

**ОСТАЁТСЯ (правка названа СЛОВАМИ, не сделана — разведка кончается артефактом).**

Отбор области в `test_shtab_box.py:2344` (`_door_names`) стои́т на `id(node)` и потому
приписывает исключению 3170 чужих узлов. Честная привязка — считать кратность вхождений
(`collections.Counter` по `id`, как в способе Б выше) либо вести родителя при обходе.
Сегодня это безразлично; перестанет быть безразличным в день, когда инвариант начнут судить
по узлу С ПОЛЯМИ (напр. `Constant`/`Name`) или когда исключений станет два и их области
начнут пересекаться — тогда протечка даст ЛОЖНЫЙ ЗЕЛЁНЫЙ молча. Решение — за владельцем.

**НЕИЗВЕСТНО (третий исход, законный).**

* Нужен ли `--by` другим контурам — это находка **#1** (ключ `eae2a347d209`, вид УПРОЩАЕМО),
  предметом ЭТОЙ заявки она не является и здесь не мерилась.
* Достаточна ли выборка пакета (п. 2.2) как класс — судить по одному случаю нельзя: замерен
  один пакет, а не корпус.

## 4. Ложный ли повод — нет, но и работы он не открывает

Повод **не ложный**: он указал на верное место верного файла, и его историческая посылка
подтвердилась дословно. Но по действию он **пуст**: советует сделанное. Ценность захода
оказалась не в находке, а рядом с ней — в п. 2.3, которого находка не увидела.

---

## Приложение: чем меряно (всё — только чтение)

| скрипт в `tmp/recon_a38b/` | что считает |
|---|---|
| `measure.py` | ключ каждой из трёх находок; область инварианта; к чему привязан тест; реально названные двери |
| `check_scope.py` | перепроверка области: 91 узел поддерева против 3261 отобранного по `id` |
| `leak.py` | контрфакт «протечка меняет вердикт?» — два способа отбора, сверка имён, доводов и обоих инвариантов |
| `outside_funcs.py` | двери в переменной, узлы вне функций, во сколько раз у́же привязка к `notify_taken` |

Ни один скрипт ничего не пишет, не шлёт и не удаляет; `.env` и конфиги не читались.
