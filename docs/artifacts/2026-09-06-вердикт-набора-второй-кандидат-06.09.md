# Вердикт экзаменационного набора на ВТОРОМ замороженном кандидате: КРАСНЫЙ, 14 из 16

вердикт набора второй кандидат 06.09

**Дата:** 06.09.2026 · **полоса:** ПК · задание из ящика Штаба, ключ
`00-j-corpus-verdict-2.0906`. Временное этого захода живёт в `_scratch_verdict98_0906/` и в
`tmp/pc_report/` — и больше нигде.

---

## ИСХОД ОДНОЙ СТРОКОЙ

**Кандидат `4aafbab6c14ef0ba78c460439a0c7d50734dfcb2` — КРАСНЫЙ: кейсов 14 из 16, чеков 295 из 300,
прогонов 2 из 2, полный набор, дерево чистое, вершина не сдвинулась (`head_moved` пуст),
«неизвестно» ноль. Круги СОВПАЛИ ПО 15 КЕЙСАМ ИЗ 16 — разошёлся кейс 10.**

**Слов «стало лучше» в этом артефакте нет, и это не скромность, а условие задания: круги
разошлись — значит сравнение не произносится.** Числа названы, читатель волен считать сам.

**Цена ДОЕХАЛА до ответа — это главный ответ захода.** Провалов с ценовым корнем сейчас **4 из 5**
против **26 из 26** в прошлый раз, и все четыре — один кейс 7, где продукт молчит НАМЕРЕННО.
Ожидание Штаба («22 уйдут, 4 останутся») сбылось ПОИМЁННО: ушли ровно те 22, остались ровно те 4.
Сверх ожидания появился **один** провал, не ценовой и не новый по классу (§5).

| | |
|---|---|
| КАНДИДАТ (назван первым делом, заморожен на входе) | `4aafbab6c14ef0ba78c460439a0c7d50734dfcb2` |
| чем отличается от прошлого кандидата | 8 коммитов поверх `c2c8aa8`, среди них `cdc794a` — починка бюджета проб сторожа свежести |
| вершина на выходе | та же, `head_moved` пуст |
| дерево на фиксации и на выходе | **чистое** (`git status --porcelain -uno` → 0 строк) |
| корпус | `trainer_cases.json`, 16 кейсов, `corpus_sha` `6d5d78f0d83acdca` — **тот же, что 05.09 и что у прошлого вердикта** |
| прибор | штатный `trainer_run.py --runs 2 --write`, поле `runner` = `trainer_run.py` |
| время прогона | 14:22:32 → 14:44:19 местного = **1312 с** |
| запись в реестре | `red["4aafbab"]`, `last` = она же, `when 2026-09-06 14:44:19` |

---

## 0. Признак сделанности — ПЕРВЫМ действием

Адрес результата открыт до всякой работы. За 06.09 в `docs/artifacts` лежало **20 файлов**. Слов
адреса — «вердикт набора **второй кандидат** 06.09» — не несёт ни один. Ближайший по теме,
`2026-09-06-вердикт-набора-кандидат-хеш-06.09.md`, отвечает на пункты 1–5 ПРОШЛОГО задания и на
ПРОШЛОМ кандидате `c2c8aa8`: там 7/16 и 274/300. На пункты этого задания он не отвечает ни одним
разделом. Заход шёл с нуля.

## 1. Премиса Штаба — сверена своими командами, разошлась в НУЛЕ мест

Читал боевой реестр `pc_orchestrator.client_trainer_green.json` **только на чтение**
(`_scratch_verdict98_0906/probe_zero.py`).

| п. | утверждение задания | замер | верно? |
|---|---|---|---|
| 1 | прошлый вердикт снят на `c2c8aa8`, красный, 7 из 16 | `red["c2c8aa8"]`: `result=red`, кейсов **7/16**, `when 2026-09-06 10:08:26` | **да** |
| 2 | 274 чека из 300 | в записи ровно `checks_passed 274 / checks_total 300` | **да** |
| 3 | два круга совпали 16 из 16 | `runs=2`; список из 26 провалов симметричен по кругам — каждому `N/1` отвечает `N/2` того же имени | **да** |
| 4 | из 26 провалов 22 несли инструкцию «цена недоступна», 4 — кейс 7 | 26 = кейс 7 (4 провала) + остальные 22 (кейсы 1, 2, 3, 8, 12, 13, 14, 16) | **да** |
| 5 | заход #94 починил бюджет проб: свой предел у пробы, общий бюджет с замера | `cdc794a` в дереве кандидата: `PRICE_GATE_ONE_SEC=43.24` заведён, `PRICE_GATE_PROBE_SEC` 90 → 256.4 | **да** |

Расхождений нет ни одного — в отличие от прошлого захода, где премиса отставала на один прогон.

## 2. Пункт ноль: заморозка держит — контрфакт, а не пересказ

Прежде чем звать прибор с `--write`, спрошен замок. Боевой реестр — только на чтение, поддельный
зелёный лежит своим файлом `_scratch_verdict98_0906/fake_green.json`.

| проба | что спрашивали | ответ 06.09 14:21 |
|---|---|---|
| З0 | флаг заморозки известен / стои́т | `True / True`, `pc_orchestrator.contour_frozen` |
| З1 | `freeze_holds_release()` | `True` + дословно «контур ЗАМОРОЖЕН … открыть может только поимённое „да“ владельца на этот коммит» |
| З3 | признаётся ли ПОЛНОЦЕННАЯ подделка зелёной ПО ФОРМЕ | да: «зелёный: кейсов 16/16, чеков 300/300, прогонов 2, коммит совпал» |
| **З4** | `release_reason` на этой подделке при ЖИВОЙ заморозке | **`None`** — записанный зелёный ворот НЕ открывает |
| **З5** | КОНТРФАКТ: та же подделка при СНЯТОМ флаге | **`'trainer'`** — держит ИМЕННО заморозка |
| З7 | `owner_approved(4aafbab)` | `False` |

Пара З4/З5 — контрфакт: снимаешь флаг — дорога открывается, ставишь обратно — закрыта. Писать
вердикт было можно, и выкатки это не порождает ни одной веткой (§6).

## 3. Арифметика ДО прогона — как требовал пункт 2

Считал ДО запуска, а не задним числом:

* цена прошлого прогона того же набора теми же двумя кругами — **1429 с** (замер 06.09 10:08);
* потолок захода — `PC_TASK_TIMEOUT = 2700 с` (`pc_orchestrator.py:132`);
* поправка на подорожавший сторож: бюджет проб поднят с 90 до **256.4 с**, вердикт сторожа
  кэшируется на `PRICE_GATE_TTL_MIN = 30 мин`, значит за прогон он платится **1–2 раза**;
  худшая добавка `(256.4 − 90) × 2 = +333 с`;
* оценка коридора: **1429…1762 с** против потолка 2700 — доля 0.53…0.65, порога остановки 0.7 не
  достигает. **Два круга влезают ⇒ гоню два, вердикт воротного качества возможен.**

**Замер вышел ДЕШЕВЛЕ оценки:** фактически **1312 с** (сумма покейсных 1305 с + шапка/запись), то
есть 41 с за кейсо-прогон против 44.7 у прошлого кандидата. Доля потолка — **0.49**. Медиана
кейсо-прогона **34 с**, минимум 25 с, **максимум 110 с — кейс 1 круг 1**: именно в нём платится
проба сторожа, дальше вердикт берётся из кэша (кейс 1 круг 2 — уже 36 с). Второй платный заход в
прогон не попал: 1312 с < 1800 с TTL.

## 4. ЧИСЛО ВЕРДИКТА и оба круга

```
ИТОГ: RED — кейсов 14/16, чеков 295/300, прогонов 2, дерево чистое
провалено: 7/1 строка J дословно; 7/1 цена цифрой; 7/2 строка J дословно; 7/2 цена цифрой;
           10/2 обязательное упоминание
```

| круг | зачтено кейсов | красных | «неизвестно» |
|---|---|---|---|
| 1 | **15** (все, кроме 7) | 1 (`7`) | **0** |
| 2 | **14** (все, кроме 7 и 10) | 2 (`7`, `10`) | **0** |

**Круги совпали по 15 кейсам из 16.** Разошёлся **кейс 10**: круг 1 — все чеки зелёные (46 с),
круг 2 — провален «обязательное упоминание» (38 с). Зачёт кейса требует зелени в КАЖДОМ круге,
поэтому в итог кейс 10 идёт красным, и **14/16 — это уже строгий счёт, а не оптимистичный**.

Знаменатель чеков **300 — полный**: 16 кейсов × 2 круга = 32 кейсо-прогона, все 32 судимы,
`unknown = 0`, ни один чек не пропал. Знаменатель тот же, что у прошлого вердикта (300), корпус
тот же (`6d5d78f0d83acdca`), прибор тот же — сравнение законно по всем трём переменным.

**Про качество вердикта честно.** Полный набор, все чеки, два круга из двух, дерево чистое,
вершина не уехала — по форме вердикт воротный. Но **круги разошлись на одном кейсе**, и задание
велело в этом случае не произносить сравнительной оценки. Не произношу: числа выше, вывод за
Штабом.

## 5. КОРЕНЬ КАЖДОГО ОСТАВШЕГОСЯ ПРОВАЛА и ПОИМЁННОЕ сравнение с прошлым разом

Провалов **5** (300 − 295) против 26 в прошлый раз. Разбор по именам чеков:

| имя чека | было (26) | стало (5) | кейсы сейчас |
|---|---|---|---|
| строка J дословно | 14 | **2** | 7 |
| цена цифрой | 8 | **2** | 7 |
| сетка прайса дословно | 4 | **0** | — |
| обязательное упоминание | 0 | **1** | 10 |

### УШЛИ — 22 из 26, поимённо

`1/1` и `1/2` (строка J + цена цифрой) · `2/1`, `2/2` (сетка прайса) · `3/1`, `3/2` (строка J) ·
`8/1`, `8/2` (строка J + цена цифрой) · `12/1`, `12/2` (строка J) · `13/1`, `13/2` (строка J +
цена цифрой) · `14/1`, `14/2` (сетка прайса) · `16/1`, `16/2` (строка J).

Это **ровно те 22**, которые несли инструкцию «точная цена из Календаря сейчас недоступна». Кейсы
1, 2, 3, 8, 12, 13, 14, 16 зелены теперь в ОБОИХ кругах. **Корень их смерти назван и он один:**
сторож свежести цены голодал по бюджету (девять проб делили 90 с при медиане круга 95.15 с) и
выбрасывал уже посчитанное число; заход #94 (`cdc794a`) дал каждой пробе свой предел 43.24 с и
поднял общий бюджет до 256.4 с. Прошлый заход назвал разрыв «у двери цена есть, в ответе нет» и
оставил исход НЕИЗВЕСТНО — сегодня этот разрыв закрыт замером: **цена доезжает до ответа**.

### ОСТАЛИСЬ — 4 из 26, поимённо и все в одном кейсе

`7/1 строка J дословно` · `7/1 цена цифрой` · `7/2 строка J дословно` · `7/2 цена цифрой`.

**Кейс 7 «месяц+ — кап месячного тарифа», срок `{when_month}` пересекает границу сезонов.**
Записанное правило цены считает период по дате НАЧАЛА, значит любое посчитанное число было бы
неверным, и третий исход границы сезонов (`season_gate`, подключён 06.09) велит НЕ называть
никакой цены. Продукт молчит ПРАВИЛЬНО; красным его делает чек корпуса, требующий названной цены.
**Это расхождение корпуса с решением владельца, а не дефект продукта** — и корпуса я не правлю:
задание прямо запретило («это замер, а не обучение»). Ровно те 4, которые Штаб и ждал.

### ПОЯВИЛСЯ — 1, и он не ценовой

`10/2 обязательное упоминание`. Кейс 10 «один день — назван минимальный срок, а не ошибка» ждёт в
ответе одну из формулировок `от 5 дней` / `5 дней` / `от 3`; в круге 2 голова её не сказала, в
круге 1 — сказала. **Это плавание головы, а не код:** тот же кейс тем же способом провалил один
круг из двух у кандидата `604a885` (05.09, замер прошлого артефакта, §4). Класс известен и не нов;
ценового корня у него нет, quote-блок здесь корпусом не требуется вовсе (`expect: {}`).

### ПУНКТ 5 ОТДЕЛЬНОЙ СТРОКОЙ

> **Провалов с корнем ЦЕНА: сейчас 4 из 5. Прошлый раз — 26 из 26.**

И оговорка, без которой строка врёт в свою пользу: оставшиеся 4 — это НЕ «цена не доехала», а
«цена намеренно не названа» (граница сезонов). Провалов класса «цена недоступна» сейчас **0**.

## 6. Дети НЕ выкачены, карточек про выкатку не порождено

Прибор напечатал своей строкой: «ворота ДЕРЖАТ коммит: зелёного вердикта нет (нужно „да“
владельца)». Вердикт красный, и красный ворот не открывает даже без заморозки; а под заморозкой их
не открыл бы и зелёный (контрфакт §2). Владелец про выкатку не спрошен, карточек не порождено,
наружу не ушло ни одного сообщения, процессов не тронуто ни одного, рестарта не звалось ни одной
веткой.

## 7. Записи прогона ложились на диск по ходу — как требовал пункт 6

Прогон шёл отдельным процессом с небуферизованным выводом (`python -u`) в
`_scratch_verdict98_0906/run.log`: строка на кейсо-прогон появлялась на диске сразу
(`run_corpus` → `log(...)` после каждого кейсо-прогона). Проверено по ходу дела трижды: 2 строки
на 14:24, 9 на 14:29, 26 на 14:39, 32 на 14:44. Цена таймаута тем самым — один кейс, а не весь
заход.

## 8. Чего этот заход НЕ доказывает

* **не про качество бота вообще** — мерена одна ось: текст ответа на 16 записанных фраз;
* **не про устойчивость кейса 10** — один разошедшийся круг говорит, что голова плавает, но не
  говорит, КАК ЧАСТО: двух кругов для частоты мало;
* **не про кейс 7** — корпус там расходится с новым решением владельца, и пока корпус не выправлен,
  зелёного набора не будет ни при какой починке кода;
* **не про доставку живому клиенту** — прогон безголовый, Telethon не поднимается, слой доставки
  (`userbot_listen.py`, `moderation_bot.py`, `moderation_core.py`) корпусом не задет вовсе;
* **не открывает ворота** — §6.

## 9. Где временное и что тронуто на диске

| путь | что |
|---|---|
| `_scratch_verdict98_0906/probe_zero.py`, `.out` | пункт ноль, контрфакт заморозки, сверка премисы |
| `_scratch_verdict98_0906/fake_green.json` | поддельный зелёный для проб (боевого не касался) |
| `_scratch_verdict98_0906/prev_failed.json` | 26 прошлых провалов, выписанных из боевого реестра |
| `_scratch_verdict98_0906/run.log`, `run.stamp`, `run_report.md` | прогон целиком, покейсно, и отчёт прибора |
| `_scratch_verdict98_0906/compare.py`, `compare.out` | поимённое сравнение с прошлым разом |
| `tmp/pc_report/task98-…-98.md` | черновик доклада (обновлялся по вехам) |

Изменён **один** боевой файл — реестр вердиктов `pc_orchestrator.client_trainer_green.json`: в него
штатным прибором добавлена запись `red["4aafbab"]`. Прежние записи целы (зелёная `74be777` и семь
красных, включая `c2c8aa8`, на месте). Ничего не удалено, процессы не трогались, `.env` и конфиги
не читались, лист «Календарь бронирования» не открывался, слепок `price_source.json` не
пересобирался ни байтом, правил, уроков и корпуса кейсов не менялось, детям ничего не выкатывалось.
