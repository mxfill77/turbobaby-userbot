# %s

**Тип:** замер (кода не менял) · **Полоса:** ПК · **Ось:** бизнес · **Дата:** 22.08.2026.

Тот же текст лёг новым разделом в узел карты `master` (добавлением в хвост). Сырые замеры: `_scratch_truenum_0822/` — `run_1.json`, `run_2.json`, `run_3.json` (по кейсу файл `case_NN_rM.json` пишется сразу после кейса), `flap_why.json`, `prove_text.json`, и отрицательный тест `_scratch_availfix_0822/negative.py`.

---

**Что сделано:** достоверное число тренажёра 22.08.2026 снято ТРЕМЯ живыми прогонами набора подряд на HEAD `c88d673` (корпус `98ad5e3e2c256542`); правка проверяющего наличия `92dd754` найдена в дереве, отрицательный тест перепрогнан 6 из 6, текст ответа бота не изменён ни на символ. Ворота не трогались, пороги не двигались.

### Три числа первой строкой

| | значение |
|---|---|
| **отрицательный тест** (фраза о свободных датах напечатана при ЗАНЯТОМ юните) | **ПРОЙДЕН, 6 вариантов из 6** — проверяющий краснеет; амнистии отрицанию, дефициту и спеццене правка не даёт |
| **три прогона набора** | прогон 1 — **11 из 12** · прогон 2 — **11 из 12** · прогон 3 — **11 из 12** |
| **покейсное совпадение трёх прогонов** | **НЕТ**, расходятся кейсы 5, 10 |

### Что показал каждый прогон

| прогон | прошло | провалились | секунд |
|---|---|---|---|
| 1 | **11 из 12** | 5 | 452.5 |
| 2 | **11 из 12** | 10 | 441.8 |
| 3 | **11 из 12** | 5 | 430.1 |

Цена замерена ДО работы на ОДНОМ прогоне: 452.5 с; три прогона = 1357.5 с = **0.503** потолка захода 2700 с (ниже порога 0.7, поэтому гнались все три, а не один).

### Покейсно: сколько раз кейс был зелёным из 3

| кейс | имя | зелёным из 3 | чем краснел | причина |
|---|---|---|---|---|
| 1 | первый контакт, одна модель | **3** | — | стабилен во всех трёх |
| 2 | прайс-интент «все модели и цены» | **3** | — | стабилен во всех трёх |
| 3 | модель + даты + гео-пин Раваи | **3** | — | стабилен во всех трёх |
| 4 | только про депозит — контрпример прайса | **3** | — | стабилен во всех трёх |
| 5 | новичок + мощный байк (A.3) | **1** | «обязательное упоминание» (прогон 1) и «без «опасн»» (прогон 3) — ДВА РАЗНЫХ чека | **ГОЛОВА.** Записка кода побайтно одна и та же (173 симв., sha `7ec5205b5d10`), черновики разные: зелёный сказал «вместе с **менеджером** подберём», красный — «вместе с **командой**»; в третьем голова сама написала «опасн», чего кейс запрещает |
| 6 | опыт не упомянут (A.2) | **3** | — | стабилен во всех трёх |
| 7 | месяц+ — кап месячного тарифа | **3** | — | стабилен во всех трёх |
| 8 | EN-клиент — язык ответа | **3** | — | стабилен во всех трёх |
| 9 | фото паспорта маркером — засчитано трекером | **3** | — | стабилен во всех трёх |
| 10 | один день — назван минимальный срок, а не ошибка | **2** | «нет утверждений о наличии» (прогон 2) | **ИСТОЧНИК ВНУТРИ КОДА, не сеть.** Записка кейса 10 несёт «свободен на эти даты» ИНЛАЙНОМ, без блока `<<<QUOTE>>>`, поэтому `_quote_block_from_note` отдаёт пустую строку и правка 92dd754 даёт проверяющему `None` — во всех трёх прогонах. Красным это становится ровно тогда, когда голова повторит «свободен» (прогон 2); в 1 и 3 она не повторила, и зелёное там — везение, а не правота |
| 11 | «есть ли в наличии?» без данных Bridge | **3** | — | стабилен во всех трёх |
| 12 | тайский в реплике — strip_thai отработал, ฿ сохранён | **3** | «строка J дословно» (сидение 04:18) | **ЖИВАЯ СЕТЬ / СОСТОЯНИЕ ДАННЫХ.** Записка красного прогона: «точная цена из Календаря сейчас недоступна» против полного quote-блока в зелёных — тот же вход, разный ответ Календаря-моста |

Кейс 12 в этом сидении зелёный трижды, но краснел в сидении 04:18 того же дня на ТОМ ЖЕ корпусе и ТОМ ЖЕ коде — поэтому он в списке флапающих, а не в стабильных.

### Остаточный дефект той же границы, найденный этими прогонами

Правка `92dd754` научила проверяющего брать наличие из quote-БЛОКА записки. Но у кода ДВА носителя наличия, а закрыт один: ветка «скутеры сдаём от N дней» пишет «свободен на эти даты» ИНЛАЙНОМ, блока `<<<QUOTE>>>` в такой записке нет вовсе (замер `carrier_probe.py`: у кейса 10 во всех трёх прогонах `j_line=''`, `avail=None`, при том что записка несёт «свободен»). Значит на этой ветке проверяющий по-прежнему краснит СОБСТВЕННУЮ строку продукта — тот же класс, что чинили, просто вторым носителем. Правка от этого не становится неверной: она fail-closed и на этой ветке ведёт себя ровно как до неё.

**Число НЕ названо: три прогона разошлись покейсно.** Флапают кейсы 5, 10 — «стало лучше» здесь писать нельзя, потому что счёт зависит от того, какой прогон взять.

### Второй ключ к воротам: НЕТ

Основанием нынешний результат быть не может, и причин две — независимых:

1. **Флап.** Между сидениями 04:18 и 11:16 того же дня, на ОДНОМ корпусе и ОДНОМ коде, набор красных сменился целиком. Порог ворот требует ВСЕХ 12 кейсов зелёными в КАЖДОМ прогоне (`client_contour.TRAINER_MIN_CASES = 12`, `cases == cases_total`); набор, у которого красный кейс переезжает от прогона к прогону, такого дать не может.
2. **Числа не хватает и без флапа.** Лучший из прогонов — 11 из 12, а нужно 12 из 12.

Вердикт ворот `pc_orchestrator.client_trainer_green.json` не переписывался ни одной строкой: в нём по-прежнему `green: {}` и июльская красная запись `55aad22`. Прогоны гнались своим раннером, который `write_verdict` не импортирует.

### Правка проверяющего: чем кончилась

Заход id=33 22.08 упал по таймауту (`CLAIM 08:25:05 UTC` → `ТАЙМАУТ 2700s` 09:10:05 UTC), и его черновик доклада остался на словах «коммита ещё нет». **Черновик протух: коммит `92dd754` лёг 09:08:02 UTC — за 2 минуты 03 секунды до потолка.** Строка проверяющего сейчас берёт ЗНАЧЕНИЕ доступности из quote-блока эталона вместо литерала `None`; диff — 1 ханк, 2 строки кода, обе внутри `_smoke_checks` (функция ВЕРДИКТА, генерация её не зовёт: 0 вхождений в `generate_draft`).

**Текст ответа бота не изменился ни на символ:** sha256 клиентского текста совпал в 14 файлах из 14 (12 кейсов), расхождений 0 — тремя независимыми замерами (до правки, на текущем коде, пересчёт прямо из файлов черновиков).

После коммита правки ворота контура остановили реконсиляцию детей РОВНО ДВАЖДЫ (16:10:11 → `92dd754`, отказ №5; 16:43:55 → `c88d673`, отказ №6) — обе «контур заморожен, повтор той же формы → в ленту, владельца не спрашиваю». Дети остались на прежнем коммите, клиентам не писалось.

### Что НЕ делалось

Чек не отключался, пороги не ослаблялись, кейсы и ожидания не правились, текст ответа бота не трогался, ворота не открывались, дети не обновлялись, клиентам не писалось, в файл секретов и конфиги не ходилось, ничего не удалялось (в т.ч. за собой), прежние разделы узла не трогались — вставка только добавлением в хвост. Временное — в явно названном `_scratch_truenum_0822/`.
