# Разведка заявки внешнего канала: 2026-09-08-разведка-zayavka-67e601b19f33

Метка ступени E: **2026-09-08-разведка-zayavka-67e601b19f33**

Полоса: ПК · дата: 2026-09-08 · вид: РАЗВЕДКА (только чтение, счёт и этот файл).
Повод: заявка очереди **#208**, вид **УПРОЩАЕМО**, источников 1, ключ находки
`67e601b19f33`, канал **codex**.

Предмет проверки — МНЕНИЕ внешнего канала. Ни одной строки оттуда не исполнено;
боевого кода эта разведка не правит и правкой не заканчивается.

## ЧТО ПРОВЕРЯЛОСЬ

Задание ступени E спросило три вещи:

1. жива ли посылка находки **СЕГОДНЯ** по адресу `pc_orchestrator.py:10908`;
2. что именно в коде полосы этой посылке отвечает;
3. чего находка **НЕ учитывает**.

Тексты, по которым судим (прочитаны целиком):

* `docs/review_inbox/2026-09-08-2026-09-08-chain-pc-2026-09-08-207-codex.md` — ответ канала;
* `docs/review_outbox/2026-09-08-chain-pc-2026-09-08-207.md` — пакет, на который канал отвечал;
* `docs/review_receipts/pc-2026-09-08-207.json` — расписка цепочки (в ней лежит текст задания);
* `docs/artifacts/2026-09-08-operational_change-расписка-154.md` — артефакт захода 207 (238 строк);
* `pc_orchestrator.py:10888–10909`, `review_auto.py:237–260, 310–355, 743–848`,
  `review_intake.py:100–116, 357–392, 442–480` — сам названный адрес и машина, которая его выбрала.

Дословный текст находки #1 — то, что стало заявкой #208:

> 1. Упрощаемо: для ответа по задаче 154 достаточно связки «дословное ограничение
> задания → список собственных коммитов → затронутые ими пути → точное определение
> `operational_change`». Массовый разбор 229 расписок и семи соседних случаев не
> нужен, чтобы установить исход (а)/(б)/(в) именно для 154.

## ВЕРДИКТ ОДНОЙ ФРАЗОЙ

**Посылка ЖИВА, и — в отличие от прецедента #167 — якорь здесь ПРЕДМЕТНЫЙ, а не
случайный.** Слово `operational_change` в живом дереве есть, и стои́т оно по
названному адресу ровно один раз (замер А2). Повод не ложный ни в одной части.

Но **адрес указывает не туда, куда находка смотрит**: `pc_orchestrator.py:10908` —
это строка **лога** демона, ПОТРЕБИТЕЛЬ признака. «Точное определение», которого
находка требует дословно, живёт по другому адресу — `review_auto.py:310`, и в
пакете, который канал читал, его **не было** (замер Д).

Сама находка **верна в узком логическом смысле и не является суждением о работе**:
приговор по задаче 154 действительно стои́т на п.1–п.3 артефакта, а п.4 его не
меняет. Однако канал объявил «не нужным» ровно тот раздел, который **видел**, и
назвал «достаточным» ровно то, чего **не видел**: до него доехали 78 строк из 238
(32.8 %) и 1954 знака задания из 3645 (53.6 %). Механизм отбора воспроизведён по
коду построчно и объясняет обе цифры без остатка (замер Ж).

## ЗАМЕРЫ (число + команда рядом)

Корпус в замерах А–Д тот же, каким пользуется проба премисы
(`review_intake_run.tracked_files` → `git ls-files`, расширения
`.py .md .json .jsonl .txt .gs`), поэтому числа сопоставимы с живым ходом заявки.

**А. Якорь `operational_change` — где он в дереве**

```
git grep -l -F "operational_change" -- '*.py' '*.md' '*.json' '*.jsonl' '*.txt' '*.gs' | wc -l
```
→ **29 файлов**. Из них живых — **4** (`pc_orchestrator.py`, `review_auto.py`,
`review_auto_run.py`, `test_review_auto.py`); остальные 25 лежат под
`docs/artifacts`, `docs/review_outbox`, `docs/review_receipts` — все три каталога
входят в `review_intake.FROZEN_DIRS` (`review_intake.py:103–104`).

**А2. Точная строка по адресу премисы**

```
git grep -n -F "operational_change" -- pc_orchestrator.py
```
→ **ровно 1 совпадение**:
`pc_orchestrator.py:10908:                 "да" if rec["operational_change"] else "нет", rec["change_reason"])`

Строка жива сегодня, стои́т в теле `_review_auto_note` (`pc_orchestrator.py:10888`)
и является **аргументом `log.info`** — то есть печатью уже готового признака.

**Б. Сколько раз слово встречается в каждом живом файле**

```
git grep -c -F "operational_change" -- '*.py'
```
→ `pc_orchestrator.py:1` · `review_auto.py:9` · `review_auto_run.py:2` ·
`test_review_auto.py:13`. **Всего 25 живых вхождений; адрес премисы называет 1 из
25 — единственное, которое не определяет признак и не проверяет его.**

**В. Второй якорь находки (длинная ёлочка)**

```
git grep -c -F "дословное ограничение задания" -- '*.py' '*.md' '*.json' '*.jsonl' '*.txt' '*.gs'
```
→ **0 файлов** (вывод пуст). Из двух якорей находки живым подтверждён один.

**Г. Клиентский контур**

```
git grep -c -F "operational_change" -- userbot_listen.py moderation_bot.py moderation_core.py suggest.py
```
→ **0**. Адрес премисы вне клиентского контура — ровно то, чем `zayavki_pc`
объяснил исход заявки («предмет клиенту не виден»).

**Д. Где живёт «точное определение», которого требует находка**

```
git grep -n -F "def operational_change" -- '*.py'
```
→ `review_auto.py:310`. Функция из 14 строк, три исхода
(`commit_verified` / `commit_unverified` / `no_commit_claimed`). Файла
`review_auto.py` **в манифесте пакета 207 нет** — приложены только расписка
`docs/review_receipts/pc-2026-09-08-207.json` и артефакт (таблица «ИСТОЧНИКИ В
ПАКЕТЕ», 2 строки). Ревьюер требовал определение, не имея его перед глазами.

**Е. Сколько задание доехало до ревьюера**

```
grep -o 'ПРОВЕРИТЬ ПЕРВЫМ ДЕЙСТВИЕМ[^"]*' docs/review_receipts/pc-2026-09-08-207.json
```
→ `…открыть саму задачу 154 в оче [ОБРЕЗАНО: показано 1954 знаков из 3645]`.

**1691 знак задания (46.4 %) не доехал**, и обрыв пришёлся ровно на фразу
«ПРОВЕРИТЬ ПЕРВЫМ ДЕЙСТВИЕМ» — то есть на перечень предписанных действий.
Обрезает `review_auto.sanitize` по потолку `HYPOTHESIS_TEXT_MAX = 2000`
(`review_pack.py:112`): лестница `task_text_for_pack` (`review_auto.py:237–260`)
ищет раздел «ЧТО СДЕЛАТЬ», а у заданий из ящика Штаба его нет вовсе — значит
работает третья, слепая ступень.

Класс не единичный:

```
grep -l "ОБРЕЗАНО" docs/review_receipts/pc-*.json | wc -l   → 95
ls docs/review_receipts/pc-*.json | wc -l                   → 230
```
→ **95 расписок из 230 (41.3 %)** несут обрезанное задание.

**Ж. Сколько артефакта доехало и ПОЧЕМУ именно эти разделы**

```
grep -n "^## " docs/artifacts/2026-09-08-operational_change-расписка-154.md
wc -l  docs/artifacts/2026-09-08-operational_change-расписка-154.md   → 238
```

Пакет сам называет пропуск: «показано 78 из 238 строк, не вошло 160: 1–10 (10),
24–121 (98), 171–209 (39), 217–229 (13)».

Отбор делает `review_auto.plan_artifact_excerpt` (`review_auto.py:808–848`):
разделы берутся по весу `_SECTION_WEIGHTS` (`review_auto.py:752–758`), порядок —
`(-вес, начало)`. Проставляю веса вручную по тем же регуляркам:

| раздел | строки | слово веса | вес | доехал |
|---|---|---|---|---|
| `# Врёт ли расписка…` (шапка-заголовок) | 1–10 | — | **0** | нет |
| `## п.0 Признак сделанности…` | 11–23 | `сделанност` | 4 | **да** |
| `## п.1 Задача 154 поднята. Дословно…` | 24–70 | — | **0** | нет |
| `## п.2 Что признак значит ПО КОДУ…` | 71–107 | — | **0** | нет |
| `## п.3 Приговор` | 108–121 | — | **0** | нет |
| `## п.4 Сколько ещё задач несут признак при задании…` | 122–170 | `задани` | 5 | **да** |
| `## п.5 Второе дело: вырождение колонки хешей…` | 171–191 | — | **0** | нет |
| `## п.6 Чего заход НЕ проверял — и почему` | 192–209 | `провер` | 1 | нет |
| `## п.7 Арифметика бюджета (как требовало задание)` | 210–216 | `задание` | 5 | **да** |
| `## Замеры и команды (число + команда рядом)` | 217–229 | `замер`,`числ` | 1 | нет |
| `## Итог одной строкой` | 230–238 | `итог` | 3 | **да** |

Сумма взятого: 49 (п.4) + 7 (п.7) + 13 (п.0) + 9 (Итог) = **78 строк** —
посимвольно то число, которое напечатал пакет. **Реконструкция сошлась.**

**Ж2. Мина, найденная этим счётом: раздел «Приговор» весит НОЛЬ**

В списке `_SECTION_WEIGHTS` нет ни слова «приговор», ни «решение», ни «исход».
Поэтому раздел, который прямо отвечает на вопрос задания («исходов ровно три…»),
получает вес 0 и вылетает **первым**. Вместе с ним вылетают п.1 (дословное
задание и пути коммитов) и п.2 (определение признака) — то есть **все три
раздела, из которых состоит «достаточная связка», названная находкой**.

**Ж3. Ветка «шапка всегда первой» на этой полосе МЕРТВА**

`_PREAMBLE_WEIGHT = 6` (`review_auto.py:759`) с комментарием «шапка до первого
заголовка: имя, дата, FACT — всегда первой» применяется только при условии
`heads[0][0] > 1` (`review_auto.py:783`) — то есть если первая строка файла **не**
заголовок. Замер:

```
for f in docs/artifacts/*.md; do head -1 "$f" | grep -q '^ \{0,3\}#\{1,6\} ' && ...; done
```
→ артефактов **638**, первая строка — markdown-заголовок у **637** (99.8 %).

Значит у 637 артефактов полосы шапка веса 6 не получает никогда: её оценивают как
обычный раздел по словам заголовка. У артефакта 207 заголовок — прозаическая
фраза без единого слова из списка → вес 0 → **строка-маркер адреса
`operational_change РАСПИСКА-154-0809`, дата и рамка захода наружу не уехали.**

**З. Насколько широк класс «ревьюер судит по выборке»**

```
grep -l "не вошло" docs/review_outbox/*.md | wc -l   → 34
ls docs/review_outbox/*.md | wc -l                   → 123
```
→ **34 пакета из 123 (27.6 %)** уехали с урезанным источником. Каждый четвёртый
ответ канала получен по неполному тексту, и пакет об этом честно предупреждает
строкой — но ревьюер, как видно здесь, предупреждение не применяет.

**И. Лоток заявок целиком (для масштаба)**

```
grep -c '^  "[0-9a-f]\{12\}": {' zayavki_pc_state.json   → 219
grep -o '"kind": "[^"]*"'    zayavki_pc_state.json | sort | uniq -c
grep -o '"outcome": "[^"]*"' zayavki_pc_state.json | sort | uniq -c
```
→ заявок **219**: `УПРОЩАЕМО` **96**, `ПЕРЕУСЛОЖНЕНО` 62, `НЕ ДЕЛАТЬ ВОВСЕ` 61.
Исход `снято отбором` стои́т у **193**; у остальных **26** поля `outcome` в срезе
нет — что это значит, здесь **не выяснялось** (третий исход, см. «НЕИЗВЕСТНО»).

Ряд заявки #208 в срезе (дословно):

```json
"67e601b19f33": {"at_iso": "2026-09-08T02:51:35.476094Z", "kind": "УПРОЩАЕМО",
 "outcome": "снято отбором", "queue_id": 208,
 "why": "отбор не дал карточку: предмет клиенту не виден: …"}
```

## КАК ПРЕМИСА СТАЛА «ЖИВОЙ» — ВОССТАНОВЛЕНО ПО КОДУ

1. `review_intake.anchors()` (`review_intake.py:357–392`) берёт две формы и именно
   в этом порядке: сначала обратные кавычки (`_RE_BACKTICK`), потом ёлочки
   (`_RE_QUOTED`). Из пункта #1 выходит ровно два якоря: `operational_change` и
   длинная фраза «дословное ограничение задания → …».
2. Ёлочка проходит порог `PHRASE_MIN_WORDS = 2` с огромным запасом — но в дереве
   её нет ни разу (замер В), проба даёт `found=False`.
3. `review_intake_run.probe_anchors()` (`review_intake_run.py:246`) идёт по корпусу,
   отсортированному `sorted(files, key=lambda rel: (frozen_address(rel), rel))` —
   живые раньше замороженных, дальше по имени. Среди живых носителей
   `operational_change` алфавитно первый — `pc_orchestrator.py` (`p` < `r` < `t`),
   и вхождение в нём единственное — строка **10908**.
4. `review_intake.premise()` (`review_intake.py:461–465`) берёт `live[0]` и
   объявляет **ЖИВА**, адрес `pc_orchestrator.py:10908`.

**Реконструкция подтверждена совпадением:** адрес, вычисленный по коду, посимвольно
равен адресу из постановки задачи. Живой ряд очереди #208 при этом **не читался**
(запрет разведки на живые таблицы) — и адрес, и тело заявки взяты из файла
состояния на диске и из постановки.

Отличие от прецедента `2026-09-07-разведка-zayavka-e417ff43967b` назвать обязан:
там «ЖИВА» дал ПОБОЧНЫЙ якорь (двухсловная интонация в чужом комментарии), здесь —
**предметный**. Класс «двухсловной ёлочки» этой заявкой не подтверждается; это
другой класс — **алфавитный выбор адреса среди 25 равноправных вхождений**.

## ЧТО В КОДЕ ПОЛОСЫ ЭТОЙ НАХОДКЕ ОТВЕЧАЕТ

Находка называет достаточной связку из четырёх вещей. Отвечают ей **не** та строка,
по которой посылка признана живой, а вот эти:

* **«дословное ограничение задания»** — `review_auto.receipt()` кладёт его в поле
  `hypothesis` через `sanitize(task_text_for_pack(...))` (`review_auto.py:348`), и
  ровно здесь оно теряет 46 % длины (замер Е). Артефакт захода 207 привёл его
  дословно в п.1 — разделе с весом 0.
* **«список собственных коммитов» и «затронутые ими пути»** — п.1 артефакта,
  таблица `git show --stat` по `8d559cb` и `ea19aca`: два коммита, по одному файлу
  каждый, оба под `docs/`, кода ноль строк. Раздел веса 0, наружу не уехал.
* **«точное определение `operational_change`»** — `review_auto.py:310–323`, три
  исхода; проверка коммита — `review_auto_run.verify_commits` (`git cat-file -e
  <sha>^{commit}`), то есть **существование объекта в дереве, без автора,
  без времени и без содержимого**. Пакет говорит это про себя вслух в СВОДКЕ:
  «коммит подтверждён только фактом существования в дереве — не содержанием».
  Раздел п.2 с этой цитатой — вес 0, наружу не уехал.
* **«исход (а)/(б)/(в) именно для 154»** — раздел `## п.3 Приговор`: исход **(в)**,
  противоречия нет. Вес 0, наружу не уехал.

То есть **все четыре пункта «достаточной связки» в работе уже были, и ни одного из
них ревьюер не видел.**

## ЧЕГО НАХОДКА НЕ УЧИТЫВАЕТ

1. **Она не видела 67 % артефакта и 46 % задания** (замеры Е, Ж) и судит о
   необходимости работы по 78 строкам из 238. Пакет об этом предупредил в разделе
   «ПРОПУЩЕНО ВНУТРИ ИСТОЧНИКОВ» и перечислил заголовки — предупреждение в ответе
   канала не отражено ни словом.
2. **Разбор 229 расписок предписан заданием, а не выбран исполнителем.** Дословно
   из задания (расписка 207, поле `hypothesis`): «ЦЕЛЬ: решение с доказательством —
   **врёт ли наша собственная расписка** о том, что произошло», и отдельно:
   «Вторым, дешёвым делом — числом: **во скольких пакетах** внешнему колонка хешей
   вырождена». Цель поставлена о **приборе**, а не только о строке 154; массовый
   срез — прямой ответ на неё. Находка судит исполнителя за послушание заданию,
   которого не видела.
3. **Ровно этот срез и дал единственный НОВЫЙ факт захода.** П.4 нашёл случай
   `pc-2026-09-02-114`, где отчёт **прямым словом называет коммит чужим**
   («последний коммит по нему чужой (`ef7d6ac`)»), а расписка засчитывает его как
   собственное операционное изменение. Это подтверждённый дефект прибора — то, о
   чём спрашивала ЦЕЛЬ. Убрать п.4 значило бы закрыть заход без ответа на неё.
4. **Пункт 2 того же ответа канала («не классифицировать соседние семь случаев»)
   запрещает довести этот факт до имени.** Вместе пункты 1 и 2 закрывают
   единственную найденную дырку прибора — сначала как «лишнюю», потом как «новую
   находку». Это не вывод из текста, а следствие того, что ревьюер видел п.4
   числом, а не разбором.
5. **Внутреннее натяжение самого ответа.** Пункт 1 зовёт срез ненужным, пункт 3
   того же ответа жалуется, что «методика и сами 121 пакета не приложены» и
   достоверность 31/121 и семи случаев «неизвестно». Оба верны по отдельности, но
   причина у обоих одна и та же — **потолок пакета в 15000 знаков и выборка по
   весам**, а не выбор исполнителя. Ревьюер просит одновременно меньше работы и
   больше доказательств.
6. **«Точного определения» у ревьюера не было** (замер Д): `review_auto.py` в
   манифест не входил. Требование «достаточно точного определения» адресовано
   тексту, которого в пакете нет.

## ЧТО ПОВОД ЗАКРЫВАЕТ, ЧТО ОСТАЁТСЯ, ЧТО НЕИЗВЕСТНО

**ЗАКРЫТО (проверено замером):**

* адрес `pc_orchestrator.py:10908` жив сегодня, вхождение единственное (замер А2);
* якорь **предметный** — класс «двухсловной ёлочки» (#167) здесь ни при чём;
* адрес указывает на **лог**, а не на определение; определение — `review_auto.py:310`,
  и в пакете его не было (замеры Б, Д);
* заявка **не клиентский дефект**: адрес вне клиентского контура (замер Г) — это
  и есть записанный `why` заявки #208, ряд остаётся открытым, текст в лотке цел;
* **узкое утверждение находки верно**: приговор по 154 стои́т на п.1–п.3, п.4 его
  не меняет — что артефакт 207 и говорит сам («задача 154 в этот класс не входит»);
* **как суждение о работе находка не стои́т**: она объявила ненужным то, что видела
  (п.4), и достаточным то, чего не видела (п.1–п.3). Механизм воспроизведён
  построчно и сошёлся по числу 78 (замер Ж).

**ОСТАЁТСЯ ОТКРЫТЫМ (решает человек, разведка не правит):**

* раздел «Приговор» весит **0** — прямой ответ на вопрос задания вылетает первым
  (замер Ж2);
* ветка «шапка всегда первой» мертва для **637 артефактов из 638** (замер Ж3):
  маркер адреса результата, дата и FACT наружу не уезжают;
* **95 расписок из 230** несут задание, обрезанное слепой ступенью (замер Е);
* **34 пакета из 123** уехали с урезанным источником (замер З) — сколько ответов
  каналов испорчено этим, здесь **не считалось**;
* дефект прибора `pc-2026-09-02-114` (чужой коммит засчитан своим) назван заходом
  207 и по-прежнему открыт; имя `operational_change` шире, чем измерение.

**НЕИЗВЕСТНО (третий исход, законный):**

* дословное тело живого ряда очереди #208 — живую очередь не дёргал ни разу
  (запрет разведки); адрес взят из постановки и совпал с расчётным;
* что означают **26 записей лотка без поля `outcome`** (замер И) — открыты они,
  сняты иначе или это другая форма ряда, здесь не выяснялось;
* какое `max_lines` сборщик выдал артефакту 207: сумма взятых разделов даёт ровно
  78, но было ли 78 самим бюджетом или остаток был меньше
  `ARTIFACT_MIN_SECTION_LINES = 6`, по файлам не различить;
* видел ли владелец заявку #208 и что решил — состояние очереди не читалось.

## ПРАВКИ, НАЗВАННЫЕ СЛОВАМИ (не сделаны; решение владельца)

Ни одна не применена — разведка кончается этим файлом. Сборщик пакетов к тому же
лежит в клиентском замыкании, замороженном словом владельца.

1. **Добавить в `_SECTION_WEIGHTS` слова приговора.** Сейчас `приговор`, `решение`,
   `исход`, `вердикт` не весят ничего, и раздел с ответом на вопрос задания
   вылетает раньше всех. Одна регулярка в весе 3 закрыла бы класс целиком.
2. **Починить ветку шапки: считать шапкой первые строки и тогда, когда файл
   начинается заголовком** (`review_auto.py:783`). Сейчас условие `heads[0][0] > 1`
   делает `_PREAMBLE_WEIGHT = 6` мёртвым для 637 артефактов из 638 — то есть имя,
   дата, маркер адреса и FACT систематически не доезжают до ревьюера.
3. **Обрезка задания обязана резать с ХВОСТА, а не по слепому потолку.** Лестница
   `task_text_for_pack` умеет отдавать голову «до ЧТО СДЕЛАТЬ», но задания из ящика
   Штаба этого раздела не имеют вовсе — и падают на третью ступень. Признать
   «ЦЕЛЬ … ЗАПРЕТЫ …» вторым разделителем стоило бы одной регулярки и вернуло бы
   ревьюеру перечень предписанных действий (замер Е: 95 расписок из 230).
4. **Премиса обязана называть, СКОЛЬКО живых вхождений у якоря и какое выбрано.**
   Сейчас `premise()` отдаёт первый по алфавиту адрес и молчит о том, что их 25.
   Одно поле («живых вхождений 25, показан первый по имени файла») сняло бы весь
   этот заход: адрес лога вместо адреса определения был бы виден сразу.
5. **Имя `operational_change` шире измерения** — остаток, названный ещё заходом 207
   и здесь только подтверждаемый: либо сузить имя до `commit_in_tree`, либо мерить
   пути коммита, а не факт его существования. К этому же примыкает открытый случай
   `pc-2026-09-02-114`.

## ЗАПРЕТЫ И ГРАНИЦЫ ЗАХОДА

Ничего не удалено. Операционного состояния не тронуто: процессов, живых таблиц,
очереди, денег, детей контура и выкаток не касался. Клиентский контур не трогал.
`.env` и конфиги не читал. Боевой код не правлен ни одной строкой. Пробных скриптов
не запускал — все числа получены `git grep`, `grep`, `wc` и одним циклом `for` по
дереву. Красного встретил один раз: инлайн `python -c` даёт карточку записи в БД
(известный класс полосы) — обхода не искал, пересчитал теми же данными через
`grep`/`git grep`.

Временное этого захода: `tmp/pc_report/task210-9032-1788836025935-210.md` (черновик
доклада, вне git).
