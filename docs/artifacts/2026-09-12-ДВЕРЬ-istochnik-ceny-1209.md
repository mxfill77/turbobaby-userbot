# ДВЕРЬ istochnik-ceny 1209 — цена клиенту приходит из живой двери, а не из счёта по файлу

**Задание** 61-a Штаба из ящика (ключ `61-a-dver-istochnik-ceny.1209`), полоса ПК.
**Дата в имени файла — дата ЗАДАНИЯ**, как назвал адрес результата; сам заход отработан
**21.09.2026** (14:36–15:05 местного, +07). Временное место захода объявлено:
`_scratch_dver_istochnik_2109/` — ничего не удалялось.

**Признак сделанности, первым действием:** артефакта по этому адресу НЕ БЫЛО вовсе (в
`docs/artifacts` за 12.09 двадцать семь файлов, ни одного со словами адреса), а вот **продукт
пунктов 1–5 в коде УЖЕ ЛЕЖАЛ** — коммит `fb8e8ab0` от 13.09.2026 00:39 +07 и доводка
`d213e7a8`. Поэтому заход не переписывал сделанное, а **проверил его живым фактом и закрыл
недостающее: обратное чтение узла, живой замер секунд (п.6) и этот артефакт (п.8)**.

---

## 0. ГЛАВНОЕ ОДНОЙ ТАБЛИЦЕЙ

| пункт задания | состояние | чем доказано |
|---|---|---|
| 1 · решение владельца в `business_rules` | **ЕСТЬ**, прочитано назад 21.09 | блок **3940 символов, 51 строка**; узел 96300 симв., sha256 `b4171f41bff8efb3` |
| 2 · цена клиенту = число двери | **ЕСТЬ** | `door_price.client_quote` возвращает ТОТ ЖЕ объект (`assertIs`); `reprice` из клиентских веток убран в обеих дорогах |
| 3 · третий исход, дверь молчит | **ЕСТЬ** | `KIND_DOOR_NO_PRICE` + `HUMAN_LINE`; белый список пост-чека на этой ветке **пуст** |
| 4 · прибор расхождения | **ЕСТЬ и СРАБОТАЛ НА ЖИВЫХ ЧИСЛАХ** | три строки `ЦЕНА РАСХОЖДЕНИЕ` на живом ответе двери 21.09 (§4) |
| 5 · три отрицательных теста | **ЕСТЬ**, зелёные | `test_door_price.py` — **20 тестов OK** |
| 6 · замер цены вызова | **ЗАМЕРЕНО СЕГОДНЯ** | один вызов **3.18 / 4.10 / 4.19 с**; три модели **13.93 с** < 30 с — **укладываемся** |
| 7 · только задетые тесты | выполнено | 4 набора, **123 теста**, полного гейта не запускалось |
| 8 · артефакт | этот файл | пишется последним действием |

---

## 1. КАНОН: РЕШЕНИЕ ВЛАДЕЛЬЦА В УЗЛЕ `business_rules` — ПРОЧИТАНО НАЗАД

Блок **лежит** и прочитан живьём 21.09.2026 отдельным читающим прибором
(`_scratch_dver_istochnik_2109/read_block.py`, только чтение, секреты берёт сам `brain_writer`).

| мера | значение |
|---|---|
| узел целиком | **96300 символов**, sha256 `b4171f41bff8efb3` |
| **длина прочитанного назад блока** | **3940 символов, 51 строка** |
| первая строка блока | `РЕШЕНИЕ ВЛАДЕЛЬЦА 12.09.2026 — ЦЕНУ КЛИЕНТУ НАЗЫВАЕТ ЖИВОЙ ЛИСТ (ДВЕРЬ-ИСТОЧНИК-1209)` |
| последняя строка блока | `tmp/brain_backup_*.txt на ПК — писатель кладёт его сам перед каждой записью.` |
| вхождений ключа `ДВЕРЬ-ИСТОЧНИК-1209` в узле | **2** (заголовок блока и строка отката) |
| слово владельца дословно внутри | **ДА** — «можно ориентироваться на этот лист» |
| второе слово владельца (CLICK 125) | **ДА** — «нет, мы его не сдаём пока что» |

Блок держит три части, названные в нём самом: **слово владельца дословно** (обе цитаты),
**что из него следует без додумывания** (источник цены — живой лист; это поправка к решению
17.08, и прежний раздел НЕ удалён; дверь молчит — бот не считает сам; CLICK 125 не сдаётся),
и **чего слова не решают** (что делать с расхождением файла и листа, сезонная слепота листа —
это прочтение Штаба, а не решение владельца). Снимок прочитанного:
`_scratch_dver_istochnik_2109/block.txt`.

Узел в этом заходе **НЕ правился ни одним знаком**: запись состоялась 12.09, здесь только
обратное чтение.

---

## 2. ЦЕНА КЛИЕНТУ = ЧИСЛО ДВЕРИ. ПРАВКИ ПОИМЁННО

Носитель решения — **`door_price.py`, 266 строк, новый файл** (коммит `fb8e8ab0`). Правки:

| место | что сделано |
|---|---|
| `door_price.client_quote` (`door_price.py:239`) | **единственная точка**, через которую цена двери идёт клиенту. Дверь дала цену → возвращается **ТОТ ЖЕ объект `res`**, по ссылке; ветки, пишущей в котировку число из файла, в функции НЕ СУЩЕСТВУЕТ |
| `suggest._safe_quote_for_model` (`suggest.py:3695`) | вместо `price_source.reprice(...)` зовётся `door_price.client_quote(...)`. Это единственная точка точечной котировки — через неё идут обе ветки `_resolve_model_price` (штатная и «минимальный срок») |
| `suggest.py:3697-3701` | отказ самой ветки источника = **третий исход**, а не старое число: `except` отдаёт `status='error'` + `noquote` |
| `suggest._SHEET_RULE_OFF` (`suggest.py:4177`, читается на `4271`) | **знак ручки перевёрнут**: по умолчанию счёт по файлу в ПРАЙС-СЕТКУ ПАРКА больше не идёт (сетка — такой же ответ клиенту, как точечная цена). Возврат — словом `PRICE_SHEET_RULE_ON=1`, без коммита |
| `price_source.reprice` | **НЕ УДАЛЁН** и остаётся на месте: файл живёт для внутренних прикидок и для прибора расхождения |

Файл `price_source.json` остался, счёт по нему работает — но **в клиентский ответ его число не
попадает ни одной веткой**: обе дороги (точечная котировка и прайс-сетка) берут число у двери.

---

## 3. ТРЕТИЙ ИСХОД: ДВЕРЬ МОЛЧИТ — СЧИТАЕТ ЧЕЛОВЕК

| место | что сделано |
|---|---|
| `door_price.KIND_DOOR_NO_PRICE` (`door_price.py:70`) | имя состояния «двери нечего сказать о цене». Прежние читатели видят прежние `status='error'`/`quote=None` и ведут себя побайтно как раньше |
| `door_price.has_price` (`door_price.py:102`) | меряет **ПОЛЯ, а не статус**: `text`, `day_price`, `total`, `cap_price` — теми же полями и в том же порядке, что клиентская фраза. Живая дверь умеет ответить `ok` С НАЛИЧИЕМ и БЕЗ ЦЕНЫ, и этот случай раньше уходил в общий `error` без слов про человека |
| `door_price.HUMAN_LINE` / `HUMAN_LINE_EN` (`door_price.py:138`) | слова про человека, RU и EN. **Ни одной цифры** — и это не стиль: белый список пост-чека строится из ЛЮБЫХ чисел ценовой записки, и цифра здесь стала бы РАЗРЕШЁННОЙ к произнесению ценой |
| `suggest.py:3893-3897` | фолбэк **один на ВСЕ ветки без числа** (одна модель, несколько моделей, подбор по классу, срок без котировки): прежний текст сохранён дословно и лишь продолжен строкой про человека |

Запрет молчания стои́т в самой строке дословно: «МОЛЧАТЬ ТОЖЕ НЕЛЬЗЯ, ответ обязан прозвучать:
молчание запрещено наравне с выдумкой».

Занятость через третий исход **не ходит**: `none_available` («байк занят») проходит насквозь
нетронутым — это ответ про наличие, у него свой разговор.

---

## 4. ПРИБОР РАСХОЖДЕНИЯ — И ОН СРАБОТАЛ НА ЖИВЫХ ЧИСЛАХ 21.09

Прибор: `door_price.measure` (`door_price.py:207`), порог **ровно 1 бат**
(`DIVERGENCE_MIN_BAHT = 1`), зовётся из `client_quote` на **каждом** ответе с ценой. Строку
собирает `divergence_line` (`door_price.py:187`): модель · даты · оба числа · разница со знаком.
Ответ не останавливается ни одной веткой; любой сбой внутри прибора = `None`.

**Куда строка падает физически:** `log.warning` логгера `door_price` → корневой логгер хоста
(`userbot_listen.py:70-76`, `FileHandler` utf-8, уровень INFO) → файл журнала userbot.
Проверено чтением кода хоста, боевых процессов не трогали.

**ЖИВОЙ ПРОГОН 21.09** (`_scratch_dver_istochnik_2109/divergence_live.py` — без сети, на
ответах двери, снятых §6; боевые функции `client_quote` / `measure`), окно 2026-10-06..10-11,
5 суток:

| модель | дверь, ฿/сут · за срок | файл, ฿/сут · за срок | строка журнала |
|---|---|---|---|
| XMAX 300 New Gen | **704 · 3520** | 682 · 3410 | есть, `разница=-110 ฿` |
| ADV 350 | **749 · 3745** | 634 · 3170 | есть, `разница=-575 ฿` |
| FORZA 300 | **518 · 2590** | 502 · 2510 | есть, `разница=-80 ฿` |
| **карточка целиком** | **9855 ฿** | 9090 ฿ | **недобор файла −765 ฿** |

Дословная строка (первая из трёх):

```
ЦЕНА РАСХОЖДЕНИЕ: модель=XMAX 300 | даты=2026-10-06..2026-10-11 | дверь=3520 | файл=3410 |
разница=-110 ฿ (файл минус дверь) | в сутки: дверь=704 файл=682 разница=-22 ฿ |
клиенту ушло число ДВЕРИ, ответ НЕ остановлен
```

У всех трёх `client_quote` вернул **ТОТ ЖЕ объект** двери (`out is res` → True), то есть
клиенту ушло число двери.

**Новое знание, которого 12.09 не было:** тогда живьём замерен был ТОЛЬКО XMAX (704), а 749 и
518 стояли премисой со снимка листа. **21.09 дверь назвала все три сама** — 704 / 749 / 518, —
и недобор **765 ฿** на карточке из трёх моделей перестал быть арифметикой по премисе: он
измерен целиком.

**Мина формата дат, названная числом.** Прибор разбирает дату **только ISO**
(`door_price.file_total` → `date.fromisoformat`), и на русском написании `06.10.2026` честно
молчит «дата старта не разобрана». Боевому пути это не грозит: он берёт `hints["iso_start"]` /
`iso_end` (`suggest.py:5635`), то есть ISO. Первый прогон пробы 21.09 шёл по-русски и дал три
молчания — это свойство ПРОБЫ, а не продукта, и исправлено в пробе.

---

## 5. ТРИ ОТРИЦАТЕЛЬНЫХ ТЕСТА — ПОИМЁННО И ЗЕЛЁНЫЕ

`test_door_price.py`, **20 тестов, OK** (прогон 21.09, 0.017 с).

**(а) дверь дала цену → в ответе ровно её число, подмены нет** — класс `TestDoorNumberWins`:
* `test_quote_object_is_returned_as_is` — `assertIs(out, res)`: **тождество объекта**, а не равенство;
* `test_answer_path_keeps_the_door_number` — живой путь целиком: в фразе есть число двери и **НЕТ** числа файла;
* `test_client_note_carries_the_door_number_and_not_the_file_one` — в боевой записке число двери **разрешено** пост-чеком, число файла — нет;
* `test_j_text_of_the_door_survives` — дословная строка столбца J больше не снимается (до 12.09 её снимал `reprice`).

**(б) дверь молчит → слова про человека и НИ ОДНОГО числа цены** — класс `TestDoorSilent`:
* `test_dead_door_is_named_third_outcome` — четыре формы молчания (`None`, `{}`, `error`, `ok` без котировки);
* `test_door_answered_without_a_price_is_also_the_third_outcome` — `ok` **с наличием и без цены**;
* `test_answer_says_a_human_counts_it` — в фразе есть «считает человек» и «МОЛЧАТЬ ТОЖЕ НЕЛЬЗЯ»;
* `test_not_a_single_price_number_is_allowed` — белый список пост-чека **пуст**, цифр нет и в самой строке про человека;
* `test_no_number_appears_even_though_the_file_could_count_it` — **главный отрицательный**: файл это число ЗНАЕТ (дал бы 3410), и оно всё равно не прозвучало.

**(в) файл и дверь разошлись → строка в журнале, клиенту число двери** — класс
`TestDivergenceIsLogged`:
* `test_line_is_written_and_client_gets_the_door_number` — ровно **одна** строка, в ней модель, обе даты, оба числа и разница со знаком; ответ `ok` с числом двери;
* `test_agreement_to_the_baht_is_silent` — сошлись до бата → строки **НЕТ**;
* `test_one_baht_is_already_a_divergence` — 3411 против 3410 уже строка;
* `test_instrument_never_touches_the_answer` — сорвавшийся прибор ответа не меняет;
* `test_rollback_word_silences_the_instrument_only` — `DOOR_PRICE_OFF=1` гасит прибор, число двери всё равно уходит клиенту.

Плюс замки границ: `test_busy_bike_is_not_the_third_outcome` (занятость насквозь),
`test_stale_file_no_longer_silences_the_client_number` (сторож свежести файла клиентское число
больше не гасит), `test_module_does_not_import_suggest` (петли импорта нет),
`test_old_path_would_have_replaced_the_number` (прежний путь **подменил бы** — замок на само
утверждение задания).

---

## 6. ЗАМЕР ЦЕНЫ ВЫЗОВА — ЖИВЫЕ СЕКУНДЫ 21.09.2026

Прибор: `_scratch_dver_istochnik_2109/door_cost.py`. Канал — клиент моста, одолженный у демона
(тот же путь, что у боевого читателя `price_freshness_run.live_handles`), `bc.timeout = 240`.
**Только чтение: один GET `fleet` и три GET `quote_price`.** Записей ноль, лист «Календарь
бронирования» и брони не открывались, токенов не печаталось, наружу не ушло ничего.
Окно — то же, что 12.09: **06.10.2026 → 11.10.2026, 5 суток**. Сырьё: `door_cost.jsonl`.

| # | модель | юнит | секунд | ответ |
|---|---|---|---|---|
| парк | `fleet` | — | **2.46** | 38 юнитов |
| 1 | XMAX 300 New Gen | `XMAX 300CC NEW RED GLOSS BKK 5709` | **4.10** | `ok`, 704 ฿/сут, 3520, available |
| 2 | ADV 350 | `ADV 350CC BLACK PHUKET 5849` | **3.18** | `ok`, 749 ฿/сут, 3745, available |
| 3 | FORZA 300 | `FORZA 300CC WHITE PHUKET 8950` | **4.19** | `ok`, 518 ฿/сут, 2590, available |

**АРИФМЕТИКА ОТВЕТА С ТРЕМЯ МОДЕЛЯМИ** — считана по тому, как его считает боевой путь:
цикл `suggest.py:5763` идёт по моделям **последовательно**, парк берётся один раз на окно кэша
(`pricing.FLEET_TTL = 300 с`), значит цена ответа = **1 парк + по одному `quote_price` на модель**
(при свободном первом кандидате; занятые юниты добавляют по вызову на каждого).

| мера | значение |
|---|---|
| **один вызов двери** | мин **3.18** · медиана **4.10** · макс **4.19 с** |
| три `quote_price` подряд | **11.47 с** |
| плюс парк | **2.46 с** |
| **ответ с тремя моделями** | **13.93 с** |
| потолок задания | 30 с → **УЛОЖИЛИСЬ**, остановки не требуется, кэш не изобретался |

**Корпус той же двери для честности** (числа чужих замеров, не пересчитывались):

| когда | n | один вызов | три модели по этой мере |
|---|---|---|---|
| 12.09.2026 | 1 | 21.4 с | 64 с — **было бы дороже 30 с** |
| 17.09.2026 | 8 | медиана 5.65 с, макс 84.77 с | 17.0 с по медиане; по среднему 27.26 с — 81.8 с |
| **21.09.2026 (этот заход)** | **3 + парк** | **медиана 4.10 с** | **13.93 с** |

Читается это так и не иначе: **сегодняшний замер в потолок укладывается, но у двери тяжёлый
хвост** — 17.09 три вызова из восьми шли 42–85 с. Порог 30 с на три модели держится на теле
распределения и НЕ держится на хвосте; хвост не привязан к сроку и его причина на полосе
по-прежнему **НЕИЗВЕСТНА**.

---

## 7. ЧТО ГОНЯЛОСЬ (полного гейта НЕ запускалось)

| набор | тестов | исход |
|---|---|---|
| `test_door_price.py` | **20** | OK |
| `test_price_gate.py` | **20** | OK |
| `test_noprice_gate.py` | **25** | OK |
| `test_price_source.py` | **58** | OK |
| **итого** | **123** | **OK** |

Ровно те наборы, которых касаются правки решения (`fb8e8ab0` менял именно их). Полный гейт не
запускался — задание это прямо запретило. Выкатки детям не было и не просилось.

---

## 8. ХЕШИ

| что | хеш |
|---|---|
| продукт решения (код + тесты) | **`fb8e8ab0`** «ДВЕРЬ-ИСТОЧНИК-1209: цену клиенту называет живая дверь + восемь соседних тестов переведены на новый канон», 13.09.2026 00:39 +07 |
| доводка соседнего набора | **`d213e7a8`** «ДВЕРЬ-ИСТОЧНИК-1209: соседний набор test_suggest зелёный», 13.09 |
| этот артефакт | **`37b8bf2f`** «Artefakt 61-a DVER istochnik-ceny 1209…», 21.09.2026 (хеш дописан сюда следующим коммитом — на момент самой записи он ещё не существовал) |

---

## 9. ЧЕГО ЗАХОД НЕ СДЕЛАЛ

* **кода в этом заходе не правлено ни строки** — продукт пунктов 2–5 лежал коммитом `fb8e8ab0`
  от 13.09; заход его проверил живым фактом и закрыл недостающее (обратное чтение, замер,
  артефакт), а не переписал;
* **узел `business_rules` не правился ни одним знаком** — только обратное чтение;
* **хвост двери в 42–85 с (замер 17.09) не объяснён** — причина НЕИЗВЕСТНА, гипотеза «это
  повторы `READ_TRIES`» не проверялась ни здесь, ни там;
* **замер 21.09 — три вызова одной минуты одного дня**: распределения по времени суток нет, и
  укладывание в 30 с доказано для ТЕЛА распределения, а не для хвоста;
* **занятые юниты в арифметику ответа не входили**: у всех трёх моделей первый же кандидат был
  свободен; на занятом парке вызовов будет больше, и сколько — не мерено;
* **сезонная слепота листа не снята** (замер 17.08: занижение пика на 27.7–31.7 %) — владелец
  12.09 велел ориентироваться на лист, зная про неё; Штаб своей властью прежнее правило не
  возвращает;
* **почему база ADV 350 в файле 616 при живых 749 — НЕИЗВЕСТНО** (поле файла называет источником
  медиану завершённых броней; сверки не делалось);
* **CLICK 125 не трогали вовсе** — решение владельца «нет, мы его не сдаём пока что» записано в
  узел и в коде ничего не меняло;
* **наружу не ушло ни одного сообщения**, клиентам не писали, менеджеров не подключали, детей не
  поднимали, о выкатке не спрашивали; боевых процессов не трогали; лист «Календарь бронирования»
  и брони не открывались ни одной командой — с дверью говорили только штатным `quote_price`;
  цен в листе не меняли; значений токенов не печатали; текста клиентских диалогов не выводили;
* **ничего не удалялось**, включая временное место `_scratch_dver_istochnik_2109/`.

---

ДВЕРЬ istochnik-ceny 1209
