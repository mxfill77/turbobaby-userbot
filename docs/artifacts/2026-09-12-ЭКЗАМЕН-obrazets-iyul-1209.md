# Задание 51-a2: ДОВЕСТИ рабочее место экзамена — что закоммичено, что зелено, что показано

ЭКЗАМЕН obrazets-iyul 1209

Полоса: ПК. Дата: 12.09.2026. Продукт захода — РЕШЕНИЕ, а не новая правка: заход 51-a (ряд #14)
закрыли по таймауту прогона в 21:51Z, его работа лежала в дереве незакоммиченной и без вердикта.
Головы звали 0 раз. Наружу не ушло ни одного сообщения.

## §1 Признак сделанности — по СОДЕРЖИМОМУ

Артефактов за 12.09 в `docs/artifacts` — 10, ни один не несёт слов адреса (`google-photos-progress`,
`АГЕНТ-obnovlen-imya`, `ВИТОК-slepota-ocheredi`, `ВОПРОС-podstanovka-dat`, `ДОЛЯ-gashenie-ceny`,
`ЗАКРЫТИЯ-po-lichnosti`, `ОБЕЩАНИЕ-v-kode`, `ПЛАНИРОВЩИК-tick-ceny`, `ПОДБОР-dve-alternativy`,
`ЭКРАН-kejs-odin`). Вопросы задания в них не отвечены → заход продолжен, а не остановлен.

## §6 Арифметика бюджета (до первого действия)

Единицы — пять наборов тестов и пять коммитов. Замер первого набора (`test_exam_show`): **2.2 с**
стенных. Произведение: 5 наборов × ~2.4 с ≈ **12 с** против потолка **2700 с** = 0.44 %. Коммиты
через замок индекса — единицы секунд. Запас таков, что резать нечего; голов 0, значит дорогой
единицы в заходе нет вовсе.

## §2 Состояние дерева ПЕРВЫМ действием (без замка индекса)

`git status` на входе — **6 трекаемых файлов изменены, все из 51-a**, новых трекаемых нет:

| файл | строк | mtime (местное +07) | он же UTC |
|---|---|---|---|
| `trainer.py` | +61 | 04:14:42 | 21:14:42Z |
| `lesson_store.py` | +9 | 04:14:53 | 21:14:53Z |
| `exam_show.py` | +660 | 04:22:16 | 21:22:16Z |
| `pc_agent.py` | +118 | 04:36:01 | 21:36:01Z |
| `test_exam_show.py` | +549 | 04:38:13 | 21:38:13Z |
| `.gitignore` | +1 | 04:39:00 | 21:39:00Z |

Итого 1295 вставок, 103 удаления. Окно mtime **21:14–21:39Z** ложится внутрь окна 51-a, названного
его черновиком (21:22–21:38Z), и обрывается за 12 минут до закрытия ряда в 21:51Z — то есть это
ровно та работа, которую заход не успел сдать, и ничего кроме неё.

**ЧУЖОГО В ТРЕКАЕМЫХ ФАЙЛАХ НЕТ.** Untracked в дереве много (≈150 каталогов `_scratch_*`, `.codex/`,
`AGENTS.md`) — это накопленное временное прошлых заходов; не тронуто ни одним действием, не
удалялось, в коммиты не попало: все пять коммитов сделаны `git commit -- <путь>` с явным списком
путей, поэтому чужой стейдж соседа в них уехать не мог по построению (класс замера 3d627d0).

`git log` за последние часы: последний коммит до захода — `2a39b4d` (03:59 +07), итог задания 50-a.
Ни одного коммита 51-a в истории не было — вся его работа была НЕ закоммичена.

## §3 Тесты — пять наборов, все зелёные

| набор | тестов | итог | время |
|---|---|---|---|
| `test_exam_show` | 98 | OK | 0.93 с |
| `test_pc_agent` | 62 | OK | 0.16 с |
| `test_trainer_run` | 143 | OK | 4.29 с |
| `test_lesson_store` | 41 | OK | 3.07 с |
| `test_dispatch_notify` | 64 | OK | 0.06 с |
| **итого** | **408** | **0 падений** | ~8.5 с |

Красного не было ни одного — ни своего, ни средового. Названного заданием
`test_anthropic_key_is_read_only_from_environment` в падениях нет: чинить и «называть чужое» не
потребовалось. Наборы прогнаны ДВАЖДЫ — до коммитов и ПОСЛЕ них, с одинаковым результатом (408/408):
это и есть доказательство, что коммиты положили полную работу, а не её часть.

Мина среды, замеченная и НЕ являющаяся падением: `pytest` в venv не установлен
(`No module named pytest`), наборы гоняются `python -m unittest <модуль>`.

## §4 Коммиты — пять, по смыслу

| коммит | смысл | файлы |
|---|---|---|
| `6f709fb` | критик: подсказка двумя полями | `trainer.py` |
| `3ee716f` | база уроков: четвёртый набор «экзамен» | `lesson_store.py` |
| `d7dc7b0` | карточка-рабочее место | `exam_show.py`, `.gitignore` |
| `56ca76a` | агент: колбэки пяти кнопок | `pc_agent.py` |
| `010d618` | набор витрины экзамена | `test_exam_show.py` |

Каждый — через замок `git_serial_pc.py` (рядом шёл второй заход, индекс один на двоих).
`.git/index.lock` не встретился ни разу, чужого замка не удалялось. Трекаемое дерево после
коммитов **чисто**: `git status --untracked-files=no` — пусто. Остатка, который «не влез в
бюджет», НЕТ: закоммичено всё, что лежало.

Предкоммитный сторож реестров зависимостей (`deploy/hooks/pre-commit` → `deps_registry_check.py`)
на входе дал `rc=0`, «реестры совпали с фактом импортов (3 теста-сторожа)», и ни одного из пяти
коммитов не задержал. Это не формальность: правка `pc_agent.py` сознательно НЕ импортирует
`exam_show` (иначе в замыкание живых ворот клиентского контура въехал бы прогонщик корпуса —
цена названа числом в самом наборе: 77 → 78 файлов).

## §5 Приёмка по существу — числом, на живом

**Подсказки в снимке двумя полями.** `hints_of` отдаёт список словарей с ключами
ровно `['observation', 'rule']`; оба поля попадают в текст карточки РАЗНЫМИ строками и с разными
значками (наблюдение номером, правило — отступом и словом «→ правило:»). Проверено живым рендером
и тестами `test_the_shot_carries_the_hints_with_both_fields`,
`test_the_card_shows_observation_and_rule_of_every_hint`.

**Старые снимки показываются без ошибки.** На диске в `exam_shots` два снимка, и у ОБОИХ ключа
`hints` нет вовсе (собраны до 12.09):

| снимок | подсказок | кнопки | слова про отсутствие |
|---|---|---|---|
| `case-1.json` | 0 | `✅ Верно`, `❌ Неверно` | есть |
| `case-1@df03e80.json` | 0 | `✅ Верно`, `❌ Неверно` | есть |

Оба собираются в карточку без исключения (1736 и 1191 символов), шапка обоих —
`🎓 ЭКЗАМЕН · кейс 1 из 17 · пройдено 0 — первый контакт, одна модель`. Слова об отсутствии —
дословно: «подсказок критика в этом снимке нет — он собран до того, как критика позвали»
(`exam_show.NO_HINTS`). Кнопок РОВНО прежние две, а не пять: «✔ Применить» на снимке без подсказок
отказывал бы всегда, и кнопка, которая не может сработать ни разу, хуже её отсутствия. Ни один
старый снимок не перезаписан и не удалён — оба читались, счёт слотов подавался доводом.

**Отрицательная сторона — пять проверок, и у них свой класс** `TestFiveNegativesOfTheWorkplace`
(7 тестов, 7 ok):

| требование задания | тест | итог |
|---|---|---|
| тап по устаревшей подсказке — отказ | `test_a_tap_on_a_stale_hint_number_refuses_and_writes_nothing` | ok |
| тап по подсказке ЧУЖОГО снимка — отказ | `test_a_tap_on_a_hint_of_another_shot_refuses` | ok |
| вердикт на чужом отпечатке корпуса — отказ | `test_a_verdict_on_a_shot_with_an_alien_corpus_is_refused` | ok |
| «Верно» дважды — одна запись | `test_a_second_yes_on_the_same_case_makes_one_record_not_two` | ok |
| «Применить» без отмеченных — отказ словами | `test_apply_without_marks_refuses_in_words` | ok |
| после 17-го — итог, а не 18-й | `test_after_the_last_case_comes_the_result_and_not_an_eighteenth` | ok |
| право fail-closed на каждой новой двери | `test_the_right_is_fail_closed_on_every_new_door` | ok |

Отказ по устаревшей подсказке проверен ещё и ЖИВЬЁМ, на обоих настоящих снимках: тап по номеру 1
даёт «⛔ тапать нечего: подсказок критика в этом снимке нет… Ничего не записано.» — то есть отказ
словами и без записи, а не молчаливый промах.

**Критик: парный режим не тронул боевой путь.** `hypotheses_prompt(paired=True)` меняет ТОЛЬКО
системную часть запроса (+248 символов, разделитель `||` появляется в ней); пользовательская часть
у плоского и парного вызовов **совпадает дословно**. Боевой путь модербота зовёт позиционно и без
довода — его запрос не изменился ни на символ. `split_pair` fail-safe в обе стороны: строка без
разделителя целиком идёт ПРАВИЛОМ (а не наблюдением), «наблюдение ||» — тоже правилом; урока с
пустым текстом правила не рождается ни на одной ветке. Парный ответ критика разбирается:
`parse_hypotheses` вернул обе строки формы «наблюдение || правило».

**Кнопки и колбэки** снимка С подсказками: `✅ Верно` / `1` / `2` / `✍️ своё` / `✔ Применить` →
`exam:ok:3`, `exam:h1:3`, `exam:h2:3`, `exam:own:3`, `exam:go:3`. Регулярка агента
`^exam:(ok|no|own|go|h[1-4]):([0-9]{1,3})$` — из Telegram в командную строку по-прежнему едет
только число; урок своими словами идёт через **stdin**, а не argv (живая кириллица, которую argv на
Windows коверкает молча). Замыкание «каждая кнопка карточки разбирается ловцом» держит тест
`test_every_button_of_the_card_is_understood_by_the_catcher`.

## Чего НЕТ — названо, а не дописано (это следующий заход)

1. **Ни одной подсказки на ЖИВОМ снимке.** Оба снимка на диске — без подсказок, и это не дефект
   кода: подсказки рождаются ТОЛЬКО при заморозке (`test_only_freeze_calls_the_head`), а заморозка
   зовёт голову, что этому заходу запрещено прямо. Значит владелец, нажав показ СЕГОДНЯ, увидит
   карточку со словами «подсказок нет» и двумя прежними кнопками — рабочее место собрано, но
   материала для тумблеров на диске пока нет. Первый снимок с подсказками — продукт следующего
   захода, у которого будет право на голову.
2. **Журнала вердиктов на диске нет вовсе** (`exam_verdicts.tsv` отсутствует): ни одного вердикта
   ещё не записано, счёт «пройдено K» честно нулевой. Уроков набора «экзамен» в `lesson_store.tsv`
   — **0**.
3. **`exam_session.json` в дереве держит СИНТЕТИЧЕСКИЙ ключ** `deadbee@2026-09-11T00:00:00Z` —
   след разработки 51-a (mtime 04:30 +07), а не сессия владельца. Текущие наборы боевой стол НЕ
   трогают: файл на 8 часов старше моих прогонов и ими не изменился. Файл не удалён (запрет) и
   с этого коммита закрыт `.gitignore`.
4. **Живого показа наружу не делалось** — запрет захода. Всё, что выше, снято рендером в памяти и
   тестами; «первое впечатление владельца» остаётся непотраченным, и замок показа
   (`agent_carries_button`, fail-closed на трёх исходах) его сторожит.

## §7 Владельцу — три строки

1. **Что увидишь на карточке:** шапку со счётом «🎓 ЭКЗАМЕН · кейс N из 17 · пройдено K», затем
   вопрос клиента, замороженный ответ бота, строку «почему так», нумерованный список подсказок
   критика (у каждой — сначала ЧТО не так в этом ответе, затем ПРАВИЛО на будущее) и внизу словами
   то же, что написано в пункте 2.
2. **Что делает каждая кнопка:** «✅ Верно» — кейс зачтён; номера «1…4» — ТУМБЛЕРЫ, тап отмечает,
   повторный тап снимает, сами по себе они не пишут ничего; «✍️ своё» — следующим сообщением в
   группу напишешь правило своими словами (10 минут); «✔ Применить» — отмеченные подсказки станут
   уроками, и урок начинает действовать СРАЗУ.
3. **Что происходит после тапа:** вердикт ложится строкой в журнал экзамена (откат по номеру
   возможен), отмеченное — уроками в базу с автором, набором «экзамен» и причиной «критик,
   подтверждено тапом», и следующий кейс приходит САМ; после 17-го вместо 18-го придёт итог.
   «Применить» без отметок и тап по подсказке чужого/старого снимка отказывают словами и не
   пишут ничего.

## Остатки честности

- `pytest` в venv нет — наборы гоняются `python -m unittest`; на зелёность это не влияет, но
  команда из головы («pytest -q») здесь не работает.
- Временное место названо и держится: `_scratch_exam_obrazets_1209/` (`probe_card.py`,
  `probe_card2.py`) — только чтение, ничего не удалено. Первая проба искала слова «подсказок нет»
  НЕ тем литералом и парный довод НЕ в том элементе пары, дала два ложных «False», и это исправлено
  второй пробой, а не переписано: ложный «False» на верном коде — тот же класс, что зелёный мок на
  сломанном.
- Процессы не трогались, демон и агент не перезапускались, наружу не ушло ни одного сообщения,
  боевых записей нет, брони и «Календарь бронирования» не открывались, значений токенов не
  печаталось.
