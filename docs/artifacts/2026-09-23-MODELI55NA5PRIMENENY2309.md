# MODELI55NA5PRIMENENY2309 — модели полос: основная claude-opus-5-5, запасная claude-opus-5

> **ПОПРАВКА 24.09.2026 (ретрай 0025-72d):** строки ниже «КОММИТ НЕ ЛЁГ» и «Готовность по коммиту:
> НЕТ» описывают 23.09 и УСТАРЕЛИ. Правка этого захода закоммичена без изменений как `96878f0a`,
> серверная пара — веткой `pk-golovy-55-2409` (`7789243`). Итог и применение:
> `docs/artifacts/2026-09-23-MODELI55RETRAYPRIMENENY2309.md`.

Задание Штаба `0023-72b.2309` (ящик, 23.09.2026). Исполнитель — headless-заход ПК, задача #64,
23.09.2026 ~21:50–22:40 местного. Демон и pc_agent руками не трогались. Сервер не трогался: ssh не
поднимался. `.env` не читался и значения из него не печатались. Временное место —
`tmp/models55_2309/`, ничего не удалялось.

**КОММИТ НЕ ЛЁГ.** Гард дважды принял прозу сообщения коммита за обращение к секретам: сначала
слово «.env», потом «dict(os.environ)». На второй раз он выдал «ВТОРАЯ КРАСНАЯ ОПЕРАЦИЯ ЭТОГО
ПРОГОНА… Останови прогон и доложи». Прогон остановлен, третьей попытки не было. Правка лежит в
рабочем дереве и в индексе: `pc_orchestrator.py`, `test_pc_orchestrator.py`, `CLAUDE.md`. Сам коммит
делает владелец (команда ниже).

## Вердикт по головам

| полоса / голова | основная | запасная | в коде (дерево) | живьём сейчас | применение |
|---|---|---|---|---|---|
| ПК, исполнитель | `claude-opus-5-5` | `claude-opus-5` | ГОТОВО: `EXECUTOR_FALLBACK`, argv с `--fallback-model` | основная 5-5, **запасной нет** (живой код c707d62d) | после коммита — штатный self-update, рестарт руками не нужен |
| ПК, думатель | `claude-opus-5-5` | `claude-opus-5` | ГОТОВО: пара читается из файла на старте процесса | **5-5 → 5 уже сейчас** (см. ниже: свежий старт 17:33) | правка закрывает класс; живую пару она уже не меняет |
| Сервер `orchestrator_daemon.py` | `claude-opus-5` | `claude-opus-4-8` | НЕ правился — **СТОП по условию задания** | по 71w (22.09): 5 / 4-8; мной не перемерено | штатного пути без рестарта НЕТ |

## Премиса 71w — проверена первым действием

- **ПК-исполнитель — сошлось.** `EXECUTOR_MODEL = "claude-opus-5-5"` стоит на `pc_orchestrator.py:2360`,
  argv на `:2521` = `[cbin, "-p", "--model", EXECUTOR_MODEL, "--effort", eff, prompt]`, `--fallback-model` нет.
  METRICS последних задач: #62 и #63 — `model=claude-opus-5-5`.
- **ПК-думатель — премиса УСТАРЕЛА.** Дефолты кода сошлись (`:5884-5885`), но «живьём перевёрнут» было
  верно только до 23.09 17:33. В 17:33:03 сторож поднял демона через `schtasks /Run` (строка лога
  `watchdog: schtasks /Run /TN pc_orchestrator → rc=0`). Прежний PID 24056 уже не существовал: машина
  перезагружалась. Это СВЕЖИЙ старт, `.env` перечитан. Дальше были две эстафеты, 19:41 → 11168718 и
  19:59 → c707d62d; они унаследовали уже свежие значения. Моё окружение унаследовано от живого демона,
  и сверка с известными id без печати значений даёт: `THINKER_MODEL == claude-opus-5-5`,
  `THINKER_FALLBACK == claude-opus-4-8`, метка эстафеты `PC_ORCH_SUPERSEDE_PID` задана. Живой код
  (`_norm_fallback_id`) превращает эту пару в **`claude-opus-5-5 → claude-opus-5`**. Перевёрнутость
  сняла перезагрузка, то есть случайность, а не код. Класс остался: следующая правка строки `.env`
  снова не доехала бы через эстафету.
- **Сервер — сошлось с кодом.** Живой origin/main = `2e98f5c`: fetch в `tmp/srv` сделан 23.09, это тот
  же HEAD, что 71w снял с живого чекаута. Там `ORCH_MODEL` по умолчанию `claude-opus-5` (`:192`),
  `ORCH_MODEL_FALLBACK` по умолчанию `claude-opus-4-8` (`:193`), исполнитель = ORCH_MODEL (`:233`).

## Что сделано на ПК (дерево + индекс, коммит — за владельцем)

1. **Запасная исполнителя.** Добавлена константа `EXECUTOR_FALLBACK = "claude-opus-5"`. В argv `run_claude`
   теперь стоит `--fallback-model claude-opus-5`; при равной паре флаг не ставится, потому что CLI такую
   пару отвергает. Семантику флага я прочитал в справке бинаря CLI этого ПК: «fallback… when the
   default model is overloaded or not available». **Лимит учётки (429) запасная НЕ лечит:** учётка у
   обеих голов одна. Одна запасная на обе головы: `_THINKER_RESERVE = EXECUTOR_FALLBACK`.
2. **Думатель независим от протухающего наследства.** Новая функция `_thinker_env_fresh` на старте
   КАЖДОГО процесса читает `THINKER_MODEL`/`THINKER_FALLBACK` из ФАЙЛА `.env` тем же парсером
   (`dotenv_values`), а унаследованное окружение не решает. Если ключа в файле нет, берётся дефолт
   кода, как на свежем старте. Если файл не прочитан, остаётся прежнее поведение, и баннер называет
   это вслух. `_thinker_env_sync` приводит окружение процесса к той же правде: дети и следующая
   эстафета наследуют уже её. Инвариант «эстафета = свежий старт» держит тест, литерала `.env` он не
   зеркалит. Правка действует с ПЕРВОЙ эстафеты: новый процесс исполняет уже новый код.
3. **Баннер старта.** Новая строка `=== ГОЛОВЫ: исполнитель … → запасная …; думатель … → запасная …
   (источник пары думателя: …; наследство эстафеты расходилось с файлом: …) ===` показывает
   действующие id и источник, сырых строк `.env` в ней нет. До этой правки живую пару думателя нельзя
   было снять ничем, кроме вывода кодом.
4. **`CLAUDE.md`** догнан. Фолбэк теперь есть у обеих голов, и 429 он не лечит. Ручки думателя живут
   в файле. У сервера нет штатного пути «коммит → применение».

## Тесты (выводы — `tmp/models55_2309/`)

| прогон | итог | файл |
|---|---|---|
| новые тесты думателя на СТАРОМ коде | **красные**: `_thinker_env_fresh` нет; эстафета с наследством 71w (`claude-opus-5`/`claude-opus-4-8`) разошлась со свежим стартом в полях **T, F, M** | `red_before_thinker.txt` |
| голден argv исполнителя на СТАРОМ коде | **красный**: `EXECUTOR_FALLBACK` нет | `red_before_executor.txt` |
| 6 точечных на НОВОМ коде | 6/6 OK | `green_after_targeted.txt` |
| гейт self-update `test_pc_orchestrator` + `test_pc_local_dec` | **1204/1204 OK** (42.8 с) | `gate_after.txt` |
| соседи `test_dispatch_notify test_env_value_leak test_pc_awake_clock test_lesson_router test_profile_choice` | 272/272 OK | `neighbours_after.txt` |
| пара, которую получит преемник в ТЕКУЩЕМ унаследованном окружении (скрипт GOLOVA55 `tmp/model_5_5_2309/effective_models.py`, новый код) | `EXECUTOR_MODEL=claude-opus-5-5`, `THINKER_MODEL=claude-opus-5-5`, `THINKER_FALLBACK=claude-opus-5` | `effective_models_inherited_env.txt` |

Мой собственный скрипт снятия (`tmp/models55_2309/effective_pair.py`) гард остановил отказом «ОБЪЕКТ
НЕ НАЗВАН»: чтение `os.environ` по именам он принял за сборку имени секрета. Скрипт не запускался.

## Готовность по коммиту и применение — раздельно

- **Готовность по коммиту: НЕТ.** Коммит не сделан, `git log -1` = `0d51037a`, прежний HEAD. В индексе
  ровно 3 файла. Команда владельцу, из `D:\turbobaby-bot`, по одной на строку:
  ```
  venv\Scripts\python.exe git_serial_pc.py -- git add -- docs/artifacts/2026-09-23-MODELI55NA5PRIMENENY2309.md
  venv\Scripts\python.exe git_serial_pc.py -- git commit -F tmp/models55_2309/commit_msg.txt -- pc_orchestrator.py test_pc_orchestrator.py CLAUDE.md docs/artifacts/2026-09-23-MODELI55NA5PRIMENENY2309.md
  ```
  Коммит с путями берёт ровно эти файлы, чужой стейдж соседа в него не уедет.
- **Применение по строке самообновления: НЕ НАБЛЮДАЛОСЬ** и в этом заходе наблюдаться не могло. Нет
  коммита, а кроме того `poll_once` джойнит обе руки (`th.join()`), и self-update стоит в цикле ПОСЛЕ
  `poll_once`. Значит, эстафета случается только после конца обоих заходов. Что искать в
  `pc_orchestrator.log` после коммита:
  1. `self-update: c707d62d→<новый хеш> — гейт пройден, новый процесс запущен`;
  2. `=== ГОЛОВЫ: исполнитель claude-opus-5-5 → запасная claude-opus-5 (константы кода); думатель
     claude-opus-5-5 → запасная claude-opus-5 (источник пары думателя: файл .env; …) ===`;
  3. METRICS следующей задачи `model=claude-opus-5-5`. Формат METRICS заморожен и одинаков в двух
     репо, поэтому запасную он не показывает;
  4. argv следующего ребёнка `claude.exe` (Win32_Process, только чтение): `--model claude-opus-5-5
     --fallback-model claude-opus-5`.
- **Пока правка не закоммичена:** грязный `pc_orchestrator.py` запрещает демону ЛЮБОЙ self-update
  (`_dirty_block`: лог, журнал, карточка владельцу). Если сосед закоммитит зависимость демона, эстафета
  встанет до коммита этой правки.

## Сервер — СТОП и доклад (условие задания «если без ручного рестарта никак»)

Без ручного рестарта никак, и причина тут не одна, их три:
1. **У серверного демона нет самообновления.** В `orchestrator_daemon.py` и `deploy/` на origin/main нет
   ни self-update, ни автопулла: ни `ff-only`, ни `git pull`, ни таймера. Юнит
   `deploy/orchestrator-daemon.service` — простой `Type=simple`. Код доходит до процесса только через
   `systemctl restart orchestrator-daemon`: его делает серверная задача (`systemd-run --on-active`)
   или владелец.
2. **Пару там задают строки серверного `.env`, а не код.** Об этом прямо говорит
   `tests/test_orchestrator_model.py:23-34`: «два литерала ниже ЗЕРКАЛЯТ боевой .env (ORCH_MODEL /
   ORCH_MODEL_FALLBACK)». Правка дефолтов `:192/:193` живую пару не сменит даже после рестарта.
3. Коммит с ПК в серверный main обошёл бы серверный предпушевой гейт (`deploy/hooks/pre-push` →
   `gate.py` на живом чекауте). Серверная полоса получила бы расхождение при своём следующем push, а
   литералы тестов при неправленом `.env` покрасили бы гейт всему репо: так уже было 24→25.07, push
   стоял 13 ч.

Поэтому в серверный репозиторий не коммитилось и не пушилось ничего. Что сделать серверной полосе
одним заходом, после сброса лимита 27.09 09:00 UTC:
- строки `ORCH_MODEL=claude-opus-5-5` и `ORCH_MODEL_FALLBACK=claude-opus-5` в серверном `.env`;
- дефолты `:192/:193` на ту же пару;
- в `_MODEL_RETIRED` добавить `claude-opus-4-8`;
- литералы `tests/test_orchestrator_model.py:31,:34` и `tests/test_executor_model.py:86`;
- `docs/ENV_PLAYBOOK.md:67` — парно с ПК;
- гейт, потом рестарт `orchestrator-daemon` штатным отложенным путём.

## Что не применилось без рестарта — числом и словами

- **ПК: 0 из 2 правок требуют рестарта.** Обе применяет штатный self-update. Сейчас **не применены 2 из 2**,
  потому что коммит не лёг. Живая пара думателя уже верна (5-5 → 5) благодаря перезагрузке 17:33.
  Запасной у исполнителя живьём нет до первой эстафеты после коммита.
- **Сервер: 2 из 2 значений не применимы без рестарта** — основная (`claude-opus-5`, она же исполнитель)
  и запасная (`claude-opus-4-8`). Нужны правка серверного `.env` и `systemctl restart
  orchestrator-daemon`, оба пункта запрещены этим заданием.

## НЕИЗВЕСТНО

- Живой вызов думателя на паре 5-5 → 5: вызовов после 17:33 в логе нет. Пара снята окружением + кодом.
- Живое состояние сервера сегодня: не перемерялось, ssh не поднимался. Цифры — из 71w (22.09).
- Сработает ли запасная у исполнителя на реальном отказе: пробных запусков `claude -p` не было (запрет
  разведки). Есть справка флага и голден argv.
- Версия канона рамки `KB_shtab_frame` в этом заходе не сверялась.

## Находки рядом

- Гард ловит слова в сообщении коммита: литерал «.env» и «dict(os.environ)» в `-m` дали две
  env-карточки. Память «env-карточка на упоминание в прозе — ЗАКРЫТО 01.08» для `git commit -m` неверна.
- Обе карточки ушли в режиме «🧪 ПРОБА — ПЕРЕХВАЧЕНО НА ГРАНИЦЕ». По словам гарда, владельцу не ушло
  ничего, маркер уведён в `test-pc_ask_64.marker` в корне репо. Файл не мой, я его не трогал.
