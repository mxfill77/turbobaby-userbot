70g KNOPKATRENAZHERA 2209

# Кнопка ТРЕНАЖЁРА: вердикт владельца ДОХОДИТ до читателя ответа без переноса — цепь замыкается на той же базе; тап 12.09 не оставил строки потому, что ввод не был завершён

**FACT — живые числа, снятые сегодня** (работа шла 21.09.2026 23:0x–23:5x местного = 16:0x–16:5x UTC;
артефакт именован 22.09 по адресу задания — задание положено поздно вечером 21.09, и адрес назвал
дату за полночь; это та самая строка, которую задание просило поставить.)

| что доказано живым фактом | число / адрес |
|---|---|
| ПРЕМИСА ШТАБА (первым действием): кнопка тренажёра пишет туда же, откуда читает сборка ответа | **ПОДТВЕРДИЛАСЬ.** Оба пути — один файл `D:\turbobaby-bot\lesson_store.tsv`, сравнение сделал сам код: `True` |
| живой прогон своей ситуации: тап «✔ Применить» на СВОЕЙ базе → читатель ОТВЕТА на ТОЙ ЖЕ базе | **0 → 1 буллет** (прочитана `False → True`), между тапом и чтением ни одного действия человека |
| один тап по «✔ Применить» с двумя отмеченными подсказками | **2 урока РАЗНЫХ состояний**: #1 `актив` (у подсказки есть наблюдение), #2 `кандидат` (наблюдения нет) |
| дверь «✍️ своё» того же набора | урок #3 **`кандидат`**, квитанция сама печатает строку перевода |
| кандидаты до читателя не доходят | строк в базе **3**, буллетов у читателя **1**, `active()` **1** |
| ОТРИЦАТЕЛЬНЫЙ ТЕСТ (развёл базы, как у живого набора) | читатель исходной базы **1 → 1** (не вырос), читатель другой базы **0 → 1**. Поломка замечена числом |
| третий исход читателя (путь, которого нет) | **(0, False)** — «действующих нет» и «не прочитал» проверка различает |
| тап 12.09 18:10:01 `exam:own:1` | **ввод не завершён.** Стол `exam_session.json` заморожен на 12.09 18:10 с ЖИВЫМ на тот момент ожиданием (`own_until` = 18:20:02 местного, `own_who` = SamHold, `selected` = []) — ключи не сняты ни разу |
| поимок текста «своими словами» за 4 файла лога (02.09→21.09) | **4**, и все — набора `live` (18.09 23:13 и позже). Набора `trainer` — **0** |
| exam-колбэков ТРЕНАЖЁРА за всю историю 4 файлов лога | **1** — ровно тот самый 12.09 18:10:01 |
| журнал вердиктов тренажёра `exam_verdicts.tsv` | **ФАЙЛА НЕТ ВОВСЕ** — ни одного вердикта тренажёра не записано никогда |
| боевая база уроков (только чтение) | **9** строк, все `перенос_книги`, все `актив`; строк источника «экзамен» — **0**; кандидатов — **0**; след переводов — **ПУСТ** |
| слово владельца «урок включи N» в 4 файлах лога | **0** раз (`команда урока:` — ноль совпадений в каждом из четырёх) |
| развод баз двух наборов (`LIVE_LESSONS`) | коммит `96f984c0`, **17.09.2026 23:43 +0700** — на 12.09 второй базы не существовало, «уйти в другое место» записи было некуда |
| наружу за весь заход | **0**: отправитель звался 2 раза, агент 2, прибор регрессии 3 — ВСЕ заглушки, ни одного байта и ни одного процесса |

---

## 0. Признак сделанности (первым действием, смотрит содержимое)

В `docs/artifacts` искался файл со словом `KNOPKATRENAZHERA` — **такого файла не было**. Соседний
`2026-09-22-70f-UROKDOSHELDOPRAVILA-2209.md` (предыдущий заход этой же цепи) открыт и прочитан
целиком: он отвечает про ЖИВОЙ набор и прямо оставляет путь ТРЕНАЖЁРА непроверенным живым
запуском («шаг 1 — приём вердикта: живым запуском НЕ проверена»). Значит вопрос этого задания в
нём не отвечен, и заход сделан целиком. Числа 70f, которые я мог перепроверить сам (кандидаты,
след переводов, строки источника «экзамен», слово перевода в логах), перепроверены и совпали.

## Арифметика до работы (замер на ДВУХ шагах, первым действием)

Шаг «имена путей в двух модулях» — 1.6 тыс. символов вывода; шаг «две выписки из `exam_show.py`» —
7.4 тыс. Средняя цена шага ≈ 4.5 тыс. символов ≈ **1.7 тыс. токенов = 0.0085 окна**. Шагов
планировалось **20** → **0.17** против потолка **0.7**. Порог не достигнут, заход шёл без
остановки. На диск писалось после каждой вехи: черновик доклада
`tmp/pc_report/task12-11672-1790007148875-12.md` обновлён трижды.

---

## 1. ДВА ПУТИ — строкой кода, и они СОВПАДАЮТ

Ответ прямой: **совпадают**. Это не вывод из чтения, а замер — пути посчитал сам код и сам же их
сравнил (`True`).

| кто | строка кода | что получается |
|---|---|---|
| **кнопка в режиме ТРЕНАЖЁРА пишет** | `exam_show.py:94` — `LESSON_PATH = None` → `exam_show.py:1843` — `path=LESSON_PATH` в `lesson_store.add/add_candidate` | `lesson_store._path(None)` → `STORE_PATH` = `D:\turbobaby-bot\lesson_store.tsv` (`lesson_store.py:172`) |
| **сборка ответа читает** | `suggest.py:932` — `LESSON_BASE_PATH = None` → `suggest.py:970` — `lesson_store.load(LESSON_BASE_PATH …)` | тот же `_path(None)` → тот же `D:\turbobaby-bot\lesson_store.tsv` |

Комментарий у самой строки говорит это вслух (`exam_show.py:89-93`): «`None` значит «боевая
таблица, та же, куда пишет кнопка «🎓 Обучить»»: второй базы уроков у полосы нет и не заводится».
Вторая база появляется РОВНО ОДНИМ движением — `use_live_set()` (`exam_show.py:2264-2273`)
подменяет `LESSON_PATH` на `LIVE_LESSONS = exam_live/lessons.tsv`, и это ветка ЖИВОГО набора,
которой у тренажёра нет. То есть разъезд баз, описанный в 70f, — свойство ключа `--live`, а не
устройства кнопки.

**Значит премиса Штаба верна, и переноса на пути тренажёра нет по построению.** Дальше проверялось
второе, чего премиса не утверждала: кандидатом урок ложится или сразу действующим.

## 2. Живой прогон ОДНОЙ своей ситуации — урок у читателя появился САМ

Прогон: `tmp/trener_knopka_2209/knopka_live.py`. Своей логики уроков, вердиктов и чтения базы в нём
нет ни строки: он подменяет ПОЛЯ НАБОРА (те же шесть имён, что переключает боевой `use_live_set`)
на файлы временного места и зовёт БОЕВЫЕ двери — `toggle`, `apply_marked`, `own_start`,
`own_take`, — а читателем ответа зовёт боевую `suggest.active_lesson_bullets`.

Своя ситуация: свой корпус из трёх кейсов (отпечаток `bbc0c9fbd29599c6`, посчитан живым
`corpus_fingerprint`), свои снимки с подсказками критика, свой стол, свой журнал вердиктов и
**своя база уроков** `tmp/trener_knopka_2209/lessons.tsv`. Отметка происхождения — в самом тексте:
каждое правило начинается со слов `ПРОБА 22.09`, имя набора корпуса — `ПРОБА KNOPKATRENAZHERA 22.09`,
автор строк — `@knopkaproba`.

```
читатель ДО тапа:            буллетов 0, прочитана False
тумблер подсказки 1          → ok=True  «☑️ отметил 1»
тумблер подсказки 2          → ok=True  «Отмечено: 1, 2»
«✔ Применить»                → ok=True  «🎓 Уроков записано 2 (действуют СРАЗУ), источник «экзамен»»
читатель ПОСЛЕ тапа:         буллетов 1, прочитана True
```

**Появился ли урок у читателя без единого дополнительного действия человека — ДА.** Между тапом и
чтением не было ничего: ни перевода, ни переноса, ни перезапуска. Читатель — боевая функция,
своего парсера базы в прогоне нет.

Оговорка, названная честно: **право объявлено параметром двери** (`right_fn`, собственный шов
`exam_show._may`), а не переменной окружения. Объявить право окружением — операция класса `env`,
красная на этой полосе; обходить её я не стал. Живой замок при этом опрошен отдельно и ответил
числом: `moderation_core.may_write_rule('knopkaproba') = False` — то есть fail-closed замок жив,
и на пустом списке он отказывает КАЖДОМУ.

## 3. Кандидатом или сразу действующим — ОБА исхода, и решает их ОДНА строка

Один и тот же тап «✔ Применить» дал в моём прогоне **два урока разных состояний**:

```
#1 состояние='актив'    причина='критик, подтверждено тапом: бот не назвал срок (ПРОБА …)'
#2 состояние='кандидат' причина=''
#3 состояние='кандидат' причина=''          ← дверь «✍️ своё»
действующих по боевому отбору active(): 1
```

Развилка стои́т в одном месте — `exam_show._lesson_write` (`exam_show.py:1844`):

```python
if why and EXAM_LESSON_MODE == LESSON_MODE_TRAINING:
    return lesson_store.add(why=why, **common)      # СРАЗУ ДЕЙСТВУЮЩИЙ
return lesson_store.add_candidate(**common)          # КАНДИДАТ
```

`EXAM_LESSON_MODE` по умолчанию `training` (`exam_show.py:119`). Причина берётся из НАБЛЮДЕНИЯ
критика (`apply_marked`, `:1944`): подсказка с наблюдением даёт непустую причину → урок действует
сразу; подсказка без наблюдения и «✍️ своё» (`own_take`, `:2064` — причина пустая литералом) дают
кандидата. Так что верны ОБА ответа, и разделяет их не дверь, а наличие «почему».

**Чем «сразу действующий» отличается от живого набора и где различие записано.** Отличие ровно
одно — ПУТЬ, а не состояние: в живом наборе урок ложится таким же `актив`, но в базу
`exam_live/lessons.tsv`, которую сборка ответа не открывает ни одной веткой. Записано это в двух
местах и обоими словами:

* `exam_show.py:72-74` — «УРОКИ ЖИВОГО НАБОРА НА ОТВЕТЫ БОТА НЕ ДЕЙСТВУЮТ… Перенос урока отсюда в
  боевую базу — отдельное движение владельца, а не побочный эффект тапа»;
* сама дверь говорит это владельцу в момент записи — `own_take`, ветка `SET_NAME == SET_LIVE`
  (`exam_show.py:2077-2084`): «📌 Правилом бота он не становится — эту базу бот не читает».
  У ветки тренажёра (`:2086-2089`) текст другой: «Причина не названа — бот по нему пока НЕ
  отвечает», то есть препятствие названо ДРУГОЕ — не база, а отсутствие «почему».

**Кто и каким событием обязан перевести кандидата в действующие в режиме тренажёра.** Владелец,
словом в теме 205: `pc_agent.lesson_word` → `alog.info("команда урока: …")` → `_lesson_cli`
(`pc_agent.py:933` выбирает дверь, `:940` собирает argv) → `lesson_promote.py --who … --promote N
--why "…"`. Эту самую строку дверь тренажёра печатает владельцу ДОСЛОВНО в квитанции — я видел её
в живом ответе `own_take`:

```
venv/Scripts/python.exe lesson_promote.py --who knopkaproba --promote 3 --why "<почему так правильно>"
```

**Звался ли этот путь хоть раз — НЕТ, ни одного.** Три независимых замера, все сегодняшние:
`команда урока:` — **0** совпадений в каждом из 4 файлов лога агента (02.09→21.09);
`lesson_promote.py --trace` на боевой базе — «След переводов пуст: ни одного урока в действующие
ещё не переводили»; `--candidates` — «Кандидатов нет».

Конец этого пути живым запуском сегодня **НЕ проверен**, и слеп я ровно правом: дверь перевода,
позванная на моей базе, ответила `⛔ Нет прав на перевод уроков — урок #3 НЕ включён. Включение
требует того же права, что и запись.` Объявить право можно только через окружение, а это красная
операция класса `env`. (Предыдущий заход 70f, объявивший право, получил от той же двери второй
замок — «урок #1 не кандидат, а «актив»», — то есть дверь проходит проверку права и дальше судит
состояние; это чужой замер, мною сегодня не повторённый.)

## 4. Тап 12.09 в 18:10:01 — ВВОД НЕ ЗАВЕРШЁН (три исхода не слиты)

Три исхода разведены поимённо, каждый со своим доказательством:

| исход | вердикт | чем доказан |
|---|---|---|
| **строка написана** | **НЕТ** | боевая база: 9 строк, все `перенос_книги`, строк источника «экзамен» — **0**. Журнала вердиктов тренажёра `exam_verdicts.tsv` **не существует как файла** — ни одного вердикта тренажёра не записано никогда |
| **запись ушла в другое место** | **НЕТ, и уйти было некуда** | вторая база уроков (`LIVE_LESSONS`) заведена коммитом `96f984c0` **17.09.2026 23:43**, на пять суток ПОЗЖЕ тапа. 12.09 у кнопки был один адрес — боевая `lesson_store.tsv` |
| **ввод не завершён** | **ДА** | стол `exam_session.json` застыл на **12.09 18:10** ровно в том состоянии, которое оставляет `own_start`: `{"case":"1", "own_until":1789212002, "own_who":"SamHold", "selected":[]}`. `own_until` = **12.09 18:20:02 местного** (11:20:02 UTC), то есть тап был в **18:10:02** — секунда в секунду с колбэком лога |

Разбор по следам, шаг за шагом:

1. **Тап состоялся и дверь отработала успешно.** `pc_agent.log.2:28574` —
   `2026-09-12 18:10:01,060 | INFO | chain-callback: data='exam:own:1' uid=504608015 → action=own ok=True`.
   Это ЕДИНСТВЕННЫЙ exam-колбэк тренажёра за все четыре файла лога (07.09→21.09): их ровно **1**.
2. **Ожидание открылось.** `own_start` пишет на стол `own_until`/`own_who` и живёт 10 минут
   (`PENDING_TTL_SEC`). Оба ключа на столе, `mtime` файла — 12.09 18:10.
3. **Текста в окно не пришло.** Ловец текста в агенте (`pc_agent.py`, ветка `chat.id ==
   EXAM_CHAT_ID`) пишет в лог строку `экзамен (%s): ловлю урок своими словами (%d симв.)`.
   Совпадений в `pc_agent.log.2` (охватывает 12.09) — **0**. Первая поимка за всю историю —
   **18.09 23:13:09**, и она набора `live`; всего поимок **4**, набора `trainer` среди них **0**.
   Ловец при этом уже существовал: он приехал коммитом `56ca76aa` **12.09 13:00:30**, за пять
   часов до тапа, — то есть «кода ещё не было» объяснением не является.
4. **Демон в это время был жив.** Между 18:06 и 18:16 в логе ровно тикает `_cowork_sync_job`
   каждые 30 секунд без единого пропуска: молчание в окне — это молчание владельца, а не
   остановка агента.
5. **`own_take` не отрабатывал ни разу.** Он снимает со стола `own_until` и `own_who`
   (`exam_show.py:2070-2073`). Оба ключа на месте девять суток спустя.

**Итог п.4:** тап открыл ожидание, владелец текста не прислал, ожидание протухло молча через 10
минут. Ни урока, ни вердикта, ни записи «мимо» — потому и нет в боевой базе ни одной строки
источника «экзамен». Отдельно назову то, чего следами НЕ различить: «владелец не стал писать» и
«написал не в тот чат / не тем аккаунтом» дают в логе одну и ту же тишину (ветка `chat.id !=
EXAM_CHAT_ID` возвращается молча и не логирует). Это остаток, а не вывод.

## 5. Отрицательный тест: сломал ровно то, что прошло на п.2

Ломался шаг п.2 — «кнопка пишет в ту же базу, которую читает читатель». Слом сделан тем же
механизмом, каким он случается по-настоящему у живого набора: база записи разведена с базой
чтения, а дверь осталась та же самая.

```
читатель ИСХОДНОЙ базы: было (1, True), стало (1, True)     ← не вырос
читатель ДРУГОЙ базы:   (1, True)                            ← 0 → 1: урок НАПИСАН, просто не туда
```

Числа читаются так: тап отработал (`ok=True`, «🎓 Уроков записано 1 (действуют СРАЗУ)»), урок лёг
действующим — и всё равно до читателя исходной базы не доехал. Была бы база одна, читатель дал бы
**2**; он дал **1**. Контроль «а не пустой ли был тап» закрыт второй строкой: в другой базе
буллетов стало **1**, то есть писать дверь не переставала.

**Проверка поломку ЗАМЕТИЛА**, значит числа п.2 опубликованы законно. Вторая проба — на третий
исход: читатель, направленный на несуществующий путь, ответил **(0, False)**, то есть «действующих
нет» и «источник не прочитан» он не путает.

## 6. Исходы тремя словами

* **ДОШЛО** — вердикт владельца, поставленный в наборе ТРЕНАЖЁРА тапом по подсказке критика,
  доезжает до читателя ответа САМ: 0 → 1 буллет, ноль действий человека между. Переноса на этом
  пути нет и не требуется — база одна, `D:\turbobaby-bot\lesson_store.tsv`.
* **НЕ ДОШЛО** — вердикт, у которого нет «почему» (тап по подсказке без наблюдения, «✍️ своё»):
  он ложится КАНДИДАТОМ и правилом не становится, пока владелец не скажет `урок включи N`. Цепь
  тут не порвана, а разомкнута НАМЕРЕННО; но живого слова за 19 суток не сказано **ни разу**, и
  след переводов боевой базы **пуст**, так что практически этот отрезок не проехал никто.
* **НЕ СМОГ ПРОВЕРИТЬ** — конец пути кандидата (сам перевод живой дверью). Причина своими словами:
  дверь `lesson_promote.py` спрашивает право, право объявляется ТОЛЬКО переменной окружения, а это
  красная операция класса `env` на этой полосе; обходить её я не стал. Дверь при этом жива и
  ответила отказом дословно — слеп я правом, а не дверью.

Отдельной строкой, чтобы не слилось с предыдущим: **вопрос «почему тап 12.09 не оставил строки»
закрыт полностью и НЕ входит в «не смог проверить»** — ввод не был завершён, доказательства в §4.

## Временное место, откат источника и что наружу не ушло

* **Временное место — одно, названное заданием:** `tmp/trener_knopka_2209/` (`knopka_live.py`,
  `cases.json`, `shots/`, `desk.json`, `verdicts.tsv`, `lessons.tsv` + `.bak`/`.version`,
  `lessons_drugaya_baza.tsv`). Другого не заводил. **Не удалено ничего**, в том числе за собой.
* **Как этот источник откатывается ЦЕЛИКОМ** — той же боевой дверью, что и любой набор уроков,
  и предварительный показ снят живьём (чтение, права не требует):

  ```
  venv/Scripts/python.exe lesson_batch.py --path tmp/trener_knopka_2209/lessons.tsv \
      --who <имя> --off "экзамен" --count 3 --why "проба KNOPKATRENAZHERA 22.09 снята"
  ```
  `--show "экзамен"` сегодня ответил: «📦 Набор «экзамен» — **3** строк(и)… Снять сейчас можно: 3…
  Залит: 2026-09-21T16:19:41Z. Записал: @knopkaproba». Во второй базе (`lessons_drugaya_baza.tsv`)
  тем же ключом лежит **1** строка. Возврат — `--back "экзамен"`. Само снятие сегодня не
  выполнялось: оно требует того же права, что и запись (класс `env`), и цели у него нет — ни одна
  из четырёх строк не лежит в боевом файле.
* **Отметка происхождения** — в самих строках: текст каждого правила начинается с `ПРОБА 22.09`,
  автор `@knopkaproba`, источник `экзамен`, заливка «без номера», залито 2026-09-21T16:19:41Z.
* **Боевого не тронуто на запись ни байта.** Боевая `lesson_store.tsv` открывалась ТОЛЬКО на
  чтение (перепись источников, `--candidates`, `--trace`) и осталась прежней: 9 строк, все
  `перенос_книги`, все `актив`. Живой экзаменационный набор владельца (`exam_live/`) не
  открывался ни одним файлом — граница, которую я всё же коснулся и называю честно: один раз
  выведен СПИСОК каталога (имена и размеры), ни один файл не прочитан, и ни одно утверждение
  этого артефакта на живом наборе не стои́т.
* **Путь до человека закрыт и посчитан:** отправитель (`sender`), проверка агента (`agent_fn`) и
  прибор регрессии (`regress_fn`) переданы дверям ЗАГЛУШКАМИ. Заглушка отправителя звалась **2**
  раза и оба раза вернула «наружу ничего не ушло»; агент — **2**; прибор регрессии — **3**, ни
  одного процесса не порождено. Карточек владельцу, сообщений в тему и строк в инбокс за заход —
  **0**. Боевой вызов от проверки отличается ровно этими тремя параметрами, и различие видно в
  выводе прогона словами, а не подразумевается.
* **Процессы не трогались, `.env` и конфиги не читались, тестов боевой базы не запускалось.**

## Что из этого следует (решение НЕ принимаю — задание просило ответ, а не правку)

Две вещи стои́т знать тому, кто будет решать дальше. Первая: **чинить на пути тренажёра нечего** —
он замкнут, и молчание базы объясняется не обрывом, а тем, что по нему за всё время проехал ровно
один тап, и тот не был доведён до конца. Вторая: **единственный практический разрыв тренажёра —
кандидат без «почему»**; его переводит слово владельца, которое не звучало ни разу за 19 суток.
Это не дефект кода: дверь жива, строка перевода печатается владельцу дословно в момент записи.
