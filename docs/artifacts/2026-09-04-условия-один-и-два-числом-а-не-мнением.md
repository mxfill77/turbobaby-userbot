# Условия один и два — числом, а не мнением (04.09.2026)

**Заход измерительный.** Кода не правил, процессов не трогал, падений не подстраивал, ничего не
удалял. Временное — `_scratch_children_alive_0904/` (одна расшифровка cp1251-лога).
Ось — бизнес: это условия 1 и 2 из четырёх, без которых менеджеры не подключаются.

---

## 0. Признак сделанности (первым действием)

Адрес результата — файл в `docs/artifacts` за 04.09 про условия один и два числом. **Файла не
было**: за 04.09 лежал единственный артефакт `2026-09-04-сутки-ящика-по-местному-времени-и-потолок-восемь.md`
(другая тема — ящик заданий). Вопросы задания в нём не отвечены ни одним. Работал с нуля.

---

## 1. Премиса Штаба — проверена, наполовину неверна

| премиса Штаба (04.09 ≈02:15, беглая) | замер | вердикт |
|---|---|---|
| «модербот за последние пять суток стартовал **трижды**: 30.08, 31.08, 03.09» | боевых запусков **пять**: 30.08 02:38:55 · 30.08 19:55:51 · 30.08 20:38:51 · 31.08 19:43:56 · 03.09 05:04:01 | **неверна** — считали ДНИ, а не старты; 30.08 их три |
| «похоже на „не падает по кругу“» | по кругу действительно не падает, но **последний круг был и стоил 86 ч 45 мин простоя** (26.08→30.08) | верна по форме, опасна по выводу |
| «про наблюдателей за детьми неизвестно ничего» | наблюдателей **два**, и они дают ПРОТИВОПОЛОЖНЫЕ ответы (§3) | верна — теперь известно |

Мина счёта, на которой родилась премиса: баннер `===== moderbot: старт …` в
`logs/moderbot_stderr.log` — это **факт спавна процесса, а не подъёма бота**. За окно 25.08–04.09
баннеров 11, из них **6 холостых** («уже запущен (PID 18200) — второй не поднимаю, выхожу»).
Считать по баннерам — завысить вдвое.

---

## 2. УСЛОВИЕ 2 числом: сколько раз модербот падал САМ

### Окна (названы и обоснованы)

| окно | источник | длина | почему такое |
|---|---|---|---|
| **А — жизнь судьи** | `pc_orchestrator.log` 22.07 23:11:31 → 04.09 04:03:02 | **43.2 суток** | единственный файл, где есть И смерть, И вердикт о ней. Раньше 22.07 вердиктов нет вовсе — файл там начинается |
| **Б — жизнь субъекта** | `logs/moderbot_stderr.log` 10.07 17:26 → 03.09 05:04 + `moderation_bot.log` 03.07 → 03.09 | **55.5 суток** | сюда и только сюда упала бы трассировка самопадения (`pc_agent.py:108-127`, фикс #128) |
| **В — окно премисы** | 30.08 → 04.09 | 5 суток | чтобы ответить Штабу его же мерой |

### Числа

**Самопадений с трассировкой — НОЛЬ.** `logs/moderbot_stderr.log`: `Traceback|CRITICAL|Unhandled`
= **0 совпадений за 55.5 суток**. В `moderation_bot.log` трассировок 4 — все июльские, ни одной
после 25.08. Канал, специально построенный ловить самоубийство ребёнка, **пуст**.

**Сетевые обрывы падением не являются и не становятся:** строк
`сетевая ошибка (не падаю, PTB переподключится): NetworkError` в окне десятки (Bad Gateway,
httpx.ReadError) — ни одна не заканчивается смертью процесса.

**Смертей, зафиксированных судьёй (окно А):** `контур-вотчдог: moderation_bot МЁРТВ` — **20**,
плюс **3** перехода `умер 3 раз подряд — СТОП попыток`. Для сравнения: `userbot МЁРТВ` — 7,
`pc_agent МЁРТВ` — 4. Итого по всем троим детям — **31 вердикт смерти**.

**Причины 20 смертей модербота — разбор каждой по источнику простоя, который вотчдог печатает сам:**

| причина | сколько | чем доказана |
|---|---|---|
| **перезагрузка Windows** (умирало ВСЁ дерево разом) | **10** | пометка `[перезагрузка]` в строке вердикта + синхронные баннеры userbot (`30.08 20:37:43`, `31.08 19:38:29`, `03.09 05:02:13`) + парные улики `tmp/stale_locks/{pc_agent,userbot,pc_orchestrator}.lock.*` с теми же метками |
| «наблюдение» — верхняя граница, момент смерти внутри окна | 1 | пометка `[наблюдение]`, 26.08 14:02 |
| момент не локализуется (дыра наблюдений) | 4 | «лежал неизвестно …» — 26.08 12:27, 26.08 14:17, 29.08 11:58, 29.08 12:15 |
| старая форма (до фикса `absence_since` 19.08) — источник не назывался | 3 | 06.08 19:44 «121942с», 18.08 14:39, 18.08 20:34 |
| **ручной цикл стоп+старт** — не падение вовсе | 1 (сверх 20) | `pc_agent.moderbot_cycle.log`: `19:55:25 остановлен, PID [9360]` → `19:55:50 запущен агентом, PID 16348`; вердикта `МЁРТВ` на 19:55 в логе демона **нет** — сторож его и не считал |

**Отдельно про 30.08 19:55 — единственный «одиночный» подъём в окне В.** userbot в этот час не
перезапускался (его баннеры 30.08 02:34:06 и 20:37:43), то есть дерево не умирало. Это не
самопадение: помимо строки цикла есть вещдок формата — лок, изъятый в 19:55:50
(`moderation_bot.lock.20260830-195550.txt`), содержит **4 байта** (голый PID `9360`, старая
форма), а лок, написанный поднятым в 19:55:51 экземпляром, — **181 байт JSON**. Коммит, вводящий
JSON-лок (`2f015bb` «личность процесса вместо голого номера»), лёг в **20:31**, на 36 минут ПОЗЖЕ.
Значит в 19:55 крутили новый код из рабочего дерева — это работа человека, а не смерть.

**Пережил ли без человека.** Из 20 вердиктов подъём взялся (`PID после` непустой) в **14**, не
взялся (`PID после=[]`) в **6**. Шесть невзявшихся — это два эпизода, 26.08 и 29.08, и оба
кончились сдачей сторожа (`умер 3 раза подряд — СТОП`).

### Цена, которой условие 2 меряется на самом деле

**Модербот лежал 86 ч 45 мин подряд** — с 26.08 ≈11:34 (последняя его собственная строка в
`moderation_bot.log`) до 30.08 02:38:55 (первый удавшийся `ЗАПУСК`). **3.6 суток.** Всё это время
сторож видел смерть верно, а поднять не мог: лок держал номер 18200, переиспользованный Windows'ом
чужим живым процессом, и каждый новый экземпляр выходил со словами «уже запущен (PID 18200)»
(6 таких строк — 26.08 ×4, 29.08 ×2). Разбор класса:
[`docs/artifacts/2026-08-27-moderbot-three-failed-starts.md`](2026-08-27-moderbot-three-failed-starts.md).
Кончилось это **перезагрузкой** 30.08, которая обнулила чужой PID, — а не действием сторожа.

### Вердикт по условию 2

> **ВЫПОЛНЕНО в узком смысле «падает сам» и НЕ ВЫПОЛНЕНО в смысле «переживает без человека».**
> Снято: 0 самопадений с трассировкой за 55.5 суток при 20 зафиксированных смертях, из которых
> 10 доказанно от перезагрузки ПК, 1 — ручной цикл; своей вины модербота в окне не нашлось ни
> одной. И одновременно: 6 подъёмов из 20 не взялись, три раза сторож сдался, последний эпизод
> стоил **86 ч 45 мин** непрерывного простоя и потребовал перезагрузки машины.
> Формулировка «модербот перестал падать сам» ПРАВДИВА и при этом БЕСПОЛЕЗНА для решения о
> менеджерах: он не падает — его **уносит**, и поднимается он не всегда.

---

## 3. УСЛОВИЕ 1 числом: ловили ли наблюдатели ЖИВОЕ падение

Наблюдателей за детьми на полосе ПК **два**, и их ответы противоположны. Ровно здесь заход и
провалился бы, если считать «прибор сработал» за «поймал живое падение».

### 3.1 Контур-вотчдог демона (`pc_orchestrator.client_watchdog_tick`, тик 5 мин внутри `poll_once`)

**ЛОВИЛ. Живое падение, названное датой, ребёнком и записью:**

| дата | ребёнок | запись-доказательство | последствие |
|---|---|---|---|
| **26.08.2026 11:54:04** | **moderation_bot** | `pc_orchestrator.log`: `контур-вотчдог: moderation_bot МЁРТВ (лежал 6 м 48 с [перезагрузка], лог молчит 1181с, смерть 1/3) → подъём ok=True, PID после=[]: запустил, но процесс не виден` | карточка владельцу `dispatch_notify.log 11:54:06 итог(критич): channel=inbox ok=True \| ⚠️ ПОДЪЁМ moderation_bot НЕ ВЗЯЛСЯ — лежит 6 м 48 с` |
| **26.08.2026 12:45:57** | moderation_bot | `ERROR контур-вотчдог: moderation_bot умер 3 раз подряд — СТОП попыток, нужен разбор` | `12:45:59 итог(критич): inbox \| ⚠️ Оркестратор: moderation_bot умер 3 раза подряд` |
| **03.09.2026 05:04:10** | moderation_bot | `МЁРТВ (лежал 2 м 40 с [перезагрузка], лог молчит 3401с, смерть 1/3) → подъём ok=True, PID после=[16896]` | `05:04:13 итог(тема): topic:328 \| 🔁 СТОРОЖ ПОДНЯЛ moderation_bot — лежал 2 м 40 с`; **этот PID 16896 живёт прямо сейчас** — `moderation_bot.lock` |
| **05.08→06.08 19:44:10** | moderation_bot | `МЁРТВ (лежал 121942с …) → подъём ok=True, PID после=[11968]` | 33.9 ч простоя, тот самый случай «наблюдатель жил внутри вставшего демона» |
| **23.08 06:08:54** | **pc_agent** | `МЁРТВ (лежал не дольше 3 м 25 с [наблюдение] …) → schtasks /Run /TN pc_agent rc=0` | подъём чужой дверью — Планировщиком |
| **05.08 01:19:00** | **userbot** | `МЁРТВ (лежал 331с, смерть 1/3) → подъём ok=True, PID после=[16412]` | |

**Числом за окно А (43.2 суток):** вердиктов смерти **31** (модербот 20 · userbot 7 · pc_agent 4),
переходов «умер 3 раза подряд — СТОП» — **4** (модербот 3, userbot 1), карточек владельцу по
детям — **20 строк в `dispatch_notify.log`, 1:1 с вердиктами**, из них **9 критических в инбокс**
за 26.08–29.08.

**Почему это НЕ «прибор записал собственную проверку»** — три независимых подтверждения на ту же
смерть: (1) физическая улика на диске — файл изъятого лока `tmp/stale_locks/moderation_bot.lock.*`
с номером мёртвого процесса внутри; (2) дыра в собственном логе ребёнка `moderation_bot.log`
ровно на объявленный простой; (3) доставленная карточка в `dispatch_notify.log` с `ok=True`.
Прибор, судящий сам себя, второго и третьего следа не оставляет.

**Третий исход по контракту — ЕСТЬ и работает на живых данных.** `absence_since`
(`pc_orchestrator.py:7249-7280`) даёт ровно три ответа, и средний не равен первому:
`перезагрузка` (число точное) · `наблюдение` (число названо ВЕРХНЕЙ границей, слово «не дольше») ·
`None` → **«лежал неизвестно (в дыре наблюдений момент исчезновения не локализуется)»**. Последнее
не выдумано ради отчёта: **4 живых вердикта из 20** его напечатали (26.08 12:27, 26.08 14:17,
29.08 11:58, 29.08 12:15). Причина незнания намеренно без числа — иначе владелец прочтёт возраст
лога как длительность простоя, а именно так 18.08 родилось «лежал 5 ч 54 м» о простое 6 м 32 с
(завышение ×53): [`docs/artifacts/2026-08-18-moderbot-deaths-1.md`](2026-08-18-moderbot-deaths-1.md).

**Живого от свежего следа покойника отличает** — вето подъёма по свежему логу
(`лог свеж ≤ CLIENT_LOG_STALE → смерть НЕ доказана`) плюс `_find_pids_by_script` (CIM по
командной строке, не по локу). Слабое место названо самим кодом и стоило 3.6 суток: **вето стоит
на СВЕЖЕСТИ ФАЙЛА, а «уже запущен» решается по ЛОКУ**, и переиспользованный номер 18200 пробил
именно вторую дверь, а не первую.

### 3.2 Слой ожиданий О3/О5 (`expectations_pc.kid_state`, задача `TurboBabyExpectPC`, тик 10 мин)

**ЖИВОГО ПАДЕНИЯ НЕ ЛОВИЛ НИ РАЗУ — и это не «не ловил», а измеренный ноль.**

- Карточек владельцу от слоя про смерть ребёнка — **0** (`dispatch_notify.log`, 63 суток файла:
  упоминания О3 есть только в итогах Code-сессий 11.08 и 18.08 — «поднято ожидание», то есть
  новости о САМОМ приборе, а не о ребёнке).
- Строк «дети контура …» в канале карточек — **0**; они идут в журнал (`maybe_kids_pulse` →
  `cowork_log`), и ни одна найденная в `docs/artifacts/journal/` не говорит «работы нет» о живом
  ребёнке.
- Единственное доказательство, что ветка приговора вообще работает, — **отрицательный тест на
  ПРОВЕРОЧНОЙ СУЩНОСТИ**, дословно из FACT задачи #76 (18.08): «отрицательный тест EXIT=0 — **на
  проверочной сущности (не на боевых процессах)**: запись возрастом 400 с, общий канал свеж 1.0 с,
  процесса из лока нет → вердикт `'работы нет'`». Это проверка прибора, а не пойманная смерть.
- Хуже: **21 строка «дети контура» из 26 за сутки была тест-происхождения** до фикса 19.08
  (`d43d8b9`+`fdcbcfa`, журнальная запись `2026-08-19-080001-done.md`). То есть до 19.08 записи
  прибора о детях в основном были его собственным эхом.
- Живой замер сейчас (`tmp/expect_pc/state.json`, 04.09 03:55): `kids.sig =
  pc_agent=делает работу|userbot=делает работу|moderation_bot=делает работу`, `ok: true`.
  Отрицательного исхода в боевом состоянии нет.

**Провал по существу:** во время 86-часового простоя 26.08→30.08 модербот был доказанно мёртв,
`meta['heartbeat']` протух многократно сверх порога 900 с — и слой ожиданий **не оставил ни одной
записи и не разбудил никого**. Смерть заметил и озвучил только контур-вотчдог.

**Третий исход у слоя — ЕСТЬ, и он сильнее, чем у соседа** (по коду, не по записям):
`kid_state` (`expectations_pc.py:920-999`) — восемь веток, из них **пять ведут в
`MOD_UNKNOWN`**; `_kid_fresh` (`:907-917`) отдаёт «делает работу» ТОЛЬКО при `who is True`, то есть
при подтверждённой живой пробе процесса, иначе «свой продукт не стар, но жив ли процесс — не
подтверждено». `kid_writer` (`:856-885`) склеивает ответ из ДВУХ положительных фактов — процесс с
номером из лока существует **И** запущен тогда же, когда написан лок (замок против
переиспользования PID), и возвращает True/False/**None**; ни один провал чтения не даёт True.

**Свежего покойника от живого он отличает — веткой 5** (`who is False` → «работы нет», `dead=True`,
`expectations_pc.py:981-984`). Ветка заведена не в теории, а по живому провалу, названному в самом
коде (`:467-469`): *«Живой провал 18.08: модербота унесло перезагрузкой, а его собственная
последняя запись осталась на диске свежей — прибор сказал „неизвестно“ о заведомо мёртвом ребёнке,
а двух других не судил вовсе»*. С 18.08 у каждого ребёнка свой источник и свой предел
(`KID_SIGNS`, `:484-516`), а 11 признаков перечислены как **явно отвергнутые** (`KID_REJECTED`,
`:520-536`) — каждый шевелится под нагрузкой и замирает в простое, то есть врёт ровно тогда, когда
нужен.

### 3.3 Перепись всех наблюдателей за детьми и их третьего исхода

Наблюдателей за детьми на полосе оказалось **16**, а не два: 10 судят ЖИЗНЬ процесса, 4 — ПРОДУКТ,
1 — КОД в памяти ребёнка, 1 пересказывает. Третий исход есть у **13 из 16**. Три места, где его
НЕТ, и слепота пробы схлопывается в «мёртв» — это готовые ложные приговоры:

| место | строки | чем опасно |
|---|---|---|
| `pc_agent._find_userbot_pids` / `_find_moderbot_pids` | `pc_agent.py:149-151`, `:314-316` | `except → return []`: таймаут CIM читается как «не запущен», и авто-реадопшн (`:1040-1042`) поднимет ребёнка поверх живого |
| `pc_orchestrator._proc_line` («статус контура») | `pc_orchestrator.py:2392` | `_find_pids_by_script` даёт `None` при слепом CIM, а `None` falsy → владельцу печатается **«НЕ ЖИВ»**, неотличимо от честной пустоты. Тот самый класс #171, вычищенный в вотчдоге и не вычищенный здесь |
| `rc_supervisor.log_age` / `established_conns` → `channel_alive` | `rc_supervisor.py:329-330`, `:343-344`, `:356-365` | оба провала → «зомби»; спасают только 3 страйка подряд и грейс, а не третий исход |

Сильнейший по контракту — `proc_identity.judge` (`proc_identity.py:382`): исходов **четыре**
(`OURS_ALIVE` · `STALE` · `HELD_UNVERIFIED` · `UNKNOWN`), и `OpenProcess` на **завершившийся**
процесс успешно открывается и честно отдаёт прежний момент старта — поэтому проверяется
`GetExitCodeProcess` (`:213-215`). Это и есть код, которым полоса умеет отличить живого от следа.

### Вердикт по условию 1

> **ВЫПОЛНЕНО — но только одним наблюдателем из двух.**
> Снято: контур-вотчдог демона поймал **31 живую смерть детей за 43.2 суток** (модербот 20,
> userbot 7, pc_agent 4), четырежды дошёл до «сдаюсь, нужен разбор» и доставил владельцу
> **20 карточек, 9 из них критических в инбокс**; каждая смерть подтверждена тремя независимыми
> следами (изъятый лок · дыра в логе ребёнка · доставленная карточка). Третий исход у него есть и
> напечатан на живых данных 4 раза из 20.
> И одновременно: **слой ожиданий О3/О5 живого падения не ловил ноль раз из ноль**. Его
> единственное отрицательное доказательство — тест на проверочной сущности. В 86-часовом простое
> 26.08→30.08 он промолчал. Считать его вклад в условие 1 — это ровно та ошибка, о которой
> предупреждал предсмертный взгляд задания.

---

## 4. Сводка одной таблицей

| условие | вердикт | чем снято |
|---|---|---|
| **1. Наблюдатели за детьми ловят живое падение** | **ВЫПОЛНЕНО** (вотчдогом) · **НЕ ВЫПОЛНЕНО** (слоем ожиданий) | 31 вердикт смерти за 43.2 сут · 20 карточек владельцу · 3 независимых следа на каждую · третий исход напечатан 4 раза ↔ 0 записей и 0 карточек от О3/О5, отрицательный тест только на проверочной сущности |
| **2. Модербот перестал падать сам** | **ВЫПОЛНЕНО** («сам не падает») · **НЕ ВЫПОЛНЕНО** («переживает без человека») | 0 трассировок за 55.5 сут · 10 из 20 смертей от перезагрузки · 1 ручной цикл ↔ 6 подъёмов из 20 не взялись, 3 сдачи сторожа, простой 86 ч 45 мин 26.08→30.08, вылеченный перезагрузкой |

---

## 5. Заявки следующему заходу (в этом заходе чинить нельзя)

1. **Дыра в вете подъёма.** Вето стоит на свежести ЛОГА, а «уже запущен» решается по ЛОКУ —
   переиспользованный номер пробивает вторую дверь. Цена уже измерена: 86 ч 45 мин. Лок с 30.08
   несёт личность процесса (`2f015bb`), но проверял ли эту личность путь «уже запущен» —
   не мерено.
2. **Слой ожиданий молчал 86 часов о мёртвом ребёнке.** Почему `kid_state` не дал «работы нет»
   26.08–30.08 при протухшем `meta['heartbeat']` — НЕИЗВЕСТНО. Гипотезы (не проверены):
   наблюдатель не тикал; `who` вернул True на переиспользованном 18200; исход был, но говорить
   его некому — у О3 нет карточки, только строка журнала раз в 6 ч.
3. **Учёт спавнов дырявый.** Запуск `2026-08-30 19:56:14` есть в `moderation_bot.log` и
   отсутствует баннером в `logs/moderbot_stderr.log` — прошёл мимо `pc_agent._child_log_handle`.
   Сколько таких ещё — не считано.
4. **`pc_orchestrator.child_raise_commit.json` держит запись 03.09 12:11:22 «door: контур-вотчдог
   демона»**, которой не соответствует ни один подъём в логах (боевой был в 05:04). Похоже на
   след ТЕСТА до фикса изоляции `f7e0e63` (13:23 того же дня) — то есть память подъёмов могла быть
   затёрта прогоном тестов. Не проверено.
5. **`logs/moderbot_stderr.log` смешанной кодировки** (UTF-8 баннеры + cp1251 тело) — любой
   будущий счёт по нему обязан это учесть либо считать по `moderation_bot.log`.
6. **Три наблюдателя без третьего исхода** (§3.3) — слепая проба у них равна приговору «мёртв».
   Дороже всех `_proc_line`: владелец читает «НЕ ЖИВ» о ребёнке, о котором прибор просто не смог
   спросить. Правка — на отдельный заход, здесь кода не трогаем.
7. **Условия 3 и 4** из четырёх не трогались этим заходом вовсе.

---

## 6. Что НЕ доказано этим заходом (честно)

- Не доказано, что модербот не может упасть сам: доказано лишь, что **за 55.5 суток не падал** и
  что канал для такой трассировки существует и пуст.
- Причина 4 смертей из 20 остаётся «в дыре наблюдений» — сам прибор так и говорит, и это его
  правильный ответ, а не пробел замера.
- Смерть САМОЙ машины ни один из двух наблюдателей не судит: оба живут на ПК.
- Живых процессов не трогал, падений не подстраивал, боевых записей не делал.
