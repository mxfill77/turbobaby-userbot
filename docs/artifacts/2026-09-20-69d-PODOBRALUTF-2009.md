69d PODOBRALUTF 2009

# Осиротевшую работу 68v подобрать НЕ УДАЛОСЬ ЗАКОННО: io_utf8.py — клиентский файл, и признак гарда говорит это числом

**FACT — коммита НЕТ, и это названная заданием законная ветвь, а не провал:**

| что | число / адрес | прочитано или вывод |
|---|---|---|
| вердикт | `client_contour.is_client("io_utf8.py")` → **True** | **прочитано** живой функцией |
| замыкание, которым судит гард | **50 файлов + 25 имён данных = 75**, `ok=True` | **прочитано** |
| путь в замыкании | `userbot_listen.py` / `moderation_bot.py` → `trainer.py` → `lesson_store.py` → **`io_utf8.py`** | **прочитано** обходом графа |
| два других файла | `exam_show.py` = **False**, `test_utf8_output_guard.py` = **False** | **прочитано** |
| коммит | **не сделан ни один**; HEAD как был — `1f6ff232` | **прочитано** |
| замок самообновления | остаётся; держит его **ОДИН** файл из трёх — `io_utf8.py` | **прочитано** |
| полный регресс | **Ran 7968 за 840.267 с · FAILED (failures=43, skipped=12)** | **прочитано** |
| принадлежность 43 провалов | **ни один не от трёх файлов**: 11 — только в общем прогоне (`test_exam_show` в одиночку **195 OK**), 32 — красны и в одиночку в 9 чужих модулях | **прочитано** одиночными прогонами |

Место для временного: `tmp/podobral_utf/` (вывод регресса и список модулей). Ничего не удалено.

---

## 0. Признак сделанности

Первым действием открыт `docs/artifacts/2026-09-20-69d-PODOBRALUTF-2009.md` — **файла не было**
(`ls` → «No such file or directory»; за 20.09 в каталоге лежало 20 других артефактов, этого среди
них нет). Ни один из пунктов 2–5 заранее не отвечен — работа начата с нуля.

## 1. Премиса Штаба — вердикт по каждой строке отдельно

| строка премисы | исход | чем измерено |
|---|---|---|
| «грязны ровно эти три файла и никакие другие» | **ВЕРНА** для отслеживаемых — и **НЕПОЛНА** про работу 68v (см. ниже) | `git status --porcelain --untracked-files=no` → ровно `M exam_show.py`, `M io_utf8.py`, `M test_utf8_output_guard.py` |
| «вместе +170/-4» | **ВЕРНА** точно | `git diff --numstat`: 14/3 + 40/0 + 116/1 = **170 добавлено, 4 удалено** |
| «io_utf8.py входит в кортеж верхних импортов демона» | **ВЕРНА** | `import io_utf8` — `pc_orchestrator.py:52`; кортеж `_ORCH_RUNTIME` — `:7440`, 23 имени; литерал `"io_utf8.py"` — `:7537` |
| «индекс пуст» | **ВЕРНА** | `git diff --cached --name-only` → **0 файлов** |
| «io_utf8.py держит самообновление с 19:11 местного» | **ВЕРНА дословно** | `pc_orchestrator.log:5028` — `2026-09-20 19:11:17,615 ERROR self-update демона: дерево ГРЯЗНОЕ — авто-рестарт «демон (self-update)» ЗАПРЕЩЁН; файлы: io_utf8.py` |
| «за замком стоят два готовых коммита» | **ВЕРНА по коду**, и уточняется числом | демон стои́т на `5c2ddfcd` (старт 16:58:08); `git rev-list --count 5c2ddfcd..HEAD` = **12**, но `.py` несут ровно **два**: `ff989bb1` (`pc_orchestrator.py`) и `336a6099` (`review_send_run.py`). Остальные 10 — артефакты и тела журнальных записей |

### Поправка, которой в премисе нет: работа 68v — ЧЕТЫРЕ файла, а не три

Четвёртый — **`test_exam_own_stdin_utf8.py`**, и он **не отслеживается git вовсе**
(`git ls-files --error-unmatch` → UNTRACKED; `git log -- <файл>` → пусто). Это не случайный сосед:

* размер **17962 Б**, mtime **2026-09-20 16:46:38** — та же минута, что у `io_utf8.py` (16:46:30);
* собственный артефакт 68v называет его своим дословно — строка 322:
  «`test_exam_own_stdin_utf8.py` | новый: живой путь записи на копии + мутант + статика», и
  строка 141: «Файл `test_exam_own_stdin_utf8.py`, 6 проверок»;
* он единственный покрывает новую функцию живым путём (`read_stdin_utf8` встречается в нём 7 раз).

**Почему это важно для решения, а не для красоты.** Коммит «ровно трёх файлов», как просило
задание п.4, оставил бы вне git именно тот тест, который стережёт новую функцию живым путём.
Селективный гейт его бы и не позвал: `gate_selective.affected_test_modules` для трёх изменённых
отдаёт `['test_exam_show', 'test_utf8_output_guard']` — правило `test_<stem>`, а `test_exam_own_stdin_utf8`
под него не подходит. То есть коммит трёх файлов родил бы код, чей страж есть на диске и
невидим ни git'у, ни гейту.

## 2. Клиентское замыкание — ТОЙ САМОЙ функцией, и она названа адресом

**Адрес функции:** `client_contour.is_client(path, repo=None, cl=None)` — `client_contour.py:280`,
опирается на `client_contour.closure(repo, entries, cut)` — `client_contour.py:257`.
Это не греп по именам: `closure` строит транзитивное import-замыкание обходом `ast` от живых
клиентских входов.

| параметр | значение | адрес |
|---|---|---|
| входные точки | `("userbot_listen.py", "moderation_bot.py")` | `CLIENT_ENTRIES`, `client_contour.py:108` |
| срез | `("pc_orchestrator.py", "pc_agent.py")` | `FOREIGN_ENTRIES`, `client_contour.py:112` |
| исход построения | `ok=True`, `reason='ok'` | замер |
| **размер замыкания** | **50 файлов + 25 имён данных = 75** | замер |

**Вердикт по каждому из трёх:**

| файл | `is_client` | в `files` | в `data` |
|---|---|---|---|
| `exam_show.py` | **False** | нет | нет |
| **`io_utf8.py`** | **True** | **да** | нет |
| `test_utf8_output_guard.py` | **False** | нет | нет |

**Путь, которым io_utf8.py попадает в замыкание** (кратчайший, обход в ширину от каждого входа):

```
userbot_listen.py  -> trainer.py -> lesson_store.py -> io_utf8.py
moderation_bot.py  -> trainer.py -> lesson_store.py -> io_utf8.py
```

Ребро реальное, а не через данные: внутри замыкания `io_utf8` импортируют **три** файла —
`anonymize_corpus.py`, `dispatch_notify.py`, `lesson_store.py`. Прямого ребра от входных точек нет
ни одного (`ВХОДНЫЕ ТОЧКИ: []`) — то есть грепом по `userbot_listen.py` этого не увидеть, и
ровно поэтому задание требовало функцию, а не греп.

### Вторая, более широкая мерка дала ТОТ ЖЕ вердикт

Ворота выкатки детей судят замыканием **без среза** (`_CLIENT_CUT = ()`, `pc_orchestrator.py:7906`):

| мерка | размер | вердикт по трём |
|---|---|---|
| признак/ворота входа `client_contour.closure()` | 50 + 25 = **75** | только `io_utf8.py` |
| ворота выкатки `pc_orchestrator._client_closure()` | 88 + 58 = **146** | `_client_paths([три])` → **`['io_utf8.py']`** |

Обе мерки сходятся: клиентский ровно один файл, и это тот самый, что держит замок.

**ЗАКОННАЯ ВЕТВЬ НАЗВАНА ЗАДАНИЕМ И ВЫБРАНА: не коммичу, пишу артефакт, останавливаюсь.**

## 3. Тесты — прогнаны, хотя ветвь уже была решена

Прогнаны потому, что владельцу решать судьбу этой работы, и «красная ли она» — часть решения.

| прогон | число | исход |
|---|---|---|
| замок, написанный самим 68v: `test_utf8_output_guard` | **10 тестов** | **OK** |
| селектив гейта (`test_exam_show` + `test_utf8_output_guard`) | **205 тестов**, 8.079 с | **OK** |
| **полный регресс, 126 модулей** | **7968 тестов**, 840.267 с | **FAILED (failures=43, skipped=12)**, rc=1 |

### 43 провала разобраны поимённо, и к трём файлам не относится НИ ОДИН

Каждый павший модуль прогнан **в одиночку** — это и есть проверка принадлежности:

| модуль | в одиночку | вывод |
|---|---|---|
| **`test_exam_show`** | **Ran 195 — OK** | 11 его провалов в общем прогоне — **загрязнение соседями**, а не правка |
| `test_pricing` | Ran 93 — FAILED (21) | красен **и без нас** |
| `test_client_contour` | Ran 111 — FAILED (2) | красен и без нас |
| `test_nonparse_ratchet` | Ran 19 — FAILED (2) | красен и без нас |
| `test_ext_refusal_unknown` | Ran 54 — FAILED (2) | красен и без нас |
| `test_moderation_card` | Ran 42 — FAILED (1) | красен и без нас |
| `test_threshold_ledger` | Ran 23 — FAILED (1) | красен и без нас |
| `test_cut_mark_scan` | Ran 20 — FAILED (1) | красен и без нас |
| `test_suggest` | Ran 718 — FAILED (1) | красен и без нас |
| `test_polzunki_pc` | Ran 46 — FAILED (1) | красен и без нас |

**Арифметика сходится точно: 11 + (21+2+2+2+1+1+1+1+1 = 32) = 43.** То есть весь красный цвет
полного прогона объяснён без единой ссылки на правку 68v: 32 провала — чужое наследство девяти
модулей, 11 — эффект одного процесса на 126 модулей, исчезающий при одиночном прогоне.

**Честно о границе этого доказательства.** Эталон полного регресса на ЧИСТОМ дереве не снят:
это второй прогон на ~840 с, и он не влезал в бюджет (§7). Принадлежность установлена одиночными
прогонами, а не сравнением «до/после» — способ слабее, и я его не выдаю за сильный. Откатить же
три файла ради «до» было прямо запрещено заданием, и правильно: единственный экземпляр 170 строк
лежит только в дереве.

**Что сказал бы гейт самообновления, а не полный регресс.** Гейт гоняет РОВНО два модуля —
`subprocess.run([VENV_PY, "-m", "unittest", "test_pc_orchestrator", "test_pc_local_dec"])`
(`pc_orchestrator.py:5590`). Полный регресс самообновление не держит ни строкой, и 43 провала
чужих модулей на него не влияют.

## 4. Коммит — НЕ СДЕЛАН (законная ветвь)

Ни `git add`, ни `git commit` по трём файлам не выполнялись. HEAD как был — **`1f6ff232`**.
Ни отката, ни `stash`, ни `checkout` по этим файлам не делалось ни одной командой: единственный
экземпляр работы 68v остаётся в рабочем дереве нетронутым.

## 5. Чтение назад числом

Отслеживаемых файлов, расходящихся с HEAD: **три** — те самые и ровно они.

```
exam_show.py · io_utf8.py · test_utf8_output_guard.py
```

**Чьи они:** работа захода **68v.2009** (ряд #134), чей автор мёртв — ряд закрыт таймаутом
сердцебиения. Я их не трогал: `+170/-4` на входе и `+170/-4` на выходе, байт в байт.
Плюс к ним — неотслеживаемый четвёртый файл того же автора, `test_exam_own_stdin_utf8.py`.

## 6. Применение: обещать нечего, и причина честнее обычной

Обычная оговорка §6 — «самообновление стои́т в конце витка, виток не кончится, пока идёт заход».
Здесь она даже не нужна: **коммита нет, значит применять нечего**. Замок остаётся ровно там, где
был, и за ним по-прежнему 12 коммитов (кодовых два).

**Событие, которое поднимет самообновление:** рабочее дерево должно перестать быть грязным по
файлам `_ORCH_RUNTIME` — а из трёх туда входит **только `io_utf8.py`** (`exam_show.py` и
`test_utf8_output_guard.py` в кортеже отсутствуют, проверено). То есть достаточно, чтобы
`io_utf8.py` был закоммичен решением владельца.

**Ожидаемый срок после этого:** `maybe_self_update()` зовётся последним в `poll_once`
(`pc_orchestrator.py:13504`) при `POLL_SEC=60`, то есть попытка — на ближайшем витке, **≤ 60 с
плюс время витка**, при условии, что демон не занят заходом.

**Готовность и применение — два разных факта, и здесь оба отрицательные:** готовности по коммиту
нет (коммита нет), применения по строке самообновления нет и быть не может. Проверять следующему
заходу нечего, пока владелец не решит вопрос §«что дальше».

Живой замок в логе, чтобы срок не звучал абстрактно, — по одной строке на каждый новый HEAD
(анти-флап: одна карточка на пару «коммит + состав грязного»):

| строка лога | коммит, который её вызвал |
|---|---|
| `19:11:17,615` … `файлы: io_utf8.py` | `0920d757` (18:52:27) |
| `19:50:06,323` | `ff989bb1`/`3953bb6a` (19:33:56/19:36:51) |
| `20:18:00,557` | `ad361bfc` (20:10:56) |
| `20:49:20,945` | `1f6ff232` (20:32:06) |

## 7. Бюджет

Потолок 2700 с, порог 1890 с. Работа не останавливалась бюджетом: ветвь решилась на п.2, а
дорогая часть (полный регресс 840.267 с плюс 10 одиночных прогонов, из них `test_suggest`
124.7 с) была уже сверх законной ветви и бралась сознательно — ради числа, которое нужно
владельцу для решения. Не снят по бюджету ровно один замер — эталон полного регресса на чистом
дереве (второй прогон ~840 с), и он назван неизвестным в §3.

---

## Что дальше — решение владельца, и вариантов ровно два

Развилка узкая, и обе стороны стои́т назвать честно.

**Вариант А — закоммитить всё равно.** Правка `io_utf8.py` — **`+40/-0`, чистое ДОБАВЛЕНИЕ**
новой функции `read_stdin_utf8()`. Ни одна существующая строка файла не изменена и не удалена;
новых импортов нет (только `sys`, уже импортированный) — замыкание не растёт ни на файл. Внутри
клиентского замыкания вызовов новой функции **ноль**: зовёт её ровно один файл, `exam_show.py:2310`,
который клиентским не является. То есть поведение живого клиента этой правкой не меняется ни в
одной точке. **Но признак `is_client` — fail-closed по устройству, и правило полосы «клиентский
файл требует слова владельца» действует по признаку, а не по моей оценке безопасности.**
Поэтому решение — владельца, и только его.

**Вариант Б — не коммитить.** Тогда замок остаётся, и 12 коммитов (два кодовых —
`ff989bb1` порог сироты и `336a6099` ручка модели канала codex) продолжают ждать неопределённо
долго. Цена измерена: демон работает на коде от `5c2ddfcd`, то есть со старта 16:58:08 не
получил ни одной правки.

**Чего делать НЕ следует ни в одном варианте:** откатывать, прятать в `stash` или чекаутить эти
файлы. Единственный экземпляр работы 68v (170 строк плюс неотслеживаемый тест на 17962 Б) живёт
только в рабочем дереве — вернуть его будет неоткуда.

**Если владелец выбирает вариант А**, к трём файлам стои́т добавить четвёртый,
`test_exam_own_stdin_utf8.py`: без него новая функция уезжает в git без своего живого стража.
Заход 69b уже смотрел на этот же замок и отказался трогать чужое (коммит `ad361bfc`: «предмет
уже в коммите 336a6099, самообновление держит ЧУЖОЙ io_utf8.py — чужое не тронуто») — то есть
это второй заход подряд, останавливающийся на одном и том же месте.
