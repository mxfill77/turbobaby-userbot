# Перекличка ПК после возвращения машины — 06.08.2026, замер 20:03 (+07:00)

Read-only. Ничего не менялось, процессы не трогались, `.env` не открывался, на сервер не ходили.
Временное — `tmp_rollcall_20260806_v4/` (скрипт расчёта простоя).

## Поправка к поводу: машина лежала 19 ч 31 м, а не ~34 ч

Число «34 часа» — это **срок молчания moderation_bot**, а не отсутствие машины. Журнал событий
Windows даёт срок машины дословно:

```
2026-08-06 19:37:17 | id=41   | The system has rebooted without cleanly shutting down first…
2026-08-06 19:37:21 | id=6008 | The previous system shutdown at 0:05:33 on 06.08.2026 was unexpected.
```

- падение (внезапное, без штатного выключения): **06.08.2026 00:05:33**
- загрузка: **06.08.2026 19:37:15** (`LastBootUpTime`)
- **простой машины = 19 ч 31 м 42 с**

Отсюда же поправка к карточке сторожа «moderation_bot лежал 33 ч 52 м»: срок считается от
**молчания лога** (`CLIENT_LOG_STALE`), а не от доказанной смерти. Лог moderbot замолчал
05.08 в 09:51:48, но демон жил после этого ещё ~14 ч и moderbot ни разу не поднимал
(`client_watch.json`: `deaths` дошло до 1 только сегодня). Доказанно мёртв moderbot был
**19 ч 38 м 37 с** (крах → подъём 19:44:10); остальные ~14 ч — молчание, не смерть.

## 1. Перекличка (замер 20:03:03, 06.08.2026)

**Не поднявшихся нет — живы все пять.** Про userbot записи у сторожа нет не потому, что он
не встал, а потому, что его поднял **не сторож**: `pc_agent.log`, 19:38:42 —
`userbot не запущен при старте агента — поднимаю (авто-реадопшн)`. Карточку в тему 328 такой
подъём не рождает, поэтому в ленте его и не видно (`client_watch.json`: у `userbot`
`deaths: 0, last_raise: 0.0`).

| процесс | PID | старт (+07:00) | аптайм | экз. | кто поднял (PPID) |
|---|---|---|---|---|---|
| `userbot_listen.py` | **7828** | 19:38:42 | 0:24:21 | 1 | `pc_agent` (1432), авто-реадопшн |
| `pc_agent.py` | **1432** | 19:38:27 | 0:24:36 | 1 | Планировщик, `svchost` (1784) |
| `rc_supervisor.py` | **1772** | 19:38:33 | 0:24:30 | 1 | `wscript` `rc_remote_control.vbs` (3320) |
| `pc_orchestrator.py` | **13880** | 19:43:02 | 0:20:01 | 1 | сторож → `schtasks /Run` |
| `moderation_bot.py` | **11968** | 19:43:59 | 0:19:04 | 1 | демон (13880) |
| сторож `pc_orchestrator_watchdog` | — | — | — | — | **задача Планировщика, не процесс** |

Сторож — задача, а не демон: `State=Ready`, последний запуск **20:03:01**, `LastTaskResult=0`,
следующий 20:08 (каждые 5 мин), действие `python.exe D:\turbobaby-bot\pc_orchestrator.py --watchdog`.
«Экземпляров» у него нет по устройству.

### Коммит кода процессов

Дословно **из памяти процесса** его называет только демон — собственной строкой старта:

```
2026-08-06 19:43:02,488 INFO === ДЕМОН СТАРТ (lane=pc, poll=60s, task_timeout=2700s,
approval_ttl=1800s, claude=C:\Users\mxfill1\.local\bin\claude.EXE, commit=95822e4) ===
```

Остальные четыре свой коммит не печатают, поэтому он **выведен**, а не прочитан: у каждого
файла `mtime` строго раньше старта его процесса, а содержимое на диске побайтно равно blob-у
в `HEAD` (`git hash-object` == `git rev-parse HEAD:<файл>`):

| файл | mtime | старт процесса | disk == HEAD |
|---|---|---|---|
| `userbot_listen.py` | 05.08 10:59:46 | 06.08 19:38:42 | `946be6343ec7` ✓ |
| `moderation_bot.py` | 23.07 13:05:28 | 06.08 19:43:59 | `756c61ef8d17` ✓ |
| `pc_orchestrator.py` | 05.08 18:32:23 | 06.08 19:43:02 | `e443775902a7` ✓ |
| `pc_agent.py` | 30.07 12:57:12 | 06.08 19:38:27 | `6ec9e7bbc40d` ✓ |

Вывод: все пять исполняют код коммита **95822e4** (= `HEAD`, изменённых отслеживаемых файлов 0).

## 2. Клиентский бот: доступен, поток апдейтов идёт, ЛС после рестарта — 0

Живые факты (не PID):

- лог, дословно: `2026-08-06T12:38:44.260768+00:00 | вошёл как @turbophuket (id=6879003264). Слушаю входящие ЛИЧНЫЕ сообщения.`
- живой сокет: PID 7828 → `Established 91.108.56.105:443` (дата-центр Telegram)
- поток апдейтов потребляется прямо сейчас: `userbot.log` рос в 20:02:45 (3 138 209 байт), хвост — строки `Got difference for channel <id> updates`

Чего **нет данных**: ни одного **личного входящего** после рестарта — счёт строк формата
`… | @<ник> | …` за 06.08 равен **0**. Последнее клиентское ЛС в логе вообще:
`2026-08-05T07:25:49+00:00 | @extthiwxer | extthiwxer | Now I need to make it`.
То есть «принимает апдейты» доказано, «принял ЛС клиента» — не доказано: клиент ещё не писал.

### Суммарный простой за двое суток

Окно `2026-08-04T12:58Z … 2026-08-06T12:58Z` (48 ч), по событиям `ЗАПУСК`/`ОСТАНОВЛЕН` лога
плюс крах питания вместо необъявленной остановки:

| окно простоя (UTC) | длительность |
|---|---|
| 04T18:13:16 → 04T18:18:50 | 0:05:33 |
| 04T18:19:56 → 04T18:38:47 | 0:18:51 |
| 04T18:39:53 → 05T00:50:31 | 6:10:37 |
| 05T02:27:12 → 05T02:30:08 | 0:02:55 |
| 05T02:31:14 → 05T02:51:36 | 0:20:22 |
| 05T17:05:33 → 06T12:38:43 (крах питания) | **19:33:10** |

**ИТОГО 95 492 с = 26 ч 31 м 31 с (55.3 % окна). Аптайм 21 ч 28 м 28 с.**

## 3. Незакоммиченных изменений нет

`git status --porcelain` без `??`: **0 строк**. `git diff --stat` пуст, `git diff --cached` пуст,
`git stash list` пуст, `main...origin/main` без ahead/behind. Боевые файлы = `HEAD` (хеши выше).

Untracked — 212 записей, всё прежнее: 108 тел журнальных записей `docs/artifacts/journal/`,
58 артефактов `docs/artifacts/2026-*`, 40 каталогов `tmp_*`, 5 маркеров `test-pc_ask_*`.

- **Задача 353 не оставила ничего**: файлов с `mtime` в окне её работы (06.08 00:00–00:06) нет
  вовсе — самое свежее untracked до сегодня датировано 05.08 20:34.
- **Задача 244 на этом ПК не запускалась ни разу**: её нет в реестре отметок
  `pc_orchestrator.task_started.json`.

## 4. Очередь ПК

Из локальных источников (реестр отметок + лог демона + реестр журнала), без похода на мост:

| id | статус | с какого времени | чем подтверждено |
|---|---|---|---|
| **377** | **выполняется сейчас** | claim `2026-08-06T12:50:08Z` (19:50:08) | `child: 5168` жив, `RUN id=377 (timeout=2700s, попытка 1/2)`, дедлайн ~20:35 |
| **353** | **висит в in_progress ~20 ч 02 м** | claim `2026-08-05T17:00:46Z` (06.08 00:00:46) | демон 24392 и ребёнок 504 умерли в крахе через 4 м 47 с; завершения нет ни в логе, ни в реестре журнала |
| **244** | в очереди, не бралась | поставлена 03.08 20:20:12 | `direct-enqueue: одиночка lane=pc … id=244`; owner-карточка инбокс 829, обновлена 05.08 02:37:25 |

Последняя строка реестра журнала — `NOTE 2026-08-05 17:00 UTC: Orchestrator: взял задачу #353
(in_progress)`, и после неё ничего. После рестарта демон 353 **не судил**: реапер-строки в логе
нет. Это соответствует заведённому правилу — задачи в окне потери связи не объявляются мёртвыми.

**Нет данных**: глубина очереди по статусу `new` целиком. Она живёт только на мосту, а
одноразовый скрипт, читающий токен ради этого чтения, — красный канал по правилу репозитория.

## 5. Правка устойчивости соединения: в git, запушена, применена в живом процессе

- **в git**: коммит **`4eed6f3`**, 05.08.2026 11:10, дословно — «обрыв связи не роняет userbot:
  реконнект штатной формой Telethon на ОДНОМ клиенте, простой виден в логе, экземпляр остаётся
  один». Диффом: `userbot_listen.py` +218, `test_userbot_reconnect.py` +390 (новый), итого 547/61.
- **запушена**: `git branch -r --contains 4eed6f3` → `origin/main`. Ворот не ждёт.
- **применена в живом процессе**: да. Файл на диске побайтно равен blob-у `HEAD`
  (`946be6343ec7`), а его `mtime` 05.08 10:59:46 строго раньше старта процесса 06.08 19:38:42 —
  значит PID 7828 загрузил именно этот код. Код правки — `userbot_listen.py:466-640`.
- **но ни разу не срабатывала**: маркер `СВЯЗЬ С TELEGRAM ВОССТАНОВЛЕНА` встречается в
  `userbot.log` **0 раз**. Причина видна по датам: процесс, живший до краха, стартовал
  05.08 в 09:51:36 — на час раньше, чем правка легла на диск (10:59:46). Сегодняшний запуск —
  **первый, несущий эту правку**, и обрыва при нём ещё не было.
