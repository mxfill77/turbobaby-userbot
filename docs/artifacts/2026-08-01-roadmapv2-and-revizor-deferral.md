# ROADMAP_V2 — что в нём уникально; РЕВИЗОР 13:07 — находки не сохранились

**Дата:** 01.08.2026, репо `D:\turbobaby-bot`, ветка `main`. **Режим: только чтение.** Ничего не
менял, не регистрировал, не перемещал, не удалял; ключей не заводил и не снимал; архивы не трогал;
процессы не трогал; на сервер не ходил; `.env` не открывал (секреты берёт САМ `brain_writer`,
класс 328); Drive-коннектор не звал — всё через живой Bridge. Временное — в
`tmp_roadmapv2_20260801_r7/` (untracked).

**Ответ моста фактом не считал.** Каждое число ниже — из ОБРАТНОГО ЧТЕНИЯ тела дока, а не из
`ok:true`: `roadmap_v2` 8 745 симв./91 стр./U+FFFD 0 (по id, орфан вне реестра), `index` (KB_MASTER)
**92 827** симв./973 стр., `rules` 16 857 симв., `cowork_log` 207 271 симв./555 стр.

---

## 1. ROADMAP_V2: ЧЕГО НЕТ В КАРТЕ

### 1.0 Оговорка, которую надо назвать первой

**Рамку Штаба §11 я прочитать не могу и не читал.** Она живёт в инструкциях проекта claude.ai;
в мозге и в репо её нет. Единственная доказуемая копия — **версия 03.07.2026** в `cc_log_archive`
(строки 419–488), и в ней **9 разделов**, не 11. Поэтому сверял с тем, что читается назад:
живой `index` (KB_MASTER, 92 827), живой `rules`, `business_rules`.

Косвенный, но сильный довод, что §11 ничего из списка ниже не держит: рамка сама себе запрещает
такое содержимое — «здесь только СТАБИЛЬНОЕ… изменчивое СОСТОЯНИЕ здесь НЕ живёт — оно в KB_MASTER
Разделы 3–4. Снимок состояния в инструкциях = второй источник правды = split-brain». А 01.08 рамку
ещё и **разгружали** (бизнес-правила вынесены из неё в `business_rules`). Всё уникальное в
ROADMAP_V2 — ровно состояние и хвосты. Это вывод, а не замер; замер за владельцем.

### 1.1 ЕСТЬ ТОЛЬКО ЗДЕСЬ — и это живое (5 пунктов)

| # | что | доказательство отсутствия |
|---|---|---|
| 1 | **Шесть просрочек масла с числами:** ADV 6895 +7365, CB650R 3503 +5074, XADV 2478 +1714, XMAX 4729 +1657, NMAX 6908 +1554, NMAX 4253 +336 | ни одного из 12 чисел нет в KB_MASTER / `rules` / `business_rules` (grep по каждому = 0). Совпадение по `2478` в KB_MASTER — **другое**: инцидент 15.07 «масло из vision-нарратива» |
| 2 | **Кривое поле H у байка 7530** (старт 33000 > пробега 31916) — открытый вопрос владельцу: исправить старт или оставить | `7530`/`33000`/`31916` — 0 везде. **Единственная строка дока, которая прямо ждёт решения человека** |
| 3 | **Развилка хранилища для расширения двухфазного ТО:** вкладка Bot-Data «то_заявки» **vs** `memory.db`, + неотвеченные вопросы 1–6, + указатель на PLAN 23.06 в `KB_review` | `то_заявки` = 0, `двухфазн` = 0 в KB_MASTER. Само ТО там есть («в проде», «цепочка доведена», строка 252) — **развилки нет** |
| 4 | **Авто-тариф (`getFormula` — live прайс из формулы таблицы)** | `getFormula` = 0, `авто-тариф` = 0. Прайс в KB_MASTER (строки 604–605) — про `price_sheet` клиентского черновика, это другое |
| 5 | **Ф2/Ф3 payments → `KB_payments_plan`** | `payments` = 0 в KB_MASTER; §5 «где детали» этот док не называет вовсе. Док жив (3 441 симв.), в теле «СТАТУС: ОТЛОЖЕНО», читателей ноль. **Строка роадмапа — единственная живая ссылка на него** |

### 1.2 ЕСТЬ ТОЛЬКО ЗДЕСЬ — но мертво по собственному тексту (2 пункта)

- **BELL (звоночек Termux):** `BELL` = 0 нигде. Сам док пишет: «чинить не надо — будет ПЕРЕКРЫТ
  heartbeat O1», а O1 ✅ закрыт. Пункт закрыт своим же текстом.
- **ПРАВИЛО ВЕДЕНИЯ КАРТЫ** («каждое завершённое — ✅ здесь, каждое вспомненное — сразу вписывать»):
  правило самоведения ЭТОГО дока. Умирает вместе с ним, переносить нечего.

### 1.3 УНИКАЛЬНА ФОРМУЛИРОВКА, СУТЬ ПЕРЕКРЫТА (3 пункта — переносить не нужно)

- **Выгрузка Доставки → «scp на VPS → офлайн-харнесс read-only»**: `fetch_delivery` = 0, но зоны
  доставки **в проде** (KB_MASTER строка 635: Bridge @75, 20 зон), `fetch_delivery.py` и
  `fixtures/delivery_zones_get.live.json` лежат в репо. План устарел.
- **Stage 5/6 (доставка тайцу из брони / фото выдачи → `activate_booking`)**: ярлыка «Stage 5/6» в
  KB_MASTER нет, а механика — да: «СХЕМА ВЫЕЗДА» (строка 809, таец фоткает → кнопка → уведомление)
  и `activate_booking` в O3-3a (строки 429–431).
- **LLM-проверка перевода в аудиторе (естественность тайского)**: `естественност` = 0; TH-зеркало
  всех рендеров в KB_MASTER есть (строка 265), проверки качества — нет. Пункт был «Отложено» уже
  26.06; ценность близка к нулю, но формально это шестой уникальный.

### 1.4 ДУБЛИРУЕТСЯ ПОЛНОСТЬЮ (перечислено, чтобы «всё остальное» не было словом на веру)

Сверх-цель / главная ось / три канала / два контура → KB_MASTER Раздел 1–2 (строки 203, 219–222,
245). ✅-фундамент (Splinter, двухфазное ТО, наблюдатель слой 1, оркестратор ст1, чистый мозг) →
Раздел 3 (251–252). O1 heartbeat, O2 касса, **O3 + тупик Этапа 2 + «состояние порождает тот, кто
оформляет сделку» + пересмотр источника выдачи**, O4 декомпозер → Раздел 4 (260, 262, 796–797,
930–934; «декомпозер» встречается 12 раз, ступень 2 обкатана 05.07). C1–C4 → почти дословно (935).
Raspberry Pi → 203/222. WhatsApp Cloud API → 551/792 — **роадмап отстал: WA-вебхук в проде с
14.07**. EdenAI + GitHub PAT до 17.08.2026 → 957 (дословно). Незапушенные коммиты `manager-bot` →
§6 чек-лист [8] (889). CRM «Завершена» approve-флоу → 435 (уже **сделано**, O3-3b). «Пометить
KB_ROADMAP_MASTER как OLD» → беспредметно: файл удалён из Drive 27.06.

Отдельно: строка 2 «ЧИТАТЬ ПОСЛЕ KB_NORTH_STAR» — **третий экземпляр известной битой ссылки**
(дока `KB_NORTH_STAR` не существует; опись 01.08). Новой информации не несёт.

### 1.5 ОТКУДА НА НЕГО ССЫЛАЮТСЯ (замер этого захода)

| где | сколько | живая ли |
|---|---|---|
| **живые доки мозга** | **0** | проверены все 29 достижимых тел; в живом `index` (прочитан назад сегодня, 92 827) — **0** |
| `cc_log_archive` / `cowork_log_archive` | 29 / 1 | **нет** — оба мёртвые архивы без читателей (опись 01.08 §1) |
| `brain_writer.py:365` | 1 | комментарий-класс: пример отказа `not_in_brain` |
| `test_brain_writer.py:571,576,583,639,641` | 5 | **несущая.** Голден «имя отказывает честно» держится ИМЕННО на `roadmap_v2` — после сокращения 01.08 это **единственное оставшееся** такое имя |
| `docs/artifacts/*` (5 файлов 01.08) | — | описи и планы, не потребители |
| `tmp/brain_*.py` (вне git) | 4 | черновики прошлых заходов |
| VPS `registry_check.py` → `KB_ALIAS`: `roadmap_v2`→`roadmap_master` | 1 | **дефект чужой полосы** (ключа `roadmap_master` нет с 31.07). Этим заходом **не переизмерялся** — на сервер не ходил |

**Боевого кода, который ЧИТАЕТ этот док, нет ни на одной полосе.**

### 1.6 ПРЯМОЙ ОТВЕТ

Нет, не всё дублируется. **Пять живых пунктов (1.1) пропадут вместе с доком** — из них по-настоящему
жаль два: байк **7530** (открытый вопрос владельцу) и **развилка хранилища расширения ТО**.
Оперативные числа масла — срез **26.06**, пять недель: сверять надо по живому листу «Байки», а не по
этому доку; но как *след* «эти шесть были просрочены» они больше нигде не лежат.

Практический вывод: «оставить в архиве и убрать ссылку» безопасно **после** того, как 5 строк из
1.1 лягут в KB_MASTER (Раздел 7 / хвосты). Иначе решение = тихая потеря. И учти: снятие ссылки
задевает голден `test_brain_writer.py` — это правка кода, здесь не делалась.

---

## 2. РЕВИЗОР 13:07 — НАХОДКИ НЕ СОХРАНИЛИСЬ

### 2.1 Факт

**Десять owner-находок не сохранены нигде.** Спула нет: `task_f`/`owner_f` — локальные списки
функции `_revizor_route`; `deferred:True` — всего лишь ключ возвращаемого dict
(`pc_orchestrator.py:6335`, единственное вхождение слова во всём файле), а `maybe_revizor` результат
`_revizor_route` **выбрасывает**. Файла-спула для находок в репо не существует.

Хуже: метка прогона ставится **ВСЕГДА**, в том числе после отказа (`pc_orchestrator.py:5846`).
На диске сейчас `pc_orchestrator.revizor_state.json` = `{"last_run":"2026-08-01T13:02:22.642166+00:00"}` —
то есть следующий прогон возьмёт `since=13:02:22` и увидит **только окна с активностью ПОСЛЕ
отказа**. Окно, замолчавшее после 13:02:22, второй раз отобрано не будет, и его находки исчезнут
совсем.

### 2.2 Что именно потерялось — и как оно набралось

Лог демона (местное время, UTC+7): 20:02:22 — `since=07:00:31` → **2 окна** (929753577, 8629287224);
20:02:23 — пост-релизный чек-лист **#92** дал по 1 находке на окно (детерминированные);
20:04:23–20:05:21 — **8** LLM-находок классов а/б/в/г/д/е/е/д переведены воротами клиентского
контура из задач в owner-карточки. Итого `2 + 8 = 10` owner и `0` задач — ровно то, что в NOTE.

### 2.3 «Дойдут ли» — по факту, а не по надежде

Не как сохранённые, а как **пересчитанные заново**, и только если окно снова активно. Это уже
происходило — прочитано назад из живого `cowork_log` (207 271 симв.), **две подряд** отсрочки за
сутки:

```
NOTE 2026-08-01 07:05 UTC: … ревизор: очередь недоступна — 0 задач и  4 owner-находок отложены…
NOTE 2026-08-01 13:07 UTC: … ревизор: очередь недоступна — 0 задач и 10 owner-находок отложены…
```

Прогон 13:02 переоткрыл окно 929753577 и **передобыл те же классы б/д/в + пост-релиз #92**,
добавив ещё `г` и `а`. То есть четыре находки 07:05 не пропали — их пересчитали, потому что клиент
продолжал писать. **Это везение, а не механизм.**

**Когда:** следующий тик не раньше **19:02:22 UTC** (02:02 ночи 02.08 местного) — период 6 ч
(`REVIZOR_HOURS`, дефолт) мерится от метки на диске, плюс зернистость поллинга (по логу фактические
интервалы 6 ч 00 м / 6 ч 17 м / 6 ч 02 м). Попадут ли те же два окна — зависит от строк `drafts`
новее 13:02:22 UTC. Косвенно активность после метки была (`moderation_ipc.db` менялся в 13:40 UTC —
это mtime файла, запроса к БД я не делал), но **какое именно окно — отсюда не видно**.

### 2.4 Механизм отказа — одной строкой

`_loc_fetch_items()` (`pc_orchestrator.py:3507`) опрашивает Bridge **шестью** запросами — по одному
на статус `new/in_progress/needs_approval/approved/done/failed`; **любой** ответ без `ok` → функция
возвращает `None` (fail-closed: «частичная картина цепи опаснее ожидания») → `_revizor_route` уходит
в ветку отсрочки, пишет NOTE и теряет находки.

Отказ **молчаливый**: ни причины, ни номера статуса нигде не записано — в логе демона между
20:05:21 и 20:08:01 нет ни одной строки. Поэтому назвать, *почему* мост промолчал, нечем. Не чинил
(запрещено), но класс называю вслух: **метка прогона ставится и на отказе — отсрочка съедает окно
отбора**; лечение — либо не двигать метку при `deferred`, либо спулить находки на диск.

---

## ИТОГ

1. **Не всё дублируется.** Пять живых уникальных пунктов (§1.1) + два мёртвых по своему же тексту
   (§1.2) + шестой полумёртвый (§1.3, проверка перевода). Всё остальное — в KB_MASTER, местами
   свежее, чем в роадмапе (WhatsApp).
2. **Рамку §11 не читал** — её нет ни в мозге, ни в репо; доказуемая копия только версии 03.07 с
   девятью разделами (§1.0). Вывод про §11 — довод от её собственного принципа, а не замер.
3. **Ссылок из живого мозга ноль**, из репо — комментарий и **голден-тест**, который держится
   именно на этом имени; на VPS есть битый алиас (чужая полоса, не мерил).
4. **10 находок ревизора не сохранились** — спула нет, метка прогона поставлена. Отсрочек за сутки
   было **две** (4 и 10 находок); первая «выжила» только потому, что окно осталось активным.
5. **Отказ:** `_loc_fetch_items` возвращает `None`, если хотя бы один из шести статусов очереди
   не прочитался; причина нигде не записана. Следующий тик — не раньше 19:02 UTC.
