# Судьба находок внешних каналов в сводке — и польза от них числом

Задание Штаба из ящика, ключ `external-fate.0904`, полоса ПК. Ось — обслуживание:
прямой заказ владельца «я не вижу, чем внешние помогают». Заход 04.09.2026,
09:20–10:00 местного (02:20–03:00 UTC).

## Признак сделанности — первым действием

Артефакта по адресу результата **не было**: в `docs/artifacts` за 04.09 лежало 7 файлов
(потолок времени на мост, приёмка и дожим, разведка O2, разведка заявки 5a67f958baa6,
сообщение о закрытии, сутки ящика, условия один и два), ни одного по теме судьбы находок.
Задание начато с нуля, ни один его вопрос заранее отвечен не был.

## Премиса Штаба — три пункта, проверены своими командами

Прибор: `_scratch_external_fate_0904/probe_premise.py` (только чтение, лоток и реестры).

| пункт премисы | мой замер | исход |
|---|---|---|
| 1. в сводке есть раздел внешних ответов, и он даёт СЧЁТ | тема `external` стои́т в `contour_digest.TOPICS` четвёртой; строка за 03.09 даёт заходы, ответы, каналы, отказы с причинами и разбивку находок по видам | **ПОДТВЕРЖДЕНО** |
| 2. судьбы находки в нём нет вовсе | ни одного из слов `заявк`, `автозадач`, `отбор`, `отклон`, `ждёт` в строке нет — 5 проб из 5 дали False | **ПОДТВЕРЖДЕНО** |
| 3. за 03.09: 8 пакетов, 16 ответов, оба канала живы, находок 48, отказов 0 | заходов 22 · ответов **19** · пакетов **11** · каналов ответило 2 из 2 · находок **57** · отказов **3** (`answer_lost`) | **ЧИСЛА НЕ СОВПАЛИ, причина названа** |

Расхождение п.3 — не ошибка Штаба и не ошибка прибора. Замер Штаба сделан 04.09 около
03:45 местного = **03.09 20:45 UTC**, то есть внутри тех же суток UTC, которые он считал:
после его замера в лоток продолжали падать ответы. Направление расхождения это
подтверждает — все числа выросли, ни одно не упало. Отдельно: «отказов 0» стало «отказов 3»
по той же причине — три `answer_lost` пришли позже. **Вывод премисы верен целиком: раздел
есть, счёт даёт, судьбы не даёт.**

## Где судьба лежала на самом деле

Три реестра, и каждый пишет свой кусок:

| реестр | что знает | ритм записи |
|---|---|---|
| `review_intake_state.json` | `claims`: ключ находки → номер ряда-заявки (ступень B ставит ряд и ТУТ ЖЕ переводит его в `needs_approval` — это и есть заявка владельцу) | ступень B тикает раз в 10 мин, пишет только при живом мосте |
| `recon_auto_state.json` | `placed`: повод → маршрут `разведка` (автозадача) либо `заявка владельцу`; `held`: повод → причина, по которой отбор его придержал | ступень E тикает раз в 30 мин |
| `queue_snapshot_pc.state.json` | исход ряда: `rejected` (владелец отклонил), `done` (сдано работой), статус `needs_approval` (ждёт решения) | демон пишет по смене состояния |

## Что добавлено в сводку: четыре строки, ни одной больше

Прирост раздела «ВНЕШНИЕ ОТВЕТЫ ЗА СУТКИ» — ровно 4 строки при потолке задания 4 и при
`LIST_MAX = 6` (пятая строка раздела — прежний счёт, он не тронут). Живой вывод за 03.09:

```
• судьба находок за 2026-09-03 (всего 57): заявкой владельцу 30 · в реестре ступени B нет 27
  — «закрыто отбором» тут НЕИЗВЕСТНО, а не ноль: отбор держит решение в памяти захода
  · review_intake_state.json · 15 мин назад
• из них ступень E: автозадачей разведки 1 · заявкой владельцу 1 · придержано её отбором 19
  (причина записана) · recon_auto_state.json · 14 мин назад
• ряды этих заявок: ждёт решения владельца 0 · владелец отклонил 0 · сдано работой 14
  · в слепке нет 16 · queue_snapshot_pc.state.json · 10 мин назад
• ПОЛЬЗА за 2026-09-03: 15 — находка, по которой ПОСТАВЛЕНА автозадача разведки ИЛИ СДАН
  работой её ряд; наличие заявки пользой НЕ считается, «умная» находка тоже; правило прибора
  не имеет и в счёт не входит
  · recon_auto_state.json + queue_snapshot_pc.state.json · 14 мин назад
```

У каждого числа свой источник и свой возраст — общего возраста у трёх реестров нет, и
печатать один на всех значило бы врать про два из трёх (тест
`test_every_new_number_carries_its_own_source_and_age` требует, чтобы адреса строк были
РАЗНЫМИ).

## Предсмертный взгляд задания закрыт числом, а не обещанием

Задание назвало провал заранее: «за пользу посчитают все находки, у которых есть заявка, — и
строка начнёт хвалить внешних за то, что они просто много говорят». Замер показывает, что
опасение материально: **заявку получили 30 находок из 57, а до дела дошли 15**. Считай мы
пользой заявку — число выросло бы вдвое и росло бы дальше вместе с болтливостью канала,
потому что постановка заявки — машинный шаг ступени B, а не признание ценности.

Поэтому определение пользы едет **в самой строке** и сформулировано узко: находка, по которой
ПОСТАВЛЕНА автозадача разведки ИЛИ СДАН работой её ряд. Строку невозможно прочитать шире, не
переписав её. Регресс `test_a_claim_alone_is_never_a_benefit` требует `benefit < claimed` на
корпусе, где заявок четыре, а дел два; `test_a_finding_with_neither_task_nor_done_row_is_not_useful`
проверяет обратное направление: четыре заявки без единого дела дают пользу **0**.

## Третий исход — у каждого нового числа, и одно место, где прибора нет вовсе

1. **Реестр не прочитан или старше своего предела → «неизвестно», а не ноль.** Пределы
   названы вместе с причиной: `intake` 3600 с (ступень B тикает раз в 10 мин, но пишет
   только при живом мосте — шесть промахов подряд), `recon` 5400 с (тик 30 мин, три
   промаха), `benefit` 3600 с (пара реестров: предел берётся у более быстрого, возраст — у
   более старого). Числа по протухшему слепку наружу НЕ ЕДУТ вовсе
   (`contour_digest_run.fresh_or_none`) — тот же приём, каким уже гасятся счёт серии и счёт
   ящика: строка с шапкой «неизвестно» и живыми цифрами читалась бы как факт.
2. **Смерть одного реестра не стирает остальных**: молчание ступени E не отменяет того, что
   ступень B уже посчитала.
3. **Польза считается только при ОБОИХ живых источниках**: это объединение двух множеств, и
   при мёртвой половине вышла бы нижняя оценка, выданная за число.
4. **«Закрыто отбором» у ступени B прибора НЕ ИМЕЕТ, и это сказано вслух.**
   `review_intake_run.tick` держит `held` в памяти захода, а на диск пишет только
   бутстрапное. Ключ находки, не доехавшей до заявки, лежать негде: его нет ни в `claims`,
   ни в `held`. Поэтому строка говорит «в реестре ступени B нет 27», а не «отбор закрыл 27»:
   заявка могла ещё не встать, а не быть отвергнутой, и различить это на диске нечем.
   У ступени E отбор своё решение пишет — там число честное (19 придержанных с причиной).
5. **«Новое против пересказа» не мерится** — прибора нет, и здесь он не выдуман.
6. **Правило как исход находки прибора не имеет**: правило ложится коммитом и текстом,
   машинной связи «находка → правило» на полосе не существует. В счёт пользы оно не входит,
   и это напечатано В СТРОКЕ, а не спрятано в коде.

## Замки, взятые у соседей, а не заведённые заново

* Ключ находки считает **ступень B** (`review_intake.claim_key`), а не своя формула: судьба
  ищется в её реестре, и второй способ считать ключ промахивался бы молча.
* Узнавание идёт по **набору** ключей заявки, а не по якорному: якорь переезжает с приходом
  второго канала (урок ступени B §4), и узнавание по одному ключу теряло бы заявку каждый
  раз, когда в лоток падает новый ответ. Тест `test_the_anchor_may_move_and_the_finding_is_still_found`.
* Ключ повода ступени E считает **сама ступень E** (`recon_auto.key_of`), слова маршрутов
  взяты у неё же (`ROUTE_RECON` / `ROUTE_OWNER`) — литералов здесь нет.
* Отказ владельца берётся **исходом ряда** `rejected`, который посчитал `queue_snapshot_pc`
  по своему маркеру. Второго экземпляра литерала «отклонено Филиппом» в сводке нет, и тест
  это сторожит: два экземпляра одного маркера разошлись бы молча.

## Мина, пойманная живьём при сборке теста

Первый корпус теста давал пять находок с цитатами «находка номер 1…5» — и все пять получили
**один ключ**: `claim_key` считается по МЕРЕ СМЫСЛА цитаты, а номер в меру смысла не входит.
На таком корпусе зелёными были бы любые числа. Корпус переписан на пять РАЗНЫХ ПО СМЫСЛУ
цитат, и в `keys()` стои́т утверждение, падающее при склейке.

## Тесты

`python -m unittest test_contour_digest` — **87 тестов, OK** (было 69, прирост 18:
`TestFindingFate` целиком).
Полный набор репозитория не гнался: правка принадлежит набору сводки.

Инварианты набора не тронуты и по-прежнему зелены: `CONTOUR_DIGEST_PURE` (чистая логика без
часов, диска, сети и `getenv`) и `CONTOUR_DIGEST_READS_ONLY` (ни одной ветки записи в
очередь). Сводка по-прежнему ничего не исполняет, задач не ставит, состояния не меняет и
кнопок не несёт — прирост это правило не трогает ни строкой.

## Что заход НЕ делал

Новых походов во внешние каналы не было ни одного. Чужой текст в сводку не переносился: в
новых строках нет ни знака цитат — только счёт, слова исходов и адреса реестров (тест
`test_no_foreign_text_reaches_the_fate_lines`). Процессы не трогались, боевых записей нет,
`.env` и конфиги не читались, ничего не удалялось. Временное — только
`_scratch_external_fate_0904/` (два прибора чтения), оставлено на месте.

## Файлы

* `contour_digest.py` — источники `intake`/`recon`/`benefit` с пределами и причинами;
  `finding_keys`, `external_fate`, `fate_words_intake`, `fate_words_recon`, `fate_words_queue`,
  `benefit_words`.
* `contour_digest_run.py` — чтение двух новых реестров, `fresh_or_none`, `_oldest`,
  расширенный `section_external`.
* `test_contour_digest.py` — класс `TestFindingFate`, 17 проверок.

---

# ДОЖИМ, попытка 2 (04.09.2026, 20:20–20:55 местного): пункт 6 «держать коротко»

Приёмка Штаба сверила артефакт с пунктами задания и назвала остаток: **пункт 6 не отвечен** —
опорных слов в тексте не нашлось. Разбор подтвердил: пункт был закрыт по имени, а не по мере.
Раздел выше называется «четыре строки, ни одной больше» и опирается на тест
`test_the_growth_of_the_section_is_four_lines_and_no_more`, который считает **пункты кода**
(`len(rows) == 5`). Пункт 6 говорит не про пункты, а про читателя: «сводку читают с телефона,
и длинная стена хуже отсутствия».

## Чем мерили и что намерили

Прибор — `_scratch_external_fate_0904/probe_width.py`: берёт ту же `line_text`, что уедет в
Telegram, и считает перенос по словам на ширине экрана. Ширины взяты диапазоном (36/40/46),
чтобы вывод не стоял на одном спорном числе.

| мера | до дожима | после |
|---|---|---|
| пунктов кода | 4 | 4 |
| знаков прироста (живой `--dry` за 04.09) | **786** | **627** |
| физических строк на экране 36 знаков | **26** | **22** |
| физических строк на экране 40 знаков | 23 | 19 |

**Четыре пункта кода были двадцатью шестью строками телефона** — то есть ровно той стеной,
которую пункт запрещал. Зелёный тест этого не видел, потому что мерил не ту единицу.

## Потолок взят у самой сводки, а не из головы

`probe_scale.py` посчитал восемь СОБСТВЕННЫХ пунктов живого `--dry` (без прироста):
50 · 50 · 73 · 101 · 130 · 171 · 302 · 396 знаков. **Медиана — 130.** Значит «четыре строки»
этой сводки = 4 × 130 = **520 знаков**. Прирост до дожима был 786 — перебор в **1.51 раза**.

## Ужато до 631, и почему не до 520 — арифметикой, а не обещанием

Резалась вода: объяснение «отбор держит решение в памяти захода» уехало в шапку раздела и в
этот артефакт (телефон читает вывод, а не разбор), исчезли `(причина записана)`,
`из них`, `этих`, `решения`, `работой`. Замки не тронуты ни один.

Остаток разложен `probe_arith.py` без остатка:

* три строки судьбы — **396 знаков против 390** у трёх медиан: они **в мере**;
* весь перебор сидит в четвёртой, строке пользы: **235 против 130**. Из них **136** — дословное
  определение пользы (**требует пункт 4**), **24** — надбавка за двойной адрес источника
  (польза стои́т на ДВУХ реестрах, назвать оба **требует пункт 3**), **75** — всё остальное
  вместе, то есть **вдвое короче медианной строки**.

Снять первые две величины значило бы снять замки пунктов 3 и 4 ради пункта 6. Столкновение
пунктов названо вслух и разрешено в пользу замков; перебор над 520 оставлен **известным и
измеренным**, а не незамеченным. Это и есть честный ответ пункта 6, а не «мы уложились».

## Замок, который мерит правильную единицу

`FATE_CHARS = 640` (замер 631 + 9 знаков на рост разрядности чисел) и два новых теста:

* `test_four_lines_are_counted_in_characters_and_not_in_rows` — сумма знаков прироста
  ≤ `FATE_CHARS`; отдельно проверяет, что сам потолок ниже той стены (786), ради которой заведён,
  и что ни одна строка не тянет на экран целиком;
* `test_the_shortening_did_not_cut_a_single_lock` — определение пункта 4, третий исход пункта 5
  (`закрыто отбором неизвестно`) и паспорт пункта 3 (адрес + возраст у каждой строки) на месте.

Прежний тест на четыре пункта **оставлен** — он ловит другое (лишнюю строку раздела), и снимать
его было бы разменом одного замка на другой.

## Мина, пойманная этим же заходом

Сокращение сделало «закрыто отбором **неизвестно**» **постоянным** текстом (прибора у этого
исхода нет никогда). Из-за этого `assertIn(FATE_UNKNOWN, words)` в
`test_a_dead_registry_says_unknown_and_never_zero` **перестал что-либо доказывать** для ступени B:
он зеленел бы и на строке, где живые числа втихую съехали в нули. Тест усилен на счёт ШТУК — при
мёртвом реестре «неизвестно» обязано быть **три** (заявки, вне реестра, отбор).

## Тесты дожима

`python -m unittest test_contour_digest` — **89 тестов, OK** (было 87). Полный набор
репозитория не гнался: правка принадлежит набору сводки. Инварианты `CONTOUR_DIGEST_PURE` и
`CONTOUR_DIGEST_READS_ONLY` не тронуты, сводка по-прежнему ничего не исполняет.

Временное — `_scratch_external_fate_0904/` (`probe_width.py`, `probe_scale.py`,
`probe_arith.py`, снимки `dry_before.txt` / `dry_after.txt`), оставлено на месте. Новых походов
во внешние каналы не было, чужой текст в сводку не переносился, процессы не трогались.
