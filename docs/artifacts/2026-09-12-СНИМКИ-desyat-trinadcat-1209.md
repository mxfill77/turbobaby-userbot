# Четыре снимка экзамена (кейсы 10–13) собраны на нынешнем коде: даты подставлены во всех, подсказок критика 8 из 16, у одного кейса критик молчал 257.1 с

СНИМКИ desyat-trinadcat 1209

**Дата:** 12.09.2026 · **Полоса:** ПК · **Задание:** ящик Штаба, ключ `58-a-snimki-desyat-13.1209`
(док `shtab_task_58-a-snimki-desyat-13.1209`, file id `1FPcZVIDLa7rd2n3XsmR4gfQzmfkxMa1c`)
**Коммит сборки всех четырёх снимков:** `aa7b1c4` · **коммит, положивший их в git:** `1a84624`
(4 файла, +185)
**Временное место захода, названо явно:** `tmp/exam58a/` (замерочный скрипт `measure_58a.py` и его
`shots_measure_58a.json`), `tmp/pc_report/` (черновик доклада и логи заморозок `freeze-10…13.log`),
`tmp/exam_critic/` (сырые ответы критика — место назначено самим механизмом, `CRITIC_RAW_DIR`).
Ничего не удалялось, включая уборку за собой. **Кода заход не правил ни строкой** — снимки собраны
готовым механизмом `exam_show.py --freeze`.

---

## FACT — живые числа

| что доказано | число / факт |
|---|---|
| **четыре слота на HEAD** | `exam_shots/case-10@aa7b1c4.json` … `case-13@aa7b1c4.json`, все четыре — коммит `aa7b1c4` |
| на входе слотов кейсов 10–13 было | **0** (в `exam_shots/` лежало 12 файлов, все кейсы 1…9) |
| корпус | `trainer_cases.json`, 17 кейсов, отпечаток `99e6b78177a1d533` — один у всех четырёх снимков |
| **даты подставлены** | фигурных скобок в ПОКАЗАННОМ вопросе — **0 у всех четырёх**; в сырой строке корпуса они остались (2 у кейсов 10, 12, 13) — значит подстановка сработала, а не «шаблонов там и не было» |
| **подсказок критика** | **8** из потолка 16 (`HINTS_MAX` 4 × 4 кейса): 2 · 0 · 3 · 3 |
| меток живых примеров | 5 · 5 · 5 · 3 |
| цена четырёх единиц | **891 с** = **0.330** потолка 2700 с |
| версия базы правил во всех четырёх | `7d19b1db352e8f76` |
| наружу ушло сообщений | **0** |

## §1 Признак сделанности — читан СОДЕРЖИМЫМ (пункт 1)

Первым действием открыты слоты: `ls exam_shots/` → 12 файлов, из них `case-1.json`,
`case-1@37c290f`, `case-1@4b6160e`, `case-1@df03e80` и `case-2@4b6160e … case-9@4b6160e`.
**Слотов кейсов 10, 11, 12, 13 нет ни одного, ни на каком коммите** — значит собирать все четыре,
и «часть уже собрана» не про этот заход.

## §2 Премисы Штаба — проверены числом ПЕРВЫМ действием

**П1 (карточка с подсказками доказана живьём) — ПОДТВЕРЖДЕНА ЧАСТЬЮ.** Проверялась по содержимому
снимка, а не по эфиру: `exam_shots/case-1@37c290f.json` — подсказок **3**, меток примеров **3**,
фигурных скобок в показанном вопросе **0**, колбэков карточки **6** в **3** рядах (раскладка
`[1, 3, 2]`: `exam:ok:1` · `exam:h1|h2|h3:1` · `exam:own:1`, `exam:go:1`). Живой тап по карточке
виден в `exam_session.json` (`shot_key` = `37c290f@2026-09-12T08:56:10Z`, ждал текст «своё»).
**Чего проверить нельзя:** номер сообщения 1923 и время 17:28 (+07) — это факты эфира, а заданию
велено наружу не ходить ни одним сообщением; отсюда они не видны, и заход их не подтверждает.

**П2 (замок показа сверяет содержимое, а не свежесть `exam_show.py`) — ПОДТВЕРЖДЕНА КОДОМ.**
`exam_show.BUTTON_CLOSURE = (AGENT_SRC,)` — по времени сверяется РОВНО ОДИН файл, `pc_agent.py`;
`exam_show.py` из сравнения по времени убран правкой 52-d (разбор — `exam_show.py:693…725`).
Расхождение кнопок ловится содержимым: `card_callbacks` → каждый колбэк прикладывается к разбору
живого агента, вынутому текстом (`agent_parse` → `EXAM_CB_RE`). Порядок замков жёсткий: сперва
время `pc_agent.py`, и лишь потом содержимое.

**П3 (цена единицы выросла, прежней арифметике не верить) — ПОДТВЕРЖДЕНА, но ЧИСЛА ДРУГИЕ.**
Замер §3: круг головы дал **45.9 … 100.5 с** — то есть у кейса 11 он оказался НИЖЕ названного
премисой пола 68 с. Главное утверждение премисы при этом верно и доказано: цена ЕДИНИЦЫ выросла
не из-за головы, а из-за поднятого потолка критика — средняя единица **222.8 с** против **93 с**
на восьми единицах 52-a (`docs/artifacts/2026-09-12-СНИМКИ-odin-devyat-1209.md`, §3), рост **2.4×**.

**П4 (заморозка умеет слот по коммиту и прежний слот не трогает) — ПОДТВЕРЖДЕНА КОДОМ И ДИСКОМ.**
Отказ считается по коммиту (`exam_show.freeze`, строки 434–438), имя слота — `case-%s@%s.json`.
На диске у кейса 1 сосуществуют четыре слота разных коммитов; ни один прежний файл этим заходом
не тронут и не перезаписан (все 12 входных файлов на месте, в `git status` они не значатся).

## §3 Замер на двух единицах и арифметика (пункт 2)

Мерилось стенное время процесса `exam_show.py --freeze --case N` целиком; круг критика берётся из
поля снимка `critic.waited` (его пишет сам механизм), круг головы — разностью. **Разность названа
честно как ВЕРХНЯЯ оценка головы:** в неё входят старт процесса, импорты `trainer_run`/`suggest`,
`placeholders()` (ходит за живой таблицей периодов), `build_transcript` и снятие меток примеров.

| единица | всего, с | круг критика, с | круг головы (верхняя оценка), с | исход критика |
|---|---|---|---|---|
| кейс 10 | **255** | 154.5 | 100.5 | подсказок 2 |
| кейс 11 | **303** | 257.1 | 45.9 | НЕ ОТВЕТИЛ (упёрся в потолок 257) |
| кейс 12 | 159 | 74.5 | 84.5 | подсказок 3 |
| кейс 13 | 174 | 98.7 | 75.3 | подсказок 3 |
| **итого четыре** | **891** | 584.8 | 306.2 | — |

**Арифметика порога, посчитанная ДО сборки третьей и четвёртой единицы** (как велит пункт 2):
по двум замеренным единицам худшая цена 303 с → 4 × 303 = **1212 с** = **0.449** потолка 2700;
по средней двух (279 с) → 4 × 279 = **1116 с** = 0.413. Порог остановки **1890 с** (0.7 потолка) в
обеих оценках не достигнут → заход продолжен. Факт по итогу: **891 с = 0.330 потолка**, то есть
оценка была верхней и не обманула.

## §4 По каждому снимку числом (пункт 4)

Замер — чтением файлов с диска (`tmp/exam58a/measure_58a.py`, головы не зовёт, в сеть не ходит).
Модели считает та же пара функций, что и у 52-a: `suggest._quote_block_from_note` →
`trainer_run.quote_models`.

| кейс | имя | коммит | корпус | `{`+`}` в показанном вопросе | `{`+`}` в сырой строке | quote-блок | моделей | первая | подсказок | меток примеров | исход критика (ждали, с) |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 10 | один день — назван минимальный срок | `aa7b1c4` | `99e6b78177a1d533` | **0** | 2 | НЕТ | 0 | — | **2** | 5 | подсказки (154.5) |
| 11 | «есть ли в наличии?» без данных Bridge | `aa7b1c4` | `99e6b78177a1d533` | **0** | 0 | НЕТ | 0 | — | **0** | 5 | **МОЛЧАНИЕ (257.1)** |
| 12 | тайский в реплике, ฿ сохранён | `aa7b1c4` | `99e6b78177a1d533` | **0** | 2 | есть | **1** | `NMAX` | **3** | 5 | подсказки (74.5) |
| 13 | EN: первый контакт, одна модель | `aa7b1c4` | `99e6b78177a1d533` | **0** | 2 | есть | **3** | `XMAX 300 New Gen` | **3** | 3 | подсказки (98.7) |

**Правило «не больше трёх, первой идёт спрошенная» соблюдено у обоих кейсов, где блок есть.**
Кейс 13 — зеркало кейса 1 и ровно та же тройка: `XMAX 300 New Gen` (спрошенная, **ПЕРВАЯ**),
`ADV 350`, `FORZA 300`. Кейс 12 — одна модель, `NMAX`. Чужой первой нет ни у одного, больше трёх
нет ни у одного.

**Подсказки критика по смыслу** (правила, которые лягут уроком при тапе владельца):
кейс 10 — «не выбирай даты за клиента» и «не подразумевай доступность без проверки»;
кейс 12 — «не подтверждай и не отрицай наличие без календаря», «называй цену и депозит дословно по
прайсу», «сверяй сумму с прайсом до отправки»; кейс 13 — «не подтверждай наличие без проверки»,
«цены и депозиты только дословно из прайс-блока», «собери весь состав брони до закрытия».

## §5 Третий исход у кейса 11 — НЕИЗВЕСТНО, и это записано, а не спрятано (пункт 5)

Снимок `case-11@aa7b1c4.json` **сохранён** и показывается как все: круг головы за него заплачен и
черновик есть (200 знаков). Подсказок в нём ноль, и механизм называет причину своими словами:
`критик НЕ ОТВЕТИЛ — ждали 257.1 с при пределе 257 с`, исход `silent`, длина сырого ответа **0**,
файл `tmp/exam_critic/case-11-20260912T111957Z.txt` лежит и **пуст — 0 байт**, что и есть материальное
доказательство молчания, а не догадка о нём. **Почему голова смолчала — НЕИЗВЕСТНО:** ответа нет
вовсе, разбирать нечего. Круг съел **257.1 с** — это 28.9 % всей цены захода.

Побочное число того же круга: в логе заморозки напечатано `SUGGEST: claude CLI таймаут 257с —
пустой черновик`. Значит поднятый потолок 257 с (52-b) на этом кейсе **исчерпан целиком**, а не
«почти хватило».

## §6 Расхождения с ожиданием — НАЗВАНЫ, НЕ ЧИНЕНЫ (кода заход не правит)

**Р1. Кейс 12: первая модель названа `NMAX`, а корпус ждёт `NMAX 155`.** Совпадение по семейству,
но не дословное: строки `NMAX 155` в записке кейса нет вовсе (проверено поиском — False). Ожидание
`expect.one_model` при этом ни одной веткой снимка не сверяется, поэтому «провалом» это не стало
нигде; расхождение названо здесь и оставлено следующему заходу.

**Р2. Круг головы у кейса 11 ниже пола премисы П3** — 45.9 с против названных 68…121 с. Пол
премисы мерился 11.09 на другом составе кейсов; своё число полосы теперь 45.9 … 100.5 с.

**Р3. Доля молчания критика — 1 из 4 (25 %).** На девятке 52-a при потолке 60 с подсказки пришли у
1 кейса из 9 (2 подсказки из 36 = 5.6 % потолка); здесь при потолке 257 с — у 3 кейсов из 4
(8 подсказок из 16 = **50 %** потолка). Механизм 52-b работает; молчание осталось у одного.

**Р4. Меток примеров у кейса 13 три, а у остальных пять.** Порог отбора у механизма один; разница
идёт от материала (кейс 13 — английский), а не от поломки. Не чинено, названо.

## §7 Чего заход НЕ делал (запреты задания — исполнены)

Наружу не ушло **ни одного** сообщения: `--show`, `--reach` и `dispatch_notify` не звались ни разу,
владельцу ничего не показывалось (показа в этом заходе и не просили). Агент `pc_agent` и демон не
перезапускались; процессов не трогали — единственное касание процессов read-only, `Get-Process` для
ответа на вопрос «идёт ли круг». Клиентских детей не поднимали, о выкатке не спрашивали. Кода не
правили ни строкой. Прежних снимков не удаляли и не перезаписывали — все 12 входных файлов на месте.
В базу уроков `lesson_store.tsv`, реестр ворот и журнал вердиктов `exam_verdicts.tsv` не писали.
Броней и листа «Календарь бронирования» не открывали. `.env` и конфиги не читали; значений токенов
не печатали. **Текста клиентских диалогов в этот артефакт не выведено ни строкой** — только имена
кейсов, имена моделей, смысл подсказок критика и числа.

## §8 Что осталось следующему заходу

1. **Показ владельцу** — четыре карточки готовы и отдаются мгновенно (`--card` / `--show`), но
   показа этот заход не делал: задание его прямо исключило.
2. **Молчание критика у кейса 11** — потолок 257 с исчерпан целиком; следующий шаг либо ещё выше
   потолок, либо короче материал запроса, и решать это числом, а не догадкой.
3. **Кейсы 14–17** — слотов нет ни одного; по цене этого захода четвёрка стоит ≈891 с.
