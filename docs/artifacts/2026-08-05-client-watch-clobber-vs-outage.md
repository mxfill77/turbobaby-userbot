# Подмена снимка надзора тестом против простоя userbot 397 минут — связи нет

**Заход:** headless, 05.08.2026 12:20–12:35 (+07). **Только чтение**, кода не касался, фикс `1724eb5`
не переделывал. Процессы не трогал, ничего не удалял, `.env` не открывал, на сервер не ходил.

## Итог одной строкой

**Подмена `client_watch.json` тестом НЕ объясняет вчерашний простой и в принципе не могла его вызвать:
для демона файл — write-only, решения вотчдога живут в ОЗУ.** Причина простоя другая и уже измерена
(сеть → 3 смерти → латч `halted`). Вдобавок **ни одна подмена за 14 суток не попала внутрь окна
простоя** — ближайшая была за 32 минуты до первой смерти.

---

## 1. Что хранит файл и кто его читает

**Схема** (`pc_orchestrator.py:5755`): `{ts, cooldown, max_deaths, children:{имя:{deaths, halted,
last_raise}}}`. Живой файл сейчас — 268 б, `mtime 12:23:57`.

| роль | место | как часто |
|---|---|---|
| **пишет** | `_persist_client_watch` (`pc_orchestrator.py:5742`) — единственный касающийся файла код; открытие `"w"` + `os.replace` (5757–5761) | из ОДНОЙ точки `maybe_client_watchdog` (5935), после каждого тика; троттлинг `CLIENT_WATCH_SEC = 300 с` (:132) |
| **читает** | **ровно один** — `pc_agent._read_watch_snapshot` (`pc_agent.py:394`) → `_supervision_label` (:405) → `status_text` (:433) → телеграм-хендлер (:731) | по команде «статус» с телефона |

Зачем вообще файл: `pc_agent` — **отдельный процесс**, ОЗУ демона ему недоступна; строку
«надзор: под вотчдогом / cooldown / ОСТАНОВЛЕН» он берёт только отсюда.

**Читающих мест в самом демоне — ноль** (греп по всему дереву: `CLIENT_WATCH_FILE` встречается в
`pc_orchestrator.py` трижды — объявление :211, обёртка `_state` :267, запись :5747).

## 2. Мог ли подменённый снимок обмануть вотчдог — НЕТ, по построению

Ни в сторону «жив», ни в сторону «сдаться». Доказательство по коду, четыре звена:

1. **Состояние решения — в ОЗУ.** `_client_watch_state = {}` (`:5635`) — модульный dict. Докстринг
   `:5632` прямо: «Состояние в памяти демона… рестарт демона его сбрасывает».
2. **Тик берёт ОЗУ, а не диск.** `client_watchdog_tick`: `state = _client_watch_state if state is
   None` (`:5822`). `_client_watch_step` (`:5724`) считает `alive|raise|cooldown|halted|halt_now`
   исключительно из `state.get(name)`.
3. **Живость меряется не файлом.** `_find_pids_by_script` (CIM, `:5645`) + свежесть лога
   `_log_age_sec` (`:5716`). Файл в вердикт не входит ни одним полем.
4. **Обратного потока нет.** В демоне нет ни `open`, ни `json.load` по `CLIENT_WATCH_FILE`.
   Даже рестарт демона не воскресит `halted` с диска — `_client_watch_state` заново `{}`.

Направление одностороннее: **ОЗУ → диск → текст статуса**. Обратно не течёт ничего.

**Весь радиус поражения подмены** — одна строка в ответе `pc_agent status`. Фикстура
`{"ts": 1301.0, …, "children": {}}` (64 б) в `_supervision_label` даёт
`now - ts > WATCH_SNAPSHOT_STALE (900 с, pc_agent.py:73)` → **«? демон молчит (снимок протух)»**
вместо правды. И знак важен: фикстура пишет `children: {}`, то есть `deaths = 0` — она способна
только **занизить** тревогу, а выдумать «сдался» не может.

Длительность порчи — до следующего тика; но `maybe_client_watchdog` зовётся из тела главного цикла
(`:7353`), а цикл блокирован на время задачи (замер 31.07:
[`2026-07-31-watchdog-raise-visible.md`](2026-07-31-watchdog-raise-visible.md) §2 «задержка
обнаружения смерти = длительность задачи»). Значит окно порчи = длительность идущей задачи, а не 5 мин.

## 3. Сколько раз подменяли и совпадает ли с простоями

**Кто именно подменял.** `test_pc_orchestrator.TestClientWatchdog.test_throttle_maybe_client_watchdog`
(`test_pc_orchestrator.py:2268`) подменяет только `client_watchdog_tick`, а `_persist_client_watch` —
нет. Поэтому `maybe_client_watchdog(now=1000)` и `(now=1000+301)` пишут в боевой путь по два раза за
прогон; выживает `ts = 1301.0`, а `children` пусты, потому что в тест-процессе `_client_watch_state`
пуст. Это дословно та фикстура, что нашёл перехват. (Соседний
`TestClientWatchSnapshot.test_maybe_watchdog_persists_after_tick` (:2378) подменяет писателя и не
пишет — потому греп по именам констант дефект и не находил.)

**Окно замера:** 22.07 20:29 → 05.08 12:26 (начало `pretool_guard.log.1`; лог демона с 22.07 23:11).

| источник подмены | до фикса `1724eb5` (05.08 00:59:08) | после |
|---|---|---|
| интерактивные прогоны (`discover` или модуль целиком), `pretool_guard.log` | **146** | 5 → записей нет |
| гейт самообновления демона (`_gate_unittests`, `:3310`, гоняет `test_pc_orchestrator`) | **34** (из 36 всего; провалов гейта 0) | 2 → записей нет |
| **итого подмен** | **≥ 180** (≈360 записей) | **0** |

`≥` честное: `pretool_guard.log` видит только команды сессий, ручные прогоны с консоли в него не
попадают. Собственный гейт демона в списке — потому что он гоняет ровно `test_pc_orchestrator`
подпроцессом с `cwd=REPO`: **демон портил свой же снимок при каждом самообновлении.**

**Совпадение с простоями userbot — НОЛЬ.** Все простои окна по логу вотчдога:

| простой (лок. +07) | подмены внутри окна | ближайшая подмена ДО |
|---|---|---|
| 31.07 ~15:05 → (смерти 15:13:05, 15:33:36, **halt 15:52:17**) | **0** | гейт демона 14:39:34 — за **26 мин** до первой смерти |
| 05.08 01:13:16 → 01:18:50 | **0** | интерактивный 00:41:05 — за **32 мин** |
| 05.08 01:19:56 → 01:38:47 | **0** | — |
| 05.08 01:39:53 → 07:50:31 (**370 м 37 с**, тот самый простой) | **0** | — |
| 05.08 09:27:12 → 09:30:08 и 09:31:14 → 09:51:36 | **0** | — |

Следующие подмены после 00:41:05 — гейт 01:01:25 (уже с фиксом на диске) и прогоны с 11:02:54, все
**вне** окон простоя. Больше того: во время простоя демон **тикал и переписывал снимок правдой** —
подъёмы 01:19:00 и 01:38:58 в логе, а снимок, прочитанный в 07:43:05, держал
`deaths: 3, halted: true, last_raise 1785869923.96` (см.
[`2026-08-05-userbot-raise-after-outage.md`](2026-08-05-userbot-raise-after-outage.md) §1) — живое
состояние, не фикстуру.

**Настоящая причина простоя остаётся прежней и уже измерена:** обрыв сети ПК (`WinError 1236/1231`)
→ два подъёма, каждый прожил 66 с и умер на 6 неудачных коннектах → `CLIENT_MAX_DEATHS = 3` →
`halt_now` в 01:58:44. Латч снимается только оживанием процесса, а оживить после `halt` мог только
человек — отсюда 321 минута лежания уже при вернувшейся сети. Разборы:
[`2026-08-05-userbot-crashes-and-mute-session-5348.md`](2026-08-05-userbot-crashes-and-mute-session-5348.md),
[`2026-08-05-userbot-raise-after-outage.md`](2026-08-05-userbot-raise-after-outage.md) §7–8.

## 4. Какие ещё файлы надзора тест может подменить

Закрыто фиксом — 12 констант через `log_setup.state_path` (11 в `pc_orchestrator` + `LEDGER_PATH`),
включая `WD_STATE_FILE` (состояние собственного вотчдога демона) и сам `CLIENT_WATCH_FILE`.
Остаётся, той же формы «у живой функции путь есть по умолчанию»:

| файл | форма | чем опасен |
|---|---|---|
| **`pc_orchestrator.heartbeat`** | `_write_heartbeat()` (`:806`) — **параметра пути НЕТ ВООБЩЕ**, изолировать можно только моком имени | **Самый острый: этот файл читает ПРИНИМАЮЩИЙ РЕШЕНИЕ.** `watchdog()` признаёт демона мёртвым по `_heartbeat_fresh` / `_log_age_sec(HEARTBEAT_FILE)` (`:7373`, `:7537`) и поднимает через schtasks. Свежая метка от теста = «мёртвый демон считается живым» — ровно тот режим отказа, которого у `client_watch.json` нет. Сейчас держится на двух моках по имени (`test_pc_orchestrator.py:3757`, `:6826`) — это ровно два вызова `poll_once()` (писатель на `:3248`); третий забывший вызов течёт |
| **`pc_session_watch.state.json`** | `session_watch.py:80`; `tick(state_path=None)`, `save_state(path=None)` | реестр «уже сигналили» детектора немых сессий — надзор за сессиями. На `state_path` не переведён; изоляция держится на договорённости (оба вызова в `test_session_watch.py` путь передают) |
| `pc_orchestrator.client_release.json`, `pc_orchestrator.client_trainer_green.json` | `client_contour.py:293–294`; `approve(path=None)` (`:322`), гейт `_load(path or TRAINER_GREEN_FILE)` (`:381`) | ворота выката клиентского контура. Сегодня тесты путь передают везде; голый `approve("не-коммит")` (`test_client_contour.py:227`) выходит до записи |
| `cowork_log.spool` | `cowork_log_append.py:22`, `path or SPOOL_PATH` в трёх местах | асимметрия: близнец `LEDGER_PATH` переведён на `state_path`, спул — нет; держится на подмене константы в `setUp` |
| `trainer_rules.json`, `pc_ask_*.marker`, `moderation_ipc.db` | уже названы остатками в [`2026-08-05-test-writes-to-live-state.md`](2026-08-05-test-writes-to-live-state.md) §4 | первый — накопленные выученные правила вне git; второй — 51 тест по боевым именам; третья — боевая очередь открыта в гейте самообновления (там нет `test_isolation`) |

Не течёт: `pc_orchestrator.stop` (ни один тест его не создаёт — рубильник вырубил бы весь
контур-вотчдог), `pc_orchestrator.lock` и `pc_agent.lock` (тесты инъектируют `lock_path`).

## 5. Итог (5 строк)

1. `client_watch.json` — витрина для отдельного процесса `pc_agent`, а не память вотчдога: демон в
   него только пишет, читающих мест в демоне ноль.
2. Подмена не могла ни удержать вердикт «жив», ни вызвать «сдался»: вердикт считается из ОЗУ
   (`_client_watch_state`), живость — из CIM и свежести лога; знак фикстуры вообще занижающий.
3. За 14 суток подмен **≥ 180** (146 интерактивных + 34 гейта самообновления), внутрь окон простоя
   попало **0**; ближайшая — за 32 минуты до первой смерти, и её демон переписал правдой до halt.
4. Вчерашние 397 минут объясняются целиком прежней причиной: сеть → 3 смерти по 66 с → латч
   `halted`, который снимает только оживший процесс. Фикс `1724eb5` вчерашний простой **не объясняет**
   и объяснять не должен — он закрыл порчу витрины, а не дефект надзора.
5. Открытым остаётся однофамилец с обратным знаком: **`pc_orchestrator.heartbeat`** — у него пути в
   сигнатуре нет вовсе, а читает его тот, кто принимает решение о подъёме демона. Правки не делал.
