# Остаток задачи 16 — В КОММИТ НЕ УШЁЛ: гейт красен на 4 при потолке 2

**Дата:** 17.08.2026, заход «забрать остаток задачи 16 в коммит».
**Итог одной строкой: КОММИТА ОСТАТКА НЕТ.** Замок B задания — «красных было две, больше —
не коммить, откатить и сказать» — сработал: полный гейт с остатком в дереве дал **4 красных**
против **2** до правки. Остаток лежит в дереве нетронутым, ровно как лежал.

> **СЛЕДУЮЩЕМУ ЗАХОДУ.** Наличие ЭТОГО файла **не значит «остаток забран»**. Признак сделанности
> — чистое дерево. Здесь дерево ГРЯЗНОЕ (`suggest.py` изменён, `price_source.py` и
> `test_price_source.py` не отслеживаются). Что чинить — §7, одна строка.

---

## 1. Что проверялось и чем (живые числа)

| проверка | результат |
|---|---|
| дерево против патча `_scratch_task16_0817/suggest.py.task16-residual.patch` | **СОШЛОСЬ побайтно**, 3498 Б обоих, sha256 `f5e8db82…95c62b` |
| та же сверка ПОСЛЕ прогона гейта | **сошлось снова**, тот же sha256 — гейт дерева не тронул |
| отслеживаемых правок в дереве всего | **1 файл** (`git diff --name-only HEAD` → `suggest.py`) |
| гейт ДО правки (`_scratch_pricewire_0817/gate_before.txt`) | `Ran 3628 tests in 229.317s` · **FAILED (failures=2, skipped=10)** |
| гейт СЕЙЧАС (`_scratch_task16commit_0817/gate_commit.txt`) | `Ran 3664 tests in 251.798s` · **FAILED (failures=4, skipped=10)**, EXIT=1 |

Прирост тестов **3664 − 3628 = 36** — это ровно число юнитов в `test_price_source.py`
(`grep -c "def test_"` → 36). Все 36 **зелены**: ни один из четырёх красных им не принадлежит.

## 2. Что ушло бы в коммит — дословно

Ни одной новой строки сверх лежавшего в дереве не написано и не предполагалось.

| файл | состояние | размер |
|---|---|---|
| `price_source.py` | новый, не отслеживается | **312 строк**, 18 297 Б |
| `test_price_source.py` | новый, не отслеживается | **461 строка**, 25 107 Б, **36 юнитов** в 8 классах |
| `suggest.py` | изменён | **+25 / −2** (`git diff --stat`) |

Правка `suggest.py` — две точки: строка импорта `import price_source` и врезка в
`_safe_quote_for_model` (единственная точка точечной котировки; `price_sheet` сюда не ходит).
Данные (`price_source.json`) в git **уже лежат** — коммит `83f1591`, в дереве чисты.

Публичные функции модуля: `enabled`, `load`, `period_of`, `bucket_of`, `resolve_row`,
`day_price`, `cap_for`, `reprice`.

## 3. Флаг и его дефолт

**Имя ручки: `PRICE_SOURCE_SEASON`** (env; `price_source.py:43`, `ENV_FLAG`).

```python
_TRUE = ("1", "true", "yes", "on", "да")
def enabled():
    return (os.getenv(ENV_FLAG) or "").strip().lower() in _TRUE
```

**Дефолт БЕЗОПАСЕН, правки не потребовал** (пункт 3 задания — единственная разрешённая правка
не понадобилась). Нет значения → `os.getenv` даёт `None` → `or ""` → `""` → не в `_TRUE` → **OFF**.
Пустое и мусор — так же. Юнит `test_flag_empty_and_garbage_is_off` перебирает
`"", "   ", "0", "нет", "off", "false", "мусор", "PRICE", "2"` — все OFF. Читается **на каждом
вызове**, не на импорте: снятие флага действует сразу (`test_flag_read_on_every_call_not_on_import`).

**Чего этот заход НЕ знает:** каково значение `PRICE_SOURCE_SEASON` в БОЕВОМ окружении. Чтение
конфигов заданием запрещено, и обходить запрет не стал. Безопасен ДЕФОЛТ КОДА; поднята ли ручка
снаружи — проверка за владельцем.

## 4. Секретов в уходящем нет

`grep -Ei "token|secret|password|api_key|bearer|credential|AKIA|BEGIN"`:

* по диффу `suggest.py` — **0 совпадений**;
* по `price_source.py` — **0**;
* по `test_price_source.py` — **3**, все три суть ИМЕНА атрибутов чужого модуля в `setUp`
  (`pricing.BRIDGE_URL`, `pricing.BRIDGE_TOKEN`), а присваиваемые значения — заглушки
  `"u"` и `"t"`. Ни одного боевого значения.

---

## 5. Четыре замка числами

### Замок A — при снятом флаге поведение прежнее, ПОСИМВОЛЬНО

Сверка не «похоже», а `ast.dump` инструкций: `git show HEAD:suggest.py` против дерева, докстринг
из сравнения выброшен сознательно (поведения не несёт). Проба —
`_scratch_task16commit_0817/lock_a_probe.py`, вывод — `lock_a.txt`.

```
инструкций в HEAD: 3 · в дереве: 5
совпавших префиксом инструкций: 2 из 3 (HEAD)   [0] Try  [1] If
хвост HEAD:        Return(value=Name(id='res', ctx=Load()))
внутри охраны:     Return(value=Name(id='res', ctx=Load()))
ХВОСТ ДОСЛОВНО СОВПАЛ: True
sha256(ast) HEAD-хвост    = d0f7858d6056b8a7dcba4e212b22c2161b68e170e979d63ce580e0c8c96095f5
sha256(ast) внутри охраны = d0f7858d6056b8a7dcba4e212b22c2161b68e170e979d63ce580e0c8c96095f5
вставлено перед охраной: Try, вызовы внутри: ['price_source.enabled']
инструкций после охраны: 1 (при снятом флаге недостижимы: охрана — return)
price_source верхний уровень: Assign, Expr, FunctionDef, Import
enabled() зовёт: os.getenv (+ .strip/.lower на строке)
```

Читается так: **обе прежние инструкции совпали дословно** (одинаковый `ast.dump`), а третья,
`return res`, живёт внутри охраны `if not on:` с **тем же sha256 разбора**. Перед охраной
вставлена ровно одна инструкция, и единственный вызов в ней — `price_source.enabled()`, который
зовёт только `os.getenv` (ни файла, ни сети). Единственная инструкция после охраны при снятом
флаге недостижима: охрана — `return`. Импорт нового модуля I/O не делает: верхний уровень
`price_source.py` — только `Import`, `Assign`, `Expr`, `FunctionDef`.

### Замок B — гейт полный: **было 2, стало 4. НЕ КОММИТИМ**

Команда: `TURBOBABY_TEST_LOGS=1 venv\Scripts\python.exe -m unittest discover -s . -p "test_*.py"`
из корня репо. `EXIT=1`, `Ran 3664 tests in 251.798s`, `FAILED (failures=4, skipped=10)`.

| # | красный | происхождение |
|---|---|---|
| 1 | `test_nonparse_ratchet.TestRatchet.test_no_growth_against_baseline` | известный храповик `main`, красен ДО правки |
| 2 | `test_nonparse_ratchet.TestRatchet.test_totals_do_not_grow` | то же |
| 3 | `test_pc_orchestrator.TestSelfUpdateDepClosure.test_lazy_dependencies_are_a_named_remainder_not_a_silent_gap` | **НОВЫЙ, от остатка** |
| 4 | `test_pretool_guard.TestMarkerIsolation.test_live_marker_stays_empty_after_full_suite` | **НОВЫЙ, ПОСЛЕДСТВИЕ №3** |

Корень у обоих новых **ОДИН**:

```
File "D:\turbobaby-bot\test_pc_orchestrator.py", line 9602, in test_lazy_dependencies_...
    self.assertEqual(lazy, set(o._ORCH_LAZY_UNCOVERED))
AssertionError: Items in the first set but not the second:
'price_source.py'
```

`suggest.py` импортирует `price_source`, а `suggest.py` сам стои́т в списке ленивых зависимостей
`_ORCH_LAZY_UNCOVERED` (`pc_orchestrator.py:5591–5593`). Новый модуль становится новой ленивой
зависимостью, не названной остатком, — тест ровно это и ловит («названный остаток, а не молчаливая
дыра»). Красный №4 — производный: `TestMarkerIsolation` гоняет полный набор **подпроцессом** и
судит по коду возврата; внутренний прогон (`Ran 1217 tests … FAILED (failures=1, skipped=1)`) упал
на том же самом `'price_source.py'`, и внешний тест увидел `1 != 0`.

**Это НЕ выдумка теста и не флак:** тест защищает ворота грязного дерева self-update, а остаток
действительно вводит модуль, о котором эти ворота не знают.

### Замок C — клиентам ноль

Прибор О5 (`client_silence_pc.py`, чтение боевой базы **только** `mode=ro`), снят дважды —
до работы и после гейта. Оба снимка **дословно совпадают**:

```
ОТПРАВЛЕНО КЛИЕНТУ: 0 (последнее: None)
попыток без доставки: 0 · взведено к отправке: 0 · всего черновиков: 470
разрез по статусам: {"posted": 434, "rejected": 21, "test_held": 15}
второй свидетель (suggest_pairs.jsonl): строк=0, отправок=0 (файла нет)
```

**О5 остался «тихо»:** отправлено 0, попыток 0, взведено 0 — оба свидетеля. Файлы:
`_scratch_task16commit_0817/o5_before.txt` и `o5_after.txt`.

### Замок D — замер не потерян

Результаты прогона задачи 16 **целы**, лежат в `_scratch_pricewire_0817/` (все от 17.08 20:05–20:12):

| файл | Б | что в нём |
|---|---|---|
| `score_fixed.txt` | 9 440 | ИТОГОВЫЙ СЧЁТ: честный знаменатель `0/15 → 2/15` |
| `verdicts_both.json` | 19 534 | вердикты обоих прогонов |
| `drafts_fixed.jsonl` | 76 869 | черновики «стало» |
| `drafts_wired.jsonl` | 75 572 | черновики «было» |
| `score_out.txt` | 9 524 | промежуточный счёт |
| `gate_before.txt` | 31 290 | гейт ДО правки (2 красных) |

Рядом цел и корпус серии — `_scratch_seriespc_0817/` (`series_report.txt`, `terminals_*.txt`,
`gate_after*.txt`). Каталог `_scratch_task16_0817/` с патчем не тронут. **Ничего не удалено.**

---

## 6. Чего заход НЕ делал

Клиентам не писал (замок C числами). Правил, промптов и книги правил не касался. В рабочие
таблицы не писал. Боевую `moderation_ipc.db` открывал **только** `mode=ro` штатным глазом О5.
Процессов не перезапускал. Ничего не удалял. Временное — только в `_scratch_task16commit_0817/`,
каталог оставлен. Конфиги не читал.

## 7. Остаток — что чинить следующему заходу (НЕ СДЕЛАНО ЗДЕСЬ СОЗНАТЕЛЬНО)

1. **Одна строка закрывает оба новых красных.** В `pc_orchestrator.py:5591–5593` в кортеж
   `_ORCH_LAZY_UNCOVERED` добавить `"price_source.py"` — рядом с уже стоящими там `"suggest.py"`,
   `"pricing.py"`, `"delivery.py"`. Это правка КОДА, а задание этого захода — «кода не править,
   ни одной новой строки», поэтому она не внесена. После неё ожидаемо: 4 → 2 красных (храповик
   `main`), и остаток можно забирать.
   **Проверять числом, а не на глаз:** полный гейт обязан дать `failures=2`, и `Ran` обязан
   остаться 3664.
2. **Значение `PRICE_SOURCE_SEASON` в боевом окружении не проверено** — чтение конфигов запрещено
   заданием. Безопасен дефолт кода; поднята ли ручка снаружи, знает владелец.
3. **Порядок для следующего захода:** правка п.1 → полный гейт → если `failures=2`, одним
   коммитом уходят ЧЕТЫРЕ файла: `price_source.py`, `test_price_source.py`, `suggest.py` и
   правка `pc_orchestrator.py`.

## 8. Что владельцу решать

Развилки нет, есть выбор темпа: правка п.1 однострочна и её ожидаемый эффект назван числом.
Заход её не внёс потому, что задание прямо запретило любую правку сверх безопасного дефолта,
а дефолт оказался безопасным сам. Разрешение «правь `_ORCH_LAZY_UNCOVERED` и коммить» снимает
блокировку целиком.

Основание захода: [`docs/artifacts/2026-08-17-task16-postmortem.md`](2026-08-17-task16-postmortem.md) (`8d07f49`).
