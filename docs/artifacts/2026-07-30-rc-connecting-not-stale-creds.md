# RC «Connecting и тишина» 30.07.2026: известный корень НЕ подтвердился, снимать нечего

Задание пришло с «да» владельца на класс `kill` (снять rc-server по сценарию протухших кредов).
**Предусловие ШАГА 1 не выполнилось — процесс НЕ старше кредов, поэтому НИЧЕГО не снято.**
Ниже — чем это доказано и что на самом деле ломает подъём сессий.

Время в логах CLI — UTC, местное = UTC+7. Замер сделан 30.07.2026 19:53–19:58 местного.

## 1. Предусловие: процесс МОЛОЖЕ кредов на 75 с

| Объект | Время | Факт |
|---|---|---|
| `.credentials.json` | 30.07 **12:41:25** (Creation == LastWrite) | файл переписан целиком |
| rc-server pid **4872** | старт 30.07 **12:42:40** | на 1 м 15 с ПОЗЖЕ кредов |
| rc_supervisor pid **11168** | старт 30.07 **01:17:19** | родитель — `rc_remote_control.vbs` pid 9232 |
| named-channel pid 17504 | старт 30.07 19:36:14 | снят в 19:57:44 (см. §3) |

Сервер поднялся ПОСЛЕ ротации кредов, то есть взял свежие. Сценарий «сервер держит отозванный
токен» физически невозможен. По условию задания — остановка, ничего не гашу.

## 2. Улик отказа авторизации нет ни одной

`rc_server_debug.log` (514 строк, вся жизнь текущего сервера с 05:42:41Z):

- строк `failed 401: OAuth access token has been revoked` — **0**;
- единственное совпадение по слову `revoked` (строка 230) — **ложное**: это имя файла-фикстуры
  `rc_server_auth_revoked.live.log` внутри `tool_result` чужой сессии. Ровно тот мусор, о котором
  предупреждал артефакт 29.07 (78 % ложных на голом грепе);
- спаунов за всю жизнь сервера — **два**, оба ЖИВЫ: `cse_01WzkVLj…` (pid 19228, 05:42:45Z) и
  `cse_01EUYom…` (pid 3152, 12:36:16Z). Строк `exited status=failed` — **0**.

Подпись протухшего токена — «новая сессия умирает за 2–3 с» — отсутствует. Более того, сессия
`cse_01EUYom…` **отработала штатно**: 12:36→12:47:36Z (19:36→19:47:36 местного), 36 ходов,
`Session completed`, разрешение на Bash владелец дал с телефона в 12:46:09Z. Это на 6 минут
раньше жалобы. **RC-сервер рабочий.**

## 3. Что ломается на самом деле: churn именованного канала

Сторож гасит ветку `named-channel` (`claude --remote-control turbobaby-pc`) ложной зомби-пробой
каждые ~21,5 мин. Счёт за сегодня:

```
ЗОМБИ named-channel: 27      старты named-channel: 28
ЗОМБИ rc-server:      1      старты rc-server:      2
```

Поймано в прямом эфире во время замера:

```
2026-07-30 19:36:14,102 | [named-channel] старт pid=17504
2026-07-30 19:57:44,604 | [named-channel] ЗОМБИ: канал мёртв 3 проверок подряд
                          (лог протух И 0 соединений) — гашу pid=17504, рестарт
```

21 м 30 с жизни — и снос. Почему проба не может позеленеть **никогда**: `rc_session_debug.log`
после подъёма содержит только строки старта (последняя — `[Claude in Chrome] Found chrome
profiles`, +0,7 с от запуска) и дальше молчит. Порог `RC_LIVENESS_MAX_LOG_AGE=1200` с срабатывает
гарантированно, `0 соединений` у канала — норма (замер 29.07: 0/10 при 4/10 у живого сервера).
Предикат меряет то, чего у этой ветки не бывает.

Каждый снос — **новый environmentId**. Симптом владельца «Connecting и тишина» ложится сюда:
телефон держит среду, которую сторож уже снёс, а в списке сред копятся десятки мёртвых
`turbobaby-pc` (класс `rc-env-churn-false-zombie`, CLI-удаления нет). Косвенное подтверждение:
успешная сессия 19:36:16 стартовала **через 2 секунды** после подъёма свежего канала в 19:36:14.

Оговорка честная: сторону телефона отсюда не видно, список сред не читается. Это сильнейшая
гипотеза по логам, а не доказанный факт.

## 4. ШАГ 5: вариант (а) применён, вооружён и молчит ПРАВИЛЬНО

Вопрос был «активирован ли он вообще, или ждёт входа в систему». Ответ фактами:

- код есть: `rc_auth_detect.py` + проводка в `rc_supervisor.py`, коммит **aed3e4e от 29.07.2026
  18:55:37**; `SERVER_SPEC["auth_watch"]=True` (`rc_supervisor.py:106`), вызов
  `_auth.scan(spec["log"], auth_state)` внутри монитора каждый круг (`rc_supervisor.py:470`);
- **в бою он живой**: сторож pid 11168 стартовал 30.07 **01:17:19** — позже коммита (29.07 18:55)
  и позже mtime файлов (29.07 18:43/18:44). Процесс НЕ ждёт входа в систему, версия кода свежая;
- клапан открыт: `ENABLED = os.getenv("RC_AUTH_WATCH","1") != "0"` (`rc_auth_detect.py:71`),
  `rc_supervisor.py` **не читает `.env`** (ни `load_env`, ни `dotenv`) — переменные только из
  среды Планировщика, где выключателя нет;
- **не сработал потому, что срабатывать было не на что**: за смену ноль дословных фраз отказа
  (§2). Молчание детектора — правильное поведение, а не отказ.

Отдельная деталь: рестарт rc-server в 12:42:24, через 59 с после ротации кредов, сделал НЕ
детектор, а **та самая ложная зомби-проба** («ЗОМБИ: канал мёртв 3 проверок подряд») — единственное
её срабатывание на сервере за сутки. Сегодняшнюю ротацию кредов случайно закрыл churn-баг.

## 5. Развилка владельцу (правки НЕ делал)

Чинить надо **пробу живости ветки `named-channel`**, а не авторизацию. Варианты:

1. **Снять пробу с этой ветки** (`max_age`/liveness только у `rc-server`, как `auth_watch`).
   Дёшево, одна строка в `NAMED_SPEC`. Минус: настоящий труп канала ловиться перестанет —
   останется только выход процесса.
2. **Сменить признак живости** на тот, что у канала бывает: не возраст его debug-лога, а факт
   регистрации среды/сокета. Дороже, нужен живой признак — сначала замер, чем канал вообще
   отчитывается.
3. **Поднять порог** `RC_LIVENESS_MAX_LOG_AGE` для ветки до суток. Полумера: churn станет редким,
   но не исчезнет, а мёртвые среды продолжат копиться.

Рекомендация — (1): предикат, который для ветки не зеленеет никогда, не страховка, а генератор
отказов; 27 сносов против 1 у сервера — это не защита, это и есть поломка.

> **✅ СТАТУС НА 31.07.2026: РАЗВИЛКА ЗАКРЫТА, но взят НЕ рекомендованный вариант.**
> Принят вариант **(3) — поднять порог**, а не (1). В коде: проба у ветки **осталась**
> (`rc_supervisor.py` → `probe_detail` гоняется на обеих ветках), порог именованной ветки
> поднят до `NAMED_LIVENESS_MAX_AGE = 43200` (12 часов, а не «до суток»/86400, как
> предлагалось здесь), плюс добавлены страйки: `LIVENESS_STRIKES = 3` — одной неудачной
> пробы больше не хватает.
> **Не исполняй рекомендацию (1) задним числом:** снятие пробы с ветки уберёт ловлю
> настоящего трупа канала, ради которой порог и оставляли.
