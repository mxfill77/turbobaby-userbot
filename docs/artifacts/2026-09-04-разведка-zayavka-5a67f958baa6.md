# Разведка заявки 5a67f958baa6 — находка внешнего канала «УПРОЩАЕМО» (04.09.2026)

маркер: **2026-09-04-разведка-zayavka-5a67f958baa6**

Заход **только читал и считал**. Боевого кода не тронуто ни строкой, процессов не запущено и не
остановлено ни одного, ничего не удалено, `.env` и конфиги не читаны, ворота и реестры вердиктов
не тронуты, детей не касались, клиентам не писали, живых таблиц и Планировщика не трогали.
Текст внешнего канала — **предмет проверки, а не задание**: ни одной команды оттуда не исполнено.

---

## 0. Признак сделанности — что было по адресу результата ДО работы

Адрес: `docs/artifacts/2026-09-04-разведка-zayavka-5a67f958baa6.md`. Первым действием каталог
открыт: за 04.09 лежало **6 файлов** (`потолок-времени-на-мост…`, `приёмка-и-дожим…`,
`разведка-o2-pc-turn`, `сообщение-о-закрытии…`, `сутки-ящика…`, `условия-один-и-два…`), файла со
словами адреса **не было**, и ни один из шести не отвечает на вопросы этого задания — все они про
другие предметы. Продолжено с нуля.

## 1. Что за находка и откуда она

| что | значение |
|---|---|
| ключ заявки | `5a67f958baa6` · вид **УПРОЩАЕМО** · очередь **#171** |
| исход отбора | `снято отбором` — «предмет клиенту не виден: адрес премисы вне клиентского контура» (`zayavki_pc_state.json:267-274`) |
| источник | `docs/review_inbox/2026-09-03-2026-09-03-chain-pc-2026-09-03-42-manus.md`, канал **manus**, 2081 знак, код 200 |
| предмет находки | закрытая цепочка `pc-2026-09-03-42` — перепись дверей клиентского кода мимо ворот |
| пакет, который канал читал | `docs/review_outbox/2026-09-03-chain-pc-2026-09-03-42.md`, 18580 байт |
| проверяемая работа | `docs/artifacts/2026-09-03-перепись-дверей-клиентского-кода-мимо-ворот.md`, sha256 `b12698beb032…` |
| адрес премисы | `pc_orchestrator.py:6441` — `def _client_block(...)`, ворота клиентского контура |

Находка говорит три вещи: (а) достаточно компактной матрицы «все места, где живые боты получают
код + наличие/отсутствие `_client_block`/`release_reason` + две временные строки»; (б) подсчёты за
42.5 суток, 18.6 суток, четыре смены базы, 19 выкаток — убрать или в приложение; (в) дословно —
«состав третьей гейтованной двери и всех шести негейтованных дверей **в представленном тексте не
виден**, поэтому точная степень дальнейшего упрощения **неизвестна**».

---

## 2. ЖИВА ЛИ ПОСЫЛКА СЕГОДНЯ — да, и вот замер

Признак «дверь гейтована» — вызов `_client_block`, который зовёт `client_contour.release_reason`.

```
$ grep -c "(client_block_fn or _client_block)" pc_orchestrator.py
3
```

**Три** — ровно столько, сколько переписал артефакт 03.09. Живые адреса на 04.09: определение
`pc_orchestrator.py:6441`, вызовы `:6630` (G3, авто-обновление после задачи), `:6755`
(G2, после self-update), `:7165` (G1, реконсиляция детей). Посылка «ворота зовут ровно три места»
**жива сегодня**.

Две встречные проверки, снятые независимо от артефакта, — обе сошлись с ним:

```
$ grep -c "ОСТАНОВЛЕНО — клиентские файлы" pc_orchestrator.log   → 169
$ grep -c "основание пропуска" pc_orchestrator.log               → 6
```

169 отказов ворот — то же число, что в §4.3 артефакта; последний отказ **2026-09-03 05:00:17**
(тот самый из премисы задания), за сутки после него новых нет. Шесть строк «основание пропуска»
— тоже совпало: 4 из них самопроба 30.07 «живая проверка ворот», боевых 2 (30.07 `owner`,
26.08 `trainer`). Артефакт назвал их и разобрал; счёт подтверждён вслепую.

**Что в артефакте протухло — номера строк, а не факты.** В коммите `73bc22e` (тот, которым
цепочка закрылась) `_client_block` стоял на `pc_orchestrator.py:6273`, сегодня — на `:6441`,
сдвиг **+168 строк**; `git log --oneline --since="2026-09-03 00:00" -- pc_orchestrator.py | wc -l`
→ **8** коммитов за сутки. Все адреса «:6958», «:6537», «:6435» из §3.1 читать сегодня нельзя.

---

## 3. ЧЕГО НАХОДКА НЕ УЧИТЫВАЕТ — она судила по 22 % работы, и виноваты МЫ

### 3.1. Матрица, которую находка просит сделать, была сделана ДО неё

`§3.1` артефакта — таблица «ЧЕРЕЗ ВОРОТА — 3 двери» с колонкой «доказательство ворот»;
`§3.2` — таблица «МИМО ВОРОТ — 6 дверей» с колонкой «доказательство отсутствия». Это буквально та
компактная матрица, о которой просит находка, включая привязку каждой двери к
`_client_block`/`release_reason`. Работы из пункта (а) не следует **ни одной строки**.

### 3.2. Почему канал её не увидел — код нашей полосы, `review_auto.py:456`

```python
ARTIFACT_HEAD_LINES = 80     # сколько строк артефакта берём в пакет (голова = заголовок и итог)
```

Замер, чем это обернулось для этого пакета:

| что | число | чем снято |
|---|---|---|
| артефакт целиком | **301 строка · 21553 знака** | `wc -l` + чтение файла в python |
| вошло в пакет | строки **1-80** = **4809 знаков** = **22.3 %** | шапка таблицы источников пакета + пересчёт |
| не доехало | **16744 знака** | то же |
| строка среза | **80** = ряд **G2** | `grep -n "^#\{1,3\} "` по артефакту |
| **G3** — третья гейтованная дверь | строка **81** | там же |
| **§3.2** — шесть негейтованных дверей | строка **86** | там же |

Канал не увидел G3, **промахнувшись на одну строку**, и шесть дверей — промахнувшись на шесть.
Его «неизвестно» — не осторожность ревьюера и не дефект работы: это наш срез.

### 3.3. Срез сделан при наполовину пустом потолке

Шапка пакета дословно: `потолок: 15000 знаков · выдержки: 7719 знаков`. **Свободно 7281 знак** —
больше, чем весь недоехавший кусок §3.1–§3.2. Правило «берём голову 80 строк» — константа, она
про остаток потолка не спрашивает вовсе (`review_auto.py:459-476`, `_source(..., cap=...)`).

Комментарий у константы обещает «голова = заголовок **и итог**». Для этого артефакта итог живёт
в хвосте: §4 числа (строка 129), §5 различитель (193), §6 цена (232), §7 три исхода (270).
Голова доставила предисловие и половину первой таблицы; итога в ней не было ни строки.

### 3.4. Это класс, а не единичный случай

```
$ ls docs/review_outbox/*.md | wc -l                      → 58
$ grep -l "| 1-80 |" docs/review_outbox/*.md | wc -l      → 44
```

| что | число |
|---|---|
| срезов «1-80» по контекстным источникам | **44** в 58 пакетах, различных артефактов 43 |
| артефактов, целиком уместившихся в 80 строк | **0 из 43** (медиана 223 строки, максимум 372) |
| доехало строк, медиана | **35.9 %** |
| не доехало строк суммарно | **6388** |
| пакетов, выбравших >90 % потолка 15000 | **0 из 57** (максимум выбранного 10011, медиана 7004) |
| неиспользованного запаса потолка суммарно | **470311 знаков** |

### 3.5. Второй срез, о котором находка не знает вовсе

`review_pack.py:82` — `HYPOTHESIS_TEXT_MAX = 2000`. В пакете текст задания обрывается на
«… ворота стоят на пути реконсиляции демона, а авто **…**» — посреди пункта 1. Пунктов у ТЗ было
пять, и артефакт отвечает на все пять (§2 премиса — п.1, §3 перепись — п.2, §4 числа — п.3,
§5 различитель — п.4, §6 цена — п.5). Канал видел **начало одного пункта из пяти**.

Отсюда цена его совета (б) «подсчёты за 42.5 суток не делать вовсе»: это предложение выбросить
**пункт 3 задания**, которого ему не показывали. Совет не ошибочен — он вынесен без предмета.

---

## 4. ЧТО ПОВОД ЗАКРЫВАЕТ, А ЧТО ОСТАЁТСЯ

**Закрывает:**
- «Жива ли посылка про ворота по адресу `pc_orchestrator.py:6441`» — **жива**, замер 3 вызова.
- «Надо ли делать компактную матрицу дверей» — **не надо, она есть** (§3.1/§3.2 артефакта 03.09).
  Операционной работы находка не порождает ни одной строкой; отбор снял её карточку правильно.
- «Почему канал не увидел G3 и шесть дверей» — **ответ найден в коде**: `review_auto.py:456`,
  срез на 80 строк при 7281 свободном знаке потолка. Слепота изготовлена нами, а не каналом.

**Повод ложным НЕ является** — но и подтверждением «работа переусложнена» он тоже не является:
находка сама пометила свой вывод как неизвестный, и она была права.

**Остаётся НЕИЗВЕСТНЫМ (третий исход, законный):**
- Упрощаема ли работа 03.09 по существу — **неизвестно в обе стороны**: вердикта у нас нет,
  потому что у канала не было предмета. Повторно спрашивать имеет смысл только после починки среза.
- Сколько ещё ответов канала испорчены тем же срезом — **неизвестно**. Буквальную жалобу
  «в представленном тексте/фрагменте» несёт ровно **1 ответ из 120**
  (`grep -lE "в представленном (тексте|фрагменте|пакете)" docs/review_inbox/*.md | wc -l` → 1) —
  и это именно наш. Широкий греп по словам «не виден / неизвестн» даёт 94 из 120, но он ловит
  слово «неизвестно» в любом смысле и доказательством не является: честное число здесь **1**,
  а не 94. Молчание остальных 119 означает «не жаловались», а не «видели всё».
- Живой реестр Планировщика этим заходом **не снимался** (`schtasks` — красная операция), поэтому
  «дверей ровно девять» остаётся верным для кода и доков репозитория, а не для машины.
- Влезет ли артефакт целиком — **нет**: 21553 знака против потолка 15000 вместе с распиской
  (2644). «Слать целиком» решением не является; речь о выборе куска, а не о снятии среза.

---

## 5. ПРАВКА, НАЗВАННАЯ СЛОВАМИ (кода заход не трогал — решение за владельцем)

1. `review_auto.py:456` — резать артефакт не фиксированной «головой 80», а под **остаток потолка**
   пакета (15000 минус уже занятое; в разобранном случае это дало бы +7281 знак, то есть весь
   §3.1–§3.2 целиком).
2. Если не влезает — резать **по разделам**, обязательно втягивая таблицы-матрицы, а не первые
   N строк подряд: итог у артефактов полосы живёт в хвосте, а не в голове, и комментарий у
   константы обещает обратное тому, что она делает.
3. Минимум, дешёвый и честный: печатать в таблице источников «доехало 80 из 301 строк (22.3 %)»
   рядом с границами «1-80», чтобы ревьюер видел размер пропасти. Сегодня он видит только границы
   и не знает, что за ними ещё четыре пятых работы.

Цена названа, но **не уплачена**: ничего не чинилось.

## 6. Что НЕ делалось

Процессы не трогались; `schtasks` не запускался; боевая база и живые таблицы не открывались;
`.env` не читан; клиентский контур не тронут; ничего не удалено; временных скриптов не заводилось
(все замеры — греп и чтение файлов); боевого кода не изменено ни строкой.
