# Авто-фетч: дедуп строк состояния — ПРОВЕРКА (задача уже сделана коммитом b790187)

Дата: 2026-07-29. Сессия: Dispatch/headless, ПК-контур.

## Итог одной строкой

Задача («строка "рабочая копия грязная — git pull пропущен" пишется каждый цикл»)
**уже реализована и закоммичена** — `b790187` («авто-фетч: строки состояния при СМЕНЕ,
а не каждый цикл — 47→≤14 строк/сутки»). Живой демон **уже на этом коде**. Новый код не
писался; проведена проверка ПО ФАКТУ + перезамер.

## Доказательства реализации (код на HEAD)

- `pc_orchestrator.py:3593-3627` — `_autofetch_state_read/_write/_note`: строка в журнал
  ТОЛЬКО при `prev != cond`. То же состояние → тишина (`return False`).
- `pc_orchestrator.py:3630-3713` — `git_ff_pull_tick`: накрыты ВСЕ периодические ветки:
  - грязно (`dirty:<файлы>` — поимённый список через `_dirty_tracked`, смена состава = новая строка) `:3656-3662`
  - fetch не удался (`fetch_failed`) `:3667-3670`
  - не-ff расхождение (`non_ff`) `:3697-3700`
  - pull не удался (`pull_failed`) `:3705-3706`
  - «снова чистое» — ОДНА строка при возврате в `ok` `:3677, 3694, 3712` (+`_AUTOFETCH_CLEAN_MSG`)
  - ff — событие, пишется всегда `:3711`.
- **Признак на ДИСКЕ** (переживает рестарт демона): `AUTOFETCH_STATE_FILE =
  pc_orchestrator.autofetch.json` `:3589`, gitignored (`.gitignore:52` `pc_orchestrator.*.json`).
  Файл на диске: `{"cond": "ok"}` (14 байт, создан 16:34:33 живым процессом → демон на новом коде).
- Тесты (`test_pc_orchestrator.py`, +6 в b790187): `test_dirty_journal_once_until_state_changes`,
  `test_dirty_state_survives_daemon_restart`, `test_fetch_failure_journaled_once`,
  `test_non_ff_journaled_once`, `test_first_clean_tick_is_silent`, `test_recovery_line_after_dirty_then_ff`.

## Шаг 2 — прочие периодические записи оркестратора (независимый аудит всех `_cowork`)

Проверены ВСЕ 50 call-site `_cowork(`. Периодических (каждый цикл, пока держится условие)
было ровно четыре — все четыре ветки авто-фетча выше. Остальные:
- **событийные** — переходы задач/уроков/цепей, self-update, ff, подъём вотчдога, сводки
  ревизора (`:1064…2677`, `:3529…3815`, `:4044/4049`, `:4458…4932`): раз на реальное событие.
- **самосбрасывающиеся** — вотчдог-слепота `:4056-4066`: счётчик `__blind__` обнуляется
  сразу после алярма (`:4064`) и на любом успехе (`:4066`) → раз на `blind_alarm`, не на цикл.
  Вотчдог застрявшей цепи `:3117-3121`: строка внутри `if _loc_release(...)` — на факт
  авто-релиза; после релиза шаг уже не «застрявший», второй раз не сработает.

Вывод: новых периодических спамеров, пропущенных b790187, НЕТ.

## Замер (лог демона `pc_orchestrator.log`, окно последних 24ч)

Окно: `2026-07-28 13:21:43 → 2026-07-29 13:21:43` (до последней строки авто-фетча).

| | Строк «рабочая копия грязная» за сутки |
|---|---|
| **СТАРО** (каждый цикл ~5 мин) | **47** |
| **НОВО** (при смене состояния) | **≤14** = 7 входов в грязь + до 7 «снова чистое» |

- 47 строк подтверждены из лога (совпадает с замером коммита).
- «7 входов»: между грязными эпизодами реальные чистые промежутки видны крупными разрывами
  (1781с, 3436с, 3819с, 6482с, 9084с, 33585с). Джиттер тика в непрерывно-грязном эпизоде даёт
  разрывы 301–530с — их не считаем сменой. Порог ≥600с даёт устойчивые **7** эпизодов
  (и при 900с — те же 7). Рестарт/сон ПК схлопнули бы ещё (признак на диске → без повтора).

## Гейт (проверено сейчас)

- `py_compile` pc_orchestrator/test/gate_selective/selfupdate_gate — **OK**
- import-smoke `import pc_orchestrator` — **OK**
- `test_pc_orchestrator` — **526 OK**
- полный `unittest discover` — **2101 OK (skipped=10)**

## Прерывания хендовера

Новый коммит НЕ делается (работа уже на HEAD), self-update демона НЕ инициируется —
управляемого рестарта/прерывания в этой сессии нет.
