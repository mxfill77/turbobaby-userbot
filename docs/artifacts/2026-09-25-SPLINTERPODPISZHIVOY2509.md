# SPLINTERPODPISZHIVOY2509 — ветка подписи к фото ПРИМЕНЕНА на живом Splinter со второго захода: `941af26` → `48ee30a`, один перезапуск, полный набор тестов сервера 261/261 → 262/262

Задание Штаба `0032-74a.2509` (ящик, полоса ПК). Хронология 25.09.2026 (UTC; по Пхукету +7):

| время UTC | что было |
|---|---|
| 10:06:39 | первый заход, задача демона #74 (CLAIM). Исполнитель успел только разведку сервера (только чтение, 10:10:51) |
| ~10:24 | ПК перезагрузился (контур-вотчдог: «лежал 8 м 10 с [перезагрузка]») |
| 10:28:25 | демон закрыл #74 как сироту (`failed`) |
| 10:47:05–10:47:22 | **применение 1** (интерактивная сессия, слово владельца «Продолжай если прервался»): fast-forward на `d8c4c69`, на живом корне краснеет `test_doc_state_claims` → `reset --keep 941af26`, службу не трогали. Этот исход был закоммичен артефактом `feeb4333` («НЕ применено»); теперь он переписан этим текстом |
| 11:0x | слово владельца «продолжай» → правка одной строки `CLAUDE.md` ветки, коммит `48ee30a`, push ветки на GitHub |
| 11:08:32–11:15:38 | **применение 2**: полный набор до/после, fast-forward на `48ee30a`, ОДИН перезапуск splinter → **ПРИМЕНЕНО** |

## FACT

- **Исход: ПРИМЕНЕНО.** Живой `main` сервера `/root/turbobaby-manager-bot`: **до `941af26` → после `48ee30a`**
  (`d8c4c69` — правка подписи + `48ee30a` — одна строка `CLAUDE.md`). Отслеживаемые файлы чисты (0).
- **Служба splinter:** до — PID **2370103**, старт **23.09.2026 22:05:22 UTC**; после — PID **2408174**,
  старт **25.09.2026 11:14:38 UTC**, `active`, `NRestarts=0`. Перезапуск ровно один (`systemctl restart`
  11:14:38 UTC). В заходе 1 перезапуска не было.
- **Тесты на живом корне, полный набор `tests/test_*.py`**, по файлу в подпроцессе, в формате гейта
  (`PYTHONPATH=корень:корень/tests`, `PRETOOL_NOPUSH=1`, `ORCH_TEST_MODE=1`, таймаут 90 с):
  **до (`941af26`) 261 файл, красных 0 · после (`48ee30a`) 262 файла, красных 0**. `test_caption_zayavka`
  **24/24**; `py_compile` у `caption_intent.py`, `splinter.py` и `tests/test_caption_zayavka.py` чист.
- **Журнал службы с момента старта** (`journalctl -u splinter`): 5 строк systemd (Stopping · Deactivated ·
  Stopped · Consumed · Started), Traceback и ERROR — **0**. **`splinter.log` после старта:** 45 строк за
  первые 60 с; Traceback 0, `[ERROR]` 0, `[WARNING]` 0; есть «Bot polling started» (11:14:38,670),
  «Инфо-кнопки: ✅ закрепов из memory.db загружено: 41», «Auditor LLM: ✅ …». Повторная проверка в
  11:16:03 UTC: служба та же, ошибок в логе с момента старта 0.
- **Откат одной строкой** (на сервере): `cd /root/turbobaby-manager-bot && git reset --keep 941af26 && systemctl restart splinter`.
  Мягче, без отката кода: строка `CAPTION_INTAKE=0` в файле настроек сервера + `systemctl restart splinter`
  — путь байт-в-байт `941af26` (улика 24.09: 20 случаев из 20).
- **Доставка `main` в GitHub: ДОВЕЗЛА.** Штатный оборот `srv_delivery` ПК в **11:45:34 UTC**
  (`tmp/srv_delivery/state.json`: `kind=deliver`, `last_deliver = [d8c4c69, 48ee30a]`, `srv_head = 48ee30a`);
  `git ls-remote` в 11:46:39 UTC → `refs/heads/main` = **`48ee30a`**. Руками `main` GitHub не двигался.
  Предыдущий оборот доставки (11:09:58 UTC) пришёлся на время до перемотки и видел `941af26`.
- **Наружу из проверок — ничего:** тесты на заглушках моста, Telegram и модели; живым сообщением в форум
  не проверялось.

## 1. Премисы Штаба (проверены первыми)

| премиса | вердикт | замер |
|---|---|---|
| П1 — артефакт `2026-09-24-SPLINTERPODPISZAYAVKA2409.md` отвечает на `0031-73d`, коммит ПК `472abd49` | верна | файл 18427 байт; `git show --stat 472abd49` → ровно этот файл |
| П2 — ветка на GitHub = `d8c4c69`, прямой потомок `941af26` | верна (на момент проверки) | `ls-remote` → `d8c4c69`; `merge-base --is-ancestor` → да; поверх `941af26` ровно 1 коммит |
| П3 — живой HEAD = `941af26`, дерево чистое, splinter active | верна | 10:42:22 UTC: HEAD `941af26` на `main`, чисто, `active`, PID 2370103, NRestarts 0 |
| П4 — `CAPTION_INTAKE` по умолчанию включена, выкл = `941af26` байт-в-байт | верна | `_caption_intake_on`: `_os_env("CAPTION_INTAKE", "1")`; при выкл `_cap = None`, все три новые ветки мертвы; `bc_base.json` = `bc_off.json` побайтно (sha256 `259f2b9d…`, 20 случаев) |
| живого применения после 24.09 не было | верно | объекта `d8c4c69` на сервере до этого захода не было; `/tmp/scaplive2509` не существовал |

**Отступление от буквы задания, названное прямо:** задание велит fast-forward на `d8c4c69`, а на живом
`main` стоит `48ee30a` — `d8c4c69` плюс ОДИН коммит, правящий только строку 1566 `CLAUDE.md` сервера
(`git diff d8c4c69 48ee30a` → 1 файл, +1/−1, код не тронут). Без этой строки полный набор сервера красный
(§3). Коммит сделан после слова владельца «продолжай», то есть вне первоначальной постановки.

## 2. Приёмка прошлого шага по содержимому

- **Тесты 24.09 на ПК** (`tmp/splinter_caption_2409/{base,before_new,after}/`): новый тест `941af26` **9/24** →
  ветка **24/24**; на обоих деревьях `test_floor_quiet` 27/27, `test_album_one_pass` 3/3, `test_never_silent`
  18/18, `test_work_intent` 26/26, `test_photo_servicing_log` 2/2; на ветке `test_handover` 7/7. Красные полного
  сьюта ПК 47 и 47 — одно множество (`comm` пуст в обе стороны). **Это сравнение и пропустило регресс — §3.**
- **Куда ложится подпись:** лист «события», поле `notes` строки события, первым сегментом `подпись: «…»`
  (`caption_intent.note_with_caption`, вызов `splinter.py:9215`).
- **Дверь заявки:** `splinter._caption_zayavka` (`:8315`) → `bridge.add_event(event_type="intake", …)` с теми
  же полями, что у фазы 1 в `941af26:9206–9210` (включая `sender`), и `bridge.service_pending_upsert` (Bot Data
  «то_заявки», статус «заявлено»). Оба метода есть в `bridge_client.py` `941af26` (`:985`, `:1182`).
- **Новых листов и колонок нет:** в диффе два вызова моста — `add_event` ×1, `service_pending_upsert` ×2;
  `bridge_client.py` не менялся.
- **Пути коммитов:** `M CLAUDE.md`, `A caption_intent.py`, `M splinter.py`, `A tests/test_caption_zayavka.py`
  (+775/−1 суммарно).

## 3. Заход 1: красный страж документов, и почему ПК его не видел

Скрипт `tmp/splinter_caption_live_2509/apply.sh` (вывод `apply.out`), восемь файлов тестов до/после на живом
корне. Всё зелёное, кроме `tests/test_doc_state_claims.py`: **17/17 на `941af26` → красный на `d8c4c69`**,
проверка `test_standing_docs_are_consistent`: «утверждение о состоянии без привязки в стоячем документе:
`CLAUDE.md:1566` [остаток] нет: дата, живой факт (путь или коммит)». Откат `reset --keep 941af26` в 10:47:22 UTC,
живое дерево стояло на ветке 7 секунд, служба не перезапускалась.

**Почему приёмка ПК его не поймала:** на ПК этот тест красный на ОБОИХ деревьях по другой причине. Первой
падает проверка «привязка декоративна»: файла `reports/2026-08-10/expect-o3-7e348d4.md` на ПК нет, он есть
только на сервере вне git (`base_full`: `CLAUDE.md:3320`, `after_full2`: `:3351`). Тест останавливается на
первой находке, и до новой находки на строке 1566 не доходит. **Сверка «то же множество красных» не видит
регресса внутри уже красного файла.** Поэтому в заходе 2 набор полный и в ОДНОЙ среде до и после, а красный
файл, сменивший причину падения, считается регрессом (сигнатура строк ошибок).

**Правка (`48ee30a`, ветка на GitHub):** `**ОСТАТКИ:**` → `**ОСТАТКИ (на 24.09.2026, `caption_intent.py`):**`
— форма соседних объявлений `CLAUDE.md` (`:287` … `:1795`). Страж ветки на ПК после правки:
`doc_state_claims.findings` = **0** (у строки 1566 дата `24.09.2026` и путь `caption_intent.py`).

## 4. Заход 2: применение

Скрипт `tmp/splinter_caption_live_2509/apply2.sh` (копия на сервере `/tmp/scaplive2509b/apply2.sh`, sha256
`46310556…` совпал; полный вывод `tmp/splinter_caption_live_2509/apply2.out`), запущен **отсоединённо**
(`setsid nohup`): новая перезагрузка ПК не оставила бы дерево на полпути, откат зашит в скрипт.

| шаг | итог |
|---|---|
| 0 · сверка до | HEAD `941af26` на `main`, чисто, `active`, PID 2370103 |
| 0b · полный набор ДО (`941af26`) | **261 файл, красных 0** (11:08:32–11:11:31) |
| 1 · fetch ветки | `FETCH_HEAD` = `48ee30a`, `merge-base` = `941af26`, поверх: `d8c4c69 48ee30a` → `merge --ff-only` → HEAD `48ee30a` |
| 2 · `py_compile` | три файла — rc 0 |
| 2 · полный набор ПОСЛЕ (`48ee30a`) | **262 файла, красных 0**; `test_caption_zayavka` 24/24 (11:11:35–11:14:37) |
| 4 · перезапуск | 11:14:38 UTC, `RESTART_RC=0`; через 60 с `active`, PID 2408174, NRestarts 0 |
| 4 · журналы | journalctl: 5 строк, Traceback/ERROR 0; `splinter.log`: 45 строк, Traceback 0, ERROR 0, WARNING 0, «Bot polling started» 1 |
| 5 · откат | не понадобился |

## 5. Где Штаб увидит первую настоящую подпись

- **`splinter.log`** на сервере: `→ подпись: <указание|сделано|прочее> (<почему>) слов=N` (`splinter.py:9216`).
  Если это указание, дальше идут `→ add_event (подпись-заявка): ok=… saved=… error=…` (`:8332`) и
  `→ подпись-заявка: <байк> работа='…' declared=[…] новая|дописана в открытую` (`:8351`).
- **Поле записи события:** лист «события», `notes` строки события — первый сегмент `подпись: «…»`; у заявки
  `event_type = intake` и строка Bot Data «то_заявки» со статусом «заявлено».
- **В теме байка:** одно сообщение `🐀 Splinter` / `📝 รับเรื่อง: …` / `📝 заявка: <байк> — …`.

## 6. Следы и пределы (честно)

- **На сервере остались:** удалённая ссылка `refs/remotes/origin/splinter-caption-zayavka-2409` → `48ee30a`
  (её обновлял `git fetch`); записи reflog захода 1 (FF → reset) и захода 2 (FF); каталоги `/tmp/scaplive2509/`
  (заход 1: `apply.sh`, `apply.out`, `before/`, `after/`, `lock/`) и `/tmp/scaplive2509b/` (заход 2: `apply2.sh`,
  `apply2.out`, `before/`, `after/` — вывод, код возврата и сигнатура каждого из 261/262 файлов,
  `splinter_after_restart.log`, `lock/`). Ничего не удалялось. Повтор любого из двух скриптов в своём
  каталоге остановится на `STOP_ALREADY_RUNNING`.
- **Тесты шли на ЖИВОМ корне** — так велит задание (шаг 2), и так же гоняет полный набор гейт сервера. За
  ~6 минут обоих прогонов `splinter.log` вырос на 18 строк. Живой бот пишет туда сам (задача `devbot_report`
  каждые 45 с — две строки), поэтому отделить строки тестов от строк бота этот замер не может.
- **Функционально не подтверждено:** настоящей подписи к фото после перезапуска ещё не было (§5 — где её ждать).
- **Отступление от буквы задания** — FF на `48ee30a`, а не на `d8c4c69` (§1).
- **Сигнальная остановка ящика** (#73 и #74 подряд `failed`, метка `2a28ad855231`) снята словом владельца
  25.09 ~10:52 UTC; оборот ящика 11:03:34 UTC прошёл без остановки.
- **Временное на ПК:** `tmp/splinter_caption_live_2509/` (`r1_recon.*` первого захода, `r2_p3.*`, `apply.sh`,
  `apply.out`, `apply2.sh`, `apply2.out`, `server_evidence.txt`, `check_claims.py`, `p2_lsremote*.txt`) и клон
  `tmp/splinter_caption_2409/srv/` (ветка на `48ee30a`).

SPLINTERPODPISZHIVOY2509
