# Судьба «описи мозга» и живость реестра Brain

**Дата:** 29.07.2026, 18:12 (локальное) / 11:12 UTC. Сессия Desktop, ПК, `D:\turbobaby-bot`, HEAD `e769a82`.
**Режим:** строго ТОЛЬКО ЧТЕНИЕ. В Brain не записано ни символа, реестр не тронут, доки не менялись,
коммитов нет, клиентский контур не тронут, `.env`/VPS/секреты не открывались и не печатались.
**Канал чтения:** `brain_writer` (`_config`/`_get`/`read_text`) — секреты берёт сам писатель, скрипты
разведки конфиг не открывали. Токенов в этом файле нет.

---

## ЧАСТЬ 1 — ОПИСЬ: вердикт **НЕ СТАРТОВАЛА**

Не «идёт» и не «упала»: падения нет — нет ни traceback, ни ошибки канала, ни таймаута, потому что
**работа не начиналась вообще**. Ни один процесс, ни один транскрипт, ни одна строка журнала
не содержат следа выполнения описи.

### Что проверено по факту

| проба | результат |
|---|---|
| Живые процессы `python.exe` (`Get-CimInstance Win32_Process`) | **5 штук, все служебные**: `pc_agent.py` (PID 7264, с 24.07), `rc_supervisor.py` (PID 6888, с 24.07), `userbot_listen.py` (PID 7276), `moderation_bot.py` (PID 15908), `pc_orchestrator.py` (PID 12952) — три последних подняты демоном 29.07 16:33. Процесса описи нет |
| Живые `claude.exe` | 8 Desktop-сессий + rc-server (PID 20552) + 3 RC-сессии. Ни одна не занята описью |
| Транскрипты сессий за сутки (`~/.claude/projects/**/*.jsonl`, 21 файл) | Слово «опись» встречается в **3** файлах: `29ab027b` (28.07, там это «опись корпуса» диалогов — другая задача), `d5e6f37e` (сессия карты миграции — там опись лишь **упомянута** как параллельная), и текущая сессия. **Задания на опись мозга нет ни в одном транскрипте** |
| Файл-артефакт | В `docs/artifacts/` за 29.07 — 12 файлов, ни одного про опись/реестр |
| Журнал `cowork_log` (живое чтение, 46 811 симв.) | За 29.07 — **21 строка** DONE/ARTIFACT/NOTE, все опознаны по другим задачам. Строк со словами «опись», «манифест», «реестр» — **0**. Последняя строка 10:47 UTC |
| Очередь оркестратора (`pc_orchestrator.log`) | Единственная задача суток — `task=42 lane=pc type=read`, два прогона: 09:11:58→09:33:29 UTC `needs_approval`, 09:35:38→09:49:22 UTC `done`. Её ТЗ прочитано дословно — это **авто-фетч-дедуп**, не опись |
| Хук `session_end` (`dispatch_notify.log`) | За 29.07 сработал **один раз** — 16:49:20 (тот же task=42). Карточки о завершении сессии-описи нет |

### Единственный кандидат — RC-сессия, которая ничего не получила

Владелец видел опись «идущей» к 15:53 (в ТЗ параллельной сессии карты миграции написано:
«ПАРАЛЛЕЛЬНО идёт опись мозга (только чтение) — ей НЕ мешать»). В окне до 15:53 на ПК создан
ровно один сеанс — RC-сессия с телефона:

| факт | значение |
|---|---|
| сессия | `cse_01Xa5KTXp3RbcmFFXqeoAcJx`, процесс PID **21216**, старт **29.07 15:36:37** |
| жив ли сейчас | да, висит до сих пор (проверено 17:52) |
| процессорное время за 2 ч 15 мин | **6,5 с** (только старт), память 45 МБ |
| события в логе | **408 heartbeat**, 3 `control_request`, 2 обрыва SSE с переподключением. Событий ассистента — **0**, `tool_use` — **0** |
| транскрипт `.jsonl` | **не создан** |

Последнее — решающее. Правило подтверждено на всех сеансах суток: сессия, реально получившая
промпт, создаёт `.jsonl` через ~4 секунды после старта процесса (`cse_0141…` 12:30:37 → `8d59bc5d`
12:30:41; `cse_013P…` 16:03:27 → `fa24e2b4` 16:03:31; Desktop 1620 15:53:08 → `d5e6f37e` 15:53:10).
У `cse_01Xa5…` транскрипта нет вовсе — **промпт до неё не дошёл**. Для сравнения: соседние
RC-сессии, делавшие работу, нажгли 37,5 с и 24,1 с CPU против 6,5 с у этой.

Ошибок в её логе две, обе транспортные и обе после факта:

```
2026-07-29T09:36:39.244Z [ERROR] SSETransport: Stream read error: The socket connection was closed unexpectedly.
2026-07-29T10:36:40.321Z [ERROR] SSETransport: Stream read error: The socket connection was closed unexpectedly.
```

Оба раза канал сам переподключился (`Reconnecting in 869ms` → `Connected`). Это не причина отказа —
это простой пустой сессии.

**Вывод:** опись не запускалась. Работу за неё не доделывал — по заданию только диагностика.

---

## ЧАСТЬ 2 — РЕЕСТР: **живых 25 / мёртвых 1** (из 26 документов; записей манифеста 27)

`list_brain` вернул **27 записей**, но одна из них — `folder_id`, это папка Brain, а не документ.
Документов-указателей, следовательно, **26**. Каждый прочитан `read_doc` по `name`; при отказе —
повторно по `id`.

**Канал даты правки не отдаёт.** Ответ `read_doc` содержит ровно четыре поля: `ok`, `id`, `name`,
`text`. Ни `modified`, ни `modifiedTime`, ни `updated` — нет. Колонка «дата последней правки»
поэтому пуста у всех строк: это ограничение моста, а не пропуск в проверке.

| # | ключ | id | статус | имя в ответе | символов | дата правки | чтение |
|---|---|---|---|---|---|---|---|
| — | `folder_id` | `1uWqHsxk7aEWoSNqaUBMmqkYOh2UKYLkY` | **папка, не документ** | — | — | канал не отдаёт | — |
| 1 | `knowledge_base` | `1Mv_zi1P33jNM0MFUBX_UEf9CnzOvCFeZ` | **ЖИВОЙ** | knowledge_base | 56 521 | канал не отдаёт | 3,1 с |
| 2 | `project_state` | `1fuptOFp2bqZva7eJRlEanaCO6eRkAO20` | **ЖИВОЙ** | project_state | 37 994 | канал не отдаёт | 1,9 с |
| 3 | `faq` | `1tv8Y-K3gLyT9Y0mXyYXZLf2c2rEzPMLHPzvg4lGqs98` | **ЖИВОЙ** | faq | 7 665 | канал не отдаёт | 2,7 с |
| 4 | `park_list` | `1jD3VJGeoET8Yf5ma6iZPmwwmuw_yIYyP2A4N5RAEZho` | **ЖИВОЙ** | park_list | 2 642 | канал не отдаёт | 2,3 с |
| 5 | `index` (KB_MASTER) | `1-bH3b6c_oamqST551iLxn-voSDragdV0rUZkFwgmAqc` | **ЖИВОЙ** | index | 86 535 | канал не отдаёт | 10,9 с |
| 6 | `cc_log` | `1464zaINaLnOwXMsHNaEyy-4FpuQCVTYF` | **ЖИВОЙ** | cc_log | 49 955 | канал не отдаёт | 2,2 с |
| 7 | `review` | `1vDfD_n8i-8cSvJmqqvvEaLZ1YC639-in` | **ЖИВОЙ** | review | 511 | канал не отдаёт | 2,6 с |
| 8 | `cc_log_archive` | `1haE-9OFgs1-ZeH-fV1g1mIxxM3L_BLYq` | **ЖИВОЙ** | cc_log_archive | 988 521 | канал не отдаёт | 3,5 с |
| 9 | `cc_userbot_log` | `1yqzVrSFRN-1Zr3y6kprn71c-T-1xTl1qfO1BzA-Rihg` | **ЖИВОЙ** | cc_userbot_log | 5 394 | канал не отдаёт | 2,3 с |
| 10 | `review_archive` | `1K0gPMOyM-ER7nweda8-f9MK3edbepYCniZpQy0HkpAA` | **ЖИВОЙ** | `null` (имя не вернулось) | 183 329 | канал не отдаёт | **75,8 с** |
| 11 | `sessions_log` | `1gNFRnHv09SKeagkGA3moG0VNlzffECeh` | **ЖИВОЙ** | sessions_log | 7 665 | канал не отдаёт | 2,1 с |
| 12 | `sessions_log_archive` | `1D-iq-Rp1g_RC9uYzqsjPJtmw6TY0l6cV` | **ЖИВОЙ** | sessions_log_archive | 44 141 | канал не отдаёт | 2,4 с |
| 13 | `cowork_log` | `1s9Fy3xm4FB99ah9xovLjYIMOjnDKz3Jc` | **ЖИВОЙ** | cowork_log | 46 811 | канал не отдаёт | 2,4 с |
| 14 | `roadmap_master` | `1Z70EpgGZmaYMaZ064sXQlCCP8z8sFyZWfzVPvRB4jWE` | **МЁРТВЫЙ** | — | — | — | 3,8 с (отказ) |
| 15 | `executors_map` | `1NyfcErxNt09CH8JB-V0in9e-UQB-K4_uJrwZ-FG3Za8` | **ЖИВОЙ** | executors_map | 5 588 | канал не отдаёт | 2,3 с |
| 16 | `orchestrator_plan` | `1_ogUGFim24Ifw60mSsfcONFc8hXMPzw539oXTbhGggo` | **ЖИВОЙ** | orchestrator_plan | 6 119 | канал не отдаёт | 2,1 с |
| 17 | `orchestrator_safety` | `1UB1MWs8ZQWDkwHYBqNgK7UyVD4dkPKYdEVYo3IM2-Zs` | **ЖИВОЙ** | orchestrator_safety | 5 107 | канал не отдаёт | 2,3 с |
| 18 | `payments_plan` | `13WtxQaDLdixR4EsjUFimtNhESn9nFDtk` | **ЖИВОЙ** | payments_plan | 3 441 | канал не отдаёт | 2,3 с |
| 19 | `rules` | `1AnBAniKQtrpJQevVWlzxVrdb1n51aj1G` | **ЖИВОЙ** | rules | 16 736 | канал не отдаёт | 2,3 с |
| 20 | `infra` | `1z2wS0I0nm-dJGqF3RRpOXlEkTKwLOsuF` | **ЖИВОЙ** | infra | 6 702 | канал не отдаёт | 2,3 с |
| 21 | `state_model` | `1OGUzeb60UbzAaCOsLNR_aBKFy-blfdn-` | **ЖИВОЙ** | state_model | 9 753 | канал не отдаёт | 2,4 с |
| 22 | `pulse` | `1v3ezYbEeI1kGNQFI6xi8mDhnM9uE3YSE` | **ЖИВОЙ** | pulse | 270 | канал не отдаёт | 2,0 с |
| 23 | `turbobaby_faq` | `1tv8Y-K3gLyT9Y0mXyYXZLf2c2rEzPMLHPzvg4lGqs98` | **ЖИВОЙ** | turbobaby_faq | 7 665 | канал не отдаёт | 2,2 с |
| 24 | `cowork_log_archive` | `1UC-fKIpjb2zwzrvCsbJjBxmgglmSlj46` | **ЖИВОЙ** | cowork_log_archive | 763 217 | канал не отдаёт | 3,7 с |
| 25 | `cowork_log_test` | `1zGPT5DqNdUT8FOrOxsVI8-mI7sZjz3kU` | **ЖИВОЙ** | cowork_log_test | 39 | канал не отдаёт | 2,0 с |
| 26 | `cowork_log_test_archive` | `1HvhvlxKFPiHUYlMp41yQL0-50q4Dv_zq` | **ЖИВОЙ** | cowork_log_test_archive | 2 372 | канал не отдаёт | 2,4 с |

### Мёртвые указатели — отдельным списком, с дословной ошибкой канала

**1. `roadmap_master` → `1Z70EpgGZmaYMaZ064sXQlCCP8z8sFyZWfzVPvRB4jWE`** — единственный мёртвый.
Три пробы, дословные ответы моста:

```json
read_doc name=roadmap_master
{"ok": false, "error": "read_failed", "message": "Exception: ไม่พบรายการตามรหัสที่ระบุ อาจเป็นเพราะคุณยังไม่ได้แก้ไขรายการนี้หรือคุณไม่มีสิทธิ์เข้าถึงรายการดังกล่าว"}

read_doc id=1Z70EpgGZmaYMaZ064sXQlCCP8z8sFyZWfzVPvRB4jWE
{"ok": false, "error": "read_failed", "message": "Exception: ไม่พบรายการตามรหัสที่ระบุ อาจเป็นเพราะคุณยังไม่ได้แก้ไขรายการนี้หรือคุณไม่มีสิทธิ์เข้าถึงรายการดังกล่าว"}

read_doc name=KB_ROADMAP_MASTER
{"ok": false, "error": "unknown_name", "message": "Нет имени \"KB_ROADMAP_MASTER\" в манифесте. Запустите setupBrain или см. list_brain."}
```

Сообщение приходит по-тайски (локаль Apps Script) и означает: «объект с указанным id не найден —
возможно, вы его не редактировали или у вас нет прав доступа». По имени и по id — **одна и та же**
ошибка, то есть проблема не в имени, а в самом файле.

**Причина найдена в живом архиве журнала**, `cc_log_archive` строка 1986 — файл был удалён
осознанно, месяц назад, а ключ в реестре остался:

> `DONE 2026-06-27 21:36 UTC: 🔴 ШАГ 5 — удаление 3 дублей ВЫПОЛНЕНО (по «да» Филиппа). ЧИСТКА BRAIN ЗАВЕРШЕНА ПОЛНОСТЬЮ. trash_brain_file (Drive-корзина обратима ~30 дн): KB_ROADMAP_v2 СТАРЫЙ 1dYIEz ✓, KB_ROADMAP_MASTER 1Z70Ep ✓, KB_index пустой 1klCUC ✓ — все trashed=True. Манифест-ключ index (висел на удалённый KB_index) перенаправлен на KB_MASTER (register_brain_doc overwrite)`

Там же, строка 2007: `- KB_ROADMAP_MASTER id 1Z70EpgGZmaYMaZ064sXQlCCP8z8sFyZWfzVPvRB4jWE — устарел → удалить.`

То есть 27.06 у **трёх** удалённых файлов ключ `index` был перевешен на живой документ, а ключ
`roadmap_master` — **не перевешен и не удалён**. Корзина Drive обратима ~30 дней; с 27.06 прошло
32 дня, поэтому файл уже не восстанавливается штатно — что и даёт «объект не найден».

### Побочные факты реестра (зафиксированы, не чинились)

| факт | детали |
|---|---|
| Дубль-псевдоним | `faq` и `turbobaby_faq` указывают на **один и тот же id** `1tv8Y-…Gqs98`; тела совпали побайтно (sha256 один, 12 582 байта). Два ключа = один документ |
| `review_archive` отвечает без имени | `read_doc` вернул `name: null` при живом теле 183 329 симв. Остальные 24 живых дока имя возвращают |
| Он же — самый медленный | 75,8 с на чтение (следующий по медленности `index` — 10,9 с). Отдельная проба того же id с таймаутом 45 с **не уложилась** (`TimeoutError`). Док жив, но у него реальный риск словить таймаут у любого читателя со стандартными 30 с `brain_writer` |
| Живой документ БЕЗ указателя в реестре | `KB_trainer_log`, id `13kp-54bzN2Xua8R5VN4EaXR7IElTKg4O` — читается по id (**2 190 симв.**, первая строка `TRN LOG v1 \| Полный лог тренажёра «Тренеровка»…`), но в манифесте его нет: `read_doc name=KB_trainer_log` → `unknown_name`. Это зеркало проблемы `roadmap_master`: там ключ без файла, здесь файл без ключа |

---

## ЧАСТЬ 3 — ССЫЛКИ В ПУСТОТУ

Прогреплены тела **всех 25 живых документов**. Найдено 91 разное `KB_*`-имя; 41 из них встречается
в справочных (навигационных) доках, 50 — только в журналах/архивах, где они фиксируют историю, а не
ведут читателя.

**Важное свойство канала, без которого таблица читается неверно:** манифест знает только
строчные ключи. Любое `KB_*`-имя из текста документа, поданное в `read_doc` буквально, отвечает
`unknown_name` — включая `KB_MASTER`. Поэтому ниже «живо» означает: имени соответствует ключ
манифеста, и этот ключ открывается.

### 3.1 Ссылки на ЗАВЕДОМО МЁРТВОЕ (ключ `roadmap_master` / имя `KB_ROADMAP_MASTER`)

| # | ГДЕ (док : строка : раздел) | КУДА ведёт | ЖИВО ли назначение |
|---|---|---|---|
| 1 | `index` (KB_MASTER) : 700 : **РАЗДЕЛ 5 — ГДЕ ДЕТАЛИ (навигация по мозгу)** | `KB_ROADMAP_MASTER` | **МЁРТВО** |
| 2 | `index` (KB_MASTER) : 765 : **РАЗДЕЛ 6 — РИТМ РЕВИЗИИ**, пункт `[4]` чек-листа | `KB_ROADMAP_MASTER` (ключ `roadmap_master`) | **МЁРТВО** |
| 3 | `rules` : 61 : свод правил (блок «штаб НЕ создаёт новые файлы россыпью») | `KB_ROADMAP_MASTER` (ключ `roadmap_master`) | **МЁРТВО** |
| 4 | `orchestrator_plan` : 2 : шапка «СТАТУС: ГЛАВНАЯ ФАЗА проекта» | `KB_ROADMAP_MASTER` | **МЁРТВО** |
| 5 | `project_state` : 393 : «✅ ОБЩИЙ „МОЗГ“ (Brain) — СДЕЛАНО» | ключ `roadmap_master` (в перечне план-листов) | **МЁРТВО** |
| 6 | `cowork_log` : 20 | ключ `roadmap_master` | **МЁРТВО** (запись 29.07 о самой находке — фиксация, не ссылка) |
| 7 | `cc_log_archive` : 1994, 2007 | `KB_ROADMAP_MASTER` / id `1Z70Ep…` | **МЁРТВО** (исторические записи об удалении 27.06) |

Дословно четыре навигационные (строки 1–4):

```
index:700  - KB_ROADMAP_MASTER — В АРХИВЕ (_archive; перемещён ревизией 16.07). Авторитет по оси
             O1-O5 = РАЗДЕЛ 4 этой карты. Хвосты → Раздел 7.
index:765  [4] KB_ROADMAP_MASTER (ключ roadmap_master) — статусы вех верны (✅/в работе/отложено)?
rules:61   ПРАВИЛО: штаб (Claude-на-сайте) НЕ создаёт новые файлы в Drive-мозге россыпью.
             ЖИВЫЕ доки (KB_MASTER, KB_ROADMAP_MASTER (ключ roadmap_master), …
orch_plan:2 СТАТУС: ГЛАВНАЯ ФАЗА проекта (выбрано Филиппом — Путь 1). Читать вместе с
             KB_ROADMAP_MASTER + project_state. НЕ сбиваться с этого пути.
```

**Расхождение в датах, зафиксированное как факт.** `index:700` утверждает: «В АРХИВЕ (_archive;
перемещён ревизией 16.07)». Журнал `cc_log_archive:1986` фиксирует, что файл `1Z70Ep…` был отправлен
в корзину **27.06**, на три недели РАНЬШЕ. Перемещения в архив 16.07 с этим файлом произойти не
могло — к тому моменту он уже лежал в корзине. Один из двух источников описывает не то, что было.

Отдельно: `rules:61` перечисляет `KB_ROADMAP_MASTER` в списке **«ЖИВЫЕ доки»**, а
`orchestrator_plan:2` предписывает читать его вместе с `project_state` — то есть мёртвый указатель
стоит не в исторической справке, а в действующем правиле и в шапке действующего плана.

### 3.2 Имена в справочных доках, которым в реестре нет соответствия

25 имён. Разделены по тому, объявляет ли сам текст их архивными.

**(а) Объявлены в тексте как слитые в свод / оригиналы в архиве — указателя и не должно быть:**

| имя | где | что сказано в тексте |
|---|---|---|
| `KB_INFRA_*` (`KB_INFRA_FORMS`, `KB_INFRA_UNIFY_FUTURE`) | index:705, infra:1, infra:53, infra:75 | «Источники KB_INFRA_* слиты сюда, оригиналы в архиве» |
| `KB_STATE_*`, `KB_LIFECYCLE_*`, `KB_RENTALS_SOURCE`, `KB_RECON_LINKAGE` | index:707–708 | «слиты сюда, оригиналы в архиве» |
| `KB_RULE_*` (`KB_RULE_CHECK_BRAIN_FIRST`, `KB_RULE_NO_PAUSE`, `KB_RULE_ROUTING_RECON`), `KB_DISPATCH_ASK_RULE`, `KB_REVISION_RULE` | index:717, rules:1, 5, 28, 38, 49, 64 | «Источники KB_RULE_*/KB_DISPATCH_ASK_RULE слиты сюда, оригиналы в архиве» |
| `KB_TRACKING_STAGE1_*` | index:709, index:141 | «детали — архив KB_TRACKING_STAGE1_*» |

**(б) Названы как действующие, но открыть их через реестр нельзя:**

| имя | где | проба | итог |
|---|---|---|---|
| `KB_NORTH_STAR` | `index`:699 (**РАЗДЕЛ 5, навигация**) и :442 | `read_doc name=KB_NORTH_STAR` → `{"ok": false, "error": "unknown_name", "message": "Нет имени \"KB_NORTH_STAR\" в манифесте. Запустите setupBrain или см. list_brain."}` | **В пустоту.** Строка 699 подаёт его как живой ориентир: «— философия главной оси (рамка над всем)», без пометки об архиве. Ключа нет, id в тексте не дан — открыть нечем |
| `KB_trainer_log` | `index`:713 (**РАЗДЕЛ 5**), index:540 | по имени → `unknown_name`; **по id `13kp-54bzN2Xua8R5VN4EaXR7IElTKg4O` → ЖИВ, 2 190 симв.** | **Документ жив, указателя нет.** В тексте id указан прямо, поэтому читатель дойдёт; через `list_brain` — нет |
| `KB_userbot_log` | `project_state`:38 | по имени → `unknown_name` | **Живо, но под другим ключом.** В манифесте есть `cc_userbot_log` (жив, 5 394 симв.), и его собственная первая строка начинается словами `KB_userbot_log — журнал userbot-ветки…` — тот же документ |
| `KB_claude_review_archive` | `review`:1, с id `1K0gPMOyM-…HkpAA` | по этому id — `TimeoutError` за 45 с; тот же id как ключ `review_archive` прочитан за 75,8 с | **ЖИВО**, имя устаревшее (нынешний ключ `review_archive`), отказ был по таймауту, а не по отсутствию |
| `KB_booking_flow`, `KB_collect_booking_spec` | `project_state`:50–51, 106 | в манифесте нет; текст говорит «СПЕКИ (Brain, читаются **по id**)», но id в строке не приведён | **Не проверяемо изнутри канала** — ни ключа, ни id |
| `KB_TODO_brain_cleanup_and_cowork_bridge` | `project_state`:391 | в манифесте нет | Текст сам помечает его `ЗАКРЫТ ✅выполнено` — историческая отсылка |
| `KB_CASH_CLOSED`, `KB_SIGNAL_HOOK_DONE`, `KB_DEVBOT_BUTTONS_DONE`, `KB_TRACKING_STAGE1_DONE` | index:136, 138, 139, 141 | в манифесте нет | Это **метки-теги в конце строк-итогов** («…фикс в проде (commit 13ee3b1). KB_CASH_CLOSED.»), а не указатели на документы |

### 3.3 Устаревший список id внутри живого дока

`project_state`:398–408 содержит блок «ID базы знаний (Brain)» — снимок от 01.06.2026. В нём,
среди прочего, `index (KB_index): 1klCUC5domgR6t-3hu-Yc7UC75hgf-y6-rCILFnN7JJs` — это id того
самого **пустого KB_index, отправленного в корзину 27.06** (`cc_log_archive`:1986). Там же сказано
«Манифест = **18 ключей**», сейчас их 27.

Оговорка в самом документе присутствует и стоит ВЫШЕ списка: «⚠️ **ЖИВОЙ источник имён→id =
`list_brain`** (не хардкод). Список ID ниже — ИСТОРИЧЕСКИЙ снимок 01.06… При сомнении —
`list_brain`, не этот список». То есть блок помечен как исторический честно; фиксирую его здесь
как содержащий заведомо мёртвые id, не более.

---

## Сводка

| часть | итог |
|---|---|
| 1. Опись | **НЕ СТАРТОВАЛА.** Ни процесса, ни транскрипта, ни артефакта, ни строки в журнале, ни карточки `session_end`. Кандидат — пустая RC-сессия `cse_01Xa5KTXp3RbcmFFXqeoAcJx` (PID 21216, старт 15:36:37): 408 heartbeat, 0 событий ассистента, 6,5 с CPU, транскрипт не создан. Падения нет — начала не было |
| 2. Реестр | Записей манифеста **27** (из них `folder_id` — папка). Документов **26**: **живых 25, мёртвый 1** — `roadmap_master`. Дата правки каналом **не отдаётся** ни для одного дока |
| 3. Ссылки в пустоту | На мёртвый `roadmap_master` ссылаются **4 навигационные точки** (`index` §5:700, `index` §6 пункт [4]:765, `rules`:61, `orchestrator_plan`:2) + `project_state`:393 + 2 исторические записи в журналах. Сверх того `KB_NORTH_STAR` (index:699) не открывается ничем, а `KB_trainer_log` (index:713) жив только по id |

Правок не предлагается и не делается — по заданию это разведка.
