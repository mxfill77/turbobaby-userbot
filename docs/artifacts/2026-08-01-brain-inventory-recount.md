# Опись мозга — ПЕРЕСЧЁТ по живому каналу (этап 1, шаг 1)

**Дата:** 01.08.2026. Репо `D:\turbobaby-bot`, ветка `main`. Режим — **строго только чтение**:
ни записи в мозг, ни правок реестра, ни перемещений, ни удалений. `.env`/секреты не открывались
(канал `brain_writer` берёт их сам, вызывающий код их не видит). На VPS не ходил.

**Основание:** остаток этапа 1 дорожной карты; владелец потребовал ПЕРЕСЧИТАТЬ числа 30.07,
а не переписать их. Пересчёт сделан — **три утверждения описи 30.07 не подтвердились** (§6).

---

## 0. ЧЕМ МЕРЯЛ И ЧТО ИЗМЕРИТЬ НЕ СМОГ

Канал — живой Bridge через `brain_writer` (`list_brain` + `read_doc` по каждому id,
`HTTP_TIMEOUT` поднят до 150с в рантайме: `review_archive` читается ~70с и на штатных 30с
отваливался). Тела 29 доков сняты в `tmp_brain_inventory_20260801/` (untracked, не удалял —
запрет уборки за собой).

**Два поля задания закрыть этим каналом НЕЛЬЗЯ, и это факт о канале, а не лень:**

| чего нет | почему | следствие |
|---|---|---|
| **перечень файлов ПАПКИ** (включая `_archive` и `OLD`) | у моста **нет экшена перечисления файлов**. Проверено по живому проду: `list_brain` отдаёт **реестр** (Script Properties `BRAIN_MANIFEST`), а не содержимое папки; POST-экшенов 62, среди них `move_brain_file`/`trash_brain_file`/`create_brain_plain` — и ни одного `list_*_files` | файл, лежащий в папке, но **снятый с реестра**, невидим отовсюду, куда я допущен. Именно туда 27.06 ушли слитые `KB_RULE_*`/`KB_INFRA_*`/`KB_RECON_*` и старые дубли (`Brain/_archive`, см. VPS-`CLAUDE.md` «правило гигиены мозга») |
| **дата последнего изменения** | `read_doc` возвращает ровно `ok/id/name/text` — поля времени в ответе нет | во всех строках таблицы дата — «канал не отдаёт». Это ровно то же ограничение, что зафиксировала опись 30.07; за сутки оно не изменилось |

Drive-коннектор (`search_files` по `parentId`), которым эти два поля берутся штатно, в headless-сессии
**не авторизован** — вызов отклонён слоем разрешений. Обхода не искал. Заявка на разрешение — в конце.

Из-за этого **счёт «29 доков» — это счёт ДОСТИЖИМЫХ**, а не всех: зарегистрированные + те
незарегистрированные, чьи id названы в телах. Сколько лежит в `_archive` — отсюда не видно вовсе.

---

## 1. ТАБЛИЦА: ВСЕ ДОСТИЖИМЫЕ ДОКУМЕНТЫ (29)

Размер — символы живого тела на 01.08. «Δ 30.07» — против описи 30.07 (`—» = не мерился тогда).
Группа: **Ж** живой · **П** проба-только · **М** мёртвый · **Д** дубль · **С** смешанная роль
(группы не исключают друг друга: `index` и живой, и смешанный).

| # | ключ реестра | id | симв. | Δ 30.07 | гр. | кто читает — и откуда это видно |
|---|---|---|---|---|---|---|
| 1 | `pulse` | `1v3ezYbEeI1kGNQFI6xi8mDhnM9uE3YSE` | 391 | +121 | **Ж** | **самый читаемый док мозга.** Штаб ПЕРВЫМ на каждое «проверь» (`rules`:правило 6, `index`:799); пишет VPS `cclog.py:229,287`, `devbot.py:2031` — той же операцией, что `cc_log` |
| 2 | `cc_log` | `1464zaINaLnOwXMs…` | 91 996 | **+42 041** | **Ж С** | VPS `cclog.py:211,222,267,278` (read→prepend→write, боевой); Штаб — «когда нужна ИСТОРИЯ». Смешение: журнал + врезка-инструкция сверху |
| 3 | `cowork_log` | `1s9Fy3xm4FB99ah9…` | **189 121** | **+138 994** | **Ж Д** | ПК-боевой: `cowork_log_append.py:20`, `pc_agent.py:862`, `pc_orchestrator.py`, хук `dispatch_notify`; содержательное чтение — `diag_status_truth.py:68`; Штаб на «проверь Dispatch» (`rules`:56) |
| 4 | `faq` | `1tv8Y-K3gLyT9Y0m…` | 7 665 | 0 | **Ж Д** | **`suggest.py` → `load_faq()`, порядок `FAQ_DOCS="faq,turbobaby_faq"` — боевой читатель на КАЖДОМ черновике клиенту** |
| 5 | `turbobaby_faq` | `1tv8Y-…` (**тот же файл**) | 7 665 | 0 | **Д** | второй ключ на тот же id; `suggest.py` — фолбэк-порядок |
| 6 | `knowledge_base` | `1Mv_zi1P33jNM0MF…` | 57 968 | +1 447 | **Ж С** | **боевой на VPS:** `splinter.py:3568 _load_service_intervals` парсит блок интервалов ТО, кэш 10 мин, фолбэк на хардкод |
| 7 | `index` (KB_MASTER) | `1-bH3b6c_oamqST5…` | 91 959 | +5 424 | **Ж С** | Штаб по §0 (сверка перед разворотом); VPS `registry_check` проверяет §-указатели пульса. Программного потребителя на ПК нет |
| 8 | `rules` | `1AnBAniKQtrpJQev…` | 16 857 | +121 | **Ж С** | Штаб — это его свод правил (правила 4 и 6 живут здесь) |
| 9 | `business_rules` | `1bqdkcecYdIo2lPG…` | 1 515 | **НОВЫЙ** | **Ж** | заведён 01.08, «перенесено из рамки Штаба»; док сам объявляет читателя: «Штаб эти правила не выдумывает — читает отсюда». Кода-читателя нет |
| 10 | `cowork_log_archive` | `1UC-fKIpjb2zwzrv…` | 763 217 | 0 | **Д** | `journal_measure.py:28` (замер недели) + цель серверной ротации. Содержательно не читает никто |
| 11 | `review` | `1vDfD_n8i-8cSvJm…` | 511 | 0 | **П С** | **читается регулярно, но содержимое выбрасывается:** `devbot._g_brain` и `health.py:43` меряют ЛАТЕНТНОСТЬ (`len(text)`), текст не разбирают |
| 12 | `project_state` | `1fuptOFp2bqZva7e…` | 38 880 | +886 | **П С Д** | то же — зонд латентности `devbot._g_brain`, `health.py:45` (порог 45с). Содержательного читателя нет |
| 13 | `cc_log_archive` | `1haE-9OFgs1-ZeH-…` | 988 521 | 0 | **М Д** | цель ротации. Ни одного читателя ни на одной полосе |
| 14 | `review_archive` | `1K0gPMOyM-ER7nwe…` | 183 329 | 0 | **М Д** | никто. Читается ~70с — на штатном таймауте канала 30с не открывается вовсе |
| 15 | `sessions_log` | `1gNFRnHv09SKeagk…` | 7 665 | 0 | **М** | никто: ни ПК, ни VPS, ни разовые |
| 16 | `sessions_log_archive` | `1D-iq-Rp1g_RC9uY…` | 44 141 | 0 | **М** | никто |
| 17 | `cc_userbot_log` | `1yqzVrSFRN-1Zr3y…` | 5 394 | 0 | **М С** | никто. Ведётся вручную |
| 18 | `cowork_log_test` | `1zGPT5DqNdUT8FOr…` | 39 | 0 | **М** | никто; всё тело — «DONE 2026-07-29 00:00 UTC: проба замка» |
| 19 | `cowork_log_test_archive` | `1HvhvlxKFPiHUYlM…` | 2 372 | 0 | **М С Д** | никто. Имя тестовое, содержимое боевое (см. §3) |
| 20 | `executors_map` | `1NyfcErxNt09CH8J…` | 5 588 | 0 | **М** | никто; ритуальный читатель — только полная ревизия |
| 21 | `orchestrator_plan` | `1_ogUGFim24Ifw60…` | 6 208 | +89 | **М** | никто |
| 22 | `orchestrator_safety` | `1UB1MWs8ZQWDkwHY…` | 5 107 | 0 | **М** | никто |
| 23 | `payments_plan` | `13WtxQaDLdixR4Es…` | 3 441 | 0 | **М** | никто; в теле — «СТАТУС: ОТЛОЖЕНО» |
| 24 | `state_model` | `1OGUzeb60UbzAaCO…` | 9 753 | 0 | **М Д** | никто в коде; §6 [5] — раз в ревизию (последняя 07.07, см. §5) |
| 25 | `infra` | `1z2wS0I0nm-dJGqF…` | 6 702 | 0 | **М** | то же |
| 26 | `park_list` | `1jD3VJGeoET8Yf5m…` | 2 642 | 0 | **М** | **никто.** `suggest.py` берёт парк из живого `pricing.fleet()`, фолбэк — ЛОКАЛЬНЫЙ `manager-bot/docs/park_list.md`, не этот док |
| 27 | `booking_flow` | `11HvMKGZRdSoRjoj…` | 6 501 | 0 | **М** | никто; спека для Штаба. **Зарегистрирован после 30.07** |
| 28 | `collect_booking_spec` | `1PZ7TeEQJLtzrw66…` | 4 553 | 0 | **М** | никто; текст скопирован в `collect_booking_prompt.txt` вручную. **Зарегистрирован после 30.07** |
| 29 | `KB_trainer_log` | `13kp-54bzN2Xua8R…` | 2 190 | 0 | **Ж С** | **боевой:** `trainer_log.py:57` по id (userbot + moderbot). **В реестре НЕТ** |
| 30 | `KB_trainer_log_archive` | `1lOco1SI58UNT0a4…` | **133** | **впервые** | **Ж** | `trainer_log.py` при перерастании порога. **В реестре НЕТ.** Опись 30.07 живость не пробовала — **пробовал я: ЖИВ** |

Строк 30, документов 29 (`faq`/`turbobaby_faq` — один файл под двумя ключами).

**Мёртвые id (проба сделана, все четыре — отказ Drive «объект не найден»):**
`1Z70Epg…` (бывш. `roadmap_master`), `1klCUC5…` (бывш. `KB_index`), `1TwNzB_…` (старый
`knowledge_base`), `1cRedmo…` (старый `project_state`).

---

## 2. ЧИСЛА ПО ГРУППАМ

| группа | сколько | символов | доля объёма |
|---|---|---|---|
| **ЖИВОЙ** — есть потребитель содержимого (код или ритуал) | **11** | 1 240 622 | 48,8 % |
| **ПРОБА-ТОЛЬКО** — читают регулярно, но текст выбрасывают | **2** | 39 391 | 1,5 % |
| **МЁРТВЫЙ** — ни одного читателя нигде | **16** | 1 264 346 | **49,7 %** |
| ВСЕГО достижимо | **29** | **2 544 359** | 100 % |

Реестр: 29 записей = 28 ключей + `folder_id`; уникальных id — **27**, живых из них — **27**
(мёртвых ключей больше нет). Вне реестра живых доков — **2**.

**Что изменилось в реестре за двое суток (пересчёт того стоил):**
- **снят** мёртвый ключ `roadmap_master` — «ключ без файла» закрыт;
- **добавлены** `booking_flow`, `collect_booking_spec` (были живыми, но невидимыми для реестра),
  `business_rules` (новый док 01.08);
- итог: 26 ключей → 28, мёртвых 1 → 0.

---

## 3. ДУБЛИ — 7 пар + 5 самодублей (все перемерены сегодня)

| # | пара | совпадает | кто свежее |
|---|---|---|---|
| 1 | `faq` ≡ `turbobaby_faq` | **один и тот же файл**, два ключа реестра | не применимо — файл один |
| 2 | `cc_log_archive` ↔ `review_archive` | **20 652 симв., 189 строк** | `cc_log_archive` (в нём даты до 08.2026, в `review_archive` — по 20.06) |
| 3 | `index` ↔ `state_model` | 2 662 симв., 11 строк (блок «ПОТОК: таец отфоткал байк…») | `index` (растёт, +5 424 за двое суток; `state_model` не менялся) |
| 4 | `cowork_log` ↔ `cowork_log_archive` | 2 531 симв., 7 строк — запись лежит И в живом, И в архиве | `cowork_log` (живой) |
| 5 | `cowork_log` ↔ `cowork_log_test_archive` | 2 099 симв., 3 строки = **89 % «тестового» архива** | `cowork_log` |
| 6 | `cc_log_archive` ↔ `index` | 332 симв., 5 строк | `index` |
| 7 | `knowledge_base` ↔ `faq` | прайс, не дословно, **с расхождением**: `knowledge_base` метит PCX 150 «⚠️ нет в парке», `faq` подаёт как действующий прайс | `knowledge_base` |

**Самодубли (повторы внутри одного тела, декоративные линии отфильтрованы):**

| док | повторов | лишних симв. |
|---|---|---|
| `cc_log_archive` | 91 | 51 744 |
| `cowork_log_archive` | 7 | 43 065 |
| **`cowork_log`** | **8** | **6 566** |
| `review_archive` | 4 | 551 |
| `project_state` | блок «ID базы знаний (Brain)» стоит дважды — строки 413 и 438 (второй помечен «дубль для быстрого доступа») | — |

Самодубль `cowork_log` — не косметика, а **известный класс**: ложный отказ записи при уже легшей
строке + слепой повтор = дубль в журнале (`[[cowork-log-false-401]]`). Восемь штук — это счёт
случившегося, а не риска.

---

## 4. СМЕШАННЫЕ РОЛИ — 7

| док | что смешано | чем это опасно ПО ФАКТУ |
|---|---|---|
| **`knowledge_base`** | бизнес-справочник (депозиты, прайс) + реестр Drive-id + **боевые данные**: блок интервалов ТО | **единственное смешение с прод-последствием.** `splinter.py` парсит из этого дока интервалы ТО; правка прайса рядом с блоком роняет разбор в фолбэк-хардкод, и увидеть это можно только в логе |
| **`index`** (KB_MASTER) | карта + журнал (§3, датированные строки с хешами) + состояние («КАРТА МИГРАЦИИ» ВЫШЕ собственной шапки) + инструкция (§0, §6) | док объявлен «точкой входа №1»; исполняющий §0 получает **91 959 символов**, из которых карта — меньше половины |
| **`project_state`** | архив-снимок + состояние + реестр id + инструкция синка | зовёт себя «снимок 11 июня», просит читать себя первым и несёт поправку от 3 июля; **три мёртвых id внутри, каждый дважды** |
| **`rules`** | инструкция + предметный справочник (карта колонок таблицы «Байки») | карта колонок — предмет `infra`, а лежит в своде правил |
| **`review`** | навигация + журнал + состояние канала — в 7 строках, 511 симв. | — |
| **`cc_userbot_log`** | журнал + свод пяти слитых доков | — |
| **`cowork_log_test_archive`** | **имя тестовое, содержимое боевое** (89 % — дословные строки живого `cowork_log`) | ключ говорит «тест», содержимое — боевая история; любая чистка «тестового» уносит боевые записи |

---

## 5. БИТЫЕ ССЫЛКИ — на что указывают рамка и код, а его нет

Историю не считаю за дефект: `cc_log_archive` полон имён доков, слитых 27.06 в своды, и это
**протокол, а не указатель**. Ниже — только ЖИВЫЕ доки и КОД, где имя подано как действующий адрес.

| # | указатель | где | состояние |
|---|---|---|---|
| 1 | **`KB_NORTH_STAR`** | `index` §5 — «— философия главной оси (рамка над всем)» | **БИТАЯ.** Ни ключа, ни id, ни файла. §5 подаёт как живой ориентир. **Главная битая ссылка рамки Штаба** |
| 2 | **имя `KB_MASTER`** | 18+ мест, включая §0 «прочитать KB_MASTER» | **каналом не резолвится:** реестр знает только ключ `index`, `read_doc name=KB_MASTER` → `unknown_name`. Док жив; не открывается имя, которым его зовут везде |
| 3 | **`registry_check.py` → `KB_ALIAS`** (VPS) | `roadmap_master`→`roadmap_master`, `north_star`→`roadmap_master`, `roadmap_v2`→`roadmap_master` | **дефект в коде, заведён снятием ключа.** Ключа `roadmap_master` в реестре больше нет → `resolved` = False. Пока молчит (в пульсе этих имён нет), сработает ложной тревогой при первом же упоминании |
| 4 | **`BRAIN_DOC_CLUTTER_AT = 28`** (VPS `registry_check.py`) | порог «засорение мозга» | **сработает на следующем доке.** Сейчас уникальных id — **27**, порог 28. Регистрация ещё одного зажжёт сигнал §6 |
| 5 | `KB_INFRA_UNIFY_FUTURE` | `infra`:«6. УНИФИКАЦИЯ (будущее, KB_INFRA_UNIFY_FUTURE)» | БИТАЯ, но мягкая: подана как будущая работа |
| 6 | `KB_INFRA_FORMS` | `infra`:«полная карта — **была** в KB_INFRA_FORMS» | прошедшее время = протокол, не адрес |
| 7 | `KB_claude_review_archive` | `review`:1 | имя устарело, но **id в той же строке живой** — читатель дойдёт |
| 8 | «счёт файлов в норме (~21 активных)» | `index` §6 [6] | норма устарела: фактически 27 в реестре + 2 вне него |

**Закрыто после 30.07** (проверено сегодня, снимаю с учёта): `KB_ROADMAP_MASTER` — во всех трёх
живых доках (`index`:866, `rules`, `orchestrator_plan`:2) теперь стоит «удалён 27.06, ключ
`roadmap_master` мёртв»; `KB_REVISION_RULE` в `rules` — теперь провенанс со ссылкой на §6 KB_MASTER;
`roadmap_master` снят с реестра; два мёртвых id из `project_state` по-прежнему в теле (п. §4).

**Ритуальный читатель почти не приходит.** Полная ревизия §6 (единственный читатель `rules`/`infra`/
`state_model`/`executors_map`) в журналах последний раз — **07.07.2026**, 25 дней назад
(упоминание 31.07 — ревизия замка карточек, это код, не мозг).

---

## 6. ЧТО НЕ ПОДТВЕРДИЛОСЬ ИЗ ОПИСИ 30.07

Владелец был прав, что потребовал пересчёт.

1. **«20 документов никто не читает»** → сегодня **16** (не 20). Три ушли из мёртвых
   (`booking_flow`, `collect_booking_spec` зарегистрированы; `roadmap_master` перестал
   существовать как ключ), и — главное — 30.07 не различала «читают содержимое» и
   «читают ради замера латентности»: `review` и `project_state` попадали то в одну графу, то в другую.
2. **«прайс и парк описаны ТРИЖДЫ (`knowledge_base`/`faq`/`park_list`)»** → **неверно.**
   В `park_list` слова «PCX 150» нет вовсе: там поимённый список без цен. Дубль **парный**,
   `knowledge_base` ↔ `faq`.
3. **«91 % объёма мозга не читает никто»** → сегодня **49,7 %**. Разница почти вдвое, и она не от
   уборки: `cowork_log` (живой, читаемый) вырос в **3,8 раза**, и доля мёртвого объёма упала
   арифметически. Само мёртвое почти не изменилось.
4. **Самодубли `index`/`rules`/`infra`/`state_model`** (30.07 их не называла, наивный счёт их
   «находит») — **ложные срабатывания**: повторяется только декоративная ═-линия.
5. **`KB_trainer_log_archive` «живость не пробована»** → **пробовал: ЖИВ**, 133 символа.

---

## 7. ПЛАН УБОРКИ — НЕ ПРИМЕНЁН

Ничего из списка не сделано. Порядок — по отношению «выигрыш / риск».

| # | что | почему | откат |
|---|---|---|---|
| 1 | **Снять ключ `cowork_log_test`** (39 симв., проба замка) | мусор, читателей ноль | `brain_writer.py --register cowork_log_test=1zGPT5DqNdUT8FOrOxsVI8-mI7sZjz3kU` |
| 2 | **Переименовать `cowork_log_test_archive` → `cowork_log_archive_2`** (или перелить 3 боевые записи в `cowork_log_archive` и снять ключ) | имя врёт про содержимое: 89 % — боевые записи; чистка «по имени» уничтожит историю | ключ снимается/возвращается `--register`; тело в бэкапе `tmp/brain_backup_*` (пишется ДО любой записи) |
| 3 | **Завести `KB_NORTH_STAR` ИЛИ убрать строку из `index` §5** | битая ссылка в рамке Штаба опаснее лишнего дока; сейчас §5 обещает документ, которого нет | правка `index` — якорная вставка `brain_writer`, бэкап тела ДО записи, откат = запись бэкапа |
| 4 | **Зарегистрировать `KB_trainer_log` и `KB_trainer_log_archive`** | два **боевых** дока (пишет `trainer_log.py`) невидимы реестру; при этом п.5 ниже | `--unregister trainer_log` |
| 5 | **Поднять `BRAIN_DOC_CLUTTER_AT`** (VPS `registry_check.py`, сейчас 28) **вместе с п.4** | 27 + 2 = 29 ≥ 28 → регистрация трénерских доков зажжёт ложное «засорение» | `git revert` коммита |
| 6 | **Починить `KB_ALIAS`** в `registry_check.py` — убрать `roadmap_master`, увести `north_star`/`roadmap_v2` на `index` | ключа нет с 31.07; алиас указывает в пустоту | `git revert` |
| 7 | **Вынести §3 `index` (журнал, ~половина тела) в `sessions_log`** | «точка входа №1» на 91 959 симв. — читателю §0 выдаётся журнал вместо карты | бэкап тела ДО записи (`tmp/brain_backup_index_*`), откат = запись бэкапа |
| 8 | **Убрать из `project_state` три мёртвых id (каждый дважды) и внутренний дубль блока** | реестр мёртвых адресов внутри дока, который зовут «читать первым» | тот же бэкап-откат |
| 9 | **Свести `index` ↔ `state_model` (2 662 симв.)** — оставить в `state_model`, в `index` ссылку | два источника правды по потоку выдачи | тот же |
| 10 | **Согласовать прайс `knowledge_base` ↔ `faq`** | `faq` (**боевой**, идёт клиенту) подаёт как действующие модели, помеченные в `knowledge_base` «нет в парке» | тот же; `faq` — канон, `docs/*.md` зеркало |
| 11 | **Разделить `knowledge_base`: интервалы ТО — отдельным доком** | единственное смешение с прод-последствием (`splinter._load_service_intervals`) | требует правки VPS-кода → отдельная задача, откат `git revert` + ре-синк |
| 12 | **Дедуп `cowork_log`** (8 повторов, 6 566 симв.) | следствие слепых повторов при ложном отказе записи | `allow_shrink=True` + бэкап; откат = запись бэкапа |

**Пункты 3, 7, 8, 9, 10, 12 меняют тела живых доков — каждый требует «да» владельца отдельно.**
Пункты 1, 2, 4 — операции реестра, обратимы одной командой. 5, 6 — код VPS.

---

## Итог (5 строк)

1. **Достижимо 29 доков, 2 544 359 символов;** реестр — 28 ключей на 27 уникальных id, все живы,
   мёртвых ключей больше нет; 2 боевых дока (`KB_trainer_log` + архив) реестру по-прежнему невидимы.
2. **Мёртвых — 16, а не 20; 49,7 % объёма, а не 91 %.** Расхождение не от уборки: `cowork_log`
   вырос втрое с лишним и разбавил долю. Ещё 2 дока — `review` и `project_state` — читаются
   регулярно, но **только как замер латентности**: содержимое выбрасывается.
3. **Дублей 7 пар + 5 самодублей**, из них `cowork_log` дублирует сам себя 8 раз — это не риск,
   а уже случившийся класс «слепой повтор после ложного отказа записи».
4. **Смешанных ролей 7**, но опасна одна: `knowledge_base` держит боевые интервалы ТО вперемешку
   с прайсом, и правка прайса может молча увести `splinter` на хардкод-фолбэк.
5. **Битых указателей 8**, главные три: `KB_NORTH_STAR` (рамка Штаба обещает несуществующий док),
   имя `KB_MASTER` (каналом не резолвится, а зовут им везде) и `KB_ALIAS`→`roadmap_master`
   в живом VPS-детекторе. Порог «засорение мозга» — 28 при факте 27: сработает на следующем доке.

**Не сделано и требует разрешения:** перечень файлов ПАПКИ (`_archive`, `OLD`) и даты правки —
мост таких экшенов не имеет, Drive-коннектор в headless не авторизован.
