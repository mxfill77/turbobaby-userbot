# Полный отчёт задачи 54 — 2026-08-18 11:39 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **9134 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 54 |
| записано | 2026-08-18 11:39:24 |
| символов в отчёте | 9134 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Судья адреса НЕ подключён превентивно: замок родился в ТОМ ЖЕ коммите `2d71c19` (16.08 17:22), что и прибор — реакцией на сбой быть не мог. Замков не один, а четыре. Подключать сегодня нечего: адрес не пишет ни один из 4 боевых вызовов. Путь до клиентского контура ЕСТЬ, один — строка 3044. Артефакт записан, коммит `fc7642c`.

**FACT:**
- `FACT: commit fc7642c в git log` → `fc7642c 2026-08-18 18:38:11 +0700 ARTIFACT почему судья адреса НЕ подключён…`, 1 файл, +561 строка.
- `FACT: read-only` — кода не правил, ничего не подключал, гейт не гонял, процессов не трогал, `series_pc.state.json` не менял, боевых записей нет (журнальную строку НЕ писал: заданием запрещено «боевых записей нет»).
- `FACT: артефакт на диске` → `docs/artifacts/2026-08-17-judge-unwired-why.md`, 561 строка.
- **Временное:** `_scratch_judgewhy_0818/` — один файл `shadow_kinds.txt` (замер видов адреса), untracked, не убран по требованию задания.

**Признак сделанности:** файла `docs/artifacts/2026-08-17-judge-unwired-why.md` на диске НЕ БЫЛО (`ls` → `No such file or directory`) — работал.

---

## 1. Кто и когда поставил защиту

```
commit 2d71c192af9a5635986d101c1c04d4cf8c2097cc
author Filipp <info@turbophuket.com>
date   2026-08-16 17:22:54 +0700

судья адреса результата на полосе ПК: … построен и НЕ ПОДКЛЮЧЁН (вызовов из боевого кода
0 из 55 модулей, замок ast)
…
Откат: судью никто не зовёт — достаточно НЕ ПОДКЛЮЧАТЬ; целиком git revert, боевой путь не
изменится ни на байт.

 docs/artifacts/2026-08-16-result-judge-pc.md | 155 ++++++
 result_judge_pc.py                           | 645 ++++++++++++++++++++++++
 test_result_judge_pc.py                      | 727 +++++++++++++++++++++++++++
```

Замок пришёл **тем же коммитом**, что и прибор (диффом коммита: строки `+550`, `+551`, `+599`, `+602`). Держащий тест — `test_result_judge_pc.py:609-610`:

```
# ═════════════════════════ ИНВАРИАНТ RESULT_JUDGE_UNWIRED ════════════════════════════════════
# ГРАНИЦА ЗАХОДА ЧИСЛОМ: судья построен и НЕ ПОДКЛЮЧЁН. Ноль вызовов из боевого кода.
```

Он **fail-closed**: `git ls-files` не ответил или файлов <30 → провал, а не «нарушений нет» (`:691-692`).

**Защит четыре, не одна** — и их путают: `RESULT_JUDGE_UNWIRED` (ast, корень репо); подстрочный запрет имени в `pc_orchestrator.py` **включая прозу** (`:733-736`); инертность ПОЛЯ в **другом** файле набора (`test_pc_local_dec.py:1053`); `SERIES_COUNTER_UNWIRED` (`test_series_pc.py:607`).

## 2. Граница — дословно

Запрещён **не импорт, а упоминание имени модуля В КОДЕ** в любом git-tracked `*.py` **корня** (`prod_sources:631-634` отсеивает подкаталоги, `test_*` и сам судью). Находки — 4 вида (`wiring_findings:671-682`): `Import`, `ImportFrom`, `ast.Name`, и **строковый литерал по подстроке**. Проза (докстринги) освобождена — поправка после ложного красного 17.08, когда замок падал на честной фразе `card_terminal_log.py`.

```
=== всего .py в git === 126
=== в подкаталогах (вне охвата) === 6   (архивная копия гарда + тесты; боевых нет)
=== боевых в корне === 64            → охват 63 сегодня против 55 при заведении
```

Импортируют судью **только тесты**; прозой законно называют три боевых файла (`brain_probe_pc.py:7`, `card_terminal_log.py:51/56`, `series_pc.py:15`).

## 3. Реакция на сбой? — НЕТ

Сбой есть (перепись `a545ad2` 15.08: 87 мест судят исход, 62 по отчёту, 25 чтением назад, **ни одно не читает продукт**), но он объясняет, зачем судью **построили**. Доктрина «заведено ИНЕРТНЫМ» старше судьи на день (`6cb2a70`, `05e54aa`). Поиск по **297** телам `docs/artifacts/journal/`, **13** отчётам `reports/` и телам всех коммитов дал **0** записей «подключили прибор → сломалось» как основания. Единственный инцидент с самим замком — его собственное ложное красное 17.08, закрытое обучением границы (`099ae5a`).

## 4. Что изменится при подключении

**Цепочки: сегодня ни одна не получила бы осмысленного вердикта.**

```
4651:    if not _loc_release(tid, 1, len(steps), steps[0]):
4992:def _loc_release(pid, j, total, text, k=0, ref=result_ref.EMPTY):
5279:                    _loc_release(pid, i + 1, new_total, new_steps[0], k=k)
5287:    _loc_release(pid, i + 1, total, txt, k=k_origin)
5383:    if _loc_release(pid, i + 1, total, txt, k=k_origin):
=== сколько передают ref= === 1   ← это строка `def`; вызовов с ref: 0 из 4
=== маркер [result_ref: по всем логам === только pretool_guard.log (эхо команд ЭТОЙ сессии)
```

Читатель `_loc_step_ref` (`5021`) не зван ни разу. Сходится с замером 16.08: «канонический маркер в истории — **0 строк из 158**». Судья ответил бы `НЕИЗВЕСТНО` на 100 % — а это **обрыв** серии, не «в порядке». Порядок работ обратный ожидаемому: сперва писатель адреса, потом судья.

**Сеть:** 1 вид из 5 — `brain` → `brain_probe_pc` → мост, **4.9 c** на чтение, кэша нет, мост флапает. Под гейтом эта дверь закрыта fail-closed, в бою открыта — проверка и прод расходятся устройством.

**КЛИЕНТСКИЙ КОНТУР — отдельной строкой: ДА, путь есть, и он один.** `pc_orchestrator.py:3044` `if status == "done":` → `maybe_update_bots` → `_restart_via_pc_agent` → рестарт живых `userbot_listen.py`/`moderation_bot.py`. Опасное направление — **молчаливое**: ложный отказ уводит задачу мимо строки 3044 целиком, рестарт не отложен, а **не запрошен**, и записи об этом не будет. Ворота контура (`5709`) судят файлы коммита, а не вердикт — лишнего не пустят, недостачи не заметят. Откатов нет (`revert`/`reset --hard` → 0), TTL и `needs_approval` вердикта не касаются.

## 5. Теневой режим

Есть у **прибора**: `chain_shadow:794` / `shadow_diff:855` / `shadow_record:868` / `SHADOW_LOG:758`; файл `result_shadow_chain_pc.jsonl` живой — **46519 б, 148 строк, 17.08 00:14**, untracked. Писателя в судье нет **устройством** (`open` запрещён инвариантом). **Но боевого хозяина у тени нет:** позвать судью из прода = снять замок; отдельным процессом нельзя (нет `__main__`/CLI, а runner был бы тем же боевым `.py` корня). Тень доступна только разовому скрипту руками — так она и была исполнена.

## 6. Где теряется вид адреса

У судьи вид **есть** (`shadow_record:890`), замер по живой тени:

```
виды адреса корня: {'(вид не назван)': 44, 'commit': 77, 'file': 27}
пары вид→вердикт: commit/ДОКАЗАН 76 · commit/НЕ ДОКАЗАН 1 · file/ДОКАЗАН 27 · не назван/НЕИЗВЕСТНО 44
```

Теряется в **трёх** местах `series_pc.py`: воронка `read_feed:440` (`make_entry(one.get(KEY), one.get(VERDICT), one.get(ROOT), one.get(CLOSED_AT))` — всё сверх 4 полей отбрасывается молча); контракт `make_entry:228-231` + `:139-142` (константы вида нет вовсе); память `fold:300/305/306`, `zero_state:237`, `render:319` (обрыв помнит слово вердикта, не вид). Итог: полоса отвечает «сколько», но не «чем доказано» — 30 чистых на `file` и 30 на `commit` неразличимы.

**Минимум (не сделано, только названо):** константа `KIND` + проводка через `make_entry`/`read_feed`; вид в `last`/`why`; и — важное — **вид у судьи брать не обязательно**: `result_ref.parse()` отдаёт его чисто, а `result_ref` уже в замыкании демона. То есть вид возвращается в счёт, **не задев ни одного из четырёх замков**.

## 7. Предсмертный взгляд

**Подключение провалится не красным гейтом, а ЗЕЛЁНЫМ: гейт самообновления гоняет РОВНО `test_pc_orchestrator` + `test_pc_local_dec` (`pc_orchestrator.py:3911`), замка судьи там нет, хуков и CI на полосе нет вовсе, а ленивый импорт внутри функции не видит и проверка верхних импортов (`test_pc_orchestrator.py:800-801`) — демон перезапустится сам и уедет в бой с подключённым судьёй мимо своей защиты, погасив на строке 3044 рестарт живых ботов молча.**

Подпорки: `core.hooksPath` пуст, `.git/hooks` без живых хуков, `.github` отсутствует — полный гейт здесь ручной ритуал; провал гейта запоминается по блобу (`3949`) и не перегоняется до следующего коммита; вид `commit` судится против `origin/main`, а HEAD сейчас впереди неё на **2** коммита (`794e70b` против `a2ecfcc`).

---

**Сводка.** Защита превентивная и ровесница прибора — сбой стои́т за постройкой судьи, а не за его неподключением. Замков четыре, они разной силы и живут в разных файлах; граница запрещает упоминание имени в коде любого боевого `.py` корня (63 модуля), прозу освобождает. Подключать сегодня нечего: адрес не пишет ни один из 4 вызовов, судья ответил бы «неизвестно» на всём. Путь до клиента ровно один — `if status == "done"` на строке 3044, и ошибка там молчалива. Вид адреса теряется воронкой `series_pc.read_feed:440` и возвращается без судьи, через `result_ref.parse`.

RESULT: разведка read-only закончена — судья не подключён превентивно (замок `RESULT_JUDGE_UNWIRED` в том же коммите `2d71c19`, 16.08 17:22, автор Filipp), защит четыре, граница = упоминание имени в коде 63 боевых `.py` корня (проза свободна), сбоя за замком нет (0 находок по 297 телам журнала и всем коммитам), подключать нечего (0 из 4 вызовов `_loc_release` пишут адрес), путь до клиентского контура один и он `pc_orchestrator.py:3044`, тень построена но без боевого хозяина, вид адреса теряется в `series_pc.read_feed:440`/`make_entry`/`fold` и возвращается без судьи через `result_ref.parse`; артефакт `docs/artifacts/2026-08-17-judge-unwired-why.md`, коммит `fc7642c`, временное в `_scratch_judgewhy_0818/`.
