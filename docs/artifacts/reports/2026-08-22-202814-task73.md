# Полный отчёт задачи 73 — 2026-08-22 20:28 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **6731 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 73 |
| записано | 2026-08-22 20:28:14 |
| символов в отчёте | 6731 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Задача 73 — это переотправленная 70: работа уже лежала в git, поэтому я её НЕ переделывал, а перемерил независимо (правило «сверяй с git log до переделки»). Все три отрицательных теста повторены своей рукой, второй — ДРУГОЙ мутацией и без правки боевого файла. Раздел в живой карте подтверждён обратным чтением. §2 перепроверки дописан в артефакт, коммит a460539, push прошёл.

**FACT:**
- `a460539` в `git log` (§2 перепроверки в артефакт), push `e199b90..a460539` в origin/main прошёл; ранее лежавшие `93e8497` (код `trainer_pin.Clock`), `9a9a46f` (артефакт), `6a81296` (тело журнала) — тоже отгружены.
- Своей рукой, чистое дерево: `det ×3 → 12 из 12`, покейсно ДА, промахи `{мост 0, док 0, голова 0, дверь6 0}`.
- Раздел `## ВОСПРОИЗВОДИМОСТЬ ЗАМЕРА ТРЕНАЖЁРА 23.08.2026` прочитан из живого узла `master`: **1 вхождение, 7311 символов, идёт первым**, прежние разделы на месте.
- Строка `DONE` легла в мозг (писатель: `OK: записано в мозг, символов: 573424`).

---

## П.2 — разошедшийся состав красных: ОТДЕЛЬНАЯ болезнь, не истечение снимка

Доказано наблюдением, не рассуждением — тремя числами, снятыми в этой сессии:

1. **Истечение краснить не умеет вовсе.** `drift` (снимок 22.08 + живые часы) → `11 из 12, неизвестно: [2], красные: нет`, промахов моста **111**. То же на подделке времени без лечения (`future_raw +1сут` и `+7сут`) → те же 111 промахов, кейс 2 → НЕИЗВЕСТНО, **красных ноль**. Промах снимка выводит кейс из вердикта раньше, чем судят хоть один чек.
2. **Вчерашнее расхождение случилось при НУЛЕ промахов.** Живой заход 64 (`_scratch_gate6_0823/res_live.json`, читал сам): `11 из 12 / 11 из 12 / 10 из 12` — при нулевых промахах границы. Истечению там взяться неоткуда.
3. **Заморозь голову — расхождение исчезает.** На том же снимке `12 из 12` трижды, покейсно совпало, и совпало на сдвигах +1/+7/+30/+365 суток.

**Вывод: болезней две и они независимы.** Истечение снимка = прибор СЛЕПНЕТ (НЕИЗВЕСТНО). Переезд красного = ЖИВАЯ ГОЛОВА. Заход лечит первую; вторая не тронута.

## Три отрицательных теста — мои результаты

**ТЕСТ 1 (подделка времени) — ПРОЙДЕН.** Часы подделаны В ПРОЦЕССЕ (перевод системных часов — красная операция, её нет):

| сдвиг | продукт видит | число | совпало с `det` покейсно |
|---|---|---|---|
| +1 / +7 / +30 / +365 сут, Clock ВКЛ | 2026-08-22 | **12 из 12** | **ДА** ×4 |
| +1 / +7 сут, Clock ВЫКЛ (контроль) | 2026-08-24 / 2026-08-30 | 11 из 12, 111 промахов | **НЕТ** |

Контроль кусается — значит тест не пустой.

**ТЕСТ 2 (порча кода бота) — ПРОЙДЕН, набор покраснел.** Порчу сделал **другой** (`"0"→"9"` против `"1"→"9"` у захода 70) и **в процессе**, а не на диске: `suggest.py` боевой, минута его порчи на диске — минута риска для живого процесса. Подменена `suggest.compose_quote_draft` — клиент получает неверную цену. Результат: **7 из 12 в трёх прогонах из трёх**, красные `{1, 3, 7, 8, 12}`, промахов 0. `sha256(suggest.py)` до и после — `0f36e707afab1b0d`, совпал. Откат порчи захода 70 подтверждён **чтением сегодня**: `git status --porcelain -- suggest.py` пуст, `grep 'replace("1", "9")'` → 0, строки 4647–4649 прочитаны. Две независимые порчи дали ОДИН состав красных — улика сильнее повтора одной.

**ТЕСТ 3 (снимок недоступен) — ПРОЙДЕН, прибор говорит «неизвестно».** Пустой снимок шестой двери → `4 из 12, неизвестно: [1,2,3,7,8,9,10,12], красные: нет` (промахов двери 9, пятно true). Снимка дверей 1–5 нет вовсе → `ОТКАЗ ПРИБОРА: FileNotFoundError` **до первого кейса**, числа не названо.

## П.8 — вердикт одной строкой

> **Настоящего второго ключа к воротам НЕТ. Есть настоящий ПРИБОР: воспроизводимый между сутками замер КОДА (12 из 12 покейсно и сегодня, и на +1/+7/+30/+365 суток; краснеет на порче кода) — но ворота ждут зелёного прогона ЖИВОГО продукта, а живое число по-прежнему не воспроизводится (11/11/10, пересечение красных пусто).**

Замок цел и проверен: `^import trainer_pin` в `trainer_run.py` → **0**; `write_verdict` в `trainer_pin.py` — только докстрока (стр. 70–71); `pc_orchestrator.client_trainer_green.json` → `"green": {}`, mtime **31.07 19:40** (сегодня не тронут).

---

## Остальные пункты (закрыты заходом 70, перемерены здесь)

- **П.1 (забор).** Срока как настройки нет: сутки идут из ПРОДУКТА — `suggest.py:5001`, якорь прайс-сетки «завтра» (`ds = (base + timedelta(days=1))`). Дата уходит в `params`, а `params` и есть ключ снимка: 111 ключей из 125 (88.8%) сдвигаются каждые сутки. Забор защищает от подлога: выкинь дату из ключа — и снимок начнёт отвечать замороженной ценой на вопрос, которого не слышал. Ключ оставлен дословным.
- **П.3 (решение).** Чинится ДЕНЬ, а не КЛЮЧ: `trainer_pin.Clock` возвращает продукту день съёмки снимка (`suggest.now_phuket` + вторые часы `price_freshness.judge`), и продукт САМ строит ровно записанные ключи. День выбирает СНИМОК (`Clock.from_snapshot`), а не оператор; запрет перезаписи не снят, `Clock` не имеет `save` и файлы только читает; срок не «увеличен» — его нет.
- **П.7 (число).** Три прогона одной командой на чистом дереве: **12 из 12 ×3, покейсно ДА**. Плюс сверка между процессами (`compare_sessions.py`): одна подпись на 6 прогонов двух сессий, вызовы моста 383 и там и тут, вызовы шестой двери 27 и там и тут. **Честная граница:** HEAD у обеих сессий один и разница 14 минут — это независимость от ПРОЦЕССА, а не от суток; сутки доказывает только тест 1.
- **П.9.** Раздел в живой карте есть — прочитан заново отдельным процессом, узел адресован по имени из живого реестра.
- **Юниты:** `test_trainer_pin` — 23 OK; соседи (`trainer_run`, `price_gate`, `price_freshness`, `trainer`) — 158 OK. Храповик неразбора красен на `main` до любой правки (1015 точек при линии 845, 2 падения); вклад обеих сессий нулевой — область счёта `git ls-files`, tracked-файлы не тронуты.

**Две поправки к цифрам артефакта, найденные перепроверкой** (внесены в §2): «соседи 181» — на самом деле 158, 181 получается только вместе с самим `test_trainer_pin`; храповик сегодня 1015, а не 1017 (момент замера был другой; воспроизвести не удалось — назвал своё число).

**Границы:** ворота не открыты, дети не обновлены, клиентам ничего не написано; снимки не переснимались и не менялись на байт, улики захода 70 открывались только на чтение (`res_det.json` mtime 03:04 как был); ничего не удалено; своё временное — **`_scratch_reprove_0823/`**; в секреты и конфиги не ходил; системные часы не переводились.

RESULT: задача 73 = переотправленная 70 — работу не переделывал, перемерил независимо: 12 из 12 ×3 покейсно, три отрицательных теста пройдены (второй — другой мутацией в процессе, 7 из 12 ×3), раздел карты подтверждён обратным чтением, вердикт прежний «второго ключа к воротам НЕТ, прибор есть»; §2 перепроверки дописан в артефакт (a460539), 4 коммита запушены, DONE в журнал лёг.
