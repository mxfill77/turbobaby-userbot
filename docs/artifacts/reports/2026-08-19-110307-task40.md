# Полный отчёт задачи 40 — 2026-08-19 11:03 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4772 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 40 |
| записано | 2026-08-19 11:03:07 |
| символов в отчёте | 4772 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Второй заход по признаку сделанности: артефакт **был** → открыл и перепроверил живыми фактами вместо доверия на слово. Фикс `a76d377` стоит, все числа воспроизвелись дословно. Нашёл одну не названную зависимость — маршрут карточки цел, но держался на прозе; закрыл тестом.

**FACT:**
- `FACT: commit 42289b9 + e2dae3b + 55a5ba0 в git log, запушены (5149b99..e2dae3b, main...origin/main без ahead)`
- `FACT: фикс В ПРОДЕ — демон PID 15008, старт 19.08 17:49:26, лог: «self-update: b5d5aab→5149b99 — гейт пройден, новый процесс запущен»` (коммит был 17:46:14)
- `FACT: гейт Ran 3799 tests in 234.407s, FAILED (failures=2, skipped=10)`

## Признак сделанности — что уже было сделано

Файл `docs/artifacts/2026-08-19-watchdog-numbers-fix.md` (21552 б) и коммит `a76d377` на месте. Сделано было: `_log_age_sec` заменён на чистую `absence_since()` со свидетелями жизни (собственный CIM-замер + запись в лог) и тремя исходами — «перезагрузка» (число доказано), «наблюдение» (верхняя граница, словом «не дольше»), НЕИЗВЕСТНО.

## Что перепроверено живьём

| заявлено | вывод команды | сошлось |
|---|---|---|
| гейт 3798 / 2 красных | `Ran 3798 tests in 247.450s, FAILED (failures=2, skipped=10)` | да |
| красные — те же храповики | `test_no_growth_against_baseline … FAIL`, `test_totals_do_not_grow … FAIL` | да |
| вклад в храповик ноль | `pc_orchestrator.py читателей 163 точек 317` против линии `164/320` | **ниже линии** |
| **история №1** | `БЫЛО 12 ч 10 м → СТАЛО 10 ч 36 м [перезагрузка]`, правда `10 ч 37 м` | **да**, остаток −66 с |
| **история №2** | `БЫЛО 5 ч 54 м → СТАЛО 6 м 14 с [перезагрузка]`, правда `6 м 27 с` | **да**, остаток −13 с |
| CIM == счётчик аптайма | оба `2026-08-18 20:27:36` | да |
| «дети пишут ЗАПУСК» | прочитаны `pc_agent.py:959`, `userbot_listen.py:635`, `moderation_bot.py:653` | все три |

**Оба отрицательных теста устояли и не пустые** — читал тела: (а) `test_a_known_absence_gives_the_true_number` держит `assertEqual(now - since, 3600)` и карточку «лежал 1 ч 00 м» без оговорок; (б) `test_b_yesterdays_false_number_is_no_longer_produced` подаёт **дословный вход 18.08** и требует, чтобы «5 ч 54 м» не появилось ни в карточке, ни в журнале.

**Размер класса на живом железе (замер 17:55:43):** `moderation_bot.log` молчит **6.4 ч** при живом модерботе (PID 10948 с 18.08 20:33:52). Старый код сказал бы «лежал 6 ч 24 м» о процессе, который не умирал.

## Находка: забор Честертона был назван неполно

§1 артефакта утверждал, что формат «лежал …» больше никто не разбирает. **Это неверно.** `dispatch_notify.awaits_reply` выбирает адрес карточки регуляркой `сторож\s+подн[яё]л` (`dispatch_notify.py:275`) — тема 328 против инбокса 1160. Фикс переписал хвост ровно этой строки.

Маршрут **по факту не поехал** — проверено на 12 комбинациях: успешный подъём → тема 328 при всех трёх исходах прибора, провал подъёма → инбокс. Но держалось это на прозе. Теперь на тесте `42289b9`; мутация («СТОРОЖ ПОДНЯЛ» → «сторож перезапустил») его роняет, значит замок не пустой.

Причина промаха — класс из CLAUDE.md: греп по дословной «СТОРОЖ ПОДНЯЛ» регулярку не находит (строчными, `\s+`, класс `[яё]`), и «нашлось 0» прочиталось как «зависимостей нет».

## Счётчик смертей — чем сбрасывается (НЕ чинил, другой класс)

Три места, проверены по коду: **(1)** любой живой тик — `_client_watch_step:6564` возвращает `deaths: 0`; **(2)** ветка `skip:6691` — `state[name] = {…"deaths": 0, "halted": False}`; **(3)** и сработало 18.08 именно оно — `_client_watch_state = {}` (`:6369`), модульная переменная в ОЗУ, обнуляемая **каждым стартом демона**: `client_watch.json` демон только пишет (`_persist_client_watch:6797`), читает его `pc_agent.py:395` — отдельный процесс. Пока смерти вызваны перезагрузками, они убивают и демона, поэтому `CLIENT_MAX_DEATHS=3` против этого класса бессилен конструктивно.

## Замок

Гейт **3799 тестов, красных 2** — те же два храповика неразбора, красные на main до любой правки. Число тестов: 3773 → 3798 (фикс) → 3799 (замок маршрута). Логика подъёма вторым заходом не тронута вообще: `pc_orchestrator.py` не менялся, правка — один тест. Процессы не трогал, `.env` не читал, ничего не удалял.

Артефакт дописан последним действием: §9 + поправка §1. Журнальная строка легла (473651 симв. в мозге, тело вынесено в `docs/artifacts/journal/2026-08-19-110206-done.md`, закоммичено `55a5ba0`).

RESULT: перепроверил фикс чисел вотчдога — стоит, все числа воспроизвелись дословно (гейт 3798/2, история 10ч36м и 6м14с, CIM 20:27:36); нашёл не названную зависимость маршрута карточки (dispatch_notify._RE_CLOSED), маршрут цел на 12 комбинациях и закрыт непустым тестом 42289b9; §1 артефакта поправлен, §9 дописан (e2dae3b); гейт 3799/2 те же храповики; всё запушено, фикс живёт в проде — демон PID 15008 обновился сам в 17:49:26.
