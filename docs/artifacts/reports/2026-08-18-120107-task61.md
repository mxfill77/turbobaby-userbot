# Полный отчёт задачи 61 — 2026-08-18 12:01 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5756 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 61 |
| записано | 2026-08-18 12:01:07 |
| символов в отчёте | 5756 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

**Сводка:** Артефакта не было → задача выполнена. Всё подготовленное 07.08 на диске и годно сегодня: `autologon_arm.ps1` (BOM, parse 0 ошибок), `lock_on_logon.vbs`, задача `tb_lock_on_logon` уже вооружена. Способ — LSA Secrets, не открытый реестр; утилита не нужна. Автологон НЕ вооружён (`AutoAdminLogon=0`). Инструкция из 10 шагов + откат + проверка выданы.

**FACT:** read-only задача — автологон не вооружён, реестр не правился, Планировщик/службы/автозагрузка не трогались, процессы не поднимались и не гасились. Живое доказательство эффекта — артефакт на диске и в git: **commit `65c2f92`** в `git log`, `docs/artifacts/2026-08-18-autologon-howto.md`, 318 строк. Живые замеры-опоры: `AutoAdminLogon = [0]`, `PowerShell parse: ошибок = 0`, `pc_orchestrator_watchdog … LogonType=Interactive … LastRun=08/18/2026 18:53:01 Next=18:58:00`.

---

## Признак сделанности

`docs/artifacts/2026-08-18-autologon-howto.md` — **не было** (`No such file or directory`). Артефакт-основание `2026-08-18-pc-autostart-why.md` **есть** (43285 байт, 18.08 17:56) → работал, а не остановился.

## Дословный вывод команд

**Реестр (значение `DefaultPassword` НЕ читалось — только наличие имени):**
```
AutoAdminLogon           = [0]
DefaultUserName          = [mxfill1]
DefaultDomainName        = [.]
DefaultPassword ИМЯ есть = False
AutoLogonCount ИМЯ есть  = False
DevicePasswordLessBuildVersion = 2
ConsentPromptBehaviorAdmin = 0
EnableLUA = 1
```

**Годность инструмента сегодня + отсутствие Sysinternals:**
```
BOM UTF-8 (EF BB BF) = True      размер = 14517 байт     mtime = 08/07/2026 05:13:24
PowerShell parse: ошибок = 0
C:\Users\mxfill1\Downloads\Autologon.exe   False      C:\Windows\System32\Autologon.exe   False
C:\Users\mxfill1\Desktop\Autologon.exe     False      C:\Tools\Autologon.exe              False
C:\Users\mxfill1\Downloads\Autologon64.exe False      D:\Autologon.exe                    False
LogonTrigger Delay = [PT5S] UserId = mxfillpc\mxfill1 Enabled = True
```

**Принципалы задач (`Get-ScheduledTask`, read-only):**
```
pc_orchestrator            State=Running  UserId=mxfill1  LogonType=Interactive  RunLevel=Limited
    MSFT_TaskTimeTrigger Start=2099-01-01T00:00:00   LastRun=08/18/2026 14:13:03 Result=267009
pc_orchestrator_watchdog   State=Ready    UserId=mxfill1  LogonType=Interactive  RunLevel=Limited
    MSFT_TaskTimeTrigger Start=2026-07-04T23:23:00 Rep=PT5M
    LastRun=08/18/2026 18:53:01 Result=0 Next=08/18/2026 18:58:00 Missed=0
pc_agent / TurboBabyRC     State=Running  UserId=mxfill1  LogonType=Interactive  (LogonTrigger)
tb_lock_on_logon           State=Ready    UserId=mxfill1  LogonType=Interactive  LastRun=14:08:55 Result=0
TurboBabyExpectPC          НЕДОСТУПНА (невидима неповышенной сессии — это НЕ «задачи нет»)
```

**Перекличка и замки:**
```
1548  pc_agent.py 14:08:50 | 13688 userbot_listen.py 14:08:59 | 21256 pc_orchestrator.py 14:13:03
15760 moderation_bot.py 14:38:55 | 7512 rc_remote_control.vbs | 9120 rc_supervisor.py
pc_orchestrator.lock -> PID 21256      pc_agent.lock -> PID 1548
```

**Права:** `mxfillpc\mxfill1` состоит в `BUILTIN\Администраторы`, но токен отфильтрован (`elevated = False`); `ConsentPromptBehaviorAdmin = 0` → повышение без диалога.

## Ответы на четыре вопроса

1. **Что лежит:** всё. `autologon_arm.ps1` (14517 б, 07.08 05:13, в git, разбирается сегодня 0 ошибок), `lock_on_logon.vbs`, артефакт 07.08. Более того — **половина работы 07.08 уже вооружена**: задача `\tb_lock_on_logon` живая и отработала сегодня. Не сделан ровно один шаг — ввод пароля владельцем. Потеряно ничего.
2. **Способ:** защищённое хранилище системы (**LSA Secrets**, `LsaStorePrivateData`), а не открытый текст в реестре — скрипт `DefaultPassword` из реестра ещё и **сносит**. Sysinternals `Autologon.exe` на машине **нет**, и он **не нужен**: скрипт делает тот же системный вызов. Доступен без установки нового — именно этот способ.
3. **Вотчдог:** `\pc_orchestrator_watchdog`, `mxfill1`, `LogonType=Interactive`, `RunLevel=Limited`, повтор 5 мин. Условие — **наличие сессии, а не способ входа**, поэтому ожидается «сработает»; подпорка — сегодняшний факт, что **запертый экран задачам не мешает** (замок 14:08:55 → вотчдог поднял демона 14:13:03). Про **RC-консоль сказано «НЕИЗВЕСТНО»**: замок на +5 с против подъёма RC на +18 с не измерен.
4. **Двух копий не будет.** Автологон действует только при загрузке, когда дублировать нечего. Защита: `MultipleInstancesPolicy=IgnoreNew` + четыре lock-файла с PID (`pc_orchestrator.lock`, `pc_agent.lock`, `userbot.lock`, `moderation_bot.lock`, все `O_CREAT|O_EXCL`) + три исхода `_find_pids_by_script` (фикс #171: таймаут пробы ≠ смерть). Единственный сценарий дубля — второй интерактивный вход поверх первого, и там второй агент упирается в свой лок.

Инструкция владельцу (10 строк, отдельные строки отката и проверки после перезагрузки) — в артефакте, раздел «ИНСТРУКЦИЯ ВЛАДЕЛЬЦУ». Ключевая ловушка названа: **после автологона экран будет заперт, и по виду экрана понять ничего нельзя** — судить только по времени строк в `pc_orchestrator.log` относительно момента разблокировки.

Запреты соблюдены: не вооружал, реестр не правил, Планировщик/службы/автозагрузку не трогал, пароль не запрашивал и не вводил, процессы не трогал, не перезагружал, код не правил, ничего не удалял, ботов не касался, в рабочие таблицы не писал, гейт не гонял.

RESULT: артефакт `docs/artifacts/2026-08-18-autologon-howto.md` создан и закоммичен (`65c2f92`, 318 строк) — read-only разбор: всё подготовленное 07.08 на диске и годно (parse 0 ошибок), способ = LSA Secrets без установки нового, вотчдог `mxfill1`/`Interactive` (демон — ожидается, RC — неизвестно), дубля не будет (4 lock-файла + IgnoreNew); инструкция владельцу из 10 шагов с откатом и признаком успеха выдана.
