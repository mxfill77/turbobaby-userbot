# Полный отчёт задачи 96 — 2026-09-01 17:25 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4532 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 96 |
| записано | 2026-09-01 17:25:07 |
| символов в отчёте | 4532 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

[V0 судит done: сделано · V0: PROVEN / all_gates_passed по адресу «docs/artifacts/2026-09-02-детерминизм-очереди-без-потери-пакетов.md»]
Детерминизм очереди исходящих подтверждён сильнее, чем просили: совпало не число тестов, а полный вывод трёх прогонов байт в байт. Реестр отодвинут переименованием и вернулся той же sha256 — 14 пакетов до и после. Ждут повтора НОЛЬ. Отрицательная проба нашла дефект (порча реестра не называлась, на пустом лотке очередь уходила в ноль молча) — закрыт правкой и 7 регрессами, коммит a0f6939.

**FACT:**
- `FACT: commit a0f6939 в git log` — «Порча реестра очереди исходящих названа вслух, а не молчит бутстрапом»
- `FACT:` состояние очереди вернулось БАЙТ В БАЙТ — sha256 `ab130de358a5a00460f7b86237b03f57177c3c297daf8a15a94e03eef330a0d8`, 18799 байт, **14 пакетов до и после**
- `FACT:` 114 тестов OK на восстановленном состоянии (было 107)
- `FACT:` артефакт `docs/artifacts/2026-09-02-детерминизм-очереди-без-потери-пакетов.md` в коммите; две строки в журнал легли («записано в мозг», тело DONE вынесено в `docs/artifacts/journal/2026-09-01-172358-done.md`)

## 1. Что отодвинуто и куда

| | |
|---|---|
| боевой файл | `D:\turbobaby-bot\review_outbox_queue_state.json` (вне git) |
| отложен как | `D:\turbobaby-bot\tmp\queue_determinism_0902\review_outbox_queue_state.json.aside-2026-09-02` |
| чем | `os.rename` (`tmp/queue_determinism_0902/move_state.py`) — инструмент **не умеет удалять ни одной веткой**, отказывается при занятой цели |

Поправка к заданию: в реестре **14 записей, а не 13**.

## 2. Три прогона с чистого состояния

Чистота доказана замером: после каждого прогона `review_outbox_queue_state.json` в корне — **НЕТ**.

| | тестов | итог | md5 полного вывода `-v` |
|---|---|---|---|
| до правки ×3 | 107 (72+35) | OK | `285345f20fc25e80ccb6d926d1ed046b` — все три |
| после правки ×3 | 114 (79+35) | OK | `0fba1c88b9d55dde263df603d4a55800` — все три |

Сверял **текст, а не число**: 107 зелёных могут быть 107 разными зелёными. `diff` попарно — расхождений ноль; длительность из сверки вычтена, она не свойство кода.

## 3. Возврат подтверждён чтением назад

Отдельный читатель (только чтение): 18799 байт, та же sha256, 14 пакетов, `bootstrap_at 2026-09-01T17:04:32.858147+00:00`. **14 = 14.**

Боевой `--status` на восстановленном состоянии **не запускал намеренно**: ключ обещает «только показать», но `tick()` всё равно зовёт `write_state` — прогон переписал бы ровно тот файл, целость которого проверяется.

## 4. Кто ждёт повтора — ноль

| причина | происхождение | почему повтора нет | сколько |
|---|---|---|---|
| `http_400` | ours | запрос не по формату — повтор воспроизвёл бы ту же ошибку | **10** |
| `accepted_no_answer` | spent | заход оплачен, id задачи нет — забрать нечем | **3** |
| `no_credentials` | ours | ключ канала не задан в окружении | **1** |

Наблюдение сверх задания: ноль ждущих — это вердикт, а не тишина. **Машина повтора на живых данных не срабатывала ещё ни разу**: паузы 300/900 и 600/1800 с и потолок 6/сут боевым корпусом не проверены ничем, кроме регресса. И чинить надо пакет, а не канал — 10 из 14 наш формат.

## 5. Отрицательная проба

Держало и до правки: пакеты порчей **не теряются** (14→14, пересборка из лотка), исход везде `unknown`, «готово» не рождается ни на одной дороге, падения нет.

Было сломано: порча **не называлась**. Битый файл выглядел честным бутстрапом, а на пустом лотке — `пакетов было 14 → стало 0, строка-индекс: ''`. Это и есть «начал с нуля молча».

Закрыто: `read_state_why()` (четыре беды названы словами; **отсутствие файла повреждением не считается** — замок от шума на каждом первом обороте), `state_why` в отчёте, `ВНИМАНИЕ:` в выводе, и единственное исключение из правила «пустой оборот молчит» в `journal_line`. Регресс `TestCorruptRegistryIsNamed` — 7 проверок на трёх видах порчи.

## Остатки (названы в артефакте §6)
битые байты затирает следующая запись — улику не посмотреть; счётчик попыток порча обнуляет (сегодня цена ноль); `--status` пишет реестр вопреки имени.

Красного не потребовалось: удалений не было, `.env` не читал, userbot/moderation_bot не трогал, временное — только в `tmp/queue_determinism_0902/`.

RESULT: детерминизм очереди исходящих подтверждён (3×107 и 3×114 тестов, полный вывод байт в байт), реестр отодвинут и возвращён той же sha256 с 14 пакетами, ждут повтора 0 (10 http_400 / 3 accepted_no_answer / 1 no_credentials), найденный молчаливый уход в ноль при порче закрыт коммитом a0f6939, артефакт docs/artifacts/2026-09-02-детерминизм-очереди-без-потери-пакетов.md
