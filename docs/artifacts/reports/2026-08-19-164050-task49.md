# Полный отчёт задачи 49 — 2026-08-19 16:40 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **7647 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 49 |
| записано | 2026-08-19 16:40:50 |
| символов в отчёте | 7647 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Готово. Пакет перенесён в узел `master` отдельным разделом: 102 961 → 127 779 символов, фраза-признак «пакет для внешнего партнёра 19.08» — ровно 1 вхождение. Все замки зелёные, включая обратимость символ в символ и повтор чтения через 60 с. Раздел о слабых местах есть и не приглажен, два вопроса на месте, утечек ноль.

**FACT:**
- Узел `master` (адрес взят из живого реестра моста по имени, id не хардкодился): **102 961 → 127 779 символов**, прирост **24 818** = вставка 24 816 + 2 перевода строки абзаца.
- Чтение назад **свежим процессом**: тело пакета (23 941 симв.) лежит дословно **1 раз**, фраза-признак **1 раз**, заголовок раздела **1 раз**, U+FFFD **0**.
- Повтор чтения через 60 с **другим процессом**: `sha256 2a9e04b47759a104` = `2a9e04b47759a104`, тело не изменилось.
- Замок обратимости: снятие вставки даёт **102 961 симв., равно прежнему телу символ в символ**.
- Журнал: строка легла в голову `cowork_log` со штампом `DONE 2026-08-19 16:39 UTC:`.
- Бэкап до записи: `tmp\brain_backup_partner_pkg_0819_20260819-233531.txt`.

**Признак сделанности (первым действием):** в узле было **0 вхождений** фразы «пакет для внешнего партнёра 19.08» → работа не была сделана, пошёл дальше.
**Основание:** артефакт на диске есть — `docs/artifacts/2026-08-19-partner-package.md`, 459 строк, 49 742 байта, коммит `2ededd2` подтверждён `git show --stat`.

---

## 1. Раздел о слабых местах — есть, приглаженным не является

`## 3. Что у нас плохо` — **10 782 символа, 180 строк, 5 подразделов**; плюс отдельный `## 4. Ограничения, о которых ты не знаешь` (3 842 симв., 57 строк).

```
### 3.1. Цена считается плохо, и плохо она считается неравномерно
### 3.2. Две модели из тринадцати бот посчитать не может вовсе
### 3.3. Внешние отказы: их немного, но система их не отличает от собственных
### 3.4. Ремонты руками: их считают прозой, а не счётчиком
### 3.5. Человеческая половина тренажёра фактически не построена
```

**Сколько неприятных чисел.** Механический счёт числовых токенов: **§3 — 246, §4 — 55**. По единице «отдельное числовое утверждение о НАШЕЙ слабости» (строка таблицы = одна единица) мой ручной счёт — **62 в §3 и ещё 12 в §4, итого 74**. Первое число — прибор, второе — моя сборка глазами; говорю оба, как принято в репо.

**Приглаженности нет, и это замерено, а не на глаз.** Восемь маркеров смягчения («в целом неплохо», «в основном», «несущественно», «незначительн», «скоро будет», «почти готово», «планируем», «временно») — **0 вхождений на оба раздела**. Против них: `0 %` — 9 раз, «ноль» — 10, «не существует» — 2, «не измерен» — 1, «не сработал» — 1, и объявленное самим разделом «Этот раздел без смягчений».

**Три худших, дословно** (все три сверены на вхождение в §3 машинно):

1. «**Как бот справляется с реальными разговорами** (36 пар из 62 эталонных, замер 17.08): назвал верную модель в **13.9 %**, обе верные даты в **8.3 %**, цену в **2.8 %**, **все три вместе — 0 %**.»
2. «живая проба записала 120 правил, в книге осталось 100, **20 потеряны без следа, и все 120 вызовов вернули «записано»**.»
3. «**живой прайс, которым бот пользуется прямо сейчас**, на тех же 381 бронях даёт ±15 % у **33.9 %** и ±5 % у **8.4 %** при медиане −18.7 %.»

Четвёртое по тяжести живёт в §4 и его стоит знать: «**умолчание в коде обратное** — если значение переключателя пропадёт, оба замка откроются **молча**, без ошибки и без строки в логе».

## 2. Два вопроса — оба на месте, дословно

Подзаголовков «Вопрос» — 2, формулировок «Вопрос:» — 2.

**(а) про мерку надёжности:** «### Вопрос (а). Верна ли наша мерка машинной надёжности?» → «Вопрос: **мерка вообще годная, или мы меряем не то?** Если не то — что бы ты мерил вместо неё, имея в виду, что различать причины отказа надо будет строить с нуля, а не включить?»

**(б) про путь через модерацию:** «### Вопрос (б). Не дешевле ли идти к клиентам через модерацию с подтверждением человека, минуя долгую стройку тренажёра?» → «Вопрос: **прав ли этот счёт, или мы недооцениваем второй путь?** Что бы ты изменил в постановке, чтобы человек в цикле не превратился в узкое горло — и есть ли способ сделать так, чтобы его решения кормили обучение автоматически, без отдельной учебной петли?»

## 3. Утечки — ноль, чистить было нечего

Двенадцать проб по телу пакета, все нули:

```
пути с расширением кода: 0        ключи окружения ВЕРХНИЙ_РЕГИСТР: 0
слэш-пути: 0                      внутренние имена доков/модулей: 0
похоже на токен/ключ: 0           телефоны: 0     email: 0     URL: 0
Telegram-идентификаторы (@ник): 0 CamelCase (имена классов): 0
номера задач «задача N»: 0        id тем/чатов «тема N»: 0
```

Вся латиница пакета целиком: `CLICK, FORZA, NINJA, VULCAN, XMAX` (названия моделей парка — они в тексте намеренно, поимённо разбираются промахи), `Windows`, `UTC`, `n`, `p90`, `p95`.

Пограничный случай называю сам: **`Windows`** в §4 («ручная перезагрузка Windows»). Это имя чужой ОС, а не наше внутреннее имя, путь или ключ, — под запрет не подпадает, и **вычищать его я не стал сознательно**: правка тела была бы переписыванием пакета, что запрещено. Путь к артефакту и хеш коммита стоят **только в моей шапке раздела** (это карта владельца), **внутрь пакета не попали**.

## 4. Длина и границы переноса

Пакет **влез одной вставкой**: тело 23 941 симв. / 383 строки, блок с шапкой 24 816 симв., узел после — 127 779. Дробить не потребовалось.

Границы взяты маркерами самого артефакта `=== НАЧАЛО ПАКЕТА ===` / `=== КОНЕЦ ПАКЕТА ===` (по 1 вхождению). **Служебная шапка (614 симв.) и приложение «откуда взято» (4 677 симв.) в узел не поехали** — они помечены в артефакте «её вставлять НЕ надо» и «(не для вставки)», то есть это граница автора пакета, а не моё сокращение. Обе остались в репозитории, и я назвал это прямо в шапке вставленного раздела.

## 5. Замки записи — все зелёные

```
[OK] прирост = длина вставки + 2 перевода строки  — вставка 24816, прирост 24818
[OK] фраза-признак присутствует ДОСЛОВНО ровно 1 раз
[OK] новый заголовок раздела встречается ровно 1 раз
[OK] тело пакета лежит ДОСЛОВНО ровно 1 раз       [OK] блок целиком ровно 1 раз
[OK] битой кодировки (U+FFFD) нет                 [OK] возвратов каретки не появилось
[OK] снятие вставки возвращает прежнее тело СИМВОЛ В СИМВОЛ — 102961 против 102961
[OK] 7 прежних заголовков целы, каждый 1× (было 1 → стало 1)
[OK] заголовков ===…=== стало ровно на один больше — было 7, стало 8
[OK] тело НЕ изменилось за 60 секунд — sha256 2a9e04b47759a104 = 2a9e04b47759a104
```

Существующие заголовки узла в теле вставки **не цитируются** — сверка строка-в-строку до записи дала **0 совпадений**. Раздел встал **вторым блоком**, сразу после «СОСТОЯНИЕ НА 08.08.2026», перед автологоном 18.08 — тем же порядком «новое сверху», которым узел рос до сих пор, и это первое, что владелец увидит с телефона.

---

**Сводка.** Пакет перенесён целиком и дословно одной вставкой; узел вырос ровно на длину вставки. Раздел о слабых местах в пакете есть, он самый крупный (10 782 симв., 5 подразделов, ~62 неприятных числовых утверждения) и не приглажен: ноль маркеров смягчения против девяти «0 %». Оба вопроса партнёру — про мерку надёжности и про путь через модерацию — на месте дословно. Утечек не найдено ни одной из двенадцати проб, чистить было нечего. Кода не правил, гейт не гонял, процессы не трогал, коммита нет — работа была переносом в мозг.

RESULT: пакет для внешнего партнёра перенесён в узел мозга master отдельным разделом (102961 → 127779 симв., фраза-признак 1 вхождение); все замки зелёные — обратное чтение свежим процессом, обратимость символ в символ, повтор через 60 с; §3 «Что у нас плохо» не приглажен, два вопроса дословно на месте, утечек 0; строка DONE легла в cowork_log.
