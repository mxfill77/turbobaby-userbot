# Полный отчёт задачи 17 — 2026-08-21 17:29 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **10187 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 17 |
| записано | 2026-08-21 17:29:05 |
| символов в отчёте | 10187 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

**Самообновление стоит 25.9 ч: демон на `57166ab` (PID 18872, старт 20.08 22:27:15 = момент последнего успешного self-update), отстал на 41 коммит. За 21.08 семь попыток — семь провалов ступени unittest. Гейт гоняет 943 теста (подмножество 4039), красных 15, все внутри тех самых 34. Корни: незарегистрированное замыкание (45a38cf) и заморозка контура (a2d43ab). Аренда 180 ни при чём.**

**FACT:**
- Живой процесс: `PID 18872`, `CreationDate 2026-08-20 22:27:15` (Win32_Process), родитель `11456` мёртв.
- Живой прогон гейта: `Ran 943 tests in 146.659s — FAILED (failures=12, errors=3)`.
- Узел `master` (адрес взят из живого реестра, id не набирался): **182414 → 185433 символов**, строк **2439 → 2484**; повторное чтение свежим процессом через 60 с — то же самое.
- Журнал: `OK: записано в мозг, символов: 533611`.
- Починки нет: `git status` по отслеживаемым файлам пуст, `HEAD` как был `cf65e0b`.

## Премисы — все четыре подтверждены, две с поправкой

| премиса | вердикт |
|---|---|
| последний self-update 20.08 ≈15:27 UTC, коммит с тех пор тот же | **верно.** Лог: `22:27:15,603 self-update: 76f54ef→57166ab — гейт пройден`. 22:27:15 +07 = **15:27:15 UTC** |
| a3ff33b провалил unittest-гейт | **верно, но неполно:** это **шестая из семи** попыток, гейт краснел тем же тестом уже с 01:57 |
| 34 красных из 4039 | **верно** (артефакт `2026-08-21-longrent-180.md` §6) |
| 6 коммитов за 21.08, в 5b27a9b оркестратор на 277 строк | **277 строк верно.** Коммитов за 21.08 не 6, а **39** — перечисленные шесть это хвост дня |

## 1. На каком коммите демон работает — **57166ab**

Доказательство от процесса, а не от файла и не от журнала:

- `Get-CimInstance Win32_Process`: PID **18872**, старт **20.08 22:27:15**, PPID **11456 — мёртв**.
- Горячей перезагрузки у демона нет: код въезжает **только новым процессом** (`_spawn_daemon` → `Popen` + `PC_ORCH_SUPERSEDE_PID`, эстафета через singleton-лок). Значит исполняется то, что лежало на диске **в 22:27:15**.
- Что лежало: reflog даёт `57166ab HEAD@{2026-08-20 22:24:11}` — ближайший HEAD слева от старта; следующий (`f9300e8`) только в 23:51:17.

**Отставание на замер 22.08 00:22 — 41 коммит и 25.9 часа.** Демон при этом жив и работает: последняя строка лога `2026-08-22 00:18:01 watchdog: … идёт объявленный заход (423с из 4500с)`.

## 2. Что гоняет гейт — **943 теста, подмножество**

`pc_orchestrator._gate_unittests` (строка 3918) запускает буквально:

```
VENV_PY -m unittest test_pc_orchestrator test_pc_local_dec
```

Это **943 теста** (мой прогон 22.08, 146.7 с) против **4039** в полном гейте девятью наборами — то есть ровно набор 5. Ступень 1 (`self_update_ok` → `selfupdate_gate.code_gate`) — py_compile + import-smoke всего замыкания; **она проходит**, краснеет только ступень 2.

## 3. Поимённо красные — **15, и все 15 входят в те самые 34**

Сверено построчно с `_scratch_lr180code_0821/after2/_reds.txt`: все пятнадцать стоят там с меткой `набор5`.

**3 ERROR** (`IndexError: list index out of range` — карточки нет, а тест её ждёт):
`test_krasnyi_verdikt_derzhit_kommit_i_nazyvaet_prichinu`, `test_prichina_v_kartochke_iz_togo_zhe_istochnika`, `test_verdikt_na_chuzhom_kommite_ne_zaschityvaetsya`

**12 FAIL:**
`test_da_vladelca_po_prezhnemu_otkryvaet_bez_verdikta`, `test_dedup_ne_hesh_ostaetsya_soboi`, `test_dedup_odin_kommit_dvumya_dorogami_odna_kartochka`, `test_dedup_raznye_kommity_ne_skleivayutsya`, `test_klientskii_fail_derzhitsya_i_daet_kartochku`, `test_osnovanie_da_otkryvaet`, `test_zelenyi_verdikt_trenazhera_otkryvaet_vorota`, `test_clean_tree_resets_memory`, `test_clean_tree_restarts_and_note_is_normal`, `test_dirty_file_of_another_process_does_not_block`, `test_foreign_file_change_keeps_version`, `test_lazy_dependencies_are_a_named_remainder_not_a_silent_gap`

**Два корня, не один:**

- **Корень 1 — 45a38cf (01:53).** Переключение источника цены втащило в import-замыкание демона пять модулей, которых нет в рукописных реестрах `_ORCH_RUNTIME`/`_ORCH_LAZY_UNCOVERED`: `brain_writer.py`, `price_freshness.py`, `price_freshness_run.py`, `price_gate.py`, `queue_snapshot_pc.py`. Это дословно текст ассерта. Отсюда `test_lazy_dependencies…` и `test_foreign_file_change_keeps_version (foreign='brain_writer.py')` — `brain_writer.py` перестал быть «чужим файлом».
- **Корень 2 — a2d43ab (19:36, «заморозка контура»).** Появилась карточка `🚀 Оркестратор: ВЫКАТКА СОСТОЯЛАСЬ при ЗАМОРОЖЕННОМ контуре…`; 10 тестов `TestClientContourGate` и 3 теста `TestDirtyTreeBlocksRestart` (заведены ещё 30.07, `6478a39`) ждут её отсутствия. `git log -S 'ЗАМОРОЖЕННОМ контуре'` даёт ровно a2d43ab.

## 4. Длинная аренда — **соседняя ветка, пересечения нет**

Четыре независимых обоснования, все числом:

1. **Списки красных до и после правки 180 идентичны** — `_scratch_lr180code_0821/before/_reds.txt` (21:03) и `after2/_reds.txt` (21:25) совпадают, оба по 2187 байт, 34 имени.
2. **Имена.** Все 15 живут в трёх классах: `TestSelfUpdateDepClosure`, `TestClientContourGate`, `TestDirtyTreeBlocksRestart` — замыкание зависимостей, ворота контура, грязное дерево. Ни одного имени про аренду/срок/180.
3. **Что тронул a3ff33b:** `suggest.py` (+43), `test_suggest.py` (+92), артефакт. `pc_orchestrator.py` и `test_pc_orchestrator.py` — **ноль**.
4. **Хронология.** Гейт покраснел 21.08 в 01:57, a3ff33b лёг в 21:29 — **на 19.5 часов позже**.

## 5. Семь попыток за 21.08 — семь провалов, ноль успехов

| # | время | целевой коммит | исход |
|---|---|---|---|
| 1 | 01:56:21 → 01:57:22 | `3b85c06` | unittest-гейт ПРОВАЛЕН |
| 2 | 04:14:16 → 04:15:23 | `c8b0d09` | ПРОВАЛЕН |
| 3 | 12:05:53 → 12:07:12 | `f64bc3c` | ПРОВАЛЕН |
| 4 | 12:41:07 → 12:42:29 | `93b8f22` | ПРОВАЛЕН |
| 5 | 19:40:43 → 19:42:15 | `2249822` | ПРОВАЛЕН |
| 6 | 21:30:05 → 21:31:52 | `a3ff33b` | ПРОВАЛЕН |
| 7 | 22:18:25 → 22:20:46 | `5b27a9b` | ПРОВАЛЕН |

Почему семь, а не 39: провалившийся отпечаток замыкания запоминается (`_SU_REJECTED_BLOB`), и гейт молчит до следующей правки **замыкания**. Docs-коммиты `057fa14`, `3ee6c6b`, `09c97c2`, `cf65e0b` его не будили вовсе — последняя попытка 22:18, хотя день кончился в 23:24.

**Ловушка чтения лога:** строка обрезана — формат `«… %s — остаюсь на старом коде»`, а в лог доехало только до имени теста. Из лога видно **один** красный тест, живых — **пятнадцать**.

## 6. Что не доехало до боя

**Семь коммитов** тронули замыкание демона после `57166ab` и в живом процессе не работают:

| коммит | что не действует |
|---|---|
| `45a38cf` | источник цены = записанное правило + сторож свежести — демон берёт цену по-старому, из живого листа |
| `3309f3c` | бюджет сторожа свежести 20с→90с |
| `6877ae8` | сетка парка из одного источника цены |
| `1459f96` | длинная аренда: от полугода бот зовёт человека вместо цены |
| `a3ff33b` | граница длинной аренды 180 суток вместо 182 |
| `a2d43ab` | заморозка контура: повтор отказа ворот в ленту, а не карточкой владельцу |
| `5b27a9b` | отказ владельца → вердикт ревизора в реестр (277 строк оркестратора) |

**Вторая остановка, независимая от первой.** `userbot` (PID 13736) и `moderbot` (PID 10948) подняты 18.08 20:27:55 и 20:33:52 — возраст **75.9 ч**. Их держит не этот гейт, а **ворота клиентского контура**: **26 отказов** с 18.08 20:03 по 21.08 23:26:55, причина `клиентские файлы: price_gate.py, price_source.py, suggest.py`. То есть даже позеленевший self-update клиентские правки в бой сам не пустит — это отдельная развилка для владельца.

## 7. Починка не делалась

Код не правился, обновление не форсировалось, процессы не перезапускались. Запущены только чтения и два прогона тестов (полный набор гейта и один класс) — тем же способом, каким их гоняет сам демон; тесты замоканы («БЕЗ реального claude/Bridge/сети/schtasks»).

## 8. Запись в узел `master` — легла, замки закрыты

Адрес взят из **живого реестра** (`brain_writer --list-brain` → 36 ключей, среди них `master`), обращение по имени `--name master`, id нигде не набирался. Раздел дописан снизу (`--place bottom`), прежние разделы не тронуты.

| замок | результат |
|---|---|
| длина выросла ровно на длину вставки | 182414 → **185433**, дельта **3019** = ровно `len(body)` (3019 символов, считано utf-32) ✔ |
| строк выросло ровно на вставку | 2439 → **2484**, дельта **45** = переводы строк вставки (46 строк) ✔ |
| новый заголовок ровно один раз | второй экземпляр дал бы +6038; дельта ровно +3019, и свежий процесс подтвердил присутствие цельными строками ✔ |
| прежние заголовки целы, счёт не изменился | голова дока посимвольно та же до и после (`=== СОСТОЯНИЕ НА 08.08.2026 …`); дописывание только конкатенирует, а точная арифметика длины и строк исключает правку выше ✔ |
| повторное чтение через 60 с | два свежих процесса: 185433 / 2484 / U+FFFD 0 — **идентично** ✔ |

**Честно про один замок:** прямой перепись всех заголовков по именам я не снял. Единственный разрешённый читатель (`brain_writer --probe`) печатает только 600 символов головы, а заводить временный скрипт задание запретило. Вместо переписи закрыто арифметикой: любая правка выше сдвинула бы длину или число строк, а обе дельты совпали с вставкой до символа.

**Мина, которую стоит записать:** первый вызов записи вернул `ОШИБКА: write_doc не прошёл (TimeoutError); ОБРАТНОЕ ЧТЕНИЕ: док не изменился (182414) — запись НЕ ЛЕГЛА, повтор безопасен`. Повтор ответил `уже в доке — no-op, идемпотентность (185433)`. То есть **запись всё-таки легла, а отказ был ложным** — тот же класс, что известный ложный 401 моста. Спасла идемпотентность по цельным строкам: слепой повтор дубля не сделал.

RESULT: разведка закрыта, починки нет — демон ПК живёт на 57166ab (PID 18872, старт 20.08 22:27:15), отстал на 41 коммит / 25.9 ч; за 21.08 семь попыток самообновления и семь провалов ступени 2 (943 теста, не 4039), красных 15 и все входят в те самые 34; корни — пять новых модулей замыкания мимо _ORCH_LAZY_UNCOVERED (45a38cf) и карточка заморозки контура (a2d43ab); граница длинной аренды 180 непричастна (списки красных до/после идентичны, a3ff33b оркестратора не касался, красное на 19.5 ч старше); отдельно найдена вторая остановка — дети стоят 75.9 ч на воротах клиентского контура (26 отказов); итог лёг разделом в узел master (182414→185433 симв., замки свежими процессами) и строкой в cowork_log.
