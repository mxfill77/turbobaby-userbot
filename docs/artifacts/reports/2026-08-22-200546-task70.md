# Полный отчёт задачи 70 — 2026-08-22 20:05 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4806 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 70 |
| записано | 2026-08-22 20:05:46 |
| символов в отчёте | 4806 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Замер тренажёра стал воспроизводимым между сутками: седьмая дверь `trainer_pin.Clock` (КАЛЕНДАРЬ) — замер идёт по часам СНИМКА. Снимок 22.08 ожил без пересъёмки: 111 промахов из 112 → 0. Три прогона одной командой: 12 из 12 ×3 покейсно. Все три отрицательных теста пройдены. Второго ключа к воротам НЕТ.

**FACT:** коммиты `93e8497` (Clock), `9a9a46f` (артефакт), `6a81296` (тело журнала) в `git log`; tracked dirty = 0. Финальный прогон на HEAD `6a81296`: `12 из 12 × 3, ПОКЕЙСНО одинаково: ДА, промахов {мост 0, док 0, голова 0, дверь6 0}`. Раздел в живую карту: узел `master`, +7312 симв., статус ok, обратное чтение сверено.

---

## п.2 — разошедшийся состав красных: ОТДЕЛЬНАЯ болезнь, а не истечение снимка

Три числа, наблюдением:

1. **Истечение не умеет краснить вовсе.** Замер `drift` (вчерашний снимок, живые часы): `111 промахов моста + 1 головы, кейс 2 → НЕИЗВЕСТНО, красных ноль`. Промах выводит кейс из вердикта РАНЬШЕ, чем судят хоть один чек.
2. **Вчерашнее расхождение было при НУЛЕ промахов** (сырой `res_live.json`): `{'bridge': 0, 'read_doc': 0, 'head': 0}` на 385 вызовах моста, дверь 6 — 27 вызовов / 0 промахов, а числа 11 / 11 / 10 с ПУСТЫМ пересечением красных.
3. **Заморозь голову — расхождение исчезает**, больше не меняется ничего: 12/12 ×3 покейсно, и то же самое на +1/+7/+30/+365 суток.

→ Болезней две, независимых. Истечение = прибор СЛЕПНЕТ. Переезд красного = ЖИВАЯ ГОЛОВА.

## Три отрицательных теста

| тест | результат |
|---|---|
| **1. Подделать время (+1/+7/+30/+365 сут)** | **ПРОЙДЕН**: каждый раз 12 из 12, промахов 0, «совпало с сегодняшним ПОКЕЙСНО: **ДА**». Контроль (тот же сдвиг, лечение выключено): продукт видит 24.08/30.08, **111 промахов**, кейс 2 в «неизвестно», покейсно — **НЕТ**. Системные часы не переводились — подделка жила в процессе |
| **2. Испортить код бота** | **ПРОЙДЕН**: одна строка в `suggest.compose_quote_draft` (неверная цена клиенту) → **12/12 → 7/12, пять красных кейсов во ВСЕХ трёх прогонах, промахов 0**. Откат подтверждён чтением: `git status --porcelain -- suggest.py` → пусто, grep следа → 0; контрольный прогон вернул 12/12 ×3 |
| **3. Снимок недоступен** | **ПРОЙДЕН**: пустой снимок двери → **8 кейсов «неизвестно», красных 0**, пятно достаёт обслуженных кэшем; снимка нет вовсе → отказ прибора **ДО первого кейса** |

Тест 1 нашёл **мою же ошибку в приборе**: первая версия подделки времени наследовала `date`/`datetime` и ломала `isinstance` у `price_freshness._date` → ложные 8 «неизвестно». Диагноз снят пробой `probe_headkey.py` (печатает первое расхождение промпта дословно), заплатка — `__instancecheck__`.

## п.8 — ВЕРДИКТ

> **Настоящего второго ключа к воротам НЕТ. Появился настоящий ПРИБОР: воспроизводимый между сутками замер КОДА — но ворота ждут зелёного прогона ЖИВОГО продукта, а живое число по-прежнему не воспроизводится.**

Это «нет», а не «неизвестно»: (1) воспроизводимость куплена заморозкой ровно того, что ворота хотят видеть живым; (2) живое число 11/11/10, пересечение пусто; (3) замок цел — `trainer_run` пина не импортирует (0), `write_verdict` в пробе только в докстроке, `client_trainer_green.json` → `green` пуст, последняя запись **red**.

## Остальное коротко

- **п.1 (забор).** Срока как настройки нет. Сутки — из продукта: `suggest.py:5001` `ds = (base + timedelta(days=1))`, якорь «завтра» уходит в `params`, а `params` и есть ключ. **111 ключей из 125 (88.8%)** сдвигаются каждые сутки. Забор защищает различие ВОПРОСОВ: убери дату — снимок начнёт отвечать на вопрос, которого не слышал. Забор оставлен стоять.
- **п.3 (решение).** Чинится ДЕНЬ, а не КЛЮЧ: продукту возвращают день съёмки из **меты самого снимка**, и он своим же кодом строит записанные ключи. Заморожены две живые точки: `suggest.now_phuket` и `price_freshness.judge` (при `now=None` берёт `date.today()` мимо Пхукета). Не подделка: `Clock` в снимок **не пишет ни одной веткой** и `save` не имеет (юнит сверяет байт в байт), день выбирает СНИМОК, а не оператор, ошибка заморозки ГРОМКАЯ (промахи), перезапись снимка запрещена по-прежнему. Честная цена названа: календарных поломок (переход месяца/года, порог возраста слепка) замер не видит.
- **Юниты/гейт.** `test_trainer_pin` 12 → **23 зелёных**; соседи — **181 зелёный**; храповик неразбора красен на `main` ДО правки, **вклад захода 0 точек**.
- **Границы.** Ворота не открыты, дети не обновлены, клиентам не писалось, в секреты/конфиги не ходил, ничего не удалено. Временное — в **`_scratch_repro_0823/`**.

RESULT: замер тренажёра сделан воспроизводимым между сутками (trainer_pin.Clock, коммиты 93e8497/9a9a46f/6a81296): 12 из 12 в трёх прогонах покейсно и на +1/+7/+30/+365 суток при нуле промахов, порча кода даёт 7 из 12; вердикт — второго ключа к воротам НЕТ, есть воспроизводимый прибор замера кода; итог в артефакте, журнале и разделе узла master.
