# Полный отчёт задачи 13 — 2026-08-21 15:18 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5056 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 13 |
| записано | 2026-08-21 15:18:18 |
| символов в отчёте | 5056 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

**Провод построен и проверен: «нет» владельца по owner-карточке ревизора теперь САМО пишет вердикт «ложная» в реестр — та же находка второй раз до владельца не доходит. Коммит `5b27a9b`. Все три премисы подтверждены числами; шесть лежащих записей — ОДИН чек в шести окнах, а не шесть находок.**

**FACT:**
- `FACT: commit 5b27a9b в git log` («отказ владельца становится вердиктом ревизора сам»), дерево чистое по отслеживаемым.
- `FACT:` новые голдены **20/20 OK**; `test_pc_orchestrator` **896 тестов, 15 красных — тот же список, что на 3ee6c6b под git stash** (замер базы на том же дереве); `test_card_terminal_log`+`test_queue_snapshot_pc` **78 OK**.
- `FACT:` живой реестр НЕ тронут — mtime `pc_orchestrator.revizor_verdicts.json` как был **2026-08-10 00:32**, 5012 байт; боевой `revizor_cards.json` не создан (`No such file`).
- `FACT:` храповик неразбора **994 = базовая линия HEAD**, мой вклад 0 (первая редакция давала +10, убраны).
- Временное: **`_scratch_revizor_autolink_0821/`** + `%TEMP%\revizor_autolink_*`. Ничего не удалялось.

**1. Премисы (проверено первым действием).** Артефакта `2026-08-21-auditor-verdict-autolink.md` **не было** (`No such file`); рядом лежала только разведка `2026-08-21-auditor-false-repeat.md`. Реестр читается каждым прогоном (`:8566` + два `_revizor_apply_verdicts`) — **подтверждено**. Запись — **только руками**: `revizor_set_verdict(` в демоне ровно **2** строки (определение + вызов в разборе `sys.argv`). В реестре **6** записей, сумма `hits` = **0**, `last_hit` не-None — **[]**, mtime **10.08 00:32** — **подтверждено дословно**.

**2. Где закрывалось «нет».** Отказ ставит Splinter (`process_approval_timeouts`: «reject Филиппа Splinter ставит сам»), ряд уходит в `failed` с `_REJECT_PREFIX` (`:4383`, тот же префикс у `answer_card:3566`). Читателей у него трое — `NO_HEAL_PREFIXES`, `card_terminal_log`, `queue_snapshot_pc`; **ревизора среди них не было**. Карточку ответили → она вышла из `needs_approval` → `_revizor_find_owner_card` вернул `None` → `prior_lines` пустые → находка написалась заново.

**3. Провод.** Расписка `pc_orchestrator.revizor_cards.json` `{карточка → [{ключ, существо}]}` пишется **в момент доставки** (`_revizor_card_keys_add` из обеих веток `_revizor_post_owner_card`), а не разбором текста карточки: имени чека в строке нет, улика режется 200/4500 → отпечаток существа вышел бы другим и вердикт не применился бы никогда. Жатва `_revizor_harvest_card_answers` стои́т **перед** чтением реестра. Предикат отказа — готовый `card_terminal_log.classify` (его `REJECT_MARK` сверен с `_REJECT_PREFIX` голденом), три исхода: отказ → вердикты; принято/TTL/закрыто → расписку снять без вердикта; ряда нет → эпизод открыт. В записи: `source="отказ владельца"`, `at`, ключ, существо, слова владельца в `why`. **Ключ устойчив по устройству** — `класс|окно|предмет`, слов времени в теле 0.

**4. Отрицательный тест (живой формат, случай 20–21.08 дословно).** До отказа: доставлено 1, заглушено 0. «Нет» по карточке 58 → `{'rejected': 1, 'verdicts': 1}`. Через сутки та же находка: **доставлено 0, заглушено 1, карточки 4 не будет, hits=1**. Глушение НЕ стало глушением всего: другой чек того же окна → 1/0; тот же ключ с другим существом («успевайте») → 1/0; тот же чек в другом окне → 1/0; `hits` после трёх проходов остался **1**. Не считается отказом: «да» → 0 вердиктов, TTL → 0, висящая карточка → 0 и расписка открыта. То же сквозь живой `_revizor_route` — класс `TestRevizorRejectEndToEnd`.

**5. Шесть лежащих записей — не трогать.** Измерено: класс один (`#92`), предмет один («депозит без противоречий»), окон **шесть разных**, различных существ **три**; все внесены рукой 09.08 за 3.5 часа. Это шесть окон ОДНОГО ложного класса (лестница депозитов прайса). Сработать не могут — окна мертвы, а окно входит в ключ; отличать их от новых теперь есть чем (нет поля `source`). Лечение класса — порог пользы (`20` против 6 разобранных) или корень, обе работы отдельные.

**6. Гейт — только задетый набор**, полный не гонялся. Один красный **был мой и исправлен**: мок доставки в `TestRevizorIpcChecks` копировал старую подпись без `now` — приведён к живому формату, класс 10/10.

**Границы вслух:** карточка сводная, кнопка одна — «нет» гасит ВСЕ её строки (каждый ключ уходит в лог поимённо); «да» вердикта не пишет; жатва ленива — прогон без находок в очередь не ходит вовсе. Клиентский контур не тронут ни строкой: корень (широкий токен `разбира\w+` в `suggest._AV_SCARCITY_RE`) только назван в остатках.

**Журнальную строку `cowork_log` НЕ писал** — задача прямо запрещает записи в мозг. Если нужна, владелец делает сам:
`python cowork_log_append.py "ARTIFACT отказ ревизора → вердикт 21.08 → docs/artifacts/2026-08-21-auditor-verdict-autolink.md: провод есть, реплей живого случая гасит повтор"`

RESULT: провод «отказ владельца → вердикт в реестре ревизора» построен, проверен реплеем живого случая 20–21.08 и голденами (20/20 OK, вклад в красноту 0, в храповик 0), коммит 5b27a9b; артефакт docs/artifacts/2026-08-21-auditor-verdict-autolink.md; боевые файлы и мозг не тронуты.
