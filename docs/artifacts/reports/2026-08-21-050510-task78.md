# Полный отчёт задачи 78 — 2026-08-21 05:05 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5273 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 78 |
| записано | 2026-08-21 05:05:10 |
| символов в отчёте | 5273 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Сетка парка переведена на записанное правило: один источник цены на оба пути. Три отрицательных теста устояли, новых красных ноль, живой повтор случаев 4 и 7 дал другие числа (пик NMAX 337→424 ฿/сут). Артефакт `docs/artifacts/2026-08-21-sheet-source-switch.md`, коммиты запушены. ВАЖНО: живой сторож свежести сегодня отказывает — в проде бот молчит по цене на обоих путях.

**FACT:** commit `6877ae8` (код) и `79b0125` (артефакт) и `f64bc3c` (тело журнала) в `git log`, все запушены в `origin/main` (`e081ba4..6877ae8`, далее). Живой прогон: `_scratch_sheetrule_0821/case_04.json` — NMAX 155 сутки **424 ฿** против **337 ฿** прогона `ab16dcd`.

---

**Первое действие (доложено):** дерево `ab16dcd` (2026-08-21 10:47:20 +0700), время 11:31:17.
**Признак сделанности:** `docs/artifacts/2026-08-21-sheet-source-switch.md` — отсутствовал, работа сделана с первого захода.

## Забор Честертона (одной строкой)

Сетка отвечает на каталог-вопрос, и врезка 20.08 сознательно оставила её на живом листе, назвав причину дословно — «у неё нет дат клиента (якорь — завтра)». **Посылка верна ровно наполовину:** дат нет у 37 % черновиков (172 из 470), значит у 63 % они ЕСТЬ и у сетки те же самые (`hints["iso_start"]`) — случай 4 ровно такой, якорь `2026-12-20`, чистый пик. Опирались на прежний путь ещё четверо, проверено поимённо: занятость, депозит и имя юнита остаются словом живой двери (правило их не трогает); подбор по классу и подвыборки берут те же `rows` и едут следом; `model_claim_mismatch` судит имена, а не числа. Потребовала замка одна вещь — служебная пометка сезона модератору (см. ниже).

## Три отрицательных теста — дословно

```
(а) поштучно  NMAX 155 2026-09-05..2026-09-12 : 298 ฿/сут, 2086 ฿ всего
    в списке  NMAX 155 колонка «неделя»: 298 ฿/сут, 2086 ฿ всего
    живой лист говорил бы: 2730 ฿ всего          ВЕРДИКТ: СОВПАЛИ до бата
(б) NMAX 155   сентябрь 289 / 2086 / 4740   январь 424 / 3059 / 6960   лист 390 / 2730 / 9000
    ADV 350    сентябрь 598 / 4312 / 9780   январь 877 / 6328 / 14370  лист 749 / 4928 / 14606
    CB 300R    сентябрь 618 / 4459 / 10140  январь 800 / 5768 / 13110  лист 757 / 4716 / 12491
                                             ВЕРДИКТ: ЯНВАРЬ ВЫШЕ по всем колонкам
(в) строк сетки при отказе: 0 (отсеяно: 0)
    блок в промпт: 'ПРАЙС ПО ПАРКУ: точные цены из Календаря сейчас недоступны…'
                                             ВЕРДИКТ: МОЛЧИТ ВЕСЬ СПИСОК
```

## Проверка живым путём — случаи 4 и 7 рядом со старыми

| случай 4 (пик 20.12) | 21.08 лист `ab16dcd` | 21.08 правило `6877ae8` |
|---|---|---|
| NMAX 155 | 337 / 2217 / от 5000 | **424 / 3059 / 6960** (+25.8 %) |
| XMAX 300 | 593 / 3902 / от 8900 | **887 / 6398 / 14520** (+49.6 %) |
| XADV 750 | 2091 / 13759 / от 33900 | **2495 / 18004 / 40890** |
| NINJA 400 | 1007 / 6274 / от 11900 | **1437 / 10374 / 23550** |
| FORZA 300 | 518 / 3408 / от 7900 | **из сетки ушла** (`norule`) |

**Со старым прогоном не совпало ни одно число из 31** — правка сработала. Случай 7 (якорь завтра, дно сезона) пошёл вниз: NMAX 337 → 289, ADV 749 → 598 — это слово правила (корзина 30+ несёт ×0.53), ровно то же, по которому с 20.08 считается точечная котировка.

Прочерки в трёх ячейках — **не правка**: код правила роняет модель целиком, а не колонку; живая дверь через минуту ответила по всем трём (`available=True total=4716/9519/1528`). Это дедлайн сборки — 114.1 и 120.0 с при `_SHEET_DEADLINE`=120 (утром те же сборки шли 78.7 и 91.9).

## Тесты

`TURBOBABY_TEST_LOGS=1 PRICE_GATE_TTL_MIN=0 … -m unittest`: набор пути цены — 176 тестов, **красных 4** (ровно базовые `TestPriceRulesV2`), 5.1 с; набор suggest — **569 → 580** тестов, **красных 10** (ровно базовые, все из 16 отложенных голденов гейта). **Новых красных ноль, +11 зелёных.**

## Названо честно

1. **Живой сторож сегодня отказывает** — 106.5 с против бюджета 90, не снимаются ручки I3 и J3. Поэтому набор без ручки давал 20 красных вместо 4, и поэтому живой повтор шёл с объявленной ручкой отката врезки `PRICE_GATE_TTL_MIN=0` — ровно условия прогона `ab16dcd`. **Следствие для прода: бот сейчас молчит по цене на ОБОИХ путях.** Сторожа задание переделывать запрещало.
2. Голдены не правились ни одним байтом: инъекция живой двери оставляет путь прежним, ветку правила в тестах включает отдельная инъекция `_gate`.
3. Правил и сезонов не менял (`price_source.json` не тронут). Один замок добавлен: пометка модератору «низкий сезон» снимается, когда правило считает пик — иначе противоречие создала бы сама эта правка.
4. Откат ручкой без коммита: `PRICE_SHEET_RULE_OFF=1`.

Остатки: полный гейт наборами и выкатка — отдельно; поколения XMAX слились в цене (известная развилка владельца); месячная колонка на дне сезона просела (NMAX «от 5000» → 4740) — число стоит увидеть до выкатки.

RESULT: сетка парка переведена на записанное правило (коммиты 6877ae8, 79b0125, f64bc3c запушены); три отрицательных теста устояли, новых красных ноль (наборы 1 и 2: 4 и 10 — ровно базовые); живой повтор случаев 4 и 7 дал другие числа — на пике NMAX 337→424 ฿/сут, ни одно из 31 числа со старым прогоном не совпало; артефакт docs/artifacts/2026-08-21-sheet-source-switch.md, журнал ARTIFACT+DONE записан; отдельно доложено, что живой сторож свежести сегодня отказывает (106.5 с против бюджета 90, ручки I3/J3).
