# Полный отчёт задачи 41 — 2026-08-26 06:55 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **6442 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 41 |
| записано | 2026-08-26 06:55:47 |
| символов в отчёте | 6442 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Модербот 26.08 **не падал** — он дважды стартовал и **выходил сам** по своему синглтон-гарду: `moderation_bot.lock` держит номер 18200 с 23.08, а после ручного ребута 11:47:04 этот номер достался `PinWin.exe`. `_pid_alive` проверяет только «номер занят», без имени и времени старта. Вотчдог при этом не соврал ни разу. Разбор записан артефактом, коммит b6df27f.

**FACT:** commit `b6df27f` в `git log` (артефакт `docs/artifacts/2026-08-27-moderbot-three-failed-starts.md` + тело журнальной записи). Три строки в журнал ПК — rc=0 каждая. Живая проба: `moderation_bot.py` в `Win32_Process` **отсутствует**; PID 18200 существует и это `PinWin.exe` (старт 26.08 11:48:14, PPID 3624); `LastBootUpTime` = 2026-08-26 11:47:04.

---

## ШАГ 4 — ответ (первым, как просили)

**Версия (а) — процесс стартует и немедленно выходит — ДОКАЗАНА. Причина в логе дословно:**

```
2026-08-26 11:53:54,696 | moderation_bot уже запущен (PID 18200) — второй не поднимаю, выхожу.
2026-08-26 12:27:33,032 | moderation_bot уже запущен (PID 18200) — второй не поднимаю, выхожу.
```

Стухший лок + переиспользованный Windows'ом номер:
- `od -c moderation_bot.lock` → ровно 5 байт `18200`, mtime **23.08 18:29:02.030612200** (миллисекунда в миллисекунду со строкой «забираю» в логе);
- настоящий модербот PID 18200 прожил 23.08 18:29 → 26.08 11:46:54 и погиб **вместе с машиной** (ручной ребут: Id=1074 `StartMenuExperienceHost.exe` в 11:46:47, Id=13 в 11:46:54, Id=12 в 11:47:05). `release_lock()` при выключении не отрабатывает — файл пережил ребут;
- через 70 с после загрузки номер 18200 занял `PinWin.exe`;
- `moderation_bot.py:583` — `_pid_alive` спрашивает у `tasklist` **только «есть ли процесс с таким номером»**. Ни образа, ни командной строки, ни возраста.

**Версия (б) — не стартует вовсе — ОТКАЗ.** Ребёнок дошёл до `acquire_lock()` (строка 640), то есть прошёл все импорты (`suggest`, `moderation_core`, `trainer`…), проверку токена и `suggest.is_enabled()`. Плюс `Application Error` (Id=1000) по `python.exe` за окно — **ноль** (Id=1001 в 11:47/11:50 — повторные WER-отправки старых дампов, не наши процессы).

Предсмертный взгляд задания **не оправдался**: лог не пуст и не провернулся.

## ШАГ 2 — хронология

| время (лок.) | что сделал вотчдог | чем судил успех | что увидел |
|---|---|---|---|
| **11:54:04** (04:54 UTC) | `ModerbotProcess.start()` → `Popen(venv\python, moderation_bot.py)`, ребёнок родился 11:53:54 | `ok=True` (команда прошла) **и** `_verify_raised`: sleep 8 с → CIM-поиск | `PID после=[]`, «лежал 6 м 48 с [перезагрузка]», смерть 1/3 → карточка в инбокс |
| **12:27:43** (05:27 UTC) | то же, ребёнок 12:27:32 | то же | `PID после=[]`, «лежал **неизвестно** (в дыре наблюдений)», смерть 2/3 → карточка в инбокс |
| **12:45:57** (05:45 UTC) | **подъёма НЕ БЫЛО** — `halt_now` при `deaths=3` | — | ERROR «умер 3 раз подряд — СТОП попыток» → карточка в инбокс |

Разрыв 33 м между 1 и 2: кулдаун истёк в 12:09, но контур-вотчдог тикает внутри `poll_once`, а демон с 12:11 по 12:26 исполнял синхронный заход.

**Вотчдог проверяет РЕЗУЛЬТАТ, а не факт запуска команды.** В строке стои́т и то и другое, намеренно раздельно: `ok=True` — команда, `PID после=[]` — результат. Результат решает громкость (`raise_is_loud`) и поднял все три карточки в 1160. Он даже назвал адрес ответа — «*проверь moderation_bot.log (возможно, сразу вышел)*». Не читает он только **содержимое** лога ребёнка.

## Остальные шаги — коротко

- **ШАГ 0.** Артефакта по адресу не было. Разбор 18.08 одной строкой: тогда доказано, что модербот не умирал сам — обе смерти нанесла перезагрузка Windows, канал `Traceback` пуст, а числа вотчдога «лежал 12 ч 10 м / 5 ч 54 м» ложны (мерили mtime разреженного лога; второе завышено ×53).
- **ШАГ 1.** Не запущен. 6 живых python — `pc_agent` 8476, `rc_supervisor` 13968, `userbot_listen` 11988, `pc_orchestrator` 13496 + 2 его ребёнка. Модербота среди них нет.
- **ШАГ 3.** Трассировки нет (выход штатный, `return`). **Код выхода — НЕИЗВЕСТНО:** `pc_agent` делает `Popen` и не ждёт, `returncode` не читает никто и в лог он не ложится ни на одной дороге.
- **ШАГ 5.** Дерево **сошлось**: `git status` по клиентским файлам пуст, `git diff 2d66cf0..HEAD` = только два файла в `docs/`. Наполовину применённого нет. Ворота 25.08/26.08 останавливали перезапуск **детей**, а не правку файлов, — и модербот после них спокойно жил ещё 8 часов. Побочно: **ребут проходит мимо ворот целиком** — userbot встал в 11:47:32 на `suggest.py`, гейт которого в 03:38 был красным на 7 падениях.
- **ШАГ 6.** «Дыру» на смерти 2/3 создала **собственная строка отказа**: она сдвинула mtime лога на 11:53:54.7 — позже загрузки, — и `absence_since` потеряла ветку «перезагрузка» (`now−seen = 2016 с > окна 600`). Контрфакт: без неё было бы точное **40 м 27 с**. Прибору не хватает одного — свидетель жизни у него **файл, а не процесс**: посылка в докстринге («каждый ребёнок первым делом пишет ЗАПУСК») ложна для ветки отказа, где `log.warning` стои́т на 609, а `ЗАПУСК` — на 653. Замок против этого класса **уже написан в этом репозитории**: О3 сверяет второе условие (`MOD_LOCK_SKEW_SEC = 600 с`) и потому чужую свежесть за работу не засчитал — `"moderation_bot=работы нет"` в живом `tmp/expect_pc/state.json`.
- **ШАГ 7.** Шесть опровержений (PinWin по PID и по командной строке, непрерывность занятия номера, миллисекундная привязка лока, порядок трёх писателей, контрфакт «дыры», отсутствие `fresh_log`/`grace`/слепого CIM). **Не опровергнут один риск, называю честно:** ветка после `acquire_lock()` (`init_db`, `run_polling`) на текущем дереве не исполнялась с 25.08 — вторая причина за локом остаётся возможной, а доказать её можно только запуском, который запрещён.
- **ШАГ 8.** Не чинил, выкатку не предлагал.

**Журнал:** `ARTIFACT` + `DONE` + короткий `NOTE`-индекс, rc=0 каждая. `DONE` вышел 612 симв. > порога 600 → авто-вынос в `docs/artifacts/journal/2026-08-26-065300-done.md`; ссылку на разбор продублировал `NOTE`, чтобы она осталась в голове записи. Черновик: `tmp/pc_report/task41-13496-1787726408859-41.md`. Временных файлов вне `D:\turbobaby-bot\tmp\pc_report\` не заводил, ничего не удалял.

RESULT: разобрал три подъёма 26.08 — процесс стартует и выходит сам по стухшему локу 18200 (номер занял PinWin.exe после ребута 11:47:04); вотчдог судит результат и не соврал, третий заход подъёмом не был (halt при deaths=3); артефакт docs/artifacts/2026-08-27-moderbot-three-failed-starts.md, коммит b6df27f, три строки в журнал.
