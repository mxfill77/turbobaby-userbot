# Полный отчёт задачи 71 — 2026-08-18 13:53 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **6296 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 71 |
| записано | 2026-08-18 13:53:37 |
| символов в отчёте | 6296 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Автологон **СРАБОТАЛ**: старт системы 20:27:34.852 → начало сеанса 20:27:39.856 = **5.004 с**; жеста ПИН в момент входа нет (ближайший на +119.9 с — это разблокировка). Экран запирает СВОЯ задача `tb_lock_on_logon` через 5 с — вот почему «признак стёрся». Лок модербота сейчас живой (10948); мёртвым был **15760**.

**FACT: read-only.** Ни одной правки: реестр, Планировщик, процессы, база не тронуты; единственный созданный файл — артефакт `docs/artifacts/2026-08-18-autologon-verify.md`. Живые доказательства: `Kernel-General ID=12` **20:27:34.852**; `Winlogon 7001` **20:27:39.848**; `UserProfileService ID=1` **20:27:39.856**; `HelloForBusiness 5002/5001` только **20:29:39.800/.996**; `\pc_agent` LogonTrigger `LastRun 20:27:40`; `moderation_bot.log:7114` → «устаревший moderation_bot.lock (PID **15760** мёртв) — забираю».

---

**1. Переключатель — ВКЛЮЧЁН.** `AutoAdminLogon=[1]`, `DefaultUserName=[mxfill1]`, `DefaultDomainName=[MXFILLPC]`, `AutoLogonSID=[S-1-5-21-…-1002]`. `AutoLogonCount` **отсутствует** — значит вход не одноразовый, повторится на каждой загрузке.

**2. Пароль в хранилище — лежит (косвенно).** `DefaultPassword` в реестре: **False** (открытым текстом нет — правильно). Прямая проба хранилища невозможна дословно: `HKLM:\SECURITY\Policy\Secrets` → `System.Security.SecurityException :: Requested registry access is not allowed` (ветка открыта только SYSTEM; повышение не помогло бы — нужна смена владельца, это правка). Доказательство функциональное: вход состоялся за 5.0 с **без жеста Hello**, а в реестре пароля нет ⇒ винлогон взял его из LSA. Значение не читалось и нигде не печатается.

**3. Времена и разница.** Старт системы **2026-08-18 20:27:34.852** («The operating system started at system time 2026-08-18T13:27:34.5Z»), начало сеанса **20:27:39.856** («Recieved user logon notification on session 1»), **разница 5.004 с**. Третий свидетель: `6013 The system uptime is 3 seconds` в 20:27:37.673. **Контроль того же дня:** утренняя *обновленческая* перезагрузка (MoUsoCoreWorker/TrustedInstaller от SYSTEM) — старт 04:02:10.853 → сеанс 14:08:50.122 = **36 399 с**, и там жест ПИН есть. Разрыв в 7273 раза.

**4. Дети относительно старта системы.** `pc_agent` **+6.05 с**, `rc_supervisor` **+13.13 с**, `userbot_listen` **+20.69 с**, RC-консоль +23.18 с, `pc_orchestrator` **+328.37 с**, `moderation_bot` +377.73 с. Первые три — задачи с **LogonTrigger**, они физически не срабатывают без входа, и отработали **за 119 с ДО** единственной предъявленной учётки. Оркестратор поздний не по отказу: он не логон-триггерный, его поднял пятиминутный вотчдог («продукта нет … и процессов демона НЕТ — поднимаю через schtasks», 20:33:03).

**5. Ручной ввод за окно — в момент входа НЕТ.** Пары Hello `5002+5001` стоя́т ровно там, где человек трогал машину: 02:28, **14:08:49.993 (ручной вход)**, 20:17:55, **20:29:39.800/.996**. В 20:27:39 пары нет — только запуск служб Passport, TPM и загрузка контейнера. Ближайший жест на **+119.94 с** после уже существовавшего сеанса = разблокировка. После 20:29:41 жестов нет вовсе: станция снова заперта с 20:30:35 (`LogonUI PID=2996`), и контур всё это время работает на запертой машине. `Security` (4624/4800/4801) без повышения не читается — назвал честно, на вывод не влияет.

**6. ВЫВОД — СРАБОТАЛ.** **7.** Причина отказа не требуется.

**Честный остаток:** ту же картину даёт штатная ARSO (авто-возобновление после пользовательской перезагрузки). Отвергнута тремя замерами: (а) утренняя *обновленческая* перезагрузка — главный сценарий ARSO — автовхода не дала; (б) между 04:02 и 20:27 изменилось ровно одно — вооружён автологон; (в) следов ARSO в реестре нет (`AutoLogonChecked`, `ARSOUserConsent`, `UserARSOExclusionList`, политика — все ABSENT). Абсолютного свидетеля (поле «Logon Process» в 4624) не добыл. Практическое следствие: **холодный пуск не проверен** — там ARSO не помогла бы, а `AutoAdminLogon` обязан сработать.

**Находка про инструкцию:** howto §«ЧТО ПРОВЕРИТЬ» велит смотреть `pc_orchestrator.log` раньше момента разблокировки. Владелец разблокировал на 2-й минуте, а оркестратор пришёл в 20:33:03 — по этому признаку получилось бы **ложное «не сработал»**. Признак работает только при полном 7-минутном ожидании, запас ~1.5 мин. Надёжный признак — логон-триггерные дети. Кода и документа не правил.

**Отдельно, лок модербота.** Сейчас аномалии нет: в файле **10948** (живой, старт 20:33:52.581, родитель — оркестратор 15988), mtime **20:33:53.0586**. Аномалия жила в окне **20:27:34 → 20:33:53**: номер в файле был **15760** (экземпляр от 14:38:56, убит перезагрузкой), время записи файла — **14:38:55**. Причина не сбой, а устройство лока: файл снимает только `release_lock()` при нормальном выходе, перезагрузка идёт мимо этой ветки; у оркестратора то же самое («устаревший лок (PID 7336 мёртв)»). **В канал в тот момент писал `userbot_listen.py` PID 13736** (жив с 20:27:55) и работающий в нём `suggest.py` — файл `moderation_ipc.db` общий, и соединение закрывается на каждый вызов, поэтому даже чтения чекпойнтят WAL и двигают mtime. Ложного зелёного контур **не выдал**: `moderbot_writer()` вернул False, а `state.json` на 20:27:40 несёт `moderation_bot=неизвестно`, а не «жив». Ничего не поднимал и не гасил.

**Сводка.** (1) Автологон вооружён и сработал: 5.004 с против 36 399 с у ручного входа того же дня. (2) ПИН владельца был на 2 минуты позже старта сеанса — это разблокировка экрана, который контур запирает сам по замыслу. (3) Логон-триггерные дети встали за 2 минуты до любой учётки — сеанс создал не человек. (4) Оркестратор пришёл на 5.5 мин позже не из-за автологона, а из-за фазы пятиминутного вотчдога; из-за этого признак успеха в howto хрупок. (5) Лок модербота держал мёртвый 15760 до 20:33:53, канал в это время двигал userbot; сейчас лок живой.

Строку `cowork_log` не писал: задача явно предписывает артефакт **последним** действием — журнальная запись за владельцем/оркестратором.

RESULT: read-only разбор завершён — автологон СРАБОТАЛ (старт системы 20:27:34.852 → сеанс 20:27:39.856, Δ=5.004 с, жеста ПИН при входе нет, логон-триггерные дети встали за 119 с до единственной учётки); лок moderation_bot сейчас живой (10948), мёртвым был 15760 при канале, который двигал userbot 13736; артефакт docs/artifacts/2026-08-18-autologon-verify.md записан, ничего не менялось.
