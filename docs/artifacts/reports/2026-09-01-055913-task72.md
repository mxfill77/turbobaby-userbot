# Полный отчёт задачи 72 — 2026-09-01 05:59 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5842 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 72 |
| записано | 2026-09-01 05:59:13 |
| символов в отчёте | 5842 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Разведка класса «тест меряет живое дерево вместо своего» по обеим полосам закрыта. Корень класса — не в тестах, а в проде: 6 констант с литералом корня. Оба образца из задания подтверждены и оказались симметричны (у каждой полосы есть и три теста надзора, и хардкод в гард-тесте). Правок кода ноль. Артефакт записан и запушен.

**FACT:**
- commit `54cb9d1` («Перепись класса абсолютного корня в тестах на обеих полосах») и `3c8df49` (тело журнальной записи) в `git log`, запушены: `77f5ac5..3c8df49 main -> main`, `git status -sb` → `## main...origin/main` (ahead 0).
- Артефакт: `docs/artifacts/2026-09-01-abs-root-class-in-tests.md`
- Журнал: две строки легли («OK: записано в мозг, символов: 682526» и «683063 | тело вынесено в docs/artifacts/journal/2026-09-01-055801-done.md»).
- Правок кода/тестов: **ноль** (`git show --stat 54cb9d1` — один новый .md).

---

## 1. Корень класса: хардкод в ПРОДЕ, а не в тестах

Главный вывод, который переворачивает постановку. Литерал корня живёт в продовых модулях, тесты наследуют его через `import` — поэтому починка одних тестов косметична.

| полоса | файл:строка | константа |
|---|---|---|
| ПК | `pc_orchestrator.py:98` | `REPO = r"D:\turbobaby-bot"` |
| ПК | `pretool_guard.py:281` | `PROJECT = r"D:\turbobaby-bot"` |
| VPS | `orchestrator_daemon.py:33` | `REPO = "/root/turbobaby-manager-bot"` |
| VPS | `orchestrator_daemon.py:34` | `BRIDGE_GS = "/root/turbobaby-bridge-gs"` |
| VPS | `pretool_guard.py:78` | `PROJECT = "/root/turbobaby-manager-bot"` |
| VPS | `guard_replay.py:36` | `PROJECT = "/root/turbobaby-manager-bot"` |

Самая дорогая строка — `orchestrator_daemon.py:38`: `sys.path.insert(0, REPO)` рядом с `load_dotenv`. Простой `import orchestrator_daemon` **из копии** переводит `sys.path` на живое дерево — безупречно написанный тест всё равно получает живой модуль.

## 2. Оба образца подтверждены — и класс оказался симметричным

- **Три ПК-теста надзора за зависимостями демона** (проверено чтением): `test_pc_orchestrator.py:822`, `:10703`, `:10733` — все строят путь как `os.path.join(o.REPO, …)`. Плюс ещё 6 методов в том же классе слепы косвенно: `_dep_files()` → `client_contour.closure(REPO,…)` с модульным `REPO`, а `setUp:10518` делает `os.listdir(o.REPO)` — засеян живым каталогом весь класс.
- **Хардкод в гард-тесте на сервере**: `tests/test_guard_cli_args.py:27`, `ROOT` уходит `cwd`-аргументом в `PG.classify` в 10 методах из 19.

**Но зеркала оказались на обеих полосах:** у VPS тоже ровно три сканера исходника демона (`test_claim_event.py:242`, `test_guard_marker_ttl.py:216`, `test_settings_allowlist.py:42→243`), у ПК тоже хардкод в гарде (`pretool_guard.py:281`, плюс собственный литерал теста `test_pretool_guard.py:39`). Называть класс по одному образцу на полосу — недооценивать его вдвое.

Числа VPS посчитал сам: **149** файлов `test_*.py`, **125** кладут абсолютный корень в `sys.path[0]` (119 прямым литералом + 6 через абсолютную `ROOT`); ни один `conftest.py` `sys.path` не трогает, конкурирующей вставки нет.

Три находки хуже чтения — тесты **пишут в живое дерево**: `test_pc_orchestrator.py:304`, `test_pretool_guard.py:3861` (ПК), `tests/test_guard_cli_args.py:166` (VPS, `tempfile … dir=ROOT` прямо в корень репо). И `test_pretool_guard.py:372` перезапускает **живую пару модулей** с `assertEqual(returncode, 0)` — вердикт копии становится функцией зелени живого дерева.

## 3. Ложно-зелёный на копии ветки (§4 артефакта, 7 видов)

- **B1** (и код, и данные живые — `test_whole_closure_compiles_right_now` и 6 соседей): зелёный **всегда**; тест структурно неспособен покраснеть от правки копии.
- **B2** (голден из копии, факт из живого — тесты `:822`/`:10703`): молчит ровно на **добавлении** незарегистрированной зависимости, то есть на собственном предмете; правка списка в копии даёт ложно-красный.
- **C** (абсолютный `cwd` подпроцесса): ребёнок импортирует живые модули — в исходнике родителя не видно.
- **D** (`cwd` как аргумент классификатора), **E** (живой прогон как утверждение), **F** (живого корня нет → красный по среде либо вакуумно-зелёный под `if os.path.exists`), **G** (запись в живое дерево — не про зелень, а про загрязнение).

## 4. Правило (текстом, не применено)

> **Корень дерева — это ОТВЕТ НА ВОПРОС «ГДЕ Я», а не константа.** Спрашивать его обязан каждый файл у своего `__file__`; передавать дальше — только параметром.

Три хода в обязательном порядке: **сначала прод** (литерал → `os.path.dirname(os.path.abspath(__file__))`; исключение — ссылка на *чужое* дерево, `_MANAGER_CLONE`/`BRIDGE_GS`, она остаётся литералом и должна быть названа), **потом тест** (путь от своего `__file__`; шаблоны уже в дереве — `test_log_setup.py:504`, `test_result_judge_pc.py:615`, `test_probe_isolation.py:45`; продовую константу — `mock.patch.object`, а не наследовать; `cwd` подпроцесса — тоже корень), **потом замок** (инвариант, а не дисциплина).

Операционный критерий, ловящий то, чего греп не видит: **«какая правка В КОПИИ красит этот тест?»** Не можешь назвать — тест меряет не своё дерево.

## 5. Честные пределы

- Полоса VPS измерена по **снимку** `tmp/srv`, HEAD `7298787` от **01.08.2026** — месяц отставания. Живая полоса могла и вырасти, и починиться.
- `D:\foreign\turbobaby-manager-bot` **не читался**: вне проекта, разрешение не выдано, обхода не искал.
- Прогонов не было — вид B2 выведен разбором кода, не доказан замером.
- Длинные таблицы §2.3 и §3.3 — перепись подагентами, сверена мной точечно (перечень сверок в §6 артефакта).

RESULT: разведка класса абсолютного корня закрыта по обеим полосам без единой правки кода; корень класса найден в проде (6 констант), оба образца задания подтверждены и оказались зеркальными, ложно-зелёный разобран по 7 видам, правило сформулировано; артефакт docs/artifacts/2026-09-01-abs-root-class-in-tests.md, коммиты 54cb9d1 + 3c8df49 запушены, две строки в журнале.
