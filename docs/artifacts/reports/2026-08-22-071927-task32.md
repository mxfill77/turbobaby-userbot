# Полный отчёт задачи 32 — 2026-08-22 07:19 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5867 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 32 |
| записано | 2026-08-22 07:19:27 |
| символов в отчёте | 5867 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Разделил два дефекта: 6 из 6 провалов тренажёра ловят ГОТОВЫЙ ТЕКСТ КОДА «свободен на эти даты», 0 — «разбираемся». Прочтение «бот обещает наличие вслепую» опровергнуто кодом: печать под `if q.get("available")`, а чек зовётся с литералом `avail=None`. Итог записан в узел master, 8 замков зелёные дважды. Правок продукта — ноль.

**FACT:**
- узел master: **264165 → 274060** символов (прирост 9895 = тело 9893 + 2 разделителя), sha `c5e94ecafb181643b1a9c1e0a205192ca5c440ac0172f5e93364a45cfea82929`
- 8 замков свежим процессом зелёные **дважды**: `t0` и `t60` (повтор через 66 с) — **одинаковый sha**
- продукт не тронут: `git diff --name-only HEAD` пуст; коммит только тела журнальной записи — `8eca684` в `git log`
- журнал: `OK: записано в мозг, символов: 545495`

---

## П.3 — КЛЮЧЕВОЙ ОТВЕТ И ЧИСЛА

| куча | что ловит | провалов |
|---|---|---|
| **А — готовый текст КОДА** (`avail_pos`, «свободен на эти даты») | **6 из 6** — кейсы 1, 3, 7, 8, 9, 12·прогон 2 |
| **Б — речь вроде «разбираемся»** (`scarcity`) | **0 из 6** |

Клеймы по видам за все 14 прогонов корпуса: `{avail_pos: 14, scarcity: 0, avail_neg: 0, special: 0}`. **В тренажёре дефект Б не сработал ни разу.** Он живой, но живёт у ревизора (`pc_orchestrator.py:8889` гоняет ТОТ ЖЕ чек-лист по ЖИВЫМ текстам окна). Объяснять разницу с июлем словом «разбираемся» — ложный след.

## Премисы — проверены первым действием

| премиса | вердикт |
|---|---|
| шесть провалов из семи — один чек | **верна** |
| фразу пишет код, а не данные | **поправлена**: пишет КОД, но **под условием на данные** |
| чек зовётся без данных о наличии | **верна** — `suggest.py:7374`, литерал `avail=None` |
| тот же чек краснеет на «разбираемся» | **верна, но не здесь** — `разбира\w+` в `_AV_SCARCITY_RE` (`suggest.py:6832`), краснеет у ревизора |

**Арифметика:** повторный прогон кейсов не понадобился — ответ снят с уже лежащих на диске результатов 22.08. Цена захода — чтение кода и один разбор.

## П.1 — два места, дословно

```
suggest.py:3458      if q.get("available"):
suggest.py:3459          parts.append("свободен на эти даты")     # ветка КАПА
suggest.py:3477      if q.get("available"):
suggest.py:3478          parts.append("свободен на эти даты")     # ветка СБОРКИ
```

Прямой ответ: **текст готовый, печать условная.** Модель его не сочиняет — дописывает код на место метки `[QUOTE]`. Но без `q["available"]` строки не будет; третья ветка (дословная J-цена, `q["text"]`) её не несёт вовсе. Выглядит безусловной потому, что `_client_price` зовётся только из `_resolve_model_price` на ветке `ok`, а `ok` выдаётся **только** по юниту с истинным `available` (`pricing.py:278`).

## П.2 — путь наличия снизу вверх

мост (пересечение периода со вкладкой «клиенты») → `pricing.py:68` перенос поля → `pricing.py:273-281` первый свободный юнит → `ok` → `suggest.py:3532-3537` → `_client_price` → печать.

**До печати доходит. Обрыв — на пути к ПРОВЕРЯЛЬЩИКУ:** у `_smoke_checks(draft, exp)` параметра наличия нет вовсе, а `exp` (`j_line`, `delivery_line`, `sheet_line`, `zone`, `zone_price`, `full_data`) поля наличия не несёт. Данные не теряются — **их некуда положить**.

## П.4 — забор

Чек — пятый в чек-листе #92 (`c895751`, 15.07), мерка родилась у гарда наличия (`9c45837`, тот же день) с посылкой из комментария `suggest.py:6809-6818`: «Данные о наличии — только из Bridge». Забор ставился вокруг **выдумки МОДЕЛИ** — тот же класс, что `AVAILABILITY_INVARIANT_RULE` промпта.

**Случай не тот, и продукт это знает сам:** в живом пути `postcheck_draft` зовётся **ДО** приклейки кодового блока (`suggest.py:7051`, `:7112`), комментарий дословно — «дословный прайс-блок КОДА не трогаем». Чек делает обратное.

## П.5 — два прочтения

| прочтение | вердикт |
|---|---|
| **чек зовётся не оттуда** | **ПОДТВЕРЖДЕНО** |
| **бот обещает наличие, не имея данных** | **ОПРОВЕРГНУТО** |

Доказательство кодом: (1) печать под условием; (2) `ok` только по свободному юниту, иначе `none_available` с запретом называть числа (`suggest.py:3538-3540`); (3) живая проба 22.08 дала `available: true` на всех трёх котировках; (4) **контроль:** `availability_violations("свободен на эти даты", avail={"available": True})` → `[]`. Красит не текст — красит `None`.

## П.6 — вслепую

**Ноль из двенадцати.** С данными фраза ушла бы клиенту на **6 кейсах** (14 строк): кейс 1 — девять строк «XMAX 300 New Gen — 642 ฿/день; итого 3210 ฿; депозит: паспорт; свободен на эти даты» и далее ADV 350, XSR 155, CBR 650R, CB 650R, CB 300R, MT-03, NINJA 400, VULCAN 650S; кейсы 3, 8, 9, 12·r2 — «NMAX — 307 ฿/день; итого 1535 ฿; депозит 3000 ฿; свободен на эти даты»; кейс 7 — то же на 158 ฿/день, итого 4740 ฿.

Развилка владельца: фраза говорит о **модели**, а данные — о **конкретном юните**. Правда, но более узкая, чем звучит.

## П.7 — что предлагаю (НЕ выполнено)

- **Дефект А:** провести `available` в эталон (`_smoke_expectations` / `trainer_run._expectation`) и звать `availability_violations(client, avail=exp.get("avail"))` вместо литерала. **В ответе клиенту не изменится НИЧЕГО** — шесть кейсов зеленеют, ревизор перестаёт находить собственную строку продукта; нет данных → `None`, fail-closed цел.
- **Дефект Б:** сузить `разбира\w+` тем же приёмом, каким сузили соседний `успева\w+`. Пропадает класс ложных находок ревизора; ответ бота не меняется — `guard_availability` **в проде не зовётся ни разу**.

Порогов ни одна не ослабляет: дефект А — это «дать чеку те же данные, что были у писавшего», а не «отключить чек».

**Временное:** `_scratch_availsplit_0822/` (не убиралось, по заданию). Живой лист парка не читался.

RESULT: разделил дефекты — 6 из 6 провалов на готовом тексте кода «свободен», 0 на «разбираемся»; прочтение «бот обещает вслепую» опровергнуто кодом (0 из 12), подтверждено «чек зовётся с avail=None»; раздел записан в узел master (264165→274060, 8 замков зелёные на t0 и t60), продукт не правился.
