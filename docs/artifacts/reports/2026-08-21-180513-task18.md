# Полный отчёт задачи 18 — 2026-08-21 18:05 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **6978 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 18 |
| записано | 2026-08-21 18:05:13 |
| символов в отчёте | 6978 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Ступень самообновления расчищена: 943 теста/15 красных → 946/0, три прогона подряд одной командой дали одно число. Оба корня подтверждены живым прогоном и починены по существу; ни один assert не снят, порог не ослаблен, skip не добавлен. Раздел лёг в узел master, 7 замков зелёные. Исход самой эстафеты — честное НЕИЗВЕСТНО: она структурно не наблюдаема из-под своей же задачи.

**FACT:**
- `FACT: commit 66a422f в git log` (правка), `d5f2fd7` (артефакт), `b1c8106` (тело журнала) — все три в `origin/main`, дерево чистое
- `FACT: ступень 946/946 OK ×3` — 155.5с / 160.6с / 166.1с, одна команда, число одинаковое во всех трёх
- `FACT: mtime боевого pc_orchestrator.gate_seen.json до и после трёх прогонов — 00:36:05.222202500 → 00:36:05.222202500` (не дрогнул)
- `FACT: узел master 185433 → 190571 символов, заголовков 15 → 16, sha256 6977274d…4026 дважды с интервалом 60с`
- `FACT: демон PID 18872, старт 20.08 22:27:15, коммит 57166ab` — на момент отчёта эстафеты НЕТ: `grep -c "self-update: 57166ab" pc_orchestrator.log` → **0**

## 1. Премисы — подтверждены живым прогоном

943 теста, **15 красных** (12 failures + 3 errors) — дословно. «Граница длинной аренды» среди них не встречается ни разу. Пятнадцать имён:

`TestClientContourGate`: `test_krasnyi_verdikt_derzhit_kommit_i_nazyvaet_prichinu`, `test_prichina_v_kartochke_iz_togo_zhe_istochnika`, `test_verdikt_na_chuzhom_kommite_ne_zaschityvaetsya` (три ERROR), `test_da_vladelca_po_prezhnemu_otkryvaet_bez_verdikta`, `test_dedup_ne_hesh_ostaetsya_soboi`, `test_dedup_odin_kommit_dvumya_dorogami_odna_kartochka`, `test_dedup_raznye_kommity_ne_skleivayutsya`, `test_klientskii_fail_derzhitsya_i_daet_kartochku`, `test_osnovanie_da_otkryvaet`, `test_zelenyi_verdikt_trenazhera_otkryvaet_vorota`; `TestDirtyTreeBlocksRestart`: `test_clean_tree_resets_memory`, `test_clean_tree_restarts_and_note_is_normal`, `test_dirty_file_of_another_process_does_not_block`; `TestSelfUpdateDepClosure`: `test_foreign_file_change_keeps_version`, `test_lazy_dependencies_are_a_named_remainder_not_a_silent_gap`.

## 2. Забор Честертона по каждому корню

**Корень 1 — реестр `_ORCH_LAZY_UNCOVERED`.** Это **не ворота**: он ничего не запрещает. Это *именованный остаток* — перечень зависимостей демона, которые ворота грязного дерева сознательно не покрывают, потому что WIP в `suggest.py` глушил бы доставку кода вовсе. Сторож ловит ровно одно: чтобы дыра была **названа**, а не молчалива. **Он сработал верно** — держал 7 попыток с 21.08 01:56. Снятие красного защиту не ослабляет: `45a38cf` внёс в `suggest.py` одну строку `import price_gate`, за ней транзитивно приехал куст (`price_gate` → `price_freshness`, `price_freshness_run`, `queue_snapshot_pc` → `brain_writer`), замыкание 24 → 29 файлов. Дыра приехала 21.08 вместе с коммитом; запись о ней её не рождает. Накрывать куст воротами нельзя — вход в него сам `suggest.py`, защита листа при открытом корне была бы видимостью.

**Корень 2 — заморозка контура `a2d43ab`.** Ворота клиентского контура fail-closed и держат в обоих исходах; заморозка решает только **адрес** сообщения. Трогать её = ослабить решение владельца, принятое накануне — **не тронута ни строкой**.

## 3. Что именно неверно во втором корне: ТЕСТ, а не код

Ручка заморозки — **файл** `pc_orchestrator.contour_frozen` в корне репо, живое состояние мира, читаемое каждым тиком. Владелец включил её 21.08 19:36 — и 15 голденов покраснели, **не изменившись ни строкой**. Между «повтор отказа уходит в ленту» и «демон не получает код» связи нет никакой. Голден, чей вердикт зависит от того, заморожен ли контур в эту минуту, голденом не является. Изоляция в репозитории **была** — но стояла в дочернем классе `TestZamorozkaKontura`, а не в `Base`. Поднята в `Base`, к шести таким же боевым state-файлам.

**Течь была в обе стороны, и вторая — боевая.** `gate_route` *пишет* реестр форм, и прогон тестов дописывал **боевой** экземпляр: замер 22.08 00:36 показал n=160 и n=60 по формам с `suggest.py` — при 14 живых отказах за сутки и при демоне на коде **старше самой ручки**, физически неспособном туда писать. Счётчики нарастили прогоны тестов; цена боевая — записанная форма гасит владельцу первую настоящую карточку. Замок от возврата класса: `TestZhivayaRuchkaMiraNevidnaGoldenam` (+3 теста).

## 4. Пункт 5 — НЕИЗВЕСТНО, и не по нехватке терпения

`maybe_self_update()` стоит в цикле (`pc_orchestrator.py:9136`) **после** `poll_once()` (`:9129`), а `poll_once → process_new` (`:3758`) исполняет задачу **синхронно**; потоков у демона нет. Задача, которая это чинит, сама и есть тот заход — id=18, CLAIM 22.08 00:32:44. Увидеть эстафету из-под неё нельзя **в принципе**. Все остальные ветки проверены и зелёные: рубильник снят, отпечаток замыкания разошёлся с запущенным (`1dcf66ce…` ≠ `c4ce9cba…`), ворота грязного дерева пропускают (`[]`), `code_gate` → `ok=True`, тест-гейт 946/0 ×3.
Проверять так: `grep "self-update: 57166ab" pc_orchestrator.log` — ждём «гейт пройден».

## 5. Пункт 6 — связка НЕ работает пока, счётчик не вырос

Живой реестр: 6 записей, у **всех** `hits=0`, файл не менялся с 10.08 00:32. Расписки `pc_orchestrator.revizor_cards.json`, которую провод `5b27a9b` пишет в момент доставки карточки, **не существует вовсе** — провод не отработал ни разу, потому что живёт в коде, до демона не доехавшем. Рост счётчика требует ещё и живого «нет» владельца на карточку ревизора: подделывать решение человека ради зелёного числа нельзя.

## 6. Пункт 7 — узел master

Адрес разрешён по имени через живой реестр моста (35 ключей, `master` — точный ключ; ловушка рядом: `map` резолвится в `executors_map`). Раздел «## Разблокировка самообновления 21.08…» дописан в хвост, прежние разделы не тронуты. Замки свежим процессом против бэкапа на диске: длина `190571−185433=5138` = ровно длина вставки (5137+1 разделитель); новый заголовок ровно 1 раз; прежние 15 заголовков целы и в прежнем порядке; счёт 15→16; прежний текст цел как префикс; повторное чтение через 60с — тот же sha256.

Отдельная новость: две первые попытки записи упали `TimeoutError` при неизменившемся доке (узел 185 тыс. символов, дефолт 30с), третья увидела текст уже в доке и вернула `noop` — одна из «упавших» записей легла **позже ответа**. Дубля не возникло только благодаря идемпотентности по цельным строкам.

**Остатки (не сделано, названо честно):** боты остаются на старом коде и после обновления демона — их держит вторая, независимая остановка, ворота клиентского контура (26 отказов с 18.08); позеленевшая ступень их не открывает ни одной веткой. Временное этой задачи — `_scratch_selfupd_0822/` (пробы, верификатор); ничего не удалялось.

RESULT: ступень самообновления разблокирована по существу — 943/15 → 946/0 при трёх одинаковых прогонах подряд, коммиты 66a422f + d5f2fd7 + b1c8106 в origin/main, раздел о разблокировке лёг в узел master (7 замков зелёные, 185433→190571); исход самой эстафеты и связка отказа владельца с реестром вердиктов — НЕИЗВЕСТНО и НЕ РАБОТАЕТ ПОКА соответственно, оба с названным механизмом.
