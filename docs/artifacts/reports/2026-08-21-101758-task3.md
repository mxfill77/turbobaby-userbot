# Полный отчёт задачи 3 — 2026-08-21 10:17 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5080 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 3 |
| записано | 2026-08-21 10:17:58 |
| символов в отчёте | 5080 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Тесты пути цены отвязаны от живого моста. Дверь оказалась не та, что названа в задании: не `suggest._bridge_read_doc`, а сторож свежести — `suggest.py:3368` зовёт `price_gate.allow()` без инъекции, та одалживает клиент демона и делает 9 живых GET (40–43 с, потолок 180 с). Подменена ровно дверь, не вердикт. Три прогона: 0/0/0. Прод не тронут.

**FACT:**
- `FACT: commit 5d41d0f в git log` — правка (test_isolation.py §5 + 4 строки импорта в ценовых модулях)
- `FACT: commit a6f7533 в git log` — артефакт `docs/artifacts/2026-08-21-tests-offline.md`
- `FACT: origin/main = a6f7533` — запушено, `git status --porcelain -uno` пуст
- `FACT: три прогона 176 тестов → OK/OK/OK, 7.786с / 14.805с / 5.489с`
- `FACT: сеть отрезана (socket.create_connection → OSError) → Ran 176 tests, OK, 6.231s`
- `FACT: журнал — две строки легли, обратное чтение подтвердило (523524 симв.)`

---

## 1. Первое действие: дерево и время

```
803911d 2026-08-21 14:26
2026-08-21 16:49:23
```

## 2. Признак сделанности

`ls docs/artifacts/2026-08-21-tests-offline.md` → `No such file or directory`. Задача ранее не делалась, начал с нуля.

## 3. Базовый замер (до правки, `803911d`)

```
$ time venv/Scripts/python.exe -m unittest test_pricing test_price_source test_price_gate test_price_freshness
Ran 176 tests in 62.613s
OK
real	1m3.251s
```

## 4. ГЛАВНАЯ ПРОВЕРКА — три прогона одной командой, без правок между

```
=== ПРОГОН 1 ===   Ran 176 tests in  7.786s   OK   wall=8s
=== ПРОГОН 2 ===   Ran 176 tests in 14.805s   OK   wall=15s
=== ПРОГОН 3 ===   Ran 176 tests in  5.489s   OK   wall=6s
```

**Три числа красных: 0 · 0 · 0. Совпали — цель достигнута.**
**Время: 62.6 с → 5.5–14.8 с.**

## 5. Три отрицательных теста — все устояли

**(а) мост недоступен вовсе** (`socket.create_connection` бросает OSError):
```
Ran 176 tests in 6.231s
OK
ОТРЕЗАНО: failures=0 errors=0 ran=176
```
0 при отрезанной сети = 0 при живом мосте. **УСТОЯЛ.**

**(б) счёт сломан искусственно** (+100 ฿/сут базе каждой модели, в памяти, файл не тронут):
```
ПОРЧА: сдвинуто моделей=11 (+100 ฿/сут)
СЛОМАННЫЙ СЧЁТ: failures=18 errors=0 ran=93
```
18 красных против 0. Зеркалом тесты не стали. **УСТОЯЛ.**

**(в) предмет = отказ моста:** 12 тестов (`test_dead_door_forbids_the_price_too`, `test_silence_is_never_turned_into_permission`, `TestHands.*`, `TestFleetStatusSplitsEmptyFromFailure`) — `Ran 12 tests in 0.003s / OK`. Заглушка их не касается: они подают свой `get=`, а `verdict` при инъекции идёт мимо `_bridge_caller` и мимо кэша. **УСТОЯЛ.**

## 6. Как сделано — заглушка не возвращает успех молча

Взят образец репо (`test_price_gate.py:158`, проба владельца 20.08 «подменялась ровно одна вещь — сама дверь источника»). Сторож работает целиком — читает настоящий `price_source.json`, обходит те же 9 проб, судит `price_freshness.judge`. Подменён один шаг, которого нет без сети: **ответ листа**, в живом формате потребителя (`answer["season"]["global_discount"]`, `ok is True`).

Правило не прочитано → дверь **бросает** → `НЕИЗВЕСТНО` → цена гаснет. Слепок старше порога → `УСТАРЕЛО` → цена гаснет. Незнакомый адрес/юнит → AssertionError-тривайр. Откат `PRICE_GATE_TTL_MIN=0` **отвергнут сознательно**: он отдаёт `(True, None)` не спрашивая сторожа — это и есть «молча успех».

## 7. Забор Честертона

Ходили **не по замыслу, а по недосмотру**: сторож врезан в путь ответа без инъекционной точки, а голдены зовут путь ответа. Теряем **побочную живую канарейку** — полный прогон раз в прогон сверял живые H3/I3/J3 со слепком. Закрывается: (1) названным прогоном `venv\Scripts\python.exe price_freshness_run.py` — те же 9 проб живой дверью; (2) самим боем — сторож судит живой лист раз в 30 мин и гасит цену клиенту быстрее любого прогона. Предложение владельцу: повесить `price_freshness_run.py` на задачу Планировщика — не сделал, `schtasks` красная.

## 8. Осталось

1. **`test_suggest.py` / `test_moderation.py` не перемерены** — обвязку получают автоматически (импорт первой строкой), но прогон в бюджет не вошёл. Основная масса «45 плавающих» там. Первым делом следующим заходом.
2. Полный `discover` не гонялся — запрещён бюджетом.
3. Вторая дверь того же класса — `park_allowlist_status()` (`suggest.py:7818`), два теста лечат её вручную.
4. У точечного пути в проде нет `_gate=` (у сеточного есть) — правка прода запрещена заданием.

**Оговорка о честности чисел:** 16/62/61 сняты задачей 81 на полном `discover` и зависят от скорости моста — сегодня мост быстрый, те же четыре модуля дали 0 красных и до правки. Поэтому доказательство отвязки лежит не в «61 → 0», а в отрицательном тесте (а): отрезанная сеть даёт то же число.

RESULT: тесты пути цены отвязаны от живого моста — коммиты 5d41d0f (офлайн-дверь сторожа свежести в test_isolation §5 + подключение 4 ценовых модулей) и a6f7533 (артефакт), запушены, origin/main=a6f7533; три прогона подряд дали 0/0/0 красных при 62.6с→5.5–14.8с, все три отрицательных теста устояли (сеть отрезана → те же 0; порча счёта → 18 красных; 12 тестов отказа моста зелены); прод не тронут, тестов не отключено; остаток — test_suggest/test_moderation не перемерены.
