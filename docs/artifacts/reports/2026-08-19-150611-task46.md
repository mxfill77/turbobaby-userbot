# Полный отчёт задачи 46 — 2026-08-19 15:06 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5115 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 46 |
| записано | 2026-08-19 15:06:11 |
| символов в отчёте | 5115 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Хвосты хранилища уроков закрыты и закоммичены: 3 коммита (dd281e9 код+тесты+.gitignore, 2ed2243 артефакт, 54150b8 тело журнала). Тесты синхронно в переднем плане: **40 тестов, OK, 1.907 с**. Ёмкость — 50 000 уроков, на превышении только замедление вслух, потери нет. Таблица `lesson_store.tsv` в git не добавлена — внесена в `.gitignore`.

**FACT:**
- `FACT: commit dd281e9 в git log` — `хранилище уроков тренажёра: отдельная таблица, 8 колонок, ёмкость 50 000 уроков` (+1244 строки: `lesson_store.py`, `test_lesson_store.py`, `.gitignore`)
- `FACT: commit 2ed2243` — артефакт `docs/artifacts/2026-08-19-lesson-store.md`; `FACT: commit 54150b8` — тело журнальной записи
- `FACT: venv/Scripts/python.exe -m unittest test_lesson_store → Ran 40 tests in 1.907s, OK` (синхронно, передний план, без фона)
- `FACT: venv/Scripts/python.exe lesson_store.py --status → «ёмкость: уроков 0 из 50000 порога комфорта; байт 0», файл «ещё не заведён»`
- `FACT: git status --porcelain lesson_store.tsv → пусто` (файла нет, в индекс не попал; `.gitignore` держит три имени)
- `FACT: журнал — «OK: записано в мозг, символов: 479518 | тело вынесено в docs/artifacts/journal/2026-08-19-150508-done.md»`

## Что было и что сделано

Разведка (`…-lesson-store-state.md` §7) назвала четыре хвоста. Проверка по факту показала: два из них (тест перекрытия приветствия и строка в `.gitignore`) уже лежали в рабочем дереве несохранёнными — писать заново не потребовалось, и я их не переписывал, а **проверил прогоном и закоммитил**. Оставшиеся закрыл сам:

| хвост | было | стало |
|---|---|---|
| `test_greeting_position_overmasks_by_design` | обещан докстрингом, в файле отсутствовал | в дереве есть, **зелёный** (проверен живым детектором, не голденом «как удобно») |
| `docs/artifacts/2026-08-19-lesson-store.md` | ссылались `lesson_store.py:81` (§6), `:524` (§5), `test_lesson_store.py:15` (§3) — файла не было | **заведён**, разделы пронумерованы под эти ссылки |
| `.gitignore` | строки не было → `test_store_file_is_outside_git` красный | три имени (`.tsv`, `.bak`, `.tmp`), тест зелёный |
| финальная правка не проверена прогоном | байткод был старше исходника | **проверена**, 40/40 |

Тесты не ослаблял: ни одного `assert` не снял и не смягчил, число тестов выросло с 39 до 40. Код не переписывал — единственная правка в `lesson_store.py` **комментарий**, и она вынужденная (ниже).

## Ёмкость числом и поведение на превышении

**`COMFORT_LESSONS = 50 000` уроков ≈ 50 МБ файла.** Порог не назначен, а взят из чужого бюджета: самая частая операция — запись урока, ближайший живой бюджет — тик модербота 5.000 с (ожидание О3). Живой замер этого захода на строках реального размера (630 симв.):

| уроков | файл | `load()` | `add()` | `withdraw(день)` | строк до/после |
|---|---|---|---|---|---|
| 1 000 | 1.0 МБ | 0.014 с | 0.011 с | 0.025 с | 1002 / 1002 |
| 50 000 | 50.2 МБ | 0.370 с | 0.422 с (**8.4 %** тика) | 0.831 с | 50002 / 50002 |
| 100 000 | 100.5 МБ | 0.717 с | 0.869 с (17.4 %) | 1.639 с | 100002 / 100002 |
| 200 000 | 201.1 МБ | 1.555 с | 1.722 с | 5.845 с | 200002 / 200002 |

**На превышении — только замедление, и оно называется вслух.** Живая проба на 50 001 уроке: состояние `тесно`, фраза «Ничего не потеряно и не будет: записи не стираются ни одной веткой», следующая запись **принята**, стало 50 002. Отказа по переполнению, ротации и обрезания нет ни одной веткой; число физических строк до и после снятия совпадает на всех пяти размерах.

**Расхождение, о котором говорю, а не прячу.** Комментарий модуля утверждал «рост линейный на всём диапазоне» и нёс числа упавшего захода, вывод которого обрыв унёс. Мой замер их подтвердил везде, **кроме одной точки**: `withdraw` на 200 000 стоит 5.845 с вместо ожидаемых по линии ~3.3 — снятие единственное копирует файл целиком (`.bak`), и на 200 МБ упирается в диск. Числа в комментарии приведены к своему замеру, нелинейность названа. Логика не тронута, `COMFORT_LESSONS` не менялся.

## Проверки, которые могли бы прогореть молча

- **Храповик неразбора не ухудшен моим коммитом:** до — 994 точки / 61 файл / 2 падения, после — 994 точки / 62 файла / **те же 2 падения** (они красны на `main` до любой моей правки). Вклад `lesson_store.py` измерен отдельно `scan_file` **до** коммита: **0 точек**. Доля слепых читателей: 38.7 % → 37.9 %.
- **Граница шага цела:** бот к хранилищу не подключён ни одним импортом (`TestBotNotWired`), клиентский контур, `userbot` и `moderation_bot` не тронуты.
- Замер шёл во временном каталоге с автоуборкой — в репозитории проб не осталось, мусора на диске тоже (соседний `bench.py` упавшего захода оставлял ~660 МБ).

**Не сделано намеренно:** `git push` не делал — задача просила коммит (ветка `main` ahead 6). Файл таблицы не заводил: по устройству он рождается лениво первым `add()`.

RESULT: хвосты хранилища уроков закрыты и закоммичены (dd281e9, 2ed2243, 54150b8); 40 тестов OK за 1.907 с синхронно; ёмкость 50 000 уроков ≈ 50 МБ (add 0.422 с = 8.4 % тика), на превышении — «тесно» + продолжение записи без потери строк; lesson_store.tsv внесён в .gitignore и в git не добавлен; храповик не ухудшен (994 точки до и после).
