# Полный отчёт задачи 64 — 2026-08-18 13:13 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **9126 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 64 |
| записано | 2026-08-18 13:13:36 |
| символов в отчёте | 9126 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Инструкция автологона поднята в узел карты (ключ `master`, адрес взят из живого реестра моста): раздел из 25 строк — 10 шагов, откат, проверка после перезагрузки. Все замки зелёные; единственный красный оказался дефектом моей мерки (строка против подстроки), доказано пересчётом обеими мерками. Автологон НЕ вооружён, реестр и Планировщик не тронуты.

**FACT:** свежее чтение узла ОТДЕЛЬНЫМ процессом (`verify_master.py`): `ref=name:master`, длина `97486 → 102961` символов, прирост `+5475` = длине фактической вставки; фраза «инструкция автологона» — **1 вхождение** (до записи было 0); новый заголовок ровно 1 раз; снятие вставки возвращает прежнее тело символ в символ; удалено 0 символов; все 25 прежних заголовков целы, число вхождений каждого не изменилось (обе мерки). Расписка моста (`ok`) доказательством не считалась — судило обратное чтение.

---

## 1. Признак сделанности и основание (первым действием)

Артефакт на диске **есть** — писать «НЕИЗВЕСТНО» не пришлось:

```
$ ls -la /d/turbobaby-bot/docs/artifacts/2026-08-18-autologon-howto.md
-rw-r--r-- 1 mxfill1 197121 28150 Aug 18 19:00 /d/turbobaby-bot/docs/artifacts/2026-08-18-autologon-howto.md
---exit=0---
```

Признак сделанности в узле **отсутствовал** — работа не была сделана раньше:

```
$ venv/Scripts/python.exe _scratch_autologon_0818/probe_master.py
АДРЕС ИЗ ЖИВОГО РЕЕСТРА: ref=name:master note=[точный ключ реестра]
ДЛИНА УЗЛА: 97486 символов, строк 1065

=== ПРИЗНАК СДЕЛАННОСТИ ===
дословно «инструкция автологона»: 0 вхождений
без учёта регистра: 0 вхождений
  подстрока автологон          : 1
  подстрока autologon          : 1
  подстрока AutoAdminLogon     : 0
  подстрока autologon_arm      : 1
  подстрока tb_lock_on_logon   : 0
```

Единственное упоминание автологона в узле было строкой ожидания («ЖДЁТ ВЛАДЕЛЬЦА У МАШИНЫ — пароль для автологона: скрипт `autologon_arm.ps1`…») — то есть **напоминание без инструкции**. Именно поэтому раздел встал сразу под ним.

## 2. Запись

```
$ venv/Scripts/python.exe _scratch_autologon_0818/write_master.py
АДРЕС ИЗ ЖИВОГО РЕЕСТРА: ref=name:master note=[точный ключ реестра]
ДЛИНА ДО: 97486 символов, строк 1065, заголовков (уникальных) 25
ДЛИНА ВСТАВКИ (тело): 5472 символов, строк 25
ЯКОРЬ найден: 1 раз (нужно ровно 1)
СТРОК ВСТАВКИ, ЦИТИРУЮЩИХ ПРЕЖНИЕ ЗАГОЛОВКИ: 0
ЗАДУМАНО: длина 102961, прирост +5475

СТАТУС: ok | расписка моста: ok  (расписка доказательством НЕ считается)
БЭКАП: D:\turbobaby-bot\tmp\brain_backup_master_autologon_howto_0818_20260818-201039.txt
ДЛИНА ПОСЛЕ (обратное чтение канала): 102961
```

## 3. Сверка свежим процессом

```
$ venv/Scripts/python.exe _scratch_autologon_0818/verify_master.py
СВЕЖЕЕ ЧТЕНИЕ: ref=name:master, длина 102961 (до записи было 97486, задумано 102961)

=== ЗАМКИ ===
  [OK] совпадает с задуманным СИМВОЛ В СИМВОЛ

=== АРИФМЕТИКА И ОБРАТИМОСТЬ ===
  фактическая вставка: 5475 символов (тело 5472 + разделители 3)
  [OK] прирост длины равен длине вставки  — прирост +5475, вставка 5475
  [OK] удалено НОЛЬ символов (чистая вставка)  — удалено 0: ''
  [OK] снятие вставки возвращает прежнее тело СИМВОЛ В СИМВОЛ
  [OK] тело вставки лежит в узле дословно
  [OK] мусора кодировки (U+FFFD) нет

=== ЗАГОЛОВКИ ===
  [OK] новый заголовок ровно 1 раз  — вхождений 1
  [OK] фраза-признак «инструкция автологона» на месте  — вхождений 1
  [ПРОВАЛ] число вхождений КАЖДОГО прежнего заголовка не изменилось (25 шт.)  — ОТКРЫТО:: 1→3
  [OK] все прежние заголовки целы  — пропали: []

=== ДЕСЯТЬ ШАГОВ И РАЗДЕЛЫ В ЖИВОМ УЗЛЕ ===
  [OK] все 10 шагов отдельными строками  — нет: []
  [OK] порядок шагов 1..10 не нарушен
  [OK] упоминание 'ОТКАТ ОДНОЙ СТРОКОЙ'  — вхождений 1
  [OK] упоминание 'ПРОВЕРКА ОДНОЙ СТРОКОЙ'  — вхождений 1
  [OK] упоминание 'autologon_arm.ps1'  — вхождений 6
  [OK] упоминание 'tb_lock_on_logon'  — вхождений 2
  [OK] упоминание 'pc_orchestrator.log'  — вхождений 4
  [OK] упоминание 'pc_orchestrator_watchdog'  — вхождений 1
  [OK] упоминание 'lsastoreprivatedata'  — вхождений 1
  [OK] упоминание '-Disable'  — вхождений 2
  [OK] упоминание 'уточнить'  — вхождений 2

ИТОГ: 20 замков из 21 зелёные
---exit=1---
```

### Разбор красного — он оказался дефектом МОЕЙ мерки, а не дока

Замок снимал счёт заголовков **до** записи построчно (строка целиком = заголовок), а **после** — подстрокой. Это разные мерки, и «ОТКРЫТО: 1→3» родилось из самого сравнения. Пересчёт обеими мерками на обоих телах:

```
$ venv/Scripts/python.exe _scratch_autologon_0818/recheck_headers.py
ПРЕЖНИХ ЗАГОЛОВКОВ ПРОВЕРЕНО: 25
ЛИНЕЙНЫЙ счёт (строка целиком) изменился у: 0
ПОДСТРОЧНЫЙ счёт изменился у: 0

РАЗБОР КРАСНОГО «ОТКРЫТО:» (тот, что дал 1→3):
  ДО:    строк-заголовков 1, подстрок 3
  ПОСЛЕ: строк-заголовков 1, подстрок 3
  в ТЕЛЕ ВСТАВКИ подстрок: 0 (если 0 — вклад вставки нулевой)
  где встречается подстрока в живом узле:
    строка  159 | 4 доходили до владельца, стало 0 из 4).⏎⏎ЧТО ОТКРЫТО: корень А — служе
    строка  214 | а живых задачах ещё НЕ срабатывала ни разу.⏎⏎ОТКРЫТО:⏎- PLAN_ADAPT вык
    строка  811 | няет, аналитики под личную подписку нет.⏎  • ОТКРЫТО: писатель журнала

ИТОГ ЗАМКА (обе мерки, обе стороны): ЗЕЛЁНЫЙ
---exit=0---
```

Вклад вставки в этот счёт — **ноль**. Замок зелёный: 21 из 21.

### Границы вставки (по `expected.txt`, доказанно тождественному живому узлу)

```
=== 6 СТРОК ПЕРЕД ВСТАВКОЙ ===
  | по событию, не постоянно.
  | ────────────────────────────────────────
  |
=== ПЕРВАЯ И ПОСЛЕДНЯЯ СТРОКИ ВСТАВКИ ===
  > === ИНСТРУКЦИЯ АВТОЛОГОНА — ВООРУЖЕНИЕ, ОТКАТ, ПРОВЕРКА (18.08.2026) ===
  > ЦЕНА, КОТОРУЮ ВЛАДЕЛЕЦ УЖЕ ЗНАЕТ И ПРИНЯЛ: пароль лежит на машине, админ достаёт его из LSA Secrets …
=== 6 СТРОК ПОСЛЕ ВСТАВКИ ===
  | === ОТКРЫТЫЕ ВОПРОСЫ ИЗ ROADMAP_V2 (перенос 01.08.2026) ===
```

Раздел встал отдельной секцией сразу за блоком состояния (там, где висит строка «ждёт владельца у машины») и перед секцией ROADMAP_V2 — на телефоне он у самого верха.

## 4. Что перенесено

Все десять шагов — отдельными строками `ШАГ 1.`…`ШАГ 10.`, в том же порядке и с тем же содержанием: окно PowerShell → строка запуска `powershell -ExecutionPolicy Bypass -File D:\turbobaby-bot\autologon_arm.ps1` → новое окно и «Разрешить внести изменения?» → «Пароль учётной записи mxfill1:» вслепую → зелёное «Пароль проверен системой — верен» / «ИТОГ: AutoAdminLogon = 1» против красного «СТОП» → перезагрузка → **откат** `-Disable` → запертый экран это норма (`tb_lock_on_logon`, 5 секунд) → 7 минут не трогать, ПИН не вводить, `Get-Content D:\turbobaby-bot\pc_orchestrator.log -Tail 5` → признак успеха: время в свежих строках раньше разблокировки. Плюс отдельными строками продублированы «ОТКАТ ОДНОЙ СТРОКОЙ» и «ПРОВЕРКА ОДНОЙ СТРОКОЙ» с пометкой «это ШАГ 7 / ШАГИ 9 и 10, а не отдельное действие» — чтобы искалось с телефона. Имена дословно: `autologon_arm.ps1`, `D:\turbobaby-bot\autologon_arm.ps1`, `tb_lock_on_logon`, `pc_orchestrator`, `pc_orchestrator_watchdog`, `pc_orchestrator.log`, `mxfill1`, `pc_orchestrator.lock`/`pc_agent.lock`/`userbot.lock`/`moderation_bot.lock`.

**Два места помечены словом «уточнить»** (перенесены как есть, не додуманы): шаг 5 — в артефакте не сказано, что делать дальше, если вышло «СТОП»; шаг 9 — не названо, в каком окне выполнять `Get-Content`. Честные «НЕИЗВЕСТНО» артефакта (RC-консоль, пустой ли пароль) перенесены тем же словом.

## 5. Что НЕ делалось

Автологон не вооружён; реестр не читался и не правился; Планировщик не трогался; пароль не запрашивался и нигде не сохранён; машина не перезагружалась; код не правился; ничего не удалено; в рабочие таблицы не писали; пульс не обновлялся; других узлов мозга не касался.

⚠️ **Строку `DONE` в `cowork_log` я НЕ писал намеренно:** журнал — тоже узел мозга, а задача прямо запрещает трогать другие узлы. Ритуал CLAUDE.md и этот запрет здесь расходятся; я выбрал явный запрет задачи и говорю об этом вслух, а не молчу. Автоматика (`SessionEnd`-хук) пишет свою строку независимо от меня.

## Сводка

1. Признак сделанности проверен первым действием: фразы «инструкция автологона» в узле не было (0 вхождений) — работа новая, не повтор.
2. Узел открыт **по имени** через живой реестр моста (`resolve_name("master")` → `name:master`), id нигде не захардкожен.
3. Вставлен раздел на 5472 символа: 10 шагов вооружения по порядку, откат, проверка после перезагрузки и признак успеха, плюс честные «НЕИЗВЕСТНО» и два места, помеченных «уточнить».
4. Все замки записи зелёные, включая обратимость символ в символ; единственный красный оказался дефектом моей же мерки и снят пересчётом, а не объявлен «ложным».
5. Бэкап прежнего тела лежит в `tmp\brain_backup_master_autologon_howto_0818_20260818-201039.txt` — откат вставки есть чем сделать.

RESULT: в узел мозга `master` (адрес из живого реестра, ключ master) добавлен раздел «ИНСТРУКЦИЯ АВТОЛОГОНА» на 5472 симв. — 10 шагов вооружения, откат `-Disable`, проверка после перезагрузки и признак успеха, дословно из docs/artifacts/2026-08-18-autologon-howto.md; узел 97486→102961, фраза «инструкция автологона» 1 вхождение, все 21 замок зелёные (красный по заголовкам оказался дефектом мерки, снят пересчётом); автологон не вооружался, реестр/Планировщик/пароль не тронуты.
