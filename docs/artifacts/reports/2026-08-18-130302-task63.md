# Полный отчёт задачи 63 — 2026-08-18 13:03 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4509 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 63 |
| записано | 2026-08-18 13:03:02 |
| символов в отчёте | 4509 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Месячный потолок ограничен сроком от 30 суток: `aeb326a` (код+тесты), `0be2c71` (артефакт), `538bbbd` (тело журнала). Рабочая корзина 7-14 вернулась 52.5→62.7 из ста, сильнее всех выиграла 15-29 (19.4→49.2). Ухудшившихся корзин нет ни одной. Корзина 30-179 (42% денег) не тронута ни одной бронью. Отрицательный тест 30/29 прошёл обеими сторонами.

**FACT:**
- `FACT: commit aeb326a в git log` — «месячный потолок стал МЕСЯЧНЫМ», suggest.py + test_suggest.py, 141 вставка
- `FACT: commit 0be2c71` — артефакт `docs/artifacts/2026-08-18-cap-short-term-fix.md` (307 строк) на диске и в git
- `FACT: гейт 3763 теста, красных 2` — те же два храповика неразбора, красные и на чистом HEAD с тем же `995 not less than or equal to 845`
- `FACT: тестов 540 → 547` в test_suggest, зелёные
- `FACT: 4 контрольные суммы совпали до и после` (units.jsonl `4e7f451f…`, price_source.json `dd382ac9…`)

**1. Где прикладывался и дословное условие.** Подмену делает клиентский `suggest.py` (не лист и не мост — сказано самим источником). Условие стояло **в четырёх местах дословно одно и то же**: `if cap_active and cap_price is not None and total is not None and total > cap_price:` — **срока не спрашивало вовсе**. Две точки клиентские (`_client_price`, `_quote_j_line`, котировка любого срока — там дефект), две месячные по построению (`_sheet_q_month`, `_sheet_month_cell`).

**Забор Честертона одной строкой:** условие писалось под акцию низкого сезона (`94ed7ad`, 05.07) с невысказанной посылкой «судимая котировка МЕСЯЧНАЯ» — верной у первого потребителя и сломавшейся, когда предикат разошёлся по четырём копиям. Соседнюю посылку **не сломали**: признак `cap_active` не гашен, потому что у него есть второй потребитель, к цене не относящийся — `_sheet_low_season` читает его как **детектор сезона**; погаси мы признак, прайс перестал бы объявлять низкий сезон на коротких сроках, и ни один ценовой тест этого бы не увидел (проверено живым вызовом).

**2. Правка.** Предикат сведён в одно место `_cap_applies` (два докстринга и раньше требовали «предикат РОВНО как в `_client_price`» — совпадение держалось на честном слове), добавлен срок `days >= 30`. Порог **взят у источника**, а не выдуман: `low_season_caps.unit` = «฿ за месяц (30 суток)». Откат — `_CAP_MIN_DAYS = 1`.

**3. Перемер, тот же корпус и та же мерка** (модуль `lib` захода 18.08 импортирован, а не пересказан; новых котировок не считано). Замок сходимости: клетка §2.5 воспроизведена до знака.

| корзина | ±15 % ДО → ПОСЛЕ | медиана | деньги ошибки |
|---|---|---|---|
| 1-3 | 56.5 → 56.5 | +0.0 → +0.0 | без изменений |
| 4-6 | 60.1 → 60.1 | +2.7 → +2.7 | без изменений |
| **7-14** | **52.5 → 62.7** | +1.0 → +5.2 | 641 806 → 394 713 (−38.5 %) |
| **15-29** | **19.4 → 49.2** | −46.3 → **+3.2** | 702 391 → 366 772 (−47.8 %) |
| 30-179 | 30.5 → 30.5 | −26.3 → −26.3 | не тронута |
| 180+ | 40.0 → 40.0 | −28.6 → −28.6 | не тронута |
| ВЕСЬ | 45.9 → 52.8 | −5.2 → −0.1 | −21.3 % |

**Ухудшившихся корзин нет ни одной** — прятать нечего. Находка, которой не было в заголовке замера: сильнее всех страдала **не** рабочая корзина, а **15-29 суток** — 101 бронь из 124 судилась месячным потолком.

**4. Корзина 30-179 (42 % денег) — не держится ничего, измерено:** срабатываний **210 до и 210 после**, все показатели побайтно те же; минимальный срок корзины и есть 30 суток. Честная оговорка: приложенный потолок остаётся вредным там в высоком сезоне — это развилка №2 (привязка к сезону), сознательно не сделана.

**5. Отрицательный тест — обе стороны устояли:** 30 суток кепка есть, 29 нет; плюс доказано, что тест **кусается** — на откате `_CAP_MIN_DAYS=1` и 29, и 10 суток снова уходят под кепку («аренда от 9900 ฿/мес» вместо `12491 ฿`).

**Границы:** бот не запускался, userbot/moderbot не перезапускались, в рабочие таблицы не писано, в сеть не ходили, календарь сезонов и база моделей не тронуты, источник цены в бота не врезан, не удалено ничего. Временное — в `_scratch_capfix_0818/` (в git не идёт). Правка **не выкачена в живой процесс**: изменится на следующем штатном подъёме.

RESULT: приложение месячного потолка ограничено сроком от 30 суток (aeb326a); рабочая корзина 7-14 ±15% 52.5→62.7, корзина 15-29 19.4→49.2, весь корпус 45.9→52.8, ухудшившихся корзин ноль, корзина 30-179 не тронута (210 срабатываний до и после); отрицательный тест 30/29 прошёл обеими сторонами и доказано кусается; гейт 3763 теста, красных 2 — те же два храповика, тестов 540→547; артефакт docs/artifacts/2026-08-18-cap-short-term-fix.md (0be2c71), журнал записан.
