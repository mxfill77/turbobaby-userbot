# Полный отчёт задачи 63 — 2026-08-31 20:16 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4548 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 63 |
| записано | 2026-08-31 20:16:49 |
| символов в отчёте | 4548 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

V0 научился отличать свежий результат от следа прежнего прогона: введён гейт `V0_RUN_BINDING` — прогон называет задача, бандл сверяет, каждая улика несёт имя прогона сама. Фикстура 2 закрыта (C2a/C2b: PROVEN → DISPROVEN), фикстура 1 осталась красной теми же байтами. Коммит `1c6f314`, пуша нет.

**FACT:**
- `FACT: commit 1c6f314 в git log` — «V0 отличает свежий результат от следа прежнего прогона», 3 файла, +335/−4
- `FACT:` тесты V0 — `Ran 29 tests ... OK (skipped=1)`, было 18
- `FACT:` фикстуры — `requirement violated in 0/12 case(s)` (замер «до» на том же наборе: 6/12)
- `FACT:` двойной прогон разными процессами — `compared 15 file(s) run1 vs run2, differing: 0`
- `FACT:` журнал — две строки приняты мозгом (668317 → 668870 символов)

## Что сделано

**1. Работа от артефакта, премиса сверена числом.** Открыт `docs/artifacts/2026-09-01-v0-negative-test.md`. Замер «до» снят на модуле с sha256 `178af445…` — ровно тем, который назван в отрицательном тесте. То есть «до» здесь не пересказ документа, а тот же прибор.

**2. Привязка свежести — по личности, не по времени.** Гейт `V0_RUN_BINDING` стоит последним содержательным (после `V0_TEST_EVIDENCE`), три проверки:
- пакет **задачи** обязан нести `run_id` (зеркало уже принятой в V0 асимметрии «гейты называет задача, не исполнитель») → иначе `UNKNOWN/task_run_id_missing`;
- бандл обязан назвать тот же прогон → иначе `DISPROVEN/run_binding_mismatch`;
- **каждая** улика обязана нести имя прогона сама: JSON — полем `run_id`, текст/питон — собственным объявлением. Чужой прогон → `DISPROVEN/stale_run_evidence`; никакого → `UNKNOWN/unbound_run_evidence`.

Сравнение точное, токен ограничен с обеих сторон: `job63-run-2` не привязывает улику, называющую `job63-run-20` (живой отказ, кейс C3d). Совпадение по времени связью не считается — закреплено дважды: поведением (труп переписан последним, mtime у него максимальный — вердикт всё равно `stale_run_evidence`) и инвариантом по AST исходника (часов в модуле нет вовсе: ни `time/datetime/calendar`, ни `st_mtime/getmtime/getctime/utcnow/monotonic`).

**3. Обе фикстуры и весь набор — дважды, побайтно.** Фикстуры только во временной папке — **`tmp/v0_fresh_0901/`** (старая `tmp/v0_neg_0901/` не тронута). Нарушений требования **6/12 → 0/12**:

| кейс | ДО | ПОСЛЕ |
|---|---|---|
| C2a труп, ничего не менялось | **PROVEN** ❌ | `DISPROVEN` stale_run_evidence |
| C2b труп, пакет зовёт его новым | **PROVEN** ❌ | `DISPROVEN` stale_run_evidence |
| C3a/C3b/C3c/C3d пробы привязки | **PROVEN** ❌ | отказ, четыре разных кода |
| C1a/C1b/C1c/C2c (фикстура 1) | отказ | отказ, **байт в байт тот же отчёт** |
| C0 и C3e (два положительных контроля) | PROVEN | PROVEN |

Прежние проверки не ослаблены и даже не сдвинуты: четыре отчёта совпали с «до» побайтно. Контроли оставлены оба (JSON-продукт и текстовая улика) — иначе отказ везде «проходил» бы тест бесполезным прибором. Двойной прогон разными процессами: 12 отчётов + сводка + реплей + расписка набора = 15 файлов, `cmp` расхождений 0. Заодно снята заноза прошлого замера — поле `run_tag`, единственный отличавшийся байт, выкинуто из сводки.

**4. Цена измерена, а не оценена.** Реальный теневой бандл H1 от 31.08 переигран read-only: **до** правки он воспроизвёл записанный тогда `V0_RESULT_PACKET.json` байт в байт (PROVEN, 1839 б), **после** даёт `UNKNOWN/task_run_id_missing` (1760 б). Старый бандл переходит в «неизвестно», а не в «опровергнуто» — верное направление.

## Что НЕ закрыто (названо в артефакте)
- привязка доказывает принадлежность **названному** прогону, а не «сейчас»: комплект прошлого захода целиком внутренне согласован. Назвать текущий прогон — работа зовущего, но теперь это одно видимое глазом поле;
- асимметрия baseline жива (развилка 2 не применена); развилка 3 (`PROVEN` при пустом `verified_scope`) не применена;
- ветка `no_run_bound_evidence` сегодня недостижима через `verify_case` — оставлена сознательно как fail-closed замок, в отчётах не встречается ни разу.

Пуша нет (запрет задачи). `CLAUDE.md` и `todo.md` — чужие незакоммиченные правки, не тронуты и не застейджены. Красных операций не потребовалось; userbot и moderation_bot не трогались.

**Адрес результата:** `docs/artifacts/2026-09-01-v0-svezhest-zakryta.md`

RESULT: гейт V0_RUN_BINDING внедрён, фикстура 2 закрыта (C2a/C2b PROVEN → DISPROVEN stale_run_evidence) при неизменной красноте фикстуры 1 (4 отчёта побайтно те же), тесты 18→29 зелёные, 12 фикстур 0/12 нарушений, двойной прогон cmp 0, коммит 1c6f314, артефакт docs/artifacts/2026-09-01-v0-svezhest-zakryta.md, журнал записан
