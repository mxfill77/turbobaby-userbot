# Полный отчёт задачи 90 — 2026-09-01 13:01 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4819 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 90 |
| записано | 2026-09-01 13:01:00 |
| символов в отчёте | 4819 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Ступень C закрыта: закрытие задачи полосы ПК судит V0, а не отчёт исполнителя. Врезка в одно место (`pc_orchestrator._judge_done`), опорную линию по названному адресу меряет демон ДО захода. Оба обязательных отрицательных красные. На 4 реальных закрытых задачах за 01.09 — «сделано» 4/4, на трёх живых отрицательных пробах тех же байтов — «неизвестно» 3/3. Коммит `f300a65`.

**FACT:**
- commit `f300a65` в `git log` — 7 файлов, +1158/−9 (`content_product_verifier.py`, `done_judge_pc.py` новый, `pc_orchestrator.py`, три теста, артефакт)
- тесты: `test_content_product_verifier` + `test_done_judge_pc` + `test_result_judge_pc` + `test_series_pc` → **198 OK** (1 skip, тот же что был); `test_pc_orchestrator` → **961 OK**
- артефакт `docs/artifacts/2026-09-01-ступень-C-V0-судит-done.md` (211 строк) в коммите
- журнал: расписка `OK: записано в мозг, символов: 705108`
- сторож реестров зависимостей на застейдженном: `deps rc=0`

## 1. От чего работали (п. 1 задания)

Первым действием прочитаны три артефакта за 01.09: `2026-09-01-v0-negative-test.md`, `2026-09-01-v0-svezhest-zakryta.md`, `docs/review_inbox/2026-09-01-независимая-приёмка-V0-закрыта.md`. Заново ничего не строилось — взята **развилка 2 отрицательного теста дословно** («пока „что было до“ приходит от исполнителя, C2b неустраним в принципе; честный вариант — снимать baseline тем же прибором ДО захода») и применена.

**Судьёй стои́т V0, а не готовый `result_judge_pc`**: у того инвариант `RESULT_JUDGE_UNWIRED` роняет гейт на любом упоминании имени в боевом коде — врезка начиналась бы со снятия замка, а задание запрещает ослаблять тесты. Он не тронут ни строкой, его инвариант зелен и после захода.

## 2. Где судит V0 (п. 2)

Одна точка: `done_base = done_judge_pc.baseline(text)` перед `run_task`, и `_judge_done(...)` внутри ветки `if status == "done"`. Что вызов ровно один и что снимок стои́т РАНЬШЕ захода — держат тесты (`body.count("done_judge_pc.judge(") == 1`), а не абзац.

Слово исполнителя умирает здесь: пакет задачи, оба манифеста и опорный снимок пишет **демон**; в бандл от исполнителя едет только заявка `reported_done`, то есть предмет проверки, а не доказательство.

## 3. Отрицательные (пп. 3–4) — оба красные

| кейс | вердикт | кто отказал | reason_code |
|---|---|---|---|
| контроль: свежий продукт по адресу | **сделано** | V0 | `all_gates_passed` |
| **адрес назван, по адресу пусто** | **неизвестно** | адаптер | по адресу ПУСТО |
| **по адресу результат ПРЕЖНЕГО прогона** | **неизвестно** | **V0** | `changed_scope_mismatch` |

Второй ловит V0, а не обвязка: адрес назван в `allowed_changed_paths`, изменений по нему ноль. Всего у судьи 24 теста, у врезки — 7.

## 4. Живой прогон (п. 5)

| задача | продукт по адресу | вердикт |
|---|---|---|
| #72 `54cb9d1` | `2026-09-01-abs-root-class-in-tests.md` | **сделано** |
| #75 `9afe6ab` | `…-сверка-двух-баз-цены-на-парах.md` | **сделано** |
| #76 `a6a8f8a` | `…-цена-удержания-ворот-в-числах.md` | **сделано** |
| #77 `892c4b4` | `…-разрешение-имени-модели-замер.md` | **сделано** |

Живые отрицательные на тех же байтах: труп прежнего прогона → `changed_scope_mismatch`; чужие слова → `text_condition_failed`; пустая дата → «по адресу ПУСТО». **До третьей добавки в V0 было 2 из 4** — #75 и #77 падали ложно, потому что слова адреса живут в ИМЕНИ файла, а не в теле; числа «до» оставлены в разборе, иначе «4 из 4» читалось бы как подгонка.

## 5. Серия (п. 6)

Скормлены вердикты, а не слова: `clean 4, broken 0` → **серия 4**. Контрфакт с одним «неизвестно» → **серия 2, обрывов 1**. `series_pc` остался неподключённым (его собственный инвариант), прогон сухой, боевой файл счёта не изменён (mtime 17.08).

## 6. Что НЕ сделано — сознательно

Врезка стои́т в режиме **`addr`** (судим там, где адрес назван), а не `all`. Полное правило владельца («нет адреса — тоже не сделано») реализовано и включается `DONE_JUDGE_PC=all`, но дефолтом не стало: на этой полосе оно уже измерено как поглощающее (артефакт 17.08 — судья ответил бы «неизвестно» на 100 % строк), а служебные закрытия адресов не несут. Это ваше решение, не моё. Незакрытая мина — гомоглиф в словах адреса (`С`/`C`).

Запреты соблюдены: ничего не выкатывалось и не перезапускалось, дети и клиентский контур не тронуты, ничего не удалено, `.env` не читался, временное — только в `tmp/donejudge_replay_0901/` и `tmp/done_judge_pc/`. Ни один тест и порог не ослаблен: V0 было 29 → стало 41, все прежние `assert` на месте.

RESULT: ступень C закрыта — V0 судит закрытие задачи полосы ПК из одного места, опорную линию меряет демон до захода, оба обязательных отрицательных красные, на 4 реальных задачах 01.09 «сделано» 4/4 и на 3 живых отрицательных «неизвестно» 3/3, серия по вердикту 4 (контрфакт 2); коммит f300a65, разбор `docs/artifacts/2026-09-01-ступень-C-V0-судит-done.md`, откат `DONE_JUDGE_PC=off`.
