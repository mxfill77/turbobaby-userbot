# Полный отчёт задачи 111 — 2026-09-02 04:56 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5188 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 111 |
| записано | 2026-09-02 04:56:46 |
| символов в отчёте | 5188 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

EN-зеркала заведены и замерены. Корпус вырос с 12 до 16 кейсов (4 EN-двойника RU-кейсов), замер на замороженном кандидате `0c91a72` дал **0 провалов языка из 20** — но при НЕДОСТУПНОЙ цене Календаря, поэтому с прежним «1 из 2» это числа с разных условий. Коммиты `19aafde`, `fae7d6a` в origin/main.

**FACT:**
- `FACT: commit 19aafde в git log` — корпус + тест-замок + артефакт (3 файла, +247 строк), запушен `b754ab5..19aafde`
- `FACT: commit fae7d6a в git log` — тело журнальной записи, запушено; `git status -sb` → `## main...origin/main` (в синхроне)
- `FACT: артефакт docs/artifacts/2026-09-02-EN-набор-база-языкового-провала.md, 9 разделов` (на входе задачи файла НЕ БЫЛО — признак сделанности отработал)
- `FACT: замер 20 единиц из 20, wall 287.7с, lang_fail=0, lang_ok=20, unknown=0, head_moved=""` (`_scratch_en_cases_0902/en_summary.json`)
- `FACT: корпус 16 кейсов, sha 6d5d78f0d83acdca, EN=[8,13,14,15,16]`; `test_trainer_run` 62/62 OK

**1. Премиса (первым действием).** Артефакта по адресу результата нет → работа с нуля. В `trainer_cases.json` 12 кейсов, `lang=en` ровно один — id 8 (sha на входе `98ad5e3e2c25…`). Артефакт причины на месте: провал — сэмплинг (язык задан одной фразой ~0.7% промпта в промпте на 89.4% кириллическом; `claude -p` без temperature и seed).

**2. Кандидат заморожен и доказан, а не назван.** Прогон физически шёл на вершине `b754ab5` (40 коммитов после `0c91a72`), и это не подмена: сверены **блобы** — `suggest.py`, `trainer.py`, `trainer_run.py`, `client_contour.py`, `model_name.py`, `price_source.py`, `trainer_cases.json` у обоих коммитов SAME, рабочее дерево по ним чистое. Вершина за прогон не двигалась (`verify_head`).

**3. Арифметика бюджета — до прогона, на замере.** Потолок `PC_TASK_TIMEOUT`=2700с, 0.7 = **1890с**. Цена единицы замерена на двух единицах (кейс 8 × 2): wall 94с = **81с холодная + 12с тёплая**. Модель `69 + 12·N`: 20 единиц → **309с = 16% потолка**. Не влезала только гипотеза «все 20 холодные» (1620с = 86%), опровергнутая самим замером. **Прогоны не резались.** Факт: вышло **287.7с**, ошибка модели 7%.

**4. Что заведено.** 13←1 (первый контакт, одна модель), 14←2 (прайс-интент, со снятием чека депозита с той же причиной, что у двойника), 15←4 (только депозит), 16←3 (модель+даты+гео, Раваи 590). Чеки — двойников плюс универсальный чек языка. **Числа не выдуманы: их в корпусе нет вовсе** — `expectations()` считает их из той же `pricing_note` живого Bridge, что питает черновик. Источник помечен у каждого: «ЗЕРКАЛО», номер двойника, 02.09.2026, имя задачи. Проба до заведения: все 4 фразы → `en`, все `expect_facts` засчитаны. **12 существующих кейсов и их чеки не тронуты.**

**5. Замок роста корпуса.** `test_trainer_run.py:260` держал `assertEqual(len(cases), TRAINER_MIN_CASES)` — точное равенство против константы-**минимума**, из-за чего краснел на каждом добавленном кейсе, то есть запрещал корпусу расти. Заменено на `assertGreaterEqual` с причиной; пол урезания сохранён (ворота и так сверяют `cases == cases_total`). 62/62 зелены.

**6. Результат.** Гнались только EN, 4 круга, результат каждой единицы на диск сразу. Таблица кейс × прогон — 20 клеток, все 🟢 по языку. **Провалов языка 0 из 20**, «неизвестно» 0 из 20.

**7. Условие, без которого число читать нельзя (главное).** Цена из Календаря была **недоступна** — записка говорит это дословно, `j_line`/`sheet_line` пусты. Отсюда 12/12 красных «строка J дословно» и 8/12 «цена цифрой» — к языку отношения не имеющих. Для чека языка это в одну сторону **строже** (вычитать вставки кода было нечего, кириллица искалась по всей прозе), в другую **слабее**: замер 01.09 с провалом 1 из 2 шёл при работающем Календаре, и русского в промпте было больше. **0 из 20 и 1 из 2 — числа с разных условий, вычитать одно из другого нельзя.** Верхняя граница частоты по правилу трёх — ~15%; какова она при живой цене — неизвестно, и это записано остатком. Выводов «стало лучше» нет нигде: бот не правился ни на байт.

**Запреты соблюдены:** ничего не удалено (временное осталось в `_scratch_en_cases_0902/`, в коммит не попало — стейджил три файла поимённо); клиентские файлы не тронуты; живой базы не касались (`isolate_ipc` уводит `moderation_ipc` в temp); чужие правки в дереве (`docs/review_*`, `todo.md`) не коммитил. Красных операций не потребовалось. Оговорка честности: обратное чтение журнальной записи мостом не удалось — исход по расписке моста (мост в те минуты таймаутил, что и подтверждает условие §7); пред-существующий красный `test_client_contour` («brain_writer обязан быть внутренним») к моим правкам отношения не имеет — доказано прогоном с застэшенными правками.

RESULT: в корпус добавлены 4 EN-зеркала RU-кейсов (13←1, 14←2, 15←4, 16←3, корпус 12→16, чеки двойников + чек языка, источники помечены), снят замок роста корпуса в тесте (62/62 зелены), на замороженном и доказанном блобами кандидате 0c91a72 замерена частота языкового провала — 0 из 20 при 4 прогонах × 5 EN-кейсов (287.7с, 15% потолка, прогоны не резались); число сопровождено условием «цена Календаря недоступна», делающим его несравнимым с прежним 1 из 2; артефакт `docs/artifacts/2026-09-02-EN-набор-база-языкового-провала.md`, коммиты 19aafde и fae7d6a запушены.
