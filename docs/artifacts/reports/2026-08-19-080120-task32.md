# Полный отчёт задачи 32 — 2026-08-19 08:01 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5431 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 32 |
| записано | 2026-08-19 08:01:20 |
| символов в отчёте | 5431 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Готово. Рабочее дерево чисто, всё запушено.

**Заглушил пульсовый канал в прогоне тестов: замок стои́т в ПУТИ (`dispatch_notify._cowork` спрашивает `log_setup.is_test_context`), в наблюдателе — ноль правок. Оба отрицательных теста устояли замером журнала: гейт пишет 0 строк, живой канал пишет как прежде. Коммиты d43d8b9 + fdcbcfa + b5d5aab в origin/main.**

## FACT

- `FACT: commit d43d8b9 в git log` (правка), `fdcbcfa` (артефакт), `b5d5aab` (тело записи) — все в `origin/main`
- `FACT: (а) полный гейт 3785 тестов / 375.5 с → в боевом журнале НОЛЬ новых строк` (1336 → 1336, посимвольно)
- `FACT: (б) живой процесс через боевой send_pulse → строка ЛЕГЛА` — журнал 1336 → **1337**
- `FACT: в наблюдателе НОЛЬ правок` — `expectations_pc.py` и `expectations_pc_run.py` в диффе отсутствуют
- `FACT: гейт 3785 против 3782, красных 2 — те же два храповика` (993 точки = как на f370c10)

## Признак сделанности

Артефакта не было — задача выполнена, не пропущена.

## Цена класса числом (что просили назвать)

За сутки 18.08 07:37 → 19.08 07:37 UTC: **26** строк «дети контура», из них **21 тест-происхождения** (81%), **10 слепых**. Боевых — 5.

**Слепых 10, но 11 остальных опаснее слепых:** слепая честно говорит «не знаю», а эти сказаны фикстурными часами и звучат как правда о живом модерботе. Они приходят из одиночного `python test_expectations_pc.py`, где `TESTING` нет вовсе — замок только на `TESTING` закрыл бы 10 из 21 и оставил худшую половину.

## Забор Честертона

**Забора не было — есть дыра, прорезанная позже под уже лежавшим полом.** Пять методов `TestRunHands` писались как проверка ЗАМЕТОК, когда пульсовой ветки в `run()` не существовало; О4 (14.08) и публикация о детях (18.08) встали ПОД эти вызовы с `pulser=None`. Что это хронология, а не замысел, доказывает сосед `TestKidsHands` — писался ВМЕСТЕ с публикацией и `pulser=` передаёт.

**Теряем ли проверку канала — да, называю прямо:** до правки эти семь сайтов единственные в гейте проходили запись насквозь (спавн → писатель → мост → док). **Но проверкой это не было ни дня — ни одно утверждение не смотрело на результат:** `test_healthy_contour_says_nothing` собирает в `self.sent` только заметки, и канал, через который течёт, в утверждение о тишине не входит. Взамен: живая ветка спавна покрыта двумя тестами (argv, `DETACHED_PROCESS`, «не ждём»); сквозной канал остаётся за живой полосой, где писатель судит исход обратным чтением — строже, чем судил гейт; рецепт разовой сквозной проверки руками положен файлом.

## Отрицательные тесты (дословно)

```
$ venv/Scripts/python.exe -m unittest discover -s . -p "test_*.py"
Ran 3785 tests in 375.499s
FAILED (failures=2, skipped=10)

$ diff.py before after_gate
снимок «before»: 1336 строк · снимок «after»: 1336 строк
ПРИБЫЛО ВСЕГО: 0
── из них ПУЛЬСОВЫХ (предмет замера): 0
```
```
$ live_pulse.py
дискриминатор видит тест-прогон: False  (ожидание: False — полоса живая)
TESTING=None PYTEST_CURRENT_TEST=None TURBOBABY_TEST_LOGS=None argv0=live_pulse.py
send_pulse → True

$ diff.py before final
снимок «before»: 1336 строк · снимок «final»: 1337 строк
ПРИБЫЛО ВСЕГО: 1
     NOTE 2026-08-19 07:55 UTC: проверка канала пульса (задача 31, отрицательный тест «б»)…
```

**Граница (б), честно:** живьём проверен КАНАЛ, а не решение пульсовать — в момент замера боевой прогон ответил «строка о детях: не нужна — оборот не доказан», потому что демон синхронно исполнял эту же задачу (штатный замок О4). Ветка решения правкой не тронута ни строкой. Первый замер (б) снят преждевременно (снимок обогнал писателя, повторявшего запись через флапающий мост) и не засчитан — приведён повторный.

## Красная проба

```
подменён dispatch_notify._cowork → редакция ДО правки (замок снят)
КРАСЕН TestCoworkDetached.test_test_run_does_not_write_to_the_owners_journal
КРАСЕН TestPulseChannelMutedInTestRun.test_live_hands_from_a_test_run_spawn_no_journal_writer
зелен  TestCoworkDetached.test_live_lane_still_writes
зелен  TestCoworkDetached.test_spawn_is_detached_and_not_awaited
```

Оба замка ловят ровно тот дефект; обе живые ветки правкой не задеты.

## Замок

`Ran 3785 tests` (+3 новых, ни один не потерян), красных 2 — `test_nonparse_ratchet.TestRatchet` ×2, красные на `main` до любой правки. **993** точки — совпадает с зафиксированным на f370c10 до символа, моих файлов в списке слепых читателей **0**.

## Осталось открытым (тот же класс, сюда не взято)

`pc_orchestrator._cowork` — вторая дверь в тот же журнал, держится ДОГОВОРЁННОСТЬЮ (подменяется в ~15 местах теста); утечки за сутки не измерено, но устройство ровно то, что уже не доехало 05.08. Симметричный замок — отдельной правкой. Также `_queue_snapshot` (другой док) и телеграм-канал — вне цели задачи. Побочно закрыто: тест-прогон больше не дописывает в репозиторный `cowork_hook.log`.

Запретов не нарушал: наблюдатель, период публикации, клиентские файлы, боты, процессы, `.env` и таблицы не тронуты; ничего не удалено. Красных операций не потребовалось.

RESULT: пульсовый канал заглушён в прогоне тестов замком в ПУТИ (`dispatch_notify._cowork` + `log_setup.is_test_context`), в наблюдателе ноль правок; (а) гейт 3785/375с → 0 новых строк в журнале, (б) живой send_pulse → строка легла (1336→1337); красная проба валит оба замка на старом коде; гейт 3785 против 3782, красных 2 — те же два храповика (993); коммиты d43d8b9, fdcbcfa, b5d5aab в origin/main; артефакт docs/artifacts/2026-08-19-test-noise-in-journal.md, обе строки журнала легли.
