# Полный отчёт задачи 98 — 2026-09-01 18:25 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4652 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 98 |
| записано | 2026-09-01 18:25:30 |
| символов в отчёте | 4652 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Разведка повода `3425476c8e2f` закрыта артефактом: «отказ канала manus: accepted_no_answer ×3» — свойство СТАРОГО ПРИБОРА, а не полосы. Все три события легли до коммита `6cdc00e` (01.09 21:13:32), которым отправщик научился забирать результат опросом; до него опроса не было вовсе. Класс закрыт за 4 ч 05 мин до постановки этой задачи. Боевого кода не правил.

**FACT:**
- commit `a9a2031` в `git log`, запушен `959bb98..a9a2031 main -> main`, `git status -sb` → `## main...origin/main` (ahead 0)
- артефакт `docs/artifacts/2026-09-01-разведка-отказ-канала-manus-accepted-no-answer.md`, 207 строк; маркер `2026-09-01-разведка-отказ-канала-manus-accepted-no-answer` стои́т строкой в теле (`grep -c` → 1)
- живая проверка эффекта: `venv/Scripts/python.exe recon_auto_run.py --status` до артефакта — **поводов 2**, после — **поводов 1**, ключ `3425476c8e2f` из списка ушёл
- журнал: две строки в мозге (732953 → 733414 символов)

## ЗАМЕР — числа и команды

**1. Три события и правка.** `stat -c '%y'` по файлам лотка с `accepted_no_answer`: `18:39:32` (пакет 72), `20:01:33` (80), `20:07:37` (84). `git log -- review_send_run.py`: `6cdc00e` — `2026-09-01 21:13:32`. Разрыв последнего события до правки — **1 ч 05 мин 55 с**; замер повода контуром `18:18:50Z` = **02.09 01:18:50** местного, то есть **через 4 ч 05 мин** после правки.

**2. Опроса до правки не было.** `git show 6cdc00e^:review_send_run.py | grep -c 'fetch_manus_task\|_wait_settled'` → **0**; то же по HEAD → **6**. Ноль — это «механизма нет», а не «мало».

**3. Сплошной счёт по каналу за 01.09 (18 ответов):** до правки — 3 `accepted_no_answer`, 10 `http_400`, 1 `no_credentials`, **ответов 0**; после — **4 из 4 `ok`**. Исключений нет ни в одну сторону.

**4. Почему событий ровно 3, а не 14.** Пакеты `accepted_no_answer`: 9576 · 10891 · **11045** знаков. Пакеты `http_400`: **16065** … 19926. Граница в **(11045, 16065]**: первая стена канала (5000 estimated tokens, код 400) громче второй, и до второй дошли ровно три коротких пакета. Число «3» — свойство размеров пакетов дня, а не частоты отказа канала.

**5. Механизм — живым телом, не рассуждением.** `tmp/review_auto/manus_protocol.json`: `create_body` = `{"task_id":…,"task_title":…,"task_url":…}`, `create_status: 200`. Три поля, ни одно не входит в `review_send._MANUS_ANSWER_FIELDS`, зато `task_id` входит в признак асинхронной приёмки (`review_send.py:524`). Значит `accepted_no_answer` был **не сбоем, а единственно возможным исходом успешной отправки** — детерминированно, в 100 % случаев. Классификатор при этом отработал верно: врал не он, а ожидание вызывающего, что расписка о приёме и есть ответ.

## ПОВОД: верен как факт, протух как задача

Не ложный — три события были. Но это свойство версии прибора, которой больше нет. Закрывает повод уже уплаченная цена: `6cdc00e` (+489/−31, опрос `GET /v1/tasks/{task_id}`, предел 1800 с) и `76b4447` (деление пакета на части).

**Что остаётся** (в артефакте разделом):
- слово `accepted_no_answer` в сегодняшнем коде означает другое: дверь `review_send_run.py:800` («не спрашивали») и дверь `review_send_run.py:855-864` — задача дошла до готовности, а текста нет. Вторая — уже свойство ПОЛОСЫ. Одно слово на два разных мира;
- `repeat_signals` (`recon_auto_run.py:156-208`) считает весь лоток без оси времени и версии: «×3» читается как «происходит», хотя означает «происходило».

**Что править — словами, решение владельца** (в коде не сделано ничего): (1) печатать дату последнего события в тексте повода — цена одно поле + 1–2 юнита, риска нет, пользы больше всего; (2) развести исход на `accepted_not_polled` / `settled_no_text` — одна ветка + тест (сегодня покрытие одним юнитом, `test_review_send.py:275`), побочный эффект назван; (3) окно у счётчика — **не рекомендую**: спрячет медленно тлеющий класс.

**Честное «неизвестно»:** какая из двух правок дала ответы — замером не различить (между `6cdc00e` и `76b4447` в канал не ушло ни одного пакета, дыра 20:26 → 23:11); воспроизводится ли дверь `settled_no_text` живьём — наблюдений 0; заход, оборвавшийся до записи файла в лоток, в замер не попал.

**Красное:** не встретилось. Только чтение, `git log`/`git show`, read-only `--status`, коммит и пуш артефакта.

RESULT: разведка повода 3425476c8e2f закрыта — артефакт `docs/artifacts/2026-09-01-разведка-отказ-канала-manus-accepted-no-answer.md` (207 строк) написан, закоммичен `a9a2031` и запушен; причина названа числом (все 3 события до `6cdc00e` 21:13:32, опроса до него 0, 14 заходов → 0 ответов против 4 → 4 после), отделено свойство прибора от свойства полосы, повод снят живой проверкой (поводов 2 → 1), боевой код не тронут, журнал записан двумя строками.
