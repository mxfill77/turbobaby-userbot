# Полный отчёт задачи 73 — 2026-08-20 21:14 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4803 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 73 |
| записано | 2026-08-20 21:14:09 |
| символов в отчёте | 4803 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Бюджет сторожа свежести починен замером, а не назначением: `PRICE_GATE_PROBE_SEC` 20с→90с (коммит `3309f3c`, запушен). Шесть проходов боевой дверью: лучший 40.1с, медиана 41.1с, худший 43.0с. Три отрицательных теста устояли 5/5. Набор пути цены 60 красных → 50 (ушли все 11 сторожевых, +1 ранее скрытый ложно-зелёный). Артефакт и обе строки журнала легли.

**FACT:** коммиты `3309f3c` (код), `8758695` (артефакт), `c8b0d09` (тело журнала) в `git log`, все запушены (`3b85c06..c8b0d09`, main), отслеживаемых изменений в дереве 0. Живой эффект: боевая дорога врезки на живом мосте после правки — `ВЕРДИКТ: СВЕЖЕЕ` → продукт пути ответа отдал **298 ฿/день, итого 2086 ฿**; до правки та же дорога отвечала `НЕИЗВЕСТНО — не снялись ручки: J3` и цену гасила.

**Первое действие:** `ce464ee 2026-08-21 02:47:36 +0700` · время `2026-08-21 03:46:19`.
**Признак сделанности:** `ls: cannot access 'docs/artifacts/2026-08-20-freshness-budget-fix.md': No such file or directory` → не сделано, работал.

## Замер (дословно, `_scratch_freshbudget_0821/measure.py`)

```
ПОДЪЁМ клиента моста (вне бюджета врезки): 0.09 с
СОКЕТ-ТАЙМАУТ одолженного клиента: 90 с (потолок ОДНОЙ пробы)
раунд 1: ИТОГО   40.1 с | проб 9 [4.8 4.3 4.4 4.5 4.0 5.1 4.3 4.6 4.1] | ok=True | H3=0.15, I3=0.15, J3=0.25
раунд 2: ИТОГО   42.8 с | проб 9 [3.4 4.6 3.6 5.1 5.1 4.7 4.8 5.2 6.3] | ok=True | …
раунд 3: ИТОГО   41.6 с | раунд 4: 40.5 с | раунд 5: 40.1 с | раунд 6: 43.0 с
РАЗБРОС по 6 раундам: лучший 40.1 с · медиана 41.1 с · ХУДШИЙ 43.0 с
ХУДШАЯ одиночная проба: 6.3 с (ADV 350CC GREY BKK 798)
```

**Как выбрано 90:** двукратный запас от ХУДШЕГО (43.0×2=86→90) **и** ровно потолок одной двери (`bc.timeout=90`, замерен там же) — бюджет меньше потолка одной пробы отдаёт весь проход одной медленной двери.

## Забор Честертона одной строкой

**20 с не стояли ни на одном замере:** докстрока обосновывала их «пробами по 306с и 599с», но эти числа в `2026-08-20-silent-timeout.md` (строки 125–131) — цена **полного unittest-гейта**, а не похода к мосту. На быстроту сторожа не опирается никто, кроме клиента: единственный зовущий — `suggest.py:3309`, внешнего предела нет (`userbot_listen.py:257` — `to_thread` без `wait_for`). **Клиент теперь ждёт 40–43 с вместо ~22 с (+≈20 с), и только раз в 30 минут** (кэш TTL); в патологии потолок 90+90=180 с.

## Отрицательные тесты (`negative.py`, боевая дорога, подменена только дверь)

```
(а) ЖИВОЙ ИСТОЧНИК → вердикт СВЕЖЕЕ · цену называть МОЖНО · ЦЕНА НАЗВАНА: 298 ฿/день, итого 2086 ฿   УСТОЯЛ
(б) дверь бросает на каждой пробе → НЕИЗВЕСТНО · ЦЕНА НЕ НАЗВАНА                                    УСТОЯЛ
(б) клиент моста не поднялся      → НЕИЗВЕСТНО · ЦЕНА НЕ НАЗВАНА                                    УСТОЯЛ
(в) настоящий sleep 15.5 с/проба (140 с > 90 с) → «не снялись ручки: J3» · ЦЕНА НЕ НАЗВАНА, 98.1 с  УСТОЯЛ
ИТОГО: устояли 5 из 5
```
(в) — искусственной задержкой, а не рассуждением; авторство гашения доказано логом `price_gate: ЦЕНА НЕ НАЗВАНА…`, а не совпадением исхода.

## Проверка набора пути цены

```
Ran 836 tests in 202.693s
FAILED (failures=50)
из 11 сторожевых ОСТАЛИСЬ красными: 0   ·   УШЛИ: 11
скрытый ложно-зелёный (§4 разведки) теперь красный: True
АРИФМЕТИКА: 60 − 11 + 1 = 50, факт 50
grep -c "price_gate:" → 0        (ни одного гашения за 836 тестов)
test_price_gate test_price_freshness test_price_source → Ran 83 tests in 0.093s / OK
```

**Сводка.** (1) Сторож перестал глушить цену по нехватке времени: бюджет 90 с против измеренной нужды 40–43 с. (2) Осторожность цела — «не успел прочитать» по-прежнему означает молчание, проверено тремя отрицательными пробами. (3) Красных 50, а не ожидавшихся 49: единица — ранее **скрытый ложно-зелёный** `test_live_xsr155_bare_day`, который был зелён только из-за молчания сторожа и теперь честно красный из группы М1 (472 против 364) — это развилка владельца. (4) Остатки названы в артефакте: докстрока `test_price_gate.py:11` всё ещё несёт ту же ложную атрибуцию 306/599 (тесты трогать было запрещено), и self-update не считает `price_gate.py` своей зависимостью — **выкатки от этой правки не произойдёт само собой**. (5) Тестов и голденов не трогал, ручку источника не трогал, ботов не перезапускал, клиентам ничего не слал.

RESULT: бюджет сторожа свежести починен замером — PRICE_GATE_PROBE_SEC 20с→90с (коммит 3309f3c, запушен): шесть проходов дали лучший 40.1с / худший 43.0с, 90 = двукратный запас от худшего и потолок одной двери; три отрицательных теста на боевой дороге устояли 5/5 (живой источник → СВЕЖЕЕ и цена 298 ฿/день, мёртвая дверь и настоящая задержка 15.5с/проба → цена НЕ названа); набор пути цены 60→50 красных (ушли все 11 сторожевых, +1 ранее скрытый ложно-зелёный), сторожевые юниты 83/83 OK; артефакт docs/artifacts/2026-08-20-freshness-budget-fix.md (8758695) и обе строки журнала записаны.
