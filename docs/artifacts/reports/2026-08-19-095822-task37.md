# Полный отчёт задачи 37 — 2026-08-19 09:58 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5028 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 37 |
| записано | 2026-08-19 09:58:22 |
| символов в отчёте | 5028 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Задача уже была сделана (код `aa2a129`, артефакт `b768097`, оба запушены). Перепроверил независимо: три исхода живые, красная проба валит ровно (б), забор `1403fa2` цел, читатели не задеты. Гейт 3785 тестов / 2 красных — те же два храповика, 993. Кода не менял; артефакт дополнен §9 и двумя поправками по фактам, коммит `cfee568`.

**FACT:** `cfee568` в `git log`, запушен (`b5d5aab..cfee568 main -> main`, `@{u}...HEAD` = `0 0`). Правка-предмет `aa2a129` жива в проде: `pc_orchestrator.py:4788 _loc_between_steps`, звана из `:2271`. Гейт: `Ran 3785 tests in 297.488s / FAILED (failures=2, skipped=11)`.

## Что уже было сделано (артефакт открыт первым действием)

`docs/artifacts/2026-08-19-queue-empty-fix.md`, 16233 байта, коммит `b768097`. Субъект разведки — серверный `devbot._announce_idle` — отсюда недостижим (`git ls-files | grep devbot` → пусто; `tmp/` под `.gitignore:65`), поэтому починен тот же класс на своей полосе: строка `🟢 ТИХО` в `_contour_status`. Это записано честно в §0 и в остатках.

## Дословный вывод проверок

**Три исхода живым рендером** (`_scratch_queueempty_0819/render3.py`, к мосту ноль обращений):

```
=== (а) очередь пуста И целей нет (одни призраки 194/200) ===
🔧 В работе:
  🟢 ТИХО
   between_steps -> []

=== (б) очередь пуста, НО цель 300 открыта (шаг 2/5 сдан) ===
🔧 В работе:
  ⏳ цепь 300: шаг 2/5 сдан, следующий ещё НЕ ПОСТАВЛЕН — работа продолжается
   between_steps -> [{'pid': 300, 'step': 2, 'total': 5}]

=== (в) снимок целей недоступен (Bridge молчит) ===
🔧 В работе:
  очередь недоступна (Bridge молчит)
   'ТИХО' в тексте: False | 'работа продолжается' в тексте: False
```

**Красная проба заново** (старый модуль `aa2a129^` первым в `sys.path`):

```
test_outcome_a_truly_empty_says_tiho ... ok
test_outcome_b_open_goal_forbids_tiho ... FAIL
test_outcome_c_unreadable_says_neither ... ok
AssertionError: '🟢 ТИХО' unexpectedly found in '...🔧 В работе:\n  🟢 ТИХО\n...'
ПАДАЮТ НА СТАРОМ КОДЕ: ['test_outcome_b_open_goal_forbids_tiho']
ЗЕЛЁНЫЕ НА СТАРОМ КОДЕ: ['test_outcome_a_truly_empty_says_tiho', 'test_outcome_c_unreadable_says_neither']
```

Валит **ровно (б)** и самой ложью; (а) и (в) на старом коде зелёные — значит регресс-сторожа, а не переписанное поведение. Все 21 теста класса `TestContourStatusChains` на новом коде — `OK`.

**Замок (полный гейт):**

```
Ran 3785 tests in 297.488s
FAILED (failures=2, skipped=11)
FAIL: test_no_growth_against_baseline
FAIL: test_totals_do_not_grow  →  AssertionError: 993 not less than or equal to 845
```

Красных 2, те же два храповика неразбора, точек 993 = линия. Артефакт называл 3782 — число было верным на момент правки; `+3` пришли чужими тестами задачи 31 (`git diff b768097..HEAD` → ровно 3 новых `def test_`). Число тестов не упало.

**Забор Честертона:** `1403fa2` (13.07) = «статус контура: секция „в работе“ без призраков» — вентиль стоял только по открытым строкам, чтобы не показывать финализированных родителей 194/200/206–208 как живую работу. Опирается на него один читатель, `_loc_chain_status:4968`, и он **не тронут**: правка добавила отдельную функцию, а `_loc_active_chains` не изменена ни на байт.

**Читатели поимённо:** правка — два ханка (`_contour_status:2257` и новая функция после `:4785`); вотчдог `_loc_watchdog_tick:5344` не задет. Все 5 удалённых строк — 2 комментария и каркас `if/else`; литералы `🟢 ТИХО` и `очередь недоступна` целы, поэтому читатель №14 (`result_judge_pc`, судит **дословным** текстом) не перевёрнут. Единственный вызов `_contour_status` — `_exec_command("status"):2303`, отдаёт текст как есть. Машинного разборщика строки нет ни одного.

## Сводка

1. Работа была выполнена ранее и запушена; я её **перепроверил независимо**, а не переделал — кода не менял ни строкой.
2. Все три обязательных отрицательных теста устояли на живом коде и правильно ведут себя на старом: (б) — единственный, кто валится до правки.
3. Замок держится: 3785 тестов, 2 красных = те же два храповика, неразбор 993 = базовая линия.
4. Две поправки по фактам внесены в артефакт: читателей в разведке **15**, а не 16; и записана ловушка — мой первый пробник собрал маркер шага не в живом формате (`[цепь родитель N]` вместо `[шаг i/n родитель N]`) и дал ложное «правка не работает». Врал пробник, класс «мок ≠ живой формат».
5. Остаток без изменений и назван честно: серверный `devbot._announce_idle` как врал, так и врёт — отдельный заход на VPS; `queue_snapshot_pc.py:226` печатает «очередь пуста» из своего снимка открытых статусов, закрыть = 26.8 с на оборот, цена названа и не взята.

Строку в `cowork_log` не писал: доставка вынесена отдельным заходом, а слепой повтор при уже легшей строке задачи даёт дубль в журнале. Файлы `_scratch_queueempty_0819/` оставлены (ничего не удалял), в git не добавлены.

RESULT: правка «пусто только когда пусто» подтверждена независимой перепроверкой — три исхода живые, красная проба валит ровно (б), забор 1403fa2 и все читатели целы, гейт 3785/2 красных (те же храповики, 993); кода не менял, артефакт дополнен §9 и двумя поправками, коммит cfee568 запушен.
