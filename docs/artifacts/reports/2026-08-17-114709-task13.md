# Полный отчёт задачи 13 — 2026-08-17 11:47 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5140 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 13 |
| записано | 2026-08-17 11:47:09 |
| символов в отчёте | 5140 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

**FACT: коммит `757b90b` в `git log` (артефакт `docs/artifacts/2026-08-17-drafts-for-owner.md`, 13 608 симв., 275 строк, 10 блоков) + коммит `0bf712e` (тело журнальной записи); узел мозга `trainer_log` 4 667 → 14 192 симв., проверено ОТДЕЛЬНЫМ обратным чтением: заголовок раздела 1 раз, 10 из 10 строк дословно; строка-индекс в `cowork_log` (проба: 411 629 симв., голова = моя запись, путь артефакта в начале).**

Защита от повтора: артефакта на диске не было, основание `docs/artifacts/2026-08-17-bot-output-path.md` (коммит `aeea8ef`) на месте и содержит замер 470/0 — заход законный.

## Отбор — правило названо до замера

Пул «свежих» = последние **50** строк `drafts` по `created_ts` (`id` 421..470, окно 04.08–17.08). В пуле **21** черновик с названной моделью, **29** без; длины min 68 / медиана 183 / max 1747. Взято: 4 самых свежих **с** моделью (466, 464, 461, 460), 4 самых свежих **без** (470, 469, 468, 467), 2 самых длинных (447 — 1747, 454 — 1741). Недобора нет (нужно 4 при 21 и 4 при 29); ничья 1741/1741 решена в пользу свежей строки. Модель ловится по 12 семействам живого парка + кириллические написания — так поймались две строки с «ФОРЗА».

## Восемь коротких — дословно (обезличено)

| id | группа | черновик |
|---|---|---|
| 466 | модель | `Хорошо, значит nmax 155 по 300 бат в день на 8-10 дней. Подскажите точные даты — с какого числа заезжаете? [собрано: модель ✅ даты ❓]` |
| 464 | модель | `С опытом на CB 650 всё понятно, спасибо 🙏 Подскажите, с какого числа планируете начать аренду — тогда сразу посчитаю точную стоимость на 8-10 дней. Подскажите даты — с какого числа и на какой срок? [собрано: даты ❓]` |
| 461 | модель | `Уточню у команды и вернусь. Подскажите, какие даты и на какой срок планируете аренду? [уточнить: наличие NMAX]` |
| 460 | модель | `На такие деньги можем предложить NMAX 155 — уточните, пожалуйста, даты аренды: с какого числа и на сколько дней?` |
| 470 | без модели | `Меня зовут [ИМЯ-БОТ-1] ☺️ Пишите — подскажу по всем вопросам. Подскажите даты — с какого числа и на какой срок? [собрано: даты ❓]` |
| 469 | без модели | `Меня зовут [ИМЯ-БОТ-2] — обращайтесь ко мне на месте, помогу с байком 🤝 Подскажите даты — с какого числа и на какой срок? [собрано: даты ❓]` |
| 468 | без модели | `Да, точка в [ГЕО] — с 10:00. Сегодня можем принять — на какое время вам удобно подъехать? [собрано: даты ✅]` |
| 467 | без модели | `Да, всё верно — это наша точка в [ГЕО], вот адрес на карте: [ССЫЛКА] ⏎ Подскажите, что хотели бы арендовать и на какие даты?` |

Два длинных (447 и 454) — выкладка прайса целиком, 1747 и 1741 симв.; в артефакте и в мозге обрезаны на 400 с пометкой и полной длиной. Их первые 400 символов совпадают дословно — это следствие правила отбора, а не ошибка копирования. Запрос клиента, судьба каждой строки и полные блоки — в артефакте.

Судьба у всех десяти одинакова фактом: карточка ушла в модерацию (`card_msg_id` есть у всех), правки нет (`final_text` пуст), решения в базе нет (`decided_by` пуст), клиенту не отправлен, заявки «БРОНЬ» по этим клиентам нет. **Чем кончился диалог — НЕИЗВЕСТНО:** база держит реплики окна, а не исход. Оценок черновиков нет ни одной — вердикт за вами.

## ПДн и боевая база

Замен **7**: `[ГЕО]`×4, `[ССЫЛКА]`×1, `[ИМЯ-БОТ-N]`×2. Имя, которым бот называет **сам себя**, скрыто индексом, но индексы **разные** — факт «два разных имени в двух соседних черновиках» остался судимым, а имена наружу не вышли. Серий 13–19 цифр — ноль (длинные числа прайса это тарифы и депозиты); присланный клиентом адрес в пересказ не переносился вовсе.

База открыта строго `mode=ro`; размер **1 728 512** байт до и после. sha256 разошёлся (`8598…4936` → `C485…C1B3`) — и это **не след чтения**: контроль без единого обращения к базе дал за 7 секунд ещё два разных хеша при том же размере (живой модербот переписывает файл пятисекундным тиком). Неизменность доказана иначе: `mode=ro` не умеет писать по устройству, счёт после чтения 470 / `max(id)` 470 как до, разрез статусов 434/21/15 совпал с замером основания.

## Остатки, честно

1. Строка журнала вышла **609** символов при пороге 600 — писатель сам вынес тело в `docs/artifacts/journal/2026-08-17-114535-done.md`; файл рождается untracked, поэтому закоммичен отдельно (`0bf712e`), иначе указатель из мозга вёл бы в дерево, а не в историю.
2. В теле сообщения коммита `757b90b` осталась опечатка «коммит aeef… aeea8ef» — верный хеш в строке есть, коммит не переписывал.
3. `_scratch_drafts_0817/` оставлен как велено; в нём лежит `raw_selected.json` с **сырыми** текстами (уборка запрещена заходом) — в git не идёт.
4. Бот не запускался, правила/промпты/книга правил и `price_source.json` не тронуты, клиентам не писали, в рабочие таблицы не писали, ничего не удалялось.

RESULT: выписаны 10 живых черновиков (правило: свежие 50 строк drafts → 4 с моделью, 4 без, 2 самых длинных), артефакт docs/artifacts/2026-08-17-drafts-for-owner.md (коммит 757b90b) + раздел «ЧЕРНОВИКИ ДЛЯ ВЕРДИКТА 17.08» в узле trainer_log (4667→14192 симв., 10/10 дословно по обратному чтению), замен ПДн 7, боевая moderation_ipc.db не менялась (mode=ro, 1728512 байт до и после), строка-индекс в cowork_log записана.
