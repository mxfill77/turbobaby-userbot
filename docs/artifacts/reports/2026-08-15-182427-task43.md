# Полный отчёт задачи 43 — 2026-08-15 18:24 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4679 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 43 |
| записано | 2026-08-15 18:24:27 |
| символов в отчёте | 4679 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Опись выполнена: 1278 строк, 23 поля. Даты оформления НЕТ, отметки предоплаты НЕТ, `booking_id` пуст у 1278 из 1278 (подтверждено). Шаги 5 и 6 — **НЕ СУДИМО**. Из трёх причин одной подписи полем обеспечена ровно одна (сезон); две другие в базе ненаблюдаемы. Мутаций ноль, ПДн наружу не выносились.

**FACT:**
- `FACT: commit 311d63c в git log` — артефакт `docs/artifacts/2026-08-15-booking-fields-census.md`
- `FACT: commit e88175c в git log` — тело журнальной записи (авто-вынос по LINE_MAX)
- `FACT: read-only` — единственный экшен `GET clients filter=all`, три захода; `create_booking`/`close_booking`/`add_transaction` не звались; в лист ни одного ввода
- `FACT: журнал` — писатель вернул `OK: записано в мозг, символов: 372710`, код возврата 0
- `FACT: живые выводы` — `_scratch_bookfields_0815/{probe_output,meaning_output,control_output}.txt`

## Ответы по шагам

**ШАГ 1 — 23 поля на 1278 строк.** Имена как есть: `auto_cancel, bike, booking_id, contacts, date_end, date_start, days_overdue, debt, deposit, deposit_raw, duration, helmets, is_active, is_fresh, is_overdue, name, note, paid, pay_per_day, row, status, to_pay, type`. Заполненность по каждому — таблица в артефакте §1. Оговорка, которую я держу явной: **пять ключей** (`row`, `is_active`, `is_fresh`, `is_overdue`, `days_overdue`) колонками не подкреплены — производные; `deposit`/`deposit_raw` — два вида одной колонки. За колонками стои́т ~17 ключей из 23.

**ШАГ 2 — даты оформления НЕТ.** Признак «похоже на дату» гонялся по всем 23 ключам: совпало ровно два — `date_start` (1274) и `date_end` (1277). `is_fresh` проверен отдельно и **опровергнут как скрытая дата**: `True` — это `date_start ≥ 2025-08-17`, `False` — `≤ 2025-08-16`, разрез без перехлёста ровно на «сегодня−365»; по номерам строк множества перекрываются (427..1279 против 2..1217). Косвенные заменители названы и за дату не выданы: порядок строк (118 шагов назад из 1273, p50 12.5 сут — лист не отсортирован, похоже на приписывание в конец, но это порядок, а не дата), `booking_id` (пуст), «Календарь» (истории положений нет).

**ШАГ 3 — отметки предоплаты НЕТ.** Полей «предоплата»/«аванс»/«статус оплаты»/«оплачено полностью» нет ни одного. `paid` 1278/1278 (>0 у 1256), `to_pay` >0 всего у 10, `debt` — зеркало `to_pay` (1267 из 1278), `deposit`/`deposit_raw` — залог, а не аванс. Поля описывают **итог расчёта**, и **даты платежа нет ни у одного**. Словами тоже ноль: словарь из 10 выражений по `note` (315 непустых) дал 0, и **ноль проверен на ложность** — буква видна у 315 из 315, кириллица у 306.

**ШАГ 4 — booking_id пуст у 1278 из 1278: ПОДТВЕРЖДЕНО.** Прочих полей с нулевой заполненностью нет. Но рядом честно стоя́т два разряда, которые процент заполненности прячет: **`auto_cancel` — «OFF» во всех 1278** (заполнено, не различает ничего), и почти-константы `days_overdue` (≠0 у 4), `is_overdue` (4), `to_pay` (10), `debt` (10), `is_active` (11).

**ШАГ 5 — НЕ СУДИМО** (поля даты оформления нет, вычитать не из чего). **ШАГ 6 — НЕ СУДИМО** (требовались оба условия, нет ни одного; группы не натягивались, порог n≥3 не трогался).

## Что это значит

Сезон разделяется `date_start` (99.7%). «Ручки не переставили» и «скидка за раннюю бронь с предоплатой» — **ненаблюдаемы ни одним из 23 полей**, и это ограничение данных, а не метода. Выводов о забывчивости менеджеров не делал.

## Две вещи, о которых докладываю отдельно

1. **Осталось открытым:** полный список колонок листа первой рукой не проверен — Apps Script вне этого дерева (`grep CLIENT_COLS` по репо → 0). Два вторых свидетельства не сходятся арифметически (25 колонок минус «ровно три неотдаваемых» дало бы 22 колоночных поля против насчитанных 17). Поэтому отрицание «даты оформления нет» строго относится к 23 полям моста, а не к листу.
2. **Журнал писался дважды.** Первая строка (18:22 UTC) легла успешно, но ушла в авто-вынос: мой замер 629 не учёл, что `stamp_line` добавляет +22 — писатель намерил 651 при пороге 600, и в мозг попал путь к **телу записи**, а не к артефакту. Дописал короткую строку (423 символа, легла без выноса) с путём артефакта. Это не слепой повтор — первая строка не терялась.

Запреты соблюдены: ничего не удалено, временное только в `_scratch_bookfields_0815` (каталог оставлен), `.env` и конфиги не читались, процессы не трогались, клиентский бот/прайс/тренажёр не запускались.

RESULT: опись полей базы броней выполнена read-only — 1278 строк, 23 поля; даты оформления НЕТ, отметки предоплаты НЕТ, booking_id пуст у 1278 из 1278 подтверждено, auto_cancel константа «OFF»; шаги 5-6 и разделимость причин — НЕ СУДИМО; артефакт docs/artifacts/2026-08-15-booking-fields-census.md, коммиты 311d63c и e88175c, строка-индекс в журнале.
