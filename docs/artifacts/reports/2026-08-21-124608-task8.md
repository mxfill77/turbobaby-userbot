# Полный отчёт задачи 8 — 2026-08-21 12:46 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **6130 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 8 |
| записано | 2026-08-21 12:46:08 |
| символов в отчёте | 6130 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Задача уже отвечена 19.08 — артефакт `docs/artifacts/2026-08-19-deposit-rules.md` существует и закоммичен (532aadb). Ответ: правило ЗАПИСАНО — таблица «менеджеру Байки», лист «цены альт», колонка H «депозит», строка = модель. Находка ревизора ложная: 3000/5000/7000/15000/20000/25000 — не противоречие одной сделки, а шесть ступеней лестницы по разным моделям. Ничего не перепроверялось и не менялось.

**FACT:** read-only. Живое доказательство сделанности — `git log --oneline -- docs/artifacts/2026-08-19-deposit-rules.md` → `532aadb ARTIFACT где записан депозит по моделям -> docs/artifacts/2026-08-19-deposit-rules.md: лист «цены альт», колонка H`; `git status --porcelain` по файлу → пусто (закоммичен, дерево чисто); размер 21030 символов. Новая журнальная строка: `OK: записано в мозг, символов: 527680`. Ни одной ячейки рабочих таблиц не тронуто, кода не правлено, процессов не трогалось, `.env` не читался.

---

## Признак сделанности — проверен ПЕРВЫМ действием

```
$ ls -la docs/artifacts/2026-08-19-deposit-rules.md
-rw-r--r-- 1 mxfill1 197121 21030 Aug 19 21:54 docs/artifacts/2026-08-19-deposit-rules.md
---EXIT=0---
```

Файл есть. По постановке это означает «уже отвечено» — открываю и докладываю, разведку заново не запускаю.

```
$ ls docs/artifacts/ | grep -i -E "deposit|depoz|депозит"
2026-08-08-class92-deposit-false-finding.md
2026-08-19-deposit-ladder.md
2026-08-19-deposit-rules.md
```

Тема закрыта **тремя** артефактами, а не одним: причина ложной находки (08.08), где записан депозит (19.08) и сама лестница со сверкой ревизора (19.08).

```
$ git log --oneline -3 -- docs/artifacts/2026-08-19-deposit-rules.md
532aadb ARTIFACT где записан депозит по моделям -> docs/artifacts/2026-08-19-deposit-rules.md: лист «цены альт», колонка H
---STATUS---
---(пусто = закоммичен, чистый)---

$ wc -c docs/artifacts/2026-08-19-deposit-rules.md docs/artifacts/2026-08-19-deposit-ladder.md docs/artifacts/2026-08-08-class92-deposit-false-finding.md
21030 docs/artifacts/2026-08-19-deposit-rules.md
14753 docs/artifacts/2026-08-19-deposit-ladder.md
30594 docs/artifacts/2026-08-08-class92-deposit-false-finding.md
66377 total
```

---

## Что в артефакте отвечено — по пунктам постановки

Артефакт покрывает все шесть требований дословно; ниже — свод, полный текст в файле.

**1. Таблицы и листы.** Четыре книги: `менеджеру Байки` (`MANAGER`, id `1sL-rw0k…JgL0`) — листы `клиенты`, `Лист28`, `Календарь бронирования`, `учет байков`, **`цены альт`**, `список мото`, `ПАМЯТКА`, `Ответы на форму (3)`, `Ответы на форму (4) не актуальн.`, `Доставка`; `байки` (`FLEET`, id нигде не назван) — `Лист1`; `Цены` (`PRICES`) — объявлена, но **ни одна строка кода моста её не читает**; таблица зарплат (`SALARY`). Оговорка честная: **двери-перечислителя листов у моста НЕТ** — 37 живых проб имён дали `unknown_action`, поэтому полное число вкладок НЕ измерено.

**2. Где слово «депозит».** В трёх разных ролях, и их слипание — корень путаницы ревизора: (а) эталон по модели — `цены альт`, колонка **H**; (б) факт по сделке — лист `клиенты`, поле `deposit`/`deposit_raw` (буква колонки не установлена); (в) проза правила — узлы мозга `faq`, `knowledge_base`, `business_rules`, `booking_flow` и `CRITICAL_FACTS` в коде. Это зеркала, не источник.

**3. Модель или сделка — к обеим, но по-разному.** Эталон привязан к МОДЕЛИ (одна строка = одна модель = одно число, больше одного быть не может по устройству листа). Факт привязан к СДЕЛКЕ (свободное поле: число ЛИБО «паспорт»). **Сверки между ними нет нигде в коде** — `booking_draft.classify_deposit()` различает только `money|passport|conflict|none`.

**4. Соответствие по 1278 сделкам.** Сумм у одной модели много (от 2 до 16), но у каждой есть выраженная модальная, и она **совпала с эталоном колонки H у 13 моделей из 15**. У `NMAX 155` — 262 сделки из 290 (90%) ровно на 3000. Разошлись двое: `XSR 155` (мода 8000 против эталона 7000) и `REBEL 300` (мода 10000 против 15000).

**5. Лист цен и календарь.** В `цены альт` колонка депозита есть — это и есть H. В `Календарь бронирования` своей колонки депозита **НЕТ**: депозит там показывается `ARRAYFORMULA` в `J5` со ссылкой `цены альт!H$<row>` — календарь его не хранит, а **цитирует**. В `Лист1` книги «байки» колонки депозита нет, что совпадает с наблюдением владельца.

**6. ВЫВОД (первый из трёх вариантов).** **Правило записано:** `менеджеру Байки` → `цены альт` → колонка **H** «депозит», строка = модель (колонка A). Не «выводится из сделок» — по сделкам мода дала бы НЕВЕРНЫЙ ответ на двух моделях из пятнадцати.

---

## Свод (3–5 строк)

1. Разведка сделана 19.08, ответ лежит файлом и в git — повторный заход не нужен, я его сознательно не делал.
2. Владелец прав: депозит приписан модели, живёт в колонке H листа `цены альт`, читается дверью `quote_price`.
3. Находка ревизора ложная и её причина названа ещё 08.08 и **не исправлена**: `suggest.py:6512` сравнивает суммы по всему тексту сразу, не привязывая сумму ни к модели, ни к брони, тогда как соседняя ветка уже умеет сужаться до фразы (`_clause_at`, `:6516`).
4. Открытый вопрос **владельцу** (не дефект разведки): по `XSR 155` и `REBEL 300` эталон H расходится с практикой — либо число в листе устарело, либо по этим двум моделям систематически берут не по прайсу.
5. Не измерено честно: колонка H живьём сегодня не перечитана (легального канала нет — `pricing.py` берёт токен из окружения, а читать секрет своим кодом запрещено постановкой); полное число листов `MANAGER` не измерено.

Новый артефакт не создавался намеренно — постановка требует писать его последним действием, но требуемый файл уже существует, и второй документ на ту же тему был бы дублем. Вместо него в журнал легла строка-указатель.

RESULT: задача уже была отвечена 19.08 — открыт и доложен артефакт docs/artifacts/2026-08-19-deposit-rules.md (коммит 532aadb, 21030 симв., дерево чисто); правило ЗАПИСАНО: «менеджеру Байки» → лист «цены альт» → колонка H «депозит», строка = модель, читает quote_price, цитирует «Календарь бронирования»!J5; находка ревизора ложная (шесть ступеней лестницы, а не противоречие); read-only, ни одной ячейки не изменено, журнальная строка записана.
