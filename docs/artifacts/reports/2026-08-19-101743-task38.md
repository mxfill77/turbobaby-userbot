# Полный отчёт задачи 38 — 2026-08-19 10:17 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4511 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 38 |
| записано | 2026-08-19 10:17:43 |
| символов в отчёте | 4511 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Дедуп карточек ворот уже был починен (`70635de`) — я перепроверил правку живыми командами, а не перечитыванием отчёта: реплей шести пар 0/6→6/6, оба отрицательных теста устояли, красная проба валит ровно (а), гейт 3785/красных 2 (те же храповики). Добавил недостающий замер: 7-символьных коллизий 0 из 610 коммитов. Артефакт дополнен §7, всё запушено.

**FACT:** commit `e3a19f1` (артефакт §7) и `a45a402` (тело журнала) в `git log`, оба запушены — `git rev-list --count origin/main..main` → **0**. Правка кода: `70635de` в `git log`, дерево чистое.

## Признак сделанности

`docs/artifacts/2026-08-19-gate-dedup-fix.md` **существовал** (16207 байт, 11:48). Открыл: правка сделана прошлой сессией коммитом `70635de`. Наличие файла не остановило — перепроверил каждое утверждение живыми командами.

## Что изменено — одна боевая строка

```
$ git show 70635de -- pc_orchestrator.py   (вне комментариев)
-    key = (str(commit), tuple(held))
+    key = (client_contour.short(commit) or str(commit), tuple(held))
```

`pc_orchestrator.py | 14 +++-` = 13 строк комментария + 1 строка кода. Тестов добавлено ровно 3. Логика ворот, тексты карточки, число входов не тронуты: `commit` идёт в `log.error`, `cowork` и `card_text` (включая `git revert`) **сырым** — нормализация живёт только в ключе.

## Забор Честертона

`short = head[:9]` (`pc_orchestrator.py:6284`) внесён `6d8a3b1` **11.07.2026** — за 19 суток до ворот (30.07). Это **подпись для показа**, не ключ. Девятка одна и ровно у реконсиляции, потому что она берёт `_full_head()` (40 симв.) и режет сама, а два других входа просят у git `rev-parse --short` (7):

```
5912  maybe_update_bots            → _head_commit()  → 7
6026  _selfupdate_restart_children → new_commit      → 7
6293  reconcile_children_tick      → head[:9]        → 9   ← стои́т вплотную к self-update
```

Не сломает: единственное сравнение коммитов во всём файле —
`5980: if not old_commit or not new_commit or old_commit == new_commit:` (внутри `_diff_names`, обе стороны из одного источника), правкой не задето.

## Отрицательные тесты — оба устояли

```
test_dedup_odin_kommit_dvumya_dorogami_odna_kartochka ... ok     (а) один коммит → ОДНА карточка
test_dedup_raznye_kommity_ne_skleivayutsya ... ok                (б) разные коммиты → ДВЕ
test_dedup_ne_hesh_ostaetsya_soboi ... ok                        (в) фолбэк на не-хеше
Ran 17 tests in 0.602s / OK
```

Красная проба (`short → ''` = прежний ключ байт в байт): падает **ровно (а)**, (б) и (в) зелены — голден кусается, а не подогнан.

## Проверка на истории — 6 из 6

```
СТАРЫЙ ключ:  ... -> карточек 2  (все шесть)     схлопнулось пар: 0 из 6
НОВЫЙ ключ:   ... -> карточек 1  СХЛОПНУЛОСЬ     схлопнулось пар: 6 из 6
```

Не схлопнувшихся нет — называть нечего. Все шесть хешей проверены как настоящие коммиты репо.

## Замок

```
Ran 3785 tests in 257.224s
FAILED (failures=2, skipped=10)
FAIL: test_nonparse_ratchet.TestRatchet.test_no_growth_against_baseline
FAIL: test_nonparse_ratchet.TestRatchet.test_totals_do_not_grow
```

Красных **2**, и это **те же два** храповика неразбора (отдельный прогон модуля: `Ran 19 … failures=2`). Число тестов не упало: 3770 → 3773 → 3785 (рост принесли задачи 30/31, не эта правка).

## Сводка

1. Правка была сделана до меня; я её **перепроверил, а не переделал** — воспроизвёл реплей, красную пробу, гейт своими прогонами.
2. Цель выполнена ровно одна: обе стороны ключа приводятся к одной длине. Боевых строк изменено **одна**.
3. Оба обязательных отрицательных теста устояли, включая важнейший (б) — нормализация разные операции не склеивает.
4. **Новое**, чего в артефакте не было: остаточный риск склейки по 7 символам был рассуждением — замерил, **0 коллизий из 610 коммитов**, git сам всё ещё отдаёт 7 симв. Честный остаток: git удлиняет `--short` при неоднозначности, `short()` режет жёстко — сторожа нет, заводить не просили.
5. Поправка к §5 артефакта: знаменатель гейта уехал 3773 → 3785 за день из-за соседних задач; условие «не падает» выполнено.

Журнал: обе строки (ARTIFACT и DONE) **подтверждены обратным чтением**. У DONE потеряна расписка (known-класс второго плеча моста) — слепой повтор запрещён и не делался. Тело выноса закоммичено `a45a402`.

RESULT: правка дедупа ворот (70635de) перепроверена независимо и признана годной — реплей 0/6→6/6, оба отрицательных теста устояли, красная проба валит ровно (а), гейт 3785/красных 2 (те же храповики); добавлен замер 7-символьных коллизий 0/610; артефакт дополнен §7, коммиты e3a19f1+a45a402 запушены, журнал записан.
