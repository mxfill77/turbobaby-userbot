# Полный отчёт задачи 12 — 2026-08-21 14:46 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5164 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 12 |
| записано | 2026-08-21 14:46:50 |
| символов в отчёте | 5164 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Разделение потерялось одной регуляркой в счётчике базы: `model_of()` разбирает имя юнита только до `CC`, а слово `NEW` стоит правее — оба поколения падают в ключ `XMAX 300`. Источник на диске поколения РАЗВОДИТ. Премиса «790/939 бат в сутки» неточна: это колонка B листа, цена суток — 593/704. Разделение измерено впервые: OLD 560 / NEW 641.

**FACT: commit `3ee6c6b` в git log** — `docs/artifacts/2026-08-21-xmax-generations.md`, 487 строк, 1 файл. Кода не тронуто (`git status -- '*.py'` → только преждесущие `_scratch_*`, все untracked). Замер воспроизведён: повтор счётчика по сохранённому срезу дал слитую медиану **623 = ровно как в файле**, значит счёт верен.

**Премиса 0:** артефакта `2026-08-21-xmax-generations.md` не существовало — работа выполнена.

**1. Записей про XMAX 300 в правиле — три, в двух блоках.** `base.models`: **ОДНА** — `"model":"XMAX 300", "n_bookings":44, "base_thb_per_day":623, "p25_p75":[500,701]`, поля `generation` нет (0 из 13 строк). `low_season_caps.models`: **ДВЕ** — `generation "2020-2022"`/`"2023-"`, `sheet_model "YAMAHA XMAX300 2020-2022"`/`"YAMAHA XMAX 300 NEW 2023-"`, капы 8900/9900. Базы у капов нет. **Файл сам себе противоречит:** блок капов пишет «поколение брони различается словом NEW в имени байка», блок остатков — «имя байка поколения не несёт».

**2. Источник найден, поколения ТАМ разведены.** `_scratch_basePrice_0816/rows.json` (494 588 Б, untracked, 1278/1243 — совпадает с `provenance` правила дословно). Поле `bike`: `XMAX 300CC NEW BLACK PHUKET 8969` против `XMAX 300CC BLUE PHUKET 5773`; рядом `fleet.json` даёт `year: 2023` против `2019`. **Шаг слияния:** `_scratch_basePrice_0816/lib.py:11` — `_RE_MODEL = ^\s*(.+?)\s*(\d{2,4})\s*[CСcс]{2}\b`, `.match()` потребляет строку до `CC`, а `NEW` стоит после. Данные не терялись — терялся КЛЮЧ: `lib.load()` кладёт сырое `bike` наружу, отброс произошёл при группировке. Ложная посылка «имя байка поколения не несёт» размножена в **3 места**; при этом 17.08 тот же файл поколения РАЗВЁЛ (`money_check` капов: 5 старых / 7 новых).

**3. 790/939 — числа другой природы.** Это колонка **B** (базовая ставка). `C = B × (1−0.25)`: `593/0.75 = 790.67`, `704/0.75 = 938.67` — сходится до бата. Цена суток низкого сезона — **593/704**. На одном сроке (8 сут, кривая листа 7 %): лист даёт **551/655**, правило — 623 обоим, т.е. **старое завышено на +13.0 %**, новое занижено на −4.9 % (а не −11.5 %). Отдельно: 790/939 живут ценой суток в боевом `suggest.py:283-284` (`CRITICAL_FACTS`) — четвёртое место, где поколения разведены.

**4. Цепочки.** 623: `rows.json` → `lib.model_of` (слияние) → фильтр `2024-01`+июнь-октябрь, n=44 → приведение к корзине 7-13 (`LADDER 97/103/100/82/53`) → медиана 623 → `base.models` → `base×season×term` = 623×1.0×1.0 = **623 ฿/д, 4984 ฿ за 8 сут** обоим. 704: строка листа `YAMAHA XMAX 300 NEW 2023-` → B=938.67 → `×(1−0.25)` → **C=704**, снято `quote_price` GET 15.08. **Правило живо: `price_source.py` — «ИСТОЧНИК ПЕРЕКЛЮЧЁН 20.08.2026 — ветка живёт БЕЗ ФЛАГА», `enabled()` при неустановленной ручке → ВКЛЮЧЕНО.** Строка `status.verdict` «НЕ ПОДКЛЮЧЕНО» протухла.

**5. Деньгами (44 брони, 956 суток).** OLD n=11 → база **560** (опора n=3, 5 из 11 месячные); NEW n=33 → база **641** (опора n=12). Разрыв денег +14.5 % против +18.7 % прайса. Кассы: факт **401 523 ฿**, правило слитое **392 858**, лист **457 768**, правило раздельное **387 471**. Против листа правило теряет **−64 911 ฿ (−14.2 %)**. Но против уплаченного слитое 623 промахивается всего **−2.2 %**, а раздельное — **−3.5 %, хуже**: NEW чинится в ноль (+0.0 %), OLD ломается (−10.9 %).

**6. Итог — правка (НЕ сделана).** (а) `base.models`: одну строку на две, зеркалом капов (`generation`+`sheet_model`+`unit_name_marker`). (б) **МИНА:** наивное добавление строки ГАСИТ цену новому поколению — `resolve_row:188` (`k in nk`) даст для `xmax300newblack…` два попадания (`xmax300` и `xmax300new`) → строка 194 → `None` → фолбэк без числа, 75 % корпуса. Разводить надо явно по `quote["bike"]`, который `resolve_row:182` уже получает; замок готов в `cap_for:250-253`. (в) **Число OLD — развилка владельца:** кандидаты 560 / 571 / 593 / 623, разброс 33 %; `owner_decision` в файле пуст. Голдены: **9 тестов в 3 файлах** — 2 красных станут зелёными по форме (но ждут 790/939 = колонку B), 4 в `test_suggest` пересчитать, 2 сквозных в `test_moderation`, 3 места `test_price_source` ломает часть (б).

Журнал в мозг не писал — запрещено постановкой (ритуал `cowork_log` пропущен сознательно).

RESULT: разделение поколений XMAX 300 теряется в `_scratch_basePrice_0816/lib.py:11` — регулярка `model_of()` режет имя юнита по `CC`, а маркер `NEW` стоит правее; источник (rows.json, поле bike + fleet.json year) поколения разводит, правило `price_source.json` — нет (1 строка базы 623 против 2 строк капов 8900/9900). Премиса уточнена: 790/939 — колонка B, цена суток 593/704. Разделение измерено впервые (OLD 560 n=11 / NEW 641 n=33) и показало, что наивная правка ухудшает попадание в уплаченные деньги (−2.2 % → −3.5 %); названа мина `resolve_row:188`, гасящая цену новому поколению. Артефакт `docs/artifacts/2026-08-21-xmax-generations.md`, коммит 3ee6c6b, кода не тронуто.
