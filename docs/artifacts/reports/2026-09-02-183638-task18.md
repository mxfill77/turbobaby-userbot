# Полный отчёт задачи 18 — 2026-09-02 18:36 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4701 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 18 |
| записано | 2026-09-02 18:36:38 |
| символов в отчёте | 4701 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Замер выполнен целиком: на кандидате `27d0f8d` снято 20 единиц из 20 — **язык красен 0 из 20** против базы 3 из 20. Дымовой русский круг чист (10/10). Вердикт по объявленному заранее правилу: правка варианта A **доказана** (p=3.9%). Артефакт закоммичен и запушен.

**FACT:** commit `42687d8` в `git log` — `docs/artifacts/2026-09-03-замер-языка-после-правки-вариант-A-вердикт.md`, запушен (`c97a30d..42687d8`, затем `42687d8..1454db0`). Кода не правилось ни байта.

---

## Признак сделанности (первым действием)
Артефакта-адреса за 03.09 со словами «замер языка после правки вариант A вердикт» **не было** — в каталоге лежал только другой документ, база `2026-09-03-база-языка-при-живой-цене-число-провалов.md`. Файл накопленных единиц в `_scratch_lang_a/` **отсутствовал**: заход 17 успел сделать правку и коммит, до замера не дошёл (его черновик это говорит дословно). Накопленных единиц 0 → начато с «круг 1, кейс 8».

## Премиса (п.1) — подтверждена
Правка лежит в `27d0f8d` (2 места в `make_system_prompt`), `git status --porcelain` по `suggest.py model_name.py price_source.json userbot_listen.py moderation_bot.py moderation_core.py` — пусто. Грязными числятся только `docs/review_*` и `todo.md`, кода среди них нет.

## Кандидат (п.2)
`27d0f8d` зафиксирован `trainer_run.bind_head()` на входе и сверен `verify_head()` на выходе **каждого** из четырёх заходов (EN круг 1, EN круги 2–3, EN круг 4, RU смоук) — вершина не двигалась ни разу. `corpus_sha 6d5d78f0d83acdca` — тот же набор, что у базы.

## Прерываемость (п.3)
`_scratch_lang_a/run_units.py`: единица «круг, кейс» пишется на диск с `fsync` немедленно, а перед стартом сделанные пары читаются и **не повторяются**. Работа механизма видна в логах: второй заход напечатал 5 строк «уже на диске», третий — 15. Файл результатов — `_scratch_lang_a/units_en.jsonl` (плюс `units_ru_smoke.jsonl`, сводки и логи).

## Дымовой прогон (п.4) — ворота открыты
| набор | единиц | результат |
|---|---|---|
| RU первый контакт (1,2,3,4,5,6,7,10,11,12) | 10 | **10/10 `ok=True` целиком**, красных чеков 0 |
| EN круг 1 (он же круг 1 замера) | 5 | 5/5 язык зелён |

Русский путь правкой не сломан.

## Таблица кругов (п.5)
| круг | единиц | язык красен | зелён | неизвестно |
|---|---|---|---|---|
| 1 | 5 | 0 | 5 | 0 |
| 2 | 5 | 0 | 5 | 0 |
| 3 | 5 | 0 | 5 | 0 |
| 4 | 5 | 0 | 5 | 0 |
| **итого** | **20** | **0** | **20** | **0** |

622 с, 31.1 с/ед. Красных чеков **любого** рода за весь набор — 0 (у базы был один вид: «язык ответа», 3 шт.).

## Условия сравнимы — числом, а не словом
Вставок кода **244/244**, из них русских **52/52**, кириллических букв в них **1324/1324** — совпало с базой посимвольно по всем шести строкам. Ноль снят при том же объёме русского в ответе, а не на мёртвой цене.

## Вердикт приёмки (п.6)
База 3/20 → 0.15 на единицу. `P(0 из 20) = 0.85²⁰ = 0.0388 = 3.9%` — ровно «четыре случая из ста» задания. Кандидат: **0 из 20** → **правка варианта A ДОКАЗАНА** объявленным правилом.

## Разбор остатков (п.7)
Провалов нет, поэтому счёт прогнан по всем 20 единицам: **кириллица от головы — 0 букв в 0 единицах из 20** (база — 551 буква в 3 единицах). Вся кириллица в EN-ответах принадлежит вставкам кода. Контраст на кейсе 14, круг 2: база — «Здравствуйте! Спасибо, что выбрали нас 🤝 / Наш парк: скутеры…»; кандидат — «Hello! Thanks for reaching out to TurboBaby 🤝 / We have scooters…», кириллицы 0.

## Честные остатки
- **Механизм не доказан.** Русские вставки остались в том же количестве, изменился только промпт — правило работает на послушании головы, а не на устранении причины.
- **3.9% — не ноль.** При истинной частоте 5% ноль из 20 выпадает в 36% случаев.
- **Знаменатель кейса 15:** цены в нём нет по замыслу, только по единицам с ценой — 0 из 16 против 3 из 16.

## Запреты соблюдены
Ничего не удалено; временное — только `_scratch_lang_a/`, в коммит не входит; кода не правилось ни байта; `suggest.py`, `model_name.py`, `price_source.json`, userbot, moderbot, корпус — не тронуты; дети не перезапускались; боевая `moderation_ipc.db` уведена в temp штатным `isolate_ipc()`; клиентам не писали; `.env` и конфиги не читались; процессы не трогались. Журнал: `ARTIFACT` и `DONE` легли в мозг, тело вынесено писателем и закоммичено (`1454db0`).

RESULT: замер языка после правки варианта A закрыт — кандидат 27d0f8d дал 0 языковых провалов из 20 при живой цене против базы 3 из 20 при посимвольно тех же условиях (244 вставки кода, 52 русских, 1324 буквы), кириллица от головы 0 в 0 единицах, дымовой русский круг 10/10 чист; вердикт по объявленному заранее правилу — правка ДОКАЗАНА (p=3.9%); артефакт docs/artifacts/2026-09-03-замер-языка-после-правки-вариант-A-вердикт.md, коммит 42687d8 запушен, журнал записан.
