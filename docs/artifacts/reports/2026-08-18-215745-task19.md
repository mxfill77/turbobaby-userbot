# Полный отчёт задачи 19 — 2026-08-18 21:57 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5232 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 19 |
| записано | 2026-08-18 21:57:45 |
| символов в отчёте | 5232 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Разведка закрыта: правила бота живут одним md-файлом вне git плюс write-only сайдкар источника; версии и автора нет вовсе, откат есть только для одного правила по плывущему номеру, а на росте ломается не промпт, а тишина — кап 100 молча стирает уроки, возвращая «added». Артефакт записан, боевые файлы правил не тронуты (sha совпал до и после).

**FACT:**
- Артефакт на диске: `docs/artifacts/2026-08-19-training-base-recon.md`, 34220 байт, 406 строк (`ls -la` + `wc -l`).
- Read-only подтверждено хешом боевых наборов правил ДО и ПОСЛЕ всех проб — идентичны:
  `88e731788784b1fe9315d84d42c60c257cd30b82fdbc697ed6fb08fcc8ffc6dd  manager-bot/docs/playbook.md`
  `bee2d75f81b916ab83796442a70b7350cf348e498865a2fdbe5a09d68a1494e4  trainer_rules.json`
  `git status --short` по обоим — пусто.
- Журнал: `OK: записано в мозг, символов: 456865 | тело вынесено в docs/artifacts/journal/2026-08-18-215645-done.md`
- Живая проба канала мозга (только чтение): `brain_writer.py --probe --name rules|business_rules|trainer_log` → `PROBE ok` 16857 / 26636 / 14192 символов.

**Ответы по пунктам**

**1. Где живут правила.** Один markdown-файл `manager-bot/docs/playbook.md`, секция `## Выученные правила`, строка = `- (ГГГГ-ММ-ДД) <текст>` (`suggest.py:668,687,736`). Замер: 2844 симв. всего, 9 правил, 1918 симв. в секции. Сбоку — `trainer_rules.json`, плоский dict `{нормализованный текст: "тренажёр"}` (`trainer.py:63-64,279`), 6 ключей. **Узла мозга под правила бота нет**, и два имени соблазняют ошибиться: `rules` — это `KB_RULES`, свод правил работы агентов; `business_rules` в своей же шапке пишет «клиентский контур обращается сюда же», а греп боевых модулей даёт **ноль чтений** (оба совпадения — прозаические ссылки-основания в комментариях `price_freshness.py`).

**2. Версия и происхождение.** Версии нет (`list_playbook_rules` → ровно `{'n','date','rule'}`). **Автора нет никогда**: сток принимает голую строку — `_default_append_style(rule)` → `append_playbook_rule(rule)`, зов `a_style(remark)`; разбор урока строит `{"remark","kind","who","window","card_msg_id","draft"}`, и до записи не доезжает ничего кроме `remark`, даже дата (`now=` не передаётся). Замер: дата у 6 из 9, источник у 5 из 9, **1 сирота в сайдкаре** (`'уже было'` — пометка без правила).

**3. Откат.** Одно правило — можно: `cancel_lesson(N)` → `remove_playbook_rule` + `unmark_source`, доказано прогоном (`removed`, «осталось правил: 2»). Но **номер поплыл в том же прогоне** (#3 стало #2), посмотреть номер негде (`/rules` написана, но у модербота нет ни одного `CommandHandler`), а `git checkout` неприменим — файл вне git. **Отката набора по источнику нет**: 8 проб `hasattr` на `remove_by_source`/`rollback_source`/… → все False, и живой `mark_source` пишет только константу.

**4. Куда класть.** Лучший образец схемы — `price_source.json`: `version` + `built_on` + у каждой записи `source`/`measured_on` + `provenance`, всё под git. Рекомендация: положить базу рядом с `trainer_rules.json` (корень репо, под git — доказано коммитом `95077e4`), а `playbook.md` оставить производной проекцией. Полная таблица кандидатов с плюсом/минусом одной строкой (включая `revizor_checklist.md`, Brain-док, `moderation_ipc.db` `meta`/`drafts`, листы) — в §4 артефакта.

**5. Как доезжает до ответа.** Читается **на каждый запрос**, кэша нет: `load_playbook()` — простое `open().read()`, зовётся в четырёх боевых точках (`suggest.py:7118`, `userbot_listen.py:259`, `moderation_core.py:158`, `trainer_run.py:222`). Рестарт не нужен. **Но показать эффект нечем**: в тренажёре нет переигровки хода — `parse_command` знает только `заново`/`до crm`/`урок:`/`отмени урок N`, а `заново` — это новый ТЕСТ-клиент. Переигровка есть в модерации (`_default_regen`), то есть не в том контуре, где учат. Одна ручка всё же зашита при старте: `PLAYBOOK_MAX_RULES` читается на уровне модуля.

**6. Что сломается на тысяче.** Тысячи не будет — кап 100 обрежет раньше, и **молча**: замер 120 добавлений → 100 в книге, 20 потеряно без следа, при этом **все 120 вызовов вернули `added`**. Плюс сироты в сайдкаре копятся вечно (`unmark_source` зовётся только из «отмени урок N», вытеснение его не зовёт), **три процесса пишут файл без замка** (userbot / модербот / демон; `flock`/`msvcrt`/`Lock` в `suggest.py` — ноль, у соседнего `trainer_log.py` лок есть), и дедуп-по-подстроке даёт двухсловному огрызку запереть тему навсегда (проверено: `'уже было'` поглощает любое правило с этой подстрокой). Раздутый промпт узким местом **не является** — до него не доходит.

**7. Предсмертный взгляд.** База провалится не переполнением, а тем, что останется вторым экземпляром: уроки лягут в новый файл с версией и источником, а бот продолжит читать `playbook.md`, который те же три процесса пишут без замка, — и расхождение никто не заметит, потому что читателя у поля «источник» нет ни одного уже сегодня.

Временное — в `_scratch_trainbase_0819/` (оставлено, ничего не удалялось). Красных операций не потребовалось.

RESULT: разведка выполнена только чтением, все 7 вопросов отвечены живыми замерами; артефакт docs/artifacts/2026-08-19-training-base-recon.md (34220 байт, 406 строк) записан, строка ARTIFACT легла в мозг; боевые playbook.md и trainer_rules.json не изменены (sha до = sha после).
