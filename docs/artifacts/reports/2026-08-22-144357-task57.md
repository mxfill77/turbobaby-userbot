# Полный отчёт задачи 57 — 2026-08-22 14:43 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **6244 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 57 |
| записано | 2026-08-22 14:43:57 |
| символов в отчёте | 6244 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Пин выродил тренажёр как ключ к воротам: правка промпта головы в EN-ветке дала **12 из 12 зелёных молча**. 66 из 112 живых чеков зелены на ПУСТОЙ голове. Сверх задания: найдена шестая незапиненная живая дверь (`price_gate` → мост, 100% времени набора). Правка откачена, артефакт 79205fa. Раздел в master НЕ ЛЁГ (2 попытки).

**FACT:**
- commit `79205fa` в `git log` — `docs/artifacts/2026-08-22-pin-degeneracy-check.md`
- прогоны на пине, HEAD=`bb22927`: база **12/12** · промпт всех языков **7/12** · промпт ТОЛЬКО EN **12/12 при промахе головы**
- откат: `git diff --stat suggest.py` пусто · `git status --porcelain` пусто · `grep -c "ПРОВЕРКИ ПИНА 22.08"` → `0`
- `netprobe2`: кейс 1 под пином — 18 запросов наружу, **40.32с из 40.43с (100%)** через `price_gate.call`
- журнал: строки ARTIFACT и DONE легли (`OK: записано в мозг`)

---

## П.2 — чем адресуется снимок и что при промахе (первым, как просили)

Голова ключуется **парой хешей промпта** (`trainer_pin.py:90-91`), где `system` — ровно выход `suggest.make_system_prompt`:

```python
def _hkey(system, user):
    return _sha(system) + ":" + _sha(user)
```

Промпт изменился → ключ другой → промах. И тогда (`trainer_pin.py:177-181`):

```python
hit = self.data["head"].get(k)
if hit is None:
    self.misses["head"] += 1
    out = ""                    # документированный фолбэк «голова недоступна»
```

**Ни один из трёх исходов задания не наступает.** Старый ответ не отдаётся, живой запрос не идёт (`real` в ветке replay не зовётся), «неизвестно» прибор не говорит — он отдаёт **пустую строку** и тихо идёт дальше, где `compose_quote_draft`/`ensure_price_figure`/`ensure_closing_question` дописывают текст КОДОМ. Мост на промахе кидает `PinMiss`, док возвращает `None`. Снимок **не привязан к коммиту**: в `meta` — `head: fc3d227`, прогон шёл на `bb22927`, сверяется только `version`.

## Главный отрицательный тест (п.3)

| правка `make_system_prompt` | число | промахов головы |
|---|---|---|
| промпт у ВСЕХ языков | **7 из 12** (красны 4,5,6,10,11 — все чеком «черновик получен») | 12 |
| промпт ТОЛЬКО в EN-ветке | **12 из 12 ЗЕЛЁНЫХ** | 1 |

Второй вариант — честная модель обычной правки. Число совпало с чистым деревом посимвольно; единственный след — счётчик в служебной строке, которая **в вердикт не входит**. Причина шире: **66 из 112 живых чеков (58.9%) зелены на ПУСТОЙ голове** — их закрывает текст кода, вот дословный черновик кейса 7 при молчащей голове: `NMAX — 158 ฿/день; итого 4740 ฿; депозит 3000 ฿; свободен на эти даты. Бронируем?`

Называю прямо, без смягчения: **прибор не отличает изменившийся промпт от неизменившегося**, если правка задела кейсы, чьи чеки закрываются текстом кода.

## П.7 — вердикт одной строкой

**Второй ключ к воротам — ВЫРОЖДЕННЫЙ**: зелёное на пине переживает изменение кода бота.

---

## П.1 — пять границ и что каждая отдаёт

`pricing._default_get` — цена/парк (124 ключа) · `delivery._default_get` — зоны (1) · `suggest._bridge_read_doc` — FAQ (1 ключ `faq`, 7665 симв.) · `_cli_llm`/`_default_llm` — ответ головы (13 ключей; у кейса 2 их два — черновик и пост-чек) · `load_playbook` — 2843 симв. подменой лямбдой. Мина: границы 1 и 2 пишут в ОДИН словарь, а `tag` в ключ не входит — столкновение возможно, замка нет.

## П.4 — сломать границу: здесь прибор честен

Нет файла → `FileNotFoundError` до первого кейса. Чужая версия → `ValueError`. Валидный, но ПУСТОЙ снимок → **0 из 12**, все красны. На пустых данных не проходит ни одной веткой.

## Находка сверх задания: шестая дверь, которую пин НЕ закрыл

Утверждение прошлого раздела «после пина за границей не остаётся ничего, кроме кода бота» — **неверно, измерено**. `suggest.py:3389/3868` спрашивают сторож свежести `price_gate`, тот берёт клиент моста у демона и идёт на `script.google.com` мимо всех пяти границ: **17 спросов за набор, 42.80с из 42.89с (100%), один живой вердикт, 8 кейсов из 12**. Следствие: совпадение трёх прогонов частично объясняется КЭШЕМ окна, а не пином (кейс 1: 38.5с в первом прогоне, 0.02с во втором и третьем).

## П.5 — кейс живой сети (12)

Перестало проверяться: отвечает ли Календарь вообще; ветка деградации «цена недоступна» (заморожен удачный ответ); согласие снимка с живым прайсом. Как проверка живого ценового пути — **мёртв**; как проверка строковой сборки (`strip_thai`, `฿`, дословный перенос строки J) — жив. Но сторожа свежести он всё равно спрашивает, то есть одной ногой в живой сети и после пина.

## П.6 — за счёт чего позеленело, числом

Живьём 11/11/11 из 12 (3 красных кейсо-прогона из 36) → на пине 12/12/12 (0 из 36). Все три снятых красных — **голова** (кейс 5 в прогонах 1 и 3, кейс 10 в прогоне 2). **Не за счёт правки проверяющего**: `92dd754` легла ДО живых прогонов, они её уже включали и дали 11. Пин убирает не голову, а её ВЫБОРКУ — заведомая порча ответа по-прежнему краснит стабильно (11 из 12 в трёх прогонах).

## П.8 — раздел в master НЕ ЛЁГ

Две попытки, как предписано: (1) мост отверг чтение — `read_failed`, сервис документов Google упал; (2) чтение прошло, запись — таймаут, и **обратное чтение писателя вынесло проверенный вердикт**: «док не изменился (318819 символов) — запись НЕ ЛЕГЛА, повтор безопасен». Исход поэтому не «неизвестно», а проверенное «не легло». Текст раздела (6178 симв., заголовок содержит 22.08 и слова о проверке пина на вырождение) лежит по названному пути **`_scratch_pindegen_0822/section57.md`**; бэкап узла — `tmp/brain_backup_master_pindegen_2208_20260822-213742.txt`.

## Запреты соблюдены

Ворота не открывались, дети не обновлялись, клиентам не писалось; снимок открывался только на чтение и не переснимался; кейсы/ожидания/пороги не правились; временное — только в `_scratch_pindegen_0822/`; ничего не удалялось; в конфиги и секреты не ходил, имён ручек в командах не набирал; прежние разделы узла не трогались (вставка добавлением в хвост).

RESULT: пин проверен на вырождение — правка промпта EN-ветки даёт 12 из 12 зелёных молча, 66 из 112 чеков зелены на пустой голове, найдена шестая незапиненная живая дверь price_gate (100% времени набора); вердикт «второй ключ к воротам ВЫРОЖДЕННЫЙ»; артефакт 79205fa, правка откачена (git diff пуст), ARTIFACT+DONE в журнале; раздел в master не лёг после двух попыток, текст в _scratch_pindegen_0822/section57.md
