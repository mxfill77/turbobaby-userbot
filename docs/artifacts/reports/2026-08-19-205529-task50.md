# Полный отчёт задачи 50 — 2026-08-19 20:55 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4599 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 50 |
| записано | 2026-08-19 20:55:29 |
| символов в отчёте | 4599 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Карточка модерации построена, закоммичена и запушена. Артефакта `2026-08-19-moderation-card.md` не было — задача выполнена с нуля. Живые боты не тронуты: механизм + тесты, выкатка за воротами контура. Все три отрицательных теста устояли, контрфакт доказал, что замки кусаются.

**FACT:**
- `FACT: commit b2fba36 (код), 0a7bfd4 (артефакт), eab38f5 (тело журнала) в git log, запушены 55a5ba0..b2fba36 → eab38f5`
- `FACT: гейт 3881 теста, красных 2 — те же два храповика неразбора (points 994 > 845); свой вклад 0 точек`
- `FACT: test_moderation_card — 42 теста OK; гонка 8 потоков прогнана 20 раз → 20 × OK`
- `FACT: контрфакт со слепым захватом краснит 5 из 6 замков (8 победителей вместо 1)`
- `FACT: артефакт docs/artifacts/2026-08-19-moderation-card.md, 9 разделов`

---

## Дословный вывод команд

**1. Признак сделанности (первым действием):**
```
ls: cannot access 'D:/turbobaby-bot/docs/artifacts/2026-08-19-moderation-card.md': No such file or directory
NOT_FOUND
```

**2. Свои тесты:**
```
Ran 42 tests in 2.380s

OK
```

**3. Гонка — 20 прогонов подряд (проверка на флак):**
```
     20 OK
```

**4. Контрфакт (захват подменён прежним `set_decision` без предусловия):**
```
AssertionError: 8 != 1 : ['ready','ready','ready','ready','ready','ready','ready','ready']

КОНТРФАКТ: прогнано 6, провалов 5, ошибок 0
  ПОКРАСНЕЛ: TestFirstResponderWins.test_eight_simultaneous_presses_send_exactly_one
  ПОКРАСНЕЛ: TestFirstResponderWins.test_press_after_delivery_is_refused
  ПОКРАСНЕЛ: TestFirstResponderWins.test_second_press_loses_and_sees_the_winner
  ПОКРАСНЕЛ: TestFirstResponderWins.test_send_after_reject_is_refused
  ПОКРАСНЕЛ: TestFirstResponderWins.test_reject_after_send_is_refused
```

**5. Полный гейт** (`TURBOBABY_TEST_LOGS=1 venv/Scripts/python.exe -m unittest discover -s . -p "test_*.py"`):
```
AssertionError: Lists differ: ['anonymize_corpus_stable.py: было читател[445 chars]5/5'] != []
AssertionError: 994 not less than or equal to 845

Ran 3881 tests in 245.431s
FAILED (failures=2, skipped=10)
```

**6. Вклад в храповик (уже с отслеживаемыми файлами):**
```
файлов с ростом: 8
moderation_card.py в списке роста: False
moderation_ipc.py  в списке роста: False
totals: {'files': 63, 'readers': 565, 'readers_all': 1509, 'points': 994}
moderation_card в области счёта: True
```

**7. Журнал:**
```
OK: записано в мозг, символов: 482956
OK: записано в мозг, символов: 483512  | тело вынесено в docs/artifacts/journal/2026-08-19-205409-done.md
```

---

## Сводка

1. **`moderation_card.py`** — карточка показывает вопрос клиента, предлагаемый ответ и **ПОЧЕМУ**: цену дословной строкой Календаря, её источник, взятый сезон (`P1 ИЮНЬ-СЕНТЯБРЬ`), срок, доставку, чего боту не хватило. Блоки ленты цены берутся **живыми** экстракторами `suggest`, порог месяца — живой константой `_CAP_MIN_DAYS`; своих копий нет, тест сверяет 9 дословных фрагментов с исходником `suggest.py`.
2. **«⛔ НЕ СЧИТАЮ» отдельной строкой** на три названных класса. Граница сезонов определяется по `price_source.period_of` для обеих дат — обработки пересечения в боте **нет вовсе** (цена берётся по дате начала), и теперь бот об этом говорит вслух вместо числа. Третий исход («не знаю») обязателен и в зелёное не превращается.
3. **Первый ответивший закрывает карточку** — замок в СУБД (`claim_decision`, `UPDATE … WHERE status IN (…)`), а не в питоне. Этим закрыта **живая** дорога второго сообщения: у `set_decision` предусловия нет вовсе, кнопки после отправки не снимаются, а отправитель `poll_and_send` дедупа не имеет ни одной строкой.
4. **Автоотправки нет по построению**: в модуле нет ни одного имени канала отправки, ни ручки таймаута, ни ручки уверенности — проверяется разбором AST, а не обещанием в докстринге; единственная дорога к `ready` — функция `decide`.
5. **Честный остаток:** живые боты не тронуты и не перезапущены; правка `moderation_ipc.py` (клиентский контур) на них не применена — карточку на ворота выпишет гард. В `suggest.on_client_message` остаются 4 ветки, где карточка не рождается вовсе (парк недоступен, генератор упал, LLM пусто, гвард модели) — это правка `suggest.py`, в заход не входила. Точки подключения к `moderation_bot` названы в артефакте с номерами строк.

RESULT: карточка модерации построена (moderation_card.py + атомарный захват moderation_ipc.claim_decision + 42 теста), три отрицательных теста устояли, контрфакт краснит 5 из 6 замков гонки; гейт 3881/2 красных — те же два храповика, свой вклад 0; коммиты b2fba36, 0a7bfd4, eab38f5 запушены; артефакт docs/artifacts/2026-08-19-moderation-card.md; журнал ARTIFACT+DONE записан; живые боты не тронуты.
