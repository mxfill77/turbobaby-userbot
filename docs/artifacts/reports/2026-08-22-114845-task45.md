# Полный отчёт задачи 45 — 2026-08-22 11:48 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4616 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 45 |
| записано | 2026-08-22 11:48:45 |
| символов в отчёте | 4616 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Отрицательный тест ПРОЙДЕН (6/6). Три прогона: **11 / 11 / 11 из 12**, но красный кейс РАЗНЫЙ → число не названо. Флапают кейсы **5, 10, 12**. Второй ключ к воротам — НЕТ.

**FACT:**
- `git log -1` → **`fea9301`** ARTIFACT достоверное число тренажёра 22.08; правка проверяющего — коммит **`92dd754`** в дереве, `suggest.py:7378`.
- Узел карты `master`: **299 184 → 305 586** символов, sha живого `6cef34501d6535df…`, **7 замков из 8** свежим процессом; обратное чтение == задуманному дословно.
- Строка `DONE` легла в мозг (журнал 550 205 символов).
- Сырьё: `_scratch_truenum_0822/run_1.json|run_2.json|run_3.json`, по кейсу `case_NN_rM.json` (писались сразу после кейса).

---

### Формат: три числа в начало

| | |
|---|---|
| **отрицательный тест** | **ПРОЙДЕН, 6 из 6** — перепрогнан СЕЙЧАС на текущем HEAD. N1 «фраза о свободных датах напечатана при ЗАНЯТОМ юните» → проверяющий КРАСНЫЙ (`quote_status=none_available`, `j_line=None`). Откатывать нечего |
| **три прогона** | прогон 1 — **11 из 12** (красный 5) · прогон 2 — **11 из 12** (красный **10**) · прогон 3 — **11 из 12** (красный 5) |
| **флапают** | **кейс 5** (зелёным 2 из 3) · **кейс 10** (2 из 3) · **кейс 12** (3 из 3 здесь, но красный в сидении 04:18 того же дня — тот же корпус, тот же код) |

### п.1 Правка в дереве — ЕСТЬ, и заход не пропал
`suggest.py:7378` сейчас: `viol = availability_violations(client, avail=(True if _AV_POS_RE.search(exp.get("j_line") or "") else None))`. Изменена коммитом **`92dd754`** 22.08 09:08:02 UTC; до неё с 15.07 (`c895751`) стоял литерал `avail=None`.

**Премиса «исход неизвестен» неверна.** Заход id=33: `CLAIM 08:25:05 UTC` → `ТАЙМАУТ 2700s → failed 09:10:05 UTC` ✔, но **коммит лёг за 2 мин 03 с до потолка**. Черновик протух — он писался в 08:35. Остальные премисы верны дословно, включая «ворота дважды остановили применение»: 16:10:11 (`92dd754`, отказ №5) и 16:43:55 (`c88d673`, отказ №6).

### п.3 Текст ответа бота
sha256 клиентского текста совпал **втроём** (замер до правки, замер на текущем коде, независимый пересчёт из файлов) — **14 файлов из 14, 12 кейсов, расхождений 0**. Диff: 1 ханк, 2 строки кода, обе внутри `_smoke_checks` — функции ВЕРДИКТА; `generate_draft` её не зовёт (0 вхождений).

### Арифметика (сделана ДО трёх прогонов)
Один прогон — **452.5 с**; 3×452.5 = **1357.5 с = 0.503** потолка 2700, ниже порога 0.7 → гнались все три. Факт по итогу: 452.5 + 441.8 + 430.1 = **1324.4 с**, ошибка предсказания 2.5 %.

### п.6 Причина флапа — поимённо
- **Кейс 5 — ГОЛОВА.** Записка кода побайтно ОДНА И ТА ЖЕ (173 симв., sha `7ec5205b5d10`), черновики разные: зелёный — «вместе с **менеджером** подберём», красный — «вместе с **командой**» (чек ждал «одно из: менеджер, коллег, опыт, ездил, водил»). В прогоне 3 та же голова покраснела ДРУГИМ чеком — сама написала «опасн».
- **Кейс 10 — ВТОРОЙ НОСИТЕЛЬ ВНУТРИ КОДА, не сеть.** Записка несёт «свободен на эти даты» **инлайном, без блока `<<<QUOTE>>>`** → `_quote_block_from_note` даёт `''`, и правка отдаёт проверяющему `None` **во всех трёх прогонах**. Красным это становится ровно тогда, когда голова повторит «свободен» (прогон 2); в прогонах 1 и 3 зелёное было везением, а не правотой.
- **Кейс 12 — ЖИВАЯ СЕТЬ / состояние данных.** Записка красного прогона: «точная цена из Календаря сейчас недоступна» против полного quote-блока в зелёных.

### п.7 Второй ключ к воротам — **НЕТ**
Две независимые причины: (1) флап — набор красных переезжает между прогонами на одном корпусе и одном коде, а порог требует 12 из 12 в КАЖДОМ прогоне; (2) числа не хватает и без флапа — лучший прогон 11 из 12. Вердикт `pc_orchestrator.client_trainer_green.json` не переписывался: `green: {}`, июльская запись `55aad22`.

### Остаточный дефект (назван, НЕ чинился — задание запрещало)
Правка закрыла ОДИН носитель наличия (quote-блок), а их **два**: инлайн-ветка «сдаём от N дней» проверяющему невидима. Тот же класс, второй носитель.

Ворота не открывались, дети не обновлялись, клиентам не писалось, пороги и кейсы не трогались, ничего не удалялось. Временное — в явно названном `_scratch_truenum_0822/`.

RESULT: правка проверяющего `92dd754` найдена в дереве и подтверждена (отрицательный тест 6/6, текст ответа бота не изменён — sha 14/14); три живых прогона дали 11/11/11 из 12, но покейсно разошлись (красные 5, 10) — достоверное число НЕ названо, флапают кейсы 5 (голова), 10 (второй, невидимый проверяющему носитель наличия), 12 (Календарь-мост); второй ключ к воротам — НЕТ; раздел лёг в узел master (299 184→305 586, 7 замков из 8), артефакт `docs/artifacts/2026-08-22-trainer-true-number.md` коммитом `fea9301`, строка DONE в журнале.
