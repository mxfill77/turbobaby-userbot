# Полный отчёт задачи 57 — 2026-08-20 08:40 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4791 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 57 |
| записано | 2026-08-20 08:40:51 |
| символов в отчёте | 4791 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Карточка модерации переведена на единицу РАЗГОВОР: `dialog_card.py` (одна карточка на диалог, сжатая история, темы клиента с тремя исходами), замок устаревшего предложения в СУБД (`supersede_open`), замер нагрузки надзора (`card_load.py`). Все четыре отрицательных теста устояли. Гейт 3947 (было 3881), красных 2 — те же два храповика. Запушено, на живых ботов НЕ выкачено.

**FACT:**
- `FACT: commit 651f436` (код), `7ca7883` (артефакт), `9c8b7a0` (тело журнала) — все три в `git log`, `## main...origin/main` без расхождения (запушены).
- `FACT: Ran 3947 tests in 251.839s / FAILED (failures=2, skipped=10)` — полный гейт после правки.
- `FACT: docs/artifacts/2026-08-20-card-per-dialog.md` создан и закоммичен.
- `FACT: журнал — OK: записано в мозг` ×2 (ARTIFACT + DONE, тело вынесено в `docs/artifacts/journal/2026-08-20-083951-done.md`).

**ПРИЗНАК СДЕЛАННОСТИ:** артефакта `docs/artifacts/2026-08-20-card-per-dialog.md` **не было** (`ls` → No such file) — работа выполнена с нуля.

## Дословный вывод команд

**Гейт ДО правки (замерен этой сессией, до единого изменения):**
```
Ran 3881 tests in 274.401s
FAILED (failures=2, skipped=10)
```
Красные: `test_no_growth_against_baseline` (8 файлов), `test_totals_do_not_grow` (`994 not less than or equal to 845`).

**Гейт ПОСЛЕ правки:**
```
Ran 3947 tests in 251.839s
FAILED (failures=2, skipped=10)
```
Красные — **те же два храповика**, тот же список из 8 файлов, `994 not less than or equal to 845` (число не изменилось).

**Новый модуль отдельно:**
```
Ran 66 tests in 6.417s
OK
```

**Соседние модули (проверка, что правки moderation_card/moderation_ipc ничего не сломали):**
```
Ran 108 tests in 17.000s
OK
```

**Вклад в храповик неразбора (замер ПОСЛЕ коммита — иначе новые файлы вне области счёта):**
```
файлы области: осмотрено 65 файлов, разобрано 65   (было 63)
ИТОГО слепых читателей 565 из 1552 (36.4 %), точек 994, файлов 65
dialog_card.py: 0 точек · card_load.py: 0 точек · moderation_card.py: 0 точек
moderation_ipc.py: 3 точки (базовая линия — тоже 3: init_db ×2, is_bot_alive)
```

**Диффстат (число тестов не падает — удалений ноль):**
```
 card_load.py        | 234 ++++
 dialog_card.py      | 553 ++++
 moderation_card.py  |  12 +
 moderation_ipc.py   |  57 ++
 test_dialog_card.py | 900 ++++
 5 files changed, 1756 insertions(+)
```

**Живой отказ по устаревшему предложению (вывод кода, не макет):**
```
decision = 'closed'
♻️ Это предложение УСТАРЕЛО: клиент написал снова, и бот собрал ответ заново.
Отправить устаревшее нельзя — решайте по свежему предложению в карточке этого разговора.
Клиенту сейчас не ушло ничего.

строк в статусе ready после нажатия: 0
```

**Живая карточка** (2207 симв., потолок Telegram 4096) — целиком в §2 артефакта; в ней на реальном многотемном первом ходе клиента из корпуса стоит `шлемы/комплект — ❗ БЕЗ ОТВЕТА`, то есть ровно тот пробел, который замер назвал сильнейшим (0 обещаний числа шлемов против 151 вопроса о нём).

## Сводка

1. **Единица — разговор.** `dialog_card.py`: одна карточка на `client_id`, показывает с чего клиент начал · что уже отправлено нами · что бот предлагает СЕЙЧАС · почему. История в три яруса; 58 ходов (максимум замера) укладываются в одно сообщение Telegram — это замкнуто тестом.
2. **Главный новый случай (б) закрыт замком в СУБД,** не в питоне: `superseded` лежит вне `CLAIMABLE_FROM` и вне `DECISION_STATUSES`. Порядок «сначала гасим, потом ставим» — и есть замок; контрфакт обратного порядка воспроизведён тестом (2 открытые строки против всегда 1). Проверено шестью дорогами, включая устаревший снимок строки в руках у нажимающего и гонку «нажали ровно в момент дописки» (6 повторов, двух сообщений не бывает).
3. **Захват первым ответившим не тронут:** своего пути к `ready` у нового модуля нет — `claim_decision` отсутствует среди имён кода `dialog_card.py` (AST-тест).
4. **Замер нагрузки встроен,** числа копятся и владельцу не показываются: `build`/`render` счётчика не читают вовсе (AST-тест требует ровно две касающиеся функции). Мерятся две разные величины — время на текущем тексте и срок жизни карточки; нажатие без показа уходит в `unpaired`, а не в нули.
5. **Честные ограничения названы** (§6 артефакта): словарь тем врёт в обе стороны — замерено и закреплено тестом, расширять его нельзя (одна линейка с замером), перекос назван строкой в самой карточке; выкатки нет, точки подключения к `moderation_bot`/`suggest.py:7169` перечислены для отдельного захода.

RESULT: карточка модерации переведена на единицу РАЗГОВОР (dialog_card.py + supersede-замок в moderation_ipc + card_load.py, 66 тестов), четыре отрицательных теста устояли, гейт 3947/красных 2 (те же храповики, вклад ноль), коммиты 651f436·7ca7883·9c8b7a0 запушены, артефакт docs/artifacts/2026-08-20-card-per-dialog.md, журнал записан; на живых ботов не выкачено.
