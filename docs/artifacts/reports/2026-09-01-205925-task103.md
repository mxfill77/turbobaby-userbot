# Полный отчёт задачи 103 — 2026-09-01 20:59 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4866 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 103 |
| записано | 2026-09-01 20:59:25 |
| символов в отчёте | 4866 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

CLAUDE.md переведён из пересказа рамки в её проекцию: вырезаны блок 31.08, свод 25.07 и три правила-класса (все их правила живут в каноне), добавлена шапка со ссылкой на канон 02.09.2026 #1, минутой живого чтения и четырьмя машинными разделами. 48196 → 46181 байт. Отрицательная проба пройдена, возврат доказан хешем. Коммит 224af43.

**FACT:**
- `commit 224af43` в `git log` — «CLAUDE.md стал проекцией канона рамки, а не его пересказом», 2 файла, +273/−138.
- Канон прочитан ЖИВЫМ вызовом отдельным процессом (`brain_writer.read_text(name="KB_shtab_frame")`, только чтение) 01.09.2026 **20:45:09 UTC** и повторно **20:54:14 UTC**: 30040 символов, 561 строка, U+FFFD 0, CR 0, `sha256 969cdf9308d10b60…86612f70`, шапка `═══ … (версия 02.09.2026 #1) ═══`, «01.09.2026» в теле 0 раз.
- Возврат после отрицательной пробы: `sha256sum -c` → `CLAUDE.md: OK`, хеш `8497f4485df522015bc3021390e30dfb3a341c23178f29866cc66f53b84f5222` совпал до и после.
- Журнал: две строки легли в мозг (747521 → 748070 символов), обе `EXIT=0`.

**1. Премиса — что подтвердилось, что осталось НЕИЗВЕСТНО**

| премиса | исход |
|---|---|
| канон = 02.09.2026 #1 в мозге (`KB_shtab_frame`) | **ПОДТВЕРЖДЕНО** живым чтением (числа выше) |
| та же редакция в утверждённом тексте на диске | **ПОДТВЕРЖДЕНО**: посимвольно равен `docs/tasks/frame-edition-20260902.txt`, расхождений 0 из 30040 |
| «в обоих местах» — второе место, инструкции проекта Штаба | **НЕИЗВЕСТНО**: с полосы ПК они не читаются ни одним каналом. Косвенный довод — утверждённый текст побайтово равен живой копии; это довод, а не чтение источника |
| CLAUDE.md несёт свою рамку от 31.08 | **ПОДТВЕРЖДЕНО**: блок «Операционная рамка… (31.08.2026)» со ссылкой на `docs/PROJECT_OPERATING_FRAME.md` («local working projection, 2026-08-31»). В git он не был закоммичен — лежал в дереве как +5 строк |
| вес 48 КБ | **ПОДТВЕРЖДЕНО**: 48196 байт, 425 строк |

**2. Что убрано и что оставлено.** Резалось только то, чьё *правило* живёт в каноне: блок 31.08 (§8а/§8в), свод 25.07 — все девять пунктов (§6, §7, §8а, §8), два правила-класса про моки и голдены (§6 «проверка повторяет живой формат»), правило-класс про зеркало (§6, §8г), преамбулы ритуала журнала и правила артефактов (§6, §ПАМЯТЬ, §8в). Числа, форматы, пути и ссылки на артефакты не потеряны — перенесены в сжатые замены (позиционный формат `delivery_zones_get`, `302`+`HTTPError`, `guard-classes.md`, `$( )`, `LINE_MAX`, `awaits_reply`). Оставлены факты полосы, которых в каноне нет и другого дома у них нет: ручки модели, мины дерева, механика писателя журнала, `brain_writer`, слой ожиданий О1–О4, слепок `queue_state_pc`. Граница названа вслух в артефакте.

**3. Правило 8в-1 держится.** Шапка «Канон рамки и эта проекция» несёт версию канона (02.09.2026 #1), чем прочитано, время чтения (02.09 03:45 местного / 01.09 20:45:09 UTC), что показало чтение и снимок на диске; там же — явный отказ от ложной свежести и третий исход («прочитать не удалось → НЕИЗВЕСТНО, а не разрешение продолжать»).

**4. КБ было/стало:** **48196 байт (47.07 КиБ, 425 строк) → 46181 байт (45.10 КиБ, 408 строк)**, нетто −2015 байт (−4.2%). Раскладка честная: вырезано и сжато **8272 байта** пересказа, добавлено **6257 байт** машинной части. Вес файла держит не рамка, а факты полосы — поэтому −4.2%, а не −70%.

**5. Отрицательная проверка пройдена.** Подстановка редакции назад (01.09.2026 #1, все 3 вхождения) даёт расхождение, видимое двумя способами: человеку — строка 17, в первой же таблице файла, с инструкцией рядом («сверь номер с первой строкой живого `KB_shtab_frame`»); машине — номер таблицы против шапки живого узла (`ВЕРДИКТ: РАСХОЖДЕНИЕ — проекция отстала на редакцию, прав канон`). Всё возвращено, равенство доказано sha256. Граница пробы: она показывает, что расхождение *видно*, но не что кто-то обязан посмотреть — принудительного сличения при старте headless-сессии в коде нет и не заводилось (кода не меняли).

**Запреты соблюдены:** канон только читался (записи в мозг по рамке не было), инструкции проекта не трогались, код не менялся, процессы/клиентский контур/дети не трогались, ничего не удалялось (`docs/PROJECT_OPERATING_FRAME.md` оставлен на месте, лишь помечен как отставшая копия), `.env` не читался, временное — в `tmp/pc_report/`. Чужие правки дерева (`todo.md`, `docs/review_*`) в коммит не брались. Push не делался — задание его не просило.

**АДРЕС РЕЗУЛЬТАТА:** `docs/artifacts/2026-09-02-CLAUDE.md-стал-проекцией.md` (147 строк, в коммите 224af43).

RESULT: CLAUDE.md стал проекцией канона — коммит 224af43; пересказ рамки вырезан (−8272 б), добавлена машинная часть со ссылкой на канон 02.09.2026 #1 и минутой чтения 20:45:09 UTC (+6257 б); 48196 → 46181 байт (425 → 408 строк); отрицательная проба показала расхождение, возврат доказан sha256 8497f448…; артефакт docs/artifacts/2026-09-02-CLAUDE.md-стал-проекцией.md, журнал ARTIFACT+DONE записан.
