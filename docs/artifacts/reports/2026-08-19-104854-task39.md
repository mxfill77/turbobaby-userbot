# Полный отчёт задачи 39 — 2026-08-19 10:48 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4793 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 39 |
| записано | 2026-08-19 10:48:54 |
| символов в отчёте | 4793 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Вотчдог больше не выдаёт возраст файла лога за длительность простоя: добавлена чистая `absence_since()` (свидетели жизни → «перезагрузка» / «наблюдение» / НЕИЗВЕСТНО). Логика подъёма не тронута — на это положен тест. Оба отрицательных теста устояли, реплей обеих смертей 18.08 сошёлся. Гейт 3798, красных 2 — те же два храповика.

**FACT:** commit `a76d377` (код+тесты) и `5149b99` (артефакт) в `git log`, оба запушены (`a45a402..a76d377`, `a76d377..5149b99 main -> main`). Живой замер загрузки через новый код: `2026-08-18 20:27:36` (CIM и GetTickCount64 совпали посекундно).

## Признак сделанности (первым действием)
```
$ ls -la docs/artifacts/2026-08-19-watchdog-numbers-fix.md
ls: cannot access '...': No such file or directory
```
Файла не было — работа сделана впервые.

## Забор Честертона (одной строкой + что ещё на нём стоит)
Возраст лога брали потому, что это **единственный след ребёнка, переживающий смерть демона** (память вотчдога — ОЗУ, `client_watch.json` демон только пишет). На нём стоят ещё две точки, и **обе оставлены**: вето `лог свеж ≤ CLIENT_LOG_STALE` (там верхняя граница на своём месте — она *запрещает* подъём) и сторож демона на `HEARTBEAT_FILE` (файл плотный: `_write_heartbeat()` раз за виток, его возраст **и есть** молчание).

## Проверка на истории — дословно
```
ЖИВОЙ замер момента загрузки этого ПК: 2026-08-18 20:27:36 (1787059656)
--- №1 Windows Update ---
  БЫЛО (возраст файла):  12 ч 10 м    =  43812 c
  СТАЛО (жизнь процесса): 10 ч 36 м    =  38200 c   [источник: перезагрузка]
  ПРАВДА (shutdown→замер): 10 ч 37 м    =  38266 c
  завышение прежнего: +5546 c (×1.1) | остаток нового: -66 c (загрузка позже выключения)
--- №2 ручная перезагрузка ---
  БЫЛО (возраст файла):  5 ч 54 м     =  21292 c
  СТАЛО (жизнь процесса): 6 м 14 с     =    374 c   [источник: перезагрузка]
  ПРАВДА (shutdown→замер): 6 м 27 с     =    387 c
  завышение прежнего: +20905 c (×55.0) | остаток нового: -13 c (загрузка позже выключения)
```
**Сошлись — с честной оговоркой:** остаток в обоих случаях ровно «загрузка позже выключения» (66 с и 13 с) и всегда в сторону **недосчёта**. Момент выключения есть только в журнале событий; загрузка снимается дёшево и всегда.

## Живой сухой прогон на настоящем контуре (подниматель заглушён)
```
=== тик 1 (наблюдаем жизнь) ===
{'pc_agent': 'alive', 'userbot': 'alive', 'moderation_bot': 'alive'}
=== тик 2: finder ЛЖЁТ «процесса нет» ===
  ЖУРНАЛ(заглушка): вотчдог поднял moderation_bot (смерть 1/3, лежал не дольше 5 м 00 с, …)
```
mtime `moderation_bot.log` в тот момент — 11:33:49, то есть **старый код сказал бы «лежал 6 ч 14 м» о простое в 5 минут**.

## Отрицательные тесты — оба устояли
- **(а)** известное время → точное число: `("перезагрузка", ровно 3600 с)`, карточка «лежал 1 ч 00 м» без оговорок.
- **(б)** момент не локализуется → НЕИЗВЕСТНО. Сильная форма: **тот же вход, что дал вчера «5 ч 54 м»**, без знания о загрузке даёт `None`, и строки «5 ч 54 м» нет ни в карточке, ни в журнале (причина незнания намеренно без числа — иначе ложная величина вернулась бы в скобках).

## Счётчик смертей (назвал, не чинил)
Сбрасывается в **трёх** местах: `:6564` любой живой тик (задумано); `:6691` ветка `skip` (модербот без токена — сбрасывает и `halted`); **`:6369` — `_client_watch_state` в ОЗУ демона, то есть каждый старт процесса**. На 18.08 сработало третье: перезагрузка убивает и демона, поэтому `CLIENT_MAX_DEATHS=3` против этого класса бессилен конструктивно.

## Замок
```
Ran 3798 tests in 254.010s
FAILED (failures=2, skipped=10)
  test_nonparse_ratchet.TestRatchet.test_no_growth_against_baseline
  test_nonparse_ratchet.TestRatchet.test_totals_do_not_grow
```
Красных 2 и это те же два храповика; `pc_orchestrator.py` в списке роста **нет** (`nonparse_scan`: 163/317 против линии 164/320 — ниже). Тестов было 3773 → стало **3798** (+13 своих). Прицельно: `Ran 47 tests, OK`.

**Осознанно изменён один прежний голден** — `test_b_downtime_measured_before_raise`: класс «мерим ДО подъёма» сохранён целиком (подниматель по-прежнему трогает mtime, число не схлопывается), изменилось лишь, чем доказан простой → «лежал не дольше 10 м 00 с».

Запреты соблюдены: логика подъёма не менялась (тест `test_raise_decisions_untouched_by_the_new_measure`), клиентских файлов не касался, процессы не трогал, ничего не удалял, `.env` не читал. Журнальные строки (`ARTIFACT`/`DONE`) не писал — по указанию «доставка отдельным заходом».

RESULT: вотчдог мерит простой по жизни процесса (absence_since: перезагрузка/наблюдение/НЕИЗВЕСТНО), логика подъёма не тронута; оба отрицательных теста устояли, реплей 18.08 дал 10 ч 36 м и 6 м 14 с вместо ложных 12 ч 10 м и 5 ч 54 м; гейт 3798 тестов, красных 2 — те же два храповика; коммиты a76d377 и 5149b99 запушены, артефакт docs/artifacts/2026-08-19-watchdog-numbers-fix.md.
