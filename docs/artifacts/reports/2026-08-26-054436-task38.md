# Полный отчёт задачи 38 — 2026-08-26 05:44 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4746 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 38 |
| записано | 2026-08-26 05:44:36 |
| символов в отчёте | 4746 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Разведка закрыта, все 7 шагов. Ответ: **НЕТ** — полоса 328 от каталога установки пакета Claude не зависит (0 из 7 звеньев цепочки в WindowsApps, греп по коду 0). Инцидент восстановлен целиком: журналы не провернулись, обновление ШЛО дважды, оба раза `deferred registration` из-за работающего приложения. Артефакт записан, журнал ПК — две строки.

**FACT:** артефакт `docs/artifacts/2026-08-26-desktop-package-lock-recon.md` создан; журнал ПК принял обе строки — ARTIFACT («OK: записано в мозг, символов: 631664») и DONE («символов: 632196»). Живой замер цепочки: PID 9628 = `C:\Users\mxfill1\.local\bin\claude.EXE` ← PID 13496 = `D:\turbobaby-bot\venv\Scripts\python.exe pc_orchestrator.py` ← PID 1836 svchost (Планировщик). Правок кода нет — задача read-only.

---

## Числа

| что | число |
|---|---|
| звеньев цепочки исполнителя внутри WindowsApps | **0 из 7** |
| совпадений `WindowsApps\|Claude_pzs8sxrjxfjjc\|CoworkVMService` в `*.py` репо | **0** |
| процессов из каталога установки пакета | **11**, все Desktop |
| версия пакета против эталона 1.37937.1.0 | **равна** |
| следов иных версий | **4** |
| событий AppX за 48 ч / со словом Claude / не-Verbose | **1196 / 184 / 84** |
| перезагрузок за 48 ч | **1**, руками владельца |
| обновлений пакета в окне инцидента | **2**, оба отложены |

## ШАГ 0
Артефакта по адресу результата не было — разведка с нуля.

## ШАГ 6 — главный ответ: **НЕТ**

Четыре независимых основания по путям:
1. Цепочка исполнителя — `powershell` ← `.local\bin\claude.EXE` ← `venv\python.exe pc_orchestrator.py` ← svchost (Планировщик) ← services ← wininit. Ноль WindowsApps. Корень полосы — **Планировщик, не оболочка и не Desktop**.
2. `(Get-Command claude).Source` → `.local\bin\claude.exe`, самодостаточный бинарь 263 931 552 б от 24.07, не симлинк. Резолвер `_resolve_claude_once` — PATH-first.
3. Греп по всем `*.py` на каталог установки → **0**.
4. **Эмпирика инцидента, сильнейшее:** пакет обновлялся дважды, приложение дважды принудительно гасилось (`ForceApplicationShutdownOption`, 01:40 и 08:50) — а демон давал обороты (лог в 01, 03, 05, 06, 07, 11 ч) и умер **только на перезагрузке**.

## ШАГ 5 — обновление ШЛО, исход не «неизвестно»

- **01:28:47** Add с опцией `DeferRegistrationWhenPackagesAreInUse`; 1.34493.1.0 → 1.37937.0.0
- **01:29:31** Id=638 «apps are still running: {Claude_pzs8sxrjxfjjc!Claude}» + Id=658 отложенная регистрация
- **01:40:04** — **замок:** Id=471 `error 0x12C: Deleting file …\WindowsApps\Deleted\Claude_1.34493.1.0_…\app\icudtl.dat failed` (+ `resources.pak`), далее **17 пар** до 08:40:20
- **08:40:05** второй заход → 1.37937.1.0, снова Id=658 «because {Claude_1.37937.0.0} is still running»
- **11:45:30 и 11:46:39** — дважды `RepairAppRegistrationOption`, Id=649 «ACLs repaired… Register next time should succeed»
- **11:46:47** владелец перезагружает руками (Id=1074, StartMenuExperienceHost, «on behalf of user mxfill1»)

**Механизм замка назван:** служба `CoworkVMService` (DisplayName «Claude», Auto, Running) с бинарём **внутри** каталога пакета — `…\app\resources\cowork-svc.exe`; её собственный лог: «failed to disarm SCM recovery actions for this stop… Access is denied». Остаточный след жив и сейчас: Id=493 каждые ~6 мин, последний 12:11:13.

## ШАГ 7 — отрицательный тест

Мой «НЕТ» ложен, если зависимость идёт через **общие данные**, а не через exe — и это не гипотеза: `_claude_base_dirs()` (строки 1479–1494) намеренно включает каталог данных пакета, и он **срабатывал боем** — первая строка живого лога демона от 22.07: `claude=…\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Roaming\Claude\claude-code\2.1.197\claude.exe`. Сегодня ветка спит только потому, что PATH выигрывает раньше.

## НЕИЗВЕСТНО (честно)

- `~/.claude/.credentials.json` — один файл на Desktop и CLI (7064 б, изменён 09:48:52); **не читался** (секреты), поэтому «обновляет ли токен именно Desktop» не проверено.
- Поведение полосы при **удалённом** пакете не наблюдалось: пакет обновлялся и гасился, но не сносился.
- Листинг `WindowsApps` и `WindowsApps\Deleted` — `Access is denied` текущей сессии; состав версий восстановлен по журналу, а не с диска.

Предсмертный взгляд не сбылся: журналы **не** провернулись, окно восстановлено целиком.

RESULT: разведка выполнена только чтением — полоса 328 от каталога установки пакета Claude НЕ зависит (0/7 звеньев в WindowsApps, греп кода 0, демон пережил 2 обновления и 2 принудительных гашения); замок опознан как отложенная регистрация MSIX из-за работающего приложения + служба CoworkVMService из каталога пакета; версия равна эталону 1.37937.1.0; названа живая оговорка — зависимость через каталог ДАННЫХ пакета, срабатывавшая 22.07; артефакт docs/artifacts/2026-08-26-desktop-package-lock-recon.md и две строки журнала записаны.
