# Полный отчёт задачи 16 — 2026-08-18 18:48 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5621 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 16 |
| записано | 2026-08-18 18:48:24 |
| символов в отчёте | 5621 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Модербот 18.08 **не умирал сам ни разу** — оба раза его снесла перезагрузка Windows (04:01:04 плановая от Windows Update, 20:27:21 ручная владельцем). Числа сторожа «лежал 12 ч 10 м / 5 ч 54 м» ложны: он меряет mtime разреженного лога, правда — 10 ч 37 м и **6 м 32 с**. Настоящая дыра не в смерти, а в подъёме: 10 ч 06 м после загрузки без контура.

**FACT:** `docs/artifacts/2026-08-18-moderbot-deaths-1.md` создан (13.6 КБ); журнал — две записи через `cowork_log_append.py`, обе с распиской «OK: записано в мозг» (454812 и 455162 символов), тело ARTIFACT вынесено в `docs/artifacts/journal/2026-08-18-184702-done.md`. Код не правился — `git status` по трекаемым файлам чист, коммита нет сознательно (задача read-only).

---

## Арифметика (первым действием)
Смертей 2 → 60 строк собственного лога + 8 строк `moderation_bot.log` за 18.08 + 12 строк `logs/moderbot_stderr.log` + 2 окна системного журнала + точечные грепы по `pc_orchestrator.log`/`pc_agent.log`. Оценка ~15–20 вызовов, ~300–500 с. **Порог 1890 с не задет.**

## Признак сделанности
`ls docs/artifacts/ | grep -i moderbot-death` → **exit=1, файла не было.** Три вопроса не были отвечены нигде. Теперь отвечены.

## Поправка к «живым фактам» задания
07:39 и 13:34 — это **UTC**; лог пишет локальное (UTC+7). Живые строки:
```
7186:2026-08-18 14:38:55,360 INFO moderation_bot запущен агентом, PID 15760
7187:2026-08-18 14:39:07,349 WARNING контур-вотчдог: moderation_bot МЁРТВ (лежал 43812с, смерть 1/3) → подъём ok=True, PID после=[15760]
7294:2026-08-18 20:33:52,585 INFO moderation_bot запущен агентом, PID 10948
7295:2026-08-18 20:34:04,480 WARNING контур-вотчдог: moderation_bot МЁРТВ (лежал 21292с, смерть 1/3) → подъём ok=True, PID после=[10948]
```

## 1. Последние 30 строк перед каждой смертью
Приведены дословно в артефакте (строки 7082–7111 и 7084–7113). Главное: **30 строк покрывают девять суток** — лог принимает только исключения, тишина в нём смертью не является. Последние две строки перед смертью №1 (02:28:27 «maximum number of running instances reached», 02:28:38 «missed by 0:00:01.30») — жалобы APScheduler на голодание по CPU во время синхронного захода `claude -p`; такие же пары лежат 15.08 и 17.08 и смертью не кончились. **Обрыва в трассировке НЕТ:** `grep -c Traceback logs/moderbot_stderr.log` → **0**; последний traceback в `moderation_bot.log` — 06.07.2026.

## 2. Сам или извне — ЗАВЕРШЁН ИЗВНЕ, оба раза
- **Канал самоубийства пуст**, и он построен именно для этого (`pc_agent.py:326`: «смерть оставит traceback», весь stderr ребёнка перехвачен в файл). `Application Error` по python.exe за сутки — 0.
- **Системные события есть**: `04:00:05 Id=1074 MoUsoCoreWorker.exe … Установка пакета обновления`, `04:01:57 Id=1074 TrustedInstaller.exe … Обновление (Запланированное)`, `20:27:15 Id=1074 StartMenuExperienceHost.exe on behalf of user mxfillpc\mxfill1 … Другое (Незапланированное)`. Оба выключения чистые (`last shutdown's success status was true`, 6008 за сутки нет).
- **Третий свидетель — молчание контур-вотчдога.** Он тикает каждые 300 с и логирует любой исход, кроме «жив». За 17–18.08 в логе **ровно две** его строки. Значит PID 11968 был жив как минимум до 03:56 (через 1,5 ч после того, как его лог замолчал), а PID 15760 — все 5 ч 48 м, пережив ещё и self-update демона в 20:03:34.
- **Корень ложного числа:** `down_sec = _log_age_sec(logfile, now)` = `now - getmtime("moderation_bot.log")`. Прибор сообщает возраст ФАЙЛА, а не длительность смерти. Завышение по второй смерти — ×53, и оно **дошло до владельца карточкой** (`dispatch_notify.log:7521 → topic:328 «лежал 5 ч 54 м»`).
- «Смерть 1/3» оба раза — не здоровье, а сброс: состояние живёт в ОЗУ демона, а демон перед каждым подъёмом новый (5540 → 21256 → 7336 → 15988). `CLIENT_MAX_DEATHS=3` против этого класса бессилен конструктивно.

## 3. Что было на машине в минуту смерти
- **04:01:04** — двойная перезагрузка Windows Update. Самообновления демона нет (первое за сутки — 20:03:12), реконсиляции нет. Другая задача была и погибла: `RUN id=43` в 03:56:30, труп найден в 14:13:11 как сирота (36997 с). Погиб не модербот — весь контур (`процессов демона НЕТ`, `лок PID 5540 мёртв`).
- **20:27:21** — ручная перезагрузка владельцем. Самообновление было за 24 мин до неё и **не виновато**; реконсиляция детей сработала правильно и детей не тронула: `применение 538bbbd к «userbot,moderbot» ОСТАНОВЛЕНО — клиентские файлы: suggest.py`.
- **Дорогая находка:** ПК загрузился в 04:02:10, контур ожил в 14:08:58 — **10 ч 06 м без наших процессов**, потому что задачи Планировщика висят на **входе пользователя** (реорганизация `ntuser.dat` в 14:08:50), а не на загрузке. Плюс `pc_agent` авто-реадоптит **только userbot** (`grep -i moder` по его логу за 18.08 → пусто), и модербот ждал ещё 29 м 57 с.

## Остатки (в код не лез — read-only)
Не проверено: спит ли ПК между входом и загрузкой (окно 04:02→14:08 может частично быть сном — не мерил, `QueryUnbiasedInterruptTime` тут не применялся). Не трогал `moderation_ipc` (sqlite — красное), поэтому продукт тика О3 как независимый источник времени смерти не привлекался; выводы стоят на CIM-молчании вотчдога, которое сильнее.

RESULT: разведка read-only закрыта — модербот 18.08 не умирал сам ни разу, оба раза снят перезагрузками Windows (04:01:04 плановая Windows Update, 20:27:21 ручная владельцем); числа сторожа «лежал 12 ч 10 м / 5 ч 54 м» ложны (меряется mtime разреженного лога, правда 10 ч 37 м и 6 м 32 с), настоящая дыра — 10 ч 06 м после загрузки без контура из-за привязки задач ко входу пользователя; артефакт docs/artifacts/2026-08-18-moderbot-deaths-1.md создан, журнал ARTIFACT+DONE записан.
