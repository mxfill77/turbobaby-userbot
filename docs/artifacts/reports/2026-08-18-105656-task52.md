# Полный отчёт задачи 52 — 2026-08-18 10:56 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5055 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 52 |
| записано | 2026-08-18 10:56:56 |
| символов в отчёте | 5055 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

РАЗВЕДКА READ-ONLY завершена. Артефакта не было — задача выполнена целиком. Причина простоя названа: дыра не в дате 2099, а в принципале `InteractiveToken`. Забор объяснён журналом: 2099 — протез вместо `ONSTART`, на который не хватило прав. Автологон, выбранный владельцем 07.08 как решение, так и не вооружён. Ничего не менял.

**FACT:** read-only. Единственная запись на диск — `docs/artifacts/2026-08-18-pc-autostart-why.md` (создан) и `_scratch_autostart_0818/raw-evidence.txt` (сырые выгрузки). Контроль неизменности состояния: `pc_orchestrator` `LastRun=18.08 14:13:03 Result=267009 Next=01.01.2099` и `pc_orchestrator_watchdog` `NextRun=17:48:00` — те же, что при первом замере; `AutoAdminLogon=0` как было; ни одной команды `schtasks`, `Set-*`, `New-*`, `Stop-*` не исполнялось.

**Ответы на 8 вопросов — по живым фактам:**

1. **Выгрузка.** Единственный триггер — `TimeTrigger StartBoundary=2099-01-01`, без повтора. Принципал — `mxfill1`, **`LogonType=InteractiveToken`** («только для вошедших»), `RunLevel` не задан. Питание: `DisallowStartIfOnBatteries=true`, `StopIfGoingOnBatteries=true` (инертны — десктоп). Сети в условиях нет (`RunOnlyIfNetworkAvailable` отсутствует → false). `RunOnlyIfIdle` отсутствует → весь блок idle мёртв. `StartWhenAvailable` отсутствует → пропуск не догоняется. `AllowStartOnDemand` по умолчанию true — задача существует ради `/Run`.

2. **История.** Заведена **04.07.2026 23:24:44**, `created == modified` посекундно — **2099 стоит с рождения и не правилось ни разу**. Журнал Планировщика **выключен** (`IsEnabled=False`) — истории нет и не будет. Зато журнал полосы по полным телам дал ответ: «2099» — **ноль совпадений**, а «ONSTART» — три, включая `schtasks /Create … /SC ONSTART /RL HIGHEST /RU mxfill1 /F` и `DONE … watchdog(5мин)+ONCE-target зарегистрированы (ONSTART нужна admin у Филиппа)`. **2099 — замена под нехватку прав, записанная в остатки; остаток открыт 45 суток.**

3. **Пароль — НЕТ, не обязателен.** На машине уже живёт `TurboBabyExpectPC`: наш venv-python по триггеру загрузки под **SYSTEM**, «пароль не вводился». Задача невидима неповышенной сессии — доказано контролем: несуществующее имя → «cannot find the file», она → **«Access is denied»** (раздельно через ФС и через COM). Пароль нужен только для ветки `LogonType=Password` от `mxfill1`. У текущей задачи вопрос не встаёт вовсе: при `InteractiveToken` триггер загрузки не изменит ничего.

4. **Механизмы.** Служб — ноль, Run/Startup — ноль (замер). Пять задач контура, все `InteractiveToken`. Вотчдог демона — **отдельная задача `\pc_orchestrator_watchdog`**, не «живущий в userbot», как сказано в диагностике 18.08: в `userbot_listen.py` совпадение одно и то комментарий, а такт лога (`:03/:08/:13/:18…`) посекундно совпадает с `23:23 + PT5M`. Поднимает его **вход пользователя**: за окно простоя пришлось **122 назначенных срабатывания, исполнено ноль**, `NumberOfMissedRuns=0`, и в логе между 03:58:02 и 14:13:03 ни строки.

5. **Moderation_bot — да, то же условие плюс два своих.** Цепь: вход → вотчдог-задача → `schtasks /Run` → демон → `client_watchdog_tick` внутри `poll_once`. Живая цена третьего звена: демон встал 14:13:05, модербот поднят **14:38:55**, потому что `poll_once` синхронно молотил задачу 44. Штатная задержка — до ~45 минут.

6. **Следующая перезагрузка:** контур встанет **на 6–20 часов** (единственный измеренный случай класса — 10 ч 07 м); окно перезагрузок 02:00–08:00 ночное и неустранимо — активные часы уже 18 из 18 возможных.

7. **Варианты (не выполнял):** А — вооружить `autologon_arm.ps1` (владелец вводит пароль в обычном PowerShell; поднимает всё, включая RC; риск — пароль на машине); Б — вотчдог под SYSTEM (админ, без пароля, **в одиночку не работает**); В — мишень под SYSTEM (админ, без пароля, ломает `claude -p`); Г — `LogonType=Password` (админ + пароль + batch-право, **убивает RC**); Д — AnyDesk/Tailscale как жгут (ноль правок, простой не исчезает); Е — сдвиг активных часов (запаса нет). Рекомендую **А** — решение уже принято 07.08, инструмент написан, остаток не закрыт.

8. **Предсмертный взгляд:** починку сделают под SYSTEM (пароля не просит, пример рядом), убедятся по heartbeat, что «демон поднялся сам», — и не заметят, что `claude -p` ищет авторизацию в `C:\Users\mxfill1\.claude\.credentials.json`, которого у профиля SYSTEM нет: демон будет брать задачи и валить их все, а О2/О4/вотчдог промолчат, потому что мерят оборот, а не успех.

Новое временное: **`_scratch_autostart_0818/`** (один файл `raw-evidence.txt`, в git не добавлял).

RESULT: read-only разведка автостарта ПК сделана, артефакт docs/artifacts/2026-08-18-pc-autostart-why.md написан последним действием; забор 2099 объяснён журналом (протез вместо ONSTART, не хватило прав), настоящая причина — LogonType=InteractiveToken (122 срабатывания вотчдога, ноль исполненных), пароль для подъёма по старту НЕ нужен (живое доказательство — TurboBabyExpectPC под SYSTEM, видна только контролем «Access is denied» vs «cannot find the file»), автологон с 07.08 так и не вооружён (AutoAdminLogon=0); ничего не менялось.
