# Полный отчёт задачи 39 — 2026-08-15 17:16 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4671 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 39 |
| записано | 2026-08-15 17:16:03 |
| символов в отчёте | 4671 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Прочитаны переключатели листа «Календарь бронирования», только чтение. Двери к листу у моста НЕТ (37 живых GET-проб). Утверждение владельца «даты в расчёт не входят» — **ОПРОВЕРГНУТО**: `G3=ABS(F3-F2)` кормит кривую срока. Ручки — **одна** пара (H3/I3/J3 лежат на Календаре). Истории положений **нет**. Сверка 722 броней: цепочка сходится там, где ручки стояли как сейчас.

**FACT:**
- `FACT: commit 5d4657d` — артефакт `docs/artifacts/2026-08-15-calendar-switches.md` в `git log`; `FACT: commit 4eba968` — тело журнальной записи.
- `FACT: строка в мозге прочитана обратно` — `cowork_log`, строка #0, 581 симв.: «ДВЕРИ К ЛИСТУ НЕТ — 37 живых GET-проб… ОПРОВЕРГНУТО: G3=ABS(F3-F2)…»; плюс вторая строка (333 симв.) с ответом про историю и путём к артефакту.
- `FACT: живой замер моста` — `season.source` = `"Календарь бронирования H3/I3/J3 (через 'цены альт' B2/B9/B19)"`, значения `0.15 / 0.15 / 0.25`, label `low`.
- `FACT: 0 записей` — все обращения GET; ни одного POST, ни `set_caps`, ни `toggle_cap`; в лист не введено ничего, переключатели не тронуты.

## Что сделано по шагам

**ШАГ 0 — двери нет.** Опрошено живьём 22 имени под этот лист (`caps`, `get_range`, `calendar_get`, `season_get`, `switches`…) — все `unknown_action`; с 15 именами прошлого захода это **37 проб, дверей ноль**. Имён с `set_`/`toggle_`/`write_` в кандидатах не было сознательно. Наружу видно только зеркало ручек и капы через `quote_price`. Новую дверь не делал, Apps Script не трогал.

Опись угла взята не живьём (двери нет), а из **записанной карты 03.07.2026** по XLSX-экспорту с формулами (`cc_log_archive`, строки 237–247). Живьём сегодня подтвердились только значения трёх ручек и блок капов — это в артефакте разделено явно.

**ШАГ 1 — угол описан.** Числа в расчёте: `F2`, `F3`, `G3`, `H3`, `I3`, `J3` (шесть). Подписи: `F1` «Даты и срок», `G2` «Дней», `H2/I2/J2` = «Моты 1»/«Моты 2»/«Скутеры» (пять). Лист — **не календарь**: разметки занятости по датам нет вовсе, только `FILTER` в `F5` под один период.

**ШАГ 2 — цепочка.** База — колонка `B` листа `цены альт` (мост её не отдаёт). `C = B × (1−g)`, затем `цена = round(C × (1 − скидка_за_срок(G3)))` — ручки **перемножаются**, не складываются (проверено арифметикой: 502×0.55=276.1 → замерено 276). Ручек четыре, и одна не та, что ждала постановка: **отдельной «надбавки высокого сезона» не существует** — это те же H3/I3/J3 с минусом, а «скидка за срок» ручкой не является вовсе. Код кодирует это в ярлыке: `label: globalDiscount > 0 ? 'low' : 'high'`. **Истории положений нет**; лист **ничем** не сверяет даты с ручками.

**ШАГ 3 — цепочка объясняет цены**, с границей: низкий сезон 2026 — медиана −0.6 %, 8 броней из 18 сошлись до рубля; май −2.3 %, октябрь −1.8 %. В глубоком сезоне промах в одну сторону: январь **+34.2 %**, февраль +33.4 %, декабрь +25.8 %. Это прямое измерение отсутствия истории. Выводов о торге не делал.

## Что осталось нерешённым (главное)

**Чем `цены альт`!B2/B9/B19 связаны с `H3/I3/J3` — формулой-ссылкой или руками скопированными числами — НЕ УСТАНОВЛЕНО.** Мост и бот читают **зеркало**, а не оригинал. Если копия ручная, владелец крутит H3, видит новую цену у себя, а бот продолжает считать по старому B2 — **расхождение будет молчаливым**. Проверяется одним взглядом в строку формул B2; через дверь — никак.

Отдельно: в постановке H3/I3/J3 названы «ячейками листа „цены альт“» — лист назван неверно, они лежат на Календаре (артефакт задачи 37 писал это правильно, ошибка возникла при пересказе).

Две поправки по ходу: мой первый прогон сверки молча отсеял все 1260 броней — колонка срока приходит **строкой** («7 Д», «3 М»), а не числом; исправлено на `lib.days_of`. Журнальная строка вышла 602 симв. при пороге 600 и была срезана авто-выносом — дописал короткий хвост с недостающими ответами и путём к артефакту.

Красного не касался: `.env` и конфиги не читал, процессы не трогал, ничего не удалял, `_scratch_calendar_0815` оставлен на месте. ПДн наружу не выводились — в срезе броней их нет по построению, в ответе `quote_price` таких полей нет вовсе.

RESULT: переключатели «Календаря бронирования» прочитаны read-only; двери к листу у моста НЕТ (37 живых проб); «цена только от ручек, даты не входят» — ОПРОВЕРГНУТО (G3=ABS(F3-F2) кормит кривую срока, при этом сезонный коэффициент от дат не зависит); ручки — ОДНА пара (H3/I3/J3 на Календаре, B2/B9/B19 — зеркало, которое и читает бот); истории положений НЕТ; сверка 722 броней — цепочка объясняет цены там, где ручки стояли как сейчас (2026 низкий: медиана −0.6 %), в январе-феврале промах +34 %; артефакт docs/artifacts/2026-08-15-calendar-switches.md, коммиты 5d4657d и 4eba968, две строки в cowork_log проверены обратным чтением.
