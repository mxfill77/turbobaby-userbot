# Полный отчёт задачи 17 — 2026-08-17 13:33 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5227 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 17 |
| записано | 2026-08-17 13:33:30 |
| символов в отчёте | 5227 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Задача 16 умерла не от объёма, а от цикла починки: её результат лёг на диск в 13:12:45, таймаут наступил в 13:12:47. Остаток жив — три файла (suggest.py + два untracked), замер цел (честный знаменатель 0/15 → 2/15, оба занижения закрыты). Куратор их не увидел по построению: он считает только коммиты и записи журнала. Разбор — в артефакте, дерево не тронуто.

**FACT:**
- `FACT: commit 8d07f49` в `git log` — артефакт `docs/artifacts/2026-08-17-task16-postmortem.md`
- `FACT: commit 67a2007` в `git log` — тело журнальной записи `docs/artifacts/journal/2026-08-17-133204-done.md`
- `FACT: журнал` — две строки легли в мозг («OK: записано в мозг, символов: 414554» и «…414936»)
- `FACT: патч` — `_scratch_task16_0817/suggest.py.task16-residual.patch`, 3498 б, sha256 `f5e8db82…9b9c62b`
- `FACT: дерево не тронуто` — после обоих коммитов `git status -s --untracked-files=no` → ` M suggest.py` (как было; не откачен, не закоммичен, не в stash)
- `FACT: заход read-only по коду` — правок кода 0, удалений 0, процессов не трогал, боевую базу не открывал

---

## 1. Дерево и патч

```
 M suggest.py            (25 строк добавлено, 2 удалено)
?? price_source.py       18297 б  — untracked
?? test_price_source.py           — untracked
```

Остаток — **не один файл, а три**. Патч снят в `_scratch_task16_0817/suggest.py.task16-residual.patch`.

## 2. Чей остаток — ПРИНАДЛЕЖИТ 16 (основание не время)

Диff дословно исполняет ТЗ (`import price_source` → `price_source.reprice` в единственной точке котировки), а имя флага сходится с единственным объявленным отличием прогона в `run26.py` — `PRICE_SOURCE_SEASON`. Штамп claim (`task_started.json["16"]` = `12:27:47.058918+00:00`, pid 1748) — основание третьей силы, не первой. Контрпроверка: задача 14 закрыта в 12:12, задача 17 взята в 13:23, коммитов в окне 0.

## 3. ТЗ 16 дословно — и граница

```
"goal": "ПОДКЛЮЧИТЬ БОТА К price_source.json И ЗАМЕРИТЬ. Промпт НЕ трогать."
```

**Это первая строка, а не всё ТЗ.** Слепок `queue_snapshot_pc.state.json` режет `goal` на ~60 символах; полное тело живёт в ряду очереди на мосту и отсюда недостижимо без чтения секретов. Полный текст — **НЕИЗВЕСТНО**.

## 4. Арифметика

**26 единиц.** Замер цены единицы **ЕСТЬ**, два независимых: коммит `164cea9` — «62 пары × 50с = 3091с при пределе 2700»; артефакт `2026-08-17-regression-fullinput.md:71` — медиана 46.9 с/пара, сумма 1337с на тех же 26.

```
26 × 50с = 1300с из 2700с  →  48% окна
фактически              1482с  →  55% окна (57.0 с/пара)
```

**Закрыться по построению МОГЛА** — в прямую противоположность задаче 36 (62 × 50 = 3091 > 2700, невозможна до старта). Убил не объём единиц: баланс окна вышел 756с подготовки + 1482с прогона + 462с разбора-починки-перепрогона = ровно 2700. На коммит, артефакт и журнал осталось 0 секунд, потому что в бюджете не было запаса на цикл «померил → нашёл дефект → починил → перемерил», а именно он и случился.

## 5. Детектор «следов нет»

`pc_orchestrator.py:1269` (литерал) ← `fail_result` `:1246` ← `_work_evidence` `:1228`, а он возвращает **ровно два** списка: `_git_commits_between` `:1184` (это `git log`) и `_journal_writes_between` `:1202` (реестр `cowork_log.ledger`). **Незакоммиченное в счёт не входит по построению** — ни `git status`, ни `git diff`, ни обход диска не зовутся ни одной строкой; untracked — тем более.

Острее всего это видно так: через 5 секунд после «следов работы нет» **тот же процесс** пишет «авто-фетч: рабочая копия грязная … файлы: suggest.py» — другой веткой (`:6096`), с вердиктом не связанной. И даже та, зрячая, ветка на `:6091` фильтрует `??`, то есть увидела бы треть остатка. Не правил — точки касания в остатках артефакта.

## 6. Спасено (главное)

Честный знаменатель по цене **0/15 → 2/15**, оба живых занижения закрыты (dlg 179: 317→437 при уплаченных 464; dlg 120: 577→710 при 798), модель не сдвинулась (4/0/17/5 до и после). **Регресс назван честно:** dlg 258 попадание потерял, прирост читается как «+3 −1». Гейт ДО правки красен на 2 — известный храповик `main`; **гейта ПОСЛЕ правки в остатке нет**, зелёность подключения НЕ ДОКАЗАНА.

## Остатки (ничего не сделано, клиентский контур не правил)

1. Прибор куратора слеп к незафиксированной работе — точки касания `:1184`, `:1202`, `:1228`, `:1269`, `:6091`, `:6096`. Класс общий с полосой VPS, **на ту полосу не проверено**.
2. Три файла остатка не закоммичены и не отвергнуты — решение владельца.
3. Гейт после правки не прогонялся.
4. Регресс dlg=258 не объяснён.
5. Сдвиг вердиктов по датам (6 пар) при правке, датам не адресованной, не объяснён.
6. Полный текст ТЗ 16 с этой полосы недостижим.
7. Бюджет окна не учитывает цикл починки — устройство, а не невезение.

RESULT: разбор задачи 16 собран и зафиксирован — артефакт docs/artifacts/2026-08-17-task16-postmortem.md (коммит 8d07f49, тело журнала 67a2007), патч остатка в _scratch_task16_0817/, две строки в cowork_log; остаток принадлежит 16 по содержанию диффа, замер цел (0/15 → 2/15, оба занижения закрыты), закрыться могла (26 × 50с = 1300с из 2700с) — убил цикл починки, итог лёг за 2 секунды до таймаута; детектор «следов нет» назван по строкам (pc_orchestrator.py:1269 ← :1228 = git log + реестр журнала, незакоммиченное не входит); код не правился, дерево не менялось.
