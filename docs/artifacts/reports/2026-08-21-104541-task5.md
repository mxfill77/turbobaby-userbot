# Полный отчёт задачи 5 — 2026-08-21 10:45 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5227 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 5 |
| записано | 2026-08-21 10:45:41 |
| символов в отчёте | 5227 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

**Ревизор помнит отказы механизмом, который читается каждый прогон, но заполняется ТОЛЬКО руками через CLI — кода «отказ → вердикт» нет ни одной строкой; глушение не срабатывало ни разу. Ложняк рождает токен `разбира\w+` в `_AV_SCARCITY_RE`. За 7 суток: 26 выдач / 13 разных / 2 повтора отвергнутого / 0 в ленту.**

**FACT:** commit `046e9c3` в git log (артефакт `docs/artifacts/2026-08-21-auditor-false-repeat.md`); дерево на входе `a6f7533`, замер 2026-08-21 17:33–17:47; журнал: две строки легли («OK: записано в мозг, символов: 524780» и «525314»). Кода не правил, ревизора не трогал, гейт не запускал — правки только в `docs/artifacts/` + untracked `_scratch_revizor_repeat_0821/`.

Артефакта до начала не было — задача отвечается впервые.

**1. Условие чека дословно.** `suggest.py:7352` → `availability_violations(client, avail=None)`. Слово ловит НЕ паттерн наличия, а паттерн дефицита `_AV_SCARCITY_RE` (`suggest.py:6811`), отдельной альтернативой `разбира\w+` посреди идиом «последний/осталось/успевай». Дальше `_av_unsupported` (`:6872`) милует только `avail_pos` при `state=True` и `avail_neg` при `state=False`; **`scarcity` — нарушение ВСЕГДА** («источника данных нет»). Живой прогон:
```
availability_claims('разбираемся') = [{'kind': 'scarcity', 'raw': 'разбираемся'}]
_AV_SCARCITY_RE  → альтернатива 'разбира\w+'
чек «нет утверждений о наличии» ok=False | факт: клеймы наличия: разбираемся
```
Это **рецидив уже закрытого класса**: комментарий на `suggest.py:6841` описывает такой же ложняк соседнего токена `успева\w+` (id=360) — его сузили до повелительных форм, а `разбира\w+` оставили широким.

**2. Ключевой ответ — отказ не читается никогда.** Реестр есть (`pc_orchestrator.revizor_verdicts.json`, ключ `класс|окно|предмет`), читается каждым прогоном (`:8529`), фильтр исправен. Но `revizor_set_verdict` зовётся **из одного места — ветки CLI `--revizor-verdict` (`:9344`)**. Отказ владельца живёт отдельно: `failed` с префиксом `отклонено Филиппом`; его читают `NO_HEAL_PREFIXES`, `card_terminal_log.py`, `queue_snapshot_pc.py` — **ревизора среди них нет**, в логе демона отказ не оставляет даже строки (пишется только `APPROVED`). Единственная межпрогонная память — `_revizor_find_owner_card` ищет карточку в статусе `needs_approval`: ответили → `prior_lines` пусты → находка пишется заново. Реестр: 6 записей, все «ложная», **все `hits=0`**, mtime 10.08 — не менялся 11 суток; ключей про наличие — **0**; вердикт нашей находки = «не разобрана» → доставлено владельцу.

**3. Два числа: 13 разных находок, из них 2 — повтор уже отвергнутого** (всего выдач 26, лишних сверх первой 13). Повторы: `#92|1791810538|нет утверждений о наличии` (20.08 карточка 58 → отказ → 21.08 карточка 4, ключ и улика совпадают) и `#92|8378811571|депозит без противоречий` (седьмая того же чека при шести вердиктах «ложная» — не заглушена, **потому что окно другое, а окно входит в ключ**).

**4. Признак — только поле `action`.** У детерминированного прохода оно **зашито константой**: `_revizor_postrelease_findings` кладёт `"action": "owner"` каждой находке #92 (`:8371`), то же у #93. Важность не оценивается ничем. **Премиса про «семь ушедших в ленту» не подтвердилась**: в ленту за 7 суток не ушло ничего (`noise`=0, заглушено 0, выбыло по порогу 0, раздел `checks` пуст). Единственная «семёрка» — строка 19.08 21:06:18 про движение в обратную сторону: «7 находок по КЛИЕНТСКОМУ контуру переведены из задач в **owner-карточки**».

**5. Правил не видит вовсе.** Думатель идёт с `--max-turns 1`, `--allowed-tools ""`, cwd=tempdir — файлов не читает физически; его мир — преамбула 3418 символов (роль + 7 классов формы ответа + контракт). Бизнес-правил в чек-листе нет; ступеней сумм (3000/5000/7000) — нет. Чек депозита — `if len(deps) > 1: return "разные суммы депозита"`, без таблицы. **Пересчёт своим проходом: ступеней шесть, а не семь** (3000·5000·7000·15000·20000·25000 на 20 моделей, `suggest.py:276-290`) — бот печатает прайс целиком, чек зовёт лестницу противоречием.

**6. Минимум (назвал, не строил):** провод «отказ → `revizor_set_verdict(«ложная»)`» с разбором строк `• [класс X] окно cid: улика` отвеченной карточки; сохранять при доставке соответствие «карточка tid → ключи» (сейчас его нет нигде, а кнопка одна на N строк); для #92/#93 убрать окно из ключа — иначе новое окно воскрешает разобранное, а порог `≥20 разобранных` недостижим (набралось 6 за 12 суток). Отдельно — корень: сузить `разбира\w+`.

**7. Предсмертный взгляд:** починку сделают проводом «отказ → вердикт», она сработает на той же улике и том же окне и провалится на следующем тике, когда регулярка поймает другое слово или другое окно — замок `:7535` честно скажет «существо находки изменилось», и та же ложная находка приедет как новая: глушили находку, а не причину.

RESULT: разведка read-only выполнена, ответы на 7 вопросов даны живыми фактами; артефакт docs/artifacts/2026-08-21-auditor-false-repeat.md закоммичен (046e9c3), журнал ARTIFACT+DONE записан; корень ложняка — токен разбира\w+ в suggest._AV_SCARCITY_RE, корень повтора — отсутствие кода «отказ владельца → вердикт» (реестр пишется только руками, глушение не срабатывало ни разу); за 7 суток 26 выдач / 13 разных / 2 повтора отвергнутого / 0 в ленту; ничего не чинил.
