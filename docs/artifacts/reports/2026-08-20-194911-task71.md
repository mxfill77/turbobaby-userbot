# Полный отчёт задачи 71 — 2026-08-20 19:49 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **6397 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 71 |
| записано | 2026-08-20 19:49:11 |
| символов в отчёте | 6397 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

**Гейт прогнан ЦЕЛИКОМ: 9 наборов из 9, 3981 тест, 442.7 с, красных 66.** 60 из них — прямо путь цены (котировка гаснет сторожем, голдены ждут число). Обе прежние известные пары красных на месте и обе выросли. Кода не правил, ничего не чинил.

**FACT:** commit `ce464ee` в `git log` (артефакт `docs/artifacts/2026-08-20-gate-after-switch.md`, 9 наборов дописаны по одному); две строки в мозге — «OK: записано в мозг, символов: 508100» (ARTIFACT) и «508627» (DONE).

---

## 1. Первое действие и признак сделанности

- Дерево: **`3b85c06`** (2026-08-21 01:55), прогон начат **02:31:33**, закончен **02:46:08**.
- Артефакта `docs/artifacts/2026-08-20-gate-after-switch.md` **не было** → прогнано ноль наборов, шёл с начала.

## 2. Перепись наборов (до прогона)

60 тест-модулей в git, 3897 тестов по грепу → **9 наборов**. Фактические числа из прогонов:

| № | набор | модулей | тестов | красных | сек | итог |
|---|---|---|---|---|---|---|
| 1 | **ПУТЬ ЦЕНЫ** (price_source, price_gate, price_freshness, pricing) | 4 | 176 | **20** | 25.9 | 🔴 |
| 2 | **SUGGEST** | 1 | 569 | **35** | 166.1 | 🔴 |
| 3 | **КЛИЕНТСКИЙ КОНТУР** | 8 | 345 | **4** | 36.3 | 🔴 |
| 4 | **ГАРД** | 10 | 634 | **3** | 110.3 | 🔴 |
| 5 | **ОРКЕСТРАТОР** | 1 | 851 | **2** | 69.0 | 🔴 |
| 6 | **ОЧЕРЕДЬ И ИСПОЛНЕНИЕ** | 12 | 396 | **2** | 16.5 | 🔴 |
| 7 | **МОЗГ · ЖУРНАЛ · МОСТ** | 6 | 260 | 0 | 12.3 | 🟢 OK |
| 8 | **УРОКИ И ТРЕНАЖЁР** | 9 | 357 | 0 | 5.1 | 🟢 OK |
| 9 | **СРЕДА ПК · RC** | 9 | 393 | 0 | 1.2 | 🟢 OK |
| | **ИТОГО** | **60** | **3981** | **66** | **442.7** | |

Непрогнанных нет. Ни одного зависания и ни одного таймаута — заходы 67/70 умирали на объёме, здесь самый долгий набор занял 166 с.

## 3. Дословный вывод (сводные строки каждой команды)

```
$ ... -m unittest test_price_source test_price_gate test_price_freshness test_pricing
Ran 176 tests in 25.878s / FAILED (failures=20)
$ ... -m unittest test_suggest
Ran 569 tests in 166.095s / FAILED (failures=33, errors=2)
$ ... -m unittest test_client_contour test_delivery test_golden_llm test_playbook_rules \
      test_intake_bridge test_booking_draft test_dialog_card test_moderation
Ran 345 tests in 36.308s / FAILED (failures=4, skipped=10)
$ ... -m unittest test_pretool_guard ... test_moderation_card
Ran 634 tests in 110.295s / FAILED (failures=3)
$ ... -m unittest test_pc_orchestrator
Ran 851 tests in 68.979s / FAILED (failures=2)
$ ... -m unittest test_pc_local_dec ... test_nonparse_ratchet test_reviewer
Ran 396 tests in 16.492s / FAILED (failures=2)
$ ... -m unittest test_brain_writer ... test_log_setup         Ran 260 tests in 12.306s / OK
$ ... -m unittest test_lesson_router ... test_trainer_run       Ran 357 tests in 5.053s / OK
$ ... -m unittest test_expectations_pc ... test_userbot_reconnect  Ran 393 tests in 1.203s / OK
```

Поимённые списки всех 66 красных с текстами отказов — в артефакте (полный дословный перечень занял бы здесь пару экранов). Опорные тексты:

```
AssertionError: '472' not found in 'ЦЕНА: точная цена из Календаря сейчас недоступна — НЕ
называй никакого числа (в т.ч. из FAQ); ответь, что уточнишь цену и вернёшься.'
AssertionError: 'Уточню у команды и вернусь.\n[уточнить: цена 6608]' != 'XSR 155: 6608 ฿ …'
факт: quote-блок не собран — цена не доехала
AssertionError: 994 not less than or equal to 845
AssertionError: '566d726c…' != 'f1e3eb1b…'   (foreign='brain_writer.py')
```

## 4. Задевает ли красное путь цены

| красных | что это | путь цены |
|---|---|---|
| **60** | test_pricing 20, test_suggest 35, dialog_card 1, moderation 2, moderation_card 2 — котировка гаснет сторожем | **ДА, прямо** |
| 2 | `TestSelfUpdateDepClosure` — незаявленные ленивые зависимости | косвенно, именами файлов |
| 2 | `test_nonparse_ratchet` — базовая отметка неразбора | нет |
| 1 | `test_client_contour`: «brain_writer.py обязан быть внутренним» | нет |
| 1 | `test_pretool_guard.TestMarkerIsolation` — производный от подсуиты | нет |

**Мина отчёта, чуть не давшая ложное число:** в наборе 4 в выводе **две** строки `Ran` — маркер-изоляция поднимает подсуиту отдельным процессом (`Ran 1244 / failures=2`). Считать надо внешние 634/3, иначе набор читается как 5 красных.

## 5. Прежние известные красные — те же или новые

**Обе прежние пары НА МЕСТЕ, обе ВЫРОСЛИ; третьей известной пары не появилось.**

- **Храповик неразбора** (2) — тот же, что был красен на `main` до захода: линия не знает **8** файлов, итог **994 против отметки 845**.
- **Замыкание зависимостей self-update** (2) — тоже прежний, но список внутри стал **пятёркой**, и одно из добавочных — **`price_gate.py`**, созданный коммитом `45a38cf`, второе — `price_freshness.py`. Практическое следствие (не чинил, только называю): ступень self-update не считает эти файлы своими → их правка не поднимет версию демона.

Новыми являются 60 красных пути цены и 2 не-ценовых (`client_contour`, маркер-изоляция).

## 6. Атрибуция и честная граница

`git show --stat 45a38cf` — коммит тронул **пять** файлов: `price_gate.py`, `price_source.py`, `suggest.py`, `test_price_gate.py`, `test_price_source.py`. Тестовых среди них два, и **оба зелёные**. А `test_pricing.py`, `test_suggest.py`, `test_moderation*.py`, `test_dialog_card.py` коммит **не трогал** — их голдены остались от эпохи живого листа. Причина 60 красных материальна: **не поломка врезки, а неприведённые к новому источнику ожидания**.

Чего прогон НЕ говорит: набор гнался **без живого моста**, и `price_gate` в такой среде обязан отвечать `may_quote=False` — это его заявленное поведение. Красное здесь значит «тесты не отличают законное молчание от поломки», а не «бот перестал называть цену клиенту». Живой проверки котировки на работающем мосту в заходе не было.

Запреты соблюдены: кода не правил, ручку источника цены и сторож не трогал, клиентам не писал, ботов не перезапускал, выкатки нет, в таблицы не писал, ничего не удалял, `.env` не читал, всё синхронно, в фон ничего не уводил.

RESULT: полный гейт прогнан наборами целиком — 9/9 наборов, 3981 тест, 442.7 с, 66 красных (60 — путь цены: голдены ждут число, где сторож отвечает молчанием); обе прежние известные пары красных на месте и выросли (храповик 994 против 845; замыкание не знает 5 файлов, прибыли price_gate.py и price_freshness.py); наборы 7–9 зелёные целиком; артефакт docs/artifacts/2026-08-20-gate-after-switch.md дописывался после каждого набора, коммит ce464ee, журнал ARTIFACT+DONE записан; ничего не чинилось.
