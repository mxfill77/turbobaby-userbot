# Полный отчёт задачи 15 — 2026-08-18 18:32 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **6532 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 15 |
| записано | 2026-08-18 18:32:50 |
| символов в отчёте | 6532 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

All checks complete. Nothing was written to any brain node; the only file touched is the mandated artifact.

**Артефакт от прошлого захода (01:25) НЕ пустой — все четыре вопроса отвечены. Перепрогнал все три чистых чтения свежими процессами: числа воспроизвелись до символа. ВСЁ НА МЕСТЕ: `business_rules` 26 636 симв. (525 строк, U+FFFD 0) — все ПЯТЬ записей 17–18.08 есть; `master` 102 961 симв. (1092 строки) — автологон стр. 45, «17.09.2026» стр. 1076. Не пропало ничего.**

**FACT:** read-only. Живое доказательство — три чистых чтения моста по ИМЕНИ через живой реестр (35 ключей, ни одного хардкоженного id): `name:business_rules` = 26636 симв. и `name:master` = 102961 симв.; `_scratch_brbuckets_0818/verify.py` → `ВСЕ ПРОЙДЕНЫ` exit=0; `_scratch_autologon_0818/verify_master.py` → `20 замков из 21` exit=1 (единственный провал — ошибка прибора, доказано ниже). В узлы мозга не записано ни байта: звались только `list_brain`, `resolve_name`, `read_text`.

## Признак сделанности

`docs/artifacts/2026-08-18-brain-integrity.md` есть, 21 018 байт, mtime **19.08 01:25**. Открыт — **не пустой, все четыре вопроса отвечены**. Текущее время замера **01:30:43 +0700**: прошлый заход был **пять минут назад**, а не сутки. Поэтому мой вклад — **воспроизводимость** (числа не выдуманы), а не независимое наблюдение суткам спустя; выдавать его за второе я не стану.

## Дословный вывод

**1. `probe_nodes.py heads KB_business_rules`** (сокращён перечень заголовков — он дословно лежит в §1 артефакта, номера строк совпали до единого):
```
РЕЕСТР ЖИВОЙ: ключей 35 (не считая folder_id)
ИМЯ KB_business_rules -> name:business_rules   [имя KB_business_rules → ключ реестра business_rules (канон business_rules)]
ЖИВОЕ ЧТЕНИЕ name:business_rules: 26636 символов, строк 525, U+FFFD 0
=== ЗАГОЛОВКОВ ОПОЗНАНО: 31 ===
 ... 293/303/314/327/339/350/360/374 (семь развилок) ... 386 РЕАЛЬНЫЕ СРОКИ АРЕНДЫ ... 452 РАЗБИВКА ТОЧНОСТИ ПО КОРЗИНАМ СРОКА: ВСЕ ШЕСТЬ
```

**2. `probe_nodes.py find KB_business_rules` — ПЯТЬ записей, по каждой ЕСТЬ/НЕТ:**
```
=== ПОДСТРОКА 'РАЗВИЛКИ ЦЕНЫ': СОВПАДЕНИЙ 1 ===
  293 > РАЗВИЛКИ ЦЕНЫ: СЕМЬ ВОПРОСОВ ВЛАДЕЛЬЦУ (замер 17.08)
=== ПОДСТРОКА 'смешанный период': СОВПАДЕНИЙ 1 ===
  407 >   Правило: смешанный период — бот спрашивает; он обязан сказать,
=== ПОДСТРОКА 'Уточнение владельца 18.08': СОВПАДЕНИЙ 2 ===
  389 > - Уточнение владельца 18.08.2026, отменяет прежнюю формулировку 17.08 об этой длине.
  400 > - Уточнение владельца 18.08.2026 о сезоне длинной аренды.
=== ПОДСТРОКА 'ТОЧНОСТЬ ЦЕНЫ НА РАБОЧЕМ ДИАПАЗОНЕ': СОВПАДЕНИЙ 1 ===
  427 > ТОЧНОСТЬ ЦЕНЫ НА РАБОЧЕМ ДИАПАЗОНЕ (замер 18.08)
=== ПОДСТРОКА 'РАЗБИВКА ТОЧНОСТИ ПО КОРЗИНАМ': СОВПАДЕНИЙ 1 ===
  452 > РАЗБИВКА ТОЧНОСТИ ПО КОРЗИНАМ СРОКА: ВСЕ ШЕСТЬ
```
| # | запись | вердикт |
|---|---|---|
| 1 | развилки цены от 17.08 | **ЕСТЬ** (стр. 293, все 7 пунктов) |
| 2 | правило про смешанный период | **ЕСТЬ** (стр. 405–411) |
| 3 | уточнение про длинную аренду | **ЕСТЬ** (стр. 389–390 и 400–401) |
| 4 | вывод о годности расчёта на рабочем диапазоне | **ЕСТЬ** (стр. 427, вывод 437–442) |
| 5 | разбивка по корзинам срока | **ЕСТЬ** (стр. 452, шесть корзин) |

**3. `probe_nodes.py find KB_MASTER "ИНСТРУКЦИЯ АВТОЛОГОНА" "17.09.2026"`:**
```
ЖИВОЕ ЧТЕНИЕ name:master: 102961 символов, строк 1092, U+FFFD 0
=== ПОДСТРОКА 'ИНСТРУКЦИЯ АВТОЛОГОНА': СОВПАДЕНИЙ 2 ===
   45 > === ИНСТРУКЦИЯ АВТОЛОГОНА — ВООРУЖЕНИЕ, ОТКАТ, ПРОВЕРКА (18.08.2026) ===
=== ПОДСТРОКА '17.09.2026': СОВПАДЕНИЙ 1 ===
 1076 > - ROTATE СЕКРЕТОВ: MODERBOT_TOKEN — утёк в лог, РОТИРОВАТЬ. EdenAI-ключ + GitHub PAT — ротация ОТЛОЖЕНА до 17.09.2026.
```
`СОВПАДЕНИЙ 2` — не размножение: поиск регистронезависимый, второе попадание это проза стр. 47 «здесь лежит инструкция автологона целиком». Сам заголовок ровно один.

**4. `verify.py` (сверка с телом, посчитанным ДО записи 18.08 18:48):**
```
OK   живое тело СОВПАЛО С ЗАДУМАННЫМ символ в символ  | живое 26636, задуманное 26636
OK   все 21 прежних заголовка целы, число вхождений не изменилось  | расхождения: нет
OK   ОБРАТИМОСТЬ по ЖИВОМУ телу: снятие вставки даёт прежнее символ в символ  | после снятия 22740 против прежних 22740
ИТОГ ЗАМКОВ ПОСЛЕ ЗАПИСИ: ВСЕ ПРОЙДЕНЫ           EXIT=0
```

**5. `verify_master.py`:** `[OK] совпадает с задуманным СИМВОЛ В СИМВОЛ`, `[OK] удалено НОЛЬ символов`, `[OK] снятие вставки возвращает прежнее тело СИМВОЛ В СИМВОЛ`, все 10 шагов и порядок 1→10 целы; единственный `[ПРОВАЛ] ... ОТКРЫТО:: 1→3`, `ИТОГ: 20 замков из 21 зелёные`.

**6. Провал добит НЕЗАВИСИМО от прибора** (этого в артефакте не было — там опора шла на вывод того же сверщика):
```
meta.json:9-33  — 25 РАЗНЫХ строк-заголовков, у каждой значение 1 (пометка множества, не счётчик)
before.txt      — 'ОТКРЫТО:' встречается 3 раза: строки 132, 187, 784
```
Настоящий переход — **3 → 3, без изменения**. `1 → 3` сравнивает пометку со счётом подстроки. Порчи нет.

## Сводка

1. **Всё на месте, не пропало ничего** из семи названных записей. Длины: `business_rules` — **26 636 символов** (525 строк), `master` — **102 961 символ** (1092 строки), мусора кодировки в обоих ноль.
2. **Пункт 3 задачи («пропало — назвать, когда наблюдалось живым») не наступил** — терять нечего.
3. **Гонка сюда не достала, и это измеримо:** она требует соседа-писателя ближе 15 с (9 потерь из 9 имели такого, 0 из 56 без него). Записи ПК в `business_rules` разделены **18 ч 32 м**, в `master` соседа нет вовсе — разрыв в четыре порядка. Узлы правил пишут редкие осознанные заходы, журнал — много писателей fire-and-forget.
4. **Честная граница:** доказано снимком «задуманного тела», а не свойством канала — **замка от гонки в канале как не было, так и нет**; окно серверных записей до 18.08 18:44 с этой полосы не видно; проверены 2 узла из 35.
5. **Журнальную строку не писал СОЗНАТЕЛЬНО:** `cowork_log` — тоже узел мозга, а запрет задачи «в узлы мозга не писать ни байта» сильнее ритуала журнала. Коммита тоже нет: он мог дёрнуть хук гейта, а гейт гонять запрещено. Артефакт лежит на диске untracked — как и прочие артефакты этого репо.

Единственная правка на диске — раздел §7 в `docs/artifacts/2026-08-18-brain-integrity.md`, дописан последним действием.

RESULT: Артефакт прошлого захода не пуст и отвечает на все 4 вопроса; перепроверил живым чтением — ВСЁ ЦЕЛО: business_rules 26636 симв. со всеми ПЯТЬЮ записями 17–18.08, master 102961 симв. с автологоном (стр. 45) и «17.09.2026» (стр. 1076); не пропало ничего, единственный красный замок доказан как ошибка прибора (3→3, а не 1→3); в мозг не записано ни байта, дописан §7 артефакта.
