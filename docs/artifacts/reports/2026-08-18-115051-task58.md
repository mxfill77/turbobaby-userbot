# Полный отчёт задачи 58 — 2026-08-18 11:50 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **6634 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 58 |
| записано | 2026-08-18 11:50:51 |
| символов в отчёте | 6634 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

**FACT:** узел `business_rules` (id из живого реестра `1bqdkcecYdIo…`, адресован ПО ИМЕНИ) — свежий процесс прочитал назад **26636 символов** против прежних 22740, прирост **3896** = длина вставки 3894 + 2 разделителя абзаца; фраза «корзины срока разнесены» — **1 вхождение**; все **21** прежний заголовок целы; снятие вставки возвращает прежнее тело **символ в символ** (22740 → 22740). Бэкап: `tmp/brain_backup_business_rules_buckets_20260818-184835.txt`.

**Сводка.**
1. Признак сделанности проверен ПЕРВЫМ действием: фразы в узле было **0** вхождений → работа не сделана, пишем. Артефакт `docs/artifacts/2026-08-17-price-accuracy-working-range.md` на диске **есть** (489 строк), коммит `fcb64b1` в `git log` подтверждён — «НЕИЗВЕСТНО» не наступило.
2. Дописан один блок «РАЗБИВКА ТОЧНОСТИ ПО КОРЗИНАМ СРОКА: ВСЕ ШЕСТЬ» — вставлен якорем перед разделом запрета безопасности, чтобы встать сразу за разделом про рабочий диапазон, который заканчивался пометкой «НЕ ЗАКРЫТО: …распределение остальных двух третей по корзинам срока в этот узел не перенесено». Ровно эта пометка и закрыта.
3. Шесть корзин одной строкой каждая (1–3, 4–6, 7–14, 15–29, 30–179, 180+): границы, брони, доля броней, доля денег, медиана отклонения, попадание в ±15 %. Числа взяты из §1 артефакта без пересчёта, сходятся до знака.
4. **Свежее окно последнего года дано отдельно только для корзины 7–14** (127 броней, медиана −1.8, попадание 81.1, промах 8.1) — для остальных ПЯТИ корзин таких чисел в артефакте нет, и это написано в узле прямым текстом, а не додумано (проверено грепом: единственное вхождение `старт с 2025-08-18` — строка 356, «тот же диапазон»).
5. Три вывода словами: **годно** — только 7–14 (при трёх названных условиях); **молчать и звать человека** — 180+ (плюс модели без цены в любой корзине); **судить не по чему** — четыре корзины без вердикта и без свежего окна плюс тонкая 180+.

**Дословный вывод команд.**

`venv/Scripts/python.exe _scratch_brbuckets_0818/probe.py` — адресация по имени через живой реестр:
```
РЕЕСТР ЖИВОЙ: ключей 35 (без folder_id)
  есть ли business_rules среди ключей: ДА
РАЗРЕШЕНИЕ ИМЕНИ: business_rules -> name:business_rules  [ключ реестра]
  (id не хардкодим; для сведения — id из реестра: 1bqdkcecYdIo2lPGbSQmti6HyTGS0JEU1)
ЧТЕНИЕ: 22740 символов, строк 455, U+FFFD 0
ПРИЗНАК СДЕЛАННОСТИ: вхождений фразы 'корзины срока разнесены' = 0 -> не сделано, можно писать
```

`venv/Scripts/python.exe _scratch_brbuckets_0818/prep.py` — замки ДО записи (локально, в сеть не ходит):
```
ТЕЛО ДО: 22740 символов, '\r' в нём: 0
ВСТАВКА: 3894 символов, строк 70
ЗАГОЛОВКОВ В ДОКЕ ДО: 21
OK   фразы-признака в старом теле нет  | вхождений 0
OK   нового заголовка в старом теле нет
OK   фраза-признак в теле вставки РОВНО 1  | вхождений 1
OK   новый заголовок в теле вставки РОВНО 1
OK   существующие заголовки в теле вставки НЕ процитированы  | процитировано: нет
OK   якорь найден РОВНО 1 раз  | совпадений 1
OK   require_above стоит выше якоря
OK   в новом теле фраза-признак РОВНО 1
OK   в новом теле новый заголовок РОВНО 1
OK   прежние заголовки целы, число вхождений не изменилось  | сверено 21 из 21; расхождения: нет
OK   прирост = длина вставки + 2 разделителя абзаца  | было 22740, стало 26636, прирост 3896, len(вставки) 3894
OK   ОБРАТИМОСТЬ: снятие вставки даёт прежнее тело символ в символ  | длина после снятия 22740 против 22740
OK   U+FFFD в теле вставки нет
OK   гард усыхания не сработает (новое длиннее)
ИТОГ ЗАМКОВ ДО ЗАПИСИ: ВСЕ ПРОЙДЕНЫ    (EXIT=0)
```

`venv/Scripts/python.exe _scratch_brbuckets_0818/write.py` — единственная запись:
```
СТАТУС: ok | док name:business_rules
ДЛИНА: было 22740 -> стало 26636 (прирост 3896)
БЭКАП: D:\turbobaby-bot\tmp\brain_backup_business_rules_buckets_20260818-184835.txt
РАСПИСКА МОСТА: ok
--- фрагмент вокруг вставки --- (полностью воспроизведён выше по сессии; это дословно тело insert.txt,
    вставшее между строкой «в этот узел не перенесено.» и заголовком запрета безопасности)
```

`venv/Scripts/python.exe _scratch_brbuckets_0818/verify.py` — **свежий процесс, расписка моста не участвует**:
```
АДРЕС (свежее разрешение имени): name:business_rules [ключ реестра]
ЖИВОЕ ТЕЛО: 26636 символов, строк 526, U+FFFD 0

OK   живое тело СОВПАЛО С ЗАДУМАННЫМ символ в символ  | живое 26636, задуманное 26636
OK   прирост = длина вставки + 2 разделителя абзаца  | было 22740, стало 26636, прирост 3896, len(вставки) 3894
OK   фраза-признак в живом теле РОВНО 1  | вхождений 1
OK   новый заголовок в живом теле РОВНО 1  | вхождений 1
OK   тело вставки лежит цельными строками РОВНО 1 раз  | вхождений 1
OK   все 21 прежних заголовка целы, число вхождений не изменилось  | расхождения: нет
OK   хвост дока на месте  | последняя строка: '=== конец документа ==='
OK   ОБРАТИМОСТЬ по ЖИВОМУ телу: снятие вставки даёт прежнее символ в символ  | после снятия 22740 против прежних 22740
ИТОГ ЗАМКОВ ПОСЛЕ ЗАПИСИ: ВСЕ ПРОЙДЕНЫ    (EXIT=0)
```

`grep -n "2025-08-18\|окн" docs/artifacts/2026-08-17-price-accuracy-working-range.md` → строка 356 `старт с 2025-08-18  n=127  медиана -1.8 | |мед| 8.1 | ±15 % 81.1 | ±5 % 31.5` — единственная; она под шапкой «ПО ОКНАМ СТАРТА (тот же диапазон)», то есть только 7–14. Утверждение узла «для остальных пяти корзин чисел нет» — факт, а не осторожность.

**Границы соблюдены.** Бот не запускался; клиентских файлов не касались; в рабочие таблицы не писано; **тронут ровно один узел мозга**, других не открывали на запись; пульс не обновлялся; ничего не удалено; кода репозитория не правлено (`git status` по заходу: только untracked `_scratch_brbuckets_0818/`); цена не пересчитывалась — все числа перенесены из артефакта; `.env` и конфиги не читались, секреты брал сам `brain_writer`. **Строку `DONE` в `cowork_log` НЕ писал сознательно:** это узел мозга, а задача явно запрещает трогать другие узлы; ритуал журнала адресован сессиям Dispatch/Cowork/RC, headless-задача очереди отчитывается через `result_ref`.

**Остаток, честно:** прежняя пометка «НЕ ЗАКРЫТО» в разделе про рабочий диапазон физически осталась в теле — снять её значило бы править существующий текст, что выходит за «одну запись в узел»; новый блок называет её закрытой словами. Если владелец хочет вычистить — это отдельная правка.

RESULT: в узел мозга business_rules (адресован по имени через живой реестр) добавлен блок «РАЗБИВКА ТОЧНОСТИ ПО КОРЗИНАМ СРОКА: ВСЕ ШЕСТЬ» с фразой-признаком «корзины срока разнесены», шестью корзинами (границы/брони/доля броней/доля денег/медиана/±15 %), пометкой что свежее окно посчитано только для 7–14, и тремя выводами словами; 22740 → 26636 символов, все 14 замков до и 8 замков после записи пройдены свежим процессом.
