# Полный отчёт задачи 52 — 2026-08-22 14:06 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4850 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 52 |
| записано | 2026-08-22 14:06:37 |
| символов в отчёте | 4850 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Флап побеждён: три прогона одной командой дали 12/12 каждый с совпавшим покейсно составом красных; отрицательный тест краснеет стабильно во всех трёх. Причина названа наблюдением: кейсы 5 и 10 — недетерминизм ГОЛОВЫ (тот же промпт побайтно → 3 разных ответа), кейс 12 — ЖИВАЯ СЕТЬ. Лекарство — пин внешней границы `trainer_pin.py` (`cde7877`). Второй ключ к воротам НЕ появился.

**FACT:** commit `cde7877` и `bb22927` в `git log`; раздел в узле `master` — 318819 симв., заголовок ровно 1 раз, тело дословно 1 раз, в хвосте (обратное чтение).

## СОСТАВ КРАСНЫХ ПО ТРЁМ ПРОГОНАМ И ОТРИЦАТЕЛЬНЫЙ ТЕСТ (в начало, как просили)

| прогон | число | состав красных покейсно | секунд |
|---|---|---|---|
| 1 | **12 из 12** | **нет** | 38.6 |
| 2 | **12 из 12** | **нет** | 0.1 |
| 3 | **12 из 12** | **нет** | 0.1 |

**Число одинаково: ДА. Состав красных покейсно одинаков: ДА.** Промахов кэша 0/0/0.

**Отрицательный тест — ПРОЙДЕН.** В ответ бота на кейсе 5 внесена заведомая ошибка (самооценка опасности — то, что голден A.3 запрещает списком `forbid`):

| прогон | число | состав красных |
|---|---|---|
| 1 | 11 из 12 | 5 → без «опасн» |
| 2 | 11 из 12 | 5 → без «опасн» |
| 3 | 11 из 12 | 5 → без «опасн» |

Ошибка жила в памяти замера (`Pin.mutate_head`) — ни снимок, ни код продукта, ни корпус не трогались, откатывать нечего.

## АРИФМЕТИКА ДО РАБОТЫ
Цена прогона из захода 11:47 — 452.5 с. Три прогона = 1357.5 с = **0.503** потолка 2700 с — само по себе ниже 0.7. Но программа задачи требует ≈9 прогонов (диагностика 2 + запись 1 + доказательство 3 + отрицательный 3) = 4072 с = **1.51 потолка** → гнать вживую нельзя, надо дробить. Пин это и решил: 1 живой проход (458.3 с) + шесть прогонов на пине (77 с суммарно).

## ПУНКТЫ

**1–2. От чего зависит исход.** Прошлый заход сравнивал ТОЛЬКО `pricing_note` — но в промпт входят ЧЕТЫРЕ живых входа (faq, парк, playbook, записка), и три не мерились вовсе. Померены все:
- `probe_env.py` (4 пробы, головы не звал): все 4 входа И сам системный промпт (26063 симв.) — **0 дрейфующих из 6**;
- `probe_head.py`: тот же промпт **побайтно**, поданный НАСТОЯЩЕЙ голове 3 раза → **3 разных ответа** (кейс 5: 403/205/401 симв.; кейс 10: 188/208/229). Источник — **ГОЛОВА**, не сеть, не порядок, не время.

**3. Кейс 12 — да, ходит в живую сеть**, прямо: `build_pricing_note → quote_for_model → pricing._default_get → urlopen(BRIDGE_URL…)`; счётчик границы намерил **1 GET моста** за прогон. Замена на замере — не подделка результата, а снимок живого ответа (правило репо «фикстура снимается с прода»).

**4. Способ.** `trainer_pin.py` пинит ПЯТЬ границ (`pricing/delivery._default_get`, `_bridge_read_doc`, `_cli_llm/_default_llm`, `load_playbook`). Не подделка — четыре проверяемых свойства: снимок снят **одним слепым живым проходом** до любого вердикта (право «переснять, пока не позеленеет» отнято кодом — `FileExistsError`); корпус/`expect`/`skip`/`TRAINER_MIN_*` модуль не читает вовсе; над замороженным текстом работает ВЕСЬ код продукта и все чеки; промах кэша считается и печатается. **Замок:** `trainer_run.py` про `trainer_pin` не знает — вердикт ворот на пине снять физически нельзя.

**7. Итоговое число: 12 из 12 на приколоченной границе. Второй ключ к воротам НЕ появился** — и это законный исход. Порог формально взят, что делает ответ опаснее: 12/12 — это КАК ЛЁГ ЕДИНСТВЕННЫЙ бросок головы, а не «стало лучше» (между сидениями `git diff c88d673 fc3d227` трогает только `pc_orchestrator.py`, его тест и два docs; корпус тот же по sha). Три причины: замороженная голова не ловит дрейф головы; живой продукт остался ровно так же недетерминирован; зелень снята одним броском, а не независимыми прогонами. Вердикт `client_trainer_green.json` не переписывался — `write_verdict` не импортировался.

**8.** Раздел лёг в узел `master` добавлением в хвост. Попутно — **важная находка о самом писателе**: попытка записи упала по `TimeoutError`, и обратное чтение отрапортовало «запись НЕ ЛЕГЛА, повтор безопасен» — **это оказалось неверно, запись легла**. От дубля спасла идемпотентность (`статус: noop`), а не расписка. Прежние разделы не тронуты (заголовков 117 → 123).

**Прочее:** `test_trainer_run` 28 из 28; храповик неразбора — вклад +5 слепых/+9 точек, доля 35.4% → 35.2% (не поднял). Временное — в `_scratch_flapfix_0822/`, ничего не удалялось. Запреты соблюдены: кейсы/ожидания/пороги не правились, текст ответа бота не изменён, чек не отключался, ворота не открывались, дети не обновлялись, клиентам не писалось, в секреты и конфиги не ходилось.

RESULT: флап трёх кейсов тренажёра устранён пином внешней границы (cde7877 trainer_pin.py) — три прогона подряд 12/12 с совпавшим покейсно составом красных, отрицательный тест краснеет во всех трёх; причины названы наблюдением (5 и 10 — голова, 12 — живая сеть); раздел записан в узел master, DONE в журнале; второй ключ к воротам НЕ появился.
