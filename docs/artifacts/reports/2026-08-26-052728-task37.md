# Полный отчёт задачи 37 — 2026-08-26 05:27 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **7446 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 37 |
| записано | 2026-08-26 05:27:28 |
| символов в отчёте | 7446 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Разведка выполнена целиком, только чтение. Автологон **вооружён** (остаток 07.08 закрыт), задача `TurboBabyDesktop` **не существует** — приложение поднимает штатный MSIX StartupTask; встроенный RDP невозможен (Home) и выключен, зато живы три канала удалённого экрана (AnyDesk, Chrome Remote Desktop, Tailscale). Полоса 328 от Claude Desktop **не зависит** — доказано путями. Артефакт записан и закоммичен.

**FACT:**
- `FACT: commit 677c730` — `docs/artifacts/2026-08-27-remote-hands-class-recon.md` в `git log`
- `FACT: commit d38e64c` — тело журнальной записи (авто-вынос по порогу 600)
- `FACT:` журнал — три записи легли, мост ответил `OK: записано в мозг, символов: 629694 / 630287 / 630516`
- `FACT: read-only` — реестр не правился, `schtasks` не звался ни разу, процессы не трогались, конфиги и секреты не открывались (только `Test-Path`/`mtime`), внешних сетевых запросов не делалось, ничего не удалялось

---

## ШАГ 0
Артефакт по адресу результата **не существовал** (`ls` → No such file or directory). Ни один вопрос не был отвечён заранее — разведка сделана с нуля.

## ШАГ 1 — оба файла на диске ЕСТЬ, выводы выложены дословно в артефакте
- `2026-08-07-pc-autologon-setup.md` — ключевое: проба пароля заблокирована внешним классификатором; пароль кладётся в LSA, не в реестр; `\tb_lock_on_logon` заведена без повышения; «ни AnyDesk, ни Tailscale до старта ОС не работают, IPMI/BMC нет — **удалённо этот шаг сделать нельзя**»; остатки 1/2/6.
- `2026-07-29-rc-token-staleness-prevention.md` — развилка **закрыта 31.07, вариант (а) применён** (`rc_auth_detect.py` в `rc_supervisor.py`, `RC_AUTH_WATCH` по умолчанию `1`); «умирают только НОВЫЕ спауны»; «голый греп по 401 — 78 % ложных»; цена: первая сессия после перелогина всё равно умрёт. **Перелогин детектор не делает.**

## ШАГ 2 — вооружение (вердикт · доказательство)
| | вердикт | доказательство |
|---|---|---|
| **а) автологон** | **ВООРУЖЁН** | `AutoAdminLogon=1`, `DefaultUserName=mxfill1`, `DefaultDomainName=MXFILLPC`, `DefaultPassword` **ОТСУТСТВУЕТ**, `AutoLogonCount` нет, `DevicePasswordLessBuildVersion=0`. Скрипт `autologon_arm.ps1` жив (14517 б, в git). Секрет LSA — **НЕИЗВЕСТНО** (`HKLM\SECURITY` закрыт всем кроме SYSTEM) |
| **б) блокировка экрана** | **ЕСТЬ, ВКЛЮЧЕНА, `Result=0`** | `\tb_lock_on_logon`, LogonTrigger `Delay=PT5S`, `LastRun 26.08 11:47:19` |
| **в) автозапуск Desktop** | **задачи НЕТ** | `TurboBabyDesktop` → пусто; задач со словом Desktop/Dispatch/Claude — ноль. **Но вопрос закрыт другим механизмом:** MSIX `ClaudeStartup State=2 (Enabled)`, `UserEnabledStartupOnce=1` (30.07 было `0`/`0`), приложение встало само 11:47:20, родитель `explorer.exe` |
| **г) подъём демона** | вотчдог | `\pc_orchestrator_watchdog`: TimeTrigger + **Rep=PT5M**, принципал `mxfill1`/**InteractiveToken**/Limited, **своей задержки нет**. Сегодня: загрузка 11:47:04 → демон 11:53:02 (**+358 с**, фаза такта) |

Мина: `TurboBabyExpectPC` **невидим неповышенной сессии** — `Get-ScheduledTask` о нём молчит. Контроль на паре имён снят: несуществующее имя → «файла нет», `TurboBabyExpectPC` → «Access is denied», и наблюдатель **жив** (`state.json` mtime 26.08 12:17:21).

## ШАГ 3 — удалённый экран
- **Встроенный RDP: принимать входящие НЕ МОЖЕТ** — `EditionID=Core` (Windows 11 Home, 25H2, 26200.9168): серверной части RDP в редакции нет. **И выключен:** `fDenyTSConnections=1`, `TermService/SessionEnv/UmRdpService` Stopped/Manual, **порт 3389 никто не слушает**.
- **Стороннее — три, все работают:** AnyDesk 9.0.12 (служба Running, LISTEN `:7070`, живой релей `15.235.230.200:443`), Chrome Remote Desktop Host 151.0.7922.13 (служба `chromoting` Running, `host.json` есть → хост спарен), Tailscale 1.102.2 (Running, UDP 41641). Все подняты `services.exe` в 11:47:10 — **до входа пользователя**.
- **НЕИЗВЕСТНО:** настроен ли пароль неконтролируемого доступа AnyDesk / PIN хоста CRD — лежит в конфигах, читать запрещено. Без него обход превращается в «нажми Принять у машины».

## ШАГ 4 — достижимость снаружи
Машина **за NAT**: `192.168.1.114/24` (Wi-Fi), шлюз `192.168.1.1`, Ethernet `Disconnected`. Свой адрес — серый по построению. **WAN белый/серый — НЕИЗВЕСТНО и снят не был сознательно:** это внешний запрос (класс `network`). **Но туннель УЖЕ ЕСТЬ:** Tailscale up, узел `100.95.10.116`, три живых соединения с контролем — белый адрес не нужен ни ему, ни AnyDesk, ни CRD.

## ШАГ 5 — список класса
14 точек в артефакте с колонками «что требует рук · обход сегодня · чем ограничен». Без обхода: **①** обесточенная машина (кнопка), **②** BIOS `Restore after AC Power Loss`/`ErP Ready` (из ОС непредставим — состояние НЕИЗВЕСТНО), **③** BSOD (`AutoReboot=0`), **⑥** перевооружение автологона (ввод пароля вслепую), **⑩** права админа, **⑫** сеть. Частичный обход: **⑦** отозванный токен (детектор рестартит, перелогин делает человек — файл `~/.claude/.credentials.json` общий на обе полосы), **④** пробуждение (авто-сон выключен: `STANDBYIDLE/HIBERNATEIDLE=0x0`; по сети не разбудить — `wake_armed` = Realtek GbE с **отключённым кабелем**, Wi-Fi не armed). **Закрыты: ⑤ вход (автологон), ⑬ Desktop (StartupTask), ⑭ TurboBabyDesktop (не нужна).**

## ШАГ 6 — прямой ответ
**НЕТ.** Полоса 328 от входа в десктопное приложение не зависит. По путям:
```
PID=15592  C:\Users\mxfill1\.local\bin\claude.EXE -p --model claude-opus-5 …
  ↑PPID    13496  D:\turbobaby-bot\venv\Scripts\python.exe  pc_orchestrator.py
  ↑PPID     1836  svchost.exe (Планировщик)  →  1156 services.exe  →  784 wininit.exe
```
`explorer.exe` (3624) в цепочке нет; Claude Desktop (13172, `WindowsApps\…\app\Claude.exe`) — нет; его дочерние сессии идут вообще третьим путём (`AppData\Roaming\Claude\claude-code\2.1.246\claude.exe`). Приложение встало 11:47:20, демон — 11:53:02, на 5 м 42 с позже, и позвал его `schtasks /Run` от вотчдога, зарегистрированного 04.07.2026.

## ШАГ 7 — чем этот вывод мог бы оказаться ложным
① **Общий файл кредов** — обе полосы читают один `.credentials.json` (mtime 26.08 09:48:52); если обновляет его именно приложение, зависимость существует **мимо дерева процессов**, и проверка путей её не видит. Самый вероятный способ ошибиться. ② **Подмена «приложение» на «интерактивный сеанс»** — задачи `InteractiveToken` живут в том же сеансе; корреляционное чтение лога даёт противоположный вывод, разводит их только причинная проба (закрыть приложение), которую делать запрещено. ③ **PID-reuse** — цепочка снята одним снимком; `1836` опознан по контексту, а не по имени службы. ④ **Одна загрузка** — на другой путь может быть иным. ⑤ **Спор об имени «полоса 328»** — если понимать её как канал Dispatch, ответ переворачивается в ДА: Dispatch живёт внутри приложения.

## ШАГ 8
Ничего не чинилось, не устанавливалось, не включалось, не вооружалось.

---

**Осталось владельцу** (не делал, класс — руки/админ): BIOS `Restore after AC Power Loss=Power On` + `ErP Ready=Disabled`; `AutoReboot=1` при BSOD; проверить пароль неконтролируемого доступа AnyDesk (окно уже открыто, PID 17580 → «Безопасность»); снять WAN-адрес.

RESULT: разведка класса «руки у машины» выполнена только чтением; артефакт `docs/artifacts/2026-08-27-remote-hands-class-recon.md` записан и закоммичен (677c730, тело журнала — d38e64c); автологон вооружён, `TurboBabyDesktop` не существует (Desktop поднимает MSIX StartupTask), удалённый экран доступен втройне (AnyDesk/CRD/Tailscale) при выключенном и неподдерживаемом RDP; полоса 328 от Claude Desktop не зависит; журнал ПК получил три строки.
