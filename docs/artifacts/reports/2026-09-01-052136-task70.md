# Полный отчёт задачи 70 — 2026-09-01 05:21 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4732 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 70 |
| записано | 2026-09-01 05:21:36 |
| символов в отчёте | 4732 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Диагноз снят: гейт self-update красный НЕ из-за фикса гарда — он красный с 30.08 (`2f015bb`), где в демона внесли `import proc_identity`, а `_ORCH_RUNTIME` не расширили. Три упавших теста — один разрыв с трёх сторон. На дереве `6cbdd15` те же три падения, новых ноль. Артефакт и журнал записаны, ничего не чинилось.

FACT: commit `9aec9a9` («Причина провала self-update: гейт был красным до фикса гарда») и `468b094` (тело журнальной записи) в `git log`; 4 живых прогона гейта — 983 теста, `FAILED (failures=3)` в каждом.

## 1. Живой формат гейта (снят с кода, `pc_orchestrator.py:4059` → `_gate_unittests`)

```
D:\turbobaby-bot\venv\Scripts\python.exe -m unittest test_pc_orchestrator test_pc_local_dec
cwd = D:\turbobaby-bot ; env = окружение демона МИНУС THINKER_MODEL, THINKER_FALLBACK ; timeout 600с
```

Живая строка провала — `pc_orchestrator.log:9722–9723`, `6cbdd15→3b911e5`, 01.09 11:14.

**983 теста, `FAILED (failures=3)`, ошибок 0. Дословно три имени:**

| тест | ассерт | вывод |
|---|---|---|
| `TestApprovalReachesExecutor.test_orch_runtime_covers_every_top_import_of_daemon` | `test_pc_orchestrator.py:841` | `Lists differ: ['proc_identity.py'] != []` |
| `TestSelfUpdateDepClosure.test_dirty_gate_watches_exactly_what_start_loads_unconditionally` | `:10706` | `Items in the first set but not the second: 'proc_identity.py'` |
| `TestSelfUpdateDepClosure.test_lazy_dependencies_are_a_named_remainder_not_a_silent_gap` | `:10713` | `Items in the first set but not the second: 'proc_identity.py'` |

Причина одна: `pc_orchestrator.py:63` несёт верхний `import proc_identity`, а ни `_ORCH_RUNTIME` (`:5737`), ни названный остаток `_ORCH_LAZY_UNCOVERED` (`:5807`) этого файла не содержат.

## 2. Тот же гейт на `6cbdd15` (откуда демон не переехал)

Дерево развёрнуто `git archive` в `tmp/task70_gate/prev6cbdd15`, гейт тем же интерпретатором и той же командой: `Ran 983 tests in 18.983s`, `FAILED (failures=3)`, **имена те же три**. Сравнение списков: **новых падений — 0, все три были и до фикса.**

Второе, независимое доказательство — по git: `git diff --stat 6cbdd15 8051768 -- pc_orchestrator.py test_pc_orchestrator.py test_pc_local_dec.py` пусто, blob id совпадают (`cfd6b95746cc` / `ae7121ee21ca` / `111ff1a3b90f`). Фикс гарда `3b911e5` трогал `pretool_guard.py`, `suggest.py`, `price_source.json` — ни один из них гейт не исполняет.

Мина, названная в артефакте: три упавших теста читают исходник по **абсолютному** `o.REPO` = `D:\turbobaby-bot`, а не по своему дереву — «прогнать гейт на чужом коммите» в общем случае даёт born-false вывод. Здесь это безвредно ровно потому, что файл побайтно один и тот же.

## 3. Три прогона подряд

| прогон | тестов | итог | время |
|---|---|---|---|
| live #1 | 983 | failures=3 | 18.3с |
| live #2 | 983 | failures=3 | 17.6с |
| live #3 | 983 | failures=3 | 17.7с |
| prev `6cbdd15` | 983 | failures=3 | 19.0с |

Числа и отсортированные списки имён совпали 3/3 (сверка `diff`). Признака хождения в живую сеть нет.

**Отдельно, как требует пункт 3:** есть ВТОРАЯ полоса провалов, эти прогоны её не воспроизвели — за 22–31.08 гейт падал **10 раз по таймауту 600с**, и во всех 10 в диффе присутствовал `suggest.py`. Сегодня набор тоже содержал `suggest.py`, а прогон уложился в 18–20с ⇒ корреляция класс не объясняет, корень зависаний **неизвестен**. Это остаток, а не диагноз.

## 4. Когда родилось красное

`git log -S"proc_identity" -- pc_orchestrator.py` → ровно один коммит `2f015bb` (30.08). В родителе `5aaef57` импорта нет (0 совпадений), в `2f015bb` есть (1). `_ORCH_RUNTIME` не правился с `e3d32ba` (07.08) — то есть до появления файла. `2f015bb` — предок `6cbdd15`: работающий демон несёт дефект в себе. Последний успешный self-update — 22.08 14:01 (`0be1ea6→bcc3f63`); текущий демон приехал на `6cbdd15` **рестартом** через watchdog/schtasks 31.08 19:43, а не эстафетой.

## Вывод одной строкой (пункт 4)

**Фикс гарда тесты не ломал: гейт был красным до него, с 30.08, и self-update заблокирован независимо от коммита `3b911e5`.**

Ничего не чинилось: `_ORCH_RUNTIME` не расширен, тесты не тронуты, пороги не менялись, демон не перезапускался, клиентский контур не тронут. Временное — только в `tmp/task70_gate/`, ничего не удалялось. Артефакт: `docs/artifacts/2026-09-01-причина-провала-self-update.md`. Журнал: строки `ARTIFACT` и `DONE` записаны (тело вынесено в `docs/artifacts/journal/2026-09-01-052033-done.md`, закоммичено `468b094`).

RESULT: причина провала гейта self-update установлена и записана артефактом (коммит 9aec9a9) — три падения (983 теста, failures=3) вызваны отсутствием proc_identity.py в _ORCH_RUNTIME с коммита 2f015bb от 30.08; на предыдущем коммите 6cbdd15 те же три падения, новых от фикса гарда 3b911e5 ноль; ничего не чинилось.
