# Полный отчёт задачи 2 — 2026-08-21 09:33 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **6146 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 2 |
| записано | 2026-08-21 09:33:01 |
| символов в отчёте | 6146 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Разбор шестнадцати красных перенесён в узел карты по ключу реестра `master`: вставка 11652 симв., тело 155201→166854, прирост 11653 сошёлся с расчётом. Семь замков сняты свежим процессом дважды (повтор через 60 с). Ничего не пересчитывал, кода не правил, строка DONE в журнале.

**FACT:**
- узел карты (ключ реестра `master`, id не хардкодился): **155201 → 166854 символов, прирост 11653**;
- тело прочитано **свежим процессом дважды**, `sha256/16 = d82232628af371ac` оба раза; sha прежнего тела `bcc9c4a281078fd6` восстанавливается снятием вставки символ в символ;
- признак «разбор шестнадцати красных 21.08» — **1 вхождение** (было 0); новый заголовок 1 вхождение; 14 прежних заголовков целы, сдвигов 0; U+FFFD 0;
- строка DONE легла в мозг: `OK: записано в мозг, символов: 522305`;
- бэкап прежнего тела: `tmp\brain_backup_reds16_2108_20260821-162633.txt`.

---

**Первое действие (коммит дерева и время):**
```
803911d 2026-08-21 14:26:37 +0700 ARTIFACT разведка задачи 81: работа сделана, потерян только отчёт 21.08 -> docs/artifacts/2026-08-21-task81-forensics.md
2026-08-21 16:15:35
```

**Основание — артефакт на диске есть,** «НЕИЗВЕСТНО» не наступило:
```
-rw-r--r-- 1 mxfill1 197121 20782 Aug 21 14:17 docs/artifacts/2026-08-21-remaining-reds.md
20782 docs/artifacts/2026-08-21-remaining-reds.md
```

**Второе действие — признак сделанности (узел открыт ПО ИМЕНИ через живой реестр):**
```
master → name:master  [ключ реестра]

ДЛИНА: 155201 символов, строк 2063
ПРИЗНАК СДЕЛАННОСТИ «разбор шестнадцати красных 21.08»: вхождений 0
```
Ноль вхождений — работа не сделана, продолжил.

**Предполётная сверка (без обращения к мосту):**
```
ДЛИНА ТЕЛА ДО         : 155201
ДЛИНА ВСТАВКИ (body)  : 11652
РАЗДЕЛИТЕЛЬ           : '\n' (1 символ)
ДЛИНА ЗАДУМАННОГО     : 166854
ПРИРОСТ ЗАДУМАННЫЙ    : 11653 = 11652 (вставка) + 1 (разделитель)
ПРИЗНАК «разбор шестнадцати красных 21.08»: в теле ДО 0, во вставке 1, в задуманном 1
ЗАГОЛОВОЧНЫХ СТРОК ВО ВСТАВКЕ: 1
   ## Итог задачи 81: разбор шестнадцати красных 21.08 — шесть закрыто, десять оставлено осознанно
   вхождений этой строки в теле ДО: 0
   вхождений в задуманном: 1
   процитировано существующих заголовков: 0
   заголовков со сдвигом числа вхождений: 0
```

**Запись:**
```
ОТПРАВЛЯЮ: 11653 символов (после rstrip хвостовых \n: 11652)
СТАТУС      : ok
ДОК         : name:master
БЭКАП       : D:\turbobaby-bot\tmp\brain_backup_reds16_2108_20260821-162633.txt
СИМВОЛОВ ДО : 155201
СИМВОЛОВ ПОСЛЕ: 166854
ПРИРОСТ     : 11653
РАСПИСКА    : ok (доказательством НЕ считается)
```

**Замки, замер #1 (свежий процесс):**
```
sha256/16 тела сейчас : d82232628af371ac
[1] ДЛИНА  до=155201  сейчас=166854  прирост=11653
    длина вставки=11652 + разделитель=1 = 11653   ПРИРОСТ РАВЕН ДЛИНЕ ВСТАВКИ: True
[2] ТЕЛО == ЗАДУМАННОМУ СИМВОЛ В СИМВОЛ: True   (sha задуманного d82232628af371ac)
[3] ОБРАТИМОСТЬ: хвост == вставке: True
    снятие вставки даёт прежнее тело символ в символ: True
    sha прежнего bcc9c4a281078fd6 / sha снятого bcc9c4a281078fd6
[4] НОВЫЙ ЗАГОЛОВОК: вхождений цельной строкой = 1 (ожидал 1)
[5] со сдвигом числа вхождений: 0   ВСЕ ЦЕЛЫ: True
    заголовочных строк всего: было 14, стало 15
[6] ПРИЗНАК «разбор шестнадцати красных 21.08»: вхождений 1 (ожидал 1)
    U+FFFD в теле: 0
ВСЕ ЗАМКИ ЗАМКА #1: СОШЛИСЬ
```

**Замок неизменности, замер #2 (через 60 с, синхронно):**
```
ЖДУ 60 с перед повторным чтением (синхронно)…
sha256/16 тела сейчас : d82232628af371ac
[1] прирост=11653 … ПРИРОСТ РАВЕН ДЛИНЕ ВСТАВКИ: True
[2] ТЕЛО == ЗАДУМАННОМУ СИМВОЛ В СИМВОЛ: True
[3] снятие вставки даёт прежнее тело символ в символ: True
[4] НОВЫЙ ЗАГОЛОВОК: вхождений цельной строкой = 1
[5] со сдвигом числа вхождений: 0   ВСЕ ЦЕЛЫ: True
[6] ПРИЗНАК … вхождений 1;  U+FFFD в теле: 0
[7] НЕИЗМЕННОСТЬ: тело замера #1 == телу замера #2 символ в символ: True
    sha #1 d82232628af371ac / sha #2 d82232628af371ac
ВСЕ ЗАМКИ ЗАМКА #2: СОШЛИСЬ
```

**Журнал:**
```
OK: записано в мозг, символов: 522305
```

---

**Сводка**

1. Перенесено ровно то, что велено, и ничего не пересчитано: шесть починенных двумя корнями (кепка низкого сезона — починен сменой модели на NINJA 400 с ручным счётом 1145 × 1.0 × 0.53 = 607 ฿/день → 18210 против кепки 11900; упразднённый канал столбца J — пять тестов с ручными счётами 298/2086 и 307/1535, включая перевёрнутый голден и признание, что по PCX прав код); десять оставленных четырьмя группами с причиной у каждой.
2. Где неправ **КОД** — помечено отдельной строкой: только группа `avail=None`, прошитый литералом в `suggest.py:7353`, три теста, и она единственная требует своего захода по коду. У пяти тестов неправ тест (не влезло в бюджет), у двух не неправ никто — неполно правило, решение владельца.
3. XMAX −11.5 % перенесён дословно: **путаются поколения, а не цена** — разброс шире в 2.4 раза, `p75 = 701` садится на прайсовые 704 с точностью 0.4 %, смесь 593·x + 704·(1−x) = 623 даёт x = 0.73; врёт ключ, не формула; `owner_decision` пуст.
4. Замер после правок заход **успел**: полный прогон пяти модулей — `Ran 858 tests`, `FAILED (failures=10)`, было 16 → стало 10, при оговорке о достоверности только при отвечающем мосте. Отрицательный тест на укус **устоял**: plus1 покрасил 5 из 6, crash — 6 из 6, зеркалом не стал ни один.
5. По дороге мост дважды отказал транспортно (медленное чтение, затем мёртвый ключ расписки на втором плече). Оба отказа пришлись на **первое чтение внутри `apply`, до `write_doc`** — дока они не касались, поэтому повтор был не слепым. Предел ожидания поднимал **в памяти своего скрипта**; файл `brain_writer.py` не тронут.

Кода не правил, тестов и голденов не трогал, источник цены и сторож не трогал, ботов не перезапускал, гейт не гонял, ничего не удалял, `.env` не читал. Черновики захода — в `_scratch_reds2brain_0821/` (вне git).

RESULT: разбор шестнадцати красных перенесён в узел карты (ключ реестра master) одной вставкой 11652 симв., тело 155201→166854 (прирост 11653 = расчёту), семь замков сошлись свежим процессом дважды с повтором через 60 с (sha d82232628af371ac), признак «разбор шестнадцати красных 21.08» = 1 вхождение, 14 прежних заголовков целы; строка DONE в журнале, кода не правилось.
