# Полный отчёт задачи 24 — 2026-08-19 04:49 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **8361 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 24 |
| записано | 2026-08-19 04:49:56 |
| символов в отчёте | 8361 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Ключ дедупа ворот починен одной строкой: коммит в подписи сверяется `client_contour.short()` с сырым фолбэком. Реплей шести живых пар: было 0/6 схлопнутых, стало 6/6. Оба отрицательных теста устояли, красная проба валит ровно (а). Гейт 3773 теста против прежних 3770, красных 2 — те же два храповика. Коммиты 70635de (правка) и 4e3465c (артефакт) запушены.

**FACT:**
- `FACT: commit 70635de в git log` — правка ключа (`pc_orchestrator.py | 14 +++-`, `test_pc_orchestrator.py | 47 +++`), запушен `a2ecfcc..70635de main -> main`
- `FACT: commit 4e3465c в git log` — артефакт `docs/artifacts/2026-08-19-gate-dedup-fix.md` (216 строк), запушен `70635de..4e3465c main -> main`
- `FACT: гейт после правки — Ran 3773 tests, FAILED (failures=2, skipped=10)`, красные те же: `test_no_growth_against_baseline`, `test_totals_do_not_grow` (`test_nonparse_ratchet.TestRatchet`)

---

## Признак сделанности

`docs/artifacts/2026-08-19-gate-dedup-fix.md` **не существовал** → работа не была сделана. Сделана в этой сессии.

## Что изменено — ровно ключ

`pc_orchestrator.py` → `_client_block`:

```python
-    key = (str(commit), tuple(held))
+    key = (client_contour.short(commit) or str(commit), tuple(held))
```

плюс комментарий над строкой. Больше в воротах — ничего: ни логики, ни текстов карточки, ни числа входов, ни `held`. Нормализатор не заводился, он уже лежал (`client_contour.py:319`) и его докстринг написан ровно про этот класс. Фолбэк `or str(commit)` обязателен: на не-хеше `short()` отдаёт пустую строку, и она склеила бы РАЗНЫЕ метки в один ключ.

## Забор Честертона (одной строкой, как просили)

Девять символов — это **показ, а не ключ, и он старше ворот**: `short = head[:9]` внесён коммитом `6d8a3b1` от **11.07.2026** («авто-применение кода детей на ЛЮБОЙ новый коммит») за 19 суток до заведения ворот (30.07), обслуживал только `log`/`_cowork`/`_notify`, и девять — потому что тик берёт полный 40-символьный `rev-parse HEAD` и режет сам, а self-update просит короткий хеш у git. Нормализация ничего не сломает: единственное сравнение коммитов во всём файле — `old_commit == new_commit` внутри `_diff_names` (оба конца из одного источника), всё прочее либо само зовёт `short()` (`release_reason`, `trainer_status`), либо печатает — карточка и `git revert` сохраняют прежние девять символов.

## Дословный вывод команд

**Забор — единственное сравнение коммитов в файле:**
```
$ grep -n "commit ==\|== commit\|commit !=\|!= commit\|short ==\|== short\|short !=" pc_orchestrator.py
5920:    if not old_commit or not new_commit or old_commit == new_commit:
```

**Происхождение девятки:**
```
$ git log -1 --format='%h %ad %s' --date=short -L 6224,6224:pc_orchestrator.py
6d8a3b1 2026-07-11 ПК-оркестратор: авто-применение кода детей на ЛЮБОЙ новый коммит (класс-фикс c6d8a30)
+    short = head[:9]
```

**Шесть девятисимвольных строк — настоящие коммиты:**
```
$ for h in 3dd040b4c 3124e25a7 921c9c6fa 8b2862632 8124a9492 538bbbd18; do git rev-parse --verify --quiet "$h^{commit}"; done
3dd040b4c6a3e88c9ae3f1d1c8feae7e831ce098
3124e25a7915897c04d6fe8ad14dd3cedaa73916
921c9c6faaea359223a2227c9f02bfa2b99043a3
8b2862632f8181d592e698ba67ab3e485731e5ed
8124a94927a33fb5ce226558cddec1b19254366b
538bbbd18a7b723661b920cdcbb160962712a2c2
```

**Реплей шести пар** (`_scratch_gatededup_0819/replay_and_red.py`, только чтение: инжекты `notifier/cowork/state/reason_fn/trainer_fn`, логи в temp через `TURBOBABY_TEST_LOGS=1` до импорта; прежнее поведение воспроизведено точно подменой `short → ''`):
```
СТАРЫЙ ключ (str(commit), сырой):
  31.07 03:44  3dd040b4c / 3dd040b   log_setup.py                 -> карточек 2
  31.07 21:13  3124e25a7 / 3124e25   suggest.py                   -> карточек 2
  05.08 01:01  921c9c6fa / 921c9c6   log_setup.py                 -> карточек 2
  05.08 11:53  8b2862632 / 8b28626   userbot_listen.py            -> карточек 2
  17.08 21:08  8124a9492 / 8124a94   price_source.py,suggest.py   -> карточек 2
  18.08 20:03  538bbbd18 / 538bbbd   suggest.py                   -> карточек 2
  схлопнулось пар: 0 из 6

НОВЫЙ ключ (client_contour.short(commit) or str(commit)):
  31.07 03:44  3dd040b4c / 3dd040b   log_setup.py                 -> карточек 1  СХЛОПНУЛОСЬ
  31.07 21:13  3124e25a7 / 3124e25   suggest.py                   -> карточек 1  СХЛОПНУЛОСЬ
  05.08 01:01  921c9c6fa / 921c9c6   log_setup.py                 -> карточек 1  СХЛОПНУЛОСЬ
  05.08 11:53  8b2862632 / 8b28626   userbot_listen.py            -> карточек 1  СХЛОПНУЛОСЬ
  17.08 21:08  8124a9492 / 8124a94   price_source.py,suggest.py   -> карточек 1  СХЛОПНУЛОСЬ
  18.08 20:03  538bbbd18 / 538bbbd   suggest.py                   -> карточек 1  СХЛОПНУЛОСЬ
  схлопнулось пар: 6 из 6
```
**6 из 6. Не схлопнувшихся нет — называть нечего.**

**Красная проба (голдены на прежнем ключе):**
```
test_dedup_odin_kommit_dvumya_dorogami_odna_kartochka ... FAIL
test_dedup_raznye_kommity_ne_skleivayutsya ... ok
test_dedup_ne_hesh_ostaetsya_soboi ... ok

AssertionError: 2 != 1 : ['⛔ … Коммит: 538bbbd18 … Где сработало: реконсиляция детей на новый коммит …',
                          '⛔ … Коммит: 538bbbd  … Где сработало: реконсиляция детей после self-update …']

Ran 3 tests in 0.061s
FAILED (failures=1)
на СТАРОМ ключе упали: test_dedup_odin_kommit_dvumya_dorogami_odna_kartochka
ИТОГ: старый ключ схлопнул 0/6, новый — 6/6
```
Дамп сам по себе улика: две карточки владельцу отличались **двумя символами хеша** и строкой «Где сработало».

**Класс ворот после правки:**
```
$ venv/Scripts/python.exe -m unittest test_pc_orchestrator.TestClientContourGate -v
test_dedup_ne_hesh_ostaetsya_soboi ... ok
test_dedup_odin_kommit_dvumya_dorogami_odna_kartochka ... ok
test_dedup_raznye_kommity_ne_skleivayutsya ... ok
…
Ran 17 tests in 0.578s

OK
```

**Замок — полный гейт до и после** (`venv/Scripts/python.exe -m unittest discover -s . -p "test_*.py"`):
```
ДО (a2ecfcc):                              ПОСЛЕ (70635de):
Ran 3770 tests in 230.655s                 Ran 3773 tests in 226.384s
FAILED (failures=2, skipped=10)            FAILED (failures=2, skipped=10)
FAIL: test_no_growth_against_baseline      FAIL: test_no_growth_against_baseline
FAIL: test_totals_do_not_grow              FAIL: test_totals_do_not_grow
      (test_nonparse_ratchet.TestRatchet)        (test_nonparse_ratchet.TestRatchet)
```

**Коммиты:**
```
$ git push origin main
   a2ecfcc..70635de  main -> main
   70635de..4e3465c  main -> main
$ git log --oneline -2
4e3465c ARTIFACT ключ дедупа карточки ворот -> docs/artifacts/2026-08-19-gate-dedup-fix.md: …
70635de ворота контура: ключ дедупа карточки сверяет коммит НОРМАЛИЗОВАННЫМ, а не сырой строкой
```

## Сводка

1. Изменена **одна строка** — ключ дедупа в `_client_block` (`client_contour.short(commit) or str(commit)`); логика ворот, тексты карточки, число входов и `held` не тронуты. Голдены положены в `test_pc_orchestrator.TestClientContourGate` намеренно: правка `pc_orchestrator.py` по карте затронутых гоняет только этот модуль.
2. Забор Честертона: девять символов — показ, введённый `6d8a3b1` 11.07 за 19 суток до ворот; на длину не опирается ни одно сравнение (единственное — `_diff_names`, чужое), карточка и `git revert` сохранили девятку — это проверяет отдельный `assertIn`.
3. Отрицательные тесты устояли оба: (а) один коммит двумя дорогами → одна карточка, (б) разные коммиты → две карточки; плюс третий на фолбэк не-хеша. Красная проба валит **ровно (а)** — голден кусается, (б)/(в) зелены и до, и после как контроль.
4. Реплей шести живых пар: **0/6 → 6/6**, прогноз разведки подтверждён живым кодом.
5. Гейт **3773** против прежних **3770** (число не упало, +3 — новые голдены), красных **2** и это **те же два** храповика неразбора.

**Оговорка, чтобы не обмануть:** правка гасит дубль **внутри одного процесса** — память ворот остаётся обычным dict, и рестарт демона её по-прежнему обнуляет (restart-proof дедуп — отдельный класс, прецедент `_notify_chain_card`; закрывать не просили). Строку в `cowork_log` не писал: по заданию доставка — отдельным заходом.

RESULT: ключ дедупа карточки ворот нормализован (`client_contour.short` с сырым фолбэком) — коммит 70635de запушен; реплей шести живых пар 0/6 → 6/6, оба отрицательных теста устояли, красная проба валит ровно (а); гейт 3773 теста против 3770 при тех же двух красных храповиках; артефакт docs/artifacts/2026-08-19-gate-dedup-fix.md — коммит 4e3465c.
