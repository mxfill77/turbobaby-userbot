# Полный отчёт задачи 46 — 2026-08-15 19:14 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5809 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 46 |
| записано | 2026-08-15 19:14:21 |
| символов в отчёте | 5809 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Разведка закончена: 87 мест в 6 видах — 62 судят исход по отчёту, 25 по чтению назад, и ни одно из 25 не читает продукт задачи или шага цепи. Исход «неизвестно» есть у одного вида из шести. Источник истины у шага не записан нигде: очередь несёт 9 колонок, под адрес результата — ни одной. Артефакт создан и закоммичен, строка-индекс в журнале.

**FACT:**
- `FACT: commit a545ad2 в git log` — артефакт `docs/artifacts/2026-08-15-orchestrator-trusts-report-pc.md` (проверено `git log --oneline -1`).
- `FACT: строка в мозг записана` — расписка писателя `OK: записано в мозг, символов: 373892`; длина строки 501 символ, ниже порога авто-выноса 600, путь артефакта стоит сразу после штампа.
- `FACT: read-only по предмету` — код не правился, гейт не гонялся, процессы не трогались, мост на запись не звался, в рабочие таблицы не писалось. Единственная запись, кроме артефакта и журнала, — `_scratch_trustreport_pc_0815/census.txt` (каталог оставлен).
- `FACT: 0.050 с` — замеренная цена `git cat-file -e <хеш>^{commit}`; три хеша, названные в блоках FACT живых отчётов (`5d4657d`, `311d63c`, `4eba968`), в `git log` есть все три.

## Ответы по пунктам захода

**1. Перепись — 87 мест в 6 видах, 62 по отчёту / 25 по чтению назад.**

| вид | мест | по отчёту | по чтению назад |
|---|---:|---:|---:|
| задача headless (`_run_task_impl`) | 13 | 10 | 3 |
| шаг локальной цепи (`_loc_*`) | 24 | 21 | 3 |
| рычаг и смоук (демон делает сам) | 9 | 7 | 2 |
| применение кода (гейт/рестарт/реконсиляция) | 31 | 17 | 14 |
| запись наружу (журнал/мозг/слепок/карточки) | 7 | 7 | 0 |
| мутация очереди (claim/complete) | 3 | 0 | 3 |

Главное число — не 62, а **ноль**: из 25 мест «по чтению назад» ни одно не читает продукт задачи или шага цепи. Виды 1–2 — это 37 мест, и там ноль. Читается назад транспорт, процесс, деплой, своя карточка — своё состояние, а не сделанное. Продукт исполнителя полоса всё же читает **дважды, и оба раза после приговора**: `_changed_files_since`→`maybe_update_bots` (только при `status == "done"`, решает что применить) и `_work_evidence`→`fail_result` (только на провале, описывает потерю).

**2. Источник истины — не записан нигде.** Очередь: `id, created, from, task_text, status, result, approved_by, updated, lane`. Колонки под «где обязан появиться результат» нет. Схема заморожена: даже родству цепи в колонке отказали (`dec_port_spec.md:115` — «ТОЛЬКО по паттернам в `task_text`, решение плана C»). В тексте шага адреса тоже нет: `PLANNER_PREAMBLE` требует самодостаточности, но не требует назвать продукт.

**3. Успех заявлен, результата нет** — по видам: задача **зелёный**; шаг цепи **зелёный, и цепь идёт дальше** (это и есть отказ экзамена, воспроизведённый в дирижёре); рычаг рестарта **красный** (пустой PID ловится), смоук `skipped` **зелёный**; применение кода раздвоено (гейт без затронутых тестов → `True` без прогона); запись наружу — **исхода не существует**, `DEVNULL` на всех потоках; мутация очереди — **красный или неизвестный**. Исход «неизвестно» есть у **1 вида из 6** (3 места из 3); у остальных 84 — ни в одной ветке. Культура третьего исхода на полосе богатая (`_executor_verdict`, `daemon_product_verdict`, `expectations_pc`, `parse_outcome`), но вся она судит живость и читаемость, а не исход шага.

**4. Образцы есть, выдумывать нечего:** `Bridge._in_status` + claim/complete (:703-748), `brain_writer.apply` (пять сверок обратного чтения), `cowork_log_append.readback_landed`, `_restart_via_pc_agent` (PID + рост лога), `_verify_raised`, `daemon_product_verdict`, `_executor_verdict`. Честный предел: все семь читают назад транспорт, процесс или оборот — **форму дают, предмета не дают**.

**5. FACT есть, последствий не имеет — с числами.** Требование в `PREAMBLE:406-409` с 11.08 (`50ae73a`). Единственный разбор — `result_spill.has_fact`, достижим только через `cap_result`, который на отчёте ≤4500 символов выходит раньше проверки. Замер по логу демона 11.08→16.08: закрыто **26 задач**, отчётов свыше потолка — **3** (11.5%); в 23 случаях (88.5%) блок доехал и на него не посмотрел никто. Даже в тех 3 последствие — приписка `⚠️ unverified`, статус остаётся `done`. Содержание не проверяется никогда.

**Сравнение с VPS — совпадает частично.** Совпадает: 9 колонок (лист один на обе полосы) и мёртвый FACT — на VPS это в коде даже явнее (`tmp/srv/orchestrator_daemon.py:3913` «оставлены для devbot и юнит-теста»). Расходится по четырём точкам: у ПК свой слой «неизвестно» вне демона (`expectations_pc`); у ПК есть улика авторства `_RUNNING_TID`, которой у многополосного соседа быть не может; цена ложного `done` на ПК выше (у исполнителя нет фолбэка); запись наружу у ПК вся fire-and-forget ради длины оборота, который меряют свои же О2/О4 — **значит форма, встающая на обе полосы, не может требовать от зовущего дождаться расписки**.

**Заборы Честертона проверены:** из шести видов доверие отчёту обосновано полностью в двух (нечего читать; проверка перенесена к писателю) и частично в одном (обоснован думатель адаптации без IO, но не дирижёр). В видах 3, 4 и в ветке `skipped → done` обоснования не нашлось.

Предсмертный взгляд записан в артефакте §8. Границы захода и непроверенное — §9: клиентский контур в перепись не входит, мост по сети не звался, клон VPS отстаёт на 15 суток.

RESULT: разведка «оркестратор ПК судит шаг по отчёту» выполнена read-only — артефакт docs/artifacts/2026-08-15-orchestrator-trusts-report-pc.md (коммит a545ad2), строка-индекс в мозге (501 символ, путь в начале); 87 мест в 6 видах — 62 по отчёту, 25 по чтению назад, продукт задачи и шага цепи (37 мест) назад не читает ни одно; «неизвестно» есть у 1 вида из 6; колонок очереди 9, под адрес результата — ни одной; FACT просят с 11.08, но проверка запускалась в 3 отчётах из 26 (11.5%) и только припиской; с VPS совпадают колонки и мёртвый FACT, расходятся 4 точки.
