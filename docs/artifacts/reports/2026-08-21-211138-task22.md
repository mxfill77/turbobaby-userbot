# Полный отчёт задачи 22 — 2026-08-21 21:11 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5590 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 22 |
| записано | 2026-08-21 21:11:38 |
| символов в отчёте | 5590 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Опись тренажёра снята чтением; раздел лёг в узел карты master. Ключевой ответ: тренажёров ДВА — `trainer_run.py` гоняет код рабочего дерева в своём процессе, `trainer.py` живёт внутри живых детей. Две премисы из трёх оказались неверны: прогон 31.07 дал 10 ЗЕЛЁНЫХ из 12 (не красных), а 50000 — порог комфорта, а не ёмкость.

**FACT:**
- Запись в мозг: `записано в name:master: 230407 → 237788 символов`, обратное чтение — «текст дословно на месте (вхождение 1 раз)», бэкап `tmp\brain_backup_trainer_recon_0822_20260822-040639.txt`. Адрес взят из живого реестра моста (`--list-brain` → ключ `master`), id не хардкодился.
- Журнал: `OK: записано в мозг, символов: 539953 | досланы отложенные: 1`.
- Код не менялся: HEAD как был — `0be1ea6`, `git diff --name-only HEAD` пуст (правок 0). Тренажёр не запускался, гейт не гонялся.

---

## 1. ЧЕМ ГОНЯЕТ — ключевой вопрос, ответ первым

**Тренажёров ДВА, и ответ у них разный.**

**1) `trainer_run.py` — код РАБОЧЕГО ДЕРЕВА, свой процесс, живых детей не трогает.** Доказано кодом запуска: `REPO = os.path.dirname(os.path.abspath(__file__))` (`trainer_run.py:70`) + `import client_contour / suggest / trainer` (`:89-91`) — Python грузит модули с диска, потому вердикт и требует чистое дерево и несдвинутый HEAD (`:148-155`). Telethon не импортируется вовсе, `userbot.lock` не берётся, боевая `moderation_ipc.db` уводится в temp до первого обращения (`isolate_ipc`, `:111-127`), логи в temp (`:87`). Наружу не уходит ничего.
**Оговорка, которую нельзя терять:** «безголовый» здесь про Telethon, а не про модель — голова ЖИВАЯ (claude CLI) и мост ЖИВОЙ (read-only GET). Пайплайн `generate()` (`:212-225`) — дословная копия `userbot_listen._trainer_generate` (`:252-263`), расхождение краснит AST-голден.

**2) `trainer.py` — наоборот, ВНУТРИ живых клиентских детей.** Импортят оба (`userbot_listen.py:44`, `moderation_bot.py:36`), и дирижёр это знает: правка `trainer.py` рестартит userbot и moderbot (`pc_orchestrator.py:5505`). То есть он гоняет код, уже загруженный процессом, — версию на момент старта детей, а не дерева.

## 2. Кейсы

12 в `trainer_cases.json`. Поле `source` у каждого называет происхождение фразы. **Ожидание есть у 11 из 12** — у кейса 11 («а XMAX 300 сейчас есть в наличии?») `expect` пуст и ни `require_any`, ни `forbid`, ни `expect_facts`. Автор один — Filipp, коммит `6478a39` (30.07), единственная правка `55aad22` (31.07). Порог ворот `TRAINER_MIN_CASES = 12` / `TRAINER_MIN_RUNS = 2` (`client_contour.py:317-318`).

**Последний прогон 31.07 19:40:01: 10 из 12 ЗЕЛЁНЫХ, чеков 221/224.** Красные дословно: `7/1 нет утверждений о наличии`, `7/2 нет утверждений о наличии`, `10/1 обязательное упоминание`. Вердикт RED — нужно 12 из 12. **Зелёных вердиктов за всю историю ноль**, раннер не запускался 22 суток.

## 3–6. Уроки · книга · режимы · петля

| | факт |
|---|---|
| Уроки | `lesson_store.tsv`, 8 колонок, **файла нет вовсе** (0 записей). К боту не подключён — заперто голденом `test_no_bot_module_imports_the_store`. Источника/версии/режима в записи **нет ни одного**. Откат набора есть по разрезам `урок/день/автор`, по источнику — нет колонки. `50000` = `COMFORT_LESSONS`, **порог комфорта, не потолок** |
| Книга правил | `PLAYBOOK_MAX_RULES = 100`, FIFO-вытеснение (`suggest.py:766-770`), возврат `added` (`:782`) на той же ветке — **честного отказа нет**. Замер 19.08: 120 дозаписей → 100 в книге, 20 потеряно без следа. Файл вне git, внутри вложенного репо |
| Режимы | различения обучение/бой **в коде нет**; `SUGGEST_TEST_MODE` про отправку клиенту. Изоляция есть, но по КАНАЛУ (chat_id, шапка `[тренажёр \| ТЕСТ-N \| правил: K]`, префикс `БРОНЬ [ТЕСТ]`), не по данным. Подтверждение обучение симулировать **не может** — модерации в тренажёре нет вовсе |
| Петля | **есть в коде, но короткая**: `apply_lesson` (`trainer.py:645`) → `append_playbook_rule` + пометка источника; «со следующего ответа» правдиво механически (`load_playbook()` на каждой генерации, кэша нет). **Замыкания нет** — перепрогона кейса после урока в коде 0 совпадений; вердикт раннера уроком не становится |

## 7. Чего нет — четыре условия

Сперва честно: **записанного списка «четыре условия» ни в репо, ни в мозге НЕТ** (искал по четырём формулировкам — ноль). Закодирован как порог этапа только `TRAINER_MIN_CASES/RUNS`. Условия названы по предмету задачи, не процитированы.

- **У1** хранилище с провенансом и откатом набора — **ОТСУТСТВУЕТ**
- **У2** книга правил без молчаливой потери — **ОТСУТСТВУЕТ**
- **У3** разрез обучение/бой в самой записи — **ОТСУТСТВУЕТ**
- **У4** петля «вердикт → правило → проверка» — **НАЙДЕНО ЧАСТИЧНО** (две трети: запись и применение доказаны кодом, замыкания нет)

Пятое, не спрошенное: петля не поворачивалась с июля — следов тренажёра в `userbot.log` за 09.06–18.08 (66 962 строки) ноль.

## Что стоило времени

Запись в master дважды упиралась в 30-секундный потолок `brain_writer` (док 230k). Отказ каждый раз честный, док не тронут. Легло со 2-й попытки цикла — расписка потеряна, но обратное чтение подтвердило текст дословно. Журнальная строка тоже сначала ушла в спул и досланa следующим вызовом. Оба обхода записал в память.

RESULT: опись тренажёра снята чтением (12 кейсов, ожидание у 11 из 12, последний прогон 31.07 = 10 зелёных из 12, зелёных вердиктов за историю 0; хранилище уроков пусто и не подключено, книга правил теряет молча при капе 100, режима обучение/бой нет, петля не замкнута, записанного списка четырёх условий не существует); итог лёг новым разделом в узел карты master (230407→237788 симв., обратное чтение OK) + строка в cowork_log; код не менялся, HEAD 0be1ea6.
