# Полный отчёт задачи 72 — 2026-08-20 20:33 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **5094 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 72 |
| записано | 2026-08-20 20:33:43 |
| символов в отчёте | 5094 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Разведка read-only закончена: гипотеза «всё гасит сторож» верна для 11 красных из 60, для 49 неверна — там правило считает другое число (голден 472 против 364). Красных на обоих источниках ноль. Найдена настоящая поломка вне списка: сторож слеп не от устаревшего правила (живьём вердикт СВЕЖЕЕ, все три ручки сошлись), а от собственного бюджета 20 с при нужде 41.1 с.

**FACT:** read-only. `git status --porcelain | grep -v '^??'` → **пусто**, ни одного изменённого отслеживаемого файла; правок кода 0. Живое доказательство эффекта — вердикт сторожа на полных данных: `ВЕРДИКТ: СВЕЖЕЕ — слепок сошёлся с живым листом (H3, I3, J3)` при боевом `НЕИЗВЕСТНО — не снялись ручки: J3`. Артефакт `docs/artifacts/2026-08-20-price-reds-triage.md` создан, обе строки легли в мозг (509235 и 509786 символов).

## Дословный вывод

**Первое действие — дерево и время:** `ce464ee 2026-08-21 02:47:36 +0700`, замер `2026-08-21 03:10:58`.
**Второе — признак сделанности:** `ls: cannot access 'docs/artifacts/2026-08-20-price-reds-triage.md': No such file or directory` → не отвечено, работал.

Три прогона одного и того же набора (`test_pricing test_suggest test_dialog_card test_moderation test_moderation_card`):

```
A. ПРАВИЛО + СТОРОЖ      Ran 836 tests in 186.691s   FAILED (failures=58, errors=2)   → 60
B. ЛИСТ (PRICE_SOURCE_SEASON=0)  Ran 836 tests in 162.314s   OK                       → 0
C. ПРАВИЛО, СТОРОЖ ВЫКЛ (PRICE_GATE_TTL_MIN=0)  Ran 836 in 177.997s  FAILED (failures=50) → 50
```

```
A правило+сторож : 60      B лист : 0      C правило, сторож ВЫКЛ : 50
== М2 — ВИНА СТОРОЖА (красны в A, зелены в C): 11 ==
== М1 — ВИНА ЧИСЛА ПРАВИЛА (красны и в A, и в C): 49 ==
== КРАСНЫ И НА ЛИСТЕ, И НА ПРАВИЛЕ: 0 — НИ ОДНОГО ==
C \ A (новые при выключенном стороже): 1 ['test_pricing.test_live_xsr155_bare_day_gives_xsr_price_not_mt03']
```

Разделяющие тексты отказа — М1 несёт котировку, но с другим числом; М2 несёт молчание:

```
М1: AssertionError: '472' not found in 'XSR 155 — 364 ฿/день; итого 5096 ฿; депозит 7000 ฿…'
М2: AssertionError: '[QUOTE]' not found in "Ты — менеджер проката мотобайков TurboBaby…"
М2: TypeError: argument of type 'NoneType' is not iterable
```

Почему молчит сторож — две дороги к одним девяти GET:

```
ПУТЬ 1 — БОЕВОЙ, бюджет 20с (21.8 с)     H3=0.15 (3 юнита) · I3=0.15 (2) · J3=None (0)
   error=I3/VULCAN…: RuntimeError; J3/NMAX…: RuntimeError; J3/XMAX…: RuntimeError; J3/ADV…: RuntimeError
ПУТЬ 2 — БЕЗ БЮДЖЕТА (41.1 с)            H3=0.15 (3) · I3=0.15 (3) · J3=0.25 (3)   error=None
ВЕРДИКТ: СВЕЖЕЕ — слепок сошёлся с живым листом (H3, I3, J3) и не старше порога
```

Возврат ручки — тремя чтениями и замером продукта: `git status` чист, `_FALSE` в `price_source.py:57` нетронут, `env | grep -c PRICE_` → `0`, и `test_pricing` после эксперимента снова `Ran 93 tests / FAILED (failures=20)` — ровно как до.

Храповик: `ИТОГО … точек 994, файлов 66` против линии `"measured": "2026-08-08"`, `"files": 46`; из восьми прибывших файлов шесть стоят «0/0 →», от цены — один (`price_source.py`), `price_gate.py` в списке нет.

## Сводка

1. **Гипотеза исполнителя опровергнута в заявленном виде**: молчание сторожа объясняет **11 красных из 60**, остальные **49** красны потому, что правило считает другое число — они остаются красными при полностью снятом стороже.
2. **Настоящей поломки среди 60 нет** (на обоих источниках красных ноль), но есть обратное: **скрытый ложно-зелёный** `test_live_xsr155_bare_day_gives_xsr_price_not_mt03` — молчание уводит все пять фраз в ветку-отступление `test_pricing.py:795`, и ядро теста не исполняется ни разу.
3. **Поломка нашлась не в тестах, а в стороже**: правило свежее (3 сут из 14, все три ручки сошлись с живым листом), но боевая врезка гасит цену, потому что девяти пробам нужен 41.1 с при бюджете 20 с. Порядок проб фиксирован — J3 голодает всегда, не мигая. В проде это значит «бот не назовёт цену никогда», и ни один тест этого не ловит: все 11 юнитов врезки мокают дверь.
4. **Прежние два красных те же, содержимое выросло**: храповик 845 → 994 — это стухшая линия от 08.08 (46 файлов против 66), вклад цены 1 файл из 8; в замыкании зависимостей ценовых имён **три**, а не два — прежний артефакт пропустил `price_freshness_run.py`.
5. **Предсмертный взгляд**: починку сделают «приведением голденов», забетонируют молчание в 11 тестах, оставят бюджет 20 с — гейт позеленеет, а бот молча перестанет называть цену, и заметят это по деньгам, а не по тестам.

Ничего не чинилось: кода не правил, тестов не трогал, ручку и сторож не менял ни в одном файле, ботов не перезапускал, клиентам не отправлял ничего. Весь гейт одной командой не запускал — гнал только нужные наборы, синхронно.

RESULT: разведка выполнена read-only, артефакт docs/artifacts/2026-08-20-price-reds-triage.md записан, журнал (ARTIFACT+DONE) лёг; 60 красных разложены замером на 49 «другое число правила» и 11 «молчание сторожа», на листе красных 0, найдена настоящая поломка — бюджет сторожа PRICE_GATE_PROBE_SEC=20с при измеренной нужде 41.1с гасит цену при вердикте СВЕЖЕЕ, плюс скрытый ложно-зелёный test_live_xsr155_bare_day; дерево не изменено.
