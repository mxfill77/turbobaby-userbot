# Полный отчёт задачи 13 — 2026-08-18 18:25 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4900 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 13 |
| записано | 2026-08-18 18:25:53 |
| символов в отчёте | 4900 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

Узлы мозга целы: пропаж нет. `business_rules` 26 636 симв./525 строк, `master` 102 961 симв./1092 строки, U+FFFD 0 в обоих. Все пять записей 17–18.08 в бизнес-правилах ЕСТЬ, оба пункта в карте ЕСТЬ. Живое тело обоих узлов совпало с задуманным СИМВОЛ В СИМВОЛ. Гонка сюда не достала: соседних записей ближе 18 ч 32 м нет.

**FACT:** артефакт `docs/artifacts/2026-08-18-brain-integrity.md` создан (2026-08-19 01:40 +07:00, 8 разделов). Живое доказательство эффекта — вывод `_scratch_brbuckets_0818/verify.py`: `OK живое тело СОВПАЛО С ЗАДУМАННЫМ символ в символ | живое 26636, задуманное 26636` и `_scratch_autologon_0818/verify_master.py`: `СВЕЖЕЕ ЧТЕНИЕ: ref=name:master, длина 102961 (до записи было 97486, задумано 102961) → [OK] совпадает с задуманным СИМВОЛ В СИМВОЛ`. В узлы мозга не записано ни байта.

**ПРИЗНАК СДЕЛАННОСТИ (первым действием).** `docs/artifacts/2026-08-18-brain-integrity.md` на диске **НЕ БЫЛО** (`ls` → `NOT_FOUND`; из 11 файлов за 2026-08-18 такого имени нет). Заход прошёл с нуля, все четыре вопроса отвечены.

---

## 1. `business_rules` — 26 636 символов, 525 строк, U+FFFD 0

Адрес взят по имени из живого реестра (`list_brain` → 35 ключей + `resolve_name`), id не хардкодился:
```
РЕЕСТР ЖИВОЙ: ключей 35 (не считая folder_id)
ИМЯ KB_business_rules -> name:business_rules   [имя KB_business_rules → ключ реестра business_rules (канон business_rules)]
ЖИВОЕ ЧТЕНИЕ name:business_rules: 26636 символов, строк 525, U+FFFD 0
```

| запись | вердикт | где |
|---|---|---|
| развилки цены от 17.08 | **ЕСТЬ** | `РАЗВИЛКИ ЦЕНЫ: СЕМЬ ВОПРОСОВ ВЛАДЕЛЬЦУ (замер 17.08)` стр. 293, 1 вхождение; все 7 развилок — стр. 303/314/327/339/350/360/374 |
| правило про смешанный период | **ЕСТЬ** | стр. 405–411: «ПЕРИОД ЛЁГ НА ОБА СЕЗОНА СРАЗУ… Правило: смешанный период — бот спрашивает» |
| уточнение про длинную аренду | **ЕСТЬ** | стр. 389–390 «Уточнение владельца 18.08.2026, отменяет прежнюю формулировку 17.08 об этой длине» + 400–401, тело 391–416 |
| годность расчёта на рабочем диапазоне | **ЕСТЬ** | `ТОЧНОСТЬ ЦЕНЫ НА РАБОЧЕМ ДИАПАЗОНЕ (замер 18.08)` стр. 427; вывод стр. 437–442 |
| разбивка по корзинам срока | **ЕСТЬ** | `РАЗБИВКА ТОЧНОСТИ ПО КОРЗИНАМ СРОКА: ВСЕ ШЕСТЬ` стр. 452, 1 вхождение; шесть корзин 468–487 |

## 2. `master` — 102 961 символ, 1092 строки, U+FFFD 0

- Автологон: `=== ИНСТРУКЦИЯ АВТОЛОГОНА — ВООРУЖЕНИЕ, ОТКАТ, ПРОВЕРКА (18.08.2026) ===` стр. **45**, 1 вхождение; все 10 шагов отдельными строками, порядок 1→10 не нарушен.
- Отзыв ключей: стр. **1076**, 1 вхождение — `ROTATE СЕКРЕТОВ: MODERBOT_TOKEN — утёк в лог, РОТИРОВАТЬ. EdenAI-ключ + GitHub PAT — ротация ОТЛОЖЕНА до 17.09.2026.` Сам перенос доказан снимками: 29.07 и 31.07 читают `до 17.08.2026`, 18.08 — `до 17.09.2026`.

## 3. Пропало — НИЧЕГО

Вопрос «когда последний раз наблюдалось живым» не возникает. Взамен — датировка целости: на **18.08 18:44** записи 1–4 уже лежали в теле (`grep` по снимку `body_before.txt`: 1/1/2/1 вхождения), 18:48:35 добавила пятую, и с тех пор тело **не изменилось ни на символ**.

## 4. Почему гонка сюда не достала

Условие потери: сосед-последователь ближе 15 с (все 9 потерь имели такого, 0 из 56 без него). Записи ПК: `business_rules` 18.08 00:15:52 и 18:48:35 — разрыв **18 ч 32 м 43 с**; `master` — одна запись 20:10:39, соседа нет. Разрыв в четыре порядка от опасной полосы.

## Две оговорки, обе в артефакте

1. **Единственный `[ПРОВАЛ]` — у прибора, не у узла.** `verify_master.py` печатает `ОТКРЫТО:: 1→3`. Доказано, что это бухгалтерия сверщика: `ОТКРЫТО:` в `before`=3, в теле вставки=**0**, в `expected`=3; `meta.json` хранит для всех 25 заголовков значение `1` (множество строк), а сравнивается оно со счётом подстроки. Прибор врал так же и в момент записи 18.08 20:10, в артефакте записи это не объяснено. Кода не правил — задача read-only; починка: в `write_master.py` вместо `1` класть `before.count(h)`.
2. **Граница честности.** Локальные бэкапы видят только записи с этого ПК; записи 2–4 (слова владельца) легли с другой полосы и бэкапа здесь не оставили — их наличие установлено живым чтением, а не распиской. И целость доказана снимком «задуманного тела», который оставили писавшие сессии, — это свойство тех двух заходов, а не канала: замка от гонки в канале как не было, так и нет.

## Что НЕ сделано намеренно

Строка в `cowork_log` **не написана**: `cowork_log` — узел мозга, а первый запрет задачи — «в узлы мозга НЕ писать ни байта». Ритуальную строку допишет `SessionEnd`-хук отдельным процессом; называю это прямо, чтобы отсутствие записи не выглядело молчаливым пропуском.

RESULT: узлы мозга целы — все 5 записей 17–18.08 в business_rules (26636 симв.) и обе записи в master (102961 симв.) на месте, живые тела совпали с задуманными символ в символ, пропаж 0; единственный красный замок оказался дефектом сверщика (доказано числами); артефакт docs/artifacts/2026-08-18-brain-integrity.md записан, в мозг не записано ни байта.
