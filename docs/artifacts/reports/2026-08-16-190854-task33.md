# Полный отчёт задачи 33 — 2026-08-16 19:08 UTC

Сохранён АВТОМАТИЧЕСКИ демоном `pc_orchestrator.py` **до** обрезки под потолок очереди:
отчёт был **4628 символов** при потолке **4500**. В очередь ушли голова, пометка обрезки
и ссылка на этот файл (очередь — витрина, а не хранилище тел).

| поле | значение |
|---|---|
| задача | 33 |
| записано | 2026-08-16 19:08:54 |
| символов в отчёте | 4628 |
| потолок очереди | 4500 |
| блок «FACT» в отчёте | есть |

---

**Правило «в бронь уходят ПОСЛЕДНИЕ названные даты» — НЕ СУДИМО:** судимых пар всего 4 из 62 строгих, счёт на них 2:2 при собственном шуме мерки 50%. Эталон 131/86/62 воспроизведён до единицы. Модель: 4 из 4 строгих — последняя названная (направление за правило, порога не берёт). Артефакт, коммит `ad757f8`, строка в мозге.

**FACT:**
- `FACT: commit ad757f8 в git log` — `docs/artifacts/2026-08-16-etalon-rules.md`, 293 строки, 1 файл
- `FACT: воспроизведение эталона — ДА` (131 пара / 86 диалогов / 62 строгих), отпечаток соли `3792b5e0bbed42f9` сошёлся со сборкой корпуса 16.08; при расхождении прогон останавливался сам
- `FACT: журнал — «OK: записано в мозг, символов: 393532»`
- `FACT: read-only` — один GET `clients`, мутаций ноль; тренажёр не запускался

**Защита от повтора:** `2026-08-16-etalon-rules.md` на диске не было → работа выполнена. **Основание:** `2026-08-16-anon-rebuild.md` (`57e294d`) на месте, 131 пара при 86 диалогах из 710 подтверждена грепом.

## Четыре раскладки

**1. Правки почти нет — ровно как ждала постановка.** Мягкий признак даёт 56 из 131, но это не правки: в 50 парах вторая дата — КОНЕЦ брони («с 5 по 12»), а не передумывание. Лестница признаков: сменилась первая дата заявления 56/18 → ≥3 разных даты 45/13 → **клиент словами отменил ранее названное 15 из 131 и 4 из 62**. Прогноз 15.08 был «≈6–7» — порядок угадан.

**2. С чем совпало (строгие / все).** ПОСЛЕДНЕЕ 2/3 · ПЕРВОЕ 2/7 · ни одно 0/5. Ни один из пяти разрезов не дал перевеса за ПОСЛЕДНИЕ; везде, где перевес есть, он за ПЕРВЫЕ. Опровержением **не считаю**: разрез «сырая лента» (3 против 30, p=8·10⁻⁵) измерил порядок слов в «с … по …», а не решение человека.

**Третья группа больше двух первых** — 30 из 56 мягких, 7 из 18 строгих, и распадается на четыре разные вещи: записана дата из СЕРЕДИНЫ разговора (8/2 — прямая улика против самой развилки «первое или последнее»); «последнее» оказалось концом брони (7/1 — дефект признака); не называли вовсе (15/4). Главное в ней — **полоса промаха «1–3 суток» ТОНКАЯ: 9 пар из 100**. Записывай менеджер «примерно сказанное», промахи копились бы там. Запись либо точна, либо уезжает на недели ⇒ **вторая половина броней согласована не в этом тексте**; точное попадание даже на строгих — 58.7%.

**3. Модель.** Не названа 75/38 · одна 34/20 · правка 22/4. Среди правок последняя названная — 11 из 22 и **4 из 4** (p=0.125). Контроль чище, чем у дат (65.0% против 50.0%): словарь замкнут, парной «даты конца» у модели нет. Направление за правило, порога не берёт.

**4. Строгие 62 против 131 — вердикт и направление СХОДЯТСЯ, величины расходятся,** мягкий отбор шумит: контроль 34.1%→50.0%, доля «правок» 42.7%→29.0%, правка модели в 2.6 раза завышена. Механизм назван числом: броней на диалог `1→62, 2→30, 3→15, 4→4, 5→5, 6→6, 9→9`; **69 пар из 131 сидят на многобронном диалоге**, и 26 из 39 промахов — оттуда. Доверять надо строгим — с припиской, что строгий отбор чинит смещение, а не нехватку материала.

## Ключ HMAC — одной строкой

**Лежит в `tmp/anon_label_salt.txt` (64 байта); переустановку НЕ переживёт (`tmp/` в `.gitignore`, строка 65); копии НЕТ — обход дерева по `*salt*` дал ровно один этот файл.** Посылка «ответить нельзя без чтения .env» **неверна и опровергнута кодом**: у `load_salt` ровно две ветки — читать файл или создать через `O_EXCL`, ветки окружения/конфига нет вовсе. Поэтому `.env` **не открывался** и `НЕИЗВЕСТНО` отвечать не пришлось. Значение соли не печаталось; сверка шла отпечатком.

## Прочее

- **Не пушил:** ветка была `ahead 4` ещё до меня — три предыдущих коммита владельца тоже не отгружены. Пуш утащил бы чужое; нужен пуш — скажите.
- **Границы, честно:** относительные сроки («завтра», «через 3 дня») не извлекаются — верхняя причина строки «дат нет» (31 пара); форма «25 числа» не разрешается в год (17 пар). Оба класса завышают шум и **ни один не сдвигает счёт «первое против последнего»** — они слепы к обоим краям одинаково.
- **ПДн:** открыт только `client_chats.anonstable.jsonl`; наружу вышли исключительно счётчики — ни фразы, ни конкретной даты, ни номера диалога, ни имени/ника/телефона/гео. Цены из текста не брались. Не удалено ничего; `_scratch_etalon_0816/` оставлен.

RESULT: правило «последних дат» проверено на эталоне 131 пары — НЕ СУДИМО (судимых 4 из 62 строгих, счёт 2:2, шум мерки 50%); по модели 4 из 4 за правило, но p=0.125; строгие сходятся с полными вердиктом и расходятся величинами (мягкий отбор шумит, 69 пар на многобронных диалогах); ключ HMAC — tmp/anon_label_salt.txt, переустановку не переживёт, копии нет; артефакт docs/artifacts/2026-08-16-etalon-rules.md, коммит ad757f8, строка в мозге записана.
