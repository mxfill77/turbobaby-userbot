# Поля страницы шаблона договора: правое 35.58 → 14.17 pt (21.08.2026)

Шаблон: Google Doc «Шаблон», id `1Hh2GlpxJsa4DplbKYaM1X0Nt6DLCWYQb6znWcG61tOE`.
Резервная копия `1Nj0sJz0…` НЕ открывалась. Временная папка захода: `_scratch_pagemargins_0821/`.

## Что было и что стало (пункты, живое чтение через мост)

| поле | ДО | ПОСЛЕ | |
|---|---|---|---|
| верхнее | `7.086614173228346` | `7.086614173228346` | не менялось |
| нижнее | `6.366141732283552` | `6.366141732283552` | не менялось |
| левое | `14.173228346456689` | `14.173228346456689` | не менялось |
| **правое** | `35.58070866141733` | `14.173228346456689` | **ИЗМЕНЕНО** |

Лист `595.4399999999999 × 841.68`. Колонка текста: `545.6860629921259` → `567.0935433070866`
(шире на 21.41 pt ≈ 0.755 см) — асимметрия листа снята, шапка и таблица центрируются по одной оси.

Правое поле выставлено **точным значением левого**, а не округлённым «14,17»: тогда
`лево == право` истинно буквально, разница `0.0`.

## Премиса (проверялась первой)

Требовалось: левое ≈ 14,17 pt, правое ≈ 35,58 pt. Замер дал `14.173228346456689` и
`35.58070866141733` — **сошлось**, поэтому правка пошла. Кроме премисы в самом мосте стои́т
замок от дрейфа: запись отказывает, если между чтением и записью левое/правое разъехались
больше чем на 0.01 pt.

## Что НЕ изменилось (чтение назад, а не расписка)

- метки `%имя%`: **27 до / 27 после**, уникальных 26/26, поимённо совпали (`%Pbt%` ×2);
- элементов в теле: **33 до / 33 после**;
- первые 8 элементов — **идентичны** по типу, тексту, кеглю, жирности, шрифту, цвету,
  выравниванию, отступам и инлайн-составу (поэлементная сверка снимков `state_before.json`
  и `state_after.json`, все восемь «совпал»);
- боевой мост: `ping` → `ok=True, TurboBaby Bridge alive`; одноразовые маршруты на штатном
  адресе → `unknown_action` (прод @79 их не знает).

## ОТКАТ — одной строкой

```
venv/Scripts/python.exe _scratch_pagemargins_0821/set_right.py 35.58070866141733
```

(скрипт сам читает текущие поля, ставит их замком от дрейфа, меняет ТОЛЬКО правое и читает
назад; остальные три поля не трогает ни одной веткой).

## Инструмент — и почему он ОСТАВЛЕН на месте

Структурной правки Doc в 89 маршрутах моста нет, поэтому в HEAD скрипт-проекта добавлен
модуль `TmpPageMargins.js` (чтение полей/элементов/меток + установка правого поля) и **7 строк**
в диспетчер `Bridge.js` (маршруты `tmp_mg_read`, `tmp_mg_set_right`). Под работу поднят
отдельный версионный деплой `@82 — TEMP 21.08 page margins task`.

**Ничего из этого не снято: владелец запретил уборку за собой этим заходом.** Прод закреплён
на номере версии (@79) и этих маршрутов не видит — проверено вызовом. Единственный оставшийся
риск: если однажды прод будут двигать на свежую версию HEAD, маршруты уедут в прод вместе с
ней. Когда уборка понадобится — два шага:

```
clasp undeploy <id деплоя @82 с меткой "TEMP 21.08 page margins">
# затем: убрать TmpPageMargins.js и 7 строк из Bridge.js в _scratch_pagemargins_0821/push и clasp push --force
```

Снимок HEAD до правки — `_scratch_pagemargins_0821/head/` (+ `head_pristine.md5`, 17 файлов):
именно он возвращает HEAD в исходное состояние.

## Побочное наблюдение (не моя правка)

Шапка документа изменена руками ПОСЛЕ захода 21.08 по TB MOBILITY: в теле 33 элемента вместо 35,
шапка теперь из **шести** элементов — «PREMIUM MOTORBIKE RENTAL · PHUKET» стала
«BIG BIKES AND SCOOTERS RENTAL · PHUKET» и жирной, строка `turbophuket.com` убрана, вторая
линейка ушла инлайном внутрь абзаца Reg./Tax, «MOTORBIKE RENTAL AGREEMENT» → «RENTAL AGREEMENT»,
кегль строк 3–4 стал 8 pt вместо 9. Здесь это только зафиксировано; ничего из этого не трогалось.
