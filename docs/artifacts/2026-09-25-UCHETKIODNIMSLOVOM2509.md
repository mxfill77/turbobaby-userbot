# Учётки подписки одним словом (25.09.2026, UTC)

Полоса: ПК. Ось: обслуживание. Заход — облачный контейнер Claude Code (Linux), ветка
`claude/confident-allen-gki2jx` репозитория `mxfill77/turbobaby-userbot`, PR в `main` —
https://github.com/mxfill77/turbobaby-userbot/pull/1. Серверный репозиторий
`turbobaby-manager-bot` НЕ правился.

## FACT

| что | факт |
|---|---|
| коммиты кода | `d6d4abd` (первая сборка) + `3a2f448` (два ревью: 32 находки, исправлены 31, одна — остаток §6) + `7d2c0dd` (мост на прежний файл дерева и кавычки в слове, §0) · артефакт — отдельным коммитом |
| пуш | **состоялся**: `7e1175d..3a2f448` в `claude/confident-allen-gki2jx` (первая попытка 25.09 дала 403, повтор по слову владельца прошёл) |
| доставка на ПК | PR №1 слит в `main` (`6fb7ac1`, 08:25 UTC), мост — PR №2. **На ПК код НЕ ДОЕХАЛ — ветки разошлись** (§0): на ПК два локальных коммита поверх `472abd4` (`0d34f16`, `4f1cb779d`), не запушены, авто-фетч демона такой `main` не тянет. Живой признак: 15:32 по ПК слово «учётки заведи»» получило «не знаю», и в списке «умею» строк учёток нет. **Нужен один ход на ПК — свести `main` (§0.3)** |
| п.0 — перевод строителей на `D:\claude_profile_3` | **сделан на ПК сессией владельца 25.09 в 15:16 по ПК — ПРЕЖНИМ рычагом** (`claude_profile_choice.txt` = путь профиля 3, коммит `0d34f16`; строка журнала 08:15 UTC: «RC и строители ПК переведены на учётку samfold01… Max 20x»). Из облака не проверялось — источник строка журнала, прочитанная через Drive. Мост §0.2 этот перевод сохраняет |
| реестр | формат и код готовы: `accounts_registry.json` в корне дерева ПК, под `.gitignore`, модуль `accounts_registry.py`. **На ПК не заведён** — слово «учётки заведи» либо консоль (§5) |
| RC | свой файл `rc_profile_choice.txt` (вне git); нет файла → мост на прежний `claude_profile_choice.txt` (§0.2), тот не решает → ОСНОВНОЙ; реестр строителей RC не читает ни одной веткой |
| поведение сразу после доставки | **учётка не меняется** (мост §0.2): реестра нет → строители по прежнему файлу (на ПК — профиль 3) плюс WARNING «заведи реестр»; своего файла RC нет → RC по прежнему файлу (профиль 3). Без моста код `6fb7ac1` увёл бы обоих на ОСНОВНОЙ |
| слова (тема 205, только владелец) | «учётки» · «учётка N» · «учётки заведи». Идут в фоне: тема не блокируется, второе слово и «обновись» во время хода — отказ словами |
| сервер | не тронут: ни файла окружения, ни демона, ни одного ssh (в контейнере нет бинаря `ssh`) |
| тесты затронутых модулей | 368 прогнано; красные только 6 Windows-тестов подъёма детей (`test_pc_agent.TestSafeModeAtBothRaisePlaces`), они красные и на `472abd4` |
| полный набор (Linux, `REPO` = своё дерево в обоих прогонах) | `472abd4`: 8466 тестов, 242 красных (среда Linux) → `3a2f448`: 8588, тот же набор 242 поимённо → `7d2c0dd`: 8597, **новых красных 0** (минус 4: копия базы лежала под `/tmp`, а эти тесты требуют пути не во временном каталоге) |
| отрицательные тесты мутантами | **30 мутантов из 30 дают красное**, без мутанта 0 красных (§4): 9 на отрицательные тесты задания, 16 на находки ревью, 5 на мост и кавычки |
| клиентский контур | **код клиентов не правился, но замыкание ботов правка ЗАДЕВАЕТ** — ворота детей (`_bots_hit`) числят клиентскими 5 файлов: `accounts_registry.py`, `claude_profile_choice.txt`, `pc_agent.py`, `pc_orchestrator.py`, `profile_choice.py`. Прежняя строка «не задет» проверяла только ворота входа и была неполной (§2.6) |

## 0. Живой случай 25.09: слово не сработало — почему и что сделано

### 0.1 Факты (журнал ПК `KB_cowork_log`, прочитан через Google Drive, только чтение)

| время UTC | строка журнала (суть) |
|---|---|
| 08:15 | Dispatch/RC на ПК: «RC и строители ПК переведены на учётку samfold01 (`D:\claude_profile_3`…). Рычаг `claude_profile_choice.txt` = путь профиля 3, коммит `0d34f16b` (не запушен)»; RC-проба: «HEAD 0d34f16 на 1 коммит впереди origin/main (не запушен)» |
| 08:15, 08:20 | ворота клиентского контура: применение `0d34f16be`, затем `4f1cb779d` к userbot/moderbot ОСТАНОВЛЕНО — клиентские файлы `claude_profile_choice.txt`, `pc_orchestrator.py` (заморозка, владельца не спрашивают) |
| 08:21 | последняя правка журнала до слияния PR №1 (08:25) |
| 08:30 | Dispatch на ПК: владелец вошёл в `D:\claude_profile_2` под samfold01 (профили 2 и 3 — теперь ОДНА учётка); «комментарий рычага поправлен, коммит `e7039239`, значение прежнее `D:\claude_profile_3`» — ТРЕТИЙ локальный незапушенный коммит |
| 08:32 (15:32 по ПК) | тема 205: «учётки заведи»» → агент «не знаю…», в списке «умею» строк учёток НЕТ — агент на старом коде |
| 08:36 | демон: «авто-фетч: локальный main разошёлся с origin (не-ff) — git pull пропущен, нужен разбор» — диагноз §0.1 подтверждён самим демоном |
| ≈09:20 | тема 205: «учётки» → снова «не знаю»; `origin/main` = `45aef68` (PR №2), ПК свои коммиты не отправлял — `main` ПК не сведён |

**Почему не доехало.** `main` на ПК = `472abd4` + два локальных незапушенных коммита; `origin/main` =
`472abd4` + PR №1. Ни один не предок другого → `pc_orchestrator._git_autofetch` уходит в ветку «не-ff
(расхождение) — git pull пропущен, нужен разбор» и не тянет НИКОГДА, пока `main` не сведут руками.
Самообновление демона и подъём агента (`selfraise_agent`) идут только после pull — их не было.

**Вторая находка того же случая — хуже первой.** Код `6fb7ac1`, доехав, ОТКАТИЛ бы перевод 15:16:
реестра нет → строители на ОСНОВНОЙ; своего файла RC нет → RC на ОСНОВНОЙ (после рестарта RC), то
есть телефон снова не увидел бы устройство — ровно то, что владелец чинил в 15:16. Задание писало
«нет реестра — ОСНОВНОЙ» и «RC — по умолчанию ОСНОВНОЙ», когда прежний файл и говорил ОСНОВНОЙ;
посылка изменилась в 15:16, и правило, взятое буквально, стало переводить учётку доставкой кода.

### 0.2 Мост (коммит `7d2c0dd`)

| чей выбор | есть свой | своего нет |
|---|---|---|
| строители | реестр `accounts_registry.json` (даже битый — решает он: битый → ОСНОВНОЙ + WARNING) | прежний файл `claude_profile_choice.txt` тем же разбором, что до 25.09, + WARNING «заведи реестр»; прежний файл не решает → ОСНОВНОЙ + WARNING |
| RC | `rc_profile_choice.txt` | прежний файл, как RC читал до 25.09; не решает → RC-страховка (ОСНОВНОЙ) |

Мост только ЧИТАЕТ: реестр не создаёт, прежний файл не правит (тест: sha256 прежнего файла тот же,
реестра и файла RC не появилось). Слово «учётка N» пишет только реестр, поэтому смена строителей RC
не двигает — отрицательный тест задания держится при ОБОИХ значениях прежнего файла (профиль 3 и
ОСНОВНОЙ). Реестр, заведённый словом «учётки заведи», сразу забирает строителей себе; RC остаётся на
прежнем файле, пока владелец не положит свой `rc_profile_choice.txt`. Шапка прежнего файла говорит
«ЗАПАСНОЙ: читают, пока нет своего», а не «никто не читает».

**Слово прощает кавычки.** «учётки заведи»» (дословно из темы 205) и «учётка «3»» разбираются как
формы; кавычки снимаются в любом месте, знаки — по краям; «учётка 3 и ещё» по-прежнему не разобрано.

### 0.3 Один ход на ПК — свести `main` (руки ПК-сессии: RC с телефона или Dispatch)

```
git fetch origin
git status                       # дерево чистое? грязное — сначала разобрать, не затирать
git merge origin/main            # НЕ rebase, НЕ reset: коммиты 0d34f16/4f1cb77/e7039239 сохраняются
#   claude_profile_choice.txt: e7039239 правил комментарий этого же файла — конфликт возможен. Итог:
#   шапка main (5 строк «ЗАПАСНОЙ…») сверху, ниже текст ПК целиком, ПОСЛЕДНЕЙ строкой значение
#   D:\claude_profile_3; значимая (не «#») строка в файле ровно одна
venv\Scripts\python.exe -m unittest test_accounts test_accounts_run test_rc_supervisor test_rc_choice_wiring test_profile_choice test_pc_agent test_vps_token_install
git push origin main             # только при зелёных тестах
```

Демона и агента руками не трогать: новый HEAD поднимет самообновление, агент поднимется сам. Проверка
— слово «учётки» в теме 205: строители и RC на `D:\claude_profile_3` с WARNING «реестра нет», пробы
всех профилей и слотов. Затем «учётки заведи» — реестр заберёт строителей себе.

**Развилка владельца после 08:30 (ASK, не решается кодом).** Задание 25.09 п.1 кладёт №2 =
`D:\claude_profile_2` + слот B («вторая учётка»). С 15:30 по ПК профиль 2 вошёл в samfold01 — ту же
учётку, что профиль 3, — а в слоте B сервера по-прежнему токен ВТОРОЙ учётки. «учётки заведи»
запишет №2 по заданию, и тогда «учётка 2» сведёт ПК на samfold01, а сервер — на вторую учётку:
полосы окажутся на РАЗНЫХ учётках под одним номером. Пробы этого не видят (у ответа поставщика нет
имени учётки). Решение за владельцем: №2 оставить (и вернуть профиль 2 во вторую учётку) либо
после заведения переписать строку №2 с консоли ПК (`accounts_run.py --set 2 …`) — либо не звать
«учётка 2», пока это не решено.

## 1. Премисы Штаба — сверка первым действием

| премиса | исход | чем проверено |
|---|---|---|
| П1: строителей выбирает `claude_profile_choice.txt`; читают обе ветки спавна демона и RC; userbot и модербот не читают; файл в git, грязное дерево останавливает авто-фетч | **подтверждена по коду** | `pc_orchestrator._run_task_impl` и `_thinker_exec` звали `profile_choice.apply_to`; `rc_supervisor.child_env` — тоже; в `userbot_listen.py`, `moderation_bot.py`, `moderation_core.py`, `suggest.py` имени нет; `pc_orchestrator._git_autofetch` на правке отслеживаемого файла пишет «рабочая копия грязная — git pull пропущен». Ссылку `4264ca90` в истории репо найти не удалось |
| П2: сервер — действующее + слоты A/B; прежний `vps_token_install.py` пишет в действующее и A; `limit_slot.py` — один повтор под другим слотом, действующий ищет равенством | **подтверждена по коду; чей вход сейчас в A — НЕИЗВЕСТНО** | `vps_token_install.WRITE_NAMES = (действующее, A)`; `manager-bot/limit_slot.py`: `VAR_SLOT = {A, B}`, `active_slot` по равенству, `plan(retried=…)` — один повтор. Живое состояние A мерит слово «учётки заведи» — тремя пробами, а не одной (§2.1) |
| П3: команд учёток в `pc_agent` нет | **подтверждена**: до правки в `pc_agent.py` ни одного слова «учётк» |

## 2. Что построено

### 2.1 Реестр и выбор строителей (`accounts_registry.py`, его импортирует демон)

```json
{"form": 1, "builders": 3,
 "accounts": {"1": {"profile": "ОСНОВНОЙ",            "slot": "",  "label": "основная"},
              "2": {"profile": "D:\\claude_profile_2", "slot": "B", "label": "вторая"},
              "3": {"profile": "D:\\claude_profile_3", "slot": "A", "label": "третья"}}}
```

- номер 1…99 (только ASCII-цифры) → профиль ПК (ОСНОВНОЙ = ключ `CLAUDE_CONFIG_DIR` снят) → буква
  слота сервера `TB_CLAUDE_TOKEN_<буква>` (пусто = слота нет) → метка без почты (`@` — отказ записи);
- строка с браком выбрасывается с названной причиной, остальные живут; слот у двух учёток снят у
  обеих и помечен «спорный» — слово «учётка N» на таком слоте сервер НЕ зовёт;
- выбор строителей: ОСНОВНОЙ → ключ снят; каталог есть → ключ = каталог; **всё прочее → ОСНОВНОЙ +
  WARNING**; **реестра нет вовсе → мост на прежний файл (§0.2)**. Пустого значения ключа нет ни на
  одной дороге; `apply_builders` не бросает никогда (исключение в спавне уронило бы каждый заход);
- обе ветки спавна демона зовут `accounts.apply_builders`; модуль внесён в `_ORCH_RUNTIME`;
- четвёртая и следующие учётки — строкой реестра, код не правится (`accounts_run.py --set`).

**«учётки заведи» — первый реестр одним словом (п.0 + п.1 задания на ПК).** Кладёт п.1: №1 —
ОСНОВНОЙ, №2 — `D:\claude_profile_2` + B, №3 — `D:\claude_profile_3`. Слот A отдаётся по правилу П2
**только при согласии трёх проб**: слот A сервера, основная на ПК, №3 на ПК (учётка одна, лимит у
неё один на обеих машинах):

| слот A | основная на ПК | №3 на ПК | решение |
|---|---|---|---|
| 200 | 429 | не 429 | №3 ← A (у основной в лимите 200 быть не может) |
| 429 | 429 | 200 | №1 ← A (у живой третьей 429 быть не может) |
| 529 / таймаут / сбой канала — у любой из трёх | | | **реестр НЕ пишется**, «повтори позже» |
| всё прочее (401; обе в лимите; основная жива) | | | слоты №1 и №3 пусты, сказано словами, назначить `--set` |

Окно правила — до **измеренного** сброса недели основной, 28.09.2026 21:00 UTC (артефакт 2209,
«Sep 29, 4am (Asia/Bangkok)»; «до 29.09» Штаба — местная дата). Потом A = 200 учёток не различает, и
правило гаснет само. Строители: №3 при пробе 200, иначе №1. Реестр уже есть (даже битый) → отказ,
ни одной пробы. Создание файла — `O_EXCL`: реестр, положенный рукой владельца между проверкой и
записью, не затирается. Сервер заведение только пробует — ни записи, ни рестарта (тест с ловушками
на все шесть серверных ходов).

**`claude_profile_choice.txt`** не удалён; с 25.09 он ЗАПАСНОЙ — его читает мост, пока нет своего
выбора (§0.2). Значение в `main` на GitHub — ОСНОВНОЙ, на ПК — профиль 3 (коммит `0d34f16`), слияние
§0.3 оставит профиль 3.

### 2.2 RC (`rc_supervisor.py`)

Читает свой `rc_profile_choice.txt` тем же разбором `profile_choice`. Файла нет → мост на прежний файл
дерева (§0.2); тот не решает → RC-страховка: унаследованный ключ снимается → ОСНОВНОЙ. Реестр
строителей в коде RC не назван ни разу (ast-замок в тесте).

### 2.3 Сервер (`vps_token_install.py`)

Секрет наружу не выходит ни одним каналом: программа для сервера несёт только имена, вывод —
только признаки (длины, коды, «совпало/нет»).

- слоты любой буквы (`slot_name`, закрытая форма, `fullmatch`);
- `--probe-slots` меряет действующее и КАЖДЫЙ слот файла; время сброса — словами поставщика и
  только на 429;
- `--slot X`: годовой токен только в `TB_CLAUDE_TOKEN_X`, одна проба слота; 401/иное → возврат из
  копии; 429 → оставлен; демон не перезапускается;
- `--use X` — ход слова «учётка N»:
  разведка → **идёт ли заход ДЕМОНА** (процесс `claude` в cgroup `orchestrator-daemon.service`;
  сессия владельца в tmux не мешает; cgroup не прочитана → «не проверено» = стоп) → отметка жизни
  демона → копия с датой → запись серверной программой → **одна проба** по файлу →
  - отказ (401/429/иное): файл из копии; если демон сам перезапустился во время пробы (или сверить
    нечем) — один рестарт на прежнем значении, иначе рестартов 0;
  - 200: повторная разведка (заход начался / разведка оборвалась → файл из копии, рестарта нет) →
    **один рестарт**; рестарт не поднял демона → файл из копии, подъём обратно;
  - слот уже действующий: демон младше файла → ничего; старше (держит прежнее) → один рестарт после
    повторной проверки захода;
  - обрыв канала на записи → исход НЕИЗВЕСТНО, возврат по ожидаемому имени копии (её нет — запись
    до файла не дошла, так и сказано); копия не сошлась с оригиналом → из неё не возвращаем.
  Копия не удаляется никогда. `X` — буква или номер учётки реестра. `--slot` и `--use` разом — отказ;
- старый режим (без ключей) не пишет в A, если реестр отдал A другой учётке;
- удалённый питон — `python3 -u` (напечатанное до обрыва не застревает в буфере), записи — с
  `O_BINARY` (на Windows без него файл окружения получил бы `\r\n`).

**Отступление от порядка задания — названо.** Задание: «копия, рестарт, одна проба, отказ — откат».
Сделано: копия → проба → рестарт только при 200. Проба читает тот же файл, с которым поднимется
демон, но отказ не стоит второго рестарта «обратно». Второй рестарт бывает в двух названных случаях:
сам рестарт не поднял демона, либо демон перезапустился во время пробы и схватил отвергнутый вход.

### 2.4 Слова владельца (`pc_agent.py` → дверь `accounts_run.py` субпроцессом)

- «учётки» — по одной живой пробе на каждый профиль ПК (модель пробы = `EXECUTOR_MODEL`, поиск
  `claude` — как у RC, cwd вне дерева, `--setting-sources project`, `--no-session-persistence`) и на
  действующее и каждый слот сервера. Ответ: номер, метка, ответ ПК и сервера со временем сброса,
  что действует у строителей, у RC и у демона; под каким слотом серверный повтор при 429 пойдёт;
  модели проб обеих сторон. Строители откатились на ОСНОВНОЙ без его строки в реестре → меряется и он;
- «учётка N» — ПК: проба профиля N, при 200 номер строителей в реестре = N (дерево не пачкается);
  сервер: `use_slot` слота N. Итог с пробой обеих сторон;
- «учётки заведи» — §2.1;
- чужому — «⛔ Нет прав…» словами, дверь не поднимается; «учётка 9» — «Учётки «9» в реестре нет.
  Есть: …»; «учётка три» / «учётка 100» — отказ словами. В командную строку уезжает только число;
- **гейт строителей:** из захода демона (метки `PRETOOL_ASK_MARKER`/`PRETOOL_MARKER_TOKEN`/
  `GIT_SERIAL_PC_OWNER`, имена сверяет тест с исходником демона) `accounts_run.py --switch/--init/
  --report` и `vps_token_install.py --use/--slot/--probe-slots` отказывают словами до ssh;
- автопереключения по лимиту нет: дверь не зовёт ни один таймер.

### 2.5 Где в коде

`accounts_registry.py` (новый) · `accounts_run.py` (новый) · `vps_token_install.py` · `rc_supervisor.py`
· `profile_choice.py` · `pc_agent.py` · `pc_orchestrator.py` (две ветки спавна + `_ORCH_RUNTIME`) ·
`.gitignore` (реестр, история, выбор RC) · `claude_profile_choice.txt` (шапка) · `CLAUDE.md`.

### 2.6 Клиентский контур: ДВОЕ ворот, и правка задевает вторые

**Код клиентов (`userbot_listen.py`, `moderation_*.py`, `suggest.py`) не правился.** Но замыканий два:
(1) ворота ВХОДА (`client_contour.is_client`/`mentions`, замыкание от точек входа ботов со срезом
чужих процессов) — ни один тронутый файл не клиентский; (2) ворота ДЕТЕЙ (`pc_orchestrator._bots_hit`,
вычисленное замыкание ботов с 07.09.2026) — клиентскими числятся **5 файлов правки**:
`pc_orchestrator.py` (живой бот грузит его лениво с диска), `pc_agent.py` (лежит в замыкании ботов),
`accounts_registry.py` и `profile_choice.py` (их импортирует `pc_orchestrator`),
`claude_profile_choice.txt` (данные этого замыкания). Прежняя строка «не задет» судила только первые
ворота. Следствие под заморозкой: детей на этот коммит НЕ перезапустят без поимённого «да» владельца
(так ворота держат и коммиты ПК `0d34f16`/`4f1cb779d`, журнал 08:15/08:20); им он и не нужен. Демон
обновляет СЕБЯ и поднимает агента мимо этих ворот (`pc_orchestrator`, ветка self-update: «сам демон
уже обновился… держим только ДЕТЕЙ-ботов»).

**Имя модуля.** Первая сборка звала модуль `accounts.py`. Ворота входа клиентского контура
(`client_contour.mentions`) ищут в тексте задания голый stem каждого модуля корня: слово
«accounts» в находке ревизора делало её «внутренней» (замер: «клиенту не приходят ответы про
accounts» → `(False, [], True)`) вместо fail-closed «клиентской» (`(True, [], False)`). Модуль
переименован в `accounts_registry.py`; сами ворота не правились. Замок — тест
`TestClientGateNotWeakened` (три прозаических текста + «модуля-слова в корне нет»).

## 3. Как проверено

- серверные программы (`_RS_PREFLIGHT`, `_RS_USE`, `_RS_APPLY`, `_RS_RESTORE`) исполнялись
  ПО-НАСТОЯЩЕМУ против **синтетической копии** файла окружения; подменены только каталог процессов
  (фальшивое дерево с cgroup), рестарт юнита (счётчик + с каким файлом поднят), отметка жизни
  демона и ответ поставщика;
- новые тесты: `test_accounts.py` (38), `test_accounts_run.py` (35); `test_vps_token_install.py`
  35 → 81; `test_pc_agent.py` 62 → 73; тесты RC и веток спавна переведены на новый контракт;
- копии и временные каталоги тестов — системный temp, префикс `uchetki_2509_`, не удаляются;
- **два ревью.** Первое: 8 находок, подтверждено перепроверкой 5, опровергнуто 3.
  Второе (безопасность, интеграция, тесты, логика, заведение): **32 находки**, исправлена 31 (одна —
  остаток §6). Перепроверку находок (≈96 проверяющих при параллельности 2 — часы) заход остановил на
  11: подтверждено 9, опровергнуто 2; большинством подтверждены 4 находки (гейт только в `pc_agent`;
  stderr старой пробы; блок темы 205 до 3600 с; гард пропускает эти команды как defer). Все 32
  закрыты кодом независимо от вердиктов.

## 4. Отрицательные тесты — мутантами, числом

Копия рабочего дерева в scratchpad; мутант — правка якоря, после прогона файл возвращён. «Красных»
— строк FAIL/ERROR, считая подтесты. Третий прогон (после моста) — на `7d2c0dd` + исправленный тест
гейта: во втором фоновом прогоне мутант M6b повесил тест на 900 с (снятый гейт вёл `--slot` в
`input()`), а не покрасил его — тест исправлен (`71cf9fe`), прогонщик закрывает stdin.

| мутант | тесты | прогнано | красных с мутантом | без |
|---|---|---|---|---|
| **задание** | | | | |
| M1 не владелец: гейт владельца снят | `TestAccountsWord` | 11 | **6** | 0 |
| M1b не владелец: ветка отказа учёток снята (тишина вместо слов) | `TestAccountsWord` | 11 | **5** | 0 |
| M2 «учётка 9»: проверка номера в реестре снята | `TestSwitch` | 6 | **1** | 0 |
| M3a пустой слот: проверка разведки ПК снята | `UseSlotOnCopy` | 9 | **1** | 0 |
| M3b пустой слот: проверка серверной программы снята | `UseSlotOnCopy` | 9 | **1** | 0 |
| M3c пустой слот: сняты ОБА рубежа | `UseSlotOnCopy` | 9 | **2** | 0 |
| M4 401 после --use: возврат копии снят | `UseSlotOnCopy` | 9 | **2** | 0 |
| M5 RC читает общий файл дерева даже при своём файле | `TestRcUntouchedByBuilders` | 4 | **1** | 0 |
| M5b смена строителей → RC: RC идёт по реестру строителей | `TestRcUntouchedByBuilders` | 4 | **4** | 0 |
| **ревью** | | | | |
| M6 гейт строителей в accounts_run снят | `TestBuilderGate` | 2 | **6** | 0 |
| M6b гейт строителей в vps_token_install снят | `BuilderGate` | 2 | **5** | 0 |
| M7 «идёт заход» снова = любой claude на машине, не cgroup демона | `BusyByDaemonCgroup` | 6 | **3** | 0 |
| M8 демон перезапустился во время пробы — рестарт на прежнем снят | `DaemonRestartedDuringProbe` | 6 | **2** | 0 |
| M8b уже действующий и демон старше файла — повторная проверка захода снята | `DaemonRestartedDuringProbe` | 6 | **1** | 0 |
| M9 «учётки заведи» не проверяет, что реестр уже есть | `TestInit` | 9 | **2** | 0 |
| M9b создание реестра снова затирает (без O_EXCL и без проверки) | `TestCreateIsExclusive` | 1 | **1** | 0 |
| M10 П2: A=200 → №3 без проверки основной в лимите | `TestInit` | 9 | **2** | 0 |
| M10b П2: A=429 → №1 без проверки третьей живой | `TestInit` | 9 | **2** | 0 |
| M11 неясная проба (529) снова пишет реестр без слотов | `TestInit` | 9 | **1** | 0 |
| M12 обрыв канала на записи: возврат по ожидаемой копии снят | `ChannelCutAndBadCopy` | 4 | **1** | 0 |
| M13 несошедшаяся копия: возврат из неё снова делается | `ChannelCutAndBadCopy` | 4 | **1** | 0 |
| M14 спорный слот снова зовёт сервер | `TestReviewFixesRun` | 5 | **1** | 0 |
| M15 слово учёток снова ждёт дверь в обработчике (блок темы до 3600 с) | `TestAccountsWord` | 11 | **1** | 0 |
| M17 apply_builders снова бросает (спавн падает) | `TestReviewFixes` | 6 | **1** | 0 |
| M16 модуль-слово «accounts.py» снова в корне (ворота клиентского контура ослаблены) | `TestClientGateNotWeakened` | 2 | **3** | 0 |
| **мост и слово (живой случай 25.09)** | | | | |
| M18 мост строителей снят: реестра нет → ОСНОВНОЙ (доставка переводит учётку) | `TestBridgeDeliveryKeepsTheAccount` | 6 | **3** | 0 |
| M19 мост RC снят: своего файла нет → ОСНОВНОЙ (телефон теряет RC) | `TestBridgeDeliveryKeepsTheAccount` | 6 | **1** | 0 |
| M19b мост RC читается ВСЕГДА (свой файл RC не бьёт прежний) | `TestRcUntouchedByBuilders` | 4 | **1** | 0 |
| M20 кавычки снова часть слова («учётки заведи»» → не знаю) | `TestAccountsWord` | 11 | **6** | 0 |
| M21 мост пишет в дерево: «ОСНОВНОЙ» из реестра-заглушки вместо прежнего файла | `TestBridgeDeliveryKeepsTheAccount` | 6 | **3** | 0 |

При M3a файл всё равно цел — держит второй рубеж; краснеет тест, требующий отказа уже на разведке ПК.

## 5. Экран владельца: как завести учётку N (выводит `accounts_run.py --screen`)

0. **Первый раз, три известные учётки — одним словом с телефона: «учётки заведи».** Ответ назовёт
   каждую пробу и решение по слоту A. Проба дала 529/таймаут — повторить слово позже. Слот не
   назначен («пробы не сходятся» или правило истекло) — шаг 3 для №1/№3.
1. Профиль ПК: `$env:CLAUDE_CONFIG_DIR='D:\claude_profile_N'; claude auth login` — вход в браузере.
2. Слот сервера: `claude setup-token` в том же окне, затем
   `venv\Scripts\python.exe vps_token_install.py --slot C` (ввод скрыт; 200/429 — оставлен, 401 — откат).
3. Строка реестра:
   `venv\Scripts\python.exe accounts_run.py --set N --profile D:\claude_profile_N --slot C --label "N-я"`
   (основной: `--profile ОСНОВНОЙ`; слота нет: `--slot -`). Номер строителей — `--builders N` или словом.
4. Проверить: «учётки» в теме 205 — у №N ответ 200 (или 429 со временем сброса).
5. Перевести обе полосы: «учётка N» (= один рестарт демона сервера; идёт заход — ответ скажет «стоп»).

Консольные ходы (`--set`, `--builders`, `--slot`, `--use`) запускает только владелец: из захода
строителей они отказывают.

## 6. Чего НЕ сделано и остатки (точки касания)

- **п.0 в облаке — НЕИЗВЕСТНО**; делает «учётки заведи» на ПК после доставки;
- **доставка ЗАВИСИТ ОТ ОДНОГО ХОДА НА ПК** (§0.3): пока `main` ПК не сведён с GitHub, авто-фетч стоит,
  а слова учёток отвечают «не знаю». Живую проверку слов из облака сделать нельзя — первое «учётки»
  после слияния на ПК и есть она;
- **расхождение `main` как класс не закрыто:** любой незапушенный коммит сессии на ПК при движении
  `origin/main` снова остановит доставку. Демон это видит и пишет в журнал («не-ff… нужен разбор»), но
  не будит владельца. Точка касания — ветка `non_ff` в `pc_orchestrator._git_autofetch`;
- **остаток ревью (1 из 32): дверь, убитая посреди переключения** (самоподъём агента через
  `taskkill /T` или таймаут двери 3600 с), может оставить сервер на новом входе без ответа владельцу.
  Сужено, не закрыто: «обновись» во время слова отказывает; ход `--use` на порядок короче таймаута.
  Увидеть исход — слово «учётки». Точки касания: `pc_agent._accounts_cli`, `ACCOUNTS_TIMEOUT`;
- **сервер: `limit_slot.py` знает только A/B.** Демон на C и дальше повтора под другим слотом не
  делает (честный ACT_UNKNOWN). Точки касания — `limit_slot.VAR_SLOT`, `_OTHER`; серверная полоса;
- **гейт строителей держится на метках окружения**: процесс того же пользователя может их снять —
  граница против ошибки, а не против умысла. `pretool_guard` эти команды красными не зовёт;
- после 28.09 21:00 UTC «учётки заведи» слотов №1/№3 не назначает — только `--set` на ПК;
- каталога `D:\claude_profile_3` нет → «учётки заведи» отказывает словами до его появления;
- `set_entry`/`set_builders` пишут через общий временный `accounts_registry.json.tmp`; одновременная
  правка с консоли и словом — не защищена (слова между собой — защищены замком двери);
- RC читает свой файл только после собственного перезапуска с новым кодом — руками не перезапускался;
- **журнал `cowork_log` не записан**: в облачном контейнере нет моста (журнал прочитан через Drive,
  писать туда мимо `cowork_log_append.py` нельзя). Строка для захода §0.3:
  `DONE Dispatch: UCHETKIODNIMSLOVOM2509 — учётки одним словом (реестр вне git, мост на прежний файл, «учётки»/«учётка N»/«учётки заведи», vps_token_install --slot/--use), коммиты d6d4abd+3a2f448+7d2c0dd, PR 1 и 2, main ПК сведён → docs/artifacts/2026-09-25-UCHETKIODNIMSLOVOM2509.md`;
- в рабочем дереве облачного контейнера лежит `accounts_registry.json` от ошибочного прогона теста
  первой сборки (исправлено до коммита); он под `.gitignore`, на ПК не едет, не удалён — запрет
  «ничего не удалять».

UCHETKIODNIMSLOVOM2509
