# Тесты, не импортирующие боевой код — полоса ПК (15.08.2026, read-only)

Порт вопроса с полосы VPS (замер 09:05: 55 модулей под тестом, 14 тест-файлов без боевых
импортов, полный гейт 99.8 с). **Числа VPS здесь не воспроизводятся — мерено заново на своей
полосе.** Ничего не удалено, не поправлено, не закоммичено.

## Метод (чтобы число можно было пересчитать)

- «Боевой модуль» = любой `*.py` в корне репо, кроме `test_*.py` → **69 модулей**.
- Импорты снимаются **AST-обходом** (`ast.walk`), а не грепом: ловит и импорт внутри функции/метода,
  и `from X import y`. Дополнительно проверено упоминание имени модуля в тексте файла — на случай
  динамики (`importlib`, `subprocess`, чтение исходника).
- Полный гейт = `all_test_modules(repo_dir)` из `gate_selective.py:74` — глоб `test_*.py`
  **только в корне**. Формат живого запуска — `pc_orchestrator.py:3795`:
  `venv/Scripts/python.exe -m unittest <модули>`, `cwd=REPO`.

## Замер полного гейта (один прогон, 15.08)

```
модулей в полном гейте: 49
rc=1
ПОЛНЫЙ ГЕЙТ: 215.7 с
Ran 3388 tests in 214.216s
FAILED (failures=2, skipped=10)
```

Оба падения — `test_nonparse_ratchet` (`test_no_growth_against_baseline`,
`test_totals_do_not_grow`, «947 not less than or equal to 845»). Это **известный красный на main
ДО любой правки** (класс «храповик неразбора»), к разбираемым файлам отношения не имеет.

## 1) Строгий класс: импортируют НОЛЬ боевых модулей — **1 файл из 49**

| файл | строк | тестов | секунд | что проверяет на самом деле |
|---|---|---|---|---|
| `test_utf8_output_guard.py` | 252 | 7 | **1.67** | **не фикстуру и не себя — ЖИВОЙ ИСХОДНИК 8 боевых файлов.** Импорта нет потому, что он их `ast.parse`-ит, а не зовёт |

Строки-доказательство (`test_utf8_output_guard.py`):

- `66: with open(os.path.join(HERE, name), encoding="utf-8") as f: → ast.parse(...)` — предмет
  проверки читается с диска как исходник;
- `42: ENTRYPOINTS = ("pc_agent.py", "pc_orchestrator.py", "dispatch_notify.py", "cowork_log_append.py")`,
  `47: + ("selfupdate_gate.py", "rc_supervisor.py", "lesson_router.py")`, `58: + ("log_setup.py",)`;
- `161: missing = [n for n in ENTRYPOINTS if not calls_force_utf8(_parse(n))]` — инвариант A;
- `174: subprocess_text_calls_without_encoding(_parse(n))` — инвариант B;
- `192/203: open_text_without_encoding / logging_file_sink_without_encoding` — инвариант C;
- `232: subprocess.run([PY, "-c", code] …)` — живой round-trip 📊/⏹ через реальный процесс;
- `217: self.assertTrue(os.path.isfile(...))` — держит честным СПИСОК ГРАНИЦ (userbot/moderbot/suggest).

**Вывод: сносу не подлежит.** Отсутствие импорта здесь — не признак пустоты, а способ проверки:
класс кусал ТРИЖДЫ за 29–30.07, чинился по одному месту из трёх и возвращался. Снять страж —
вернуть класс молча.

## 2) Смежный класс: импортируют только НЕ-рантайм — **1 файл**

Рантайм-замыкание контура (транзитивно от 15 боевых точек входа) = **34 модуля**; остальные 35 —
скрипты, разовые замеры и `tmp_*`. Единственный тест, чей боевой импорт целиком вне замыкания:

| файл | строк | тестов | секунд | предмет |
|---|---|---|---|---|
| `test_status_truth.py` | 134 | 16 | **0.12** | голдены **инструмента замера** `diag_status_truth.py` (в рантайме не участвует) |

Это тоже забор, а не пустышка: `50: test_quoted_failure_inside_result_is_not_a_failure` держит
живой ложняк 30.07 — подстрочный regex считал провалом ЦИТАТУ чужого лога внутри отчёта задачи 51
(`54: self.assertIsNone(d._J_FAIL.match(quoted))`). Правило репо №7 («числа — факт») стои́т ровно
на том, что инструмент замера не врёт. **Сносу не подлежит.**

## 3) Тест-файлы, которые гейт НЕ ВИДИТ вовсе — **5 файлов, 0 секунд**

Глоб гейта — только корень, поэтому эти пять не гоняются НИ ОДНИМ прогоном на этой полосе:

| файл | строк | во что целится | исполнимо здесь |
|---|---|---|---|
| `docs/artifacts/2026-07-23-guard-v3-files/tests/test_pretool_commit_msg.py` | 90 | `ROOT = "/root/turbobaby-manager-bot"` | **нет** |
| `…/tests/test_pretool_hardblock.py` | 264 | тот же ROOT + `import fcntl` | **нет** |
| `…/tests/test_pretool_infoflags.py` | 82 | тот же ROOT (`splinter.py`, `venv/bin/python3`) | **нет** |
| `…/tests/test_pretool_probe_dedup.py` | 103 | тот же ROOT (`bot.py`, `gate.py`, `splinter.log`) | **нет** |
| `docs/artifacts/2026-08-14-service-history-named-work/test_service_named_work.py` | 187 | `import splinter` из соседней папки | **нет** |

Замер: `/root/turbobaby-manager-bot` → `False`; `…/splinter.py` → `False`; `splinter.py` в корне →
`False`. Первые четыре к тому же импортируют **архивную копию** `pretool_guard.py` (та самая мина
из CLAUDE.md), а не живой гард.

**Но это НЕ кандидаты на снос.** Они лежат в `docs/artifacts/` — это приложение к артефакту
23.07/14.08 (рядом `guard-v3.patch`, `splinter-named-work.diff`): доказательство того, чем правка
проверялась на ТОЙ полосе в ТОТ день. Удаление стирает доказательство, а гейту не возвращает
ни секунды — он их и так не гоняет.

## 4) Секунды гейта

| | секунд | доля 215.7 с |
|---|---|---|
| `test_utf8_output_guard.py` | 1.67 | 0.77 % |
| `test_status_truth.py` | 0.12 | 0.06 % |
| **оба вместе** | **1.79** | **0.83 %** |
| 5 файлов вне гейта | 0.00 | 0 % |

(Одиночный прогон включает старт интерпретатора 0.06 с — он вычтен.)

## 5) Итог одной строкой

**Сносу без потери покрытия подлежит НОЛЬ файлов из найденных; трогать нельзя все 7** (2 в гейте —
живые заборы над исходником контура и над инструментом замера, 5 вне гейта — приложения к
артефактам, которые гейт и так не гоняет); экономия от полного сноса составила бы **1.79 с из
215.7 с (0.83 %)** — платить за неё возвратом класса кодировки не стоит.

Класс VPS (14 файлов) на полосе ПК **почти отсутствует**: 1 из 49 против 14 у сервера, и этот
единственный — не пустышка. Настоящая цена гейта здесь лежит не в пустых тестах: 215.7 с на
3388 тестов при 49 модулях, и адрес экономии — тяжёлые модули, а не лёгкие.
