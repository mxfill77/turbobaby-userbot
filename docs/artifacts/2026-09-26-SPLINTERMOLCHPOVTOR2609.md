# SPLINTERMOLCHPOVTOR2609 — повторное применение ветки 09587d9 на живом Splinter (задание Штаба 0044-74m.2609)

**Сводка. Исход — ПРИМЕНЕНО.** Премисы П1–П3 сошлись. Базу классов работающего 48ee30a записал до
перемотки. Живой `main` перемотан на `09587d9` fast-forward. `py_compile` зелёный, полный набор живого корня
тоже: 264 файла, красных 0. Сделан ОДИН перезапуск splinter. Проверки через 60 с и через 10 мин зелёные:
служба `active`, NRestarts 0, PID не менялся, **новых классов ERROR/Traceback 0, новых WARNING 0**, в журнале
службы красных строк 0. Строка «подшипники» 4685 на старте была, как предсказал #82. Её вид сменился на
«ждут 0, потеряны 1», класс тот же, и он есть в базе. Отката не было, перезапуск назад не понадобился.
**GitHub `main` на 14:00:32 UTC ещё `48ee30a` — доставка не довезла:** после перемотки она не делала ни
одного замера (последний 13:24:13 UTC).

```
FACT: живой HEAD до 48ee30a6c3780ab665787e61f80dcf8b79284852 → FF 09587d944760e287af85f0326b0536126029d5bc
      (reflog main 13:47:10Z, скрипт 13:47:11 UTC); после — 09587d9 на main, отслеживаемых изменений 0
      (замер 14:00:33 UTC).
FACT: splinter до — PID 2462580, старт 26.09 12:34:25 UTC, NRestarts 0; после — PID 2470428, старт 13:50:17 UTC;
      14:00:33 UTC: active, NRestarts 0, тот же PID.
FACT: набор живого корня на 09587d9 по одному файлу — 264 / 0 красных (179 с); именные 9 зелёные; py_compile 10 rc=0.
FACT: вердикт скрипта VERDICT=APPLIED 14:00:18 UTC — /tmp/molchpovtor2609/apply.out (копия ПК
      tmp/molch_povtor_2609/apply.out); apply.sh sha 5ddfd99556254e68, judge.py sha 9af406ece925228a (ПК = сервер).
FACT: GitHub (ls-remote 14:00:32 UTC): main = 48ee30a6…, splinter-molch-vetka-2609 = 09587d944760….
```

## 0. Признак сделанности, бюджет, временное

- Артефакта SPLINTERMOLCHPOVTOR2609 за 26.09 до захода не было: в `docs/artifacts/2026-09-26-SPLINTER*` лежали
  RAZGOVOR, MOLCHVETKA, MOLCHZHIVOY и PODSHIPNIK. Работу начал с нуля в 13:42:21 UTC.
- Временное на ПК — `tmp/molch_povtor_2609/`: `p0.sh`/`p0.out` (премисы), `p2_lsremote.txt`, `judge.py`,
  `judge_selftest.out`, `apply.sh`/`apply.out`, `base_classes.txt`, `p6_where.out`, `final.out`,
  `final_lsremote.txt`. На сервере — `/tmp/molchpovtor2609/`: `apply.sh`, `judge.py`, `apply.out`, база
  `base_48.log`/`base_48_jctl.txt`/`base_classes.txt`, окна `cand_60s*`, `cand_10m*`, `after/` (rc, вывод и
  сигнатура каждого файла набора), `lock/`, `DONE`. Ничего не удалялось.
- Основание — «да» владельца 26.09 ~13:35 UTC (взято из текста задания, чат не читался).

## 1. Премисы Штаба (проверены первыми)

| премиса | вердикт | замер |
|---|---|---|
| П1 — #81 и #82 есть, правило отката из второго | **верна** | `2026-09-26-SPLINTERMOLCHZHIVOY2609.md` (`e85e9086`) и `2026-09-26-SPLINTERPODSHIPNIK2609.md` (`8039722b`); правило — #82 п.6, `rule.py` sha `b9c4f1a0be1a7855` |
| П2 — ветка на GitHub = 09587d9, потомок 48ee30a | **верна** | `ls-remote` 13:44:36 UTC: ветка `09587d944760…`; на сервере `merge-base --is-ancestor 48ee30a 09587d9` → 0 |
| П3 — живой HEAD 48ee30a, чисто, active; main GitHub 48ee30a | **верна** | 13:44:35 UTC: HEAD `48ee30a6…` на `main`, изменений 0, `active/running`, PID 2462580, старт 12:34:25 UTC, NRestarts 0; GitHub `main` = `48ee30a6…` |
| применения 09587d9 после 26.09 12:34 UTC не было | **верно** | последняя запись reflog `main` — `reset: moving to 48ee30a` 12:34:24Z; стартов службы после 12:34:25 нет; `/tmp/molchpovtor2609` не существовал |

## 2. База до перемотки (48ee30a, от старта назад 12:34 UTC до FF)

Окно `splinter.log` — строки **442061..442319**, от `12:34:26,261 … STARTING` до 13:47:07 UTC: 259 строк,
sha `033b2af5199d2b74`. Журнал службы — с 12:34:24, 5 строк. Записано в `/tmp/molchpovtor2609/base_48.log`
и `base_classes.txt` до перемотки. Класс считается по #82: уровень·логгер·текст до первой цифры или кавычки;
для Traceback — исключение @ файл:функция.

| класс | первая встреча | раз |
|---|---|---|
| `[ERROR] splinter: ⚠️ журнал принятых работ: принятых работ без пробега` | 12:34:29 | 1 |
| `[ERROR] bridge_client: Bridge timeout` | 12:48:30 | 11 |
| `[ERROR] bridge_client: Bridge request error` | 12:51:03 | 4 |
| `[WARNING] apscheduler.scheduler: Execution of job` | 12:48:15 | 13 |
| `[WARNING] bridge_client: Bridge: echo-ретрай отменён — …` | 12:48:30 | 11 |
| `[WARNING] bridge_client: Bridge get_pending: backoff-ретрай отменён — …` | 12:48:30 | 11 |
| `[WARNING] bridge_client: Bridge: echo-слой сбоит` | 12:49:54 | 2 |
| `[WARNING] bridge_client: Bridge` | 12:51:03 | 4 |
| `[WARNING] bridge_client: Bridge get_pending: request_failed — backoff-ретрай` | 12:51:03 | 4 |
| **Traceback** | — | **0** |
| journalctl: Traceback/ERROR/Failed/«Main process exited» | — | **0** |

## 3. Применение (скрипт `apply.sh`, на сервере отсоединённо, ожидание синхронное)

Образец — `apply.sh` #81. Отличия: база пишется до FF; набор ДО не гонялся. Сравнение шло с
`/tmp/molchlive2609/before`: это тот же 48ee30a, 262 файла, красных 0. Красное шага 4 судит `judge.py` —
`rule.py` #82 п.6 по букве этого задания (п.4 ниже).

| шаг | время UTC | итог |
|---|---|---|
| 0 · сверка до | 13:47:07 | HEAD `48ee30a` на `main`, чисто, `active`, PID 2462580 |
| 0b · база | 13:47:07 | см. §2 |
| 1 · fetch, merge-base | 13:47:07 | `FETCH_HEAD` = `09587d9`, `merge-base` = `48ee30a`, поверх `69ea814 90b7df7 744619b 24a5e72 09587d9` |
| 1 · FF | 13:47:11 | HEAD `09587d9`; 11 файлов: `CLAUDE.md`, `addressee.py`, `bike_position.py`, `reply_floor.py`, `splinter.py`, `work_intent.py` и 5 тестов |
| 2 · `py_compile` | 13:47:11 | 10 файлов — rc 0 |
| 2 · полный набор ПОСЛЕ | 13:47:11–13:50:10 | **264 файла, красных 0**, 179 с; `splinter.log` за время тестов +8 строк |
| 2 · именные | — | test_molch_vetka 15/15, test_bike_position 32/32, test_floor_quiet 27/27, test_never_silent 18/18, test_album_one_pass 3/3, test_caption_zayavka 24/24, test_work_intent 26/26, test_doc_state_claims 17, test_handover 7 |
| 4 · перезапуск | 13:50:16→13:50:17 | `RESTART_RC=0`, PID **2470428**, NRestarts 0 |
| 4 · через 60 с | 13:51:17 | **ЗЕЛЕНО** (ниже) |
| 4 · через 10 мин | 14:00:17 | **ЗЕЛЕНО** (ниже) → `VERDICT=APPLIED` 14:00:18 |

## 4. После старта — классы поимённо

Окно — `splinter.log` с первой строки после перезапуска, судится со строки `STARTING` нового процесса.
Журнал службы — со строки `Started`.

| проверка | служба | строк | ERROR | WARNING | Traceback | polling | journalctl |
|---|---|---|---|---|---|---|---|
| 60 с | active, PID 2470428, NRestarts 0 | 46 | 1 | 0 | 0 | 13:50:17,982 | 5 строк systemd, красных 0 |
| 10 мин | active, PID 2470428, NRestarts 0 | 74 | 2 | 2 | 0 | да | 5 строк systemd, красных 0 |

Классы после старта — все есть в базе:

- `[ERROR] splinter: ⚠️ журнал принятых работ …` — 13:50:22,485. Это запись «подшипники» 4685, дословно:
  «принятых работ без пробега 1 (ждут 0, потеряны 1)». Вид сменился с `waiting` на `lost_no_km`, как
  предсказал #82 п.6 случай 4. Класс тот же, в базе есть. По заданию красным не является.
- `[WARNING] apscheduler.scheduler: Execution of job` — 13:55:07: `devbot_report … skipped: maximum number of
  running instances reached`. Есть в базе (×13).
- `[ERROR] bridge_client: Bridge request error` — 13:55:09: `(get_pending): HTTP 302`. Есть в базе (×4).
- `[WARNING] bridge_client: Bridge get_pending: request_failed — backoff-ретрай` — 13:55:09. Есть в базе (×4).

**Новых классов ERROR/CRITICAL/Traceback — 0. Новых WARNING — 0.**

**Правило суда `judge.py`, отличия от `rule.py`, по букве задания:**
- красное — только НОВЫЙ класс ERROR/CRITICAL/Traceback;
- новые WARNING называются и красным не считаются;
- строка журнала принятых работ не красная, даже если бы её класс оказался новым;
- судится окно с `STARTING` нового процесса: строки остановки прежнего называются, но не судятся;
- journalctl судится со строки `Started`.

Сверх класса красным считались также: служба не `active`, смена PID или NRestarts, нет `Bot polling started`
за 10 мин. Ни одно не сработало.

**Самопроверка судьи** перед запуском — на копиях #82 в `/tmp/podshipnik2609`, вывод `judge_selftest.out`:
- старт ветки #81 против 48ee30a 06:19 — ЗЕЛЕНО, rc 0;
- синтетика `NameError @ reply_floor.py:decide` — КРАСНО, 2 новых класса, rc 1;
- синтетика journalctl «Main process exited» — КРАСНО, rc 1.

## 5. Откат, доставка, следы

- **Откат одной строкой** (служба назад на 48ee30a): `cd /root/turbobaby-manager-bot && git reset --keep 48ee30a && systemctl restart splinter`.
  Мягче, без отката кода: строка `TALK_STAGE=0` в файле настроек сервера и `systemctl restart splinter` снимают
  правки 0041-74j. Правки 744619b снимает `BIKE_POSITION=0` (#81 §5, по коду `splinter._talk_stage_on` /
  `_bike_position_on`). Файл настроек этим заходом не читался.
- **Доставка main в GitHub — НЕ довезла на момент артефакта.** `ls-remote` 14:00:32 UTC: GitHub `main` =
  `48ee30a`. Состояние доставки ПК `tmp/srv_delivery/state.json`: последний замер `probed_at` 13:24:13 UTC,
  `kind=nothing`, `srv_head` 48ee30a. После FF 13:47 замеров не было. Доставка живёт в обороте демона ПК,
  а этот заход исполнялся внутри его оборота, так что повезёт следующий замер. Сверка — `ls-remote … main`
  должен дать `09587d9…`. Руками `main` GitHub не двигался (запрет).
- **Наружу из-за перезапуска:** `bot.py` `on_startup` шлёт приветствие в группу (#82 §5). Ушло ли оно в
  13:50:17 — **НЕИЗВЕСТНО**: успех журнал не пишет, история группы не читалась. Из тестов наружу ничего:
  тот же набор и те же флаги (`ORCH_TEST_MODE=1`, `PRETOOL_NOPUSH=1`), что у #81.
- **Запись «подшипники» 4685** не трогалась. На старте 13:50 первый зов `_wl_tick` перевёл её `waiting` →
  `lost_no_km` — это код, а не заход, и #82 это предсказал. `[ERROR]` на каждом старте останется, пока
  записи не дадут свою правку (#82 п.7, исход `void`).
- **Следы на сервере:** reflog `main` (FF 13:47:10Z), каталог `/tmp/molchpovtor2609/`.
- **Отступления, названные прямо:**
  1. Поиск ключа ssh был рекурсивным. Один Grep по `*.py` рабочего дерева ПК искал путь ключа, который
     зовёт `srv_delivery.py`, — это нарушает запрет «рекурсивный grep не запускать». На сервере
     рекурсивного поиска не было.
  2. Набор ДО не гонялся: его заменило сравнение с набором ДО #81 (тот же 48ee30a, 262/0).
  3. Три критерия сверх «только класс» перечислены в §4. Ни один не сработал.

## 6. Где увидеть первый живой случай

Файл — `/root/turbobaby-manager-bot/splinter.log`, строки кода по `09587d9` (сверено `git show 09587d9:…`,
вывод `p6_where.out`):

- **Молчание при согласии или плане людей** (дверь I): `→ намерение: СОГЛАСИЕ/ПЛАН (<почему>) клауза='…' …` —
  `splinter.py:9551`.
- **Молчание пола:**
  - строка лога — `→ ПОЛ ОТВЕТА: молчу по делу (<почему>) — тема=<id>`, `splinter.py:10573`;
  - причина, когда адресат другой, — «ответ на сообщение бота, но адресат другой: <кто>»
    (`reply_floor.py:387`);
  - причина при согласии — «согласие или план людей — не факт работ, ход за людьми» (`reply_floor.py:385`).
- **Правда вместо «не понял»:**
  - строка лога — `→ ПОЛ ОТВЕТА: заход промолчал (исход …) — отвечаю сам`, `splinter.py:10584`;
  - текст в теме — «…я понял как замечание о байке…», `reply_floor.py:282`.
- **Совет C:**
  - молчание — `→ совет C молчит: положение <состояние> (<почему>) · байк=<байк>`, `splinter.py:9910`;
  - отправка — `✅ подсказка C отправлена (…) · байк=…`, `splinter.py:892`;
  - замок повторов — `🔁 подсказка C ПОДАВЛЕНА (замок повторов): …`, `splinter.py:884`;
  - положение — `→ положение <байк>: <состояние> (<почему>)`, `splinter.py:9609`.
- **Поле события:** лист «события», `notes` строки события — сегмент `положение: <состояние> (<источник>)`
  (`bike_position.note`, `bike_position.py:419`).

За 10 мин проверки ни одной из этих строк в окне `cand_10m.log` нет (счёт 0). Строк со стрелкой в окне 4, и
все стартовые: привязки тема→байк и интервалы ТО. Сообщений в темах бот за окно не разбирал, живого случая
ещё не было.

SPLINTERMOLCHPOVTOR2609
