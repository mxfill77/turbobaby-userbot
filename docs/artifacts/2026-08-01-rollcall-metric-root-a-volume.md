# Перекличка + ЗАМЕР МЕТРИКИ прерываний владельца, и объём корня А для решения

**Дата:** 2026-08-01, время замера 11:35 местного (UTC+7). **Тип: только чтение.**
Код не менялся, коммитов нет, ничего не удалялось и не перезапускалось, боевых записей нет,
`.env` и конфиги не открывались, `userbot`/`moderation_bot` не тронуты. Единственный созданный
файл — этот артефакт (untracked, не коммичен).

**Источники.** `dispatch_notify.log` (все отправки владельцу), `pretool_guard.log` +
`pretool_guard.log.1` (архив ротации 05:28 — цел, след 22.07→01.08 НЕ потерян),
`pc_orchestrator.log`, живая очередь моста через `chain_plan_read.py` (read-only GET,
секреты берёт сам модуль), `git log`, снимок процессов CIM.

---

## §0. Оговорка о самом замере: первый проход соврал, и ровно корнем А

Первая выборка карточек фильтровала лог сравнением `$0 >= "2026-08-01 04:56"` — то есть
**лексикографически по всей строке**, а не по своему полю. Строки-продолжения без метки времени
(`Объект: .env`, `🔴 Хочу удали`, `Коммит: cb`) в байтовом порядке стоят ВЫШЕ цифр, поэтому
фильтр впустил их как «свежие», и получилось **5 фантомных карточек после замков**.

На деле это хвост одной записи `session_end` от 04:59, которая ЦИТИРОВАЛА поля карточек в своём
отчёте. Настоящих карточек там ноль.

Это тот же корень А, что мы метим: значение (время) бралось подстрокой, а не из своего поля.
Верные числа ниже получены якорным разбором (`^\d{4}-\d{2}-\d{2} ...` + требование, чтобы строка
была отправкой `итог…: channel=`).

---

## §1. МЕТРИКА: карточки владельцу (прерывания), а не задачи

### 1.1 ДО замков — 31.07 18:00 → 01.08 04:56 (10 ч 56 мин)

**Карточек владельцу: 17.**

| вид | штук | какие |
|---|---|---|
| гардовые в личку (🔴 / ⛔ ВЫСШАЯ ЦЕНА) | **7** | 23:54:25, 01:51:34, 02:27:57, 02:46:29, 02:55:57, 03:30:14, 03:38:07 |
| задачные «задача #N ждёт да» (🔔) | **6** | #125, #128, #136, #141, #144, #145 |
| ворота контура «авто-выкатка ОСТАНОВЛЕНА» (⛔) | **4** | 19:42:42, 19:52:08, 21:13:18, 21:13:29 |

**Разбор по происхождению — по ДОСЛОВНОЙ команде из `pretool_guard.log`:**

| происхождение | карточек | дословно |
|---|---|---|
| **проба** | **7** | `tmp/probe_kind_stamp_origin.py` (23:54 + #136), `tmp/probe_obj_read.py` (02:27), `tmp/probe_check.py` (02:55 + #144), `rm -rf tmp/srv-audit` — уборка своего же временного клона (03:30 + #145) |
| **ложное красное** (чтение выдано за опасное) | **4** | 01:51 `ls docs/artifacts/journal/ \| grep …` + #141; 02:46 `ls -a tmp/srv \| grep -iE "^\.env"` — оба лицо 6 корня А (A-29); #125 — демон принял за красную операцию ПРИМЕР ИЗ ПРОЗЫ отчёта |
| **настоящая команда/решение** | **6** | 03:38 `grep … .env` — единственное настоящее обращение к секретам; #128 (гейт заблокировал красное); 4 карточки ворот контура = ОДНО решение, повторённое четырежды |

**То есть из 17 прерываний командой агента вызвано ровно 1.** Остальные 16 — проба (7),
ложное красное (4) и служебные повторы решения о контуре (4) плюс гейт (1).

**Вечер 31.07 отдельно (18:00–24:00, 6 ч): 7 карточек** — 1 проба, 1 ложная, 5 настоящих
(#128 + 4 ворот).

### 1.2 Цена этих 17 прерываний — по живой очереди моста

Все шесть задач, ради которых владельца дёргали, сейчас **`failed`**: #125, #128, #136, #141,
#144, #145. Владелец ответил ровно один раз (`APPROVED id=141` в 02:42) — задача пере-запустилась,
**снова** упёрлась в `needs_approval` и всё равно умерла по `APPROVAL_TTL=1800`.

**17 прерываний → 1 ответ → 0 доведённых задач.**

### 1.3 ПОСЛЕ замков

| окно | длительность | задач исполнено | карточек |
|---|---|---|---|
| после замка проб `53bc936` (04:56) → 11:35 | 6 ч 39 мин | **5** (#149, #150, #151, #152, #153) | **0** |
| после всех трёх (`2ec0481` 11:12 / `24420fd` 11:14) → 11:35 | 21 мин | **1** (#153 — эта) | **0** |

**Честная граница замера.** Последняя карточка — 03:50:41, а замок проб выкатан в 04:56. Значит
тишина началась **за час до замка**, и приписывать замку весь ноль нельзя. Проверяемое
утверждение сегодня такое: **17 карточек за 11 часов до → 0 карточек за 6 ч 39 мин и 5 задач
после**. Окно «после всех трёх» (21 мин, 1 задача) замером считаться НЕ может — слишком коротко,
это только отсутствие регресса.

---

## §2. ОЧЕРЕДЬ ПК (живой мост, read-only)

| статус | задач |
|---|---|
| `new` | **0** |
| `in_progress` | **1** — #153, «ultrathink: перекличка и замер метрики» (эта задача), claim 11:28:38, возраст ~7 мин |
| `needs_approval` | **0** |

Очередь пуста: ни одной задачи не ждёт ни исполнения, ни решения владельца. Все прежние
`needs_approval` (#125/#128/#136/#141/#144/#145) уже перешли в `failed` по таймауту.

---

## §3. ПЕРЕКЛИЧКА

| процесс | PID | старт (местн.) | аптайм на 11:35 | экз. | коммит процесса |
|---|---|---|---|---|---|
| `pc_orchestrator.py` | 10344 | 01.08 04:59:18 | 6 ч 36 мин | 1 | **`afabd4e`** — назван самим демоном в строке старта |
| `userbot_listen.py` | 19284 | 31.07 16:07:18 | 19 ч 28 мин | 1 | дерево на 31.07 16:07 — **до `55aad22`** |
| `moderation_bot.py` | 10080 | 31.07 12:58:50 | 22 ч 36 мин | 1 | дерево на 31.07 12:58 — **до `55aad22`** |
| `pc_agent.py` | 12124 | 31.07 12:58:42 | 22 ч 36 мин | 1 | дерево на 31.07 12:58 |
| `rc_supervisor.py` | 12444 | 31.07 00:26:07 | 35 ч 09 мин | 1 | дерево на 31.07 00:26 |

Двойников нет ни у одного — по одному экземпляру каждого.

**Вотчдог — не резидент.** В снимке процессов его нет, и это не пропажа: вотчдог живёт как
короткий запуск `pc_orchestrator.py --watchdog` (`pc_orchestrator.py:6641`), поэтому пишет в
общий `pc_orchestrator.log` и не виден точечному снимку. Сам код это подтверждает
(`:6450`: «PID процессов САМОГО демона … без `--watchdog`-запусков — вотчдог тоже
`pc_orchestrator.py`»). Работает: строки идут ровно каждые 5 минут, последняя 11:13:01.
`schtasks` не запускался (красный класс) — вывод сделан по коду и по каденции лога.

**Замок «одна карточка — одна операция» жив, хотя демон на старом коммите.** `2ec0481` тронул
только `pretool_guard.py` + тесты (`git show --stat`), `pc_orchestrator.py` не менялся → self-update
не нужен, а гард работает хуком отдельным процессом на каждый вызов, то есть читает новый файл
уже сейчас. Демон на `afabd4e` при этом УЖЕ содержит замок проб `53bc936`.

**Отдельно: userbot и moderbot — да, всё ещё на suggest ДО `55aad22`.** Оба стартовали
31.07 (16:07 и 12:58), `55aad22` лёг в 19:31 и тронул `suggest.py` (+103 строки). Причина не в
забытом рестарте, а в воротах, и она записана в лог дословно — 4 отказа реконсиляции:
`19:42:41`, `19:52:07`, `21:13:17` ×2 — «ворота контура … применение <коммит> к
«userbot,moderbot» ОСТАНОВЛЕНО» (вердикт тренажёра КРАСНЫЙ).

---

## §4. КОРЕНЬ А — объём для решения владельца

### 4.1 Поправка к постановке: мест не 28, а 59 на ПК

«28 на ПК, 19 на VPS» — это списки **второго** захода
(`2026-08-01-two-roots-sweep.md`: `A-1…A-28` на ПК, `B-1…B-19` на VPS).
**Третий** заход (`2026-08-01-three-roots-sweep.md`) добавил `A-29…A-59` — ещё **31 место на ПК**,
которых в тех 28 нет.

| полоса | мест корня А |
|---|---|
| ПК | **59** (28 + 31) |
| VPS | **19** |
| **всего** | **78** |

Из них 5 лиц уже закрыты (`9e518b7`, `80d5cb4`, `6250c22`, `c35ac94`, `f4c3cff`) — все пять
про карточку одобрения. Лицо 6 (`A-29`) открыто и **живое**: сработало дважды за сутки.

### 4.2 Раскладка по механизму

**Закрываются ОДНИМ общим правилом — 36 мест (ПК 30, VPS 6):**

| группа | мест | ПК / VPS | чем закрывается |
|---|---|---|---|
| **Г1.** Объект / число / цель карточки вынимаются свободным `re.search` по сырой команде | **17** | 17 / 0 | `_decide_*` отдаёт структуру `Op(kind, obj, num, rollback)`; повторно грепать `cmd`/`scan`/`result` запрещено всем. Места: A-4…A-9, A-11, A-12, A-29, A-32, A-33, A-36, A-50, A-52…A-55 |
| **Г2.** «Зелёное/красное» по ПОДСТРОКЕ, без якоря командной позиции | **8** | 4 / 4 | тот же разборщик: судить сегмент по его КОМАНДЕ, а не по совпадению где угодно. A-1, A-2, A-3, A-30, B-1…B-4 |
| **Г3.** «Жив ли процесс» — подстрока в `CommandLine`/`tasklist` | **5** | 4 / 1 | один хелпер живости: PID из lock-файла + `returncode` + `IMAGENAME`. A-15, A-16, A-23, A-47, B-12 |
| **Г4.** «Чей это лог / это тест-прогон» — по `argv[0]` | **6** | 5 / 1 | один предикат тест-контекста и один источник `owner_tag`. A-17, A-18, A-28, A-35, A-46, B-17 |

Общее у всех четырёх: канон существует и лежит рядом — надо перестать его обходить. Правило
одно («у значения одно место и один текст, и то же у его смягчения»), плюс **механический
сторож-тест**, краснеющий на новый `re.search` по `cmd|scan|raw_cmd|result` вне белого списка.
Образец сторожа в репо уже есть — `test_free_signs_declare_action_check`.

**Требуют ОТДЕЛЬНОГО захода — 42 места (ПК 29, VPS 13):**

| группа | мест | ПК / VPS | почему не одним правилом |
|---|---|---|---|
| **Г5.** Статус / план / итог берутся из ПРОЗЫ модели | **29** | 18 / 11 | у каждого места СВОЙ структурный источник, на который надо переключиться: JSON думателя, `client_contour.closure()`, sentinel `<<<QUOTE>>>`, кнопка `process_callback`, `mode`/`src`/`from`, `FAIL_CODE_RE`. Общего «правильного места» нет — каждое решение своё. A-14, A-19…A-22, A-26, A-27, A-38, A-43…A-45, A-48, A-49, A-51, A-56…A-59, B-5…B-11, B-14, B-15, B-18, B-19 |
| **Г6.** Признак из вывода ЧУЖОГО бинаря или файла вне репо | **10** | 8 / 2 | контракт внешний и у каждого свой (`claude doctor`, stdout `dispatch_notify`, иглы в логе claude, `~/.ssh/config`, `tasklist`). Либо `returncode`/структура, либо фикстура на формат — решать по каждому. A-10, A-13, A-24, A-25, A-39…A-42, B-13, B-16 |
| **Г7.** Не чтение, а политика | **3** | 3 / 0 | A-31 — цена вида назначена наоборот (`delete tmp/x` высший вид с объектом, а ДОПИСЫВАНИЕ в файл секретов — обычный без объекта); A-34 — канон-доки утверждают «`deny` 0 вхождений», в коде 20 и ветка рабочая; A-37 — `PRETOOL_APPROVED_TASK` объявлен и не читается ни одной веткой |

### 4.3 Что говорит живой след

Из ~10 мест, сработавших ПО ФАКТУ, **7 лежат в группах Г1–Г4** — тех, что закрываются одним
правилом: A-29 (дважды, #141 и #144), A-30, A-15, A-17, A-46, B-1, B-2. В Г5–Г7 живых меньше
(A-45 косвенно, A-31, A-34).

**Это и есть аргумент к развилке:** 36 мест одним правилом дают 7 из 10 живых срабатываний;
42 места отдельными заходами — 3 из 10. Первый заход окупается вчетверо, но оставляет 42 места
открытыми, и Г5 (29 мест) — самая большая группа каталога.

---

## §5. Границы

* Ничего не менялось, не коммитилось, не удалялось, не перезапускалось; `userbot`/`moderbot`
  не тронуты; `.env` и конфиги не открывались; `schtasks` не запускался.
* Очередь снята живым read-only GET через `chain_plan_read.py` (штатный клиент, секреты внутри
  модуля, свой лог-файл — боевой `pc_orchestrator.log` не засорён).
* «Коммит процесса» для `userbot`/`moderbot`/`pc_agent`/`rc_supervisor` — не самоотчёт процесса
  (они его не печатают), а вывод из времени старта против времени коммитов. Для демона — дословно
  из его строки старта.
* Вотчдог как `--watchdog`-запуск — вывод из кода и каденции лога, а не из списка задач
  Планировщика (красный класс, не запускался).
* Числа §1 — по `dispatch_notify.log` якорным разбором; окно «после всех трёх замков» (21 мин,
  1 задача) замером не является и так и назван.
