68s OTBORNASH 2009

# Прав ли наш отбор, снимающий находки внешнего контура, и сколько стоит полоса на сборку пакетов

Задание Штаба `68s.2009` (док `shtab_task_68s.2009` папки мозга), полоса ПК. Вид работы —
**РАЗВЕДКА**: читал, считал, написал этот файл. Боевого кода не тронуто ни строкой, порогов не
менял, процессов не запускал и не останавливал, ничего не удалял, `.env` и конфиги не читаны,
в реестр находок не писано ни строки, наружу не ушло ни одного пакета и ни одной пробы, клиентам
не писано. Клиентских реплик, переписки менеджеров, ключей и содержимого конфигов здесь нет.

Временное место названо явно: `tmp/vyborka_nahodok/` (три счётных скрипта только-чтение +
`vyborka20.json` с выборкой). Всё остаётся на месте.

Замер: **20.09.2026**, дерево на `ffeec9f5`, полоса ПК.

---

## 0. Признак сделанности

Первым действием открыт адрес результата. Файла `docs/artifacts/2026-09-20-68s-OTBORNASH-2009.md`
на диске **не было** (`ls` → `No such file or directory`); среди 15 файлов `docs/artifacts` за
20.09 метки `68s OTBORNASH 2009` нет ни в одном. Вопросы п.3 и п.4 не отвечены нигде.
**Работа с нуля.**

---

## Короткий ответ

| вопрос задания | ответ числом |
|---|---|
| прав ли наш отбор | **НЕТ на четверть. Снятых зря — 5 из 20 = 25%, это ВЫШЕ пятой части. Значит речь про НАШ ОТБОР, а не про канал** |
| доля неизвестных | **14 из 20 = 70%** — и это второй приговор отбору: он снимает вслепую то, о чём сам не может судить |
| что именно снимает | отбор показа ступени D: **188 заявок из 248 (75.8%) немедленно не показаны** |
| цена полосы за 7 суток | **19 пакетов собрано и отправлено, ответов получено 0 (ноль)**. Занятость витка демона сборкой — **≥ 3562 с за окно ≈ 509 с/сут = 0.59% суток** |
| пакет называет выпавшее? | **ДА**, тремя разными местами (`review_pack.py:912`, `:923-932`, `:1002-1010`) |

---

## Премиса Штаба и что с ней сегодня

Гипотеза Штаба (срез 05.09): находок 170, снято фильтром 133, ждут владельца 25, до задачи или
правила дошло 0.

Сегодня числа другие, а **форма та же**: доля снятого держится — было 133/170 = 78.2%, стало
**188/248 = 75.8%**. «Дошло ноль» сегодня не подтверждается буквально: **16 находок дошли до
отдельной разведки** (см. п.1). Правил из находок по-прежнему **0**.

---

## 1. ЖИВЫЕ ЧИСЛА С АДРЕСОМ

**Где лежит реестр — двумя путями, и это два разных реестра, их нельзя складывать:**

| что | путь | что в нём |
|---|---|---|
| реестр ЗАЯВОК (ступень B) | `review_intake_state.json` | `claims` **243** (ряд очереди/лоток заведён) + `held` **5** (снято ступенью B) = **248** |
| реестр ПОКАЗА (ступень D) | `review_audit_state.json` | `shown` **60** (показано владельцу немедленно), `held` **32** (бутстрап), `digests` **20** суток |
| сами находки текстом | `docs/review_inbox/` (293 файла) | разобрано живым `review_intake.parse_answer`+`split_findings`: **543 находки-источника**, после склейки по смыслу — **247 заявок** |
| заявки, ушедшие мимо ряда | `docs/zayavki_lotok/` | **19 файлов** + `.gitkeep` (9 из них числятся в `claims` строковым `queue_id`) |

**Сколько находок всего.** Два законных ответа, и путать их нельзя: **543 находки-источника**
(строка ответа канала) либо **248 заявок** после склейки дублей по смыслу
(`review_intake.merge`, порог `MERGE_JACCARD = 0.145`). Дальше считаю по ЗАЯВКАМ — отбор судит
именно их.

**Сколько снято отбором.** Отборов на полосе ДВА, и снимает почти всё второй:

| отбор | адрес | снял |
|---|---|---|
| ступень B, `select()` | `review_intake.py:691` | **5 из 248** (все — «бутстрап: ответ лежал в лотке до рождения ступени B») |
| ступень D, `select_immediate()` | `review_audit.py:197` | **188 из 248 = 75.8%** |

Разбор 188 снятых ступенью D по ПРИЧИНЕ (пересчитано живой `review_audit.weight`):

| причина | штук | адрес правила |
|---|---|---|
| премиса `unknown` — ЖЁСТКИЕ ворота | **62** | `review_audit.py:159,173` (`alive` обязателен) |
| премиса `stale` — те же ворота | **32** | там же |
| премиса жива, но вес 0 < `HIGH_MIN=2` (не стоп-вид и один источник) | **66** | `review_audit.py:95,147-173` |
| высокая, но не показана (бюджет/бутстрап/очередь) | **23** | `IMMEDIATE_BUDGET = 3`, `review_audit.py:102` |
| премисы нет вовсе (held ступени B) | **5** | `review_intake_run.py:543` |

**Потолок показа упирается КАЖДЫЕ сутки:** `shown` по суткам — ровно **3, 3, 3 … 3** все 20 суток
подряд (01.09–20.09). Это не совпадение, это `IMMEDIATE_BUDGET = 3`, и очередь высоких находок
сегодня длиннее, чем дней.

**Сколько дошло до задачи или правила.** До ОТДЕЛЬНОЙ РАЗВЕДКИ дошло **16** находок — по числу
артефактов `docs/artifacts/*разведка-zayavka-*.md` (01.09 ×3, 02.09 ×4, 03.09, 04.09, 05.09 ×2,
06.09, 07.09 ×2, 08.09 ×2). До **ПРАВИЛА — 0**: ни одного правила, заведённого по находке, в
дереве нет. Три из шестнадцати ключей (`fb36e990a3b5`, `e417ff43967b`, `67e601b19f33`) попали и в
мою выборку снятых — то есть **«снято отбором показа» НЕ равно «никем не прочитано»**: ряд
очереди `needs_approval` с карточкой владельцу заявка получает всё равно
(`review_intake_run.py:405-434`). Эту часть премисы Штаба живой замер **не подтверждает**.

**За какое окно.** Реестр заявок: `bootstrap_at` = **2026-09-01T11:08:08Z**, последняя постановка
— **2026-09-12**. Находки в лотке: **01.09–11.09** (11 суток, поле `отправлено:`). Показ: **01.09
–20.09** (20 суток). Дальше 12.09 новых заявок нет — потому что новых находок нет вовсе (п.5).

---

## 2. ПРАВИЛО ОТБОРА ВЫБОРКИ — объявлено ДО чтения текстов и не менялось

Объявлено в черновике доклада `tmp/pc_report/task131-22688-1789891638809-131.md` ДО того, как
был прочитан хоть один текст находки (чёрный по белому в файле, второй правкой захода; первой
правкой шёл только п.0). Читались до объявления ТОЛЬКО счётчики реестра — это п.1 задания,
который стоит раньше п.2.

1. **Корпус** — заявки реестра, которых отбор показа НЕ показал: ключ (и ни один из
   `claim_keys`) не встречается в `review_audit_state.json → shown`. Это ровно те **188**.
2. **Порядок** детерминированный: по `placed_at`/`seen_at` возрастанию, при равенстве — по ключу
   лексикографически.
3. **Выборка систематическая**, а не «первые двадцать»: шаг = 188 // 20 = **9**, берутся индексы
   0, 9, 18, … — двадцать штук, растянутые по всему окну.
4. Бюджет не дал бы двадцати — читаю подряд с начала и называю сколько. **Не потребовалось:
   прочитаны все 20.**
5. Текст находки — дословно из лотка по ключу (`review_intake.read_records` + `merge`), выписан в
   `tmp/vyborka_nahodok/vyborka20.json`.

Правило после чтения не менялось ни в одном пункте.

---

## 3. ДВАДЦАТЬ СНЯТЫХ ПОИМЁННО — три исхода

Критерий объявлен вместе с правилом и применён ко всем двадцати одинаково: **«снята зря» ставлю
ТОЛЬКО когда утверждение находки о живом коде проверяемо И проверка сошлась**, и тогда называю
адрес проверки. Проверяемо и НЕ сошлось → «снята верно». Проверять нечем (оценка формы доклада,
адрес съехал, файла нет в дереве, сама находка говорит «неизвестно») → **НЕИЗВЕСТНО**, а не
«верно».

| # | ключ | вид | премиса | исход | чем проверено (адрес) |
|---|---|---|---|---|---|
| 1 | `03ada24ab21c` | НЕ ДЕЛАТЬ | — | **НЕИЗВЕСТНО** | совет не делать работу; живого адреса не назвала, якорь «те же экзаменационные пары» — проза |
| 2 | `fb36e990a3b5` | УПРОЩАЕМО | alive | **снята верно** | утверждала два источника интервала; ручки `CONTOUR_DIGEST_SEC` в дереве **нет вовсе**, источник один — `contour_digest.py:133` (`INTERVAL_SEC = 14400`), раннер читает его же (`contour_digest_run.py:587,649,713`) |
| 3 | `8156f86e500e` | УПРОЩАЕМО | stale | **НЕИЗВЕСТНО** | о таблицах артефакта, не о коде |
| 4 | `b61b08494e67` | НЕ ДЕЛАТЬ | alive | **НЕИЗВЕСТНО** | сама пишет «полный дифф `9041f29`, состав тестов … неизвестны» |
| 5 | `64f88b24bacc` | УПРОЩАЕМО | unknown | **НЕИЗВЕСТНО** | расхождение sha в `bundle.json`; файла `bundle.json` в git-дереве нет — проверить нечем |
| 6 | `ec715429f609` | ПЕРЕУСЛОЖНЕНО | alive | **СНЯТА ЗРЯ** | «три ручных режима запуска для read-only сводки» живы: `contour_digest_run.py:709-711` (`--status`, `--dry`, `--send`); размер сегодня **746 строк** против названных 400 — упрёк не протух, а вырос |
| 7 | `ae06048f370b` | УПРОЩАЕМО | alive | **СНЯТА ЗРЯ** | необязательный `rx` в ОБЩЕМ `recon_auto.markers` жив: `recon_auto.py:572` (`def markers(items, kind="task", rx=None)`), `:579` («ЧУЖАЯ РЕГУЛЯРКА МАРКЕРА … ящик заданий Штаба»), `:595` — ровно то, на что находка указывала |
| 8 | `b034b9f441d7` | НЕ ДЕЛАТЬ | stale | **НЕИЗВЕСТНО** | о поведении прошлого захода (коммит ради read-only), не о коде |
| 9 | `1223c5d6fb61` | УПРОЩАЕМО | alive | **НЕИЗВЕСТНО** | адрес `pc_orchestrator.py:8902` съехал: файл сегодня **14060 строк**, на 8902 — тело `_read` внутри другой функции |
| 10 | `f226718b28ce` | УПРОЩАЕМО | alive | **НЕИЗВЕСТНО** | о форме журнала прогресса прошлого захода |
| 11 | `84ebd4f0a80c` | ПЕРЕУСЛОЖНЕНО | unknown | **НЕИЗВЕСТНО** | якорей нет вовсе (`anchors` → пусто); о форме доказательства |
| 12 | `4854eada8dee` | УПРОЩАЕМО | alive | **НЕИЗВЕСТНО** | о числе чтений реестра в прошлом заходе |
| 13 | `e0bab63d459f` | УПРОЩАЕМО | stale | **НЕИЗВЕСТНО** | о форме правки самоописания |
| 14 | `ddd6e82cf611` | НЕ ДЕЛАТЬ | alive | **СНЯТА ЗРЯ** | «пакет — лишь самосообщение исполнителя с обрезанным `result_head`»: замер собственной полосы 10.09 — **в 76 пакетах окна 0 источников `.py`**, 68.8% строк — наша проза из `docs/artifacts`, 31.2% — расписка `.json` с нашим же `result_head` (`docs/artifacts/2026-09-10-ВНЕШНИЙ-КОНТУР-ЧИСЛА-ПАКЕТ-МОЛЧАНИЕ-1009.md`) |
| 15 | `898107dead27` | УПРОЩАЕМО | alive | **СНЯТА ЗРЯ** | ручка жива с той самой веткой тихого выключения: `pc_orchestrator.py:3087` — `AGENT_STALE_SEC = int(os.getenv("PC_AGENT_STALE_SEC", "5400") …)   # 90 мин; 0 → ветка мертва` |
| 16 | `41db0e3b259f` | УПРОЩАЕМО | alive | **НЕИЗВЕСТНО** | сама пишет «содержание пяти адресов и регресса неизвестно»; и это «не упрощать», а не находка |
| 17 | `bf024c946d16` | НЕ ДЕЛАТЬ | stale | **НЕИЗВЕСТНО** | о методике прошлого замера |
| 18 | `a61782a29fc6` | УПРОЩАЕМО | alive | **СНЯТА ЗРЯ** | «ошибочный отбор области по `id(node)`» жив в тесте: `test_shtab_box.py:2507` — `if (inside and id(node) not in picked) or (outside and id(node) in picked)` |
| 19 | `e417ff43967b` | УПРОЩАЕМО | alive | **НЕИЗВЕСТНО** | о форме доклада; по ней при этом БЫЛА отдельная разведка 07.09 |
| 20 | `67e601b19f33` | УПРОЩАЕМО | alive | **НЕИЗВЕСТНО** | о форме ответа по задаче 154; отдельная разведка 08.09 |

---

## 4. ЧИСЛОМ

| доля | число | |
|---|---|---|
| **снятых зря** | **5 / 20 = 0.25 = 25%** | #6, #7, #14, #15, #18 |
| **неизвестных** | **14 / 20 = 0.70 = 70%** | |
| снятых верно | 1 / 20 = 0.05 = 5% | #2 |

**0.25 > 0.20. Говорю прямо, как требует п.4: ЭТО ПРО НАШ ОТБОР, А НЕ ПРО КАНАЛ.** Четверть
прочитанной выборки — находки, чьё утверждение сошлось с живым кодом сегодня, через две-три
недели после снятия. Отбор снял их не потому, что они неверны, а потому, что у них **один
источник и не стоп-вид** (вес 0 при пороге 2) либо **премиса не «жива»**.

Второе число хуже первого, и его нельзя проглотить: **70% НЕИЗВЕСТНО**. Отбор ступени D судит
не содержание, а три уже посчитанных поля (`review_audit.py:147-173`), и по построению не может
отличить «находка про код» от «находка про форму чужого доклада». В выборке таких 14 из 20 — то
есть **канал в основном критикует не код, а наши отчёты о коде**, и наш отбор об этом не знает
ничего. Доля «снято зря» 25% измерена на той трети выборки, где вообще было что проверять: из
6 проверяемых находок сошлись **5**.

Что именно ломает отбор — названо числом в п.1: ворота премисы снимают **94** заявки (62 unknown
+ 32 stale), а вес < 2 — ещё **66**. Самая крупная одиночная причина — «жива, но один источник и
не стоп-вид»: при живом канале один (codex отвечает, manus молчит) **второго источника не
бывает никогда**, и `AGREEMENT_WEIGHT` мёртв по построению. Значит «высокой» может стать только
стоп-вид, то есть ровно треть анкеты.

---

## 5. ЦЕНА ПОЛОСЫ ЗА СЕМЕРО СУТОК (14.09–20.09 UTC)

| что | число | адрес |
|---|---|---|
| пакетов собрано и отправлено | **19** (14.09 ×4, 15.09 ×4, 16.09 ×4, 17.09 ×2, 18.09 ×2, 19.09 ×2, 20.09 ×1) | `docs/review_outbox`, дата в имени; сходится с 19 строками `ревью-контур A: повод …` в `pc_orchestrator.log` |
| ответов получено | **0 (НОЛЬ)** | ответные файлы окна: codex `refused` ×19, manus `unknown` ×20; знаков ответа 0 у обоих |
| находок получено | **0** | поэтому в реестре заявок пусто после 12.09 |
| цена по вердиктам | manus 8 `request`; codex — 0 токенов (исход `refused`, заход не состоялся) | метка `цена захода:` в файлах лотка |
| занятость витка демона сборкой | **≥ 3562 с за окно ≈ 509 с/сут = 0.59% суток** | `pc_orchestrator.log`, строки вотчдога «идёт объявленная ступень «ступень A: сборка пакета внешнему ревьюеру» (NNNс из 3900с)»; суточные максимумы 352 / 662 / 637 / 1001 / 564 / 346 с |

**Три оговорки, без которых эти числа врут.**

1. Число занятости — **НИЖНЯЯ ГРАНИЦА, а не измеренная длительность**: вотчдог печатает возраст
   ступени только в момент своей проверки, и заход, уложившийся между проверками, в сумму не
   попал вовсе. Настоящая цена больше; насколько — **НЕИЗВЕСТНО**.
2. Сборка идёт **СИНХРОННО внутри витка**: пока ступень A собирает пакет, демон не делает ничего
   другого (`watchdog: продукту 366с … демон работает, не трогаю`). Это не фон, это простой.
3. **Отдача полосы за окно — ровно ноль.** Девятнадцать пакетов, ноль ответов, ноль находок,
   ноль правил. Платим сборкой, получаем тишину; codex за окно перешёл в `refused` ×19, manus
   молчит с 03.09 (шестнадцатые сутки).

---

## 6. ПАКЕТ, РЕЖУЩИЙСЯ ПО ДОРОГЕ: называет ли он выпавшее внутри себя

**ДА.** Тремя разными местами, и все три — в теле самого пакета:

| место | что печатает | адрес |
|---|---|---|
| раздел «пропуски ВНУТРИ источника» | `- \`%s\` — показано %s из %s строк, не вошло %d: %s` — **число выпавших строк поимённо по источнику** | `review_pack.py:912` |
| раздел `## ОПУЩЕНО (omitted)` | каждый опущенный источник: путь, роль, полоса, причина; пусто → «ничего не опущено» | `review_pack.py:923-932` |
| строка-индекс в журнал | `… источников=%d, опущено=%d, знаков=%d/%d, sha=%s` | `review_pack.py:1002-1010` |

Сверх того обязательный источник, не влезший в потолок `REVIEW_MAX_CHARS = 15000`, **не режется
молча**: пакет становится `blocked` со словами «Обязательный источник не влез в потолок либо не
доехал. Пакет НЕ урезан» (`review_pack.py:577,806`), а раздел «ПРОПУСКИ» стои́т ДО «опущено»
намеренно (`review_pack.py:897`).

Это единственный пункт задания, где ответ в нашу пользу: **тут наша вина не подтвердилась.**

---

## 7. ГРАНИЦА ВСЛУХ

**Прочитано по адресу (число взято из файла или кода):** 543 находки-источника и 247 заявок
(живой `review_intake` по `docs/review_inbox`); 243 `claims` + 5 `held` и даты 01.09/12.09
(`review_intake_state.json`); 60 `shown`, 32 `held`, 20 сводок и «3 в сутки ×20»
(`review_audit_state.json`); 19 файлов лотка заявок (`docs/zayavki_lotok`); 16 артефактов
разведок (`docs/artifacts`); 19 пакетов (`docs/review_outbox`); 19 `refused` + 20 `unknown` и
0 знаков ответа (метки файлов лотка); все пороги и веса (`review_audit.py:88,94,95,102`,
`review_intake.py:127`); все адреса проверок п.3; все три места печати выпавшего (`review_pack.py`).

**Вывод, а не чтение:** 188 снятых (вычтено: реестр минус `shown` по набору ключей);
75.8% и 25%/70%/5% (деление); разбивка 188 по причинам (пересчитана мной живой `review_audit.weight`
на полях реестра — сама ступень такой разбивки нигде не хранит); «`AGREEMENT_WEIGHT` мёртв по
построению» (вывод из «второй канал молчит», а не запись); 509 с/сут и 0.59% суток (деление
нижней границы 3562 с на 7 суток); соответствие 19 пакетов 19 заходам ступени A (сопоставление
двух источников).

**Чего заход НЕ делал:**
- не отправлял наружу ни одного пакета и не дёргал ни одного внешнего канала;
- не писал в реестр находок, не менял `review_*_state.json`, не ставил и не снимал ни одной
  заявки, не трогал очередь и мост;
- не правил ни строки боевого кода, ни одного порога (`HIGH_MIN`, `IMMEDIATE_BUDGET`,
  `MERGE_JACCARD` — как были), не запускал `review_intake_run --place` ни разу;
- не перепроверял премису заново живым `build()` (полный проход по дереву на 248 заявок — цена
  выше бюджета): исходы `alive/stale/unknown` в п.3 взяты из реестра, а не переизмерены сегодня;
- не читал остальные 168 снятых находок — прочитаны ровно 20 по объявленному правилу;
- не судил, ПОЧЕМУ codex перешёл в `refused` ×19 за окно — это отдельный предмет;
- не трогал `.env`, конфиги, боевую базу, денежные пути, процессы; ничего не удалял.

**Чего этот замер НЕ доказывает.** Доля «снято зря» 25% получена на выборке 20 из 188 по
объявленному правилу — это оценка по выборке, а не перепись. Доверительного интервала не считал.
При 5 сошедшихся из 6 проверяемых устойчивость вывода «выше пятой части» держится на том, что
хотя бы 4 из 5 адресов проверки живы, — все пять проверены поимённо и названы в п.3.

## 8. БЮДЖЕТ

Единица одна, потолок 2700 с, порог 0.7. Двадцать находок **влезли**: прочитаны все 20 из 20,
урезать выборку не потребовалось. Арифметика захода: п.0–1 (поиск реестра, структура, живые
числа) ≈ 0.35 бюджета, п.2–3 (выборка и двадцать проверок по живому коду) ≈ 0.35, п.5–6 (цена и
пакет) ≈ 0.15, запись артефакта и журнала ≈ 0.15. Порог 0.7 пройден на п.3 — дальше шли только
пункты, которые уже опирались на снятые числа.
