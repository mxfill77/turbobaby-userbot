# Снимки кейсов 14–17 сняты: экзамен владельца больше не упирается после тринадцатого

KARTOCHKI ostatka nabora 1709

**Дата:** 17.09.2026, 14:05–14:20 по Пхукету · **Полоса:** ПК · **Задание:** ящик Штаба, ключ
`63-g-kartochki-ostatka.1709` (док `shtab_task_63-g-kartochki-ostatka.1709`, file id `1mesyTwE-0jDfXw3Vureaxx2okIT3tJJL`)
**Коммит снимков:** **`3da9707`** (4 файла `exam_shots/case-1{4,5,6,7}@d1e904c.json`, +170). Кода заход не правил ни строкой.
**Временное место, названо явно:** `tmp/krug_63g_0917/` (вне git). Сырые ответы критика легли туда, куда их кладёт
сам механизм: `tmp/exam_critic/case-1{4..7}-20260917T07*.txt`. Черновик доклада — `tmp/pc_report/`. Ничего не удалялось.

---

## ⚠ Нарушение запрета, сказанное первым: заморозка кейса 17 позвала отправку владельцу

Запрет задания: «в группу и любой чат не уходит ни одного сообщения». **Проверка (пункты 3–4) его соблюла:
6 попыток, все пойманы ловушкой.** Но **сама заморозка кейса 17** (`exam_show.py --freeze --case 17`, пункт 2)
прошла мимо ловушки:

- кейс 17 — третий исход «период через границу сезонов». Путь головы `suggest.py:5678` зовёт
  `season_gate.note_for(hints, lang=lang)` **без отправителя**, дальше `raise_card` (`season_gate.py:220`) берёт
  боевой `dispatch_notify.deliver` (`season_gate.py:213–217`);
- замок пробы `dispatch_notify.probe_verdict` (`dispatch_notify.py:169–191`) судит по точке входа, а
  `exam_show.py` лежит в корне репозитория — **«боевая точка входа»**, и `_api` отправку пропускает;
- в логе заморозки (`tmp/krug_63g_0917/freeze-17.log`) стоит строка `season_gate: ⛔ НЕ СЧИТАЮ: период через
  границу сезонов | • модель: XMAX | • даты: 2026-09-26 → 2026-10-05 (9 сут) | …`. Строки
  `карточка владельцу НЕ ушла` нет, то есть `send(text)` исключения не бросил.

**Попытка отправки — 1, это факт кода и лога. Дошла ли карточка — НЕИЗВЕСТНО.** `deliver` выбирает адрес
сам (карточка без кнопок → тема 328, фолбэки 1160 и личка). Запись о доставке ушла не в боевой
`dispatch_notify.log` (последняя запись там — 13:34, до захода), а в тестовый лог:
`trainer_run` на импорте ставит `TURBOBABY_TEST_LOGS=1`, и `log_setup.log_path` уводит его в
`%TEMP%\turbobaby_TESTING_dispatch_notify.log`. Этот файл лежит вне проекта, и я его **не читал**. Как проверить:
открыть этот файл и найти строку от 17.09 около 14:16 (`в тему 328 ok` / `критич. в инбокс ok`), либо
посмотреть тему 328 около 14:16.

**Почему не предусмотрел.** У кейсов 10–13 (12.09) третьего исхода не было: 17 — первый снимок, путь которого
идёт через `season_gate`. Ни клиентам, ни в группу «Тренеровка» эта ветка не пишет: адресат только владелец.
**Цена закрытия — 1 заход:** `freeze` ставит на время круга ловушку на `season_gate._default_sender` и
`noprice_gate._default_sender` (у второго та же форма, `noprice_gate.py:196–222`) и кладёт текст карточки в
снимок. Сделать это нужно до следующей пересборки любого кейса с третьим исходом или без цены. Не сделано:
заданием не просили, а клиентский путь не правим.

---

## FACT — живые числа

| что доказано | число / факт |
|---|---|
| коммит снимков | `3da9707` в `git log`, 4 файла, только `exam_shots/` |
| снимки на диске | `case-14@d1e904c.json` · `case-15@d1e904c.json` · `case-16@d1e904c.json` · `case-17@d1e904c.json` |
| цена единиц (стенное время процесса `--freeze`) | **216 · 68 · 91 · 206 с = 581 с** |
| подсказки критика | **4 · 3 · 4 · 3 = 14** из потолка 16, исход `hints` у всех четырёх (ждали 89.8 · 45.6 · 58.7 · 66.6 с при пределе 257) |
| версия правил в снимке | **строка** `"7d19b1db352e8f76"` у всех четырёх |
| кнопок на карточке | **7 · 6 · 7 · 6**, раскладка `[1,4,2]` / `[1,3,2]` |
| живой замок агента на каждую карточку | **ДА** ×4: pid 13740 разбирает все колбэки |
| переход по хвосту (копия дерева) | 13 → 14 → 15 → 16 → 17 → `🏁 ЭКЗАМЕН ПРОЙДЕН: кейс 17 был последним. Верных 4 из 17.` |
| ловушка проверки | **6** попыток = 6 показов, которые код обязан был сделать; сеть **0**, процессы **0**, утечек из проверки **0** |
| попытка отправки из заморозки | **1** (кейс 17, `season_gate`), дошла ли — **НЕИЗВЕСТНО** (см. раздел выше) |
| боевые стол / таблица уроков / журнал | `exam_session.json` 133 байт, mtime `1789211402` (12.09) до и после; `lesson_store.tsv` 7223 байт, mtime `1789127894` (11.09) до и после; `exam_verdicts.tsv` **не существует** до и после |
| прежние снимки | `git status exam_shots` после коммита пуст: 16 прежних файлов не тронуты |

---

## 0. Признак сделанности

Первым действием открыт `docs/artifacts/` за 17.09: 18 файлов других шагов, файла со словами адреса нет.
Работа начата с нуля.

## Премисы — проверены первым действием

| премиса | исход |
|---|---|
| **П1** у кейсов 14–17 снимка нет, после 13 переход отказывает словами об этом | **ВЕРНА.** `ls exam_shots/` показал 16 файлов, среди них ни одного кейса 14–17. Отказ живьём, на копии дерева без новых снимков: `➡️ Следующий кейс 14 НЕ ушёл: ⛔ сохранённого черновика кейса 14 нет (…\do\exam_shots\case-14.json). Сперва «--freeze --case 14» — показывать свежую генерацию запрещено` (§4). |
| **П2** кейсов 17, снимки у 13, подсказки у 1, 8, 10, 12, 13 | **ВЕРНА.** `trainer_cases.json` — 17 кейсов. Снимки у кейсов 1–13 (16 файлов, у кейса 1 их четыре). Непустой `hints` у `case-1@37c290f`, `case-8@4b6160e`, `case-10/12/13@aa7b1c4`, у остальных `[]`. |
| **П3** с `d7bfa76` версия правил в новом снимке — строкой | **ВЕРНА.** Все четыре новых снимка: `"rules": "7d19b1db352e8f76"`. Тип `str` в копии дерева тоже (§3). |

---

## 1. Замер на кейсе 14 и арифметика

`exam_show.py --freeze --case 14`: старт **14:06:18** (`1789628778`), конец **14:09:54** (`1789628994`), **216 с**, rc 0.
Критик — 89.8 с (поле `critic.waited`). Голова вместе с импортами, `placeholders()` и метками примеров —
разность, **≤ 126 с**.

**Арифметика до сборки остальных трёх:**
- по цене единицы: 4 × 216 = **864 с** < 1890 с (0.32 потолка);
- по времени захода: к концу кейса 14 прошло **293 с** от создания задачи (14:05:01, штамп `1789628701324` в имени
  черновика). Худшая замеренная полосой единица — 303 с (кейс 11, 12.09). 293 + 3 × 303 = **1202 с** < 1890;
- худшая единица по построению: голова 60 с (`suggest.CLI_TIMEOUT`) + критик 257 с + около 40 с обвязки ≈ 357 с.
  293 + 3 × 357 = 1364 с < 1890.

Порог не достигнут ни в одной оценке, поэтому заход продолжен.

## 2. Четыре снимка

Собраны готовым механизмом, последовательно, на HEAD `d1e904c`. Логи — `tmp/krug_63g_0917/freeze-1{4..7}.log`,
время — `time-14.txt`, `time-rest.txt`.

| кейс | имя | с | критик: исход, ждали | подсказок | сырой ответ критика | `built_at` |
|---|---|---|---|---|---|---|
| 14 | EN: прайс-интент «все модели и цены» | **216** | hints, 89.8 с | **4** | 1202 знака (`…/case-14-20260917T070954Z.txt`) | 07:09:54Z |
| 15 | EN: только про депозит | **68** | hints, 45.6 с | **3** | 639 знаков | 07:11:15Z |
| 16 | EN: модель + даты + гео-пин Раваи | **91** | hints, 58.7 с | **4** | 901 знак | 07:12:46Z |
| 17 | период через границу сезонов — третий исход | **206** | hints, 66.6 с | **3** | 806 знаков | 07:16:12Z |

Подсказки есть у всех четырёх, объяснять отсутствие не нужно ни по одному. Корпус у всех `99e6b78177a1d533`.

**Что сказали логи сверх «собрано»:**
- кейс 16: `ЦЕНА РАСХОЖДЕНИЕ: модель=NMAX | даты=2026-10-06..2026-10-11 | дверь=1685 | файл=1535 | разница=-150 ฿ … клиенту ушло число ДВЕРИ, ответ НЕ остановлен`.
  Это строка лога прибора `door_price.measure` (`door_price.py:229`), отправки у него нет. Клиенту ничего не ушло:
  клиента в заморозке нет. Число в черновике — дверное, 337 ฿/день;
- кейс 17: окно через границу собралось (26.09 → 05.10, P1 → P2), голова вызвана. Черновик цену не называет,
  зовёт человека. Отправка — в разделе «Нарушение» выше.

**Смысл подсказок** (они станут уроками при тапе владельца):
- 14 — цены только дословно из блока ЦЕНА; не утверждать сезон и срок действия цен; не писать «от X»;
  сверять категории моделей с парком;
- 15 — здороваться в первом сообщении; не подавать жёсткое правило депозита как гибкое; не смешивать наличные и перевод;
- 16 — цена дословно из блока; не подтверждать, что байк свободен, без проверки; не называть процент скидки;
  отвечать целиком на языке клиента;
- 17 — не выдумывать обоснований; не звать «Бронируем?» без цены и локации; не обещать сроков ответа менеджера.

Критик в 14 и 16 называет «каноничный прайс» (449 ฿ у NMAX 155) и сравнивает с ним дверное число 337 ฿.
Кто из них прав, заход не судил: цен не трогали, прайс не открывали. Это показание критика, а не находка захода.

## 3. Карточка собирается у всех четырёх

Карточки собраны **в копии дерева** `tmp/krug_63g_0917/run-20260917-141756/posle/` тем же `card_text`/`markup`,
которые зовёт `show`. Замок агента **живой**: боевой `pc_agent.lock` и `pc_agent.py`, только чтение. Полные
тексты — `report.json` там же, сокращённые — `tmp/krug_63g_0917/karty.txt`.

| кейс | ключ черновика (`shot_key`) | версия правил | кнопок | колбэки | длина текста (знаков / ед. UTF-16) | замок агента |
|---|---|---|---|---|---|---|
| 14 | `d1e904c@2026-09-17T07:09:54Z` | `str` `7d19b1db352e8f76` | **7** `[1,4,2]` | `exam:ok:14`, `exam:h1..h4:14`, `exam:own:14`, `exam:go:14` | 4006 / 4013 | ДА |
| 15 | `d1e904c@2026-09-17T07:11:15Z` | `str` `7d19b1db352e8f76` | **6** `[1,3,2]` | `exam:ok:15`, `exam:h1..h3:15`, `exam:own:15`, `exam:go:15` | 1876 / 1882 | ДА |
| 16 | `d1e904c@2026-09-17T07:12:46Z` | `str` `7d19b1db352e8f76` | **7** `[1,4,2]` | `exam:ok:16`, `exam:h1..h4:16`, `exam:own:16`, `exam:go:16` | 2307 / 2314 | ДА |
| 17 | `d1e904c@2026-09-17T07:16:12Z` | `str` `7d19b1db352e8f76` | **6** `[1,3,2]` | `exam:ok:17`, `exam:h1..h3:17`, `exam:own:17`, `exam:go:17` | 2236 / 2243 | ДА |

Замок агента, дословно (одинаково ×4, меняется только число колбэков):
`живой агент (pid 13740) стартовал позже правки «pc_agent.py» И разбирает ВСЕ её колбэки, числом 7 (разбор «^exam:(ok|no|own|go|h[1-4]):([0-9]{1,3})$») — кнопки он поймёт`.

**Текст карточки, сокращённо (кейс 14):**
```
🎓 ЭКЗАМЕН · кейс 14 из 17 · пройдено 0 — EN: прайс-интент «все модели и цены» — зеркало кейса 2
Показан ЗАФИКСИРОВАННЫЙ ответ от 2026-09-17T07:09:54Z (коммит d1e904c, корпус 99e6b78177a1d533). Голова сейчас НЕ звалась.
❓ КЛИЕНТ: What makes and models of bikes do you offer? … Is a deposit required?
🤖 БОТ: Hello, thank you for reaching out to TurboBaby! 🤝 Our fleet includes … [сетка 11 моделей] …
🔎 ЧТО УЛУЧШИТЬ — подсказки критика (того же, что стои́т за кнопкой «🎓 Обучить»):
1. Цены по моделям не совпадают с каноничным прайсом (…)
    → правило: называй клиенту цены строго дословно из блока ЦЕНА/прайс, не пересчитывай и не изменяй их самостоятельно.
2. … 3. … 4. …
Кнопки: «✅ Верно» — кейс зачтён и сразу придёт следующий. Номера 1…4 — ТУМБЛЕРЫ …
```
У кейсов 15–17 та же форма: шапка «кейс N из 17», подсказки 1…3 / 1…4, легенда кнопок.

**Мина рядом с краем:** карточка кейса 14 — **4013 единиц UTF-16** при лимите текста сообщения Telegram
4096. Запас **83**. Прошла бы она живьём, ловушка не доказывает: она длину не меряет. Будь текст длиннее
на 84 единицы, показ этого кейса упёрся бы в отказ Telegram. Сегодня **по счёту не упирается**, живьём не
проверено. Длины карточек прежних 13 кейсов заход не мерил.

## 4. Переход по хвосту — копия журнала и стола

**Как устроено** (`tmp/krug_63g_0917/hvost.py`). Два вида копии дерева: `do` — снимки без кейсов 14–17
(как было до захода), `posle` — снимки с диска сейчас. В копию легли `exam_show.py`, его зависимости,
`trainer_cases.json`, **копия стола** `exam_session.json` и снимки. **Копии журнала нет, потому что нет боевого:**
`exam_verdicts.tsv` не существует, копия начинается с пустого журнала. Пути стола, журнала и снимков модуль
выводит из своего `__file__`, и все они лежат в копии: ни один путь в скрипте не переназначен. Модули
подгружены из копии (`modules_from` в отчёте: `exam_show`, `lesson_store`, `proc_identity`, `io_utf8` — все
`…\run-20260917-141756\<вид>\`). Право — подставленный предикат (`vladelec_0917`): переход от права не зависит,
живое право мерили 63-e и 63-f. Отправитель — ловушка, сеть — ловушка (`socket.connect` / `create_connection`).
Сценарий: показ 13 → вердикт 13 «верно» → (сам) 14 → вердикт «неверно» → 15 «верно» → 16 «верно» → 17 «верно».

**`posle` — прошёл до конца, дословно:**

| шаг | ответ двери (сокращено) | стол после шага |
|---|---|---|
| показ 13 | `✅ кейс 13 из 17 показан в ловушка, message_id=ловушка#2. Ждём тапа.` | `13`, `aa7b1c4@2026-09-12T11:25:51Z` |
| вердикт 13 ok | `📝 Записано КАНДИДАТОМ №1: кейс 13 из 17 — «верно» … правила 7d19b1db352e8f76.` · `➡️ Следующий кейс 14: ✅ кейс 14 из 17 показан в ловушка, message_id=ловушка#3. Ждём тапа.` | `14`, `d1e904c@2026-09-17T07:09:54Z` |
| вердикт 14 no | `📝 Записано КАНДИДАТОМ №2: кейс 14 из 17 — «неверно» · Коммит d1e904c …` · `➡️ Следующий кейс 15: ✅ … ловушка#4` | `15`, `d1e904c@…07:11:15Z` |
| вердикт 15 ok | `📝 … №3 …` · `➡️ Следующий кейс 16: ✅ … ловушка#5` | `16`, `d1e904c@…07:12:46Z` |
| вердикт 16 ok | `📝 … №4 …` · `➡️ Следующий кейс 17: ✅ кейс 17 из 17 показан в ловушка, message_id=ловушка#6. Ждём тапа.` | `17`, `d1e904c@…07:16:12Z` |
| вердикт 17 ok | `📝 Записано КАНДИДАТОМ №5: кейс 17 из 17 — «верно» …` · **`🏁 ЭКЗАМЕН ПРОЙДЕН: кейс 17 был последним. Верных 4 из 17. Следующего кейса нет — просить его неоткуда.`** | `17` (не сдвинут) |

Журнал копии — 5 строк. У всех графа «правила» = `7d19b1db352e8f76`, **ни одного `НЕ ЗАПИСАНО`**. Строки 2–5
ссылаются на `exam_shots\case-1{4..7}@d1e904c.json`. `passed_count` = 4. Шапки пойманных карточек несут
счёт: `пройдено 0 → 1 → 1 → 2 → 3`.

**`do` (контрфакт, снимков 14–17 нет) — встал ровно там, где назвал 63-e:**
`➡️ Следующий кейс 14 НЕ ушёл: ⛔ сохранённого черновика кейса 14 нет (D:\turbobaby-bot\tmp\krug_63g_0917\run-20260917-141756\do\exam_shots\case-14.json). Сперва «--freeze --case 14» — показывать свежую генерацию запрещено`.
Вердикт 13 при этом записан (№1), стол остался на 13.

**Нигде не встало.** Оговорка: живой первый показ и живой тап этим не доказаны. Их звено — агент и Telegram,
а проверка шла на копии и с ловушкой, как и велено.

## 5. Счёт ловушки

| вид | попыток | каких |
|---|---|---|
| `do` | **1** | показ 13 (после вердикта 13 показ 14 отказал ДО отправителя) |
| `posle` | **5** | показ 13 + «следующий сам» 14, 15, 16, 17. После 17 — итог, попытки нет |
| **итого проверки** | **6** | каждая — показ, который код обязан был сделать; адрес у всех `-5193185299` |
| сеть / процессы в проверке | **0 / 0** | — |
| **утечек из проверки** | **0** | отправитель заменён по построению, сеть закрыта уровнем ниже |
| **вне проверки** | **1 попытка** | заморозка кейса 17 → `season_gate` → `dispatch_notify.deliver`, дошла ли — НЕИЗВЕСТНО (раздел «Нарушение») |

## 6. Бюджет

Единица — один черновик. Потолок захода 2700 с, доля 0.7 — 1890 с. Задача создана в **14:05:01**
(`1789628701`). Цена четырёх единиц — **581 с** (0.215 потолка). Проверка карточек и перехода — меньше секунды
счёта и около 3 мин чтения кода и сборки. Коммит `3da9707` — 14:18. Запись артефакта начата в **14:18:51**
(`1789629531`): **830 с = 0.307 потолка**. Итог захода вместе с коммитом артефакта и журналом — в отчёте захода.

## 7. Гард — что было на самом деле

Один отказ, по моей опечатке. В составную команду чтения логов попал вызов несуществующего
`tmp/krug_63g_0917/svodka.py`, и гард ответил:
`⛔ ОТКАЗ: высшая цена, а ОБЪЕКТ НЕ НАЗВАН — Хочу выполнить python с боевой записью … Гард смог сказать только: скрипт не прочитан`.
Обхода не искал: та же команда без этого вызова прошла. Файлы гарда не открывались, не правились и не
коммитились (`pretool_guard.py` / `test_pretool_guard.py` в дереве изменены не мной и в коммит не вошли).

## 8. Чего заход НЕ сделал

- Показа владельцу не было: `--show`, `--reach`, `dispatch_notify` из проверки не звались. Процессов не трогал,
  агента не перезапускал, детей не поднимал.
- Боевые стол, журнал вердиктов, таблица уроков не менялись (FACT). Прежние 16 снимков не тронуты.
- `.env` и конфиги не читал, значений секретов не печатал. Цен, прайса, депозитов не менял, в таблицы не
  писал. Лист «Календарь бронирования» и брони своей командой не открывал. **Оговорка:** путь головы при заморозке
  читает прайс мостом (read-only GET, `trainer_run.py:28`; в записке снимка 14 — `PARK PRICE LIST from the
  Calendar`). Это тот же путь, что у всех 13 прежних снимков, и без него черновик не собирается.
- Лог `%TEMP%\turbobaby_TESTING_dispatch_notify.log` не читал (вне проекта), поэтому доставка карточки кейса 17 —
  НЕИЗВЕСТНО.
- Утечку заморозки не чинил, названа её цена (1 заход).

## 9. Временное место `tmp/krug_63g_0917/` (ничего не удалялось)

| файл | что |
|---|---|
| `freeze-14.log` … `freeze-17.log`, `time-14.txt`, `time-rest.txt` | вывод и стенное время четырёх заморозок |
| `hvost.py`, `hvost-console.txt` | проверка карточек и перехода на копиях дерева (ловушка отправителя и сети) |
| `run-20260917-141756/do/`, `…/posle/`, `…/report.json` | копии дерева (стол, журнал, снимки) и полный отчёт с текстами карточек |
| `chitat.py`, `chitat.txt`, `karty.py`, `karty.txt` | чтение отчёта: шаги, журнал, ловушка; карточки сокращённо с длинами |
| `boevye-do.txt` | размер и mtime боевого стола и таблицы уроков до проверки |
