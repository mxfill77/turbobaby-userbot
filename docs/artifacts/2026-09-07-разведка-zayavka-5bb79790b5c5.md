# Разведка заявки 5bb79790b5c5: жива ли посылка находки внешнего канала про ворота и `pricing_advisor.py`

**Ключ адреса результата, отдельной строкой — по требованию задания:**

2026-09-07-разведка-zayavka-5bb79790b5c5

**Дата:** 07.09.2026 08:53 местного · **полоса:** ПК · **вид работы:** РАЗВЕДКА (только чтение,
счёт и этот файл) · **HEAD на момент замера:** `fe07d00`
**Предмет:** заявка очереди #151, вид «УПРОЩАЕМО», источников 1 —
[`docs/review_inbox/2026-09-07-2026-09-07-chain-pc-2026-09-07-149-codex.md`](../review_inbox/2026-09-07-2026-09-07-chain-pc-2026-09-07-149-codex.md)
**Пакет, на который канал отвечал:** [`docs/review_outbox/2026-09-07-chain-pc-2026-09-07-149.md`](../review_outbox/2026-09-07-chain-pc-2026-09-07-149.md)

> Текст канала — МНЕНИЕ, а не задание. Ни одной строки оттуда не исполнено; боевой код не
> тронут (`git status --short -- pc_orchestrator.py client_contour.py pricing_advisor.py
> test_pc_orchestrator.py test_client_contour.py` → пусто). Разведка кончилась этим файлом.

---

## ИСХОД ОДНОЙ СТРОКОЙ

**Повод НЕ ложный: посылка жива у всех трёх пунктов находки — но обещанная п.1 экономия
измеряется в НОЛЬ снятых деталей, а п.3 меряет цену не там.**

| пункт находки | посылка сегодня | что показал замер |
|---|---|---|
| 1 · «оставить один вычислитель: полное замыкание + именованные исключения» | **жива** — `pricing_advisor.py` и правда ловится картой и отсутствует в замыкании | «именованных исключений» потребуется **ровно 1**, а карта из кода всё равно не уходит: она МАРШРУТ РЕСТАРТА, а не слагаемое ворот. Обмен 1-в-1, не упрощение |
| 2 · «не включать модули ящика Штаба» | **жива** — 3 файла ящика вошли в множество и доказанной связи с ответом клиенту у них нет | исключение стоит **3 именованных исключения**, то есть тот же рукописный список; 4-й файл ящика был клиентским и ДО правки |
| 3 · «переусложнено: 37 подпроцессов» | **посылка о ЦЕНЕ неверна** | прибор лежит в `tmp/contour_hole_fix_0907/break_all.py`, вне git (`git ls-files \| grep -c break_all` → **0**): одноразовая разведка, постоянной цены ноль строк |

---

## 1. ЗАМЕРЫ — число и команда рядом

Прибор: `tmp/razvedka_5bb79790b5c5/probe.py` (только чтение и счёт; ничего не пишет, процессов не
трогает, боевых файлов не касается — назван по правилу «место для временного назвать явно»).

**Команда:**

```
venv\Scripts\python.exe tmp\razvedka_5bb79790b5c5\probe.py
```

**Вывод дословно:**

```
ЗАМЕР 1 · замыкание СО СРЕЗОМ (дефолт client_contour.closure): ok=True файлов=41 данных=15
ЗАМЕР 1 · замыкание БЕЗ СРЕЗА (_client_closure, ворота выкатки): ok=True файлов=78 данных=50
ЗАМЕР 1 · дельта: файлов +37, данных +35
ЗАМЕР 2 · отслеживаемых файлов в корне репо: 244
ЗАМЕР 2 · КАРТА ловит, ВЫЧИСЛЕНИЕ не видит (сколько «именованных исключений» потребовало бы предложение находки): 1 — pricing_advisor.py
ЗАМЕР 2 · ВЫЧИСЛЕНИЕ видит, КАРТА не относит к ботам: 74
ЗАМЕР 3 · shtab_box* в множестве БЕЗ среза: 4 (shtab_box.py, shtab_box_accept.py, shtab_box_run.py, shtab_box_signals.py)
ЗАМЕР 3 · shtab_box* в множестве СО срезом: 1 (shtab_box.py)
ЗАМЕР 4 · _procs_for_file('pricing_advisor.py') = ['moderbot', 'userbot']
ЗАМЕР 4 · 'pricing_advisor.py' в замыкании ворот: False
ЗАМЕР 4 · _client_paths(['pricing_advisor.py']) = ['pricing_advisor.py']
```

**Второй замер — живыми голденами, а не пробой:**

```
venv/Scripts/python.exe -m unittest -v test_pc_orchestrator.TestDyraVorotZakryta0709
→ Ran 8 tests in 5.553s · OK
```

В том числе `test_karta_ne_podmnozhestvo_i_eto_vidno` — зелёный. Его собственная шапка говорит,
что красный там означал бы «файл наконец импортировали» или «карту сняли». Ни того, ни другого
на 07.09 не случилось: **посылка находки жива сегодня, а не только вчера.**

**Третий замер — кто ещё читает семантику СО СРЕЗОМ (это declared «неизвестно» самой находки):**

```
git grep -n "client_contour.closure" -- "*.py"
```

Боевых потребителей дефолта (то есть замыкания СО срезом) — **4**:

| адрес | вызов | что судит |
|---|---|---|
| `client_contour.py:293` (`mentions`) | `closure(repo)` | **ворота ВХОДА** — дев-ТЗ находки ревизора |
| `deploy_voice.py:294` | `closure(repo or REPO)` | голос выкатки |
| `recon_auto_run.py:247` | `closure(root)` | авто-реконсиляция |
| `zayavki_pc_run.py:284` | `closure(repo)` | заявки полосы |

---

## 2. Что именно в коде отвечает посылке (адреса, не пересказ)

| посылка находки | код полосы | адрес |
|---|---|---|
| «карта» | `_FILE_PROCESS_RULES` — 12 правил, из них 5 **префиксных** (`startswith` userbot/suggest/pricing/delivery/booking) и 7 по точному имени | `pc_orchestrator.py:6876-6889` |
| «специальный режим замыкания без среза» | **одна константа** `_CLIENT_CUT = ()` плюс функция `_client_closure` на 8 строк; дефолт `client_contour.closure` НЕ тронут | `pc_orchestrator.py:7534`, `:7537-7544` |
| «отдельные проверки двух направлений расхождения» | две ветки `log.info` внутри `_bots_hit`: `gap` (вычисление шире карты) и `extra` (карта шире вычисления) | `pc_orchestrator.py:7101-7116` |
| «сохраняет защиту `pricing_advisor.py`» | объединение в воротах: `if hit or (_procs_for_file(p) & {"userbot","moderbot"})` | `pc_orchestrator.py:7564` |

---

## 3. Чего находка НЕ учитывает — по пунктам, с числом

### 3.1 Карта из кода не уходит: она маршрут рестарта, а не слагаемое ворот

Находка предлагает «не держать одновременно карту». Но карта зовётся **из пяти неворотных мест**
(`git grep -n "_procs_for_file(\|_classify_changed(" -- "*.py"`, боевые строки):

| адрес | зачем | к воротам отношения |
|---|---|---|
| `pc_orchestrator.py:6953` | признак «пора помечать агента» | нет |
| `pc_orchestrator.py:7440` | запрет авто-рестарта поверх грязного дерева | нет |
| `pc_orchestrator.py:7866` | **МАРШРУТ рестарта** (комментарий в коде: «карта, не тронута») | нет |
| `pc_orchestrator.py:8407` | **МАРШРУТ рестарта** | нет |
| `pc_orchestrator.py:7987` | поимённая пометка процессов | нет |

Снять карту из объединения ворот можно — **из кода она при этом не исчезнет ни строкой**. То есть
экономия п.1 равна «минус одно слагаемое в одном выражении», а не «минус механизм».

### 3.2 Цена «именованных исключений» измерена: она равна 1, и это обмен, а не упрощение

ЗАМЕР 2: карта ловит и вычисление не видит **ровно один** файл на 244 отслеживаемых в корне —
`pricing_advisor.py`. Значит предложенный список именованных исключений сегодня короткий.

Но это ровно тот механизм, чей отказ и чинила правка 07.09: комментарий кода называет её **пятым
случаем одного класса**, где первые четыре чинились **добавлением имени** (`delivery.py` dae330a,
`trainer.py`, `trainer_log.py`, агент 02.09 — `pc_orchestrator.py:7028-7031`). Замена
предиката на имя меняет и покрытие: `startswith("pricing")` держит **класс** файлов, включая
ещё не созданные, а список имён держит только написанное.

### 3.3 Про модули ящика Штаба (п.2) — посылка верна, но критерий у ворот другой

ЗАМЕР 3: `shtab_box_accept.py`, `shtab_box_run.py`, `shtab_box_signals.py` действительно появились
в множестве только со снятием среза, и таблица разрядов артефакта дня сама ставит им **«OK (импорт
демона выжил)»** — то есть доказанной связи с ответом клиенту у них нет. **Канал прочитал текст
верно.**

Чего в тексте пакета не было видно:
1. **Они попали в множество не решением, а фактом обхода** — они в import-замыкании живых входных
   точек. Их исключение — это 3 именованных исключения, то есть возврат рукописного списка через
   вторую дверь.
2. **Критерий ворот — «что процесс грузит в память», а не «что доказано меняет ответ»**
   (`pc_orchestrator.py:7064-7068`). Проба `break_all.py` меряла ОДНО ребро — «сломай модуль →
   переживёт ли `import pc_orchestrator`». Для лениво грузимых модулей это не тот же вопрос, что
   «может ли неверная правка изменить ответ на ВЫЗОВЕ».
3. **Четвёртый файл ящика — `shtab_box.py` — лежал в множестве и СО срезом**, то есть был
   клиентским до правки 07.09. Разговор идёт о 3 файлах, а не о «модулях ящика» вообще.

### 3.4 Про 37 подпроцессов (п.3) — посылка о цене неверна

`break_all.py` лежит в `tmp/contour_hole_fix_0907/` и в git его нет: `git ls-files | grep -c
break_all` → **0**. Не в git и приборы `probe_sets.py` / `probe_corpus.py`. Это ОДНОРАЗОВАЯ
разведка, оплаченная один раз; в боевом коде от неё нет ни строки, и «удешевлять» нечего.
Сам канал это и оговорил: «Сравнение с альтернативным, более компактным набором в пакете
неизвестно» — компактного набора в пакет действительно не клали.

### 3.5 Что в находке верно и неснимаемо

«Возвращающий также причину попадания файла» — причина СЕГОДНЯ производится
(`log.info` на обоих направлениях, `pc_orchestrator.py:7106` и `:7115`), но живёт **в логе, а не в
возврате**: `_bots_hit` отдаёт два списка путей без причин. Кто читает вердикт ворот, причину
получает только грепом лога. Это законная претензия и она замером не снимается.

---

## 4. ПРАВКА, КОТОРУЮ Я НАЗЫВАЮ СЛОВАМИ И НЕ ДЕЛАЮ

Решение за владельцем; разведка боевого кода не правит.

> `_bots_hit` (`pc_orchestrator.py:7080`) мог бы возвращать третьим значением словарь
> «файл → причина попадания» (`замыкание` / `карта` / `нет графа — запасная карта`), а обе
> ветки `log.info` печатали бы его же. Это НЕ снимает ни карту, ни `_CLIENT_CUT`, и множество
> ворот не меняет ни на один файл — меняется только диагностируемость вердикта. Цена: три
> вызывающих места (`:7869`, `:7996`, `:8412`) и голдены `TestDyraVorotZakryta0709`.

**Чего делать НЕ предлагаю и почему:** снимать карту из объединения ворот (§3.1 — она из кода не
уходит, а покрытие сужается с класса до имени) и исключать 3 файла ящика (§3.3 — 3 именованных
исключения ради непроверенного «на вызове» выигрыша).

---

## 5. ЧТО ПОВОД ЗАКРЫВАЕТ, А ЧТО ОСТАЁТСЯ

**ЗАКРЫВАЕТ (проверено числом):**
- «Нужна ли прежняя семантика замыкания со срезом другим потребителям — **неизвестно**» —
  собственное «неизвестно» находки. Ответ: **нужна, потребителей дефолта 4** (§1, третий замер).
- Посылка п.1 жива **сегодня**, а не только на день пакета: голдены зелёные 8/8, ЗАМЕР 4.
- Посылка п.3 о **цене** — снята: прибор вне git, постоянной цены 0 строк (§3.4).
- Цена предложенного списка исключений — **измерена**: 1 имя для п.1, 3 имени для п.2 (§3.2, §3.3).

**ОСТАЁТСЯ (не сделано этой разведкой):**
- Перенос причины попадания из лога в возврат — правка названа словами, не сделана (§4).
- Решение владельца по обоим предложениям находки — не моё.

**НЕИЗВЕСТНО (третий исход, законный):**
- Может ли неверная правка `shtab_box_accept/run/signals.py` изменить ответ клиенту **на вызове**
  (не на импорте). Прибор такой пробы не делали ни разу; «OK» в таблице разрядов отвечает на
  другой вопрос (§3.3 п.2).
- Корпусная цена «более компактного набора контрфактов» из п.3 — альтернативного набора не
  собирали, сравнивать не с чем (сам канал это и оговорил).
- Появится ли у `pricing_advisor.py` живой импортёр. Сегодня импортёр один и он тестовый
  (`git grep -n "import pricing_advisor" -- "*.py"` → 1 строка, `test_pricing_advisor.py:26`).

---

## 6. КРАСНОГО НЕ ВСТРЕТИЛОСЬ

Ни одной операции класса delete/env/kill/schtasks/sqlite/network/outside разведке не
потребовалось. Всё сделанное — чтение, счёт и этот файл.
