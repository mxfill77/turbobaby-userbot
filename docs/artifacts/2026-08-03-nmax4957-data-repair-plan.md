# NMAX 155 GREEN-B 4957 — что в живых данных СЕЙЧАС и план правки (ФАЗА 1, ничего не применено)

Дата разведки: 03.08.2026, ПК-контур, только чтение через мост (`fleet`, `read_events` — GET;
`service_list`, `service_pending_list`, `state_get`, `state_list` — POST-чтения из
`_IDEMPOTENT_POST_ACTIONS`). Ни одной мутации не делалось. Скрипты разведки:
`tmp_nmax4957_recon_20260803_q3/` (untracked, секретов не читают — окружение поднимает
`pc_orchestrator`, как у демона).

Факт владельца, принятый ДАННОСТЬЮ (не проверялся у механика и в источниках):
**29.07 при пробеге 36982 сделаны ЧЕТЫРЕ работы — моторное масло, масло редуктора, передние
тормозные колодки, задние тормозные колодки. Текущий пробег ≈37000. 38982 — ошибка OCR,
отклонена механиком в тот же день.**

---

## 1. Лист1 «Байки» (экшен `fleet`) — дословно

Каноничное имя в листе: **`NMAX 155CC GREEN-B PHUKET 4957`** (не «NMAX 155 GREEN-B 4957»).

```json
{
 "number": 2,
 "status": "ДОМА",
 "name": "NMAX 155CC GREEN-B PHUKET 4957",
 "purchase_date": "2022-09-05 00:00",
 "year": 2020,
 "cost": 73000,
 "total_revenue": 151163,
 "mileage": 12600,
 "oil_last_km": 37000,
 "gear_last_km": 36982,
 "abs_last_km": 12600,
 "airfilter_last_km": 0,
 "current_rental": null
}
```

Раскладка по колонкам (карта колонок — `knowledge_base.md` §«2 регистра», зеркало
`set_fleet_oil`/`set_fleet_service`):

| Колонка | Поле ответа | ЖИВОЕ значение | Что это |
|---|---|---|---|
| **H** | `mileage` | **12600** | «пробег при покупке (стартовый)», НЕ текущий |
| **I** (= I16) | `oil_last_km` | **37000** | ТО Oil — пробег последней замены моторного масла |
| **J** | `gear_last_km` | **36982** | ТО Gear — редуктор |
| K | `abs_last_km` | 12600 | ABS (равен H → фактически «не делалось», засеяно стартовым) |
| L | `airfilter_last_km` | 0 | возд. фильтр — не делалось |

**Про номер строки.** Экшен `fleet` номер строки листа НЕ отдаёт, и поле `number` строкой не
является (по парку встречаются дубли `41`, `37`, `34` и девять нулей). Косвенно: байк —
**14-я запись списка** (позиция 13 при счёте с нуля) из 38, что сходится с названным владельцем
адресом **I16**, если данные листа начинаются со строки 3. Штатной проверки адреса через мост нет.

## 2. Лист «события» (экшен `read_events`, limit=50 → отдал 7 строк, newest-first) — дословно

| # | recorded_at | msg_date | тип | mileage | notes |
|---|---|---|---|---|---|
| 1 | 2026-07-31T07:36:31.864Z | Fri Jul 31 2026 00:00:00 GMT+0700 | `other` | (пусто) | обнаружена неисправность, напоминание делать фото резины при инспекции, заказ покрышек: 6 задних ND (xmax300, nmax155) + 2 передних |
| 2 | 2026-07-31T07:31:06.809Z | Fri Jul 31 2026 00:00:00 GMT+0700 | `other` | **37000** | текущий пробег 37000 |
| 3 | 2026-07-29T09:32:19.528Z | Wed Jul 29 2026 00:00:00 GMT+0700 | `photo` | (пусто) | Приборная панель Yamaha, общий пробег **38982** км, индикатор топлива показывает полный бак, время 4:16, экран с пылью и разводами \| грязный |
| 4 | 2026-07-29T09:32:15.576Z | Wed Jul 29 2026 00:00:00 GMT+0700 | `repair` | **38982** | **замена передних тормозных колодок — 38982 км** |
| 5 | 2026-07-29T09:24:34.536Z | Wed Jul 29 2026 00:00:00 GMT+0700 | `photo` | (пусто) | На фото изношенные тормозные колодки байка, лежат рядом с колесом. Колодки сильно стёрты, требуют замены. Шина выглядит в хорошем состоянии. \| Использованные тормозные колодки байка, сильно изношены, |
| 6 | 2026-07-29T09:07:55.176Z | Wed Jul 29 2026 00:00:00 GMT+0700 | `photo` | (пусто) | Приборная панель Yamaha, индикатор топлива полный, общий пробег (ODO) **38982** км, время 4:16, экран в пыли и разводах \| грязный |
| 7 | 2026-07-29T09:07:15.727Z | Wed Jul 29 2026 00:00:00 GMT+0700 | `photo` | (пусто) | Скутер серо-красного цвета припаркован перед мотомастерской, вид сзади. Видно номерной знак, заднее колесо и багажник. Байк в целом чистый, лёгкие следы эксплуатации. Приборная панель не видна. \| гряз |

**ЧЕГО В «событиях» НЕТ:** ни одной строки про **задние** тормозные колодки; ни одной строки
про моторное масло и редуктор (у них свои колонки — так и задумано, `_classify_work`).

**msg_id штатным чтением НЕ ОТДАЁТСЯ.** `read_events` возвращает ровно пять полей
(`recorded_at`, `msg_date`, `event_type`, `mileage`, `notes`); ключа сообщения нет ни в нём,
ни в `service_list`, ни в `service_pending_list`. Ожидаемый ключ строки #4 по конвенции писателя
(`splinter._write_info_works`: `info:{номер}:{стем-работы}:{км}`, стем «колодки» из `_WORK_STEMS`)
— **`info:4957:колодки:38982`**, но это РЕКОНСТРУКЦИЯ, а не прочитанное значение. Проверяется
только пробным `delete_event` (он возвращает `{ok, deleted, rows}`) — это уже запись.

## 3. Лист «обслуживание» (ТО-трекер, экшен `service_list`, 13 строк по парку) — дословно, 2 строки по 4957

```json
{"updated_at":"2026-07-31T07:31:12.732Z","bike":"NMAX 155CC GREEN-B PHUKET 4957","topic_id":79,
 "service_type":"oil","current_km":37000,"last_service_km":30800,"interval_km":4000,
 "next_km":34800,"status":"overdue","pinned_msg_id":"","last_reminded_at":17853161900047500,"note":""}

{"updated_at":"2026-07-31T07:31:23.001Z","bike":"NMAX 155CC GREEN-B PHUKET 4957","topic_id":79,
 "service_type":"gear","current_km":37000,"last_service_km":36982,"interval_km":4000,
 "next_km":40982,"status":"ok","pinned_msg_id":"","last_reminded_at":"","note":""}
```

**Расхождение двух регистров по маслу:** Лист1 кол.I = **37000**, а трекер держит
`last_service_km` = **30800** и потому считает масло **overdue** (срок был 34800). Оба числа
не равны факту владельца (36982).

## 4. Лист «то_заявки» (`service_pending_list`) — дословно

Открытых по 4957 — **нет** (всего открытых по парку 2). Закрытая по 4957 — одна:

```json
{"created_at":"2026-07-29T09:24:51.515Z","updated_at":"2026-07-29T09:53:12.670Z",
 "chat_id":-1002751134848,"topic_id":79,"bike":"NMAX 155 GREEN-B 4957",
 "declared":"gear","done":"gear","status":"закрыто","odometer":36982,
 "last_reminded_at":"","note":"written=gear by @Pleummmm","_row":3}
```

Читается так: через двухфазный сервисный поток 29.07 провели **только редуктор** (одометр 36982,
запись подтвердил @Pleummmm). Масло и обе пары колодок этим путём не проходили.

## 5. Состояние байка (`state_get` / `state_list`)

`state_get(bike="NMAX 155CC GREEN-B PHUKET 4957")` → `{"ok":false,"error":"not_found"}`;
`state_list` → **0 строк по всему парку**. Слой «состояние_байка» пуст — по 4957 там ничего нет.

## 6. Карточка Инфо

**Живую карточку с ПК прочитать нечем** — её собирает и рендерит VPS (`splinter._build_bike_card`
→ Telegram). Ниже — РЕКОНСТРУКЦИЯ по коду карточки (клон VPS-репо на ПК, срез 29.07) на
прочитанных выше живых данных, а не снятый экран:

- источники карточки: `find_bike` (Лист1) + `service_list` + `read_events(limit=6)` + `service_pending_*`;
- **текущий пробег карточки = max** (Лист1 H=12600; `current_km` трекера=37000, 37000; км инфо-работ
  из событий=38982) → **38982**. То есть в шапке карточки стоит ошибка OCR;
- Плановое ТО (интервалы: масло 4000, редуктор 4000, ABS 10000, фильтр 20000 — скутер):
  - Масло — ✅ ещё **2018** км (срок 41000) ← от I=37000;
  - Редуктор — ✅ ещё **2000** км (срок 40982) ← от J=36982;
  - ABS — ⚠️ просрочено на **16382** км ← от K=12600;
  - Возд. фильтр — ❗ не делалось ← L=0;
- аренда: «дома (в офисе)» (status ДОМА);
- «В работе» — пусто; секции «История» нет (у закрытой заявки `done=gear` — колоночный вид, из
  истории карточки он вычищается);
- «Обслужено дополнительно на пробегах:» → **38982 км (на текущем пробеге) — замена передних
  тормозных колодок** (единственная инфо-работа, распознанная из событий).

---

## 7. ПЛАН ПРАВКИ — ячейка за ячейкой (НЕ ПРИМЕНЁН)

### 7.1. Что чинит ошибку OCR 38982

| # | Объект (адрес) | Старое | Новое | Чем делается | Откат |
|---|---|---|---|---|---|
| **O1** | «события», строка 29.07 09:32:15 — поле `mileage` | `38982` | `36982` | **Штатного экшена правки строки НЕТ.** Либо двухходовка `delete_event(msg_id=…)` + `add_event(...)` (см. O2), либо **РУКАМИ** в листе «события» Bot Data | вписать обратно `38982` |
| **O2** | она же — поле `notes` | `замена передних тормозных колодок — 38982 км` | `замена передних тормозных колодок — 36982 км` | тот же путь, что O1. Штатно: `delete_event(msg_id="info:4957:колодки:38982", max=1)` → затем `add_event(bike="NMAX 155CC GREEN-B PHUKET 4957", event_type="repair", mileage="36982", notes="замена передних тормозных колодок — 36982 км", msg_id="info:4957:колодки-перед:36982", group="Обслуживание / тема 79", msg_date=29.07.2026)`. **Риск:** ключ строки не прочитан (см. §2); если `deleted=0` — ключ другой, тогда РУКАМИ | `delete_event(msg_id="info:4957:колодки-перед:36982")` + `add_event(... mileage="38982", notes="замена передних тормозных колодок — 38982 км", msg_id="info:4957:колодки:38982")`; при ручной правке — вернуть текст с 38982 |
| **O3** | «события», фото-строки 29.07 09:07:55 и 09:32:19 (`38982` внутри описания фото) | текст с `38982` | **НЕ ТРОГАТЬ** | — (описание фото = улика; в расчёты не входит: `mileage` пуст, под шаблон «работа — км» не подходит) | — |
| **O4** | Карточка Инфо, «пробег» в шапке | `38982` | `37000` | **отдельной правки не требует** — величина производная: после O1/O2 max берётся из 12600 / 37000 / 36982 | откатывается сам вместе с O1/O2 |

### 7.2. Что дописывает 4 работы 29.07 на 36982

| # | Объект (адрес) | Старое | Новое | Чем делается | Откат |
|---|---|---|---|---|---|
| **R1** (моторное масло) | **Лист1, ячейка I16** (`oil_last_km`) | `37000` | `36982` | **РУКАМИ.** Штатный `set_fleet_oil(4957, 36982, confirmed=true)` **ОТКАЖЕТ**: 36982 < 37000 → ошибка `oil_decreasing`, записи не будет. Ветки «понизить» у экшена нет вовсе | вписать в I16 `37000` |
| **R2** (масло редуктора) | **Лист1, кол. J** (`gear_last_km`) | `36982` | `36982` — **УЖЕ ВЕРНО** | ничего не делаем | — |
| **R3** (передние колодки) | «события» | строка есть, км неверный | см. **O1/O2** | см. O1/O2 | см. O1/O2 |
| **R4** (задние колодки) | «события» — строки **НЕТ** | — | новая строка: `repair`, `mileage=36982`, `notes="замена задних тормозных колодок — 36982 км"` | **ШТАТНО:** `add_event(bike="NMAX 155CC GREEN-B PHUKET 4957", event_type="repair", mileage="36982", notes="замена задних тормозных колодок — 36982 км", msg_id="info:4957:колодки-зад:36982", group="Обслуживание / тема 79", msg_date=29.07.2026)`. **Ключи задать РАЗНЫЕ вручную:** конвенционный стем у передних и задних колодок один («колодки»), на одном км ключи совпали бы и вторая строка была бы съедена дедупом `botMsgExists_` | `delete_event(msg_id="info:4957:колодки-зад:36982")` |
| **R5** (масло — второй регистр) | «обслуживание», строка `service_type=oil`: `last_service_km` | `30800` (→ `next_km` 34800, `status` overdue) | `36982` (→ `next_km` 40982, `status` ok) | **ШТАТНО:** `service_upsert(bike="NMAX 155CC GREEN-B PHUKET 4957", service_type="oil", last_service_km=36982, interval_km=4000)` (рост — сторож убывания не мешает; `current_km` НЕ передавать, чтобы не трогать одометр) | `service_upsert(..., last_service_km=30800, interval_km=4000)`; если Bridge не примет понижение — РУКАМИ в листе «обслуживание» (вернуть 30800 / 34800 / overdue) |
| **R6** (редуктор — второй регистр) | «обслуживание», строка `service_type=gear` | `36982` / 40982 / ok | **УЖЕ ВЕРНО** | ничего не делаем | — |

Пока R5 не сделан, трекер продолжает считать масло просроченным (30800 + 4000 = 34800 против
текущих 37000) и слать напоминания о просрочке — ложные.

### 7.3. Колонка H — отдельно (не относится ни к OCR, ни к 4 работам)

| # | Объект | Старое | Новое | Чем делается | Откат |
|---|---|---|---|---|---|
| **H1** | **Лист1, кол. H** (`mileage`) | `12600` | **правку НЕ предлагаю** | Штатного экшена записи в H нет **ни одного**: `set_fleet_oil`/`set_fleet_service` её сознательно не трогают. Любое изменение — только РУКАМИ | вписать `12600` |

Причина: по карте колонок H — «пробег при покупке (стартовый)», а не текущий; 12600 согласуется
с K=12600 (ABS засеян стартовым). Если владелец решит держать в H **текущий** пробег (37000) —
это ручная правка и смена смысла колонки; на карточку она не повлияет (там `max`, а 37000 уже
приходит из трекера).

### 7.4. Что решить владельцу до применения

1. **I16: 37000 → 36982** — правка ручная (штатный путь закрыт сторожем `oil_decreasing`).
   Разница 18 км; если 37000 приемлемо — пункт R1 просто выпадает, но тогда в Лист1 и в трекере
   будут стоять РАЗНЫЕ числа последней замены (36982 против 37000).
2. **Как чиним строку колодок** — пробуем штатный `delete_event` по реконструированному ключу
   (при `deleted=0` вреда нет, ключ просто не совпал) или сразу правим две ячейки руками.
3. **Фото-строки с 38982** — оставляем как улику (моя рекомендация) или чистим руками.

## 8. Транспорт (отмечено по требованию)

Два чтения с первого раза не прошли: `fleet` → `BridgeTransportError`, `state_get` →
`BridgeReceiptLost` (потеря расписки на втором плече; для чтения безопасно). Повтор дал ответ:
`fleet` — со второй попытки, `state_get` — семантический `not_found` (слой «состояние_байка» пуст).
Канал не подвешивался, таймауты не срабатывали.
