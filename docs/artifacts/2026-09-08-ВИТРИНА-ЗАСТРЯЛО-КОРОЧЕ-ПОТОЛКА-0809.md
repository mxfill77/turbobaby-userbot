# Блок ЗАСТРЯЛО витрины стал короче потолка: 785 → 369 при окне 400

ВИТРИНА-ЗАСТРЯЛО-КОРОЧЕ-ПОТОЛКА-0809

Дата: 08.09.2026 · полоса ПК · заход 222 · задание ящика Штаба
`shtab_task_02-c-vitrina-koroche.0908` (file id 1pa1hOS2Gdx_P28onnrG_xiy0cSREdZCD).
Единица работы одна: измерить, заменить, прочитать назад.

## Премиса П1 — ВЕРНА (проверена первым действием, до любой правки)

П1: «окно раздела равно 400 символам, а нынешний блок ЗАСТРЯЛО равен 785».

| что | живое число 08.09 | чем снято |
|---|---|---|
| окно раздела | **400** | `vitrina_pc.SHTAB_PART_MAX = 400` (`vitrina_pc.py:173`), читается `shtab_words` → `one_line(body, SHTAB_PART_MAX)` (`vitrina_pc.py:354`) |
| блок ЗАСТРЯЛО ДО правки | **785** символов (и сырых, и в одну строку — переносов внутри нет) | живое чтение узла дорогой демона |
| что видно наружу ДО правки | **400** символов, `обрезано=True` (399 текста + многоточие) | `vp.shtab_words(parsed, "stuck", "")` на живом тексте |

Числа сошлись с заданием, считали по ним.

## Признак сделанности: блок был ДЛИННЕЕ окна → правим

Правило задания: «меньше 400 — назвать число и остановиться». Живое 785 > 400 → работа идёт.

Дорога чтения — ТА ЖЕ, какой текст берёт демон, и другой в этом заходе не использовалось:
`vitrina_pc.SHTAB_NODE = "shtab_vitrina"` → `shtab_box_run.read_folder(prefix=…)` →
единственное совпадение по имени, file id **`1UOm_uPqKTu4Eo27plMigO-aDz17uo79Z`** →
`read_doc_text` → `vitrina_pc.parse_shtab` → раздел `stuck`.

## Счёт нового текста ДО записи: 369 ≤ 395 → писать разрешено

Текст задания, положенный в файл дословно (`_scratch_vitrina_short_0908/new_stuck.txt`) и
посчитанный отдельным процессом ПЕРЕД записью:

- сырых символов **369**, в одну строку **369** (переносов нет, `one_line` его не меняет);
- порог задания 395 — **влезает** (запас 26);
- окно раздела 400 — **влезает целиком**, обрезки не будет.

Своими словами текст не сокращался и не переписывался: скрипт записи читает его ИЗ ФАЙЛА,
а не держит копию в коде — именно чтобы в узел не попал пересказ.

## Что заменено: тело блока ЦЕЛИКОМ, по маркерам, а не по цитате середины

Якорь — маркер раздела и начало следующего маркера
(`^[ \t]*\[\[[ \t]*ЗАСТРЯЛО[ \t]*\]\][ \t]*\r?\n` … lookahead на `^[ \t]*\[\[` или конец текста),
совпадение обязано быть РОВНО одно. Цитировать середину тела в якоре нельзя: тело меняется
целиком, и первое же расхождение пробела читалось бы как «раздела нет».

Убрано вместе со старым телом (это НЕ потеря видимости, а потеря текста в узле — называем честно):
две фразы, которые заход 221 сохранял дословно, — про карточку ворот («принимает только слово
„выкати“ — сказать ей „нет“ нечем») и про менеджеров («из четырёх условий не выполнено ни одно»).
Наружу они не выходили и ДО правки — обрезка резала их первыми, — но теперь их нет и в узле.
Факт про тренажёр Штаб в новом тексте сохранил сжатой формой («Вердикта тренажёра на нынешний
код нет»), а номер коммита `74be777` из витрины ушёл.

## Гард усыхания сработал и был снят ЯВНО

Первая попытка записи упала штатно: `brain_writer.apply` → `ГАРД УСЫХАНИЯ … было 1554, стало бы
1138 — НЕ пишу; осознанное сокращение объявляется allow_shrink=True` (`brain_writer.py:635`).
Это не препятствие, а замок против МОЛЧАЛИВОГО укорачивания: цель захода — сокращение по прямому
тексту задания, поэтому `allow_shrink=True` объявлен в скрипте отдельной строкой с причиной.
Старое тело сохранено движком до записи: `tmp/brain_backup_vitrina_short_0908_20260908-155743.txt`.

## Доказательство чтением назад (расписка механизма фактом не считается)

Механизм сказал `status=ok before=1554 after=1138`. Это расписка. Факт снят ОТДЕЛЬНЫМ процессом,
дорогой демона, после записи:

- документ `shtab_vitrina` — **1138** символов (было 1554, −416);
- раздел `stuck` — **369** символов (было 785);
- `vp.shtab_words(parsed, "stuck", "")` → **369 символов, `обрезано=False`**: блок помещается
  в окно раздела ЦЕЛИКОМ, многоточия в витрине больше нет;
- дословность: sha256 живого блока и текста задания совпали —
  `d66580b5211b10e64e26c96181fca281948d0eff68d128818696892c6428012c`, посимвольное сравнение `True`;
- разделов по-прежнему три (`now/stuck/goal`), штамп `дата = 2026-09-05` не тронут,
  `[[СЕЙЧАС]]` и `[[КУДА ИДЁМ]]` посимвольно те же.

Живой текст блока после правки, дословно:

> Ворота не держат подъём: 08.09 в 01:03:46 они остановили f3e50ee21, а ребут в 02:39:53 применил
> его детям. Дети НЕ на коде 03.09 — все четыре на f3e50ee, разница с HEAD ноль .py. Мимо ворот за
> 42 суток 39 пусков, через них 0. Цена клиенту не уходит: три замка TEST_MODE. Вердикта тренажёра
> на нынешний код нет. Ждём слово владельца: переносить ли проверку в сам подъём.

## Запреты, которые держались

`SHTAB_PART_MAX` не тронут ни одной строкой — потолок раздела упирается в потолок сообщения, и
подъём просто передвинул бы обрезку на соседний раздел. Кода не менял вовсе: python-файлов
репозитория в правке нет, шесть дверей подъёма не тронуты, детей не поднимал и не выкатывал.
Ничего не удалял, включая скрипты разведки. Временное названо явно —
`_scratch_vitrina_short_0908/` (`measure.py` — замер, `write_short.py` — замена,
`verify_same.py` — сверка дословности, `new_stuck.txt` — текст Штаба), черновик доклада —
`tmp/pc_report/`. Секретов не читал: адрес и токен моста берёт САМ `brain_writer`/мост, в скриптах
живёт только строковая логика. Листа «Календарь бронирования» и броней не касался ни одной
командой. Процессов не трогал и не перезапускал.

## Цена

Три живых похода в мозг (замер, отказ гарда усыхания, запись) плюс два чтения назад
(длина и sha256). Потолок задания 2700 с, рабочая доля 1890 с — уложились заметно ниже: заход
дешевле 221-го, как и предписано арифметикой бюджета.
