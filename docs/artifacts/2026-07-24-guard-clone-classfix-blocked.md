# 2026-07-24 — Класс-фикс гарда в клоне VPS-репо: СТОП на шаге 1 (красная зона для headless)

## Что просили (сжато, имена боевых операций — только `<OP>`)

8 шагов в `D:\foreign\turbobaby-manager-bot`:

1. Жёсткий сброс клона на чистую `origin/main` (9a8c47a).
2. В `pretool_guard.py` добавить `_strip_script_cli_args(cmd)`: после токена, оканчивающегося
   на `.py`, вырезать все последующие позиционные аргументы, включая строковые литералы в
   кавычках; интерпретатор и имя скрипта оставить.
3. Вызвать её в `_units()` ДО построения scan-текста.
4. Мёртвую `_strip_all_git_msgs` удалить (с проверкой фактом, что в `origin/main` её нет).
5. Тесты в `tests/` (литералы конкатенацией из кусков): (а) вызов `cclog.py` со строкой с `<OP>`
   в аргументе → зелёное; (б) реальный вызов `<OP>` как операции → красное; (в) `git commit -m`
   с красным словом → зелёное; (г) файл секретов → блок; (д) чтение тела `.py` со словом `<OP>` →
   текущее поведение не меняем, тест фиксирует.
6. `python -m pytest tests/ -k guard -q`.
7. Гейт.
8. Коммит одним изменением + `git push origin main`.

## Почему СТОП на шаге 1 (и на всём контуре записи)

- Вся работа — записи ВНЕ проекта. Живой ПК-гард даёт `("ask", "write_outside")` на любую
  запись вне `D:\turbobaby-bot`, кроме скретчпада и памяти: `pretool_guard.py:368-369`;
  карве-аута для `D:\foreign` в `_sanctioned_outside` (`pretool_guard.py:269-273`) НЕТ.
- Жёсткий сброс — ask-класс и в живых settings (`.claude/settings.json:58-59`).
- Headless: ask-карточку подтверждать некому (факт класса 1830ef4) → каждый из шагов 1–5 и 8
  блокируется ДО исполнения. По доктрине ТЗ обход не искался; тот же класс, что вчерашний факт
  67918da («правка клона D:\foreign — красная карточка, запись вне проекта»).

## Факты ДО (сняты read-only, 2026-07-24)

- Клон: `HEAD` = `9a8c47a` «guard в3: ambiguous сам по себе НЕ красный — defer к слоям
  settings…», `origin/main` (локальный реф, без fetch) = `9a8c47a`, рабочее дерево чистое
  (`git status --porcelain` пуст). Целевое состояние шага 1 УЖЕ достигнуто: вчерашний
  guard-в3 влит в origin чужим коммитом.
- Шаг 4 проверен фактом: `git grep -e _strip_all_git_msgs -e _strip_script_cli_args
  origin/main -- pretool_guard.py` → пусто, exit 1. `_strip_all_git_msgs` НЕТ ни в
  `origin/main`, ни (дерево чистое) в рабочей копии — удалять нечего, пункт 4 сводится к
  этому факту. `_strip_script_cli_args` тоже ещё нет — фикс не начат.
- Точка будущей врезки: `def _units(cmd, depth=0)` — `origin/main:pretool_guard.py:633`.

## Развилка для владельца

1. **Интерактивная сессия / руками**: тот же промпт в сессии с живым подтверждением
   ask-карточек; шаг 1 можно свести к проверке (клон уже ровно на 9a8c47a, чистый).
2. **Карве-аут**: временно/постоянно допустить `D:\foreign\turbobaby-manager-bot` в
  `_sanctioned_outside` ПК-гарда + снять ask на жёсткий сброс для этого пути в settings —
   после этого headless-прогон пройдёт целиком.
3. **Исполнить на самом VPS**: фикс в родном репо своим контуром гарда; ПК-клон потом
   просто подтянуть fetch-ом.

## Карточка (для «да»)

NEEDS_APPROVAL: класс-фикс гарда в клоне VPS-репо `D:\foreign\turbobaby-manager-bot`
(жёсткий сброс + правка `pretool_guard.py` + тесты + пуш в main) · зачем: срезать класс
ложного красного — аргументы скриптовых CLI с именами `<OP>` не должны попадать в
scan-текст · последствия: запись и пуш вне ПК-проекта; в headless каждая ask-карточка
блокируется, исполнить может владелец или интерактивная сессия.
