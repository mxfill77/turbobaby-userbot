# KB_business_rules: раздел ТЕХОБСЛУЖИВАНИЕ между ОДОМЕТР и КАССА (13.08.2026)

Задача владельца: вставить в живой Brain-док `KB_business_rules` новый раздел
`ТЕХОБСЛУЖИВАНИЕ` строго между блоками `ОДОМЕТР` и `КАССА`, дословно, ничего больше
в документе не меняя. Красное (запись в мозг) — по подтверждению владельца.

## 1. Адресация — ПО ИМЕНИ через живой реестр моста

Захардкоженный file id не использовался ни разу. Имя разрешено живым реестром
(`brain_writer.resolve_name`, экшен `list_brain`):

```
KB_business_rules → name:business_rules  [канон business_rules]
```

Запись ушла тем же адресом (`--name`), не по id.

## 2. Откат, снятый ДО записи

Каталог (новый, с уникальным суффиксом):

```
D:\turbobaby-bot\tmp\brain_rollback_business_rules_20260813-130540_c7205fd6
```

| файл | чем снят | размер |
|---|---|---|
| `rollback_A_before.txt` | процесс №1, `brain_writer.read_text` | 1515 симв. / 2604 байт |
| `rollback_B_before.txt` | процесс №2, независимо | 1515 симв. / 2604 байт |
| `brain_backup_business_rules_pre_to_20260813-130751.txt` | встроенный бэкап писателя (третья копия) | тот же текст |

SHA-256 обеих независимых копий совпал:
`71dca5c5e8e2d1911bd738d77412acb7d8e78f801913a295e2372e76d5595065`.
Откат = запись любым из этих файлов обратно.

## 3. Структура блоков — проверена, а не предположена

Соседние блоки документа разделены ОДНОЙ пустой строкой. В снимке ДО записи:
`^ОДОМЕТР$` — ровно 1 совпадение целой строкой, `^КАССА$` — ровно 1,
`ТЕХОБСЛУЖИВАНИЕ` — 0 (раздела не было). Якорь `^КАССА$` + `require_above=ОДОМЕТР`,
`place=before` — вставка отдельным абзацем в стиле соседей.

Мутация сперва прогнана ОФФЛАЙН на снимке (`expected_after.txt`): 1515 → 1950,
текст вне точки вставки не тронут (посимвольная сверка головы и хвоста).

## 4. Что легло (тело — вербатим, UTF-8 без BOM)

Источник — `section.txt` в том же каталоге: 433 симв. (без хвостового `\n`),
10 строк, U+FFFD 0, NBSP 0, CR 0,
SHA-256 `1dac87403f9465ecec4ee2c38513f7fbb1b692c557a29eadd4f1dd9cfa630183`.
Кириллица в argv не передавалась ни разу: тело и якоря читались из файлов,
командные строки — чистый ASCII (класс «argv коверкает кириллицу»).

## 5. Проверка — ДВА независимых чтения назад, ответ моста фактом не считался

Расписка моста была `ok`, но вердикт вынесен чтением. Два отдельных процесса:

| число | значение |
|---|---|
| длина ДО | 1515 симв. (2604 байт) |
| длина ПОСЛЕ | 1950 симв. (3361 байт), дельта +435 |
| U+FFFD | 0 |
| `ТЕХОБСЛУЖИВАНИЕ` | 1 |
| `ОДОМЕТР` | 1 |
| `КАССА` | 1 |
| `конец документа` | 1 |
| CR | 0 |

Порядок разделов после записи:
`ПАРК И АРЕНДА > ПАРТНЁРЫ > ОДОМЕТР > ТЕХОБСЛУЖИВАНИЕ > КАССА > КОМАНДА > ЗАПРЕТ БЕЗОПАСНОСТИ`.

Чтение №1 и чтение №2 совпали дословно:
SHA-256 `07161fedbd40cb6a751f47c1b96939f32333bd22bc3f8b3d6f449837958b8c9c` у обоих.
Оба совпали и с оффлайновым `expected_after.txt` — то есть в документе не изменилось
НИЧЕГО, кроме вставленного блока. Обрезки/усечения нет: тело найдено дословно,
целыми строками, ровно 1 раз.

## 6. Границы захода

Других доков мозга не касались, кода не меняли, ничего не удаляли (старые откаты на
месте), `.env` не открывали, в рабочие таблицы не писали, процессы не трогали.
