# Полная замена KB_shtab_frame на версию 08.08.2026: откат снят, ЗАМЕНА НЕ СДЕЛАНА — нет текста

**Дата:** 2026-08-09, headless-заход полосы ПК.
**Задание:** переписать `KB_shtab_frame` (ключ `shtab_frame`,
id `1DiXr3jxoZYxkWaS7k4X4SR0u2xQKUTCi`) ЦЕЛИКОМ под версию рамки 08.08.2026, замена не дописка;
перед заменой снять копию нынешнего содержимого с меткой даты; судить не по расписке моста, а по
чтению назад **другим процессом и ПО ИМЕНИ**.

**Итог:** сделано всё, кроме самой записи. **Текста рамки 08.08 нет ни в строке задачи, ни в
дереве, ни в каналах — сочинять его нельзя, живой док не тронут** (доказано чтением назад:
посимвольно равен снятой копии). Это третий заход того же вида: 04.08 (текст пришёл файлом позже
и замена прошла), 06.08 (`docs/artifacts/2026-08-06-shtab-frame-rewrite-ask.md`, текст не пришёл),
сегодня.

## 1. Адрес — сошёлся (живой мост, только чтение)

| проба | ответ прода |
|---|---|
| реестр `list_brain` | ключей **33**; `shtab_frame` → `1DiXr3jxoZYxkWaS7k4X4SR0u2xQKUTCi` — **совпадает с id задания** |
| второй ключ на этот файл | **нет**: на id ссылается ровно один ключ (`shtab_frame`) |
| чтение по id и по имени | **11546 символов оба раза, посимвольно равны** |

## 2. Живой док ДО замены (замер, не пересказ)

```
11546 символов, 234 строки, U+FFFD 0, «конец рамки» 1 раз
строка   1: КОПИЯ. Источник — инструкции проекта Штаба, версия 04.08.2026.
строка   5: ═══ РАМКА ПРОЕКТА TurboBaby — ИНСТРУКЦИИ ШТАБА (версия 04.08.2026) ═══
строка 234: ═══ конец рамки ═══
```

## 3. НАХОДКА: док уже НЕ тот, что описан прошлым артефактом — и это законно

Артефакт 06.08 фиксировал **10202 символа**; сегодня в доке **11546 (+1344, +23 строки)**, а шапка
по-прежнему называет версию **04.08**. Дрейф прослежен до причины, а не оставлен догадкой:

- в журнале ПК (`cowork_log`) записи о правке рамки после 06.08 **нет ни одной** (`grep` по
  `cowork_log.ledger` на `shtab` — последняя строка от 06.08);
- в журнале VPS (`cc_log`, прочитан по имени через мост) она есть дословно:
  **`DONE 2026-08-08 08:09 UTC (local)`** — «копия рамки штаба (ключ `shtab_frame`) приведена к
  решению владельца: **§8г блок метрики заменён целиком (13 строк на 35), §11 формула провала**
  теперь про шумовые вмешательства и ремонты руками. **10202 на 11546 знаков, +23 строки**; два
  независимых чтения назад побайтово равны… Детали → `docs/artifacts/2026-08-08-frame-copy-metric-unit.md`»
  (артефакт лежит в репозитории VPS, здесь его нет);
- машинный дифф живого текста против копии 06.08 совпадает с этим описанием ровно: изменены блок
  `МЕТРИКА` и хвостовой `КРИТЕРИЙ ПРОВАЛА АРХИТЕКТУРЫ`, остальное посимвольно прежнее.

**Два следствия, оба практические:**
1. **копия-откат 06.08 в git (`docs/artifacts/2026-08-06-shtab-frame-before-rewrite.txt`) сегодня
   протухла** — запись ею вернула бы док на состояние до правок 08.08. Актуальный откат — только
   сегодняшняя копия (§4);
2. **док сейчас гибрид:** шапка и заголовок рамки говорят «04.08», а §8г и §11 уже несут решения
   08.08. Полная замена присланным текстом это и вылечит — но только настоящим текстом.

## 4. Откат снят ДО всего остального — в двух местах

| файл | что это |
|---|---|
| `tmp_shtab_frame_20260809_w1/KB_shtab_frame_before_2026-08-09.txt` | копия рабочего каталога задачи (untracked) |
| `docs/artifacts/2026-08-09-shtab-frame-before-rewrite.txt` | **та же копия в git** — чтобы откат пережил уборку временных каталогов |

Обе: **19 805 байт, 11546 символов, sha256 `62b22fa8cfd2c2bd…`, CR 0**, побайтно равны друг другу
и посимвольно — живому доку (сверено чтением файла обратно). Откат = записать этот текст обратно
тем же каналом. Правило `.gitattributes` (`docs/artifacts/*-before-rewrite.txt -text`, заведено
06.08) накрывает и сегодняшнюю копию: свежий чекаут не подменит LF на CRLF, и откат останется
побайтным.

## 5. Почему замена НЕ сделана

Текста рамки 08.08 нет:

- **в строке задачи его нет.** Пришла оговорка «текст пришлю следующим сообщением — жди его,
  ничего не сочиняй»; демон отдаёт исполнителю текст очереди целиком, без обрезки, значит это и
  есть всё, что было написано;
- **в дереве его нет.** `grep` по репозиторию на `версия 08.08.2026` и на
  `ИНСТРУКЦИИ ШТАБА (версия` даёт только: живой док/его копии (04.08), фикстуры прошлых заходов
  (04.08 и 06.08) и **литерал головы в моих же скриптах**, написанных сегодня. Файла `new_frame.txt`
  в каталоге задачи нет;
- **в каналах его нет.** `pc_agent.log`, `dispatch_notify.log`, `pc_orchestrator.log`,
  `rc_remote_control.log`, `rc_session_debug.log`, `rc_server_debug.log`, `cowork_log.ledger`,
  `cowork_hook.log` — вхождений «РАМКА ПРОЕКТА TurboBaby» **0**, вхождений `08.08.2026` **0**;
- **«следующего сообщения» в headless не бывает.** В очереди досыл к задаче — это новая задача.
  Так кончились заходы 04.08 и 06.08; 04.08 разрешилось тем, что владелец **положил текст файлом**.

**Сочинить текст нельзя по существу дока, а не из осторожности.** Это КОПИЯ инструкций Штаба, и её
первая строка велит исполнителям по ней сверяться. Придуманная «версия 08.08.2026» стала бы для них
источником правды, неотличимым от настоящего. Источник живёт в инструкциях проекта у владельца; с
ПК он не читается ничем (Drive-коннектор заданием объявлен красным — инструменты коннектора в
сессии видны, но не вызывались; мост отдаёт только Brain-доки).

**Взять текст 04.08 и подписать его 08.08 — не годится тем более:** он лежит рядом
(`tmp_shtab_rewrite_20260804_w3/new_frame.txt`), но это была бы старая рамка под новой датой, то
есть та же ложь, только дешевле. Тем более теперь, когда §8г и §11 живого дока уже ушли вперёд него.

## 6. Что готово к запуску (одна команда, когда текст будет)

```
1) положить текст владельца в  tmp_shtab_frame_20260809_w1\new_frame.txt   (UTF-8)
2) venv\Scripts\python.exe tmp_shtab_frame_20260809_w1\replace_frame.py
3) venv\Scripts\python.exe tmp_shtab_frame_20260809_w1\verify.py
```

`replace_frame.py` — полная замена через `brain_writer.apply` (секретов сам не читает, запрет
класса 328):

- **головной абзац принадлежит заданию**: скрипт снимает ЛЮБОЙ головной абзац «КОПИЯ …» из
  присланного файла (в том числе со старой датой — самая вероятная ошибка при копипасте) и ставит
  дословный текст задания 08.08. Первая строка дока гарантированно называет верную версию и не
  задваивается;
- `allow_shrink=True` объявлен **явно**: новая версия может быть короче, гард усыхания снимается
  сознательно, а не молча;
- `expect` = весь новый текст, `marker` = «конец рамки» (передаётся писателю, только если в новом
  тексте он ровно один раз — иначе число уходит в отчёт как есть, без подгонки);
- отказ моста записью не считается: судит обратное чтение; бэкап писателя ложится третьим
  экземпляром отката;
- **без `new_frame.txt` скрипт останавливается и мост не трогает вовсе** — проверено запуском
  сегодня (exit 2, ни одного обращения к доку);
- флаг `--dry` готовит текст и печатает числа, **не касаясь моста** — проверено сегодня на
  фикстуре `sample_head_check.txt` (голова 04.08 снята — 150 символов; первая строка стала ровно
  строкой задания, `равна заданию: True`; «конец рамки» 1 раз).

`verify.py` — независимый процесс, читает **ПО ИМЕНИ** через реестр и печатает доказательства
задания. Работает в обе стороны: до замены он доказывает, что док не тронут.

## 7. Доказательства, которых требует задание (сняты при нетронутом доке)

Вывод `verify.py` (отдельный процесс, чтение по имени, расписка моста не участвует):

```
РЕЕСТР: ключей 33 (до задачи было 33, без изменений: True); shtab_frame -> 1DiXr3jxoZYxkWaS7k4X4SR0u2xQKUTCi
        (== id задания: True); ключей на этот файл: 1 (shtab_frame)
ЖИВОЙ ДОК ПО ИМЕНИ KB_shtab_frame: 11546 символов, 234 строк, U+FFFD 0, «конец рамки» 1 раз
ДЛИНА ДО (копия-откат KB_shtab_frame_before_2026-08-09.txt): 11546 символов; ПОСЛЕ: 11546 (+0)
ЖИВОЙ ДОК ПОСИМВОЛЬНО РАВЕН КОПИИ-ОТКАТУ: True  ← значит док НЕ ТРОНУТ
ПЕРВАЯ СТРОКА ДОСЛОВНО: 'КОПИЯ. Источник — инструкции проекта Штаба, версия 04.08.2026.'; равна голове задания (08.08): False
ТЕКСТ ЗАДАНИЯ: файла new_frame.txt нет — сверять не с чем (замена не запускалась)
КОПИЯ-ОТКАТ НА МЕСТЕ: True (19805 байт, sha256 62b22fa8cfd2c2bd); та же копия в git: True (19805 байт, побайтно равна: True)
```

Читается так: длина до и после совпала (+0), потому что **записи не было**; U+FFFD 0; «конец
рамки» ровно один раз; реестр без изменений (33 ключа, один ключ на файл); первая строка честно
показывает, что шапка в доке пока прежняя — 04.08.

## 8. Развилка владельцу

Пришли текст рамки 08.08 — новой задачей или файлом `tmp_shtab_frame_20260809_w1\new_frame.txt`.
Тогда замена и сверка укладываются в один заход по §6. Отдельно стоит решить, нужна ли полная
замена вообще: §8г и §11 копии уже приведены к решениям 08.08 заходом VPS 08.08 08:09 — если
других расхождений с источником нет, дешевле обновить одну шапку, а не переписывать док целиком.

## 9. Запреты задания — соблюдены

`.env` не открывался; Drive-коннектор не использовался (инструменты коннектора в сессии
поднялись, но не вызывались) — только живой Bridge через `brain_writer`; ничего не удалено,
включая старые копии и прежние каталоги задач рамки; других Brain-доков не правили (`cc_log` и
`pulse` читались, и только на чтение); второго документа не создавали; временное — в новом
каталоге `tmp_shtab_frame_20260809_w1` (уникальный суффикс); процессы не трогались; клиентский
контур не задет.
