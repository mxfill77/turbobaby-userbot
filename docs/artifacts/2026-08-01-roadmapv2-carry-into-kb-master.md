# Перенос живых пунктов ROADMAP_V2 в KB_MASTER — сделано, с числами

**Дата:** 01.08.2026, 21:58 местного. **Основание:**
[`docs/artifacts/2026-08-01-roadmapv2-and-revizor-deferral.md`](2026-08-01-roadmapv2-and-revizor-deferral.md) §1.1.
Канал записи — `brain_writer` (класс 328): `.env` не открывался, Drive-коннектор не звался,
секреты берёт сам процесс писателя. Временное — `tmp_rmv2_carry_20260801_z9/` (untracked).
Roadmap_v2 не воскрешался, не регистрировался, из архива не доставался; живые таблицы не
читались и не писались; процессы не трогались; на сервер не ходили; ничего не удалялось.

## Что легло

Блок `=== ОТКРЫТЫЕ ВОПРОСЫ ИЗ ROADMAP_V2 (перенос 01.08.2026) ===` — **в НАЧАЛО** KB_MASTER
(ключ реестра `master`), старое не тронуто. Пять пунктов, каждый помечен «числа/текст из
roadmap_v2, дату не проверяли»; заголовок блока прямо говорит, что это **висяки, а не факты**:

1. Шесть просрочек масла — строки перенесены как стоят, поля не расшифровывались; названо, что
   срез роадмапа 26.06 и числам больше пяти недель, сверять по живому парку.
2. **Кривое поле H у байка 7530** (старт 33000 > пробега 31916) — помечен отдельно:
   ОТКРЫТЫЙ ВОПРОС ВЛАДЕЛЬЦУ, ждёт его решения (исправить старт / оставить), расчёты по байку
   до решения ненадёжны. Единственный пункт, который прямо ждёт человека.
3. Развилка хранилища расширения двухфазного ТО: вкладка Bot-Data «то_заявки» ЛИБО `memory.db`
   + неотвеченные вопросы 1–6 + указатель на PLAN 23.06 в `KB_review`.
4. Авто-тариф `getFormula` (живой прайс из формулы таблицы) — идея, не начата.
5. Ф2/Ф3 payments → `KB_payments_plan`: строка роадмапа была единственной живой ссылкой на этот
   док, теперь ссылка живёт в карте.

Хвостом — «что не перенесено и почему» (BELL, правило ведения карты, выгрузка Доставки,
Stage 5/6, LLM-проверка перевода): чтобы следующий заход не искал их заново.

## Доказательства — по ЧТЕНИЮ НАЗАД, не по ответу моста

Расписка моста была `ok`, но фактом записи не считалась: судили ОТДЕЛЬНЫМ повторным чтением
живого дока (`verify_after.py`).

| что | число |
|---|---|
| длина KB_MASTER ДО | **92 827** симв. / 973 стр. |
| длина ПОСЛЕ | **95 424** симв. / 1022 стр. |
| прирост | **+2 597** = блок 2 596 + перевод строки (сходится точно) |
| вхождение блока цельными строками | **1** (заголовок тоже 1) |
| блок стоит в начале дока | **True** (`back.startswith(блок)`) |
| голова дока == отправленному тексту дословно | **True** |
| хвост после блока == бэкапу побайтово | **True**, sha256 `b7f2d36442245c956ed04882d7f2d9c0d457020c28792c8366ade4b4cb8b6fb8` — тот же у бэкапа и у снимка «до», длины 92 827 = 92 827 = 92 827 |
| U+FFFD в доке после записи | **0** |
| бэкап с меткой | `tmp/brain_backup_kbmaster_roadmapv2_carry_20260801-215809.txt` |

Уникальность переносимого перепроверена по снимку «до» уже этим заходом: grep по
`ROADMAP_V2|roadmap_v2|7530|то_заявки|getFormula|payments|6895|33000|31916|двухфазн` —
**0 вхождений** в KB_MASTER до записи.

## Остатки (не делались сознательно)

- Ссылка на `roadmap_v2` из `test_brain_writer.py` (голден «имя отказывает честно») жива —
  снятие ссылки задевает тест, это правка кода, здесь не делалась.
- Битый алиас `roadmap_v2`→`roadmap_master` в `registry_check.py` чужой полосы — не мерился
  и не правился (на сервер не ходили).
