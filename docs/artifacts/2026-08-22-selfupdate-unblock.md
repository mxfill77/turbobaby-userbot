## Разблокировка самообновления 21.08: ступень держали два корня, и ни один не был про длинную аренду

СОСТОЯНИЕ ДО. Демон жил на `57166ab` (PID 18872, старт 20.08 22:27:15) — 42 коммита и 26.5 часа
позади HEAD. Ступень тестов самообновления давала 943 теста при 15 красных, и на этом красном
демон отбил СЕМЬ штатных попыток обновиться (21.08 01:56, 04:14, 12:05, 12:41, 19:40, 21:30,
22:18 — замер по `pc_orchestrator.log`). Премиса разведки «943 теста, 15 красных» подтверждена
живым прогоном дословно; премиса «граница длинной аренды к красным отношения не имеет» — тоже:
среди пятнадцати имён её нет ни одного.

КОРЕНЬ ПЕРВЫЙ — реестр, который пишется рукой, против факта, который считается с диска.
`45a38cf` сделал источником цены записанное правило и внёс в `suggest.py` ОДНУ строку —
`import price_gate`. За ней транзитивно приехал целый куст: `price_gate` → `price_freshness`,
`price_freshness_run`, `queue_snapshot_pc` → `brain_writer`. Замыкание демона выросло с 24 файлов
до 29, а ручной реестр `_ORCH_LAZY_UNCOVERED` остался на десяти именах.
ЗАБОР: реестр — не ворота, он не запрещает ничего. Это ИМЕНОВАННЫЙ ОСТАТОК: перечень
зависимостей демона, которые ворота грязного дерева СОЗНАТЕЛЬНО не покрывают, потому что
незакоммиченная правка `suggest.py` — самого правимого файла репозитория — глушила бы доставку
кода вовсе. Сторож ловит ровно одно: чтобы дыра была НАЗВАНА, а не молчалива. Он сработал верно.
ЧИНИЛОСЬ: пять модулей записаны в остаток (10 → 15). Дыру это НЕ рождает и не расширяет ни на
день — дыра приехала 21.08 вместе с `45a38cf`, а реестр про неё просто молчал. Накрыть куст
воротами грязного дерева нельзя: вход в него — сам `suggest.py`, и защита листа при открытом
корне была бы видимостью защиты, а не защитой. `brain_writer.py` тем же фактом переехал из
«посторонних» в зависимости демона.

КОРЕНЬ ВТОРОЙ — решение владельца о ТИШИНЕ остановило ДОСТАВКУ КОДА.
Ручка заморозки контура (`a2d43ab`) — это ФАЙЛ `pc_orchestrator.contour_frozen` в корне репо,
живое состояние мира. Владелец включил её 21.08 19:36 — и пятнадцать голденов ворот покраснели,
НЕ изменившись ни строкой. Между «пусть повтор отказа уходит в ленту» и «пусть демон не получает
код» связи нет никакой; вторую половину сделал ТЕСТ, а не код.
ЗАБОР: сами ворота клиентского контура fail-closed и держат в обоих исходах — заморозка решает
только АДРЕС сообщения. Трогать её значило бы ослабить только что принятое решение владельца, и
она не тронута ни строкой.
ЧТО ИМЕННО НЕВЕРНО — ТЕСТ. Голден, чей вердикт зависит от того, заморожен ли контур в эту минуту,
голденом не является. Изоляция ручки в репозитории БЫЛА, но стояла в дочернем классе
(`TestZamorozkaKontura`), а не в `Base`, рядом с шестью такими же боевыми state-файлами. Поднята
в `Base`.
ТЕЧЬ БЫЛА В ОБЕ СТОРОНЫ, и вторая половина — боевая. `gate_route` ПИШЕТ реестр форм, и прогон
тестов дописывал БОЕВОЙ экземпляр. Замер 22.08 00:36: живой `pc_orchestrator.gate_seen.json`
держал n=160 и n=60 по формам с `suggest.py` — при 14 отказах ворот за 21.08 и при демоне,
крутящемся на коде СТАРШЕ самой ручки, то есть неспособном записать туда ни строки. Счётчики
нарастили ПРОГОНЫ ТЕСТОВ. Цена этому боевая: записанная форма гасит владельцу ПЕРВУЮ настоящую
карточку по ней.

ЧТО СТАЛО. Ступень: 946 тестов, красных 0; три прогона подряд одной командой — 946/946, 946/946,
946/946, число одно во всех трёх. Ни один assert не снят, ни один порог не ослаблен, skip не
добавлен. mtime боевого реестра форм до и после трёх прогонов не дрогнул
(00:36:05.222202500 → 00:36:05.222202500) — течь на запись закрыта живым фактом, а не
рассуждением. Замок от возврата класса — `TestZhivayaRuchkaMiraNevidnaGoldenam` (+3 теста).
Коммит `66a422f`. Проверены и остальные ветки самообновления: рубильник снят, отпечаток
замыкания разошёлся с запущенным, ворота грязного дерева пропускают (`[]`), `code_gate` — ok.

ЧЕГО МЫ НЕ ЗНАЕМ, И ЭТО НЕ УСПЕХ.
1. Состоялось ли обновление — НЕИЗВЕСТНО, и не по нехватке терпения, а по устройству:
`maybe_self_update()` стоит в цикле ПОСЛЕ `poll_once()`, а `poll_once → process_new` исполняет
задачу СИНХРОННО. Задача, которая это чинит, сама и есть тот заход (id=18, CLAIM 22.08 00:32:44),
потоков у демона нет — значит из-под неё эстафету увидеть нельзя в принципе. Первая штатная
попытка придёт в тик после её закрытия. Проверять так:
`grep "self-update: 57166ab" pc_orchestrator.log` — ждём строку «гейт пройден».
2. Связка отказа владельца с реестром вердиктов (`5b27a9b`) — НЕ РАБОТАЕТ ПОКА, счётчик не вырос.
Живой реестр: 6 записей, у всех `hits=0`, файл не менялся с 10.08 00:32. Расписки
`pc_orchestrator.revizor_cards.json`, которую провод пишет В МОМЕНТ ДОСТАВКИ карточки, НЕ
СУЩЕСТВУЕТ вовсе — провод не отработал ни разу, потому что живёт в коде, до демона не доехавшем.
Рост счётчика требует ещё и живого «нет» владельца на карточку ревизора: это решение человека, и
подделывать его ради зелёного числа нельзя.
3. Боты остаются на старом коде и ПОСЛЕ обновления демона: их держит вторая, независимая
остановка — ворота клиентского контура (26 отказов с 18.08 20:03). Позеленевшая ступень
самообновления их не открывает ни одной веткой.

---

## Точки касания

| что | где |
|---|---|
| реестр именованного остатка (10 → 15) | `pc_orchestrator.py` → `_ORCH_LAZY_UNCOVERED` |
| изоляция живой ручки заморозки | `test_pc_orchestrator.py` → `Base.setUp` (рядом с шестью такими же state-файлами) |
| замок от возврата класса | `test_pc_orchestrator.py` → `TestZhivayaRuchkaMiraNevidnaGoldenam` (3 теста) |
| голден переезда brain_writer в зависимости | `test_pc_orchestrator.py` → `TestSelfUpdateDepClosure.test_transitive_and_lazy_dependency_change_moves_version` |
| ступень самообновления | `pc_orchestrator.py` → `_gate_unittests` (`test_pc_orchestrator` + `test_pc_local_dec`) |
| коммит | `66a422f` |
| узел мозга | `master`, раздел «Разблокировка самообновления 21.08…» (адрес разрешён по имени через живой реестр моста) |
| временное этой задачи | `_scratch_selfupd_0822/` (пробы и верификатор; ничего не удалялось) |

## Замки вставки в узел master (свежим процессом, против бэкапа на диске)

Снимок «до» — не наше утверждение, а файл `tmp/brain_backup_master_selfupd_2108_20260822-005822.txt`,
который `brain_writer` кладёт ДО записи.

```
длина ДО 185433 -> ПОСЛЕ 190571; вставка 5137 (+1 разделитель)
1) длина выросла РОВНО на длину вставки              OK  190571-185433=5138, ожидали 5138
2) новый заголовок встречается РОВНО один раз        OK
3) прежние заголовки целы и в прежнем порядке        OK  15 из 15
4) счёт заголовков вырос ровно на 1                  OK  15 -> 16
5) тело лежит цельными строками ровно 1 раз          OK
6) прежний текст цел как ПРЕФИКС                     OK  первые 185433 символа совпадают
7) повторное чтение через 60с даёт ТО ЖЕ             OK  sha256 6977274d…4026 дважды
```

Отдельно: две первые попытки записи упали `TimeoutError` при НЕИЗМЕНИВШЕМСЯ доке (узел 185 тыс.
символов, `write_doc` перезаписывает целиком, дефолт 30с). Третья попытка увидела текст уже в доке
и честно вернула `noop` — то есть одна из «упавших» записей легла ПОЗЖЕ ответа. Это ровно тот
класс, ради которого в канале стоит правило «ответ моста фактом записи не является»: дубля не
возникло только потому, что идемпотентность считает цельные строки, а не доверяет коду возврата.
