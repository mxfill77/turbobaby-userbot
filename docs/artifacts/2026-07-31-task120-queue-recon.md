# Задача #120 и очередь на 12:58 UTC 31.07.2026 — разбор read-only

**Тип:** только чтение. Код не менялся, коммитов нет, процессы не трогались, живые таблицы не писались.
**Источники:** Bridge `get_pending` (GET, читает доверенный модуль репо — секреты в этой сессии не
видны), `pc_orchestrator.log`, `git log/show`, `pc_orchestrator.client_trainer_green.json`,
`pc_orchestrator.client_watch.json`.
**Единственный созданный файл — этот. Он untracked, в git не заводился.**

---

## 1. Что такое задача #120

| поле | значение |
|---|---|
| id | 120, `lane=pc` |
| from | `Filipp-328-dev` |
| создана | 2026-07-31 **12:07:43 UTC** |
| взята (CLAIM) | 2026-07-31 **12:09:31 UTC** (лог: `CLAIM id=120 in_progress`, 19:09:31 локально) |
| закрыта | 2026-07-31 **12:41:57 UTC**, `status=done` (`COMPLETE id=120 status=done`) |
| длительность | 1943.84 с (~32 мин), `attempts=1`, `selfheals=0`, `outcome=done` |
| модель/усилие | `claude-opus-5`, `effort=xhigh` |

**Она НЕ выполняется сейчас.** В `in_progress` висит только #124 — эта самая сессия.

### ТЗ дословно

```
ultrathink

Класс-фикс трёх подтверждённых дефектов клиентских черновиков.
Только доказанные дословным текстом. Ложные срабатывания
и отложенные темы НЕ трогать.

ЧТО ЧИНИМ:

1. ДЕПОЗИТ = ДЕНЬГИ ИЛИ ПАСПОРТ, НИКОГДА ОБА.
Окно 529849022: «YAMAHA NMAX 155 на 5 дней — 1685 ฿ (337 ฿/день),
депозит 3000 ฿ — па[спорт]». Названы сумма и паспорт разом.
Правило владельца: одно ИЛИ другое.

2. НАЛИЧИЕ НЕ ОБЕЩАЕМ.
Окно 45349667, слово «успеваем». Бот наличие не подтверждает
и не отрицает — ведёт к менеджеру.

3. ПОКОЛЕНИЕ НЕ НАЗЫВАЕМ.
Окно 529849022: «XMAX 300 (2020-2022) | 5 дней: 2965 бат».
Год поколения в клиентский текст не уходит.

ГРАНИЦЫ:
- клиентское замыкание, коммит НЕ применяется автоматически;
- закончить ТРЕНАЖЁРОМ: 12 кейсов, все чеки, тот же коммит,
  два прогона из двух. Красный тренажёр держит коммит — это норма,
  не обходить и не уговаривать;
- SUGGEST_TEST_MODE не трогать;
- клиентам не писать, черновики не отправлять;
- процессы НЕ перезапускать вовсе — рестарт идёт отдельной задачей;
- к .env и секретам не обращаться ни в какой форме. Упрёшься —
  остановись и напиши, на чём именно. Сегодня ложная карточка
  на слово env уже убила одну одобренную задачу;
- тему прав, штрафов и сезонных цен не трогать — отложены
  владельцем, лежат в KB_MASTER, раздел «Ящик отложенного».

РЕГРЕСС на живом формате: дословные три черновика выше. Каждый
красный до правки и зелёный после — покажи оба прогона.

ИТОГ ОБЯЗАН СОДЕРЖАТЬ ОДНУ ИЗ ДВУХ ФРАЗ ДОСЛОВНО, она нужна
следующей задаче: «ТРЕНАЖЁР ЗЕЛЁНЫЙ, коммит <хеш> применён»
либо «ТРЕНАЖЁР КРАСНЫЙ, коммит удержан».

Владелец в пути и на карточки не ответит. Упрёшься в красное —
не жди, закройся с честным отчётом, что осталось.
Финал: строка в cowork_log.
```

## 2. Главное: основание НЕ было принято вслепую — опровержение сделала САМА #120

Предпосылка задания #124 («построена на опровергнутом основании, не продолжай, доложи, что
откатить») **на фактах не сходится**. Разбор, о котором идёт речь, — файл
`docs/artifacts/2026-07-31-revizor-false-findings-audit.md` (511 строк, mtime **12:05 UTC**,
за 4 минуты до CLAIM #120). Это её ВХОД, а не опровержение задним числом: она его прочла,
сослалась на него в коммите и сама же внесла в git (`edbe892`).

Из отчёта #120, дословно:

> Расхождение с заданием: по дословному тексту прода из трёх названных дефектов подтверждается
> **один** — год поколения; два других оказались ложными срабатываниями проверяльщиков на
> текстах, которые правилу владельца отвечают.

| # | цитата в ТЗ #120 | ДОСЛОВНО в проде | вывод #120 |
|---|---|---|---|
| 1 | «депозит 3000 ฿ — па[спорт]» | «депозит 3000 ฿ — **паспортом можно, всё верно**» (id=213, отправлено клиенту) | выбор предложен → правило ВЫПОЛНЕНО, находка ложная |
| 2 | слово «успеваем» | «По доставке сегодня всё подтверждаю: **раньше ночи не успеваем**» (id=360) | отрицание + про время подачи → находка ложная |
| 3 | «XMAX 300 (2020-2022) \| 5 дней: 2965 бат» | тот же текст (id=292) | дефект НАСТОЯЩИЙ |

Это ровно тот вывод, который #124 приводит как опровержение. Совпадает и номер окна (529849022),
и номер настоящего дефекта (поколение XMAX 300).

**Следствие:** правка легла «по месту дефекта, а не по месту находки» — п.3 гардом в боте,
пп.1–2 правкой ДВУХ ПРОВЕРЯЛЬЩИКОВ, которые красили выполненное правило. Правила владельца
не ослаблены: позитивы «3000 ฿ **и** паспорт», «успевайте», «в наличии», сырой
«XMAX 300 (2020-2022)» закрыты отдельными голденами и остаются красными.

## 3. Что #120 затронула по факту

**Коммит `55aad22`** (12:31:44 UTC, 5 файлов, +309/−24):

| файл | что |
|---|---|
| `suggest.py` | новая `enforce_no_gen_year` (врезана в `generate_draft` и `regenerate_draft`); граница `(?!\d)` в `_GEN_YEAR_RE`; `_scrub_gen_year`; `_smoke_deposit_conflict`; `_AV_SCARCITY_RE` + `availability_claims` |
| `reviewer.py` | `_depcheck`: скоуп ±150 символов → та же клауза, живые ИЛИ-формы прода |
| `test_suggest.py`, `test_reviewer.py` | +26 голденов на ДОСЛОВНЫХ текстах прода |
| `trainer_cases.json` | снято снятие чека «нет годов» с кейса 2 |

Побочный улов, о котором стоит знать отдельно: без границы `(?!\d)` боевой `_scrub_gen_year`
превращал «депозит: 20000 бат» в «депозит: **0** бат». Мина по корпусу ещё не выстрелила.

**Коммиты-записи:** `edbe892` (3 артефакта, 702 строки), `cbcaa86` (тело DONE-записи журнала).
**Состояние:** в `pc_orchestrator.client_trainer_green.json` добавлена запись `red` для `55aad22`
(221/224 чека, 10/12 кейсов, 2 прогона, дерево чистое, 19:40:01 локально).
**Журнал:** ARTIFACT + DONE легли в мозг, тело вынесено в `docs/artifacts/journal/2026-07-31-124109-done.md`.
**Все три коммита на `origin/main`:** `git rev-list --left-right --count origin/main...HEAD` → `0 0`.

**Чего #120 НЕ трогала:** живой клиентский контур (см. §4), процессы, `SUGGEST_TEST_MODE`,
секреты, БД (тексты брала из артефакта разбора), отложенные темы владельца.

## 4. Откатывать сейчас нечего — живой контур правку не получил

* Тренажёр на `55aad22`: **КРАСНЫЙ**, чеков 221/224, кейсов 10/12, 2 прогона из 2.
* `client_trainer_green.json` → `green: {}` — зелёных коммитов нет вовсе;
  `release_reason` → `None`, `trainer_verdict('55aad22')` → `(False, 'вердикт КРАСНЫЙ …')`.
* Лог демона, 12:42:41 и 12:52:07 UTC: `ворота контура … применение к «userbot,moderbot`
  **ОСТАНОВЛЕНО** — клиентские файлы: suggest.py`.
* `client_release.json`: одобрения владельца стоят на `1599bd7 / d75952c / f9bb1e2 / 14dd3fd /
  5e5ff69` — `55aad22` среди них **нет**.

То есть боты работают на коде ДО правки; отката «в живом» не существует. Если владелец всё же
захочет убрать правку из истории — это `git revert 55aad22` + push (ветка уже на origin),
и вместе с ней уедут: гард на год поколения, починка боевой мины «20000 → 0 бат» и 26 голденов
на дословных текстах прода. Артефакты и журнальные записи откату не подлежат — это протокол,
и он честно фиксирует расхождение с заданием.

**Рекомендация:** не откатывать. Отдельного решения владельца просят только отложенные темы,
перечисленные #120: «свободен на эти даты» в канонической строке J (держит кейс 7 корпуса),
«разные суммы депозита» (Ф1), класс «обещание срока подачи» («всё подтверждаю»), шум ревизора (Ф6).

## 5. Очередь целиком — срез 12:58 UTC 31.07.2026

| статус | id | откуда | тема одной строкой | создана (UTC) | возраст |
|---|---|---|---|---|---|
| **new** | 125 | `Filipp-328-dev` | разбор задачи 117 (что она писала в кассу) + почему карточка родилась пустой (гипотеза: ветка `op=other` объект не извлекает) + повтор вопросов про #120 и очередь | 12:30:38 | ~27 мин |
| **in_progress** | 124 | `Filipp-328-dev` | ЭТА сессия: разбор #120 + очередь (взята 12:53:52 UTC) | 12:28:14 | ~30 мин, в работе ~4 мин |
| **needs_approval** | — | — | пусто (0) | — | — |
| **approved** | — | — | пусто (0) | — | — |

Красных карточек в очереди нет. #125 — почти дубль #124 по пунктам 3 и 4 (те же вопросы про #120
и очередь), новое в ней только про задачу 117 и пустую карточку.
