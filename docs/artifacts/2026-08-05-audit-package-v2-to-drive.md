# ПАКЕТ АУДИТА v2 выгружен в папку Brain на Google Drive

**Дата:** 05.08.2026, 20:12–20:40 местного (13:12–13:40 UTC), ПК `D:\turbobaby-bot`, ветка `main`.
Канал записи — `brain_writer` (секреты берёт САМ писатель; скрипты захода конфигов не касались).
`.env` не открывался, ничего не удалялось, чужие доки не правились, процессы не трогались,
клиентский контур не задет, прод моста не двигался.

---

## Итог одной строкой

Док `KB_audit_package_v2` лежит в папке Brain **отдельным plain-файлом, БЕЗ регистрации в
реестре** (32 ключа до и 32 после). Тело — **дословно** содержимое источника, сверено
**обратным чтением отдельным процессом**: 10 739 символов → 10 739, sha256 совпал, U+FFFD 0.

| что | значение |
|---|---|
| источник | `docs/artifacts/2026-08-05-audit-package-v2.md` **полосы VPS**, коммит `bd768fb` |
| символов | 10 739 (18 241 байт, 75 строк, BOM нет, CRLF 0) |
| file id | `1WNSptrYftQ2U7UUNkUoMcn9D6nhr4Za4` |
| **ССЫЛКА (для телефона)** | **https://drive.google.com/file/d/1WNSptrYftQ2U7UUNkUoMcn9D6nhr4Za4/view** |

---

## 1. ИСТОЧНИКА НА ПК НЕ БЫЛО — он на полосе VPS

Задание называет путь `docs/artifacts/2026-08-05-audit-package-v2.md` и коммит `bd768fb`. В
репозитории ПК **нет ни того, ни другого**: `git cat-file -t bd768fb` → `Not a valid object name`,
файла на диске нет, `main` = `origin/main` = `95822e4`. Пакет собран **полосой VPS**, и это
подтверждено её же журналом (`cc_log`, запись `2026-08-05 13:00 UTC`):

> «Пакет внешнего аудита v2 собран и запушен (commit bd768fb):
> docs/artifacts/2026-08-05-audit-package-v2.md, 10739 символов из лимита 12000. Read-only,
> боевых записей нет, **в мозг не писал**.»

Тело взято с VPS **байт в байт** (`scp`), а не пересобрано:

| замер | VPS | ПК после копии |
|---|---|---|
| sha256 рабочего файла | `7944f711…a160a` | `7944f711…a160a` |
| sha256 блоба на `bd768fb` | `7944f711…a160a` | — |
| байт / символов | 18 241 / 10 739 | 18 241 / 10 739 |
| U+FFFD / BOM / CRLF | 0 / нет / 0 | 0 / нет / 0 |

`git status` по файлу на VPS чист — рабочая копия равна коммиту `bd768fb`.

## 2. ДУБЛЕЙ НЕ БЫЛО — проверено ДО записи шестью следами

`create_brain_plain` **неидемпотентен** (Drive спокойно держит одноимённые файлы), а перечислить
файлы папки Brain мост не умеет: `list_brain` отдаёт **манифест**, `read_doc`/`write_doc` по имени
ходят **только через манифест** (`ReadDocs.js` → `handleReadDoc_`/`writeDocUnlocked_`), а
`findChildFileByName_` наружу экшеном не выведен. Живой прод — 66 POST-экшенов, перечисления
папки среди них нет. Поэтому «прошлый заход» искался косвенно — все шесть следов пусты:

| проба | результат |
|---|---|
| реестр `--list-brain` | 32 ключа, ключей вида `audit`/`package`/`v2` — **нет ни одного** |
| `--resolve KB_audit_package_v2` | **ОТКАЗ**: канон `audit_package_v2`, в реестре нет, похожих нет |
| `cowork_log` (285 431 симв., 802 строки) | все пять игл → **0 строк** |
| `cc_log` (213 934 симв., 436 строк) | только запись о **сборке** пакета, про выгрузку — ничего |
| `cc_log_archive` (988 521 симв.) | **0** |
| греп репо ПК и репо VPS | **0 совпадений** |

## 3. РАСПИСКА ПОТЕРЯЛАСЬ, А ДЕЙСТВИЕ ИСПОЛНИЛОСЬ — и вот как это установлено

Единственный вызов `create_brain_plain` вернул **`BridgeReceiptLost`**:

```
второе плечо моста (echo) бросает обратно на наш же /exec — ключ расписки мёртв.
… Действие МОГЛО исполниться — судить обязан ФАКТ, слепой повтор запрещён.
```

Это известный класс 02.08 (`2026-08-02-bridge-receipt-leg-not-token.md`): отбита **расписка**, а не
запись. **Слепого повтора не делал** — он положил бы в папку Brain второй файл тем же именем.

Реестр смотреть бесполезно (создание шло с `key=""`), имя не резолвится, перечисления папки у
моста нет. Поэтому факт снят **с самого Drive**, read-only, из браузера владельца:
`embeddedfolderview` папки Brain — простая HTML-страница, где каждая строка это ссылка с file id.

- в папке **ровно один** `KB_audit_package_v2` (34 позиции листинга, дублей нет);
- порядок ссылок совпал с алфавитным листингом, и два id в нём **независимо подтверждены**
  артефактом 02.08 (`part1` = `1QUmsOzB…`, `part2` = `1ccXyu…`) — значит третий, `1WNSptrY…`,
  и есть `KB_audit_package_v2`;
- время изменения файла в Drive — `06:20` в часовом поясе аккаунта = **13:20 UTC** = минута
  вызова (тем же сдвигом `KB_claude_code_log 06:00` = запись `13:00 UTC`).

Окончательно личность файла доказана **содержимым** (п. 4), а не порядком строк.

## 4. ВЕРДИКТ — ОБРАТНОЕ ЧТЕНИЕ ОТДЕЛЬНЫМ ПРОЦЕССОМ

Ответ моста фактом не считался (его и не было). Судило чтение дока **по file id** из живого
Drive (`tmp/audit-v2-upload-20260805-q9/verify_back.py`; чтение идемпотентно, поэтому повторы
безопасны — вердикт снялся с 5-й попытки, четыре до неё съел тот же флап моста):

```
ДОК: KB_audit_package_v2   id=1WNSptrYftQ2U7UUNkUoMcn9D6nhr4Za4
  ДЛИНА  источник: 10739 символов | прочитано назад: 10739 символов | разница: +0
  СТРОК  источник: 75 | назад: 75
  U+FFFD в источнике: 0 | в прочитанном назад: 0
  ДОСЛОВНО: СОВПАЛО ПОЛНОСТЬЮ
  sha256 источника (норм.)         : 3737f02cc508f4ac12a3b10750dd0f7bbffd5ceb9370d5660a7d3c7b0c624a02
  sha256 прочитанного назад (норм.): 3737f02cc508f4ac12a3b10750dd0f7bbffd5ceb9370d5660a7d3c7b0c624a02
  КИРИЛЛИЦЫ в прочитанном назад: 7296 букв
```

Первые и последние строки **из прочитанного назад** (живой Drive, не с диска) — совпали с файлом:

```
# ПАКЕТ ВНЕШНЕГО АУДИТА TurboBaby — ВЕРСИЯ 2

Продолжение. Контекст, карту сред и два контура не повторяю.
```

```
**Что в нашей картине мы всё ещё не видим?** Три известных слепых пятна: половина находок
рождена нашими же заходами; ни один замер не заглядывает внутрь полосы ПК; и мы дважды
поверили числу, у которого не оказалось артефакта. Какое четвёртое ты видишь отсюда?
```

## 5. РЕЕСТР НЕ ТРОНУТ

По заданию — без регистрации. Вызов шёл с `key=""`. Реестр **32 ключа до и 32 после**, ключей
`audit`/`package`/`v2` нет ни одного, `--resolve KB_audit_package_v2` по-прежнему отказывает.
Док адресуется **только по file id** — что для выгрузки владельцу и требовалось.

## 6. ЧТО ЭТО ЗА ФАЙЛ ФОРМАТОМ

`create_brain_plain` создаёт **`text/plain`** (`folder.createFile(name, text, 'text/plain')`), а не
Google Doc — как боевые `cowork_log`/`cc_log` после миграции. Отсюда адрес вида
`drive.google.com/file/d/<id>/view`. С телефона открывается приложением Drive; разметка Markdown
не рендерится, это простой текст.

## 7. ОТКАТ

Удалить один файл из папки Brain (`trash_brain_file` по id `1WNSptrYftQ2U7UUNkUoMcn9D6nhr4Za4`) —
больше ничего не менялось: реестр не тронут, чужие доки не тронуты, прод моста не двигался,
в репозиториях обеих полос ни одной правки.

## 8. СЛУЖЕБНОЕ

Временные файлы захода — в **новом** каталоге `tmp/audit-v2-upload-20260805-q9/`
(`source.md`, `dup_probe.py`, `upload_v2.py`, `verify_back.py`); существующие каталоги не
засорялись. Ни один скрипт секретов не читает — транспорт и `BRIDGE_*` берёт сам `brain_writer`.
Адрес `/exec` нигде не печатался, значение токена не читалось и не выводилось.
