# dec_port_spec — эталон порт-спеки мозга декомпозера

Источник: Brain-док `cc_log`, скачано через Bridge `read_doc` (read-only,
образец вызова — `cowork_log_append.py`). Две записи ПОРТ-СПЕКА от 2026-07-11 (UTC),
извлечены дословно и сверены на целиковое вхождение:

- часть 1/2 — `DONE 2026-07-11 16:38 UTC` (12052 символов)
- часть 2/2 — `DONE 2026-07-11 16:43 UTC` (9987 символов)

---

## ⚠️ ЧИТАТЬ ПЕРВЫМ: это спека **VPS-полосы**, а не ПК

**Всё ниже — дословный порт `orchestrator_daemon.py`, который живёт на VPS.** Тело спеки
приведено без правок (в этом её ценность — это цитата записи мозга); полосой она не помечена
была, и ровно на этом 30.07 построили ложный диагноз.

Числа спеки **верны для VPS** и проверяемы: сверка-2 нашла все 14 «несуществующих» сущностей
в клоне `D:\foreign\turbobaby-manager-bot` (`ORPHAN_TTL`, `TASK_TIMEOUT_DEV`, `PC_STEP_TIMEOUT`,
`process_orphans`, `_task_timeout`, `_DEC_WAIT_STATUSES` …), значение в значение с текстом ниже.
Вывод «дока описывает несуществующий механизм ⇒ выбросить» **неверен** — выбросите живую спеку
соседней полосы. Источник: [`artifacts/2026-07-30-docs-vs-code-audit-verify.md`](artifacts/2026-07-30-docs-vs-code-audit-verify.md) §0.

**На ПК одноимённые механизмы имеют ДРУГИЕ пороги.** Это самый вредный класс расхождения:
имя совпадает, поведение — нет. Перевод спеки на ПК-контур (проверено по коду 2026-07-31):

| строка спеки | что обещает (VPS) | что на ПК на самом деле | код ПК (искать по ИМЕНИ) |
|---|---|---|---|
| `:89` «Полоса pc реапером **НЕ трогается**» | vps-реапер до полосы pc не дотягивается | **у ПК есть СВОЙ реапер, и он полосу pc именно трогает** | `pc_orchestrator.py` → `process_stuck_singles()` |
| `:89` `ORPHAN_TTL=600с` | 10 минут молчания = смерть | **5400с (90 мин)** | `PC_SINGLE_STALE` |
| `:89` реапер идёт **ПЕРВЫМ** в цикле и тянет хук цепи | `process_orphans` первым, сиблинги → «⏭ пропущен» | реапер **второй** (перед ним `process_lesson_waits()`); halt цепи делает **не он**, а `process_local_chains()` строкой ниже; каскада «⏭ пропущен» нет | `poll_once()` |
| докстринг реапера «**ТОЛЬКО одиночки**» | шаг цепи реапер не тронет | **тронет:** фильтра по `from` нет, отбор идёт только по полосе — под нож попадает любая `in_progress` полосы pc, включая шаг цепи | `process_stuck_singles()` → `_lane_ok()` |
| `:87` `TASK_TIMEOUT=600` / `TASK_TIMEOUT_DEV=2700`, выбор `_task_timeout` | два порога по суффиксу `from` | **один порог 2700 на всё**; `TASK_TIMEOUT_DEV` и `_task_timeout` на ПК не существуют | `TASK_TIMEOUT` |
| `:87` «Heartbeat — независимый поток, бьёт `updated`» | `updated` пульсирует у живого прогона | **потока нет.** `_write_heartbeat()` в конце цикла пишет живость **демона**, а не поле задачи ⇒ `updated` задачи заморожен с момента claim у ЛЮБОГО живого прогона | `poll_once()` → `_write_heartbeat()` |
| `:90` `PC_STEP_TIMEOUT=3600` — надзор ПК-цепи | 60 мин на шаг | имени нет в ПК-коде. Реально два порога: **900с** (цепь стоит МЕЖДУ шагами) и 5400с (шаг застрял `in_progress`) | `PC_CHAIN_STALE`, `process_stuck_chains()` |
| `:90` «`needs_approval` ждёт Филиппа **без ограничения**» | красный шаг стоит хоть до утра | **рубится через 30 мин.** Исключение ровно одно — owner-карточка ревизора | `process_approval_timeouts()`, `APPROVAL_TTL`, `_is_revizor_owner_card()` |
| `:91` `APPROVED_TTL=1800` | одобрено ⇒ доводим | **одобренная задача тоже умирает через 30 мин** — второй авто-килл по тому же порогу, о нём спека молчит | `process_approved()` |
| `:30-31` метка родителя `Filipp-328-dec` / `Filipp-pc-dec` | греп очереди по этой метке | у ПК **своя** метка локального дирижёра; `Filipp-pc-dec` ПК-надзор намеренно игнорирует как ЧУЖОЕ | `PC_LOCAL_DEC_FROM` (= `Filipp-pcloc-dec`) |
| `:45`, `:96` guard последовательности (`_DEC_WAIT_STATUSES`, `_earlier_new_sibling`) | защита порядка в `process_new` | этих имён на ПК нет — и **спека сама это говорит** на `:48`: «у `pc_orchestrator` НЕТ guard'а последовательности». Порядок держит sequential-релиз. Абзацы `:45`/`:96` — про `process_new` **VPS-демона** | — |
| `:95` тема красной карточки | `INBOX_TOPIC_ID` задан → единый инбокс 1160 | **ключа нет в окружении ⇒ работает вторая ветка условия: pc→829.** Спека тут права, ошибается тот, кто читает первую ветку как абсолют | `NEEDS_APPROVAL_TOPIC` (дефолт 829), `INBOX_TOPIC_ID` (1160, инциденты) |
| — | «все задачи жёстко в `LANE = pc`» (так пишет `revizor_recon.md`) | полоса **env-переопределяема**: `PC_LANE` в чужом окружении уведёт очередь | `LANE` |

### Два флага спеки на этом ПК **ВЫКЛЮЧЕНЫ**

`:69` обещает `STEP_SELFHEAL=1`, `:77` — `PLAN_ADAPT=1`. На этой машине **ни один не задан**:
читатели требуют ровно строку `"1"` (`pc_orchestrator.py` → функции-предикаты на `:2289` и `:2565`),
ключей нет ни в конфиге, ни в переменных среды пользователя и машины, а в живом
`pc_orchestrator.log` — ноль срабатываний (`selfheals=0` во всех строках METRICS, ни одной
`♻️`/`🩹`/`коррекц`). Проверено тремя независимыми способами, см. сверку-2 §1 (V8/V9).

Значит: вопрос «почему думатель не перезапустил упавший шаг / не скорректировал план» **уводит в
код напрасно** — механики в коде полно, причина в отсутствующем ключе. Зеркало случая с реапером:
там верили доке про «не трогает», здесь верят коду про «чинит».

### Копия этого файла разошлась с копией соседнего репозитория

Файл лежит в обоих репозиториях, и копии **молча разъехались на 11 дней**:

| | `docs/dec_port_spec.md` (ЗДЕСЬ, ПК) | `foreign/docs/dec_port_spec.md` (клон VPS) |
|---|---|---|
| строк | 99 | 93 |
| правился | 2026-07-12 22:37 | 2026-07-23 16:59 |

**Верна копия соседнего репозитория — и вот почему:** спека описывает `orchestrator_daemon.py`,
который живёт ТАМ; там же лежит код, по которому её можно проверить построчно, и там она правилась
на 11 дней позже. Наша копия — **устаревший дубликат чужой спеки**, а не источник правды: ни одной
строки ПК-кода она не описывает (что и доказывает таблица выше). Для ПК-контура она годится только
как справка «как устроена соседняя полоса».

Свести копии побайтово отсюда нельзя: правка `D:\foreign\…` — выход за пределы проекта (класс
`outside`), нужен «да» владельца. Записано в остатки; до этого момента считать источником правды
копию соседнего репозитория, а эту — читать вместе с шапкой.

---

## ПОРТ-СПЕКА часть 1/2 — DONE 2026-07-11 16:38 UTC

DONE 2026-07-11 16:38 UTC (headless): ПОРТ-СПЕКА мозга декомпозера, часть 1/2 (механический порт из orchestrator_daemon.py @ HEAD e343041; read-only, ничего не менялось).

═══ 1. ПРОМПТ ПЛАНИРОВЩИКА (дословно) ═══
PLANNER_PREAMBLE (стр. 310–330; для ПК-театра ПОСЛЕ него дописывается PLANNER_PC_NOTE — порядок важен: startswith(PLANNER_PREAMBLE)-роутинг в run_task/тестах не ломается):
«Ты — планировщик декомпозиции в headless-режиме в репо /root/turbobaby-manager-bot (CLAUDE.md действует). Твоя задача — РАЗБИТЬ крупное ТЗ на шаги, НЕ выполняя его: можно читать код/логи/доки (read-only разведка), НЕЛЬЗЯ править файлы, коммитить, деплоить, писать в таблицы.
ФОРМАТ ОТВЕТА — СТРОГО и ТОЛЬКО нумерованный список шагов, каждый с новой строки «N. <шаг>», без заголовков, без кода, без текста до/после списка. Шагов 2–7. Каждый шаг — САМОДОСТАТОЧНОЕ дев-ТЗ (до 45 мин, ≤400 символов): исполнитель увидит ТОЛЬКО текст шага, поэтому впиши в каждый нужный контекст (файлы, функции, что сделать, как проверить). Шаги строго в порядке исполнения; правки кода раньше, деплой/рестарт/проверка — последними.
КРАСНАЯ ЗОНА В ТЗ — НЕ ПОВОД ОТКАЗЫВАТЬСЯ ОТ ПЛАНА (урок задачи 166): ты ТОЛЬКО планируешь и сам ничего не исполняешь, поэтому упоминание clasp/деплоя/рабочих таблиц/денег/удаления в ТЗ НЕ требует подтверждения на этапе плана — НЕ выводи NEEDS_APPROVAL из-за содержимого ТЗ. Красное действие оформи ОТДЕЛЬНЫМ шагом (обычно последним): исполнитель ЭТОГО шага сам спросит «да» Филиппа кнопкой по штатной механике. Если ТЗ явно говорит, что прод-применение (деплой/рестарт) делается отдельно/хвостом — тем более просто строй план. ЕДИНСТВЕННОЕ исключение: ВСЁ ТЗ целиком = одно красное действие и разбивать не на что (например «задеплой прод») — тогда вместо списка выведи РОВНО одну строку «NEEDS_APPROVAL: op=other | <карточка: что · куда · последствия>».

КРУПНОЕ ТЗ:
»
PLANNER_PC_NOTE (стр. 333–338; ТОЛЬКО для родителя ПК-театра, frm=Filipp-pc-dec):
«ОСОБЕННОСТЬ ТЕАТРА ИСПОЛНЕНИЯ: шаги будет исполнять headless-агент на ДРУГОЙ машине (ПК, pc_orchestrator) — НЕ этот VPS. Пиши каждый шаг самодостаточно для ТОЙ машины: не ссылайся на пути/сервисы/файлы этого VPS, если само ТЗ явно не про них; весь контекст, нужный шагу, впиши в его текст.»
Обвязка вызова (_dec_plan_and_fanout, стр. 1665+): run_task(tid, task_text, task_timeout=TASK_TIMEOUT_DEV, preamble=PLANNER_PREAMBLE+[PC_NOTE]); NA-детект run_task для планировщика ОТКЛЮЧЁН по startswith(PLANNER_PREAMBLE) (стр. 585, урок 166) — red-маркеры в выводе не глушат план; status!=done → родитель failed (needs_approval от read-only планировщика = аномалия). Парс плана: _PLAN_LINE_RE = ^\s*(\d{1,2})[.)]\s+(\S.*) («N. шаг» / «N) шаг», прочие строки молча игнор). План пуст + _detect_needs_approval(out) → failed «планировщик needs_approval: <карта>» (фейл-сейф чисто-красного родителя, НЕ кнопка). len(steps) > MAX_STEPS=8 → failed «упрости ТЗ».

═══ 2. ПОЛЯ ОЧЕРЕДИ BRIDGE + ВСЕ МАРКЕРЫ ═══
Лист очереди (BotData.js QUEUE_COL): id=1, created=2, from=3, task_text=4 (обрез 5000), status=5, result=6, approved_by=7, updated=8, lane=9. Статусы: new → in_progress (claim, атомарно) → done|failed (complete_task принимает ТОЛЬКО их) | needs_approval → approved. API (bridge_client.py): enqueue_task(from_, task_text, lane=None→Bridge-дефолт vps)→{ok,id}; get_pending(status[,lane; 'all'=обе полосы])→{ok,items newest-first}; get_pending_multi(statuses CSV одним вызовом, старый Bridge → тихий по-статусный фоллбэк, item.setdefault('status')); claim_task(id[,lane-guard: чужую полосу не отдаст]); complete_task(id,status,result); set_needs_approval(id,what); task_heartbeat(id) бьёт updated. Родство цепи — ТОЛЬКО по паттернам в task_text (схема очереди не менялась, решение плана C).
РОДИТЕЛЬ: from='Filipp-328-dec' (vps) | 'Filipp-pc-dec' (PC_DEC_FROM; devbot кладёт БЕЗ lane → полоса vps: план строит ТОЛЬКО VPS-демон). task_text = крупное ТЗ (без служебных паттернов). result при done = restart-proof ИСТОЧНИК ПЛАНА: vps «🧩 Декомпозиция: N шагов, в очереди id …\n<план: строки 'i. <шаг>'>\n[🔴-пометка]\nИсполняю по одному… Красный шаг спрошу кнопкой.»; pc «🧩 Декомпозиция (театр PC): N шагов — исполняет ПК-агент ПО ОДНОМУ (lane=pc), план и надзор на VPS.\n<план>\n[🔴-пометка]Шаг 1 в очереди lane=pc (id X)… ПК молчит >60 мин → честный failed цепи (без самопочинки).»
ШАГИ: vps — from='Filipp-328-dec', веерный fan-out ВСЕХ сразу (порядок держит guard в process_new); pc — from='Filipp-pc-dec', lane='pc', ПО ОДНОМУ. Текст шага: «[шаг i/N родитель pid] [опц. «[коррекция плана K] »][опц. «[самопочинка шага i, попытка 1] »]<текст шага>» (обрез RESULT_MAX).
МАРКЕРЫ (regex дословно):
· _STEP_RE = ^\[шаг (\d+)/(\d+) родитель (\d+)\]  (match с начала)
· _SUM_RE = ^\[сводка родитель (\d+)\]  — synthetic-сводка: enqueue → claim → complete done (result = текст сводки); идемпотентность = скан этих маркеров (_dec_summary_exists) + кэш _summarized
· _HEAL_RE = \[самопочинка шага (\d+), попытка (\d+)\]  (search — маркер идёт ПОСЛЕ [шаг i/N]); повторный провал носителя = терминальный halt
· _HEAL_TASK_RE = ^\s*\[самопочинка задачи (\d+), попытка (\d+)\]  (одиночные тз:/задача:, якорь ^, N = id исходной)
· _ADAPT_MARK_RE = \[коррекция плана (\d+)\]  (на шаге; K-происхождение, restart-proof счётчик коррекций = max K из маркеров очереди)
· _ADAPT_CARD_RE = ^\[коррекция плана родитель (\d+)\]  — карточка адаптации: task_text «[коррекция плана родитель pid] после шага i (K=k)», result = 🧭-карточка с «НОВЫЙ ОСТАВШИЙСЯ ПЛАН» нумерованным списком (шаги i+1..new_total); база восстановления = _PC_ADAPT_BASE_RE «после шага (\d+)» из task_text
· _PC_CARD_RE = ^\[карточка родитель (\d+)\]  — событийная карточка pc-цепи (🩹 retry / 🛑 terminal / 🧭 adjust / ⚠️ план не восстановился / 🏁 finish): enqueue на vps from=Filipp-pc-dec → claim → complete done (devbot несёт в 829)
· _CONVERT_RE = ^\s*\[конверт одобренной заявки  — конверт op=other (самопочинка НЕ трогает; разрыв петли ре-конвертов)
· PC_SILENT_MARK = «⏱ ПК-театр не отвечает» (в result failed-шага; думатель по нему НЕ зовётся)
· _REJECT_PREFIX = «отклонено Филиппом» (префикс result devbot-отказа → halt без думателя)
· ADAPT_REPLACED_MARK = «♻️ заменён коррекцией плана», ADAPT_FINISH_MARK = «⏭ закрыт досрочно» — done-карты шагов, закрытых адаптацией (Bridge принимает только done|failed; failed глушил бы цепь как failed-сиблинг; сводка эти карты исключает)
· 🔴-ПОМЕТКА ПЛАНА: _dec_red_note(steps) → «🔴 красные шаги: i, j — исполнитель шага спросит «да» кнопкой, сам не исполнит.\n»; красность = _HEADLESS_IMPOSSIBLE_RE (IGNORECASE): clasp|redeploy|\bsqlite3\b|set_fleet_(?:oil|service)|delete_event|confirmed\s*=\s*true|лист\s*1|\bcrm\b|зарплат|байки|транзакц|проводк|деньг|касс|удал(?:и|ени|яе|ён)|календар. Пометка — ТОЛЬКО дисплей в result родителя (строка с 🔴 не матчит _PLAN_LINE_RE → restart-proof парс плана цел); текст шагов в очереди НЕ помечается.
· GUARD vps-последовательности (для полноты): _DEC_WAIT_STATUSES = (in_progress, needs_approval, approved) — сиблинг там → шаг не берём; failed-сиблинг → шаг пропускается (halt-on-fail); _earlier_new_sibling — есть new-шаг того же родителя с МЕНЬШИМ номером → пропуск (порядок по НОМЕРУ шага, не по id: перерождение самопочинки имеет id ВЫШЕ следующих шагов).

═══ 3. РЕЛИЗ ШАГОВ PC-ЦЕПИ (process_pc_chains, стр. 1639–1651 + хелперы 1268–1636) ═══
ИНВАРИАНТ: в очереди lane=pc живёт максимум ОДИН шаг цепи; следующий встаёт ТОЛЬКО после done предыдущего (sequential release). Причина: у pc_orchestrator НЕТ guard'а последовательности (FIFO claim подряд) — веер дал бы гонку halt-on-fail и порядка перерождений.
ЦИКЛ (каждый cycle демона, read-only снимок): _pc_fetch_items = get_pending_multi(("new","in_progress","needs_approval","approved","done","failed"), lane='pc'); ошибка чтения → None → пропустить цикл ЦЕЛИКОМ (частичная картина опаснее ожидания). _pc_group_chains: ТОЛЬКО from==PC_DEC_FROM И _STEP_RE-матч → {pid: [(i,n,item)…]}; одиночные Filipp-pc[-dev] не трогаются, claim чужой полосы НЕТ (complete на своих шагах = финализация собственной цепи). Для каждого pid вне _summarized → _pc_chain_tick; исключение тика одной цепи не валит остальные.
ТИК (_pc_chain_tick): рабочий элемент = ПОСЛЕДНИЙ шаг цепи = max по (номер шага, id) — при дублях номера старший id = перерождение самопочинки. По статусу:
· needs_approval → return (ждём Филиппа; карточка уже в инбоксе от devbot);
· new / in_progress / approved → ДЕТЕКТ МОЛЧАНИЯ ПК: _age_sec(updated) > PC_STEP_TIMEOUT (.env, дефолт 3600с; ошибка парса возраста → None → таймаут НЕ объявляем, ждём цикл) → complete_task(failed, диагноз «⏱ ПК-театр не отвечает: шаг i/n висит в <st> M мин…») + правка снапшота + сводка = halt цепи (думатель такое не чинит); иначе просто ждать;
· терминальный (done/failed): сначала _dec_summary_exists(pid) (сводка лежит на полосе vps) → цепь закрыта до рестарта → в _summarized, не трогать;
· failed → _pc_after_fail: (а) result startswith «отклонено Филиппом» ИЛИ содержит PC_SILENT_MARK → сводка, halt без думателя; (б) _HEAL_RE в тексте шага (перерождение упало ПОВТОРНО) → 🛑-карточка «попытка 1 исчерпана» + сводка (терминальный halt); (в) STEP_SELFHEAL=0 → сводка (прежний halt-on-fail); (г) иначе думатель самопочинки → verdict retry+fixed_step → перерождение enqueue lane=pc «[шаг i/n родитель pid] [самопочинка шага i, попытка 1] <fixed>» + 🩹-карточка; halt/сбой думателя/enqueue-fail → карточка с диагнозом + сводка. Halt в sequential-модели = просто НЕ релизить дальше;
· done → _pc_after_done: ВОССТАНОВЛЕНИЕ ПЛАНА _pc_current_plan(pid) — restart-proof из очереди vps: get_pending("done") → result родителя (нумерованный список → {num:(текст,K=0)}) + карточки _ADAPT_CARD_RE своего pid по возрастанию id: база B из task_text («после шага B»), остаток из result; план режется до num<=B и накрывается остатком с K=k (пустая/осиротевшая карточка → игнор = keep); возврат (plan, k_cnt, last_base); ошибка чтения/родитель не найден → ({},0,None) → честный halt. total = max(plan) (фоллбэк n из маркера шага). Дальше: i>=total → сводка (финал цепи); plan[i+1] нет → ⚠️-карточка «план не восстановился… поставь декомпозируй: заново» + сводка (halt); АДАПТАЦИЯ зовётся если PLAN_ADAPT=1 И last_base != i (шаг i сам из свежей коррекции — не переспрашиваем) И (pid,i) вне _pc_adapted (дедуп памяти процесса; после рестарта максимум один лишний keep-вопрос): finish → 🏁-карточка + сводка (остаток НЕ релизится, _adapt_finish[pid]=reason); adjust → K=k_cnt+1: K>PLAN_ADAPT_MAX=2 → 🛑 «план дрейфует, нужен владелец» + сводка; i+len(new)>MAX_STEPS=8 → fail-safe keep; иначе карточка коррекции (enqueue vps «[коррекция плана родитель pid] после шага i (K=k)» → claim → complete done с 🧭-картой и новым остатком) + релиз ПЕРВОГО скорректированного _pc_release(pid,i+1,new_total,new[0],k=K); сбой карточки → keep. keep/fail-safe/выкл/уже спрошено → релиз следующего шага ПРЕЖНЕГО плана: _pc_release(pid, i+1, total, текст, k=K-происхождение шага).
_pc_release(pid,j,total,text,k): enqueue lane=pc «[шаг j/total родитель pid] [коррекция плана k] <text>» (маркер K только при k>0); enqueue-fail → warn + повтор следующим циклом.
РЕЛИЗ ШАГА 1: в _dec_plan_and_fanout, ДО complete_task родителя (crash-окно: шаг не встал → родитель остаётся in_progress, devbot поднимет «зависла»); только при ok шага 1 родитель закрывается done с планом в result.
СВОДКА (_pc_post_summary/_pc_summary_text): идемпотентна (_summarized + _dec_summary_exists); done/failed-шаги снапшота, дубли номера → последняя по id; head «🧩 Сводка декомпозиции (родитель pid, театр PC): d/total шагов done» + («🏁 завершено досрочно: <reason>» | «есть упавшие/пропущенные»); строка на шаг «✅|❌ шаг i/n: <первая строка result, ≤400>»; total = n-маркер последнего релизнутого шага (несёт актуальный итог после коррекций).
ПАМЯТЬ ПРОЦЕССА — только дедуп-кэши (_summarized, _pc_adapted, _adapt_finish); всё состояние цепи — из очереди → после рестарта демона цепь продолжается с того же места.

Хвост: часть 2/2 (THINKER_PREAMBLE/ADAPT_PREAMBLE думателей, vps-ветка process_new/гейты, реапер сирот + claim-verify) — отдельной задачей.

---

## ПОРТ-СПЕКА часть 2/2 — DONE 2026-07-11 16:43 UTC

DONE 2026-07-11 16:43 UTC (тз:328, headless): ПОРТ-СПЕКА МОЗГА ДЕКОМПОЗЕРА, ЧАСТЬ 2/2 (read-only, источник = orchestrator_daemon, ~2088 строк; ничего не менялось).

── 1. САМОПОЧИНКА ШАГА/ОДИНОЧКИ (флаг STEP_SELFHEAL=1 в .env, один на обе ветки; 0/нет → ветка не зовётся вовсе) ──
ТРИГГЕРЫ (вход _maybe_selfheal из process_new, только status=failed): исполнительский провал шага декомпозера «[шаг i/N родитель pid]» ИЛИ одиночной «тз:»/«задача:». НЕ триггерит: needs_approval (красное отсечено раньше отдельной веткой), ⏱-диагнозы (гейт: fail_text начинается с TIMEOUT_MARK=⏱ → return False), конверты одобренных заявок (_is_convert — маркер конверта обязан стоять ПЕРВЫМ, перерождение сдвинуло бы его → разрыв петли ре-конвертов сломался бы), артефакт декомпозиции без [шаг i/N] при from=*-dec, плановый рестарт самомод-задачи (уже done внутри run_task, думателя не видит).
ДУМАТЕЛЬ (_thinker_exec, общий для самопочинки и адаптации): claude -p, --model ORCH_MODEL (.env, дефолт «fable») + --fallback-model ORCH_MODEL_FALLBACK (дефолт «claude-opus-4-8[1m]»), --output-format json, --max-turns 1 (чистый генератор, НИЧЕГО не исполняет, инструментов нет), --settings headless_settings.json (тот же строгий забор), cwd=репо, ANTHROPIC_API_KEY/OPENAI_API_KEY выпилены из env (идём по ~/.claude), таймаут STEP_SELFHEAL_TIMEOUT=180с. Промпт шага = THINKER_PREAMBLE + цель родителя ДОСЛОВНО (≤1500, из done-родителя очереди: task_text=цель, result=план; не нашли → заглушки, не валимся) + план шагов (≤2000) + текст упавшего шага + суть провала (≤1200). Одиночка = TASK_THINKER_PREAMBLE + текст задачи ДОСЛОВНО (≤2000) + суть провала (≤1200) — без родителя/плана.
JSON-ВЕРДИКТ (строго один объект, без обёртки): {"verdict":"retry"|"halt","fixed_step"/"fixed_task":"<новая формулировка>","reason":"<1 строка>"}. Парс терпит мусор вокруг (от первой { до последней }); verdict вне retry|halt → None. Правило retry по преамбуле: ТОЛЬКО если провал чинится переформулировкой (неверный путь/имя, недостающий контекст, кривая команда) и правка очевидна; fixed — САМОДОСТАТОЧНОЕ ТЗ ≤400 симв (исполнитель видит только его); всё прочее (неясно/нужен человек/красная зона/не влезает в таймаут) → halt + пустой fixed. Ровно одна попытка — многошаговые планы запрещены преамбулой.
РОВНО-1-ПЕРЕРОЖДЕНИЕ: retry+непустой fixed → enqueue НОВОЙ задачи. Шаг: from=Filipp-328-dec, текст «[шаг i/N родитель pid] [самопочинка шага i, попытка 1] <fixed>» (маркер ПОСЛЕ [шаг i/N] → у шага regex search; порядок в очереди держится по НОМЕРУ шага — id перерождения выше сиблингов, guard _earlier_new_sibling). Одиночка: та же полоса from (фолбэк Filipp-328-dev), маркер «[самопочинка задачи tid, попытка 1]» ПЕРВЫМ (regex с якорем ^ — страхует от ложного матча на ТЗ, где маркер лишь упомянут). Исходная задача закрывается done с 🩹-картой (правка+причина ≤200 + id перерождения + исходный провал ≤400) → done-рапорт в 328 несёт решение думателя. После 🩹-done шага адаптация плана НЕ зовётся (guard по номеру: new-сиблинг с номером ≤ step_i).
ТЕРМИНАЛЬНЫЕ УСЛОВИЯ: (а) перерождённый (маркер найден) упал ПОВТОРНО → терминальный failed/halt «🛑 самопочинка не помогла (попытка 1 исчерпана)» — петля невозможна; (б) думатель halt / пустой fixed → failed с диагнозом «думатель: halt, причина: …», цепь остановлена; (в) FAIL-SAFE = любой сбой думателя (запуск/exit≠0/таймаут/мусор-JSON/verdict вне словаря) или enqueue перерождения не встал → None/False → прежнее поведение байт-в-байт (halt-on-fail шага / голый failed одиночки).
PC-ВЕТКА (_pc_after_fail): тот же думатель/_selfheal_consult, перерождение lane=pc; отказ Филиппа (result начинается «отклонено Филиппом») и молчание ПК (PC_SILENT_MARK) → сводка БЕЗ думателя; повторный провал маркированного → 🛑-карта + сводка; halt в sequential-модели = просто не релизить дальше.

── 2. АДАПТАЦИЯ ПЛАНА (флаг PLAN_ADAPT=1, отдельный, независимый откат) ──
Вызов: после КАЖДОГО done-шага декомпозера (хук _dec_after_step → _maybe_plan_adapt, ДО проверки сводки). ПРОПУСК на последнем шаге: оставшихся new-шагов ноль → думатель НЕ зовётся (экономия лимитов; план из 2 шагов ⇒ зовётся только после шага 1). Также пропуск, если в new есть шаг с номером ≤ step_i (перерождение самопочинки ждёт — шаг реально не закрыт).
ДУМАТЕЛЬ: та же схема (_thinker_exec), PLAN_ADAPT_TIMEOUT=180с, ADAPT_PREAMBLE. Промпт: цель родителя ДОСЛОВНО + исходный план + результаты сделанных шагов сжато (первая строка результата ≤300; при 🩹-дублях одного номера берётся последний по id; ♻️/⏭-карты исключены — не результаты) + оставшиеся шаги (текст без [шаг i/N], ≤400).
JSON: {"verdict":"keep"|"adjust"|"finish","adjusted_steps":[…],"reason":"1 строка"}. keep = ДЕФОЛТ при малейшем сомнении (ноль изменений очереди). adjust без непустых adjusted_steps → None → keep.
ADJUST: adjusted_steps = новый ПОЛНЫЙ список оставшихся; новые шаги «[шаг j/new_total родитель pid] [коррекция плана K] <s>» from=Filipp-328-dec. Счётчик K restart-proof: max из маркеров «[коррекция плана K]» в очереди +1. ЛИМИТ PLAN_ADAPT_MAX=2: K>2 → все remaining закрываются failed «🛑 план дрейфует… лимит 2 на цепь — нужен владелец» (терминальный halt). Потолок: step_i+len(steps)>MAX_STEPS → fail-safe keep. Порядок fail-safe: СНАЧАЛА полностью встают новые шаги, ТОЛЬКО потом закрываются старые; не встал целиком → вставшие новые закрываются done+«♻️ — отменён…», действует прежний план. Карточка 🧭 в 328 — synthetic-задачей «[коррекция плана родитель pid]» (enqueue→claim→done).
ЗАКРЫТИЕ adjusted/finish-шагов = done+МАРКЕР-КАРТА (не skipped!): Bridge completeTask_ принимает ТОЛЬКО done|failed, редеплой Bridge = красная зона; failed нельзя — failed-сиблинг глушит цепь guard'ом. Заменённые → done «♻️ заменён коррекцией плана K: …»; finish → done «⏭ закрыт досрочно: шаг i/n не нужен — цель достигнута после шага step_i: reason» + _adapt_finish[pid]=reason (память процесса; сводка идёт тем же вызовом) → сводка «завершено досрочно». Сводка ♻️/⏭-карты ИСКЛЮЧАЕТ.
FAIL-SAFE везде (сбой/таймаут/мусор думателя, пустой adjust, потолок, enqueue-fail, исключение слоя — ловится в _dec_after_step) = keep. Совместимость: провал шага → самопочинка; done → адаптация; после 🩹-done адаптация не зовётся.
PC-ВЕТКА (_pc_after_done): тот же вердикт; keep → релиз следующего шага прежнего плана; adjust → карточка «[коррекция плана родитель pid] после шага i (K=k)» с НОВЫМ остаточным планом нумерованным списком в result (restart-proof восстановление) + релиз первого скорректированного; finish → 🏁-карта + сводка; дедуп консультаций _pc_adapted(pid,i) + после только-что-вышедшей коррекции на том же шаге не консультируемся (last_base); карточка не встала → keep.

── 3. ТАЙМАУТЫ И СИРОТЫ ЦЕПЕЙ ──
TASK_TIMEOUT=600с («задача:»), TASK_TIMEOUT_DEV=2700с («тз:» from=*-dev И ВСЁ семейство *-dec: планировщик и шаги — те же дев-ТЗ), оба из .env; выбор по суффиксу from (_task_timeout). Просрочка → kill claude-подпроцесса → failed «⏱ … не завершилась (лимит TASK_TIMEOUT из .env); думатель таймауты не чинит». Heartbeat — независимый поток каждые HEARTBEAT_SEC, бьёт updated (глушит свои ошибки).
⏱-ГЕЙТ ДУМАТЕЛЯ: fail_text начинается с ⏱ → _maybe_selfheal сразу False — переформулировка не ускорит зависший claude и не оживит Bridge; честный failed + прежний halt цепи хуком.
РЕАПЕР process_orphans (ПЕРВЫМ в cycle()): vps-in_progress с updated старше ORPHAN_TTL=600с (.env) → честный failed «⏱ задача-сирота: … heartbeat мёртв …» + _maybe_dec_after(failed) → цепь декомпозера глушится штатным хуком (остальные new-сиблинги → failed «⏭ пропущен», сводка). Демон однопоточный → любая vps-in_progress в cycle() = сирота, TTL — страховка от гонки. Полоса pc реапером НЕ трогается.
PC_STEP_TIMEOUT=3600с (.env): надзор pc-цепи (_pc_chain_tick) — ПОСЛЕДНИЙ шаг цепи висит в new/in_progress/approved дольше → failed «⏱ ПК-театр не отвечает: … ПК выключен или агент не работает» + halt цепи + сводка; самопочинка такое НЕ чинит (PC_SILENT_MARK в fail_text гасит думателя в _pc_after_fail). ВАЖНО: needs_approval на pc-шаге под PC_STEP_TIMEOUT НЕ подпадает (тик выходит раньше проверки возраста) — ждёт Филиппа без ограничения; approved — подпадает.
На vps approved-задача живёт APPROVED_TTL=1800с (30 мин) — не довёл → авто-failed «approve истёк».

── 4. КРАСНОЕ В ЦЕПИ (NEEDS_APPROVAL шага) ──
Шаг выводит маркер NEEDS_APPROVAL: op=… (или фоллбэк-фразы гейта — _detect_needs_approval) → демон НЕ исполняет: set_needs_approval(tid, дескриптор what). Исключение: конверт одобренной заявки снова упёрся в красное → терминальный failed с ручной картой «✋ ТРЕБУЕТСЯ РУЧНОЕ ДЕЙСТВИЕ» БЕЗ approve-кнопки (разрыв петли ре-конвертов) + хук цепи failed.
КАРТОЧКА: несёт devbot (фон-job report_results, раз в 45с) с кнопками ✅/❌ (+«да N»/«нет N»). Тема: задан INBOX_TOPIC_ID в env → ЕДИНЫЙ ИНБОКС (прод = 1160) для ОБЕИХ полос; не задан/0 → старые полосы: vps→328, pc→829. Approve живёт APPROVED_TTL=30 мин.
ЦЕПЬ ДО «ДА»: guard последовательности в process_new — шаг декомпозера, у которого сиблинг в _DEC_WAIT_STATUSES (in_progress/needs_approval/approved), ПРОПУСКАЕТСЯ в цикле (continue — чужие задачи дальше по очереди НЕ блокируются) → цепь просто стоит и ждёт Филиппа. На pc: тик цепи при needs_approval → return (ждём; карточка уже в инбоксе от devbot), следующий шаг не релизится (sequential-модель).
ОТКАЗ («нет N»/❌ — финализируется devbot'ом мимо демона, failed «отклонено Филиппом»): следующий new-шаг при взятии видит failed-сиблинга → сам failed «⏭ пропущен: другой шаг родителя упал/отклонён — цепочка остановлена» + хук глушит остальных + сводка. На pc отказ гасит думателя самопочинки (префикс-гейт) → сводка без починки.
КРАСНОЕ НЕ ОСЛАБЛЕНО ни самопочинкой, ни адаптацией: думатели ничего не исполняют (генераторы без tools), перерождённые/скорректированные шаги идут обычным путём — NEEDS_APPROVAL → кнопка как раньше; approve заявки обхода гейта тестов НЕ создаёт (красное снова даст кнопку).
СТАТУС: read-only порт-спека, код/очередь/таблицы не тронуты. Часть 1/2 — см. предыдущую запись порт-спеки. ХВОСТЫ: прежние без изменений.
