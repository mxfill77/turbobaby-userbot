# Ступень 2 отправка работает — 01.09.2026

Это **сводка контура**, а не ответ канала: файлы-ответы в этом лотке называются
`<день отправки>-<имя пакета>-<канал>.md` и начинаются строкой «НЕ ИСПОЛНЯТЬ».
Здесь — итог того, что ступень 2 заработала, и адреса доказательств.

## Что доказано живым фактом

**Ступень 2 отправка работает.** Пакет второго мнения за 31.08 ушёл в канал и
вернулся ответом на все три вопроса; второй канал отказал названной причиной, а не
молчанием.

| канал | исход | доказательство |
|---|---|---|
| **Codex CLI** | **ОТВЕТ ПОЛУЧЕН** | `2026-09-01-2026-09-01-chains-2026-08-31-codex.md` — 1365 знаков ответа, sha256 `3f1c026369eb`, цена 1909 токенов |
| **Manus HTTP** | **КАНАЛ ОТКАЗАЛ** (`no_credentials`) | `2026-09-01-2026-09-01-chains-2026-08-31-manus.md` — ключа канала на ПК нет ни одного, наружу не ушло ничего, цена 0 |

Отправленный пакет: `docs/review_outbox/2026-09-01-chains-2026-08-31.md`
(sha256 `ea11af69d67a…`), исходящий текст 11 093 знака (sha256 `e9715f5279d1…`),
стража исходящего — чисто.

## Три исхода контура

`ОТВЕТ ПОЛУЧЕН` · `КАНАЛ ОТКАЗАЛ` · `НЕИЗВЕСТНО`. Пустой ответ, ответ ниже пола
осмысленности, обрыв, объявленный каналом, и ненулевой код возврата — это **отказ**,
а не успех. Отрицательная проба (заведомо недоступный адрес обоим каналам) даёт
именно отказ с причиной `channel_unreachable` и ненулевой код возврата — постоянным
юнитом, а не разовым прогоном.

## Цена одного пакета до массовой отправки

- Codex: **16 086 токенов** холодным заходом (повтор тем же текстом — 1 909; бюджет
  считать по холодной).
- Manus: **1 заход** (`request`) на пакет; отказ до сокета — 0.

## Чего этот файл НЕ утверждает

Качество ревью здесь не судится. Массовой отправки не было. Ответы внешних каналов
не исполняются и задач не создают.

Разбор целиком, с числами и границами:
[`docs/artifacts/2026-09-01-review-send-stage2.md`](../artifacts/2026-09-01-review-send-stage2.md).
