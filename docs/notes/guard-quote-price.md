# guard-quote-price — карта расхождения quote ↔ текст клиенту

Родитель #310, шаг 1/5. Разведка: где считается quote, где формируется текст с ценой,
откуда LLM/шаблон берёт числа и где они могли разойтись с котировкой.

> ## ⚠️ Адреса в этом файле — по ИМЕНАМ, номеров строк здесь больше нет (починка 31.07.2026)
>
> Файл заморожен 14.07; после этого в `suggest.py` прошло 47 коммитов. Из 14 проверенных
> утверждений содержание разошлось у двух, а **адреса — у десяти**: `_client_price` уехал
> `1458 → 3210`, `build_pricing_note` `2036 → 4247`, `CRITICAL_FACTS` `142 → 275`,
> `postcheck_draft` `2689 → 5346`. Это не «немного мимо»: по старому адресу лежит
> осмысленный ЧУЖОЙ код, и правку кладут туда, ничего не заподозрив.
>
> Номера сняты потому, что удержать их нельзя — они гниют за часы (замер сверки-2:
> `pretool_guard.py` за сутки сдвинул её собственные адреса). Правило:
> `grep -n "def <имя>" suggest.py`. Содержание документа при этом верно —
> расхождений по существу было ровно два, оба помечены «Поправка 31.07» ниже.

Кейс-провал: окно `504608015`, «NMAX 155 **на 5 дней — 2 245 ฿**» вместо **1 685 ฿** из
quote. Ключ: `449 × 5 = 2245`, где `449 ฿/день` — суточная ставка NMAX 155 (сетка-ориентир
и/или `day_price` котировки), а `1 685 ฿` — period-total из Календаря (тариф со скидкой за
срок, ≈337 ฿/день). То есть период-итог из quote заменён наивным «дневная ставка × дни».

---

## 1. Модуль расчёта quote (источник ИСТИНЫ)

**`pricing.py`** — read-only котировка из «Календаря бронирования» через Bridge (GET, без
записи, любые ошибки → `None` → честный фолбэк).

- `quote(bike, date_start, date_end)` → dict-котировка или `None`.
  `_normalize` отдаёт поля: `day_price`, `total`, `deposit`, `available`,
  `season`, `days`, `text`, `cap_active`, `cap_price`. **`total` — это цена за ВЕСЬ период
  (с дисконтом за срок), `day_price` — суточная ставка.** Они НЕ связаны `total = day_price ×
  days` при сроке со скидкой (в кейсе `total=1685`, `day_price=449`, `days=5`).
- `quote_for_model(model, ds, de)` — резолв модель→конкретный байк парка
  (`fleet` + `_candidates`) и котировка; статусы `ok|none_available|no_candidates|error`.
- `sanity_days_ok(quote_days, hint_days, monthly)` — гард согласованности
  `days` котировки со словами клиента (>1 дня расхождения → режем цену). Сверяет ДНИ, но
  **не сумму** — наивный пересчёт суммы этим гардом не ловится.

Вывод: quote отдаёт правильный `total=1685`. Расхождение возникает НЕ в `pricing.py`, а
ниже по потоку — в сборке текста и/или в генерации LLM.

---

## 2. Формирование текста цены клиенту (`suggest.py`)

Поток: `build_pricing_note` → `_resolve_model_price` → `_client_price` → `make_system_prompt`
(вставка в промпт) → LLM `generate_draft` → `postcheck_draft` (страховка).

- **`build_pricing_note(hints, lang)`** — собирает инструкцию-блок ЦЕНА.
  Одиночная модель (наш кейс) → `products` длиной 1 → одиночная ветка.
- **`_resolve_model_price(model, ds, de, hint_days, monthly)`** — зовёт
  `_safe_quote_for_model` → `pricing.quote_for_model`; при `status==ok` возвращает
  `("ok", _client_price(q), q)`.
- **`_client_price(q)`** — КЛЮЧЕВОЕ место сборки фразы:
  - если есть `q["text"]` (J-текст Календаря) → берётся **дословно** + депозит;
  - иначе собирается `«{day_price} ฿/день; итого {total} ฿; депозит {deposit} ฿»`.
  **В обоих случаях в блок ЦЕНА попадает и суточная ставка (449), и период-итог (1685)** —
  т.е. у LLM перед глазами оба числа, из которых `449 × дни` даёт неверный итог.

  > **Уточнение 31.07 (утверждение выше в силе, но не всегда).** Появился **маркерный режим**:
  > при `kind == "ok"` и НЕ процентном вопросе (`suggest.py` → `marker_mode`) в промпт уходит
  > `_quote_marker_note()` («сам никакие числа НЕ называй»), а служебный `<<<QUOTE>>>`-блок из
  > промпта **вырезается** (`_QUOTE_BLOCK_RE.sub`) — на этом пути LLM цифр действительно не видит,
  > их вставляет КОД. Путей, где числа перед LLM ОСТАЮТСЯ, три: процентный вопрос,
  > `kind != "ok"` и мульти-модельная ветка.
  > **Но это ОДИН путь из нескольких.** Числа остаются перед LLM при: процентном вопросе,
  > `kind != "ok"` и мульти-модельной ветке. Сверх того `CRITICAL_FACTS` (там дословно
  > «NMAX155 449/3000») уходит в **каждый** промпт безусловно — это и есть остаточный источник
  > числа 449 даже в маркерном режиме.
  > Практический вывод: не неси правку в `_client_price` «чтобы LLM не видела `day_price`» —
  > на маркерном пути она его и не видит, а на остальных дело не только в нём.
- **`_wrap_single(kind, phrase)`** — оборачивает: «ЦЕНА из Календаря
  бронирования (использовать ДОСЛОВНО, не пересчитывать и не округлять): …». Инструкция «не
  пересчитывать» есть, но она текстовая (LLM), не гард.
- **`make_system_prompt(...)`** — вставляет `pricing_note` в промпт
  (переменная `price_block`), ниже — ценовая политика «СТРОГО».
- **`generate_draft` / `regenerate_draft`** — `call_llm` рождает
  текст, затем `postcheck_draft`.

---

## 3. Второй источник суточной ставки в промпте (утечка сетки)

**`CRITICAL_FACTS`** (`suggest.py`, искать по имени константы):
`… NMAX155 449/3000 …` — прайс-ОРИЕНТИР ДЛЯ МЕНЕДЖЕРА (฿/день, депозит), в промпт **ВСЕГДА**.
Помечен «клиенту цену не называть», но физически даёт LLM суточную ставку `449` даже когда
`_client_price` отдал J-текст без явного `day_price`. `449 × 5 = 2245` берётся отсюда легко.

---

## 4. Пост-чек черновика (последний рубеж) и его дыра

**`postcheck_draft`** → `_pc_classify`: детерминированно
клеймит числа-итоги вне белого источника и переписывает сегмент в «уточню».

- Белый источник — `_pc_wl_price_numbers(pricing_note)`: ВСЕ числа из блока
  ЦЕНА (включая `449`, `1685`, `3000`). Число `2245` в белый источник НЕ входит → в теории
  должно клеймиться.
- Итог-цена ловилась `_PC_PRICE_TOTAL_RE` через `_PC_PERIOD`:
  **`за\s+\d… (дн|день|нед|мес|days|weeks|months)`**. Обе константы с тех пор **удалены** —
  см. раздел «РЕШЕНО» ниже; читать этот абзац как историю провала, а не как карту кода.

**ДЫРА (почему 2245 дошёл до клиента):** живая фраза «NMAX 155 **на** 5 дней — 2 245 ฿»
использует предлог **«на»**, а `_PC_PERIOD` требует **«за»**. Регэксп не матчит → итог-за-
период не распознан → `2245` (которого нет в белом источнике) проходит мимо гарда. Дополнительно
уязвимы формулировки: разделитель «—» без «за», EN-форма «for 5 days» (в `_PC_PERIOD` нет
«for»), «5 дней — сумма» с обратным порядком.

---

## 5. Где именно числа расходятся с quote (итог разведки)

1. **LLM-пересчёт**: имея в промпте суточную ставку `449` (из `_client_price` `day_price`
   и/или из сетки `CRITICAL_FACTS`) и срок 5 дней, модель считает `449 × 5 = 2245` вместо
   того чтобы взять `total=1685` из quote. Инструкция «не пересчитывать» (`_wrap_single`) —
   мягкая, не гард.
2. **Пост-чек не ловит** этот итог из-за узкого `_PC_PERIOD` («за», без «на»/«—»/«for»).

Наименьшие по объёму точки фикса для следующих шагов #310 (**план исполнен — см. «РЕШЕНО»**):
- `_PC_PERIOD` / `_PC_PRICE_TOTAL_RE` — расширить предлоги/разделители
  («на», «—», EN «for»), чтобы любой итог-за-период сверялся с белым источником.
- опц. `_client_price` — не подавать LLM голую суточную ставку рядом с
  period-total, или явно помечать `total` как «итог за весь срок, не умножать».
- опц. golden-тест по ПРАВИЛУ-КЛАССУ CLAUDE.md: дословная фраза провала «NMAX 155 на 5 дней —
  2 245 ฿» (позитив-клейм) + парафразы RU/EN, негатив — корректный `1 685 ฿` из quote.

## РЕШЕНО (#310, закрытие дыры пост-чека, аудит 13:22)

Дыра была ЖИВА в проде дольше плана: узкий `_PC_PERIOD` («за») пропускал живой итог окна
`504608015` «на 5 дней — 2 245 ฿». Закрыто ОДНИМ поясом — пост-чеком:

- Разбор денег черновика сведён в единую `extract_money_figures` (точка правды одна): она
  распознаёт срок при ЛЮБОЙ формулировке («за/на/—/for N дней», «N дней:», падежи —
  `_MF_PERIOD` с необязательным предлогом) и отделяет суточную ставку (`rate`, не клеймим)
  от итога-за-срок (`total`) и депозита. Каждый фиг несёт `has_cur` — клеймим `total`/`deposit`
  только с валютным токеном рядом (отсекает счётчики «на 5 дней для 2 гостей»).
- `_pc_classify` больше НЕ использует `_PC_PRICE_TOTAL_RE`/`_PC_DEPOSIT_RE`/`_PC_PERIOD` (удалены)
  — клеймит числа из `extract_money_figures` вне белого источника `pricing_note`.
- Гард `guard_quote_price` (#310, был мёртвым кодом — нигде не подключён) и его обвязка
  (`quote_price_numbers`/`quote_price_mismatches`/`_quote_hard_directive`/
  `price_fallback_from_quote`) УДАЛЕНЫ; репро-кейс окна перенесён на пост-чек
  (`test_suggest.py::TestDraftPostcheck` — дословная фраза + парафразы RU/EN + негатив 1685).

## Файлы
- `pricing.py` — quote/quote_for_model/fleet/sanity (истина).
- `suggest.py` — build_pricing_note / _resolve_model_price / _client_price / _wrap_single /
  make_system_prompt / CRITICAL_FACTS / generate_draft / postcheck_draft / _pc_classify /
  ~~_PC_PRICE_TOTAL_RE~~ → **`extract_money_figures` / `_MF_PERIOD`**.

> **Поправка 31.07 — хвост противоречил самому документу.** Семью строк выше док СВОИМИ словами
> сообщает, что `_PC_PRICE_TOTAL_RE`/`_PC_DEPOSIT_RE`/`_PC_PERIOD` удалены, а этот перечень
> продолжал называть `_PC_PRICE_TOTAL_RE` живым ориентиром. Это не «док документирует
> удалённое» (что нормально и полезно), а **несогласованный хвост**: два абзаца одного файла
> учат разному. Живая замена — `extract_money_figures` и `_MF_PERIOD` (искать по именам).
