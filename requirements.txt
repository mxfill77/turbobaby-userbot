# requirements.txt — зависимости ПК-контура turbobaby-userbot
#
# Собран 07.09.2026 из ЖИВЫХ фактов, а не из памяти:
#   • имена — разбором AST 224 отслеживаемых .py в корне репозитория (git ls-files);
#   • версии — из метаданных установленного окружения (importlib.metadata),
#     venv/Scripts/python.exe, Python 3.12.5.
# Версии закреплены теми, что стоя́т сейчас: сегодняшнее окружение —
# единственное доказанно работающее.
#
# ЧЕГО ЭТОТ ФАЙЛ НЕ ДОКАЗЫВАЕТ: установка по нему с нуля НЕ проверялась —
# для этого нужны сеть и пустое окружение, а заход, писавший файл, окружения
# не касался. Файл есть ГИПОТЕЗА О ПОЛНОТЕ, проверяемая первой чистой сборкой.
# Разбор, числа и границы: docs/artifacts/2026-09-07-requirements-inventory.md
#
# Пары «имя импорта → имя пакета» разные у 3 из 5 — ставится ПРАВОЕ:
#   dotenv   → python-dotenv        telegram → python-telegram-bot
#   telethon → Telethon (регистр)   anthropic/requests — совпадают

# ── обязательные: импорт верхнего уровня, без перехвата ──────────────────────
# 13 файлов, включая userbot_listen.py, moderation_bot.py, pc_orchestrator.py
python-dotenv==1.2.2
# 17 мест в 10 файлах — клиент телеграма для пользовательского аккаунта
Telethon==1.43.2
# 11 мест в 2 файлах — moderation_bot.py и pc_agent.py
python-telegram-bot==21.7

# ── обязательное отложенное: импорт ВНУТРИ функции, без перехвата ────────────
# suggest.py:5841 (_default_llm) и collect_booking.py:127 (call_llm) — ветка
# работы по API-ключу. Ветка по умолчанию (SUGGEST_LLM_VIA_CLI, подписка Max)
# пакета не требует, но при пустом SUGGEST_LLM_VIA_CLI отсутствие пакета —
# ImportError в момент вызова, а не при старте. Поэтому пакет обязателен.
anthropic==0.104.1

# ── необязательное: единственный импорт под try/except ───────────────────────
# pc_agent.py — при отсутствии пакета ветка деградирует штатно, старт живёт.
requests==2.34.2

# ── границы описи (полный разбор — в артефакте) ──────────────────────────────
# Транзитивные зависимости НЕ перечислены сознательно: их ставит сам менеджер
# пакетов по метаданным дистрибутивов (anyio, httpx, pydantic, jiter, sniffio,
# distro, docstring-parser, typing-extensions · pyaes, rsa · certifi, idna,
# urllib3, charset_normalizer). Канон проекта — простой список прямых имён.
# Инструментов разработки здесь нет: тесты гоняются штатным `-m unittest`,
# pytest в репозитории НЕ импортируется ни разу (все 7 упоминаний — строковые
# проверки «идём ли мы под pytest», не зависимость).
