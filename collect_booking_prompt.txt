<<< ВСТАВЬ СЮДА системный промпт ЦЕЛИКОМ из KB_collect_booking_spec (Brain/Google Drive) >>>

Это файл-держатель системного промпта для collect_booking.py.
Замени ВЕСЬ этот текст содержимым промпта-парсера из KB_collect_booking_spec:
точный system-промпт, формат карточки, логику оценки опыта (ok/review/unknown), тарифы.
НИЧЕГО в логике опыта не меняй — она выверена по 710 реальным диалогам.

Пока в файле остаётся слово-маркер "ВСТАВЬ СЮДА", collect_booking.py НЕ обращается к LLM
(защита от запуска с пустым/фейковым промптом).
