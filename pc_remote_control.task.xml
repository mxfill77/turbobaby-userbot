<?xml version="1.0"?>
<!--
  pc_remote_control — задача Планировщика для ОСНОВНОГО канала владельца (Claude Code Remote
  Control). Стиль скопирован с существующей задачи pc_agent: вход в систему → InteractiveToken,
  без лимита времени, IgnoreNew, RestartOnFailure 3×PT1M.

  Цепочка запуска: wscript (//B, без окна) → rc_remote_control.vbs (WshShell.Run …, 0: скрытая,
  но ЖИВАЯ консоль — интерактивной сессии claude нужен TTY) → rc_supervisor.py (вечный цикл
  «claude ‑‑remote‑control turbobaby-pc» + резолв новейшей версии CLI перед каждым стартом).

  УСТАНОВЛЕНО 22.07 под именем TurboBabyRC (владелец завёл задачу этим именем; /TN
  перекрывает URI ниже, оба пути рабочие). Установка требует ПОВЫШЕННОЙ PowerShell —
  обычный токен получает «Access is denied»:
    schtasks /Create /XML "D:\turbobaby-bot\pc_remote_control.task.xml" /TN "TurboBabyRC"
    Start-ScheduledTask -TaskName TurboBabyRC
  (schtasks разбирает XML ДО проверки прав — проверено: битый файл даёт «malformed»,
  наш даёт «Access denied», значит UTF-8 он читает нормально.)

  ЭКСПЛУАТАЦИЯ: перед рестартом задачи убедись, что старая сессия мертва —
  осиротевший «claude --remote-control turbobaby-pc» переживает смерть супервизора и
  занимает имя устройства (живой прокол 22.07: первый старт задачи повис с НУЛЁМ
  TCP-соединений, пока рядом догорала прежняя сессия с тем же именем).

  Триггер — ВХОД В СИСТЕМУ (как у pc_agent), а не «загрузка ПК»: сессии нужен интерактивный
  токен пользователя, до логина его не существует.
-->
<Task version="1.3" xmlns="http://schemas.microsoft.com/windows/2004/02/mit/task">
  <RegistrationInfo>
    <URI>\pc_remote_control</URI>
    <Description>Claude Code Remote Control - osnovnoj kanal vladel'ca: wscript -> rc_remote_control.vbs (skrytaja konsol') -> rc_supervisor.py -> claude --remote-control turbobaby-pc v D:\turbobaby-bot</Description>
  </RegistrationInfo>
  <Principals>
    <Principal id="Author">
      <UserId>S-1-5-21-3727781399-3432839651-1010305476-1002</UserId>
      <LogonType>InteractiveToken</LogonType>
    </Principal>
  </Principals>
  <Settings>
    <DisallowStartIfOnBatteries>true</DisallowStartIfOnBatteries>
    <StopIfGoingOnBatteries>true</StopIfGoingOnBatteries>
    <ExecutionTimeLimit>PT0S</ExecutionTimeLimit>
    <MultipleInstancesPolicy>IgnoreNew</MultipleInstancesPolicy>
    <RestartOnFailure>
      <Count>3</Count>
      <Interval>PT1M</Interval>
    </RestartOnFailure>
    <IdleSettings>
      <Duration>PT10M</Duration>
      <WaitTimeout>PT1H</WaitTimeout>
      <StopOnIdleEnd>true</StopOnIdleEnd>
      <RestartOnIdle>false</RestartOnIdle>
    </IdleSettings>
    <UseUnifiedSchedulingEngine>true</UseUnifiedSchedulingEngine>
  </Settings>
  <Triggers>
    <LogonTrigger />
  </Triggers>
  <Actions Context="Author">
    <Exec>
      <Command>C:\Windows\System32\wscript.exe</Command>
      <Arguments>//B //Nologo "D:\turbobaby-bot\rc_remote_control.vbs"</Arguments>
      <WorkingDirectory>D:\turbobaby-bot</WorkingDirectory>
    </Exec>
  </Actions>
</Task>
