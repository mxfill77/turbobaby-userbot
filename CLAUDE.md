# CLAUDE.md — turbobaby-userbot (ПК-контур)

Репозиторий ПК-контура TurboBaby: userbot (`userbot_listen.py`), модерация
(`moderation_bot.py`/`moderation_core.py`), клиентский suggest (`suggest.py`),
ПК-оркестратор (`pc_orchestrator.py`) и агент управления (`pc_agent.py`).
Brain/Bridge живут на VPS — здесь только исполнение на ПК.

## Канон рамки и эта проекция (правило 8в-1)

**Канон рамки один, и он НЕ здесь.** Источник — инструкции проекта Штаба, копия в мозге — узел
`KB_shtab_frame`. Этот файл каноном не является: он **проекция полосы ПК** и держит ссылку на
канон, его версию и машинную часть. Пересказа рамки здесь нет — общие правила (FACT-культура,
доктрина подтверждений, экономия кругов, метрика) читаются в каноне, а не отсюда.

| что | значение |
|---|---|
| версия канона, с которой собрана проекция | **02.09.2026 #1** |
| чем прочитано | `brain_writer.read_text(name="KB_shtab_frame")` — отдельный процесс, только чтение |
| время чтения | **02.09.2026 03:45 местного (01.09.2026 20:45:09 UTC)** |
| что показало чтение | 30040 символов, sha256 `969cdf93…86612f70`, шапка `═══ РАМКА ПРОЕКТА TurboBaby — ИНСТРУКЦИИ ШТАБА (версия 02.09.2026 #1) ═══` |
| дословный снимок утверждённого текста на диске | `docs/tasks/frame-edition-20260902.txt` — тот же sha256, посимвольно равен живой копии |

**Расхождение версий обязано быть ВИДНО, и вот как его увидеть.** Проекция хранится, а канон
живёт: она отстаёт МОЛЧА и продолжает авторитетно советовать редакцией назад (замерено 02.09 —
локальная копия рамки в репозитории отстала на редакцию за сутки). Поэтому:

1. Первая строка живого узла `KB_shtab_frame` — `═══ РАМКА ПРОЕКТА TurboBaby — ИНСТРУКЦИИ ШТАБА
   (версия ДД.ММ.ГГГГ #N) ═══`. Сверь её номер с версией из таблицы выше.
2. Номера разошлись — **прав канон, а не этот файл**: всё ниже читается как справка о полосе ПК,
   а не как действующее правило рамки, и правится проекция, а не канон.
3. Прочитать не удалось (мост молчит, нет версии, нет времени чтения) — исход **НЕИЗВЕСТНО**,
   а не разрешение продолжать.

Строка «02.09.2026 #1» в таблице свежести НЕ доказывает: это запись о чтении, состоявшемся в
названную минуту, а не утверждение, что канон с тех пор не менялся. Свежесть даёт только новое
чтение.

**Мина рядом:** `docs/PROJECT_OPERATING_FRAME.md` — ХРАНИМАЯ локальная проекция от **31.08.2026**
(её собственная шапка: «local working projection, 2026-08-31»), отставшая от канона на две
редакции (01.09 #1, 02.09 #1). Файл на месте и ничего не удаляем, но рамкой он не является: о
мощности, полосах и воротах судит канон, а не он.

### Машинная часть: стандартный блок запретов (идёт ПЕРВЫМ в любом ТЗ)

Ничего не удалять, включая уборку за собой · место для временного назвать явно · пробных
скриптов в разведке не запускать · `.env` и конфиги не читать (читать код, который их читает) ·
боевых записей нет · процессы не трогать без отдельного разрешения · тесты не касаются боевой
базы, денежных путей и боевых файлов состояния. Запреты стоя́т сразу после цели и до пунктов:
длинное задание режется с хвоста — обрежется вопрос, а не защита.

### Машинная часть: адрес результата

Последней строкой ТЗ, каноном из прода. Форму маркера в прозе задания НЕ цитировать — разбор
принимает цитату за адрес. Адрес вида `brain` несёт ключ и ожидаемую подстроку, делятся ПЕРВЫМ
пробелом; подстрока обязана быть УНИКАЛЬНОЙ для этого шага (дата плюс суть, а не общее слово):
прежнего состояния узла судья не знает и позеленеет на тексте, написанном вчера.

### Машинная часть: признак сделанности

Смотрит СОДЕРЖИМОЕ, а не наличие. Первым действием исполнитель ОТКРЫВАЕТ артефакт задачи и
говорит, отвечены ли в нём вопросы задания: отвечены — назвать дату и остановиться; отвечены
частью — продолжать с недостающего. Само существование файла не останавливает ничего.

### Машинная часть: разведение по полосам

Чтение сервера — на серверную полосу. Правка кода ПК, тренажёр и дети — на полосу ПК. Срочная
разведка, живые таблицы, реестр моста и длинные тексты — в Dispatch, мимо очереди.

## Модель и мышление (дефолт репозитория)

Дефолт всех сессий этого репо задан в `.claude/settings.json`: модель
`claude-opus-5`, `effortLevel: xhigh`, `alwaysThinkingEnabled: true`,
`env.MAX_THINKING_TOKENS = 31999`. **Каждая задача — ultrathink**: думай на полный
бюджет, а не «по-быстрому».

**Решение владельца 30.07.2026: `claude-opus-5` — единственная голова обеих полос.**
Основная — `claude-opus-5`, запасная — `claude-opus-4-8` (полными идентификаторами:
короткий алиас даёт HTTP 404, класс #194). Так на ПК (`THINKER_MODEL`,
`EXECUTOR_MODEL`) и на VPS (`ORCH_MODEL`, `ORCH_MODEL_FALLBACK`, `EXECUTOR_MODEL`).
Прежняя голова снята с работы: её имена нормализатор моделей уводит на
`claude-opus-5`, чтобы застрявшее в чьём-то окружении старое значение не вернуло её
мимо `.env`. Клиентский `suggest` живёт на своей паре (`SUGGEST_MODEL=sonnet`) — это
отдельный контур, его не трогаем.

**Какая ручка что переключает (проверено по коду 31.07.2026).** Список выше читается как
«четыре env-ручки»; на ПК это не так, и правка «не той» ручки не переключает ничего:

| ручка | ПК | VPS |
|---|---|---|
| `THINKER_MODEL`, `THINKER_FALLBACK` | **настоящие env-ручки** думателя (`pc_orchestrator.py` → `THINKER_MODEL`/`THINKER_FALLBACK`, оба через нормализатор в полный id) | своя пара |
| `EXECUTOR_MODEL` | **НЕ ручка — константа в коде** (`pc_orchestrator.py` → `EXECUTOR_MODEL = "claude-opus-5"`). Env не читается сознательно: комментарий рядом — «застрявший `.env` не смеет молча переключить модель, класс #194». Смена модели исполнителя = правка строки + коммит, self-update перезапустит демон | env-ручка |
| `ORCH_MODEL`, `ORCH_MODEL_FALLBACK`, `EXECUTOR_EFFORT` | **имена VPS-полосы, ПК-код их не читает вовсе** | настоящие ручки |

Отсюда два ложных диагноза, которых надо избегать:
- «поправил `EXECUTOR_MODEL` в конфиге» — на ПК не переключит ничего, а греп по конфигу даст
  ложное подтверждение «настроено». Ключа там и нет (сверка-2 §1 V3).
- «отказ основной головы подхватит запасная» — **у ПК-исполнителя ветки фолбэка нет**:
  `--fallback-model` есть только у думателя (`pc_orchestrator.py` → `_thinker_exec`), в argv
  исполнителя его нет. Для headless-исполнителя отказ головы = провал задачи, а не фолбэк.

Ручка **модели** на ПК — это НЕ `.claude/settings.json`: оттуда headless-полоса берёт только
`effortLevel` и `MAX_THINKING_TOKENS` (`pc_orchestrator.py` → `repo_thinking_settings`).
Поле `model` в settings.json правит интерактивные сессии, а не демона.

## Ритуал журнала (ОБЯЗАТЕЛЬНО)

Само требование «каждая сессия финальным шагом пишет итог в журнал» и FACT-культура отчёта —
канон (§6), здесь их пересказа нет. Машина полосы: журнал ПК зовётся `cowork_log`, писатель —
`cowork_log_append.py`, строка идёт в самом конце (после коммита/пуша, с локальным временем и,
если был коммит, коротким хешем), развилка с выбором человека — с префиксом `ASK` вместо `DONE`.
Сессия без итогового FACT-отчёта в самой сессии — **дефект**, а не «молча всё хорошо».

```
python cowork_log_append.py "DONE Dispatch <время>: <что сделано>"
```

**Что ЛОЖИТСЯ в мозг на самом деле (проверено по `cowork_log_append.py` → `stamp_line`, 31.07).**
Писатель сам штампует дату **сразу после типа**, поэтому строка в мозге выглядит НЕ так, как
образец выше. Это важно не для красоты, а для поиска:

| что пишешь | что ложится в мозг |
|---|---|
| `DONE Dispatch 14:20: …` | `DONE <дата время> UTC: Dispatch 14:20: …` |
| `DONE RC 2026-07-31 14:20: …` | `DONE <дата время> UTC: RC 2026-07-31 14:20: …` |
| `ARTIFACT тема → docs/artifacts/…` | `DONE <дата время> UTC: ARTIFACT тема → …` |

Следствия, каждое — готовый ложный диагноз:
- **Искать RC-итоги по `^DONE RC` бесполезно** — совпадений ноль всегда, и это НЕ значит,
  что RC-сессии не пишут журнал. Ищи `RC` в теле строки.
- **`ARTIFACT` типом записи не является.** Опознаваемые типы — ровно
  `DONE|NOTE|ASK|PLAN|BLOCKED|WAITING|SKIPPED` (`cowork_log_append.py` → `LOG_TYPES`);
  всё прочее получает тип `DONE`. Поэтому ритуал «ARTIFACT, потом DONE» выглядит в мозге как
  **два DONE**, а статистика по типам это не различает.
- Приведение формата к коду (или кода к формату) — правка кода, здесь не сделана;
  записано в остатки сверки.

**«Журнал = индекс» — канон (§ПАМЯТЬ), и на полосе ПК это ещё и поведение кода:** строка
длиннее **600 символов** (`cowork_log_append.py` → `LINE_MAX`) автоматически
раскладывается писателем на две части — тело уходит в
`docs/artifacts/journal/ГГГГ-ММ-ДД-ЧЧММСС-<тип>.md`, а в мозг ложится голова, обрезанная по
границе фразы, плюс путь и полная длина:

```
DONE <штамп>: <итог> … → docs/artifacts/journal/2026-07-31-091200-done.md (полный текст 2785 симв.)
```

Порог не назначен, а **измерен** (81 правильная строка даёт максимум 588 симв.; замер и эффект —
[`docs/artifacts/2026-07-31-journal-index-threshold.md`](docs/artifacts/2026-07-31-journal-index-threshold.md)).
Следствия для сессии: **пиши итог сам** — авто-вынос это страховка, а не замена смысла (машина
режет по границе фразы, но не знает, что в отчёте главное); файл-тело **не попадает в git сам**
(untracked) — нужное коммить руками; если вынос сорвался (нет доступа к диску), писатель
СОЗНАТЕЛЬНО отдаёт строку в мозг целиком: потерянная запись хуже длинной.

**Про таймаут вызова.** Внутренний бюджет писателя больше 30с: два запроса (чтение+запись),
на каждом свой сокет-таймаут, плюс повтор на транспортных ошибках
(`cowork_log_append.py` → `RETRY_TRIES`, `with_retry`). **Внешний `timeout 30` на ручном вызове
опасен**: он убивает процесс ДО ветки `except`, и строка не уходит ни в мозг, ни в спул —
тихая потеря записи при честном рапорте «журнал повис». Если ограничиваешь время — бери
запас (≥150с) либо не ограничивай. К автоматике это не относится: `SessionEnd`-хук
(`dispatch_notify.py` → `_cowork`) запускает запись **отдельным процессом**, внешний таймаут
к ней неприменим.

Если `cowork_log` действительно повис — так и скажи («cowork_log повис, DONE запишем позже»),
не блокируйся на нём. И **не повторяй запись вслепую**: known-класс — ложный отказ при уже
легшей строке, слепой повтор даёт дубль в журнале.

Автоматика этот ритуал **страхует, но не заменяет**: хук `SessionEnd` шлёт карточку
«✅ Code-сессия завершена: <сводка>» в **тему постановки задач 328** (инбокс 1160 и личка —
фолбэки) и сам дописывает строку в `cowork_log` (`dispatch_notify.py --hook session_end`).
Автокарточка — это хвост-страховка на случай обрыва; развёрнутый FACT-отчёт по пунктам
всё равно за сессией.

**Адрес карточки выбирает ОДИН признак — ждёт ли сообщение ОТВЕТА владельца**
(`dispatch_notify.deliver` → `awaits_reply`, правило владельца 10.08.2026). Ждёт → инбокс
1160; не ждёт (итог сессии, сводка, «задача выполнена») → тема постановки 328. Вид сообщения
адресом не распоряжается: перечень видов протухает на первом новом виде — именно так итог
сессии 175 раз из 184 занимал инбокс собой. Замок: карточка **с кнопкой** и **высшая карточка
§7** попадают в 1160 ВСЕГДА, мимо признака и переживая его поломку. `--topic <id>` — это
адрес, названный вызывающим, а не маршрут. Разбор и числа:
[`docs/artifacts/2026-08-10-inbox-awaits-reply-route.md`](docs/artifacts/2026-08-10-inbox-awaits-reply-route.md).

**RC-сессия (канал с телефона, задача Планировщика `TurboBabyRC`) набирает свой вид строки** —
`DONE RC <дата> <время>: <что сделано, с фактами и хешами>`; в мозг она ляжет со штампом писателя
между `DONE` и `RC` (см. таблицу выше). Когда RC-работа шла на VPS — ещё и `cclog … --pulse` там,
двойная фиксация ПК+VPS. Шаблоны и детали — [`docs/RC_BRAIN_BRIDGE.md`](docs/RC_BRAIN_BRIDGE.md).

## Мины в рабочем дереве: почему `grep -rn` по репо врёт (заведено 31.07.2026)

В дереве лежат файлы, которых **нет в git**, но которые греп находит наравне с боевыми.
Каждый из них уже стоил ложного диагноза — проверяй принадлежность, а не только совпадение.

| что лежит | чем опасно |
|---|---|
| **`manager-bot/`** — снимок VPS-кода от 01–02.06.2026 (`bot.py`, `splinter.py`, `bridge_client.py`, свой `.git`, свой `venv`), под `.gitignore`, `git ls-files manager-bot` → **0** | 1) `grep -rn` молча смешивает две реальности: имя `BridgeClient` живёт ТОЛЬКО там (в ПК-дирижёре класс называется `Bridge`) — грепающий чинит чужой июньский код, думая, что смотрит ПК-дирижёра; 2) **боевой `suggest.py` ходит туда за тремя фолбэками** — `LOCAL_FAQ`, `PARK_LIST_FILE`, `PLAYBOOK_FILE`: при падении Bridge клиент получит июньский срез FAQ и парка как живой, и это не будет видно нигде |
| ↳ **`manager-bot/docs/playbook.md` — не снимок, а ЖИВОЕ состояние вне git** (поправка 31.07, замер mtime) | FAQ и парк действительно лежат от **02.06**, а playbook — от **23.07**: в него **пишет боевой `suggest.py`** (`append_playbook_rule`, раздел «## Выученные правила», открытие на `"w"` в трёх местах). Отсюда два ложных диагноза: (а) «в `manager-bot/` всё июньское» — playbook свежий и меняется сам; (б) «каталог — мусор, снести» — снос уничтожит **накопленные выученные правила**, которых нет ни в git, ни в мозге. Каталог сносить НЕЛЬЗЯ |
| **`docs/artifacts/2026-07-23-guard-v3-files/pretool_guard.py`** — копия гарда на 23.07, **872 строки** против живого, выросшего почти втрое (мерить `wc -l pretool_guard.py`, число растёт каждую неделю) | греп по гарду выдаёт две версии; вывод «правила X в гарде нет» получается из архивной копии. Материальная причина класса «чинишь не тот гард» |
| `.claude/settings.json.new`, `.claude/settings.json.bak-2026-07-23` | обе держат снятую голову в поле `model`: `grep '"model"' .claude/` даёт три ответа, два из них — неработающая модель |
| экспорты Планировщика `*.xml` в `docs/artifacts/` | они в **UTF-16**: `grep` по ним молча возвращает **ноль**. «Не нашёл» здесь значит «не та кодировка», а не «этого нет». Читать `Get-Content -Raw` |

**Правило:** прежде чем строить вывод на «grep → 0» или «grep нашёл», ответь, **в каком дереве**
ты искал. Формулировка «в репо этого нет» без указания полосы, дерева и даты замера — это
класс born-false: ровно так родились неверными два документа (`dec_port_spec.md` и
`task-2026-07-20-questions-filter.md`, второй — в том же коммите, что внёс отрицаемые строки).

## Правило артефактов (решения владельца)

«Число без артефакта не существует» и «артефакт пишется последним действием» — канон (§6, §8в).
Машина полосы: файл кладётся в `docs/artifacts/ГГГГ-ММ-ДД-<тема>.md` и фиксируется строкой

```
python cowork_log_append.py "ARTIFACT <тема> → docs/artifacts/<файл>: <суть в 10 слов>"
```

`ARTIFACT` — слово ритуала, а НЕ тип записи: в мозг строка ляжет как `DONE <штамп>: ARTIFACT …`
(см. таблицу типов выше). Правило «финальный DONE ссылается на артефакт ПУТЁМ» проверяющего в
коде не имеет — нарушение молчаливо, держи его сам.

## Запись в мозг (Brain-доки) с ПК — ТОЛЬКО через brain_writer

Единственный легальный канал записи в Brain-доки с ПК — доверенный писатель
`brain_writer.py` (образец — VPS-cclog): секреты Bridge берёт САМ процесс писателя,
агентские скрипты их **не читают и в коде не видят**. Интерфейс:

```
venv/Scripts/python.exe brain_writer.py (--id <file id> | --name <док>) "строка"   # CLI
brain_writer.append(text, doc_id=…|name=…, [anchor=…, place=…, require_above=…])   # функция
brain_writer.apply(mutate, …)   # движок для одноразовых скриптов со СВОЕЙ якорной логикой
```

Встроено и неотключаемо: бэкап старого текста в `tmp/brain_backup_*.txt` ДО записи,
якорная вставка (якорь обязан найтись ровно 1 раз), идемпотентность, ОБРАТНОЕ ЧТЕНИЕ
с дословной сверкой. Гард ПК держит канал зелёным ПО ИМЕНИ модуля (`_RE_SAFE_SCRIPTS`),
содержимое не сканируется.

**ЗАПРЕЩЕНО** (класс 328, «утренний ASK про пульс»): одноразовые скрипты в `tmp/` (и где
угодно), которые САМИ читают конфиг с секретами (load_env/BRIDGE_TOKEN) ради записи в мозг, —
это красный канал по гарду и потерянное время владельца. Нужна запись — импортируй
`brain_writer` или зови его CLI; в скрипте живёт только строковая логика (якоря, склейки,
текст блока). Журнальная строка DONE/ASK — по-прежнему через `cowork_log_append.py`.

## Форматы, на которых полоса ПК уже обожглась

Правило «проверка обязана повторять живой формат запуска» живёт в каноне (§6) и здесь не
пересказывается. Своё у полосы — сами форматы: из головы они не восстанавливаются, а мок,
разошедшийся с ними, зеленеет молча.

- **Зоны доставки — позиционный список, а не dict.** Живой `delivery_zones_get` отдаёт
  `["Раваи", 7.771, 98.327, 590, 4]` = `[name, lat, lon, price, radius]`; dict-only резолвер
  ронял ВСЕ 16 зон → ложный `[уточнить]`. Голдены класть на снятую read-only пробой фикстуру
  `fixtures/delivery_zones_get.live.json`.
- **Короткая ссылка отдаёт `302 Found` + `Location`, и `_NoRedirectHandler` поднимает `HTTPError`
  на `opener.open`** (а не возвращает 3xx). Мок, звавший `_expand` мимо сети, это прятал —
  продовый `_default_expand` падал на первом 302. Редирект мокать через `HTTPError` с реальным
  `Location`, а не в обход.
- **Голдены детекта — дословные фразы клиента из живого провала.** Юнит на «прайс»/«все модели»
  был зелёным, а живая фраза «Какие марки и модели… какие цены на аренду…» давала False. К фиксу
  детекта — реальная фраза плюс 4–5 парафраз RU/EN в позитивы и контрпримеры (одна модель /
  приветствие / только депозит) в негативы.
- Подогнать синтетическую зону под пин ради «красивого» числа — тот же анти-паттерн: голден врёт.

## Зеркало правила с чужой полосы: посылку меряют ЗДЕСЬ (заведено 04.08.2026)

Канон уже требует судить по живой посылке и проверять, не убил ли наш фикс посылку соседнего
места (§6, §8г). Остаток полосы ПК — три вопроса перед любым зеркалом с VPS, все с ЧИСЛОМ в
ответе: на каком факте правило стои́т там и жив ли этот факт здесь; не закрыт ли класс здесь
раньше и другим устройством (тогда вклад зеркала — ноль, и честный «ноль» дороже красивого
«снято N»); что здесь есть такого, чего там нет (у ПК это `unknown`/`unknown_tool` — имена,
которыми гард зовёт СОБСТВЕННОЕ незнание; у сервера в `_ACTIONS` таких нет вовсе).

Живой случай с числами — дежурный по карточкам: все четыре условия дали на ПК ноль, а слепая
копия заглушила бы карточку на снос всех python машины.
[`docs/artifacts/2026-08-04-card-duty-pc-mirror.md`](docs/artifacts/2026-08-04-card-duty-pc-mirror.md).

## Слой ожиданий полосы ПК — О1, О2 и О3 (заведён 11.08.2026, зеркало сервера)

Система спрашивает, что ДОЛЖНО было произойти и не произошло. До 11.08 слоя на ПК не было вовсе
(перепись 467: 287 цепочек, 42 оборвал контур, ожидание заметило бы 0 — потому что механизма нет).
Решение — `expectations_pc.py` (чистая функция «факты → вердикт»), руки — `expectations_pc_run.py`,
регресс и инвариант `EXPECT_PC_PURE` — `test_expectations_pc.py` (49 проверок, в гейте).

| ожидание | предмет | порог | чем обоснован (замер СВОЕЙ полосы 22.07–11.08) |
|---|---|---|---|
| **О1**·`new`/`approved` | строку не берут при СВОБОДНОМ исполнителе | **45 мин** ЧИСТОГО ожидания | 139 пар «создана → взята»: медиана 1.8 · p90 7.2 · max законного 19.7 мин. Контрфакт без условия занятости: 30 мин → 16 флагов, 15 мин → 35 |
| **О1**·`in_progress` | строка замерла в работе | **100 мин** | окно `CLAIM→COMPLETE`: 112 окон, max законного 2712с; потолок законного = 2 попытки × 2700 + хвост = 5412с; реапер демона 5400с |
| **О2** | демон даёт ОБОРОТ `poll_once`, а не просто существует | **20 мин** тишины **в часах бодрствования** | 992 паузы витка: медиана 172с · p95 305с · max законной 605с. Порог 1200с → 0 эпизодов за 20 суток |
| **О3** | модербот ДЕЛАЕТ РАБОТУ (запись его тика в `moderation_ipc`), а не просто жив процессом | **15 мин** тишины **бодрствования** | собственные паузы тика: 849 пауз лога — медиана 5.000с, max законной **5.022с**; 26 промахов планировщика за 39 суток — худший 4.97с; живая проба продукта 11.08 — 189 шагов, max 5.02с; сам контур зовёт его мёртвым через 15с |
| **О4** (14.08) | полоса оставляет наружу СЛЕД ЖИЗНИ, а не только след работы. НЕ СУДИТ, а ПРОИЗВОДИТ: пульс в журнал при ДОКАЗАННОМ обороте | **6 ч** тишины журнала (пол повтора 30 мин) | порог серверного О4 измерен: последняя строка 13.08 13:26 UTC → заметка 05:30 UTC = **16 ч 04**; два окна пульса обязаны уместиться в него (16/6=2.67). Корпус 452 следа за 11.1 сут: **4 эпизода О4, ложных 4 из 4**; при пульсе остаётся **0** ценой 1.26 строки/сут |

Четыре вещи, которые здесь легко перепутать, и каждая уже стоила бы ложного диагноза:

1. **Порог О2 нельзя брать по возрасту heartbeat.** ПК спит, и стенной возраст после сна равен сну.
   Тишина мерится `QueryUnbiasedInterruptTime` (сон в неё не идёт), накоплением от наблюдения к
   наблюдению. Часов нет → исход **«неизвестно»**, а не «жив».
2. **Идущий заход — работа, а не остановка.** Демон исполняет `claude -p` СИНХРОННО внутри
   `poll_once`, а heartbeat пишет ПОСЛЕДНЕЙ строкой (`pc_orchestrator.py:3500`), поэтому любой
   заход выглядит остановкой. Замок: штамп `pc_orchestrator.task_started.json` оправдывает тишину,
   но не дольше `TASK_TIMEOUT` + хвост (55 мин). Контрфакт БЕЗ замка на том же корпусе — **25
   ложных заметок за 20 суток** (на сервере тот же класс дал 10 ложных из 10).
3. **Третий исход обязателен.** «Проверить невозможно» → «неизвестно», и оно НЕ закрывает открытый
   эпизод: молчание источника выздоровлением не является. Ни одна ветка не превращает «не смог
   проверить» в «проверено и хорошо».
4. **Наблюдатель не живёт на том, за чем следит.** Все сторожа очереди ПК (реапер одиночек, реапер
   сирот, TTL подтверждения, сторож цепей) живут ВНУТРИ `poll_once` — вставший демон не судит себя
   ни одной из них. То же и с модерботом: единственный его сторож (контур-вотчдог) живёт там же,
   и в живом случае 05.08 09:51:48 → 06.08 19:44:10 модербот лежал **33.9 часа** молча ровно
   потому, что стоял сам демон. Наблюдатель ожиданий стои́т ВНЕ обоих.
5. **У О3 «жив» и «работает» — разные слова, и слой никогда не говорит первого.** Предмет — ПРОДУКТ
   пятисекундного тика (`mtime moderation_ipc.db`, `os.stat`, базу не открываем). Файл ОБЩИЙ (в него
   пишут userbot и тренажёр), поэтому свежесть засчитывается за работу только при ДОКАЗАННОМ
   авторстве (процесс из лока есть И запущен тогда же, когда лок написан — против переиспользования
   PID). Автор опровергнут → чужая запись счётчик тишины не обнуляет; не подтверждён → «неизвестно».
   Проба процесса — `OpenProcess(QUERY_LIMITED_INFORMATION)`; `os.kill(pid,0)` на Windows зовёт
   TerminateProcess и запрещён здесь навсегда. Разбор:
   [`docs/artifacts/2026-08-11-expectations-pc-o3-moderbot.md`](docs/artifacts/2026-08-11-expectations-pc-o3-moderbot.md).

**НАБЛЮДАТЕЛЬ В ПРОДЕ УСТАНОВЛЕН (поправка 11.08.2026, замер).** Строка «не установлен» ниже
описывала состояние на час заведения слоя и протухла в тот же день: задача `TurboBabyExpectPC`
жива и тикает каждые 10 минут — доказательство не на слово, а файлом состояния
`tmp/expect_pc/state.json` (два последовательных тика замером 11.08: 15:55:25 → 16:04:50). Новых ожиданий
это ставить не мешает: **О3 поехал на уже стоящем наблюдателе, командная строка задачи не
менялась, красных операций не потребовалось.** Ставилась она так (владельцем, из-под
администратора — `schtasks` красная):

```
schtasks /Create /TN TurboBabyExpectPC /SC MINUTE /MO 10 /RL HIGHEST /F /TR "D:\turbobaby-bot\venv\Scripts\python.exe D:\turbobaby-bot\expectations_pc_run.py"
```

(одной строкой намеренно: перенос через `^` живёт только в cmd.exe и в PowerShell ломает вызов.)

До установки ожидания живут только ручным прогоном (`--status` / `--dry`), и у него есть названная
ловушка: сухой прогон состояние НЕ ПИШЕТ, а тишина О2 копится от наблюдения к наблюдению — значит
на ручных вызовах О2 при отставшем heartbeat всегда честно отвечает «неизвестно». **Откат:** порог
ветки в ноль (`EXPECT_PC_NEW_MIN` / `EXPECT_PC_RUN_MIN` / `EXPECT_PC_TURN_MIN`) — ветка мертва
целиком; полностью — снять задачу Планировщика.

**Пятая мина — у О4, и она обратная остальным: ошибка здесь МОЛЧАЛИВА.** Пульс от залипшего
процесса был бы ложным зелёным навсегда, поэтому ветка «тишина оправдана объявленным заходом»
законна для О2 и **запрещена для О4**: штамп CLAIM доказывает, что демон объявил заход, а не что
он замкнул виток. Heartbeat годится фактом только потому, что `_write_heartbeat()` зовётся РОВНО
ОДИН РАЗ и ПОСЛЕДНЕЙ строкой `poll_once` (`pc_orchestrator.py:3608`), а потоков в демоне нет
вовсе (`threading`/`Thread(` → 0 совпадений). Молчание пульса — это адресованная СЕРВЕРУ новость
«ПК не даёт оборота», а не тишина по бездействию; права сказать «ПК мёртв» у полосы нет ни одной
строкой. Разбор и числа:
[`docs/artifacts/2026-08-14-o4-life-trace-pc.md`](docs/artifacts/2026-08-14-o4-life-trace-pc.md).

**Что слой НЕ покрывает (честно):** смерть самой машины судит по-прежнему СЕРВЕРНОЕ О4 —
наблюдатель живёт на ПК, и выключенный ПК не наблюдает никто; наш вклад — положить под тот
приговор ФАКТ (пульс жизни), которого до 14.08 не было вовсе. Дальше: молчание моста (снимка нет → О1 честно молчит;
предмет серверного О5), `needs_approval` (ждёт живого владельца — не судим), клиентский контур.
Задач слой НЕ СТАВИТ и процессов НЕ ПЕРЕЗАПУСКАЕТ ни одной веткой — в отличие от серверного
оригинала, где сильная ветка есть.

Числа реплея живым кодом (что заметили бы задним числом по корпусу переписи 467): **О1 — 3 из 42**
(id 61, 291, 353; раньше живого терминала на 3 мин, 28 мин и 8.3 ч), ложных 0; **О2 — 0 из 42**,
ложных 0. Разбор:
[`docs/artifacts/2026-08-11-expectations-pc-o1-o2.md`](docs/artifacts/2026-08-11-expectations-pc-o1-o2.md).

## Слепок очереди полосы ПК в мозге — `queue_state_pc` (заведён 14.08.2026, зеркало класса 539)

Штаб читает мозг САМ, а состояния очереди там не было: между «взял» и «сдал» пустота, из которой
на полосе VPS родились четыре дубля за трое суток. Слепок кладёт `queue_snapshot_pc.py`; демон
зовёт его отдельным процессом из ДВУХ точек — сразу после claim (`process_new`) и перед
`_write_heartbeat()` в конце `poll_once`.

**Четыре вещи, каждая — готовый ложный диагноз:**

1. **`queue_state` — док СЕРВЕРА, ПК пишет в `queue_state_pc`.** Ключи разные, доки разные;
   правка серверного с этой полосы запрещена. Серверный к тому же показывает обе полосы, наш —
   только свою.
2. **Пишется ПО СМЕНЕ СОСТОЯНИЯ, и «сегодня записей нет» ≠ «сломалось».** Подпись смены не
   содержит возрастов и штампов СОЗНАТЕЛЬНО (иначе запись каждый тик). При темпе полосы
   4.3 закрытых/сут это ≈13–17 записей в сутки против 1440 при записи каждый оборот.
3. **«Отклонено владельцем» — НЕ статус очереди, а МАРКЕР в причине** (замер 15.08:
   `get_pending("rejected")` → `ok=True, items=0`; отказ лежит в `failed` с префиксом
   `отклонено Филиппом`, и таких **23 строки из 55** живых `failed` полосы). Отсюда два вывода:
   искать отказы по статусу бесполезно — ветка будет вечно пустой при живом классе; и до 15.08
   слепок звал решение человека сбоем, то есть приглашал переотправить отказанное. Теперь у отказа
   свой разрез со словами «переотправке не подлежит», маркер сверяется тестом с литералом
   `pc_orchestrator._REJECT_PREFIX`, а записей в сутки это не добавило ни одной (исход поменял имя,
   а не число смен). Approval-TTL («подтверждение не получено») отказом НЕ считается — владелец
   ничего не решал. Разбор:
   [`docs/artifacts/2026-08-15-queue-snapshot-pc-rejected.md`](docs/artifacts/2026-08-15-queue-snapshot-pc-rejected.md).
4. **Слепок НЕ судья жизни полосы.** Писатель живёт В демоне (наблюдатель ПК под запретом, своя
   задача Планировщика — красная операция), поэтому замерший демон замораживает слепок. Смерть
   демона судят О2/О4, живущие вне. Замок от вранья свежестью: промах моста короче 15 мин не
   пишет НИЧЕГО, длиннее — меняет шапку на «НЕ СВЕРЕНО» с датой последнего верного; свежего
   `снято:` при несверенном чтении не бывает ни на одной дороге.

Мина импорта: `TURBOBABY_TEST_LOGS=1` нужен на время одалживания клиента моста у демона, но ТОТ
ЖЕ флаг заставляет `brain_writer` отказать в живой записи — поэтому он живёт ровно один импорт
(`_guard_test_logs`). Откат: `QSNAP_PC_OFF=1`. Разбор, замеры и числа:
[`docs/artifacts/2026-08-14-queue-snapshot-pc.md`](docs/artifacts/2026-08-14-queue-snapshot-pc.md).

## Свод 25.07: что осталось полосе после канона

Девять правил свода 25.07 целиком живут в каноне рамки (§6 FACT-культура, §7 доктрина
подтверждений, §8а мощность, §8в экономия кругов) — их пересказа здесь больше нет. Полный разбор
с живыми инцидентами 23–25.07 остаётся в [`docs/ENV_PLAYBOOK.md`](docs/ENV_PLAYBOOK.md) (файл
одинаков в ОБОИХ репозиториях). Своим у полосы ПК остаётся ровно это:

- **Перечни классов гарда — [`docs/guard-classes.md`](docs/guard-classes.md)**: три списка разной
  длины, и «да» на класс снимает карточку ТОЛЬКО этому классу (одобренный `env` не открывает
  `Read` секретов — у него класс `read_secret`).
- **Вывод гейта НИКОГДА не захватывать в `$( )`.**
- **Клиент (RC/Desktop) перекрывает `effortLevel` репозитория** — уровень усилий сессии задаёт он,
  а не `.claude/settings.json`; замер 24.07: слово в тексте задания не меняет усилие вовсе.
- **Боевые токены в тексте журнальной записи называть СЛОВАМИ, а не цитировать**: поверх нашего
  гарда работает второй, внешний слой защиты, которого в репозитории нет.
